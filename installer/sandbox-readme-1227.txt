========================================
 히트판 ERP 종단 설치 테스트 (v1.2.27)
 목표: 터널 경유로 setup→부모계정(아이디방식)→로그인→6단계 종단 통과
========================================

이 창은 Windows 샌드박스(깨끗한 백지 가상 PC) = 실고객 PC입니다.

※ 이번 봉합(1.2.27): 부모계정 = 아이디 방식으로 전환(헌법 #40).
  랜딩은 비번 안 받음. 설치마법사에서 아이디+비밀번호를 직접 등록.
  본사(랜딩·백오피스)는 부모계정 비번을 저장하지 않음.
  + create-parent 토큰만료 면제(비번 고르다 10분 넘겨도 저장됨).
  + 아이디 로그인 400 차단 제거([EmailAddress] 삭제).

※ 개발 PC·localhost 정상은 의미 없음. 반드시 터널 주소(test1.hitpan.kr)로 검증.

────────────────────────────────────────
[1] 설치 EXE 실행
────────────────────────────────────────
  C:\hitpan-test\HitPan-ERP-Setup-1.2.27.exe
  → 우클릭 → 관리자 권한으로 실행
  (MariaDB·.NET·cloudflared·빈 DB 124테이블 자동 설치)

────────────────────────────────────────
[2] ★핵심★ 터널 주소로 setup 화면 뜨나
────────────────────────────────────────
  설치 후 브라우저 → https://test1.hitpan.kr
  → 로그인 화면 뜨면 주소창에 직접:
     https://test1.hitpan.kr/setup/license

  이 화면이 떠야 정상. 안 뜨면 = 즉시 PM에게 (화면·F12 콘솔 캡처)

  ※ 반드시 test1.hitpan.kr 로. localhost:5234 는 로컬 전용이라 의미 없음.

────────────────────────────────────────
[3] 한 벽씩 종단 (막히면 그 자리에서 멈추고 알려주세요)
────────────────────────────────────────
  ① 시리얼 HITP-4ZQK-NEVE-2MCJ-WDJT + 사업자번호 → 라이선스 검증
  ② 회사정보(대표자명 등) → 다음
  ③ ★부모계정 = 아이디 + 비밀번호(8자+)★ → 부모 계정 생성
     ← 아이디는 이메일 형식이 아니어도 됨(예: hitpan_admin)
     ← "생성됨" 나오면 아래 cmd로 DB 확인
  ④ 로그인 (방금 만든 아이디 + 비번) → 200 되나
  ⑤ 로그인 후 6단계 메뉴(설정/마스터/매입/판매/현황/재무) 뜨나
     각 메뉴 눌러서 오류·빈화면 없나

  ①~⑤ 각 단계에서 막히면 = 그게 다음 진범. 화면·에러 캡처해서 PM에게.
  "정상처럼 보이는데 실제 안 됨"도 반드시 알려주세요.

────────────────────────────────────────
[부모계정 실제 저장 확인 — 관리자 cmd]
────────────────────────────────────────
  "C:\Program Files\MariaDB 11.4\bin\mysql.exe" -u root hitpan_erp_t001 -e "SELECT email,is_active FROM users;"
  → 방금 만든 아이디가 email 컬럼에 나오면 저장 성공(아이디를 email 컬럼에 재사용).

  ①~⑤ 다 통과 = 터널 경유 종단 첫 통과 = 진짜 검증 완료!
