@page "/platform/revenue"
@inject NavigationManager Nav

@*
    레거시 경로 → 결제·환불 리다이렉트 (사장님 결재 2026-06-05)
    실제 작동 페이지: /admin/billing (PG 거래 추적)
*@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/admin/billing", replace: true);
}
