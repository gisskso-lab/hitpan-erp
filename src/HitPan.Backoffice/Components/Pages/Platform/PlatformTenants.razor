@page "/platform/tenants"
@inject NavigationManager Nav

@*
    레거시 경로 → V2 리다이렉트 (사장님 결재 2026-06-05, 헌법 #35 정합)
    실제 작동 페이지: /admin/tenants (AdminTenantsV2.razor)
*@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/admin/tenants", replace: true);
}
