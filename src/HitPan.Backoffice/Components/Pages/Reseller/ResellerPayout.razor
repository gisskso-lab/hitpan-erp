@page "/reseller/payout"
@inject NavigationManager Nav

@*
    레거시 경로 → 정산 내역 통합 (사장님 결재 2026-06-05)
    ResellerSettlement과 의미 중복 → 단일 화면으로 통합
*@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/reseller/settlement", replace: true);
}
