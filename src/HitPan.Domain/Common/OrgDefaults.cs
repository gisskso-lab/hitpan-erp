namespace HitPan.Domain.Common;

/// <summary>
/// 조직(직급·부서) 기본값. 작(2026-08-14).
/// </summary>
/// <remarks>
/// <para>
/// 🔴 <b>여기 있는 것은 "우리가 정하는 조직도"가 아니다.</b> 헌법 #11 대로 직급 체계는
/// 고객사 어드민이 직접 만들고 고친다. 이 파일이 담는 것은 <b>부모계정 자신의 직급</b>
/// 하나뿐이다 — 가입한 사람은 정의상 그 회사의 대표이기 때문이다.
/// </para>
/// <para>
/// <b>왜 상수로 빼나</b> — 부모계정의 사원 행을 만드는 자리가 <b>둘</b>이다:
/// 신규설치 프로비저닝(<c>CompanyBootstrapProvisioner</c>)과
/// 로그인 시 백필(<c>AuthService</c>). 두 곳에 문자열을 따로 적으면
/// <b>한쪽만 고쳐 서로 달라진다</b> — 실제로 종전엔 두 곳 다 직급을 안 넣어
/// 부모계정 직급이 NULL 이었다.
/// </para>
/// </remarks>
public static class OrgDefaults
{
    /// <summary>
    /// 부모계정(고객사 대표)의 직급 표시명. 사장님 지시(2026-08-14):
    /// <i>"부모계정 = 직급은 자동으로 대표.등록"</i>
    /// </summary>
    /// <remarks>
    /// ⚠️ <b>직급 마스터(<c>positions</c>)에 이미 있는 이름과 같아야 한다.</b>
    /// 시드는 <c>CEO / 대표이사 / sort_order 100</c> 이다.
    /// 여기에 마스터에 없는 이름(예: "대표")을 적으면 사원 행에는 그 글자가 남지만
    /// <b>직급 목록에 없어 결재선에서 고를 수 없다</b> — 이름이 갈리는 순간
    /// "대표"와 "대표이사"가 둘 다 생겨 고객이 어느 쪽인지 모르게 된다
    /// (사장님 결재 2026-08-14: 기존 "대표이사" 를 쓴다).
    /// </remarks>
    public const string OwnerPositionName = "대표이사";
}
