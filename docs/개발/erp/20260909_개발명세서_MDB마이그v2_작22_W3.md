# 개발명세서 — MDB 마이그 v2 · 작22(W3): 계산서 · 정답식 · DOCME · 두 모드 · 1/2단계 · 체크포인트 · 찾아보기

| 항목 | 내용 |
|---|---|
| 작업지시서(PRD) | [`20260909작22_MDB마이그v2_두모드_계산서_DOCME_작업지시서.md`](../../운영기록/20260909작22_MDB마이그v2_두모드_계산서_DOCME_작업지시서.md) · 설계 [별지 W3](../../설계/erp/20260909_설계보강_MDB마이그v2_작22_W3.md) · 선행검증 [20260909검1](../../검증/선행/20260909_선행검증서_MDB마이그v2_작22_8항목_전제_실측.md) |
| 구현 | 갈래 C·D·A+B 서브에이전트 3(worktree 분리) + PM 브라운킴([3-V] 봉합 1건) |
| 브랜치 | `feat/20260909-w22` ← `-c`(`be211284`) · `-d`(`24b04e0e`) · `-a` 머지 · [3-V] `54cee6da` |
| 빌드·시험 | `dotnet build src/HitPan.sln -c Release -p:TreatWarningsAsErrors=true` **경고 0 오류 0** · `dotnet test src/HitPan.Tests -c Release --no-build` **1,347/1,347 통과 · 건너뜀 0**(작21 1,217 + C 41 + D 36 + A 53) |
| 실측 | `hitpan_e2e`(헌법 #39) · MDB 3개 `Mode=Read` · 1차(빈 DB) 396.1s · 2차(병합 재이관) 339.1s |

## 0. 한 줄

계산서 21쌍이 **6항목 전부 0 차**가 됐고(66,631 = 레거시 전량 · 방향 분포 755/65,876 일치), 대사표 정답 칸이 **사장님 확인대로 히트판 화면 식**으로 바뀌었으며(재고 기말 0 차), 레거시 메모 242,106행이 **일일보고서 9,167장**으로 들어왔다. 그 과정에서 [3-V] 가 **미수·미지급 DIFF 3건이 한 뿌리**임을 잡아 봉합했다 — 수금·지급이 거래처 미등록이라고 5행을 버리고 있었다. 대사표 **OK 10/DIFF 16 → OK 22/DIFF 9**.

## 1. 갈래별 구현

### 갈래 C — 계산서 · 대사표 (`be211284`)

| 파일 | 무엇 |
|---|---|
| `LegacyMdbMapping.cs:106·119-133` | `TaxInvoiceNoMaxLength=32` · `TaxInvoiceNo(direction,txNo,seq,pdt,remHash8)` → `{S\|B}-{TX_NO}-{SEQ}-{PDT}-{HASH8}` · S/B 외 throw · 32 초과 throw. 기존 7개 시그니처 불변 |
| 〃 `:240` | `ItemKey(name,spec)` — 끝의 `\|` 제거(규칙을 한 군데로) |
| `MdbMigrationService.cs:3512` | `invoiceNoUnique` 를 위 함수로. `source_id` 형식 **불변**(재이관 열쇠) |
| 〃 `:3838·:3840` | stage SELECT `ORDER BY source_id` · UPSERT 갱신 목록에 `invoice_no = VALUES(invoice_no)` |
| 〃 `:1829-1853` | `EnsureMigAutoItemAsync` 가 `품명\|규격` 을 `item_name`/`spec` 으로 나눠 등록(`item_code` 해시는 키 전체 그대로 — BOM #78 재사용 유지) |
| `MdbReconciliationService.cs` | ⑤ DOCFB 품목별 Σ(IO=1)−Σ(IO=2) · ⑧ DOCFE IO=2 −수금(1~5) · ⑨ DOCFE IO=1 −지급(B~F) · 거래처별 차이도 새 식 · 종전 식은 Detail 참고값으로 보존 · ⑩ `LEGACY_FALLBACK` 제외 · ⑪ 일일보고서 · ⑫ 건수 4항목(cashbook·expenses·bank·발주수주) · POTHER 선택 열기 |
| `tools/MdbMigrationBaselineRun/Program.cs` | 판정 SQL 5행 추가(66,631 · 방향≠토큰 · invoice_no 길이 · MIG-AUTO 키 · hr_reports) · 컬럼 없음(1054)은 그 행만 NA |
| 게이트 | `MdbMigrationV2ReconGateTests.cs` 신설 41 — G9(21쌍 **MDB 실측 표본**) · G9-2(ItemKey) · W14~W16 배선 · W10 에 8번째 시그니처 |

### 갈래 D — DOCME → 일일보고서 (`24b04e0e`)

| 파일 | 무엇 |
|---|---|
| `LegacyDocmeMapping.cs` **신설** | 순수 static — `IsValidDate`(8자리·연도≠0000·실제 날짜) · `ReportKey` · `SourceId`(`docme-{ymd}-{sha8}`) · `FormatTime`(6자 아니면 `--:--`) · `Line`(`HH:MM [구분] 내용` · 공지 표기) · `Title`(`BuildTitle` 자구를 읽고 맞춤) |
| `MdbMigrationService.cs:484-495` | `events` 잡 **뒤** `hr_reports` 잡(POTHER · `continueOnFail: true`) |
| 〃 `:6087-6274` | `MigrateDailyReportsAsync` — (사원,날짜) 묶음 · 사원 이름 매칭(동명은 `emp_no` 첫 사람) · 미매칭/빈값은 `LEGACY_FALLBACK` + 본문 첫 줄 `원작성자:` · `status='approved'` · 19컬럼 1,000행 `INSERT IGNORE`(400만자 방어) · **본문·사원명 로그 금지**(#5) |
| `DB-119_hr_reports_source.sql` **신설** | 컬럼 3 + `uq_hr_reports_source(tenant_id, source_type, source_id)` · DB-118 관용구(멱등) · clean DDL 편입 + `('DB-119','clean-ddl',1)` 시드(#36) |
| 화면·결과 | `_totalTables` 18 · 카드 「일일보고서(레거시 메모)」 · 결과 `DailyReports` |
| 게이트 | `MdbMigrationV2DocmeGateTests.cs` 신설 36 — G10-1~5(값 호출) · W15a~d(배선·DDL) |

### 갈래 A·B — 모드 · 단계 · 체크포인트 · POST · 찾아보기

| 파일 | 무엇 |
|---|---|
| `MdbMigrationPhase.cs` **신설** | `MdbMigrationPhase`(All/MasterOnly/TransactionsOnly) · `MdbMigrationModes`(어휘·`UniqueChecksFor`) · `MdbMigrationCheckpointPolicy`(항상재실행 표) |
| `MdbMasterMergeClause.cs` **신설** | 갱신 절 조립 순수 static — 병합이면 `COALESCE(NULLIF(col,''), VALUES(col))`(숫자는 `NULLIF(col,0)` · 날짜·bool 은 NULL 만) |
| `MdbFolderBrowsePolicy.cs` **신설** | 경로 규칙 순수 static — UNC · `..` · 드라이브 상대경로(`C:HITWIN`) 거부 · 시스템 폴더 제외(하위까지) · MDB 3파일 판정 |
| `MigrationController.cs` | preview·reconcile **GET→POST**(GET 제거) · `/start` 는 `MasterOnly` 후 `paused`+`PhaseCompleted(1)` · 신설 `POST …/{jobId}/continue`(DB 조회 → tenant 대조 → paused/failed 만) · `GET …/current` · `GET …/browse`(+`MainPcOnly`) · `RunMigrationInBackground`(start·continue 공유) · `overwrite` 요청은 **400 "덮어쓰기는 준비 중입니다"** |
| `MdbMigrationService.cs` | mode/phase overload(기존 4개 보존) · `MigrateCoreAsync` 에 phase 분기(**메서드 분할 없음** — D 의 W15a 앵커 보존) · `RunTableStepAsync` 체크포인트 3훅(**tx 밖 autocommit** · jobId 없으면 skip · done 이면 `skipped` 콜백) · `unique_checks` 모드 분기 · 마스터 3표 UPSERT 가 병합 절 사용 · `EnsureMigrationWarehouseAsync` 비활성 MAIN 되살림 · 진행 행수를 **후보 행수**로 |
| `MigrationJobStore.cs` | `GetFromDbAsync`·`GetLatestByTenantAsync`·`GetCheckpointsAsync`·`Restore` · `SyncToDbAsync` 에 `paused_at` |
| `MdbMigration.razor` · `MdbFolderBrowseDialog.razor` | 찾아보기 버튼·다이얼로그 · 「이어서 가져오기」 배너 · POST 2곳 · 진입 시 재접속 · 1단계 완료 다이얼로그(**5/16 사장님 문안 그대로**) · `skipped` 카드 |
| 게이트 | `MdbMigrationV2ModeGateTests.cs` 신설 53 — W13(POST·GET 부재) · W14(DTO·overload) · W16(병합 절) · W17(경로 규칙) |

### [3-V] PM 봉합 (`54cee6da`) — 병렬이슈 16

`MigrateCollectionsAsync`·`MigratePaymentsAsync` 가 거래처 미등록이면 `continue` 로 버리던 자리를 **명세서 경로와 같은 폴백**으로. 새 함수 0(있는 `EnsureLegacyFallbackPartnerAsync` 재사용) · 로그 문구 「거래처 미매핑」→「거래처 폴백」.

## 2. 실측 (`hitpan_e2e` · tenant `e2e-mig-baseline-tenant`)

### 2-1. 판정 SQL — 작지서 §5 기대값 전부 일치

| 항목 | 기대 | 1차(빈 DB) | 2차(병합 재이관) |
|---|---:|---:|---:|
| `tax_invoices` migration | 66,631 | **66,631** | 66,631 |
| `direction` ≠ `source_id` 토큰 | 0 | **0** | 0 |
| `invoice_no` 최대 길이 | ≤32 | **30** | 30 |
| MIG-AUTO 품목 `\|`+spec NULL | 0 | **0** | 0 |
| `hr_reports` migration | 9,167 | **9,167** | 9,167 |
| 원장 행수 · 순수량 · 공급가 | 레거시와 0 차 | **0 차** | 0 차 |
| 회계 행수 · 차변 · 대변 | 0 차 | **0 차** | 0 차 |

### 2-2. 대사표 — **OK 22 / DIFF 9 / 31항목** (작21 종료 시 OK 10 / DIFF 16 / 26항목)

0 차가 된 것: 계산서 6항목 전부 · 재고 기말(새 식) · 수금 · 사원 · 일일보고서 · 건수 4항목 · 원장 3 · 회계 3 · 매입 공급가 · BOM.

남은 DIFF 9 — **전부 원인이 적힌다**:

| 항목 | 차이 | 왜 |
|---|---:|---|
| 판매 건수 / 공급가 / 부가세 | +353 / +33,059.20 / −5 | **레거시 내부 불일치** — 헤더표 DOCFE 에 없는 DOCFB 헤더그룹 353개. 히트판은 라인표를 100% 옮겼다(작21 리뷰서 §3 d-3) · 부가세는 반올림 |
| 매입 건수 / 부가세 | +209 / +1 | 같은 계통 · 반올림 |
| 미수 | +33,054.20 | 위 33,059.20 − 부가세 5 (같은 뿌리) |
| 미지급 | +1 | 반올림 |
| 업체 | +1 | 폴백 거래처 `LEGACY_UNKNOWN_PTNR`(사장님 정리 대상 · D1 결재) |
| 상품 | +859 | 자동등록 858 + 폴백 1 (D1 결재) |

### 2-3. [3-V] 봉합 전후

| | 봉합 전 | 봉합 후 |
|---|---:|---:|
| 수금 | 109,818건 (−4 · −1,450,000) | **109,822건 · 0 차** |
| `payments` | 2,227 | **2,228** |
| 미수 차이 | +1,483,054.20 | **+33,054.20** |
| 미지급 차이 | +50,001 | **+1** |

로그도 바뀌었다: `거래처 미매핑=4` → `거래처 폴백=4` · `미매핑=1` → `폴백=1`.

### 2-4. 작지서 §5 나머지 게이트 — 실측 (측정 도구에 `--mode`·`--phase`·`--job` 추가)

| 게이트 | 어떻게 쟀나 | 결과 |
|---|---|---|
| **1단계 분리** (오더 2 · 5/16 결재) | `--phase master` | ✅ **22.7s** — 마스터 4표만(입출고·수금 0). 목표 "1단계 ≤ 1분" 만족 |
| **G-MH 병합 보존** | 이관된 업체·사원·상품을 화면에서 고친 것처럼 바꾸고(`partner_name`·`ceo_name`·`emp_name`·`item_name`·`sale_price`·`purchase_price`) `--mode merge` 재이관 | ✅ **6개 값 전부 유지** · 업체 13,543 그대로. 빈칸만 채우는 절이 실제로 동작 |
| **G-CK 체크포인트 재개** | 같은 `--job` 으로 재실행 | ✅ **20표 skipped · 3표 실행 · 225.5s → 20.3s**. 실행된 3표 = `warehouse_migration`·`pyojun_master`·`item_stock_rebuild`(항상재실행 — 마스터는 2단계가 쓰는 사전을 다시 만들어야 하고, 리빌드는 최종 상태를 반영해야 한다) |
| **G-CK 대조군** 🔴 | `bills`·`card_payments` **체크포인트 2행만 지우고** 같은 잡으로 재실행 | ✅ **정확히 그 둘만 실행 · 나머지 18표 skipped** — 건너뛴 이유가 체크포인트임을 대조로 증명(초록불이 어디서 오는지) |
| **재이관 멱등** | 같은 MDB 2회(빈 DB → 병합) | ✅ 21표 행수·합계 동일 · `tax_invoices` Σ 흔들림 **없음**(작21 P1 해소) · `hr_reports` 9,167 그대로 |
| ⚪ **HTTP 경로(POST 전환)** | 인증 없이 `GET/POST` 를 던져 404/401 로 가르려 했다 | ⚠️ **판정 불가** — **없는 주소(`/api/zzz-no-controller`)도 401** 이었다. 인증 미들웨어가 라우팅보다 먼저 서서 이 방법으로는 경로 유무를 못 가른다. 401 만 보고 "POST 가 산다" 고 적으면 **초록불의 출처를 모르고 쓰는 것**이라 그렇게 쓰지 않는다. 대신 **W13 리플렉션**(액션의 `HttpGet` 부재·`HttpPost` 존재)이 이 자리를 재고, GET 을 되살리면 FAIL 하는 것까지 확인했다. 화면 종단(비번이 URL 에 안 남는지)은 **사장님 화면 실측**으로 |

## 3. 게이트 무력화 (봉합 빼면 빨간불인가)

| 게이트 | 무력화 | 결과 |
|---|---|---|
| G9 (계산서 번호) | `TaxInvoiceNo` 에서 방향 제거 | 실패 25 / 통과 16 → 원복 41/41 |
| G9-2 (품목 키) | `TrimEnd('\|')` 제거 | 실패 5 / 통과 36 → 원복 |
| W14 (배선) | 호출부를 종전 문자열 조립으로 | 실패 1 / 통과 106 → 원복 |
| G10-1~5 (DOCME) | 규칙 6군데 고의 파손 | 실패 22 / 통과 14 → 원복 36/36 |
| G10-1 단독 | 날짜 판정 자체를 `return true` | 실패 4 / 통과 7 (연도 0000 은 `TryParseExact` 이중 방어라 한 겹 더 벗겨 확인) |
| W13 (POST) | `[HttpGet("legacy-mdb/preview")]` 되살림 | 실패 1 → 원복 |
| W13/14/16/17 전체 | 스텁 상태 | 53건 전부 FAIL → 봉합 후 전건 통과 |

DB·MDB 를 타는 자리는 **게이트가 아니라 대사표로** 잰다(글자검사 금지 · 가짜 게이트 누적 24번). §2 가 그 측정이다.

## 4. DDL (헌법 #13·#36)

`DB-119_hr_reports_source.sql` — `hitpan_e2e` 에 2회 적용: 1차 4개 적용 · 2차 `already exists` **skipped 4**. `SHOW CREATE TABLE hr_reports` 에 컬럼 3 + `uq_hr_reports_source` 확인. clean DDL 편입 + 시드. 리셋 DB(clean DDL 로 만든 표)에서도 컬럼·시드 확인.

## 5. 안 한 것 / 못 한 것

| 항목 | 왜 | 후속 |
|---|---|---|
| **A1 덮어쓰기 본체**(백업→DataReset→뼈대 재시드→잡 생성→이관) | 사장님 **Q1** 미결(6/23 "직원 삭제" ↔ 8/14 "대표 사원 자동등록" 충돌) | 분기 자리·DTO 만 · 요청은 400 · 화면에 모드 선택 UI 도 만들지 않았다(막히는 버튼이 되므로) |
| **업로드** | 사장님 **Q2** — 터널 100MB/요청 · 8/13 조각 업로드 접힘 | 코드 0 · 안내 문구로 대체 |
| **일일보고서 목록 페이징·기간** | 사장님 **Q3** · 그룹웨어 영역 | 별도 작지서 — **관리자 탭에 9,167장이 한 번에 실린다** |
| 병합 **충돌표 UI**(전결1 Q8-C) | 이번은 기본값 (A) 보존만 | 작23 |
| `JournalSides` 양쪽 비영 카운트 | 담당 구간 밖(`:5318`) | 작23 (이 MDB 는 0행) |
| 행 단위 재개(`last_pk_value`) | 표 단위로 충분 | W4 |
| 계산서 21쌍 — **옛 형식 DB 위 재이관** | 방향 토큰은 빈 DB 에서만 짝을 가른다 | **병렬이슈 15** · 덮어쓰기(A1)로 해소 |
| 남은 판매·매입 DIFF | 레거시 내부 불일치 — 고칠 것이 아니라 기록할 것 | — |

## 6. [3-V] 병렬검증 이슈

| 이슈 | 판정 |
|---|---|
| [병렬이슈 15](../../검증/병렬이슈/20260910_병렬이슈15_계산서_방향토큰이_이미이관한_DB에서는_짝을_흡수한다.md) | ⚠️조건부 — 실물 표 사본으로 3경우 재현(빈 DB 2행 · 일치 2행 · 어긋난 8행 모양 1행). 회귀 아님 · 검증 기준에 "빈 DB" 조건 명기 · 덮어쓰기로 해소 |
| [병렬이슈 16](../../검증/병렬이슈/20260910_병렬이슈16_수금4행_지급1행은_거래처가_없다고_버려지고_있었다.md) | ✅봉합 — 원인 확정(중복 키 아님을 MDB 로 먼저 반증) 후 명세서 경로와 같은 폴백 적용 |
| 곁가지 확인 | `ORDER BY` + `ON DUPLICATE KEY UPDATE` MariaDB 11.4 실행 확인 · `hr_reports` 19컬럼 `INSERT IGNORE` 멱등(2행→0행) — **빌드·단위시험이 못 잡는 자리라 실물 DB 에 던졌다** |

---
**작성**: PM 브라운킴 · 2026-09-10 / **다음**: [4] 검증팀장 데이비드 박 작업리뷰서 → [5] CTO 래리 → 사장님
