# 20260920작1 갈래 S4 — 게이트 보강 · 감시 공백 봉합 개발명세서

| 항목 | 내용 |
|---|---|
| 루틴 | [3] 개발 — 갈래 S4 (게이트·CI) |
| 작업지시서 | [`20260920작1`](../../운영기록/20260920작1_수금등록_RC_안전경로_작업지시서.md) **§16-2 · §17 · §18** |
| 설계 정본 | [`20260920_설계_수금등록_RC_안전경로.md`](../../설계/erp/20260920_설계_수금등록_RC_안전경로.md) **§12-6 · §12-7 · §12-8** |
| 근거 이슈 | [3-V] 병렬이슈**53**(잠금 범위) · 병렬이슈**54**(게이트가 제품과 다른 모양) · [4] 병렬이슈**60**(재시도가 교착을 덮는다) |
| 기준 커밋 | `815b1885` → S4 `2222e1d0`(뼈대) · `fb32338f`(게이트) |
| 실측 인스턴스 | 위험 `127.0.0.1:33406` MariaDB 11.4.10-log · `log_bin=1` · `binlog_format=STATEMENT` (PM 이 띄워 둔 그대로 · 무접촉) / 안전 `127.0.0.1:33306` |
| 상태 | 🟡 **미완 — 턴 한도로 중단.** 실측 3건이 **설계 전제를 뒤집었다**(§3). PM 판단 필요(§7) |

---

## 0. 상태

| # | 내용 | 상태 | 커밋 |
|---|---|---|---|
| ① | 명세서 뼈대 | 🟢 | `2222e1d0` |
| ② | G-RC5 교착축 정밀화 · G-RC6 제품 상수 · G-RC12·13 신설 (빌드 0/0) | 🟢 | `fb32338f` |
| ③ | 🔴 실측 반증 3건 기록 | 🟢 | (이 커밋) |
| ④ | G-RC12·G-RC13 초록화 · 무력화 표 4건 · CI 주석 | 🔴 **못 함** | — |

---

## 1. 이 갈래의 목적 — 「감시자 0」을 없앤다

[4] 실측(작지 §18): **반품 잠금절만 빼면 전체 1,524 시험 중 한 건도 안 깨진다.**
초록불이 봉합을 지키고 있지 않았다. S4 는 초록불을 만드는 갈래가 아니라 **빨간불이 나게 만드는 갈래**다.

그 관점으로 게이트를 고쳤더니, **게이트가 먼저 봉합(DB-124)을 반증했다.** 아래가 그 기록이다.

---

## 2. 무엇을 고쳤나 (작지 §18 요구 6항 대비)

| 요구 | 한 일 | 상태 |
|---|---|---|
| ① G-RC5 「시도 횟수 = 1」 | 재시도 로그(`재시도 attempt=`) · `Innodb_deadlocks` 증분 · `SHOW ENGINE INNODB STATUS` **세 가지로 교착을 실제로 잰다.** 🔴 단, 「= 1」은 **도달 불가**로 실측됨 → §3-1 | 🟡 축 변경 |
| ② G-RC6·G-RC10 제품 상수 | 합계 3문장을 `CollectionService.DeliveryUsedSql`·`ReceiptPaidSql`·`ReceiptReturnedSql` **호출**로 교체(복제 제거) · 매입 축 `paid + returned` **두 문장** · 전표 조회 조건을 제품과 동일하게(`pr.status='confirmed'`) · 문구 단언을 `MsgDeliveryOver`·`MsgReceiptOver`·`MsgMatchBusyCollection` 의 `string.Format` 으로 | 🟢 |
| ② 확정반품 사례 | G-RC6 (다) 신설 — **스냅숏을 연 뒤** 다른 연결이 확정 반품 50,000 을 커밋 → 잠금절이 없으면 못 보고 통과(= 빨간불). 병렬이슈54 의 「감시자 0」을 메우는 자리 | 🟢 통과 |
| ③ G-RC12 무관 거래처 비차단 | UPDATE/INSERT 6건(수금·지급·매입반품) 신설 | 🔴 **5/6** — §3-2 |
| ④ G-RC13 EXPLAIN | 8문장 `EXPLAIN` · `key` 단언 · rows 상한 · 30→300 불변 · 테넌트 넘는 인덱스 금지 | 🔴 기대표 수정 필요 — §3-3 |
| ⑤ 지급 축 동시 | G-RC5 (가)에 지급 축 두 연결 추가([4] 미실측 ④) | 🟢 통과 |
| ⑥ CI GATES 유지 | 🔴 **못 함**(주석 정정 포함) | 🔴 |

사례별 결과(위험 33406 · 13사례): **통과 11 · 실패 2**(G-RC12·G-RC13) · Skipped 0.
안전 33306 회귀는 **안 돌렸다**(턴 한도).

---

## 3. 🔴 실측이 뒤집은 것 셋 — 이게 이번 갈래의 본체다

### 3-1 「다른 거래처 동시 = 교착 0」은 **인덱스로 도달할 수 없다** (병렬이슈60 의 답)

**절차**: 위험 33406 · 전용 DB · 거래처 PA(이월 미수 100,000)·PC(50,000) / PB(미지급 80,000)·PF(50,000).
두 연결이 **제품 진입점**(`CreateCollectionAsync`·`CreatePaymentAsync`)으로 **서로 다른 거래처**를 동시에 등록.

**결과**: DB-124 가 들어간 상태에서도 **1213 교착이 난다.**
```
[CollectionService] 수금 매칭 트랜잭션 재시도 attempt=1 errno=1213 mode=RepeatableReadLocking
[CollectionService] 지급 매칭 트랜잭션 재시도 attempt=1 errno=1213 mode=RepeatableReadLocking
```
재시도 1회로 둘 다 성공한다 — **고객 눈에는 안 보이고, 예전 단언(「예외가 안 샜다」)도 초록이었다.**

**그런데 교착 상대가 매칭 잠금이 아니다.** `SHOW ENGINE INNODB STATUS` 원문:
```
*** (1) TRANSACTION: ... inserting
INSERT INTO payments (payment_id, tenant_id, partner_id, ... payment_type, ref_order_id, ...)
VALUES ('6f006827-…', 'f727c4a3-…', '3bb97fe3-…', …, 'legacy_balance', '3bb97fe3-…', NULL, 'rc')
*** WAITING FOR THIS LOCK TO BE GRANTED:
RECORD LOCKS index uq_payments_source of table `hitpan_s2_rc_…`.`payments`
  lock_mode X insert intention waiting
  Record lock, heap no 1 … asc supremum;;
*** CONFLICTING WITH:
RECORD LOCKS index uq_payments_source … lock mode S
  heap no 1 … supremum ;;
  heap no 8 … (tenant_id 'f727c4a3-…', SQL NULL, SQL NULL, PK '41771279-…')
```

- 잠금 대상 = **`uq_payments_source` (`tenant_id`, `source_id`)** · 값 구간 = **`source_id` 가 NULL 인 자리 + supremum 간극**.
- 손으로 넣는 수금·지급은 `source_type`·`source_id` 가 **NULL** 이다(이관분만 값이 있다). 그래서 **모든 수기 등록이 이 유니크 인덱스의 같은 자리**에 들어간다.
- INSERT 는 유니크 중복검사로 그 자리에 **S 잠금**을 잡고, 상대의 **insert intention X** 를 막는다. 서로 그러면 1213.
- RR 에서만 난다 — RC 는 간극을 안 잠근다. **즉 STATEMENT 고객사 전용 사고다.** `collections` 도 `uq_collections_source`·`uq_collections_source_hash` 로 같은 모양이다.

🔴 **결론**: 병렬이슈53 이 지목한 원인(매칭 잠금 범위)은 **원인의 일부**였다. 남은 교착은 **유니크 중복검사 간극 잠금**이고, 이건 DB-124 로 안 없어진다.
PM 이 §13 에서 결재 근거로 적은 「교착 **원인 제거**」는 **지금도 성립하지 않는다.** (병렬이슈60 의 지적이 맞다.)

⇒ 게이트는 「교착 0」 대신 **「교착이 나더라도 그 상대가 매칭 잠금 인덱스여서는 안 된다」**로 단언을 바꿨다(도달 가능하고, DB-124 를 지우면 빨간불이 되는 축). 유니크 축은 **별도 사례(G-RC14)로 분리해야 한다 — 못 만들었다**(§6).

### 3-2 무관 거래처 **지급** INSERT 는 아직 막힌다 (6건 중 1건 · B4)

**절차**(S3 명세서 §3-1 과 같은 모양): 세션 A 가 RR 트랜잭션에서 매칭 8문장(L1~L5 + D1-a/b/c)을 잠근 채 유지 →
세션 B(`innodb_lock_wait_timeout=3`)가 **무관 거래처**(noise-0003·noise-0009, 양옆에 남의 행 30건)에 6건.

| # | 세션 B | 결과 |
|---|---|---|
| B1 | `collections` UPDATE (무관 거래처) | 🟢 3ms |
| B2 | `collections` INSERT | 🟢 3ms |
| B3 | `payments` UPDATE | 🟢 3ms |
| **B4** | **`payments` INSERT (`payment_type='legacy_balance'`)** | 🔴 **3,008ms · ERROR 1205** |
| B5 | `purchase_returns` UPDATE | 🟢 4ms |
| B6 | `purchase_returns` INSERT | 🟢 4ms |

- S3 는 **전/후 대조(5/6 → 6/6)** 를 쟀고 여기서는 **다른 시드**(거래처 30명 · 각 1행)로 쟀다. 모양이 달라 값이 갈린다.
- 유력한 원인: **L3 `PayableMatchedLegacyLockedSql`** 이 `idx_pay_tenant_type_ref` 를 `(tenant_id, payment_type)` **두 칸만** 쓰면
  그 회사의 **`legacy_balance` 지급 전부**가 잠긴다(S3 §2-2 도 L3 만 `ref|filter` 로 적었다 — 상수 3칸이 아니다).
  ⚠️ **확정 아님** — 잠금 대상 인덱스를 직접 못 찍었다(1205 는 교착이 아니라 `INNODB STATUS` 에 기록이 안 남는다).
- 같은 모양인 `collections` 축(B2)은 통과했다 — 수금 축은 좁혀졌고 **지급 축만 남았다.**

### 3-3 EXPLAIN 기대표가 **데이터에 따라 갈린다** (G-RC13)

실측: `L1 ReceivableMatchedLegacyLockedSql` · 표 `lc` · `type=ref` · **`key=idx_tenant_partner`** · `rows=1`.
S3 0단계는 같은 문장을 **`idx_coll_tenant_doc`** 으로 쟀다(명세서 §2-2). 둘 다 **테넌트 접두 · rows 1** 이라 잠금 범위는 좁지만,
「문장별로 이 인덱스여야 한다」는 고정표는 **시드 분포에 따라 흔들린다.**

⇒ 단언을 「**DB-124 인덱스여야 한다**」가 아니라 「**테넌트 접두 인덱스 + rows 상한 + 무관 행 10배에도 불변 + 테넌트 넘는 인덱스 금지**」로 바꿔야 한다.
**안 바꿨다**(턴 한도) — 지금 G-RC13 은 L1 에서 빨간불이다.

### 3-4 곁가지 실측 — 갭 잠금은 **인접 거래처**에서 진짜로 남는다

거래처가 둘뿐인 표에서는 두 거래처가 인덱스에서 **바로 이웃**이라, 거래처가 달라도 서로의 간극에 INSERT 해서 막힌다(1205·1213).
게이트에 **완충 시드**(`5s-…` 구분자 · 무관 행 30건)를 넣고서야 안정적으로 재진다. 설계 §12-6 이 「갭 잠금은 게이트로 못 지운다」고
적어 둔 그 자리가 **시험에서 그대로 드러났다.** ⇒ G-RC12 가 재는 것은 「무관 거래처면 무조건 안 막힌다」가 **아니라**
「**회사 전체가 통째로 막히지는 않는다**」까지다. 코드 주석에 그대로 적었다.

---

## 4. 무력화 실측 — 🔴 **못 했다**

작지 §16-2 의 4건(①반품 꼬리절 ②문구 한 글자 ③DB-124 DROP ④`USE INDEX` 강제)을 **한 건도 못 돌렸다.**
사례 2건(G-RC12·G-RC13)이 아직 빨간불이라, 무력화 전/후를 가를 기준선이 없다. **초록불만 보고 CI 에 등록하지 않는다**는 관례대로
CI(`GATES`)도 **안 건드렸다**.

---

## 5. 빌드·회귀

| 항목 | 결과 |
|---|---|
| 빌드 `HitPan.Tests` | 🟢 **errors 0 · warnings 0** (#19) |
| 위험 33406 `RcSafePathGate` | 🟡 13사례 중 **통과 11 · 실패 2**(G-RC12 B4 · G-RC13 L1) · Skipped 0 |
| 안전 33306 회귀 40 · `Migration\|Ddl` 274 | 🔴 **안 돌렸다**(턴 한도) |
| 제품 코드(`src/HitPan.Application`·`src/HitPan.API`) | 🟢 **한 줄도 안 고쳤다** — 변경 파일은 게이트 1개뿐 |

---

## 6. 안 한 것 · 못 한 것

1. **G-RC12 를 초록으로 못 만들었다** — B4(지급 INSERT)가 1205. 봉합이 덜 된 것인지 시드 탓인지 **확정 못 했다**(§3-2).
2. **G-RC13 기대표를 못 고쳤다** — 「테넌트 접두 + rows 상한 + 10배 불변」으로 바꿔야 한다(§3-3). 30→300 대조군은 코드에 있으나 **한 번도 못 돌았다**.
3. **G-RC14(유니크 축) 안 만들었다** — PM 지시 3항. 유니크 중복검사 교착을 별도로 재는 사례.
4. **무력화 4건 안 돌렸다**(§4). **CI 주석·GATES 안 고쳤다.**
5. **안전 서버 회귀·전체 회귀 안 돌렸다.**
6. **전표 조회 문장(FOR UPDATE) 2개는 아직 복제다** — S3 가 뺀 것은 합계 3문장뿐이라 게이트가 술어를 손으로 맞춰 뒀다. 그 술어가 갈라지면 아무도 못 잡는다.
7. **1205 의 잠금 대상 인덱스를 직접 못 찍었다** — MariaDB 11.4 에는 `performance_schema.data_locks` 가 없다. 교착(1213)은 `INNODB STATUS` 로 찍었지만 대기(1205)는 대기 중에만 보인다.

---

## 7. 🔴 PM 결정 필요

| # | 안건 | 근거 |
|---|---|---|
| **S4-1** 🔴 | 「다른 거래처 동시 = 교착 0」을 **완료 조건에서 내릴 것인가** — 인덱스로 도달 불가로 실측(§3-1). 남은 원인은 `uq_*_source` 유니크 중복검사 간극 잠금이다. 없애려면 **제품/DDL 변경**(수기 등록에도 `source_id` 를 채우기 등)이라 S4 범위를 넘는다 | §3-1 · 병렬이슈60 |
| **S4-2** 🔴 | **지급 축 B4 잔여 차단**(§3-2) — L3 가 `legacy_balance` 블록 전체를 잠그는지 확정할 **추가 실측**을 붙일지, 붙인다면 갈래 S5 로 뺄지. 확정되면 DB-125(`payments(tenant_id, payment_type, ref_order_id)` 는 이미 있다 → 술어·인덱스 재검토)로 간다 | §3-2 · 설계 §12-9 #5 |
| S4-3 | G-RC13 단언 축 변경 승인 — 「DB-124 인덱스 고정」 → 「테넌트 접두 + rows 상한 + 10배 불변 + 테넌트 넘는 인덱스 금지」 | §3-3 |
| S4-4 | 병렬이슈53 **원인 범위 정정** — 「매칭 잠금 범위」 하나가 아니라 「매칭 잠금 범위 + 유니크 중복검사 간극」 둘이다 | §3-1 |
| S4-5 | 제품 코드 변경은 **한 줄도 안 했다.** 필요해 보이는 자리는 위 S4-1(수기 등록의 `source_id`)인데, 금액·멱등 키에 닿는 변경이라 **설계로 돌려보내야 한다**는 것이 구현 의견 | 작지 §16-2 금지사항 |

---

**작성**: 개발팀 (루틴 [3] · 갈래 S4) · 2026-09-21 · **미완 상태로 인계**

---

# 8. 🔄 재개분 (2026-09-21 · PM 브라운킴 직접 수행 · 작지 §19)

> 위 §0~§7 은 **중단 시점의 기록 그대로 둔다**(지우지 않고 상태만 바꾼다).
> 이 절이 그 뒤에 일어난 일이고, **§7 의 PM 결정 5건은 작지 §19-3 에서 답했다.**

## 8-1. 먼저 잰 것 (판정 전)

| 잰 것 | 결과 |
|---|---|
| wip `2110dfe7` 빌드 | 🟢 **0 errors · 0 warnings** — §5 의 「빌드 미확인」 해소 |
| 위험 33406 전 사례 | **14사례 · 통과 13 · 실패 1 · skip 0** — §6-1(G-RC12 B4)·§6-3(G-RC14)은 **이미 해소돼 있었다**(wip 커밋이 담고 있었다) |
| 남은 빨간불 | **G-RC13 하나** — `L3` 이 `idx_pay_partner`(첫 칸 `partner_id`)를 고른다 |

## 8-2. G-RC13 을 어떻게 고쳤나 (작지 §19-1)

**제품은 한 줄도 안 고쳤다.** 고친 것은 게이트의 단언 축이다.

- **지운 것**: 손으로 적은 이름 목록 **둘** — `CrossTenantIndexes`(금지 목록) · `TenantPrefixExempt`(면제 목록).
  이름 목록은 나중에 진짜 위험한 인덱스가 와도 **이름만 더하면 조용히 초록**이 된다. 그게 [4] 가 잡은 「감시자 0」의 재발 경로다.
- **넣은 것**: `IndexSafety` — 셋을 **잰다**.
  ① 첫 칸이 `tenant_id` 인가(`information_schema.STATISTICS`)
  ② 아니면 그 칸이 **어느 표의 단일칸 PRIMARY KEY** 인가(= 한 값이 표 전체에 한 행 → 회사도 하나)
  ③ 그리고 그 표에서 **실제로** 두 회사에 걸친 값이 0 건인가(`COUNT` · 시험 DB 에는 회사가 2개 심겨 있다)
- **별칭 되돌리기**: `EXPLAIN` 의 `table` 칸은 별칭(`lp`·`ec`…)이라 스키마에 물어볼 수 없다 →
  **제품 문장의 `FROM/JOIN` 을 읽어** 진짜 표 이름으로 되돌린다. 못 되돌리면 별칭 그대로 넘겨 **빨간불이 되게** 둔다.
- **순서**: 안전판정을 **행 수 상한보다 먼저** 잰다 — 범위가 남의 회사까지 가는 것이 더 근본적인 사고이고,
  행 수로 먼저 걸리면 「왜 빨간불인지」가 잘못 읽힌다(무력화 ⑤ 에서 실제로 그랬다).

왜 `idx_pay_partner` 가 안전한가(작지 §19-1): `partners.partner_id` 는 **단일칸 PK** 다. 한 값 = 한 행 = 한 회사.
잠그는 행 집합도 `idx_pay_tenant_partner` 와 **같다**. 「첫 칸이 `tenant_id`」는 목적이 아니라 대리물이었다.
⚠️ 다만 `payments.partner_id` 에는 **FK 가 없어** 스키마가 그것을 강제하지 못한다 → 그래서 ③ 으로 **센다.**

## 8-3. 🆕 G-RC15 고객 문구 앵커 (작지 §19-6-1)

무력화 ②(`MsgReceiptOver` 한 글자 변경)가 **초록으로 남았다.** 원인은 결함이 아니라 **요구 둘의 충돌**이다 —
「게이트가 제품 문구 상수를 쓴다」(복제 금지)와 「상수를 고치면 빨간불」은 같이 성립할 수 없다.

⇒ 공용 식의 SHA 앵커(G-RC11)와 같은 방식으로 **문구 본문을 기록값으로 박았다**(4개).
막는 게이트가 아니라 **알리는 게이트**다 — 정말 바꿔야 하면 기록값을 같이 고치면 통과한다.

## 8-4. 🔴 무력화 실측 — 5건 전부 빨간불 확인

> 방법: 매 건 실제로 넣고 → 빌드 → 위험 33406 전 사례 실행 → **원복 후 `git diff` 0 확인**.

| # | 무력화 | 기대(작지 §16-2) | 실측 결과 |
|---|---|---|---|
| ① | `ReceiptReturnedSql` 잠금 꼬리절 제거 | G-RC6 | 🔴 **G-RC6 FAIL** (13/14) |
| ② | `MsgReceiptOver` 한 글자 변경 | G-RC6 | ⚠️ 처음엔 **14/14 초록** → G-RC15 신설 후 🔴 **G-RC15 FAIL** (14/15) |
| ③ | DB-124 인덱스 4개 제거 | G-RC12·13·**5** | 🔴 **G-RC12·G-RC13 FAIL** (12/14) · **G-RC5 는 초록** → 작지 §19-6-2 |
| ④ | `DeliveryUsedSql` 에 `USE INDEX(idx_tenant_partner)` | G-RC13 | 🔴 **G-RC13 FAIL** |
| ⑤🆕 | `L3` 에 `FORCE INDEX(idx_pay_type)` | (PM 추가) | 🔴 **G-RC13 FAIL** — 문구가 「첫 칸이 `payment_type` 다 — `tenant_id` 도 아니고 단일칸 PK 도 아니다」 |

🔴 ⑤ 를 왜 더했나: ①~④ 중 **새 안전판정 규칙 자체를 겨눈 무력화가 없었다.**
처음 시도(`USE INDEX(idx_pay_date)`)는 옵티마이저가 힌트를 무시하고 전체 스캔으로 가서 **다른 단언**이 잡았다 —
그건 새 규칙에 이빨이 있다는 증거가 못 된다. `FORCE INDEX(idx_pay_type)` 로 바꿔 **그 규칙이 그 이유로** 빨간불을 내는 것을 확인했다.

⚠️ 실측 중 만난 함정: 출하 DDL 에서 `KEY` 줄만 지우면 **앞 줄의 쉼표가 남아** DDL 적재가 통째로 실패한다
(→ 전 사례가 2.6초 만에 FAIL = 인덱스 때문이 아니다). 쉼표까지 지워야 진짜 무력화다. 처음엔 그걸 **초록/빨강으로 잘못 읽을 뻔했다.**

## 8-5. CI (작지 §19-5 ㉣)

- `GATES` 에 `RcSafePathGate` 는 **이미 등록돼 있다**(S2). 주석을 **정정·보강**했다:
  - `G-RC1~11` → **`G-RC1~15`**
  - 🔴 「다른 거래처 동시는 교착이 안 난다」가 **기대값이 아님**을 명시(작지 §19-3 S4-1 · §19-6-2).
    게이트가 재는 것은 「교착 0」이 아니라 **① 상대가 매칭 잠금이 아닐 것 ② 재시도가 받아 사용자에겐 성공**.
  - 무력화 5건 + G-RC5 가 안 걸린다는 사실 + 안전판정을 스키마로 잰다는 규칙을 등록 근거로 적었다.
- **출하 DDL 적재 스텝이 DB-124 를 포함하는지 확인함** — S3 가 `installer/hitpan_db_clean.sql` 에 편입했고,
  CI 는 그 파일을 그대로 적재한다(3306) · 위험 서버(3307)는 fixture 가 같은 파일을 적재한다. 별도 스텝 추가 불필요.

## 8-6. 빌드·회귀

| 항목 | 결과 |
|---|---|
| 빌드 `HitPan.Tests` | 🟢 **errors 0 · warnings 0** (#19) |
| 위험 33406 `RcSafePathGate` | 🟢 **15사례 전부 통과 · Skipped 0** |
| 제품 코드 | 🟢 **한 줄도 안 고쳤다** — 변경 파일은 게이트 1개 + CI 주석뿐 |

## 8-7. 아직 안 한 것 (정직하게)

1. **별건**: 수기 등록 `source_id` NULL 유니크 간극(교착 원인 ②) — 작지 §19-4 로 범위 밖 확정.
2. **§6-6 그대로 남음**: 전표 조회 문장(`FOR UPDATE`) 2개는 아직 게이트가 술어를 손으로 맞춰 둔 복제다.
3. **§6-7 그대로 남음**: 1205 의 잠금 대상 인덱스는 **대기 중에만** 보인다(MariaDB 11.4 엔 `data_locks` 없음).
   wip 이 넣은 관찰 장치가 그 한계 안에서 찍는다.
4. **CI 의 STATEMENT 스텝 실동작**은 여기서 못 잰다 — **PR 첫 실행이 최종 확인**이다.

---
**재개 작성**: PM 브라운킴 (루틴 [3] 지휘 · 직접 수행) · 2026-09-21 · **다음: [4] 검증팀장 재검증**
