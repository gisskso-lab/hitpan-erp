# 작업지시서 20260707작2 — 로그인 종단 전수봉합 (설치→계정생성→로그인 완결)

> 발행: PM 브라운킴 | 사장님 결재: **"중요도, 우선순위에 따라 모두 봉합!! 작업지시서 발행해서 절차대로 해!!"** (2026-07-07)
> 근거: 매니저 12건 독립 전수조사 (1차 8인 + 2차 4인, 수정 0줄 조사 완료)
> 원칙: 헌법 #1(수정은 추가만) #15(빈 catch 금지) #19(빌드 0/0) #20(워크플로우 끊김 0) #33(결재 완료) #36(출하 DDL) #40(아이디 방식)

---

## 0. 진범 요약 (조사 확정)

- **확정 진범**: `CompanyBootstrapProvisioner.cs:225`가 `users.role='tenant_admin'`(언더스코어) INSERT ↔ `UserConfiguration.cs:22` `HasConversion<string>()`이 `UserRole` enum(멤버 `TenantAdmin`)으로 역변환 → 로그인 조회(`AuthService.cs:35`) 시 EF materialize 폭발 → 로그인 실패. **계정은 DB에 있는데 읽는 순간 터짐.** EF8 실측: 대소문자 차이는 무해, **언더스코어가 사인**. 2026-07-01 emp_type 사고와 동일 부류의 재발.
- **구조 결함**: 설치 성공 화면과 "그 계정으로 로그인 가능" 사이에 **검증 0개** (안철수 상무 감수).

## 1. 봉합 목록 (중요도·우선순위)

### Wave 1 — P0 (로그인 종단 한 세트, 반드시 동시 수술)

| # | 봉합 | 파일 | 스펙 |
|---|---|---|---|
| W1-1 | **users.Role 관용 컨버터** | `UserConfiguration.cs:22` | EmployeeConfiguration.ParseEmpType 패턴 동일 적용: 쓰기=`ToString()`, 읽기=`Enum.TryParse(ignoreCase)` → 실패 시 언더스코어 제거 후 재시도(`tenant_admin`→`tenantadmin`→TenantAdmin) → 최종 실패 시 `UserRole.User` 폴백+경고로그. **기존 깨진 행(Sandbox·demo 시드)까지 자가치유** |
| W1-2 | **저장 어휘 단일화** | `CompanyBootstrapProvisioner.cs:225` | users INSERT의 role 값 `'tenant_admin'`→`'TenantAdmin'` (UserService.cs:112와 동일 사전). **account_type은 `'tenant_admin'` 유지**(claim·정책이 snake 사용). employees.role도 `'tenant_admin'` 유지(employeeRole claim 정합) |
| W1-3 | **로그인 employees 조회 테넌트필터 우회** | `IAuthUserLookup.cs`, `AuthUserLookup.cs`, `AuthService.cs:75·183·288·332` | 신규 메서드 `FindActiveEmployeeByUserAsync(userId, tenantId)` + `FindEmployeesByTenantAsync(tenantId)` — `IgnoreQueryFilters` + 명시적 tenant_id 조건(보안: user.TenantId로 한정). LoginAsync·RefreshAsync·BackfillParentEmployeeAsync의 employees 조회 전부 이걸로 교체. (로그인은 익명이라 CurrentTenant=''로 기존 조회가 항상 0건 → 매 로그인 헛INSERT 1062 + employee_id claim 공백 = 결재·경비·HR 침묵 고장의 진범) |
| W1-4 | **SalesManager 정책 tenant_admin 추가** | `Program.cs(API):258-259` | 다른 정책들과 동일하게 `tenant_admin` 추가. **W1-3과 반드시 동시** (W1-3만 하면 부모계정이 거래명세서 수정·취소·반품에서 403 즉발 — 헌법 #20 판매흐름) |
| W1-5 | **설치 종단 로그인 스모크 게이트 (정공법)** | `SeedParentCommand.cs` | CreateParent Ok 직후: DI에서 `IAuthUserLookup` resolve → `FindUserByEmailAsync(loginId)` (EF materialize = enum 컨버터 실제 관통) → null이면 **exit 6** / `BCrypt.Verify(password, hash)` 실패면 **exit 6**(인코딩·해시 불일치 적발). 예외도 catch→exit 6. 비번은 콘솔·로그 미출력. EF 연결이 db.conf로 해석되는지 확인 후 필요 시 AuditLogMiddleware 패턴으로 구성. 종료코드 주석 갱신(6=스모크 실패) |
| W1-6 | **ParentExists 별도 exit 10 + 고지** | `SeedParentCommand.cs:142-145`, `.iss RunSeedParent·CurStepChanged` | ParentExists → **exit 10**(멱등 성공이되 구분). .iss: code 10이면 설치 계속하되 MsgBox **"기존 부모계정이 유지되었습니다. 방금 입력한 비밀번호는 적용되지 않았으니 기존 비밀번호로 로그인하세요."** (재설치 시 새 비번 조용히 버려지는 함정 제거) |
| W1-7 | **인코딩 분열 봉합** | `.iss JsonEscape(:997-1017)` | 비ASCII 문자(Ord>126) 및 제어문자(Ord<32) 전부 `\uXXXX` 이스케이프 → 입력파일이 순수 ASCII가 되어 CP949/UTF-8 무관. (한글 아이디·비번 깨짐 = 로그인 영구불가, 한글 대표이름 오염의 진범) |
| W1-8 | **죽은 폴백 안내 정정** | `.iss:722-727` | "설치 후 로그인 화면에서 최초 등록" 약속 **삭제**(그 기능 실존 안 함). 정정: "「예」 진행 시 부모계정 없이 설치되어 **로그인이 불가능합니다. 재설치가 필요합니다.** 「아니오」로 돌아가 시리얼·사업자번호 재입력을 권장합니다." |

### Wave 2 — P1 (설치 신뢰성)

| # | 봉합 | 파일 | 스펙 |
|---|---|---|---|
| W2-1 | appsettings 정정 실패 승격 | `.iss FixupOneAppSettings(:910-913)` | SaveStringToFile 실패 시 Log만 → **RaiseException** (실패 방치 = 로그인이 demo로 새는 진범) |
| W2-2 | 기동 실패 가시화 | `.iss:1519-1531·1561-1564` | schtasks /Create·/Run 결과코드 검사, 최종 실패 시 **MsgBox 경고**(수동 기동 안내) + Log (재부팅 자가복구 가능하므로 비차단) |
| W2-3 | 터널 실패 폴백 문구 정정 | `.iss:1628-1634` | localhost 열 때 "이 화면에서는 로그인이 안 될 수 있으며 워치독이 자동 복구 후 {도메인}으로 접속하세요" 명시 |
| W2-4 | 로그인 서버측 Trim 방어깊이 | `AuthService.cs:35` | `request.Email?.Trim()` 한 줄 (헌법 #40 백로그) |

### Wave 3 — P1 휴면 enum 지뢰 (읽기 안전장치만, 쓰기·DDL 무변경)

| # | 봉합 | 파일 | 스펙 |
|---|---|---|---|
| W3-1 | items.item_type 관용 컨버터 | `ItemConfiguration.cs:20` | 읽기: TryParse(ignoreCase) → 명시 맵 `finished/promo→Product, semi_finished→Semi, raw→Material` → 폴백 Product+경고로그. `assembly→Semi`+경고(어휘 재설계는 별도 결재) |
| W3-2 | warehouses.wh_type 관용 컨버터 | `WarehouseConfiguration.cs:22` | 명시 맵 `자사창고→Normal, 3PL/위탁→Consign, 불량→Defect, 반품→Return, 기타→Normal` → 폴백 Normal+경고 |
| W3-3 | SubscriptionStatus Trial 멤버 추가 + 관용 컨버터 | `SubscriptionStatus enum`, `SubscriptionConfiguration.cs:29` | enum에 `Trial` 추가(가산 — DDL DEFAULT 'trial' 정합), 컨버터 맵 `suspended→Paused` 폴백 Paused+경고 |

## 2. CTO 결재 의견 (리스크 분석)

1. **W1-3 보안**: IgnoreQueryFilters 우회는 반드시 명시적 tenant_id 조건 동반 — user 행에서 얻은 TenantId로만 한정(테넌트 격리 유지, 헌법 #2 정합). 무조건 우회 금지.
2. **W1-5 스모크의 DB 해석**: seed-parent의 EF 경로가 db.conf를 못 읽으면 스모크가 오탐 → 구현자가 헤드리스 해석 실측 후 필요 시 명시 구성. 스모크 실패(exit 6)는 설치 실패 처리 = "거짓 성공" 원천 차단.
3. **W1-7 Inno 유니코드**: Ord()는 UTF-16 코드유닛 — 서로게이트 쌍도 `\u` 2개로 유효 JSON. 안전.
4. **W3 폴백 의미론**: 휴면 컬럼(현재 EF 읽기 0건)의 크래시 방지 보험 — 어휘 확정은 별도 설계 결재로 남김(경고로그로 추적).
5. **회귀 게이트**: 빌드 0/0 + 검증팀 SoD 독립반증 + (배포 후) Sandbox 백지 종단 = 3관문. 커밋·EXE 1.2.29 빌드는 사장님 최종 확인 후.

## 3. 배정

- **개발 A (백엔드·보안 개발자)**: W1-1~5, W2-4, W3 전체 (C# 일괄 — 빌드 정합 단일 책임)
- **개발 B (설치 개발자)**: W1-6(.iss측)·W1-7·W1-8, W2-1~3 (Inno Setup 일괄)
- **검증팀장 데이비드 박**: 구현 완료 후 SoD 독립반증 (빌드 0/0 + diff 전수 + 진범 재현 논증)

## 4. 검증 기준 (통과 못 하면 보고 안 올림)

1. `dotnet build` errors 0 + warnings 0 (헌법 #19)
2. users.role 'tenant_admin' 행이 폴백 아닌 TenantAdmin으로 읽히는 것 논증(컨버터 경로)
3. 로그인 경로에서 employees 조회가 필터 우회+tenant 한정으로 도는 것 확인
4. SalesManager 정책에 tenant_admin 포함 확인
5. seed-parent 종료코드 0/2/3/4/5/6/10 전부 .iss 판정과 짝 맞음
6. JsonEscape 산출물 순수 ASCII 확인
7. 기존 기능 무손상: 웹 create-parent·UserService 경로·자식계정 로그인 회귀 없음
