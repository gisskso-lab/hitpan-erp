# 개발명세서 2026-09-17 — 20260915작1 개정 3판 갈래 R3 · 재고 이어 계산 · 기준일 · 99999999

| 항목 | 내용 |
|---|---|
| 근거 작업지시서 | `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` §15-2 · §15-5 · §15-9 |
| 근거 설계 | `docs/설계/erp/20260915_설계_자료이관_머리없는줄_분류봉합.md` §23 · §24(99999999) · §25(G10) · §26(R-C2 행) |
| 근거 선행검증 | `docs/검증/선행/20260917_선행검증서_자료이관_이월잔액수금_재고이월계산_실측.md` B-0~B-3 |
| PM 결재 | ✅ 2026-09-17 조건부(§15-9 C1·C2) |
| 구현 담당 | 개발팀 갈래 R3 (hp-implementer · worktree) |
| 기준 커밋 | `285dedfc` → `7f9c6987`(계약 뼈대) · `7584e54b`(본체) · `082242a0`(G10) · 이 명세서 커밋 |
| 상태 | 🟢 구현 완료 · 🔴 PM 결정 1건(§6-1) |

## 진행 상태

| # | 덩어리 | 상태 | 커밋 |
|---|---|---|---|
| 1 | 계약 시그니처(§15-2 R3) + `RollForward`·`CarryBaseDate` | ✅ 빌드 0/0 | `7f9c6987` |
| 2 | 독립 재계산(사본 DOCFC 202512 자름) | ✅ 기대값 일치(§4-2) — 단 규칙 해석 1곳(§6-1) | — |
| 3 | `MdbMigrationService` 배선 | ✅ | `7584e54b` |
| 4 | `LegacyTaxNo` 실번호 우선 | ✅ | `7584e54b` |
| 5 | G10 게이트 + 봉합 빼고 FAIL | ✅ 11 PASS · 봉합 빼고 5 FAIL | `082242a0` |

## 사실 (첫 턴 ⚠️확인 · 실측 2026-09-17 · MDB 사본 `C:/HitPanBuild/mdb-0917/` 을 scratchpad 로 복사해 OleDb 로 읽음)

| # | 확인 | 결과 |
|---|---|---|
| ① | DOCFB 창고 칸 이름 | **`IJ_CHANG`**(String). DOCFB 칼럼 = IJ_DT·IJ_IO·IJ_SEQ·IJ_BUY·IJ_SAWON·IJ_SUN·IJ_PUM·IJ_KU·IJ_QTY(Decimal)·IJ_DC·IJ_DAN·IJ_AMT·IJ_VAT·IJ_REM·IJ_CHANG·IJ_TAXNO·IJ_DSEQ·IJ_TAXBUY. DOCFC 칼럼에 IM_CHANG·IM_DAN 있음 |
| ② | 이어 계산 키에 창고 포함? | **포함**(품명·규격·창고 = `LegacyMdbMapping.StockItemKey(pum,ku,chang)`) — 계산은 창고별 · 결과 `FinalStockResult` 는 창고 합산(갈래 D 규칙 R7 그대로). `MdbLegacyFinalStock.cs:280` 이하 |
| ③ | ROUND 방식 | `Math.Round(x, 0, MidpointRounding.AwayFromZero)` — 기존 `FinalUnitCost`(`MdbLegacyFinalStock.cs` 끝전)와 같은 쪽. **공영정보 사본에서는 ToEven 과 결과 같음**(§4-2 · 가르는 사례 0) |
| ④ | DOCFB IJ_IO 값 | "1" 5,284줄(입고) · "2" 118,868줄(출고) — 다른 값 0 |
| ⑤ | DOCFC 달별 행 | 202511 71 · 202512 73 · 202601 73 · 202602 74 · 자른 사본 삭제 147행 → 52,517행 |
| ⑥ | DOCFS | 309행 · 품명·규격 재고 키 겹침 0 · S_IDAN NULL 0 |
| ⑦ | 이관일 | `MdbMigrationService.cs:255` `now = DateTime.UtcNow` → 로컬 날짜 변환 `MigrationLocalDate(:2457)` |

## 1. 무엇을 만들었나

| 요구(작지 §15-5) | 구현 | 상태 |
|---|---|---|
| `RollForward` 규칙 ③ | `MdbLegacyFinalStock.cs:280` — 달마다 입고수량·입고금액(IO1) · 출고수량(IO2 · 음수=반품 · 금액 안 씀) · 입고≠0 & (기초+입고)≠0 → 월 총평균 · 아니면 S_IDAN(0/없으면 직전) · ROUND(수량×단가,0) | ✅ |
| 🔴 움직임 없는 달 | `:375` 그 달 DOCFB 줄이 없는 키는 **기말 그대로**(마스터 단가로 다시 재지 않음) · 수량 0 이면 금액 0 | ✅ §6-1 PM 확인 |
| 수량 0 금액 줄 | 계산에서 빼고 `RollForwardInfo.ZeroQtyAmountLines/ZeroQtyAmount` | ✅ |
| `CarryBaseDate` | `:261` min(DOCFB 마지막 달 말일, 이관일) | ✅ |
| `final` 받는 오버로드 2 | `AdjustStockAsync(db,tx,tenant,docfc,final,baseDate,ensureItem,logger,ct)` · `ApplyFinalCostAsync(db,tx,tenant,docfc,final,logger,ct)` — 기존 오버로드는 `ComputeFinalStock` 뒤 새 오버로드로 넘김(동작 같음 · WG-D1 통과) | ✅ |
| S1 안내 상수 | `RollForwardNotice(:241)` 결재 문구 그대로 · G10-8 | ✅ |
| 1단계 DOCFS S_IDAN 사전 | `MdbMigrationService.cs:268·:309` pyojun_master 스텝(2단계도 지남) · `ReadLegacyMasterIdan` | ✅ |
| 기준일 · DOCFB 뒤 줄 | `ReadLegacyPostingContext(pandata, now, masterIdan)` — 가드 발동이면 `BaseDate = CarryBaseDate` · `SELECT * FROM DOCFB WHERE IJ_DT > '{DOCFC 마지막 달}99'`(`:2441`) → `StockMovesAfter` · `LastValidLegacyDate` 도 로컬 날짜 | ✅ |
| skip 대신 이어 계산 | `RunLegacyStockFinalizeAsync(:645 부근)` — 같은 ①맞춤→②리빌드→③끝전 · `LegacyFinalStockSkipReason=null` · `LegacyFinalStockRollForward(:7386)` · 경고 로그(안내 문구 포함) | ✅ |
| 99999999 | `MdbLegacyUnpostedArchive.LegacyTaxNo` 실번호 → 99999999 → 0 | ✅ |
| `MigratedDocumentLock.cs` | 판정 영향 없음(실번호·99999999 모두 잠김 · 사유만 다름) → **손대지 않음**. 주석 `:93` 「첫 비0」 표현은 옛 규칙 그대로 남음 | ⬜ 안 함 |

## 2. 변경 파일

| 파일 | 변경 |
|---|---|
| `src/HitPan.Application/Services/MdbLegacyFinalStock.cs` | 추가(이어 계산 절 · 오버로드 2) · 기존 오버로드 2개 몸통을 새 오버로드 호출로 옮김 · `docfc!` → `docfc?` |
| `src/HitPan.Application/Services/MdbMigrationService.cs` | 추가(사전 · 컨텍스트 속성 2 · 3인자 오버로드 · 헬퍼 3 · 결과 속성) · `RunLegacyStockFinalizeAsync` 분기 추가 · 2인자 `ReadLegacyPostingContext` 는 3인자로 넘김 |
| `src/HitPan.Application/Services/MdbLegacyUnpostedArchive.cs` | `LegacyTaxNo` 규칙 · 주석 |
| 🆕 `src/HitPan.Tests/Integrity/MdbLegacyStockRollForwardGateTests.cs` | G10 순수 9 + DB 2 |

## 3. 헌법

| # | 어떻게 |
|---|---|
| #1 | 공개·비공개 기존 시그니처 전부 유지 · 오버로드 추가 · `LegacyPostingContext` 생성자 그대로(속성 init 추가) |
| #4 | 수량·금액·단가 전부 decimal |
| #13 | 새 SQL 없음(DB 쪽) — 기존 맞춤·끝전 SQL 그대로. OleDb 읽기 SQL 2(DOCFB·DOCFS)는 사본 칼럼 실측(§사실 ①⑥) |
| #15 | catch 추가 0 |
| #19 | Application · Tests 빌드 errors 0 / warnings 0 |
| #39 | 운영 3306·서비스·appsettings·마이그 SQL·출하 DDL 무접촉 · 원본 `Desktop/공영정보DB` 무접촉 |

## 4. 자체 확인 (실측)

### 4-1. 빌드 · 게이트
| 항목 | 결과 |
|---|---|
| `build-brief.ps1 -Project src/HitPan.Application/...` | errors 0 / warnings 0 |
| `build-brief.ps1 -Project src/HitPan.Tests/...` | errors 0 / warnings 0 |
| G10 + 회귀 `MdbLegacyFinalStockGate·MdbLegacyWiringGate·MdbPostingAllPathsGate·MdbPostingClassifyGate` (33306 · `HITPAN_REQUIRE_DB=1`) | **Passed 50 · Failed 0 · Skipped 0** |
| G10 + `TaxInvoiceMigratedLockGate·MdbReconPostingGate·LegacyUnpostedViewTenantGate` | **Passed 38 · Failed 0 · Skipped 0** |
| G10 단독 | **11 PASS**(순수 9 · DB 2) |
| 임시 DB 정리 | `SHOW DATABASES LIKE 'hitpan_r3_%'` 0행 |

### 4-2. 🔴 봉합 빼고 FAIL (치환 건수 각 1 확인 · 원복 = `git checkout --` 후 재실행 PASS)
| 뺀 봉합 | 치환 |
|---|---|
| S-a 서비스 이어 계산 분기 | `if (staleReason is not null && posting.StockMovesAfter is not null)` 에 항상 거짓 조건 추가 |
| S-b 반품 = 출고수량만 | IO2 음수를 「입고 절대값 + 금액」으로 |
| S-c 수량 0 키 금액 0 | `st.Amt = 0m` → `st.Amt + 0m` |
| S-d 실번호 우선 | `no == LegacyTaxNoNotIssuedValue` → `no == -1` |

결과: **Failed 5 · Passed 6** — G10-2(S-b · 지우개 반품) · G10-3(S-b) · G10-4(S-c) · G10-9(S-d) · G10-D1(S-a). 원복 뒤 11 PASS.

### 4-3. 독립 재계산 (PM §15-9 C1·C2)
- 사본: `C:/HitPanBuild/mdb-0917/{PANDATA,PYOJUN}.mdb` → 자기 scratchpad `r3mdb/` 복사 · `DELETE FROM DOCFC WHERE IM_YM > '202512'` 147행 삭제. 정답 = 원 사본(읽기 전용 `Mode=Read`) DOCFC 전체 `ComputeFinalStock`.
- 스크립트: scratchpad `r3calc/Program.cs`(읽기 전용 · 구현 호출 1 + **구현 코드를 안 쓰는 별도 식** 4변형) — 저장소에 안 넣음.
- DOCFB 마지막 유효 날짜 2026-02-20 · 기준일 `CarryBaseDate(2026-02-20, 2026-09-17)` = **2026-02-28** · 가드 발동 · 뒤 줄 583(계산 575 · 수량 0·금액 0 8)

| 계산 | 품목 키 | 수량 | 키 수량 불일치 | 금액 | 금액 다른 키 · 합 |
|---|---|---|---|---|---|
| 정답(DOCFC 전체) | 1,192 | 3,147,291 | — | 54,753,974.40 | — |
| **구현 `RollForward`** | 1,192 | **3,147,291** | **0** | 54,632,984.40 | **2키 · −120,990** (`수수료` −120,000 · `HTP21C/SQL-SERVER` −990) |
| 별도 식 · 움직인 키만 · AwayFromZero / ToEven | 1,192 | 3,147,291 | 0 | 54,632,984.40 | 2키 · −120,990 (둘 같음) |
| 별도 식 · **설계 글자대로 모든 키에 마스터 단가** | 1,192 | 3,147,291 | 0 | 54,935,484 | **5키 · +181,509.6** — 위 2키 + `HTP21C/표준형-L` +299,500 · `도메인연장` +3,000 · `기타<호호>` −0.4 |

- 키 수 1,192 = 품명·규격 키(창고 합산). 선행검증 B-1 표의 1,211 은 창고 포함 키 — 셈 단위 차이.

## 5. 안 한 것 / 못 한 것

1. `ReadLegacyPostingContext` 의 기준일 교체·DOCFB 뒤 줄 읽기는 OLEDB 경로라 **G10 이 직접 부르지 않는다**(CI = ubuntu). DB 게이트는 컨텍스트를 반사로 만들어 `RunLegacyStockFinalizeAsync` 부터 잰다. 실 MDB 로 끝까지 도는 재이관 실측은 [4] 몫(설계 §26 R-C2 행).
2. `MigrationLocalDate`(로컬 날짜 P6) 게이트 없음 — 시간대 의존이라 CI 에서 의미 있는 판정 불가.
3. 대사표 경로(`MdbReconciliationService`)가 이어 계산을 쓰는 것 · 안내 1줄 표시 = R2/R4 몫(이 갈래 파일 아님). 대사표가 DOCFS 를 읽는지 ⚠️미실측 그대로.
4. `MigratedDocumentLock.cs:93` 주석의 「첫 비0」 문구 미갱신(판정 무영향).
5. CI GATES 등록 = 갈래 Z.
6. 가드 발동인데 `StockMovesAfter` 가 null(자료 안 읽음)이면 **종전 skip 유지** — 실제 경로에선 발동 시 항상 읽으므로 반사 게이트(WG-D3 · G10-D2)에서만 탄다.

## 6. PM 결정 필요

1. 🔴 **「입고 없는 달 = S_IDAN」 적용 범위** — 설계 §23 글자(「달마다 … 아니면 S_IDAN」)대로 **그 달 움직임 없는 키까지** 마스터 단가로 다시 재면 선행검증 기대값(2키 −120,990)이 안 나오고 5키 +181,509.6(§4-3). 레거시 DOCFC 는 움직인 품목만 그 달 행이 있으므로(B-0) **그 달 DOCFB 줄이 있는 키에만** 규칙 ③ 을 적용하도록 구현했다 — 이때 기대값과 정확히 같다. 수량 0·금액만 남은 키(P/L)는 설계 「자동 0」 대로 0. 이 해석 승인 여부.
2. S_IDAN=0 인 품목의 `S_IDANB` 폴백 — 미구현(선행검증 B-2 ⚠️ · `수수료` 차이 원인). 결재 대상 유지.
