# 개발명세서 2026-09-17 — 20260915작1 개정 3판 갈래 R2 (11곳 소비자 · 대사표)

| 항목 | 내용 |
|---|---|
| 근거 작업지시서 | `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` §15-0~15-2 · §15-4 · §15-9 · §15-11 |
| 근거 설계 | `docs/설계/erp/20260915_설계_자료이관_머리없는줄_분류봉합.md` §20 · §24 · §25(G9) |
| PM 결재 | ✅ 2026-09-17 조건부 승인(작지 §15-9) |
| 개발팀 지휘 | PM 브라운킴 |
| 구현 담당 | hp-implementer (갈래 R2 · worktree `agent-ae9370d18e7823005`) |
| 기준 | `feat/20260915-migration-posting` `bc1c1092` (R1·R3 머지됨 · ff 병합) |
| 커밋 | 뼈대 = 이 커밋 · push 안 함 |

## 0. 진행 상태

| # | 덩어리 | 상태 | 커밋 |
|---|---|---|---|
| 0 | 명세서 뼈대 · 첫 턴 사실 | ✅ | `36680097` |
| 1 | #1·#2 `FinanceService.ReceivableBalanceSql`/`PayableBalanceSql`(공용 문자열) · #7 전잔액 R(P) · #8 연체 R>0 · #11 재집계 증분 규칙 · **병렬이슈45** 확정취소 수금 역분개 | ✅ API 빌드 0/0 | `ac07b03b` |
| 2 | #9 `MdbReconPosting.PartnerBalanceErpSql`(+M) · #10 FinanceService 같은 문자열 · ⑥ 3행 `AddLegacyBalanceRows` · F2 `ApplyExplained`/`StatusExplained` · 재고 ④⑤ `RollForwardForReconAsync` | ✅ API 빌드 0/0 | `34e1c488` |
| 3 | G9 `LegacyBalanceFormulaGate` 5/5 · #11 취소 중 명세서 제외 봉합(실측 발견) | ✅ 빌드 API·Tests 0/0 | `c5289604` |
| 4 | ⑥ 3행을 `AddBalanceItems` 밖(서비스에서 따로 호출)으로 — 기존 G6e 「⑥ 전부 OK」 기대 유지 · 회귀 111/111 | ✅ | `5c3f54f1` |
| 5 | 명세서 §2~§7 | ✅ | (이 커밋) |

### 병렬이슈45 처리 (PM 추가 지시)
- `SalesService.CancelConfirmedDeliveryAsync` — 수금 끄기(`UPDATE collections SET is_active=0`) **앞에서** 꺼질 활성 수금(id·거래처·금액·수단·수금일)을 같은 트랜잭션으로 읽고, 끈 뒤 수금마다 `AutoJournalHelper.RecordCollectionCancelAsync`(원 수금일 · 원 분개 없으면 역분개 없음 = R1 `ShouldReverseAsync` 규칙) 호출.
- ⚠️ 수금일이 마감된 달이면? — 명세서 취소 경로의 마감 검사는 기존 그대로(수금일 달 마감 검사는 안 넣음 · R1 P4 는 수금 삭제 경로 몫) → [4] 확인 요청.

### 대사표 Status 값 (R4 전달)
- ⑥ 3행: 「레거시 이월」 = **OK/DIFF**(F3 ↔ ΣL0 · 한쪽 null 이면 NA) · 「그 뒤 수금·지급」「남은 금액」 = **NA**(레거시 칸 null · Detail 「정보 — 비교할 레거시 값이 없습니다」). **INFO 는 안 쓴다**(계약 §15-2 새 값은 EXPLAINED 하나) — R4 가 INFO=「참고」로 받게 하려면 PM 결정 후 `AddLegacyBalanceRows` 두 줄만 바꾼다.
- F2: 업체·상품 = **EXPLAINED**(차이 = 설명 수) / 아니면 **DIFF** + 「설명된 n · 설명 안 되는 m」. `report.OkCount`/`DiffCount` 에 EXPLAINED 는 안 들어간다(별도 카운트 없음).
- 재고 안내 줄: 가드 발동 + 이어 계산 성공 시 **항상** `report.Warnings` 에 `RollForwardNotice`(S1) 1줄 + 수량 0 금액 줄 문구(0건이면 없음) · `stock_qty`·`stock_amount` Detail 에도 같은 문구. 가드 미발동 MDB 는 안 실린다(이어 계산 안 함). 이어 계산 실패 = 재고 항목 Detail 에 「재고 이어 계산 읽기 실패: …」 + 레거시 열은 DOCFC 값(종전).

## 1. 사실 (첫 턴 확인)

| # | 확인 | 결과 |
|---|---|---|
| ④ | 명세서 status `invoiced` 전이 경로 | `src/HitPan.Application`·`src/HitPan.API` Grep — `invoiced` 로 **바꾸는 UPDATE 0건**. 읽기만(`IN ('confirmed','invoiced')`) · `SalesService.cs:1142` 는 계산서 연결 건수 조회. enum 값은 `DB-33_sales_orders_enum_expand.sql`. → #11 재집계는 R1 P2 와 같이 `confirmed`+`invoiced` 둘 다 셈(값이 생겨도 안 빠지게) |
| ⑤ | 이관이 `purchase_returns` 를 만드는가 | **안 만든다** — `MdbMigrationService.cs:7347` 은 건수 자리표시(주석)뿐 · `INSERT INTO purchase_returns` 0건. `sales_returns` 도 INSERT 0건. → `rm`(이관 매입의 확정 반품)은 사람이 이관 매입에 붙인 반품만 |
| ⑦ | 대사표가 DOCFS 를 읽는 경로 | **있다** — `MdbReconciliationService.BuildAsync:103` 가 PYOJUN 을 열고 `:678` 이 `SELECT COUNT(*) FROM DOCFS` 를 이미 읽는다. 재고 이어 계산용 S_IDAN 사전은 이 연결로 읽는다(PANDATA 4표 `LoadPostingTablesAsync:338` 는 DOCFB 에 품명·규격·창고 칸이 없어 가드 발동 때만 DOCFB 를 다시 읽는다) |
| ⑧ | MIG-AUTO 1,149 구성 | ⚠️ 실 MDB 이관 DB 가 이 worktree 에 없어 실측 불가. 코드: `MdbMigrationService.cs:731` 주석 「DOCFC 에만 있는 품목 · R8 → MIG-AUTO 등록」 → DOCFC 전용 품목도 MIG-AUTO 로 들어간다(324 포함 추정). F2 판정은 건수 구성과 무관하게 `item_code LIKE 'MIG-AUTO-%'` 전체 + 받이 품목으로 센다 → [4] 실 재이관에서 1,149·1 확인 요청 |
| #13 | DESCRIBE (격리 33306 `hitpan_e2e_gi`) | `sales_returns` — **source_type 칼럼 없음** · status · is_deleted · delivery_id · total_amount · `partner_balance` total_sales·total_receipt decimal(15,2) · `partner_legacy_balances` base_date date · `sales_deliveries` source_type varchar(20) · is_deleted |
| G4·G6 | 기존 기대값이 3판 식에서도 같은가 | **같다** — G4(`MdbLegacyBalanceGate`)·G6(`MdbReconPostingGate`) 자료엔 이월 수금·지급(legacy_balance)·취소 수금·이관 매입 반품이 없어 M 의 이월분 = 0 · E 호환분은 종전 식에서도 빼던 금액을 R 로 옮긴 것(합계 같음). 기대값 수정 0 · 통과. 단 G6e 가 「⑥ 항목 **전부** OK」를 단언 → 3행(정보 행 NA)을 `AddBalanceItems` 안에 넣으면 FAIL → 서비스에서 따로 붙임(§2) |
| 발견 | #11 재집계 순서 | `CancelConfirmedDeliveryAsync` 는 재집계(5) **뒤에** `status='cancelled'` UPDATE → 취소 중인 명세서가 `total_sales` 에 남았다(종전 코드도 동일 결함 · G9-d 실측 50,000). `AND delivery_id <> @Did` 로 봉합 |

## 2. 무엇을 만들었나 (작지 §15-4 · 설계 §20·§24 대비)

| # | 곳 | 구현 | 상태 |
|---|---|---|---|
| 1 | 대시보드 미수 | `FinanceService.ReceivableBalanceSql`(public static readonly) — 명세서 쪽 명세서 − `CollectionDocSideWhere` 수금(활성) + Σ`ReceivableRemainingSql`.remaining · `kpiSql` 는 const → var 로 보간 | ✅ |
| 2 | 대시보드 미지급 | `FinanceService.PayableBalanceSql` — 명세서 쪽 매입 − `PurchaseReturnDocSideWhere` − `PaymentDocSideWhere` + ΣR · 이월 지급은 R 에서만 1번 | ✅ |
| 5 | 거래처 잔액 | **무수정** · G9-c 「이월 수금 30,000 → 잔액 30,000 한 번만 감소」 | ✅ |
| 7 | 전잔액 | `SalesService.GetDeliveryAsync` legacyPrevSql — 수금 `CollectionDocSideWhere("c")` + `ReceivableRemainingSql` 의 R(P) · 이월잔액 없는 회사는 종전 NULL 경로 | ✅ |
| 8 | 연체 버킷 | `PartnerController.GetAging` — 이월 부분 = `ReceivableRemainingSql` R>0 · 나이 base_date (hc 서브쿼리 제거) | ✅ |
| 9 | 대사표 거래처별 | `MdbReconPosting.PartnerBalanceErpSql` — 같은 공용 조건 · 히트판 열 = 식 값 + M · `ReceivableMatched`/`PayableMatched` 열 추가 · 교차 Σ(열 − M) = #1·#2 | ✅ |
| 10 | 대사표 ⑥ 합계 | `ReadBalanceErpAsync` = `FinanceService.ReceivableBalanceSql`/`PayableBalanceSql` 같은 문자열 + ΣL0·ΣM·ΣR·R<0 곳 수(`BalanceErp` nullable 속성 추가) | ✅ |
| ⑥ | 3행 | `MdbReconPosting.AddLegacyBalanceRows` — 키 `receivable_legacy`(F3↔ΣL0 OK/DIFF) · `receivable_matched`·`receivable_remaining`(NA 정보) · payable 같은 3키 · R<0 → `WarnNegativeRemaining` 경고. `BuildAsync` 경로 `AddBalanceItemsAsync` 에서 `AddBalanceItems` 바로 뒤 호출 | ✅ |
| 11 | 확정취소 재집계 | total_sales = confirmed·invoiced · 이관 제외 · **취소 중 명세서 제외** − 확정 반품 · total_receipt = 활성 수금 ref 무관 · 이관 제외 | ✅ |
| 45 | 확정취소 수금 역분개 | §0 참조 | ✅ |
| F2 | EXPLAINED | `MdbReconPosting.StatusExplained` · `ApplyExplained(item, 설명 수, 사유)` · 업체 = 받이 거래처(`LEGACY_UNKNOWN_PTNR`) · 상품 = `MIG-AUTO-%` + `LEGACY_UNKNOWN_ITEM` | ✅ |
| ④⑤ | 재고 이어 계산 | `RollForwardForReconAsync` — 가드(`FinalStockStaleReason`) 발동 시 PANDATA DOCFB(`IJ_DT > '{DOCFC 마지막 달}99'`) + PYOJUN DOCFS S_IDAN → `MdbLegacyFinalStock.RollForward` · 기준일 = `CarryBaseDate(DOCFB 마지막, 이월잔액 MAX(base_date) ?? 오늘)` · `AddStockItems` 오버로드(rollInfo) = 안내 1줄 + 수량 0 금액 줄 | ✅ |

## 3. 변경 파일

| 파일 | 내용 |
|---|---|
| `src/HitPan.Application/Services/FinanceService.cs` | 공용 식 2개 추가 · kpiSql 두 줄 교체 |
| `src/HitPan.Application/Services/SalesService.cs` | #7 · #11 · 병렬이슈45 |
| `src/HitPan.API/Controllers/PartnerController.cs` | #8 |
| `src/HitPan.Application/Services/MdbReconciliationService.cs` | #9·#10·⑥·F2·④⑤ (기존 public 시그니처 유지 · 오버로드·속성 추가만) |
| 🆕 `src/HitPan.Tests/Integrity/LegacyBalanceFormulaGateTests.cs` | G9 a~e · 전용 DB `hitpan_r2_g9_*`(끝나면 DROP · 실행 뒤 잔여 0 확인) |

## 4. 헌법 준수

- #1 기존 public 시그니처 무변경(`AddStockItems`·`AddBalanceItems`·`ReadBalanceErpAsync`) · 추가만 · `PartnerService.cs` 무수정 · 다른 갈래 파일 무수정.
- #2 tenant_id 는 인자·`HttpContext.Items`(JWT 미들웨어)만 · #4 decimal · #13 DESCRIBE(§1) · #15 빈 catch 0 · #16 Task.WhenAll 0 · #19 API·Tests 빌드 0/0.
- 복사 식 금지: 11곳 모두 `LegacyBalanceMatching` 상수/함수 보간 · #1·#10 은 같은 문자열 한 곳.

## 5. 자체 확인 (격리 33306 · `HITPAN_REQUIRE_DB=1`)

- **G9 5/5 PASS** (a 11곳 손계산·교차 · b R<0 · c #5 한 번 · d #11+45 · e F2·재고 안내).
- **봉합 빼고 FAIL** (Edit 1건씩 치환 성공 확인 → 실행 → `git checkout` 원복):

| 뺀 봉합 | 결과 |
|---|---|
| #2 `PaymentDocSideWhere` → 활성·비이관 전부 | G9-a FAIL 「#2 미지급 55,000 ≠ 80,000」(두 번 빼기) |
| #1 `CollectionDocSideWhere` 에서 `is_active = 1` 제거 | G9-a FAIL 「#1 미수 153,000 ≠ 160,000」 |
| #11 total_receipt 필터 → 종전 `ref_doc_type='sales_delivery'` | G9-d FAIL 「total_receipt 0 ≠ 30,000」(이월 수금 소실) |
| 45 `RecordCollectionCancelAsync` 호출 제거 | G9-d FAIL 「병렬이슈45 수금 취소 역분개 0 ≠ 1」 |
| F2 `diff == explained` → 항상 거짓 | G9-e FAIL Expected EXPLAINED · Actual DIFF |
| (실측 발견) #11 `delivery_id <> @Did` 없던 상태 | G9-d FAIL 「total_sales 50,000 ≠ 0」 |

- 원복 뒤 회귀: `LegacyBalanceFormulaGate|LegacyBalanceMatchGate|MdbLegacyBalanceGate|MdbReconPostingGate|MdbLegacyWiringGate|MoneyFlowJournal|Collection|TaxInvoiceMigratedLockGate|MdbMigrationV2ReconGate|SalesWorkflow` **111/111** skip 0.

## 6. 안 한 것 / 못 한 것

- 대사표 재고 이어 계산의 **실 MDB 경로(OLEDB)** 는 게이트 없음 — DOCFB `IJ_PUM·IJ_KU·IJ_CHANG` 칼럼명은 R3 `ToLegacyStockMoves` 와 같은 이름(설계 §23 ⚠️ 창고 칸 미실측 그대로) · `AddStockItems` 오버로드만 G9-e 순수 검사.
- 이어 계산 기준일의 이관일 = 이월잔액 `MAX(base_date)`(F4 로 같은 날) → 이월잔액이 없는 회사는 오늘 로컬 날짜(이관일과 다를 수 있음).
- R3 `MdbMigrationService.ToLegacyStockMoves`·`ReadLegacyMasterIdan` 은 private 이라 대사 쪽에 같은 변환을 따로 썼다(행 → 입력 레코드 변환만 · 계산 식은 공용 `RollForward`).
- `report.OkCount/DiffCount` 에 EXPLAINED 카운트 없음 · ⑥ 정보 행 Status = NA(INFO 아님).
- MIG-AUTO 1,149 구성(⑧) 실측 불가(§1).
- 병렬이슈45: 수금일 달 마감 검사는 확정취소 경로에 추가 안 함.
- CI GATES 등록은 갈래 Z.

## 7. 검증팀에 요청 · PM 결정 필요

1. ⑥ 정보 행 Status NA vs INFO(R4 화면 「—」/「참고」) — PM 결정 시 `AddLegacyBalanceRows` 4줄의 `null` 레거시 → Status 지정 1곳.
2. [4] 실 MDB 재이관 뒤 대사표: F2 업체 EXPLAINED(받이 1) · 상품 EXPLAINED(1,149+1) · 재고 안내 1줄 · ⑥ receivable_legacy/payable_legacy OK.
3. #11 취소 중 명세서 제외 결함은 종전 코드 결함 — 기존 고객 partner_balance 영향 범위(재집계 한 번 더 돌면 치유) 판단.
4. 병렬이슈45 확정취소 대상 수금일이 마감 달인 경우 동작.
