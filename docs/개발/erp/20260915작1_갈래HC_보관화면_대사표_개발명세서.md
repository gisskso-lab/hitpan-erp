# 개발명세서 2026-09-15 — 20260915작1 갈래 H+C (장부 미반영 보관 조회 화면 · 대사표 개편)

| 항목 | 내용 |
|---|---|
| 근거 작업지시서 | `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` §11-2(H·C) · §11-3(G6) · §11-4 · §13 · §14-3·§14-6·§14-7·§14-8 |
| 근거 설계 | `docs/설계/erp/20260915_설계_자료이관_머리없는줄_분류봉합.md` §7 · §14 · §15 · §17 · §18 |
| PM 결재 | ✅ 2026-09-15 (§13) · 3차 투입 §14-8 |
| 구현 담당 | hp-implementer (갈래 H+C 한 에이전트) · 브랜치 `worktree-agent-adf12cabc0585d613` (기점 `4c8a5fc0`) |
| 커밋 | H = 이 문서가 들어간 첫 커밋 · C = 뒤 커밋 (git log 참조) |

## 1. 무엇을 만들었나 (PRD 대비)

### 갈래 H — 보관 표 조회 화면 (읽기 전용)

| 요구 | 구현 | 상태 |
|---|---|---|
| 목록: 기간·거래처·판매/매입·사유 필터 · 페이징 | `ILegacyUnpostedDocumentService.GetPagedAsync` · `LegacyUnpostedDocumentService.cs` (고정 SQL `(@X IS NULL OR …)` · 쪽 최대 200 · QueryMultiple 1회 #16) | ✅ |
| 상세 줄 | `GetDetailAsync` (머리 + 줄 · 다른 회사 = null → 404) | ✅ |
| 쓰기 API 0 | 컨트롤러 GET 2개뿐 · 인터페이스 `Get*` 뿐 (H4 가 반사로 확인) | ✅ |
| tenant_id JWT 에서만 (#2) | `HttpContext.Items["TenantId"]` · 쿼리 파라미터에 tenant 없음 · 거래처 조인도 `p.tenant_id = u.tenant_id` | ✅ |
| 권한 = 자료 가져오기와 같은 규칙 | `[Authorize(Policy = "TenantAdminOnly")]` (= `MigrationController`) | ✅ |
| 입구 = 자료 가져오기 화면 버튼 1개 · Sidebar 무수정 | `MdbMigration.razor` 머리 아래 버튼 1개 (`Href="/settings/mdb-migration/unposted"`) | ✅ |
| 현장 용어 · 사유 사람 말 · 영문 코드 노출 0 | 머리글 「이전 프로그램 장부에 반영되지 않은 명세서」 · 사유 = `LegacyMdbMapping.UnpostedReasonText` · 구분 = 판매/매입 · 거래처 없음 = 「등록 안 된 거래처」 · 날짜 없음 = 「날짜 없음 — 기준일로 표시」 · 합계 미포함 안내 1줄 | ✅ |
| 한 화면 완결 | 필터 1줄 · 합계 1줄 · 표 · 줄 펼침(행 안) · 쪽 번호 | ✅ |
| tenant 격리 게이트 + GATES 등록 | `src/HitPan.Tests/Integrity/LegacyUnpostedViewTenantGateTests.cs` (H1~H4) · `build-and-test.yml` `LegacyUnpostedViewTenantGate` | ✅ |

### 갈래 C — 대사표 개편

계산은 `MdbReconciliationService.cs` 같은 파일 끝 🆕 `public static class MdbReconPosting`(OS 무관 · DataTable/IDbConnection 입력)에 모았다 — CI 에 Access 엔진이 없어 G6 가 합성 표로 부를 수 있게. 서비스는 MDB 를 읽어 넘기기만 한다.

| 요구 | 구현 | 상태 |
|---|---|---|
| ① 판매·매입 DOCFE ↔ 이관 명세서 | 종전 `AddDeliveryItemsAsync` 그대로(이미 DOCFE 기준) | ✅(변경 없음) |
| ② 장부 미반영 보관 행 (건·줄·공급가·부가세 × 판매·매입) | `ComputePosting` = `MdbLegacyUnpostedArchive.Partition`(이관과 같은 판정 · 날짜 없는 묶음 포함) ↔ `ReadArchiveAsync` · 항목 `archive_{sales,purchase}_{count,lines,supply,vat}` | ✅ |
| ③ DOCFB 줄 = 명세서 줄 + 보관 줄 | `docfb_lines` 레거시 = 전체 − 구분 없는 줄 ↔ 이관 명세서 줄(판매+매입 items) + 보관 줄 | ✅ |
| ④ 재고 = DOCFC 최종(ROUND 2) · 품목별 불일치 수 · 맞춤 줄 · DOCFC 없음/오래됨 | `AddStockItems`: `stock_qty`·`stock_amount`(Σ current_qty×avg_cost) · `stock_item_mismatch`(키 = `ItemNameKey` ↔ DOCFC `StockItemKey`) · `stock_adjust_net` · 알림. 신선도 = **DOCFC MAX YM vs DOCFB 마지막 달 직접 비교**(갈래 I 사유 키 아직 없음) · 오래됐으면 맞춤 기대값 NA | ✅ |
| ⑤ 원장 이력 입·출 LedgerMove · 맞춤 줄 분리 | `ComputeLedgerLegacy`(줄마다 `LedgerMove`) ↔ `ReadLedgerErpAsync`(`mb-adj-%` 제외/분리) · `ledger_rows`·`ledger_qty_in`·`ledger_qty_out`·`ledger_amount` | ✅ |
| ⑥ 미수·미지급 = F3 ↔ 갈래 E 식 | `ComputeBalanceLegacy`(`PartnerLegacyBalances`+`SummarizeBalances`) ↔ `ReadBalanceErpAsync`(FinanceService kpiSql 복제) · 곳 수 Detail · 거래처별 차이 = F3 를 히트판 거래처로 모아(해시→MIG 코드→폴백 거래처) E 식 거래처별 잔액과 비교 | ✅ (⚠️ 식 복제 — FinanceService 식이 바뀌면 같이 바꿔야 함 · G6e 가 결과로 문다) |
| ⑦ 분류 못 함 · 거래처원장 표 없음 | `report.Warnings` 🆕 + 보관 항목 Detail · 화면 `MdbMigration.razor` 알림 줄 | ✅ |
| ⑧ excluded_leftover > 0 → 「덮어쓰기로 다시 가져오기가 필요합니다」 | `CountLeftoverAsync`(보관 대상 source_id ↔ 이관 명세서 source_id · 1,000개씩) · 항목 DIFF + 알림 | ✅ |
| ⑨ 자기 자신과 비교 제거 | 종전 ④ `ledger_net_qty`(DOCFB 순수량) · ⑤ `stock_qty`(DOCFB 품목별) 삭제 · ⑧⑨ DOCFE−DOCF5 식·거래처별 사전 삭제 | ✅ |
| G6 `MdbReconPostingGate` + GATES | `src/HitPan.Tests/Integrity/MdbReconPostingGateTests.cs` G6a~e · `build-and-test.yml` | ✅ |
| W16(글자 게이트) | 옛 질의 모양 단언 → 새 함수 호출 단언으로 갱신(`MdbMigrationV2ReconGateTests.cs`) | ✅ |

## 2. 변경 파일 목록 (H)

| 파일 | 변경 | 왜 |
|---|---|---|
| `src/HitPan.Application/Interfaces/ILegacyUnpostedDocumentService.cs` | 🆕 | 조회 계약 + 응답 DTO |
| `src/HitPan.Application/Services/LegacyUnpostedDocumentService.cs` | 🆕 | SELECT 만 |
| `src/HitPan.API/Controllers/LegacyUnpostedDocumentsController.cs` | 🆕 | `GET api/migration/legacy-unposted` · `GET …/{docId}` |
| `src/HitPan.API/Program.cs` | 추가 2줄 | DI 등록 |
| `src/HitPan.Web/Pages/Settings/LegacyUnpostedDocumentsPage.razor` | 🆕 | 화면 (raw string 0 · #14) |
| `src/HitPan.Web/Pages/Settings/MdbMigration.razor` | 추가 8줄 | 입구 버튼 1개 |
| `src/HitPan.Tests/Integrity/LegacyUnpostedViewTenantGateTests.cs` | 🆕 | H 게이트 |
| `.github/workflows/build-and-test.yml` | 추가 4줄 | GATES 등록 |

- 기존 코드 추가만 (#1) ✅ · 새 표·칼럼 없음 (DB-123 은 갈래 F) — 해당없음 (#36)

## 3. 헌법 준수 (H)

| 조항 | 어떻게 | 근거 |
|---|---|---|
| #2 | tenant 는 Items 에서만 · 모든 SELECT 첫 조건 tenant · 조인도 tenant | 컨트롤러 · 서비스 `FilterSql`·`FromSql`·`DetailSql` |
| #4 | 금액 decimal | DTO |
| #13 | DESCRIBE 격리 33306 `f_new` (legacy_unposted_documents · _lines · partners) | 서비스 머리 주석 |
| #14 | razor 에 raw string 없음 | 화면 파일 |
| #15 | catch 전부 Snackbar/오류 줄 (빈 catch 0) | 화면 파일 |
| #16 | QueryMultipleAsync 1회 · 병렬 0 | 서비스 |
| #19 | Web · Tests(API 포함) 빌드 0/0 | §4 |
| #21 · #39 | appsettings 무접촉 · 운영 DB(3306)·5257·5234 접속 0 | — |

## 4. 자체 확인 결과 (H)

| 항목 | 명령 | 결과 |
|---|---|---|
| 빌드 Web | `scripts/dev/build-brief.ps1 -Project src/HitPan.Web/HitPan.Web.csproj` | errors 0 / warnings 0 |
| 빌드 Tests(+API·Application) | `… -Project src/HitPan.Tests/HitPan.Tests.csproj` | errors 0 / warnings 0 |
| H 게이트 | `HITPAN_DB_HOST=127.0.0.1 HITPAN_DB_PORT=33306 HITPAN_REQUIRE_DB=1` · `test-brief.ps1 -Filter FullyQualifiedName~LegacyUnpostedViewTenantGate` | Passed 3 · Failed 0 · Skipped 0 |

### 봉합 빼고 FAIL (H · 20260915 격리 33306 · 끝나고 원문 복원)

| 무력화 | 결과 |
|---|---|
| M1 목록·합계·사유 SQL `WHERE u.tenant_id = @T` → `WHERE (@T IS NOT NULL)` | Failed 2 (H1·H3) |
| M2 거래처 조인 `AND p.tenant_id = u.tenant_id` 제거 | Failed 1 (H1 — H2 함정 「회사 A 거래처 이름이 붙었다」) |
| (첫 시도 M1 은 CRLF 로 치환 0건이라 초록 → LF 로 다시 해서 빨간불 확인. 치환 0건 성공 함정 기록) | — |

## 5. 안 한 것 / 못 한 것 (H)

- 화면 실물(브라우저) 확인 안 함 — 5234 는 운영 계열이라 띄우지 않았다(#39). [4] 테스트환경 확인 필요.
- 보관 표 엑셀 내보내기·인쇄 없음(요구 없음).

## 6. 갈래 C — 파일 · 확인 · 봉합 빼고 FAIL · 못 한 것

| 파일 | 변경 |
|---|---|
| `src/HitPan.Application/Services/MdbReconciliationService.cs` | BuildAsync 호출 교체 · 🆕 `LoadPostingTablesAsync`·`AddPostingAndStockItemsAsync`·`AddBalanceItemsAsync` · 종전 ④⑤ 메서드 삭제 · ⑦ 수금만 남김 · `BuildPartnerDiffs` 거래처로 모아 비교 · 🆕 `MdbReconPosting` · 보고서 `Warnings` |
| `src/HitPan.Web/Pages/Settings/MdbMigration.razor` | 알림 줄 foreach + DTO `Warnings` (raw string 0) |
| `src/HitPan.Tests/Integrity/MdbReconPostingGateTests.cs` | 🆕 G6 |
| `src/HitPan.Tests/Integrity/MdbMigrationV2ReconGateTests.cs` | W16 단언 갱신 |
| `.github/workflows/build-and-test.yml` | GATES `MdbReconPostingGate` |

| 확인 | 결과 |
|---|---|
| 빌드 API · Web · Tests | 0/0 · 0/0 · 0/0 |
| G6 (격리 33306 · `HITPAN_REQUIRE_DB=1`) | 5/5 통과 · skip 0 |
| W16 + H + G6 묶음 실행 | 49 중 48 통과 → G6e 시드 창고 NULL 수정 뒤 G6 5/5 |

| 봉합 빼고 FAIL (원문 복원 확인) | 결과 |
|---|---|
| leftover SQL `IN @Ids AND 1=0` | G6e FAIL |
| 보관 표 합계 tenant 조건 제거 | G6e FAIL |
| 원장 맞춤 줄을 이력에 섞음 | G6e FAIL |
| 미수 식 이관 명세서 제외(E 규칙) 제거 | G6e FAIL |
| 판정에 거래처원장 연결 안 넘김 | G6a · G6e FAIL |

- #13 DESCRIBE 격리 33306 `f_new`(서비스·`MdbReconPosting` 머리 주석) · #16 순차 await · #15 빈 catch 0(GuardAsync 로그) · 운영 DB 무접촉.
- ⚠️ 못 한 것 / 확인 필요 ([4])
  - 공영정보 MDB 실물 대사 미실행(ACE + MDB 사본 + 재이관 DB 필요) — §11-4 기대값(보관 353/414·210/235 · 줄 124,152 = 123,503 + 649 · 재고 3,147,291/54,753,974.4 · 미수 142,062,113/51,577,479 · leftover 새 DB 0/병합 562) 은 [4] 실측.
  - DOCF5 61만행 + DOCFB 12만행을 DataTable 로 올린다(이관과 같은 방식) — 대사 1회 메모리·시간 [4] 측정.
  - 「갈래 I 가 남기는 DOCFC 신선도 사유 키」 없음 → 직접 비교로 구현. I 가 키를 남기면 그 키를 우선하도록 후속.
  - 폴백 병합 거래처는 곳 수가 레거시와 다를 수 있어 곳 수는 판정 없이 Detail 표시만.
  - 미리보기 카드(「가져올 자료 요약」)에는 알림 줄을 넣지 않았다(이관 후 카드에만).
  - ⑥ E 식을 대사 쪽에 복제 — FinanceService 공용 함수화는 수정 금지 파일이라 안 함.