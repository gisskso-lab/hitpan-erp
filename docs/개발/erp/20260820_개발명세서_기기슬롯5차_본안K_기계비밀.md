# 개발명세서 — 기기슬롯 5차 · 본안 K "소거를 교체로" (20260819작1)

| 항목 | 내용 |
|---|---|
| 문서번호 | 20260820개1 · SOP [3] 산출물 |
| 작업지시서 | `docs/운영기록/20260819작1_메인PC_판정축_재설계_대표진입로_작업지시서.md` |
| 설계 근거 | `docs/설계/erp/20260819_설계서_메인PC_판정축_재설계.md` §3-10 (11차 본안 K) |
| [3-V] | 7회 반증 전건 코드 착수 전 완료 — 7회차 **조건부 가** (`docs/검증/병렬이슈/20260819_3V_7회차_K조건부가_K0실재확정.md`) |
| 결재 | 사장님 8/20 오더 *".88 업데이트 파일을 해야지"* = 착수·게시 결재 접수 · PM ✅ |
| DDL | **변경 없음** (`auth_key_hash` varchar(64) 그대로 — 기계비밀 해시도 64 hex) |
| 게시 | **1.2.88** (`deploy-update.yml` dispatch · Major 채널 고정 · 헌법 #43) |

## 1. 무엇을 고쳤나 — 한 줄

**verify 성공 시 인증키 해시를 소거(NULL)하던 것을 기계비밀 해시로 교체**했다.
사람이 본 키는 지금처럼 죽고(결재 4 의도 유지), 기계비밀이 **매 요청 통행증**이 된다.
대표는 관문 안 **[이 컴퓨터 승인]** 한 번으로 직원과 **같은 문**(approve→verify)을 지난다.

## 2. 파일별 변경

| 파일 | 변경 | K |
|---|---|---|
| `src/HitPan.Infrastructure/Services/TenantDeviceService.cs` | verify 성공 UPDATE: `auth_key_hash = NULL` → `= hash(기계비밀)` + 원문 1회 반환. 신규 `VerifyDeviceSecretAsync`(tenant+device_id+hash+approved 넷 다) | K-1 · K-3 판정 본체 |
| `src/HitPan.Application/Interfaces/ITenantDeviceService.cs` | `VerifyDeviceSecretAsync` 추가 (헌법 #12 — 구현체 grep: `TenantDeviceService` 1개뿐) | K-3 |
| `src/HitPan.API/Middleware/DeviceAuthMiddleware.cs` | 축① 인라인 SQL → `VerifyDeviceSecretAsync` 호출(`device_id` 결합). 축②·③·Bypass **무변경**. 미사용이 된 `Sha256Hex` 제거 | K-3 |
| `src/HitPan.API/Controllers/DeviceController.cs` | `verify-key` 응답에 `deviceKey`(기계비밀 원문 1회) 추가. `deviceId` 계약 유지 | K-1 |
| `src/HitPan.Web/Components/Common/DeviceAuthGate.razor` | 보관값 = 죽은 사람 키 → **기계비밀**. 대표(`account_type` 클레임)면 대기 관문에 [이 컴퓨터 승인]: `StopPolling` → approve → **null 이면 reissue-key 경유**([3-V] 조건 1) → verify 자동 투입 → 실패 시 **키 화면 표시 + 수동 입력** fallback | K-2 · K-4 |
| `src/HitPan.Tests/Integrity/DeviceAndKeyGateTests.cs` | G-31-b·G-34·G-34-b 를 K 의미론으로 갱신 + **G-35~G-38 신설**([3-V] 조건 2) | 게이트 |

## 3. 게이트 — 초록불의 출처를 물었다

### 🔴 G-E 실측 — 봉합을 실제로 빼고 FAIL 확인 (가장 먼저)

| 실험 | 무엇을 뺐나 | 결과 |
|---|---|---|
| A | K-1 되돌림(`auth_key_hash = NULL` 소거로) | 🔴 **FAIL 4건** — G-31-b · G-35 · G-36 · G-37 |
| B | K-3 되돌림(축① 판정에서 `AND device_id = @Did` 제거) | 🔴 **FAIL 1건** — G-36 |

⇒ 초록불이 **내 봉합에서** 온다. DB 제약이 아니다(지문 전부 상이 — uq_tenant_fp 무접촉).

### 신설 게이트 (격리 DB + 출하 DDL + 실제 서비스 — 글자 검사 0)

| G | 값으로 세운 것 | 결과 |
|---|---|---|
| **G-35** | 🔴🔴 verify 통과한 직원 기기가 기계비밀로 **매 요청 통과** + **사람 키로는 안 통함**(결재 4) — **K-0 닫힘의 종단** | ✅ |
| **G-36** | 남의 기계비밀+자기 번호 / 자기 번호+남의 비밀 = 0 · 짝이 맞으면 통과(8/10 모양 방지 쌍) | ✅ |
| **G-37** | 대표 자기승인 종단(approve→verify→통행) + 🔴 **재시도 갈래**: approve 재클릭 null(멱등) → **reissue 경유로 통행까지** + 옛 비밀 사멸 | ✅ |
| **G-38** | pending 기기는 해시가 맞아도 통행 0 (승인제 유지) | ✅ |

### 실행 결과 (2026-08-20 · MariaDB 11.4 로컬 격리 DB)

```
기기·키 계열 4스위트: 통과 62 / 실패 0 (DeviceAndKey·DeviceApproval·DeviceTypeQr·MainPcLocalConsole)
전체:               통과 584 / 실패 0
빌드(-warnaserror):  경고 0 · 오류 0   (헌법 #19)
```

## 4. 무엇을 안 했나

- 축②(`is_main_pc` 장비넘버 통과)의 G-32-d 한계 — **범위 밖 유지**(번호는 비밀이 아님, 별건)
- 기존 verify 통과 기기(해시 NULL) 자동 이행 — **없음.** [인증키 재발급] → 재입력 1회가 정본 경로. ⚠️ 배포 후 그런 기기는 관문 회귀가 **정상**이며 재발급으로 잇는다
- `MainLayout.razor` `@Body` · Bypass · 대표 탈출로(축③) — 무변경
- appsettings(헌법 #21) · DDL · 슬롯 계수 — 무접촉

## 5. 남은 것 ([4] 이후)

1. [4] 검증팀 독립반증 → [5] CTO·사장님 → 커밋·머지
2. **게시 1.2.88** → TEST1 반영 확인
3. 🔴 실측 — **`https://test1.hitpan.kr` + TEST1 계정** (🚫 demo 금지 — 사장님 8/20):
   실1 대표 [이 컴퓨터 승인] 한 번 → 업무 화면 · 실1′ 조회·저장 · 실2 직원 키 입력 → 업무 API 정상(K-0 닫힘) · 실3 직원 폰 여전히 대기
