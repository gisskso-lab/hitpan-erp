# 개발명세서 2026-09-21 — 20260920작1 갈래 S1 · 판정기 · 두 경로 · 재시도 · D1

| 항목 | 내용 |
|---|---|
| 근거 작업지시서(PRD) | `docs/운영기록/20260920작1_수금등록_RC_안전경로_작업지시서.md` §4 · §5-3 · §8 갈래 S1 · §11 |
| 설계 정본 | `docs/설계/erp/20260920_설계_수금등록_RC_안전경로.md` §3 · §4 · §5 |
| 선행검증 | `docs/검증/선행/20260920_선행검증서_수금등록_READCOMMITTED_서버설정의존_실측.md` |
| PM 결재 | ✅ 2026-09-21 (작지 §11) |
| 개발팀 지휘 | PM 브라운킴 |
| 구현 담당 | 개발팀 구현 담당 (hp-implementer · worktree `agent-a72ed09e032ce3084`) |
| 커밋 | §6 단계별 표 |

## §0 카드 (≤20줄)

- **한 줄**: 서버가 `log_bin=ON` + `binlog_format=STATEMENT` 이면 READ COMMITTED 쓰기가 거절된다(선행 F1). 등록을 막는 대신 **판정해서 RR 경로로 간다**. 두 경로가 같은 정확성 계약을 지킨다.
- **파일 4**: `BinlogSafetyProbe.cs`🆕 · `LegacyBalanceMatching.cs` · `CollectionService.cs` · `src/HitPan.API/Program.cs`.
- **0단계 결과**: §3 — RR + STATEMENT 에서 `FOR UPDATE` 가 통과하는지 raw 실측.
- **안 한 것**: 게이트 신설(`RcSafePathGate`)·CI 등록은 **갈래 S2 몫**. 이 명세서는 S1 만.

## §1 계약 (갈래 간 고정 · S2 가 소비한다)

```csharp
public enum LegacyMatchMode { ReadCommittedFresh, RepeatableReadLocking }
public static IsolationLevel LegacyBalanceMatching.IsolationFor(LegacyMatchMode mode);

public interface IBinlogSafetyProbe {
    Task<LegacyMatchMode> GetModeAsync(IDbConnection db, ILogger? logger, CancellationToken ct);
    void Invalidate();
}
public sealed class BinlogSafetyProbe : IBinlogSafetyProbe {
    public BinlogSafetyProbe(TimeSpan? ttl = null, TimeProvider? clock = null);
}

Task<Remaining?> LegacyBalanceMatching.GetForUpdateAsync(IDbConnection db, IDbTransaction tx,
    LegacyMatchMode mode, string tenantId, string partnerId, bool receivable, CancellationToken ct);
Task LegacyBalanceMatching.EnsureMatchAllowedAsync(IDbConnection db, IDbTransaction tx,
    LegacyMatchMode mode, string tenantId, string partnerId, string? refId, decimal amount,
    DateTime date, bool receivable, ILogger? logger, CancellationToken ct);

private Task<T> CollectionService.RunMatchTxAsync<T>(LegacyMatchMode mode,
    Func<IDbTransaction, CancellationToken, Task<T>> body, string what, CancellationToken ct);
```

- `mode` 는 `tenantId` **앞**(잊고 못 지나간다) · `CollectionService` 생성자 `IBinlogSafetyProbe? probe = null` **선택 인자**(기존 호출자·테스트 무변경).
- 재시도 화이트리스트 1213·1205·1665 · 3회 · 50→150ms + 지터 · `InvalidOperationException` 에는 **절대 안 건다** · 감사로그는 껍질 밖.

## §2 변경 파일 목록

| 파일 | 변경 | 왜 |
|---|---|---|
| `src/HitPan.Application/Services/BinlogSafetyProbe.cs` 🆕 | 판정식 · 프로세스 캐시 · TTL · 실패 시 RR · `Invalidate()` | 설계 §3 |
| `src/HitPan.Application/Services/LegacyBalanceMatching.cs` | `LegacyMatchMode` · `IsolationFor` · 가드(모드↔격리) · R 재계산 잠금절 | 설계 §4-2 · §5 |
| `src/HitPan.Application/Services/CollectionService.cs` | 트랜잭션 모드 분기 · 재시도 껍질 · 전표 SUM 잠금절(D1) · 소진 문구 | 설계 §4-3 · §4-4 |
| `src/HitPan.API/Program.cs` | `IBinlogSafetyProbe` 싱글턴 등록 (appsettings 무접촉 #21) | 작지 §8 |

## §3 0단계 — RR + STATEMENT raw 실측

*(코딩 전 필수 · PM 보고 대상 · 작지 §8 · 설계 §10-1)*

### 판정: 🟢 **RR + STATEMENT 에서 `FOR UPDATE` 통과 — 설계대로 진행.** 작지 §8 의 1665 분기(거래처 행 잠금을 `LOCK IN SHARE MODE` 로 내리는 안)는 **쓰지 않는다.**

**인스턴스**: `C:\HitPanBuild\mariadb-stmt-0920`(선행검증이 만든 datadir 재사용) · 포그라운드 `mariadbd.exe --no-defaults --port=33406 --log-bin=stmt --binlog-format=STATEMENT --server-id=9920` · **서비스 등록 안 함** · 시험 후 종료. 운영 3306 · 격리본 33306 무접촉(#39).
**스키마**: `rc_safe_s1` ← 출하 DDL `installer/hitpan_db_clean.sql` 적재(테이블 155) · `--default-character-set=utf8mb4`.
**서버 확인값**: `@@log_bin=1` · `@@binlog_format=STATEMENT` · `11.4.10-MariaDB-log`.

| 단계 (REPEATABLE READ · 실제 테이블) | 결과 |
|---|---|
| `SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ` → `@@tx_isolation` = `REPEATABLE-READ` | ✅ |
| `BEGIN` | ✅ |
| `SELECT balance_id FROM partner_legacy_balances … FOR UPDATE` | ✅ **통과** (1665 없음) |
| `SELECT COALESCE(SUM(amount),0) FROM collections … LOCK IN SHARE MODE` | ✅ 통과 |
| `INSERT INTO collections …` | ✅ 통과 |
| `COMMIT` | ✅ 통과 |

**대조군(같은 인스턴스 · 같은 테이블 · 격리수준만 RC)** — 실패해야 판정이 성립한다:

```
SET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED; BEGIN;
SELECT balance_id FROM partner_legacy_balances WHERE tenant_id='t1' AND partner_id='p1' FOR UPDATE;
→ ERROR 1665 (HY000) at line 3: Cannot execute statement: impossible to write to binary log
  since BINLOG_FORMAT = STATEMENT and at least one table uses a storage engine limited to
  row-based logging. InnoDB is limited to row-logging when transaction isolation level is
  READ COMMITTED or READ UNCOMMITTED.
```

→ 변수는 **격리수준**이다(선행 F5 와 같은 결론, 이번엔 우리 실제 테이블로). 설계 §10-1 미실측 **해소**.
→ 이 대조군이 곧 무력화 시험 **M1**(§4) 의 실측 근거다 — 모드 분기를 빼면(=RC 고정) 이 서버에서 등록의 첫 문장이 1665 로 거절된다.

## §4 무력화 시험 표 (봉합을 빼고 빨간불을 확인)

> 게이트 신설은 S2 몫이다. 여기에는 **내 손으로 확인한 것만** 적는다. 안 돌린 칸은 빈칸으로 남기고 「안 했다」고 쓴다.

| # | 봉합 | 빼면 무엇이 빨간불이어야 하나 | 실측 | 근거 |
|---|---|---|---|---|
| M1 | 모드 분기(RR 경로) | STATEMENT 서버에서 `FOR UPDATE`/INSERT 가 ERROR 1665 | 🔴 **확인** — RC 고정 시 `FOR UPDATE` 에서 1665 · 같은 문장이 RR 에서는 통과 | §3 대조군 (raw · 33406) |
| M2 | R 재계산 잠금절 | RR 에서 최종 R 음수 · 초과 거절 안 남 | ⬜ | |
| M3 | 재시도 껍질 | 다른 거래처 동시에서 1213 노출 | ⬜ | |
| M4 | 전표 SUM 잠금절(D1) | 같은 전표 동시 수금 합 초과가 통과 | ⬜ | |
| M5 | 판정 기본값 | 조회 실패 시 RC 로 가면 위험 서버에서 막힘 | ⬜ | |

## §5 안전 서버 회귀

⬜ 진행 중.

## §6 커밋 (상태 3단)

| 단계 | 내용 | SHA | 빌드 |
|---|---|---|---|
| ⓪ | 개발명세서 뼈대 | | — |
| ① | 판정기 + 순수 판정 | | |
| ② | 모드 · 가드 · R 잠금절 | | |
| ③ | 서비스 분기 · 재시도 · D1 | | |

## §7 안 한 것 · 못 한 것

- 게이트 신설 `RcSafePathGateTests.cs` · `LegacyBalanceMatchGateTests.cs` §5-3 한 줄 · CI 등록 → **갈래 S2**.
- (진행하며 채운다)

---
**작성**: 개발팀 구현 담당 · 2026-09-21 / **검증**: [4] 검증팀장 데이비드 박 (작성자 ≠ 검증자)
