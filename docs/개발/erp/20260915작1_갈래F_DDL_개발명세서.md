# 개발명세서 2026-09-15 — 20260915작1 갈래 F DDL (DB-123 + 출하 DDL)

> 근거: 작업지시서 `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` §11-2 F · §11-3 G7 · §13(R6-2·R-A2) · 설계 §15·§16·§17 · 병렬이슈39 ①②③
> 브랜치: worktree 브랜치(`feat/20260915-migration-posting` d299e479 위) · push 안 함

## 1. 무엇을 만들었나
- 🆕 `legacy_unposted_documents` · `legacy_unposted_document_lines` — 설계 §15 칼럼 그대로. 이관만 INSERT · 읽기 전용.
- 🆕 `partner_legacy_balances` (R-A2 (나)) — tenant_id · partner_id · legacy_buy_code · base_date · balance_amount decimal(15,2)(+미수/−미지급) · source_type/source_id/migrated_source_hash · created_at.
  UNIQUE 2개: (tenant_id, source_id) · **(tenant_id, partner_id)** (병렬이슈39 ①).
- `item_stock.avg_cost` decimal(15,2) → **decimal(19,6)** NOT NULL DEFAULT 0 (R6-2 (가)) — 넓히기만.
- 공통: ENGINE=InnoDB · utf8mb4_unicode_ci · tenant_id NOT NULL · FK 없음. 멱등 = CREATE TABLE IF NOT EXISTS + avg_cost 는 information_schema(precision 19·scale 6) 확인 후 PREPARE (DB-118 관용구).
- 출하 DDL 에 같은 정의(알파벳 자리) + 시드 `('DB-123','clean-ddl',1)` (ddl-smoke 5단계 파일 수 == 시드 수).
- 게이트 G7 `LegacyUnpostedDdlGate` 4개 + CI db-gate GATES 등록.

## 2. 변경 파일 목록
| 파일 | 내용 |
|---|---|
| 🆕 `src/HitPan.API/Migrations/SQL/DB-123_legacy_unposted_documents.sql` | 표 3 생성 · avg_cost 넓히기 |
| `installer/hitpan_db_clean.sql` | 표 3 추가 · item_stock.avg_cost 1줄 · 시드 DB-123 |
| 🆕 `src/HitPan.Tests/Integrity/LegacyUnpostedDdlGateTests.cs` | G7-1~4 |
| `.github/workflows/build-and-test.yml` | GATES 에 `LegacyUnpostedDdlGate` + 무력화 근거 주석 |

## 3. 헌법 준수
- #1 기존 코드 삭제 0 (출하 DDL avg_cost 한 줄은 결재 R6-2 형 변경) · #2 tenant_id NOT NULL · #4 금액 decimal · #13 격리 DB 에 출하 DDL 적재 후 DESCRIBE(item_stock·partner_balance·stock_adjust_logs·sales_delivery_items·stock_ledger) 로 형 근거 · #17 InnoDB · #19 빌드 0/0 · #21 appsettings 무접촉 · #36 두 파일 information_schema 대조 · #39 운영(3306·3307) 무접촉 — 격리 인스턴스 `C:\hp_iso_33306` 포트 33306 만.

## 4. 자체 확인 결과 (실측)
- 빌드: `build-brief.ps1 -Project src/HitPan.Tests/HitPan.Tests.csproj` → **errors=0 warnings=0**.
- 수동 대조: 옛 출하 DDL + DB-123 2회 vs 새 출하 DDL — 4표 columns·statistics·tables 77줄 **차 0**, 2회차 `skipped`.
- G7 (격리 MariaDB 11.4.10 · HITPAN_REQUIRE_DB=true): **4/4 통과** (UNIQUE 반영 후 재실행).
- 봉합 빼고 FAIL (무력화 · 매번 원복):
  | 무력화 | 결과 |
  |---|---|
  | M1 출하 DDL = HEAD · DB-123 파일 없음 | **4/4 FAIL** |
  | M2 DB-123 `partner_legacy_balances.source_id` varchar(64) | G7-2 만 FAIL (「DB-123 에만: …varchar(64)」) |
  | M3 DB-123 에서 avg_cost 단 제거 | G7-2 · G7-3 FAIL (305872.549020 → 305872.55) |
  | M5 `IF NOT EXISTS` 제거 | G7-2 FAIL (already exists) |
  | M6 partner UNIQUE → 일반 KEY (두 파일) | G7-1 · G7-2 FAIL |
  ⚠️ M1~M5 는 병렬이슈39 반영(UNIQUE·AssertTableShape) **전** 게이트 코드로 돌렸다. 반영 후엔 M6 과 통과만 재실행.
- DataReset 자동 포함(코드 수정 0): `src/HitPan.Application/Services/DataResetService.cs:111-120`(information_schema 표 목록 − PreservedTables) · `:133-148`(tenant_id 칼럼 있으면 `DELETE … WHERE tenant_id`) · `PreservedTables :47-70` 에 세 표 없음. G7-4 가 **실제 `ResetAllAsync`** 로 이 회사 0행 · 다른 회사 1행 유지 확인.
- avg_cost C# 타입 grep: `StockBalanceDto.AvgCost` decimal(`DTOs/Stock/StockBalanceDto.cs:15`) · `TotalValue` decimal(`StockOperationDtos.cs:73`) · Dapper `<decimal>` (`BomService.cs:839`) — double/float 0건. 수정 불필요.
- G7 준비 중 발견: Windows 에서 `Process.StandardInput` 기본 인코딩이 CP949 라 한글 COMMENT 가 깨져 G7-2 가 거짓 빨간불 → 게이트에 `StandardInputEncoding = UTF8(no BOM)` 지정. 같은 준비물을 쓰는 `MainPcRestartMarkGateTests.cs:130` 도 같은 모양(COMMENT 를 비교하지 않아 영향 없음 · 수정 안 함).

## 5. 안 한 것 / 못 한 것
- 🟢 **번호 충돌 해소 (PM 결정 2026-09-15)**: `DB-120_main_pc_single_row_guard.sql:23·153` · `docs/운영기록/20260913결1_메인PC표식_V4_마이그정지_CTO결재.md:129` 가 「DB-121 = 탑승비밀 예약 · 메인PC 2차 재정합 = DB-122 이후」 ⇒ 1차 구현의 DB-121 을 **DB-123** 으로 바꿨다(파일명·출하 DDL 시드·게이트·CI 주석·이 문서). 121·122 는 메인PC 트랙 몫으로 비워 둔다(러너 번호 정렬 + id 문자열 skip · PM 근거 `MigrationRunner.cs:64-68`). ⚠️ 작업지시서·설계 문서의 「DB-121」 표기는 이 갈래가 고치지 않았다.
- ⚠️ (tenant_id, partner_id) UNIQUE 는 설계 §17 「폴백 병합」과 부딪친다 — 레거시 코드 여러 개가 한 거래처로 합쳐지면 갈래 E 가 거래처별 합산 1행으로 넣어야 한다(아니면 INSERT 중복 오류/IGNORE 로 잔액 누락). 설계 §15/§17 에 이 UNIQUE 없음 → 병렬이슈39 지시로 추가.
- ⚠️ `legacy_unposted_document_lines` UNIQUE(tenant_id, migrated_source_hash) — 레거시에 내용이 같은 줄 2개가 있으면 해시가 같아 한 줄이 빠진다. 갈래 B 가 해시에 줄 위치(dt·seq·line_no)를 넣어야 한다.
- ⚠️ qty 자리수: 설계 「DOCFB MAX(|IJ_QTY|) 실측 후 확정」 — MDB 실측 **못 함**(범위·턴 한도). `sales_delivery_items.qty`·`stock_ledger.qty_in` 과 같은 decimal(15,3) 로 둠(정수부 12자리).
- ⚠️ `stock_adjust_logs.before_cost/after_cost` 는 decimal(15,2) 그대로 — BOM 생산·해체(`BomService.cs:620·666·852·925`)가 avg_cost 를 복사하면 2자리 반올림 저장(Note). 범위 밖 · 보고만.
- ⚠️ `item_stock.current_qty` decimal(10,2) · stock_ledger qty (15,3) 불일치 — 범위 밖 · 보고만.
- 「덮어쓰기」 경로의 초기화는 같은 `DataResetService` 를 부른다고 보고 G7-4 로 갈음 — 덮어쓰기 컨트롤러 종단 실측은 안 함.
- CI(리눅스 db-gate 잡) 실행은 push 뒤 첫 실행이 최종 확인.

## 6. [3-V] 병렬검증 지적 사항
- 병렬이슈39 ① partner UNIQUE — 반영 · ② 새 표 3개 ENGINE·collation·tenant_id·UNIQUE + avg_cost scale 6 을 출하 DDL(G7-1)·DB-123(G7-2) 둘 다 `AssertTableShapeAsync` 로 — 반영 · M6 FAIL 기록 · ③ 초기화 뒤 0행 — G7-4 (덮어쓰기 종단은 §5).

## 7. 검증팀에 요청
- DB-123 번호 결정(§5) · partner UNIQUE 와 폴백 병합 정합(갈래 E) · 줄 해시 규칙(갈래 B) 교차 확인.
- CI db-gate 에서 G7 SKIP 0 확인.
