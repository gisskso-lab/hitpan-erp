# 20260922작3 — 메인PC 물리식별 게이트 시험 개발명세서

| 항목 | 내용 |
|---|---|
| 작성 | PM 브라운킴 (개발팀 지휘) · 2026-09-22 |
| 근거 | 작업지시서 [`20260922작3`](../../운영기록/20260922작3_메인PC물리식별_게이트시험_작업지시서.md) — **PM 결재 §11** · 설계 [`20260922_설계_메인PC물리식별_왕복증명.md`](../../설계/erp/20260922_설계_메인PC물리식별_왕복증명.md) §8 |
| 오더 | 사장님 전결 2026-09-22 — *"게이트 시험 붙이고 → 실측"* |
| 상태 | 🟢 순수 게이트 **13건 붙었고 실제로 돈다** · 🟡 DB 게이트 **5건 붙었으나 이 PC 에서 계측 불가**(§5) · 🔴 실물 실측(M-1) **여전히 0회** |

---

## 1. 무엇을 만들었나

| 파일 | 줄 | DB | 담은 게이트 |
|---|---|---|---|
| `src/HitPan.Tests/Integrity/MainPcProofRoundTripGateTests.cs` | 신설 | **무접촉** | G-1a · G-2a · G-4 · G-5a · G-5b · G-6 · G-7a · G-7b · G-7c · G-7d · G-9 · G-9b · G-13a (**13건**) |
| `src/HitPan.Tests/Integrity/MainPcSealRegistryGateTests.cs` | 신설 | **필요** | G-11a · G-14 · G-15 · G-16 · G-16b · G-18 · G-19a (**5개 시험 · 7게이트**) |
| `.github/workflows/build-and-test.yml` | +13 | — | `db-gate` 잡에 `MainPcSealRegistryGate` 등록 + 근거 주석 + echo 갱신 |
| `docs/설계/erp/20260922_설계_메인PC물리식별_왕복증명.md` | ±1 | — | §8 **G-17 정정 줄**(지우지 않고 상태만 바꿈 · #41) |

🟢 **생산코드 변경 0줄.** `git diff --stat` 으로 확인 — 위 4개 외에 변경 없다.

### 왜 두 파일로 갈랐나
섞으면 **DB 없는 환경에서 순수 게이트까지 함께 건너뛴다.** 순수 파일에는
`ForbiddenDbConnection` 을 꽂아 **DB 를 만지면 그 자리에서 터지게** 했다 —
"DB 무접촉" 을 주석이 아니라 **구조로** 증명한다.

---

## 2. 🚨 이 PC 의 TPM 키를 덮어쓰는 함정 — 시험이 피해 간 자리

`src/HitPan.Application/Services/Security/TpmKeyService.cs` 의 `SealKey` 는
`CngKey.Create(CngAlgorithm.Rsa, "HitPan.TaxInvoice.MasterKey", KeyCreationOptions = OverwriteExistingKey)` 다.

⇒ **부르면 이 PC 의 그 이름 TPM 키를 덮어쓴다.** 이 PC 는 정식설치 실물이다(#39 · 인계3 §7-6).
⇒ 그래서 두 파일 모두 **봉인(`SealKey`)을 실물로 부르지 않는다.**
   · 순수 파일: `NeverCalledTpm` — 부르면 시험이 터진다
   · DB 파일: `LocalSealTpm` — "이 PC 가 봉인한 것만 풀린다" 성질만 흉내 낸 **대역**
⇒ `UnsealKey` 는 **비파괴**(열기만 한다)라 실물을 썼다 — 그것이 G-13a 다.

🔴 **그러므로 실물 TPM 봉인 경로는 이 시험으로 단 한 번도 돌지 않았다.** G-13b(실측)의 몫이다.

---

## 3. 🟢 실행 결과 (실측)

```
빌드   dotnet build src/HitPan.Tests/HitPan.Tests.csproj -c Release /p:TreatWarningsAsErrors=true /warnaserror
       → 경고 0개 · 오류 0개                                    (#19)

순수   dotnet test --filter FullyQualifiedName~MainPcProofRoundTripGate
       → 통과 13 / 실패 0 / 건너뜀 0   · 2회 연속 같은 결과(82ms · 81ms — 깜빡임 없음)

DB     dotnet test --filter FullyQualifiedName~MainPcSealRegistryGate
       → 🔴 "통과 5" 로 보이지만 **전부 [SKIP] 이다.** §5 참조. 통과로 세지 않는다.

전체   dotnet test (1550건)
       → 통과 1549 / 실패 1   · 🔴 실패 1건은 **내 변경과 무관한 작2 절A 의 결함**이다 (§6)
```

🔴 **DB 게이트의 "통과 5" 를 통과로 읽지 마라.** `--logger console;verbosity=detailed` 로 다시 돌려
`[SKIP]` 5줄을 눈으로 확인했다. 이것이 바로 20260828 작14 W1 이 봉합한 그 사고 모양이다 —
**xUnit 에게 `return` 은 PASS 다.**

---

## 4. 🚨 G-3 대조군 — 봉합을 빼고 실제로 돌렸다

누적 25회 지적된 자리다. **표만 쓰는 것으로 끝내지 않았다.**
절차: 봉합 일시 제거 → 빌드 → 그 게이트만 실행 → **FAIL 을 눈으로 확인** → `git checkout --` 즉시 복원.
🔴 **커밋하지 않았다.** 스크립트는 케이스마다 `finally` 로 되돌렸고, 끝에 `git diff --stat` 이 비었다.

| # | 어디서 무엇을 뺐나 | FAIL 해야 하는 게이트 | 결과 |
|---|---|---|---|
| C-1 | `Consume` 의 `TryRemove` → `TryGetValue` | G-4 | 🟢 **FAIL 확인** |
| C-2 | `Consume` 의 `entry.IsExpired` 검사 무력화 | G-5a | 🟢 **FAIL 확인** |
| C-3 | `ChallengeLifetime` 60초 → 600초 | G-5b | 🟢 **FAIL 확인** |
| C-4 | `Consume` 의 `SessionKey` 대조 제거 | G-6 | 🟢 **FAIL 확인** |
| C-5 | `Consume` 의 `TenantId` 대조 제거 | G-6 | 🟢 **FAIL 확인** |
| C-6 | `Outcome == MainPcConfirmed` 조건 제거 | G-7d | 🟢 **FAIL 확인** |
| C-7 | `IsMainPc` 의 `IsPassValid` 호출 → 항상 true | G-2a | 🟢 **FAIL 확인** |
| C-8 | DI 없을 때 fail-closed → fail-open | G-7b | 🟢 **FAIL 확인** |
| C-9 | 요청 본문에 `bool IsMainPc` 칸 추가 | G-7a | 🟢 **FAIL 확인** |
| C-15 | `SelfClaimPathDisabled` → `false` | G-7c | 🟢 **FAIL 확인** |
| **C-14** | **되돌린 뒤 전체 재실행**(대조군의 대조군) | **전부 PASS** | 🟢 **빌드 0 · 시험 0(전부 통과)** |
| C-10 | 옛 메인PC `is_main_pc=0` UPDATE 제거 | G-15 | 🔴 **미실행 — §5** |
| C-11 | `rows == 0 → false` 제거 | G-16b | 🔴 **미실행 — §5** |
| C-12 | `IsDataEmptyAsync` 한쪽만 세기 | G-18 | 🔴 **미실행 — §5** |
| C-13 | 키 없음 → `NotRegisteredYet` 갈래 제거 | G-14 | 🔴 **미실행 — §5** |

**10/14 확인 · 4건은 DB 계측이 막혀 못 했다.** 못 한 것을 한 것처럼 적지 않는다.

---

## 5. 🔴 DB 게이트를 이 PC 에서 계측할 수 없다 — 실측한 이유

| 확인한 것 | 실측 결과 |
|---|---|
| `root` / 빈 비번 | ❌ 접속 거부 |
| `root` / `Hitpan2025!` | ❌ 접속 거부 (**root 비번은 사장님만 아신다**) |
| `hitpan` / `Hitpan2025!` | 🟢 접속됨 |
| `hitpan` 의 `CREATE DATABASE` | ❌ `ERROR 1044 Access denied` |
| `hitpan` 의 실제 권한 (`SHOW GRANTS`) | `GRANT ALL` 이 **딱 4개 DB 이름에만** — `hitpan_erp` · `hitpan_backoffice` · `hitpan_e2e` · `hitpan_trgtest`. 그 밖의 이름은 만들 수 없다 |
| 이 PC 에 실재하는 스키마 | `hitpan_backoffice`(19표) · `hitpan_e2e`(152표) · `hitpan_trgtest`(32표). **`hitpan_erp` 는 없다** |

⇒ 게이트는 격리 DB(`hitpan_mainpc_seal_<guid>`)를 만들어 **출하 DDL 한 방**을 넣는 방식이다(#36 · 형제 게이트와 동일).
   그 이름은 `hitpan` 권한 밖이므로 **로컬에서는 만들 수 없다.**

🔴 **하지 않은 것 — 그리고 그 이유**
- `hitpan_e2e` · `hitpan_trgtest` 를 **DROP 해서 재사용하지 않았다.** 사장님 PC 의 기존 자료다.
  지우는 것은 되돌릴 수 없고, 제 판단으로 할 일이 아니다.
- **GRANT 를 주지 않았다.** DB 권한 변경은 인프라 조작이다 — **#29 사전 승인제**.
- `hitpan_erp` 를 **새로 만들지 않았다.** 그 이름은 히트판이 쓰는 이름이다(CLAUDE.md).

### 🟢 5-2. CI 계측 완료 — 실제 MariaDB 에서 5건 전부 PASS

사장님 결재 「CI 에 맡긴다」에 따라 push(→ draft PR #408 재기동) 후 계측했다.

```
run 35740148757 · SHA 2cf53752 · 잡 「DB 게이트 (Integrity — 실제 DB 실행)」

Passed G-14  미등록·다른PC·등록된그PC 세 갈래를 정확히 가른다            [716 ms]
Passed G-15·G-16·G-11a  메인PC 를 옮겨도 줄은 1개 · 옛 PC 없이 된다       [705 ms]
Passed G-16b 없는 기기로 등록하면 실패로 답한다                          [759 ms]
Passed G-18  거래처·상품이 둘 다 0 일 때만 「자료 없음」으로 본다          [735 ms]
Passed G-19a 복원 → 봉인 불일치 → 재등록까지 전이가 끊기지 않는다         [789 ms]

잡 집계: Passed 314 / Failed 1
스텝 「Gate — SKIP 이 한 건도 없어야 한다」: success  ⇒ 건너뛴 게이트 0건
```

🟢 **각 700~790ms 는 출하 DDL 을 실제로 적재하고 서비스를 부른 시간이다** — 로컬의 12~14ms(=건너뜀)와 대조된다.
🟢 **필터에 실제로 물렸다** — 잡 로그의 `▶ 이 잡이 무는 게이트:` 줄 꼬리에 `MainPcSealRegistryGate` 가 있다
(N-1 사고가 「등록했는데 클래스 이름이 달라 안 물린」 것이었으므로 이름이 뜨는 것까지 봤다).
🔴 **같은 잡의 유일한 실패는 별건 A(G-RC13)** 이고 이 게이트와 무관하다 —
사장님 결재로 *"초록으로 만들지 않는다"* 로 둔 그 건이다.
🔴 **C-10~C-13 대조실험은 여전히 미실행이다** — 봉합을 빼고 CI 를 다시 돌려야 하는데
그것은 PR 에 일부러 깨진 커밋을 올리는 일이다. 사장님 결재 없이 하지 않는다.

🟢 **CI 는 로컬의 문제가 없다** — `db-gate` 잡이 컨테이너 MariaDB 에 `root`/`dbgate_ci` 로 붙고
`HITPAN_REQUIRE_DB=true` 라 **건너뛰면 그 자체가 실패**다(`build-and-test.yml:183`).
⇒ 🔴 **`db-gate` 잡이 이 5건의 유일한 계측 경로다.** 등록은 마쳤다(§1).
⇒ ⚠️ 다만 그 잡은 **PR 에서만 깨어난다**(feat 브랜치 push 로는 안 돈다 — 9/22 인계1 에서 실측된 사실).

**⇒ 사장님 결재 대기: §7-1**

---

## 6. 🔴 이번에 드러난 별건 2건 (이번 범위에서 고치지 않았다)

### 별건 ① — DB-126 이 출하 DDL 시드에 빠졌다 (작2 절A)

전체 시험을 돌려 나온 실패 1건이다. **내 변경과 무관하다** —
내가 건드린 파일은 시험 2개·워크플로·설계문서뿐이고(`git diff --stat`), 이 게이트는
`src/HitPan.API/Migrations/SQL/*.sql` 과 `installer/hitpan_db_clean.sql` 만 읽는다.

```
HitPan.Tests.Integrity.MigrationLocationGuardTests.모든_마이그레이션이_clean_DDL_시드에_등록돼_있다 [FAIL]
  clean DDL 의 schema_migrations 시드에 빠진 마이그레이션이 있다 — 빠진 것: DB-126
```

실측 근거: `installer/hitpan_db_clean.sql` 의 `schema_migrations` 시드가 **`('DB-125','clean-ddl',1);` 에서 끝난다.**
칸(`mainpc_sealed_key`·`mainpc_key_issued_at`)은 출하 DDL 표 정의에 **들어가 있다.** 빠진 것은 **「이미 반영됨」 표시**뿐이다.

- **영향** — 🟡 신규 설치 고객이 첫 부팅에 DB-126 을 **다시 돌린다.** DB-126 은 `ADD COLUMN IF NOT EXISTS` 로
  **멱등**이라(95~106줄) 실제 피해는 없다. 그러나 이 게이트는 **DB-89 가 죽은 그 사고**를 막기 위해 서 있는 것이고,
  지금 **빨간불**이다.
- **고침** — 시드 꼬리 한 줄: `('DB-125','clean-ddl',1);` → `('DB-125','clean-ddl',1),('DB-126','clean-ddl',1);`
- 🟢 **사장님 결재 「지금 고친다」(2026-09-22) — 고쳤다.** 고친 뒤 전체 회귀 **1550/1550 통과 · 실패 0 · 건너뜀 0**.
  🟢 **대조 양쪽을 다 봤다** — 고치기 전 이 게이트는 **빨간불**이었고(위 출력), 한 줄 더한 뒤 초록이 됐다.
  그 한 줄이 판정을 바꾼 것이 맞다.

### 🚨 별건 ③ — **P0. ②가 미들웨어에 잘린다. 왕복 증명이 실물에서 절대 완성되지 않는다**

실측 준비로 이 PC 의 API 를 두드려 보다가 나왔다. **가설이 아니다 — 돌려서 쟀다.**

```
TenantMiddleware 를 직접 불러 잰 것 (DB·포트·설치본 무접촉)

/api/devices/mainpc-proof         → status=401  컨트롤러 도달=False   🔴
/api/auth/update-status-local     → status=200  컨트롤러 도달=True    (대조군 — 화이트리스트에 있다)
/api/devices/zzz-없는-길          → status=401  컨트롤러 도달=False   (대조군 — 없는 길)
```

- `src/HitPan.API/Middleware/TenantMiddleware.cs:71` 이 **인증 안 된 `/api/*` 를 전부 401** 로 자른다.
  화이트리스트에 `mainpc-proof` 가 **없다**(`grep -c mainpc` = **0**).
- ②는 설계상 **토큰을 싣지 않는다** — `hitpan-mainpc-proof.js:51` 이 `credentials: 'omit'` 이고
  `Authorization` 헤더도 없다. 설계문서·인터페이스 주석이 *"로그인 토큰 없이 지나간다"* 로 그렇게 정해 놨다.
- `[AllowAnonymous]` 는 **소용이 없다** — 이 미들웨어가 라우팅·인가보다 앞에 있다.
- 나머지 미들웨어는 벽이 아니다(확인함): `TermsConsent`·`SessionLimit` 은 인증 안 된 요청을 통과시키고,
  `Idempotency` 는 `[IdempotencyKey]` 가 붙은 엔드포인트에만 걸린다. **벽은 이 한 곳뿐이다.**

🔴 **무슨 뜻인가** — 왕복의 ②가 컨트롤러에 닿지 못하므로 `confirmed` 가 영원히 `false` 다.
⇒ **모든 PC 가 클라이언트로 판정된다.** 등록 팝업도 안 뜨고, 자료관리도 안 열린다.
⇒ **사장님이 보신 증상 ②③ 이 하나도 봉합되지 않았다.** 절A~절F 의 구조는 맞는데 **길이 막혀 있다.**

🔴 **세 번째 재발이다.** 같은 파일 주석이 이미 두 번 적고 있다 —
*"엔드포인트에 `[AllowAnonymous]` 가 붙어 있는데도, 그 앞의 이 미들웨어가 먼저 401 로 잘랐다"*
(20260825작10 `update-status-local` · 20260825작12 `update-consent-local`).

🔴 **내가 오늘 붙인 게이트 18건은 이것을 못 잡는다.** 게이트는 `MainPcOnlyAttribute` 와
`MainPcProofService` 를 **따로** 부른다 — **미들웨어 파이프라인을 지나가 보지 않는다.**
⇒ §8 「이 시험이 못 하는 것」에 이 줄을 더한다. 실측이 왜 필요했는지가 바로 이것이다.

**고침(추정 · 결재 대기)** — `TenantMiddleware` 화이트리스트에 **그 주소 하나만** 더한다.
같은 파일의 원칙을 그대로 따른다: *"🔴 `/api/auth` 를 통째로 열지 않는다 — 이 주소 하나만 연다."*
⇒ 열어도 안전하다고 볼 근거: 컨트롤러가 **스스로 `IsLocalConsole` 을 본다**(터널 헤더 있으면 즉시 거절),
표는 **1회용·60초·세션 묶음**이고 회사 식별자도 **표 안에서** 나온다. `update-status-local` 을 연 논리와 같다.
⚠️ 그래도 **보안 상무 검토 대상**이다 — 인증 면제 경로를 하나 늘리는 일이다.

### 별건 ② — 세금계산서 마스터키와 메인PC 봉인키가 **같은 TPM 키 이름을 쓴다**

`TpmKeyService.SealKey` 가 `"HitPan.TaxInvoice.MasterKey"` 를 `OverwriteExistingKey` 로 만든다(§2).
⇒ **메인PC 등록이 세금계산서 마스터키를 무효화할 수 있다.** 이미 봉인해 둔 값이 있으면 못 푼다.
⇒ ⚠️ **가설이다 — 실측하지 않았다.** 실측하려면 이 PC 의 TPM 키를 덮어써야 하므로 하지 않았다.
⇒ 별도 선행검증 대상으로 **등록만** 한다. 이번 범위 밖.

---

## 7. ⚠️ 사장님 결재가 필요한 것

| # | 쟁점 | PM 의견 |
|---|---|---|
| 7-1 | **DB 게이트 5건을 어디서 계측할까** | 🟢 **사장님 결재 「CI 에 맡긴다」** — push 후 draft PR 로 `db-gate` 잡에서 잰다. 개발 PC 권한은 늘리지 않는다(#29·#39). ⚠️ **C-10~C-13 대조실험은 여전히 부방**이다 |
| 7-2 | **별건 ① 시드 한 줄을 지금 고칠까** | 🟢 **사장님 결재 「지금 고친다」 — 고쳤다.** 전체 회귀 1550/1550 (§6 별건 ①) |
| 7-3 | 별건 ② 실측 시점 | 🟡 이 PC 에서는 못 한다(TPM 키를 덮어쓴다). 예비 PC 나 백지환경에서 |
| 7-4 | **그 다음 순서** | 🟢 **사장님 결재 「실물 실측」** — M-1 로 간다 |

---

## 8. 이 시험이 못 하는 것 — 🔴 초록불을 실측으로 읽지 마라

두 파일 머리 주석에 같은 내용을 새겨 뒀다.

- **브라우저가 실제로 loopback 을 두드리는지 못 잰다** (G-1b). 🔴 헤드리스로 대신 재면 **결과가 정반대**로 나온다(인계3 §6-2)
- **자료관리 메뉴가 열리는지 못 잰다** — 재는 것은 `IsMainPc` 판정값까지다
- **팝업 횟수를 못 잰다** (G-11b · G-12 · G-20)
- **키가 물리적으로 그 PC 에 묶였는지 못 잰다** (G-13b)
- **실제 백업/복원을 못 돌린다** (G-19b) — 재는 것은 복원이 만든 **DB 상태의 전이**다
- **실물 TPM 봉인 경로는 한 번도 안 돌았다** (§2)
- **G-7c 는 소스 회귀 감시이고 동작 시험이 아니다** — `.razor` 는 bUnit 없이 못 띄운다.
  "껐다는 표시가 세 자리에 그대로 있다" 까지가 범위다
- **G-8(브라우저 교체) · G-20(두 갈래) · G-21(갈 곳 안내)은 붙이지 않았다** — G-21 은 M-3(랜딩 다운로드 주소)·M-5(문구)가 확정돼야 한다
- 🔴 **미들웨어 파이프라인을 지나가 보지 않는다.** 게이트는 `MainPcOnlyAttribute`·`MainPcProofService` 를
  **따로** 부른다. 그래서 **별건 ③(P0 · ②가 401 로 잘린다)을 18건 전부가 못 잡았다.**
  ⇒ 게이트가 초록이어도 **실제 요청이 그 함수까지 닿는지는 다른 질문**이다. 이것이 실측의 몫이다

🔴 **이 전부가 초록이어도 M-1(실물 실측)은 여전히 0회다. 그것이 다음 단계다.**

---

## 9. 헌법 확인

| 조항 | 지킨 방법 |
|---|---|
| #1 | 시험 파일 **신설만** · 생산코드 0줄 · G-3 대조는 일시 제거 후 `git checkout --` 복원 · **커밋 0** |
| #19 | 빌드 경고 0 · 오류 0 (`/warnaserror`) |
| #29 | DB 권한·서비스·레지스트리 **무접촉.** GRANT 를 주지 않았다(§5) |
| #32 | CI 등록 근거를 **초록불이 아니라 무력화 시험**(§4)으로 적었다 |
| #36 | 격리 DB 는 출하 DDL 한 방. 손 DDL 없음. 출하 DDL **수정 안 함**(별건 ①은 결재 대기) |
| #39 | 운영 무접촉 — 기존 DB 를 **하나도 지우지 않았다**(§5) |
| #41 | 설계 §8 G-17 줄은 **지우지 않고 상태만** 바꿨다 |
| #42 | 작지(결재) → 이 명세서 → [4] 로 이어진다 |
