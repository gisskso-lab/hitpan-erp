# 개발명세서 2026-09-17 — 20260915작1 개정 3판 갈래 R1 (공용 식 · 수금·지급 서버)

| 항목 | 내용 |
|---|---|
| 근거 작업지시서 | `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` §15-0~15-3 · §15-9 |
| 근거 설계 | `docs/설계/erp/20260915_설계_자료이관_머리없는줄_분류봉합.md` §20·§21·§22·§25(G8)·§27(P2~P4·S2) |
| PM 결재 | ✅ 2026-09-17 조건부 승인(작지 §15-9) · S2 = 사장님 여쭘(기본값 추천안으로 구현) |
| 개발팀 지휘 | PM 브라운킴 |
| 구현 담당 | hp-implementer (갈래 R1 · worktree `agent-a1ed74145175a2f3c`) |
| 기준 | `feat/20260915-migration-posting` `285dedfc` (ff 병합) |
| 커밋 | 계약 뼈대 `9450df87` · 배선 `6f36ddb2` · G8 `829f7ca9` · 명세서 = 이 커밋 · push 안 함 |

## 0. 진행 상태

| # | 덩어리 | 상태 | 커밋 |
|---|---|---|---|
| 1 | 계약 — `LegacyBalanceMatching.cs`(식·검사 본문 포함) · DTO · 취소 기표 2개 | ✅ 빌드 0/0 | `9450df87` |
| 2 | `CollectionService` 배선 — 등록 검사 · P2 · P3 · 삭제 역분개 · P4 · 요약 `ListAsync` · 목록 `source_type` | ✅ 빌드 0/0 | `6f36ddb2` |
| 3 | G8 a~k + 보강 l~p · 봉합 빼고 FAIL 2회 · 기존 게이트 회귀 | ✅ 16/16 · 회귀 97/97 | `829f7ca9` |
| 4 | PM 후속(§6) — 이관 수금·지급 삭제 거절 · S2 문구 · G8-q | ✅ 17/17 · 빌드 0/0 | `f52d847d` |

## 1. 사실 (첫 턴 확인 · 격리 33306 `hitpan_e2e_gi` 실측)

| # | 확인 | 결과 |
|---|---|---|
| ③ 미실측 | `journal_entries` source UNIQUE | **있다** — `UNIQUE KEY uq_je_source (tenant_id, source_type, source_id)`. 취소 기표는 `source_type` 이 `collection_cancel`/`payment_cancel` 이라 원 분개와 충돌 0. 같은 건 두 번째 취소는 `ShouldReverseAsync` 가 0건 처리(G8-i 에서 두 번 삭제 → 역분개 1) |
| 계약 | `ICollectionService` 시그니처 | **무변경** (파일 미수정 · 구현체 `CollectionService` 1개 · 호출자 `CollectionController` 4곳 그대로) |
| #13 | DESCRIBE | `collections`(ref_doc_type varchar(30) · source_type varchar(30)) · `payments`(payment_type varchar(20) NOT NULL · CHECK 없음) · `partner_legacy_balances`(UNIQUE tenant_id+partner_id) · `journal_entries`·`journal_lines`(FK `fk_jl_account` → accounts(tenant_id, account_code)) · `sales_deliveries`·`purchase_receipts`(FK partner · pr 에 is_deleted 칼럼 없음) · `purchase_returns`·`purchase_return_items` · `monthly_closing` · `partners`(PK partner_id 하나) |
| 접근성 | `AutoJournalHelper` | `internal static class` — 계약의 `public static` 메서드는 클래스 접근성 안에서 public. 클래스 접근성은 안 바꿨다(#1) |

## 2. 무엇을 만들었나 (작지 §15-3 대비)

| 요구 | 구현 | 상태 |
|---|---|---|
| 계약 상수·식 | `LegacyBalanceMatching.cs:27·30·36·56·84·125·136·147` — 파생표 열 `partner_id, base_date, legacy_amount, matched_amount, remaining_amount` · 파라미터 `@TenantId` | ✅ |
| M 정의(§20 표) | 수금: `ref_doc_type='legacy_balance' AND ref_doc_id=partner_id` + E 호환(사람 수금 → 이관 명세서 · `sd.partner_id`) · 지급: `payment_type='legacy_balance' AND ref_order_id=partner_id` + 이관 매입 사람 지급 + 이관 매입 확정 반품 · 전부 `is_active=1` · `COALESCE(source_type,'')<>'migration'` · 키 = partner_id | ✅ |
| 명세서 쪽 조건 | M 의 **정확한 여집합** + 종전 규칙(이월잔액 회사의 이관 수금·지급 제외) — 이월잔액 없는 회사는 종전 필터와 같은 값(G8-k 실측 35,000 = 35,000) | ✅ |
| `ListAsync` | `:163` — L0>0 또는 M≠0 인 거래처 · R<0 그대로(클램프 없음) | ✅ |
| `GetForUpdateAsync` | `:181` — `partner_legacy_balances` 행 `FOR UPDATE` 잠금 뒤 R 재계산 · 행 없으면 null | ✅ |
| `EnsureMatchAllowedAsync` | `:204` — ① ref=partner ② 금액>0 ③ 행·방향 ④ S2 ⑤ 금액≤R · 실패 = `InvalidOperationException` 고객 문구 | ✅ |
| S2 상수 1곳 | `RejectBeforeBaseDate = true` · 판정 `IsBlockedByBaseDate` `:228` 한 곳 | ✅ (§5-1 PM 확인) |
| ① 등록 검사 | `CollectionService.cs:133·291` → `EnsureCollectionTargetAllowedAsync:670` · `EnsurePaymentTargetAllowedAsync:709` — 트랜잭션 안 INSERT 앞 | ✅ |
| P2 명세서 초과 | 명세서(`confirmed`/`invoiced` · 미삭제 · 같은 거래처)·매입(`confirmed` · 같은 거래처) 행 `FOR UPDATE` → 남은 금액 = 합계 − 활성 수금(지급 − 확정 반품) · 초과·대상 없음 거절. ref 없는 수금·지급 · 다른 ref 종류는 종전대로 검사 없음 | ✅ |
| P3 | 이월잔액 회사(`HasLegacyBalanceSql`)의 이관 명세서·매입에 새 수금·지급 거절 | ✅ |
| ② 삭제 역분개 | `:232·385` 같은 트랜잭션 · 날짜 = 원 수금·지급일 · 원 분개(`collection`/`payment`)가 **있을 때만** 뒤집음 | ✅ |
| P4 | `:206·361` 원 날짜 월마감 검사(등록과 같은 `EnsureNotClosedAsync`) | ✅ |
| 삭제 동시성 | UPDATE 에 `AND is_active = 1` · 0행이면 롤백·종료(잔액·역분개 두 번 방지) | ✅ (지시 밖 보강 · §5-3) |
| ③ 요약 | `GetLegacyOutstandingAsync:593` → `ListAsync` · R>0 만 요약 합산 · 응답 `LegacyBalances`(`:491·580`) | ✅ |
| ④ 목록 `source_type` | `:35·87·262` — SELECT 칼럼만 추가 · **WHERE 필터 추가 0**(사장님 원칙 · G8-e 에서 이관 줄 목록 표시 실측) | ✅ |
| 취소 기표 2개 | `AutoJournalHelper.cs:637·669` — 수금 취소 차 외상매출금/대 현금·보통예금 · 지급 취소 차 현금·보통예금/대 외상매입금 | ✅ |
| DTO | `CollectionListDto.SourceType` · `PaymentListDto.SourceType` · `LegacyBalanceRowDto` · `LegacyBalances` 2 | ✅ |

## 3. 변경 파일 (5개 + 명세서 · 다른 갈래 파일 0)

| 파일 | 변경 |
|---|---|
| 🆕 `src/HitPan.Application/Services/LegacyBalanceMatching.cs` | +248 |
| `src/HitPan.Application/Services/CollectionService.cs` | +184 / −59 (−59 = 갈래 E 옛 이월 SQL 2개 → 공용 식으로 대체 · 지시 ③) |
| `src/HitPan.Application/Services/AutoJournalHelper.cs` | +84 (추가만) |
| `src/HitPan.Application/DTOs/Approval/CollectionDtos.cs` | +19 (추가만) |
| 🆕 `src/HitPan.Tests/Integrity/LegacyBalanceMatchGateTests.cs` | +546 |

- DDL·마이그 SQL·출하 DDL·appsettings 무접촉 · 새 테이블·칼럼 0 (#36 해당없음).

## 4. 자체 확인 결과 (실측)

| 항목 | 명령 | 결과 |
|---|---|---|
| 빌드 #19 | `build-brief.ps1` Application · Tests · Web | 3개 모두 errors 0 / warnings 0 |
| G8 | `HITPAN_DB_HOST=127.0.0.1 HITPAN_DB_PORT=33306 HITPAN_REQUIRE_DB=1` `test-brief.ps1 -Filter FullyQualifiedName~LegacyBalanceMatchGateTests` | **16 통과 / 0 실패 / 0 건너뜀** |
| 🔴 봉합 빼고 A (치환 건수 각 1) | ① `if (amount > row.RemainingAmount)` 무력화 ② lm `COALESCE(lc.source_type,'')<>'migration'` 삭제 ③ lm `AND lc.is_active = 1` 삭제 ④ `RecordCollectionCancelAsync` 호출 무력화 | **5 실패 / 11 통과** — b · e · f · h · i (기대와 정확히 일치) |
| 🔴 봉합 빼고 B (치환 건수 1·1·1·1·2·2·1·1) | ① 방향 검사 `|| row.LegacyAmount <= 0m` 삭제 ② ref=partner 검사 삭제 ③ `RejectBeforeBaseDate=false` ④ 미수 R 식에서 E 호환 `em` 삭제 ⑤ P2 초과 검사 2곳 무력화 ⑥ P3 검사 2곳 무력화 ⑦ P4 삭제 월마감 2곳 삭제 | **7 실패 / 9 통과** — c · d · l · m · n · o · p (기대와 정확히 일치) |
| 원복 | `git checkout --` 두 파일 → 재실행 | 16/16 |
| 회귀 | 같은 환경 `-Filter` G8 + G1 `MdbPostingClassify` · G2 `MdbPostingAllPaths` · G3 `MdbLegacyFinalStock` · G4 `MdbLegacyBalance` · G5 `TaxInvoiceMigratedLock` · G6 `MdbReconPosting` · G7 `LegacyUnpostedDdl` · `LegacyUnpostedViewTenant` · `MdbLegacyWiring` · `MoneyFlowJournal` · `~Collection` | **97 통과 / 0 실패 / 0 건너뜀** (3분 35초) |
| 격리 DB 정리 | `SHOW DATABASES LIKE 'hitpan_r1%'` | 0개(픽스처가 DROP) |

### G8 사례표

| 사례 | 내용 | 결과 |
|---|---|---|
| a | 이월 수금 30,000 → R 70,000 · total_receipt 30,000 · 분개 1 | PASS |
| b | 70,001 거절 · DB 스냅숏(수금·지급·분개·줄·잔액 6칸) 무변화 · 70,000 경계 허용 | PASS |
| c | 미지급 거래처 수금 · 미수 거래처 지급 거절(문구 대조) | PASS |
| d | ref ≠ partner 거절 | PASS |
| e | migration 수금·지급 M 제외 · 목록 `SourceType='migration'` 표시 | PASS |
| f | is_active=0 M 제외 | PASS |
| g | 이월잔액 행 삭제·재삽입(balance_id 다름) 뒤 M 30,000 유지 | PASS |
| h | 이월 지급 50,000 → R 30,000 · 30,001 거절 | PASS |
| i | 삭제 → 역분개 1(두 번 삭제해도 1) · 날짜 = 원 날짜 · 계정별 순액 0 · R·total_receipt 복원 | PASS |
| j | 요약 = R · `LegacyBalances` 한 줄(이름·기준일·L0·M·R) · R=0 이면 사라짐 | PASS |
| k | 대조군(이월잔액 없음): ListAsync 빈 목록 · 요약 종전 값 125,000 · 명세서 쪽 조건 = 종전 필터 | PASS |
| l | S2 기준일 당일·이전 거절 · 다음 날 허용 | PASS |
| m | P2 초과·없는 명세서 거절 · 경계 허용(수금·지급) | PASS |
| n | P3 이관 명세서·매입 거절 | PASS |
| o | P4 마감 달 삭제 거절 · DB 무변화 | PASS |
| p | E 호환 M(수금 5,000 · 지급 3,000) · 명세서 쪽 합계에서 빠짐 | PASS |

## 5. 안 한 것 / 못 한 것 / PM 결정 필요

1. ⚠️ **S2 경계 — 기준일 당일도 막는다**(`date <= base_date`). 설계 문구는 「기준일 **이전**」이지만 레거시 최종잔액은 기준일까지의 거래를 품으므로 당일도 두 번 빼기다(§15-9 「2/20~2/28」 표현과 같은 해석). 문구도 「기준일(yyyy-MM-dd) 이전 날짜는…」 그대로라 당일 거절 때 어색할 수 있다 → PM 확인. 바꾸려면 `IsBlockedByBaseDate` 한 줄.
2. ⚠️ **P2 명세서 status** — `confirmed`·`invoiced` 둘 다 허용(대시보드 식과 같게). 수금 화면 목록은 `confirmed` 만 보인다. `invoiced` 전이 경로는 미실측④ 그대로 → [4] 확인 대상.
3. 지시 밖 보강 1건 — 삭제 UPDATE 에 `AND is_active = 1` + 0행 롤백. 역분개 신설로 동시 삭제 시 잔액·역분개 이중이 생길 수 있어 넣었다. 빼라면 두 줄.
4. **역분개는 원 분개가 있을 때만** — 이관 수금·지급(분개 `migration` 계열)과 20260827작4 이전 수금은 삭제해도 역분개 0. 과거 취소분 치료는 범위 밖(작지 §15-0) 그대로.
5. 명세서 쪽 조건의 이관 제외는 **거래처 단위 M 여집합**(설계 문언) — 이월잔액 회사인데 이월잔액 행이 **없는** 거래처의 이관 명세서에 붙은 사람 수금은 M 에도 안 들고 명세서 쪽에 남는다(종전 대시보드와 같은 값). P3 로 새로 생기지는 않는다. R2 가 #1·#9 교차 일치를 볼 때 참고.
6. 안 한 것(지시대로 다른 갈래): 11곳 소비자 식(R2) · 화면 칩·이월 한 줄·Web 문구 전달 메서드(R4) · CI GATES 등록(Z) · push.
7. 삭제 역분개의 `employeeId` 는 null(삭제 메서드 시그니처에 사용자 인자가 없음 · #1 로 시그니처 유지).

> 상태(§6 반영 뒤): 1 = ✅ PM 승인·문구 교체 · 2·3·4 = ✅ PM 승인(그대로) · 5~7 = 기록 유지.

## 6. PM 후속 (브라운킴 · 2026-09-17) — 반영 기록 · 커밋 `f52d847d`

| # | PM 지시·결정 | 반영 | 실측 |
|---|---|---|---|
| 1 | 🔴 새 결함 — 이관 수금·지급(`source_type='migration'`)도 삭제되며 `partner_balance` 를 깎았다(이관은 잔액 칸 무기록 · 사장님 원칙 「레거시 줄은 빼지 않는다」 위반) | `DeleteCollectionAsync`·`DeletePaymentAsync` 조회에 `source_type` 추가 → `migration` 이면 `InvalidOperationException` 「이전 프로그램에서 옮겨 온 수금(지급)은 삭제할 수 없습니다.」 · 트랜잭션·월마감 검사 전에 거절 → DB 무변화. 문구 상수 `CollectionService.MsgMigratedCollectionDelete`·`MsgMigratedPaymentDelete` | G8-q: 사람 이월 수금·지급으로 잔액 칸이 있는 상태에서 이관 줄 삭제 → 거절 · 스냅숏(건수·분개·줄·잔액 합) 무변화 · is_active=1 · 분개 0 · 목록에 남음 |
| 2 | S2 `<=`(기준일 당일 포함) **승인** · 문구를 규칙과 맞게 | `LegacyBalanceMatching.MsgBeforeBaseDate` 한 곳: 「이전 프로그램 기준일({0})까지의 {1}은 이미 이월잔액에 들어 있어 맞출 수 없습니다. 그 뒤에 {2} 돈이면 {2} 날짜로 입력해 주세요.」 · {1}=수금/지급 · {2}=받은/준 | G8-l 문구 대조(수금·지급 둘 다) |
| 3 | P2 `invoiced` 포함 · 삭제 이중 가드 · 원 분개 없으면 역분개 없음 — **승인(그대로)** | 코드 변경 없음 | — |

- ⚠️ 지급 문구의 「받은 돈」은 「준 돈」으로 적용했다(「지급은 『지급』」의 자연스러운 적용 · 다르면 상수 한 줄).
- 🔴 봉합 빼고: 삭제 거절 두 곳 무력화(치환 건수 1·1) → **G8-q 만 FAIL(1 실패 / 16 통과)** → `git checkout` 원복 → **17/17**.
- 빌드: Tests(Application·API 포함) · Web errors 0 / warnings 0.
- 기존 게이트 중 수금·지급 **삭제**를 부르는 테스트는 G8 밖에 0건(Grep `DeleteCollectionAsync|DeletePaymentAsync` → `LegacyBalanceMatchGateTests.cs` 1파일) — 회귀 97건은 후속 뒤 재실행하지 않았다.

## §7 R1b — 병렬이슈 42·43·44·46 (작지 §15-12)

| 항목 | 내용 |
|---|---|
| 구현 담당 | hp-implementer (갈래 R1b · worktree `agent-a43db121a06008fea`) |
| 기준 | `feat/20260915-migration-posting` `347e31bb` (ff 병합) |
| 수정 파일 | `CollectionService.cs` · `LegacyBalanceMatching.cs` · `LegacyBalanceMatchGateTests.cs` 만 |

### 7-0. 진행 상태

| # | 덩어리 | 상태 | 커밋 |
|---|---|---|---|
| 0 | 뼈대(이 절) | ✅ | `084b88c0` |
| 1 | 42 유형값 정규화 · 43 금액 검사 · 44 잠금 읽기 · 46 삭제 월마감 tx 안 | ✅ 빌드 0/0 | `31c1999a` |
| 2 | G8-r~v · 새 문구 상수 3개 public | ✅ 22/22 | `b3e91a7f` |
| 3 | 봉합 빼고 FAIL 4회 · 회귀 · 명세서 | ✅ | `be50b596` |
| 4 | PM 후속 2 — 44 를 READ COMMITTED 로 교체(파생표 잠금 읽기 폐기) · G8-w·x · u 를 RC 로 | ✅ 24/24 · 빌드 0/0 | `6435a7fa` |
| 5 | 명세서 §7 갱신(PM 후속 1~3) | ✅ | 이 커밋 |

> **PM 판정(2026-09-17 · R1b 후속)**: ① 지급 `payment`·`receipt` 유지 승인 ② 44 교착 **불수용**(직원 둘이 다른 거래처 동시 수금 → 500 은 「처음 쓰는 직원이 혼자 쓸 수 있냐」 탈락) → READ COMMITTED 대안 ③ 46 private 검사 승인 · 중복 SQL 은 백로그. 아래 7-2·7-3 의 44 줄은 **후속 2 반영 뒤 기준**이다(최초 SHARE MODE 방식 기록은 7-1 실측에 남김).

### 7-1. 사실 (첫 턴 확인)

| # | 확인 | 결과 |
|---|---|---|
| 42 전수 | `collections.ref_doc_type` 에 쓰는 값 — Grep Web·API·Application·테스트·scripts | 서버 입구(`CollectionController` → `CreateCollectionAsync` 1곳)로 들어오는 값: Web `CollectionPage.razor:495·552` `sales_delivery` · 이월 매칭 `legacy_balance` · ref 없음(null). 이관(`MdbMigrationService:3014~3150`)은 벌크 INSERT 로 서버 입구를 안 거친다. 기타 테스트·scripts 시드 = `sales_delivery` 만. 출하 DDL = `varchar(30) DEFAULT NULL`, 주석에만 `delivery, tax_invoice`(쓰는 코드·읽는 코드 0) |
| 42 전수 | `payments.payment_type` | Web `PaymentPage.razor:454·511` `purchase` · DTO 기본값 `purchase` · 이월 `legacy_balance` · **`payment`**(`MoneyFlowJournalGateTests.cs:102` · 출하 DDL 뷰 `v_partner_payments_total`) · **`receipt`**(출하 DDL 뷰 `v_partner_receipts_total` · 칼럼 주석). 출하 DDL = `varchar(20) NOT NULL` 기본값 없음 · CHECK 없음 |
| 42 콜레이션 | 두 칼럼 `utf8mb4_unicode_ci` | 대소문자·끝 공백(PAD SPACE)뿐 아니라 전각 글자(`ｌegacy_balance`)도 같게 볼 수 있어 **Trim+소문자만으로는 못 닫는다** → 정규화 뒤 목록과 Ordinal 정확 일치 · 목록 밖 거절로 구현(G8-r·s 에 전각 사례) |
| 44 실측 | MariaDB 11.4.10 · RR · 두 세션(잠금 전 일반 SELECT → 다른 세션 60,000 INSERT 커밋 → 재계산) | 일반 SELECT = 100,000(옛 값) · **바깥에만 `LOCK IN SHARE MODE` = 100,000(옛 값)** · `FOR UPDATE` 바깥만 = 100,000 · **GROUP BY 파생표 안에 `LOCK IN SHARE MODE` = 40,000(최신)** → (최초) 파생표마다 잠금 절 |
| 44 교착 실측 (PM 후속 2 · 1회) | SHARE MODE 방식 그대로 G8-w(두 연결 · 다른 거래처 PA·C · 둘 다 잠금·검사 → 둘 다 INSERT) | **재현됨** — c1 `MySqlException: Deadlock found when trying to get lock; try restarting transaction`(1213) · c2 성공. → 방식 폐기 |
| RC 전제 | 격리 서버 `@@log_bin=0` · `@@binlog_format=MIXED` · `innodb_deadlock_detect=1` | RC 는 `binlog_format=STATEMENT` + 바이너리 로그 켠 서버에서 쓰기 거절. 설치본 `HitPan-Universal.iss:2169` 는 log_bin ON 가능성만 언급 · binlog_format 설정 0건 → MariaDB 기본 MIXED 로 동작 ⚠️고객 PC 실측 안 함 |
| 46 | `ApprovalTriggerHelper.EnsureNotClosedAsync` tx 오버로드 | 없음. 헬퍼 파일은 이 갈래 수정 목록 밖 → `CollectionService` 안에 private `EnsureNotClosedInTxAsync`(같은 SQL + `LOCK IN SHARE MODE` · 같은 문구) 추가. 마감 처리(`MonthlyClosingService.cs:126`)는 `INSERT … ON DUPLICATE KEY UPDATE`(자동 커밋) → 같은 행/틈 잠금과 겹쳐 순서가 선다 |
| #13 | DESCRIBE(격리 `hitpan_e2e_gi`) | `collections.amount decimal(15,2)` · `ref_doc_type varchar(30) NULL` · `payments.payment_type varchar(20) NOT NULL` · `monthly_closing`(uq_tenant_ym) · 인덱스 collections `idx_tenant_partner` · payments `idx_pay_partner`·`idx_pay_tenant` |

### 7-2. 무엇을 만들었나

| 이슈 | 구현 (파일:줄) |
|---|---|
| 42 | `CollectionService.cs:127·296` 입구에서 `NormalizeCollectionRefType:709`(빈 값 → null · Trim+소문자 → `KnownCollectionRefTypes:700` = `sales_delivery`·`legacy_balance` 정확 일치 · 밖 = 거절) · `NormalizePaymentType:719`(빈 값·null 도 거절 · `KnownPaymentTypes:706` = `purchase`·`legacy_balance`·`payment`·`receipt`). 정규 값을 `request` 에 다시 넣어 뒤의 검사·INSERT 가 모두 정규 값만 본다. 문구 `MsgUnknownCollectionType`·`MsgUnknownPaymentType` |
| 43 | `EnsureAmountValid:728` — `amount <= 0 || decimal.Round(amount,2) != amount` 거절 · 모든 유형(ref 없음 포함) · 월마감·트랜잭션 전. 문구 `MsgAmountInvalid` |
| 44 (후속 2) | `LegacyBalanceMatching.MatchIsolation:209` = `IsolationLevel.ReadCommitted` · `CollectionService.cs:132·301` 수금·지급 **등록** 트랜잭션을 이 수준으로 연다(삭제는 종전). `GetForUpdateAsync:181` 는 공용 식 그대로(파생표 잠금 절 · `BuildLockingSql` **제거**) · 첫 줄 가드 `:185` — 트랜잭션 격리 수준이 RC 가 아니면 `NotSupportedException`(개발 계약 위반 = 500 · 옛 R 로 조용히 판정하지 않음). 거래처 단위 직렬화는 종전 `partner_legacy_balances` 행 `FOR UPDATE`. RC 라 P2(명세서·매입 남은 금액 SUM)도 문장마다 최신 커밋을 본다 |
| 46 | `DeleteCollectionAsync:220`·`DeletePaymentAsync:384` 트랜잭션 첫 문장 `EnsureNotClosedInTxAsync:739`. 기존 트랜잭션 밖 검사는 **남겨 둠**(#1 · 빠른 거절) |

### 7-3. 자체 확인 결과 (격리 33306 · env `HITPAN_DB_HOST=127.0.0.1 HITPAN_DB_PORT=33306 HITPAN_DB_USER=root HITPAN_TEST_DB=hitpan_e2e_gi HITPAN_REQUIRE_DB=1`)

| 항목 | 결과 |
|---|---|
| 빌드 #19 | Application · Tests(API 포함) · Web — errors 0 / warnings 0 |
| G8 | **22 통과 / 0 실패 / 0 건너뜀** (a~q 17 + r~v 5) |
| 🔴 봉합 빼고 42 (치환 1·1 · 정규화 두 줄 주석) | **2 실패 / 20 통과** — G8-r · G8-s |
| 🔴 봉합 빼고 43 (치환 2 · `EnsureAmountValid` 두 곳 주석) | **1 실패 / 21 통과** — G8-t |
| 🔴 봉합 빼고 44 — 최초(SHARE MODE · 치환 1 · 잠금 변형 → 공용 식) | 1 실패 / 21 통과 — G8-u (방식 폐기) |
| 🔴 봉합 빼고 44 — **후속 2 (RC · 치환 1 · `MatchIsolation = ReadCommitted` → `RepeatableRead`)** | **2 실패 / 22 통과** — G8-u · G8-x (`6435a7fa` 위에서 · 원복 24/24) |
| G8 (후속 2 뒤) | **24 통과 / 0 실패 / 0 건너뜀** (a~v 22 + w · x) |
| 회귀 (후속 2 뒤) | `~LegacyBalanceMatchGate` · `~MoneyFlowJournal` · `~Collection` · `~TaxInvoiceMigratedLock` **52/52** (skip 0) · Web·Tests 빌드 0/0 |
| 🔴 봉합 빼고 46 (치환 1·1 · tx 안 검사 → `Task.CompletedTask`) | **1 실패 / 21 통과** — G8-v |
| 원복 | `git checkout --` 두 파일 → 재실행 22/22 |
| 회귀 | `~LegacyBalanceMatchGate` 또는 `~MoneyFlowJournal` 또는 `~Collection` **31/31** · `~TaxInvoiceMigratedLock` 또는 `~ExpenseLinkGuard` **31/31** (skip 0) |
| 격리 DB | 픽스처 `hitpan_r1_g8_*` DROP 확인(`SHOW DATABASES LIKE 'hitpan_r1%'` 0개) · 44 실측용 `hitpan_r1b_probe` DROP |

| 사례 | 내용 |
|---|---|
| r | 수금 `'Legacy_Balance'` −500,000(기준일 이전) · `'legacy_balance '` 70,000(기준일 이전 → S2 문구) · `'LEGACY_BALANCE'` 100,001(초과) · ref 대문자 · `'Sales_Delivery'` 100,001(명세서 초과) · 전각 `ｌegacy_balance` · `delivery` → 전부 거절 + 스냅숏 무변화 · `' Legacy_Balance '` 30,000 → BINARY 저장값 `legacy_balance` · `'SALES_DELIVERY'` → `sales_delivery` · 공백만 → NULL · 최종 M 30,000 · R 70,000 |
| s | 지급 대칭 + `'PURCHASE '` 55,001 매입 초과 · 빈 값·null 거절 · `' Purchase'` → `purchase` · R 50,000 |
| t | −50,000 · 0 · 0.004 · 1.005 × (수금 ref 없음·명세서·이월 / 지급 payment·purchase·이월) 거절 · 이슈43 재현(명세서 160,000) 거절 · 1.25 저장 1.25 |
| u | 두 연결(**RC 트랜잭션** · 후속 2) · 둘 다 잠금 전 일반 SELECT → 먼저 60,000 커밋 → 나중 연결 「40,000원 남았습니다」 거절 · 최종 R ≥ 0 (미수·미지급 둘 다) · RR 트랜잭션으로 `GetForUpdateAsync` → `NotSupportedException` |
| w | 두 연결 · 서로 다른 거래처(PA 100,000 · C 50,000) · 둘 다 잠금·검사 → 둘 다 INSERT → **둘 다 성공 · MySqlException 0** · R 90,000 / 40,000 (SHARE MODE 방식에선 1213 교착) |
| x | 두 연결 · 같은 거래처 · 둘 다 앞선 일반 읽기 · 각 70,000(≤ R) 합 140,000 > 100,000 → 뒤 연결이 거래처 잠금에서 1.5초 넘게 기다림 → 앞 커밋 → 「30,000원 남았습니다」 거절 · 최종 R 30,000 |
| v | 마감 INSERT 커밋 전에 삭제 시작 → 1.5초 안에 안 끝남(기다림) → 마감 커밋 → 「마감된 기간」 거절 · is_active=1 · 취소 분개 0 (수금 3월 · 지급 4월) |

### 7-4. 안 한 것 / 못 한 것 / PM 결정 필요

1. ⚠️ **알려진 지급 종류에 `payment`·`receipt` 를 남겼다** — 화면은 안 쓰지만 MoneyFlowJournal 게이트와 출하 DDL 뷰 2개가 쓰는 값이라 빼면 회귀. `receipt` 는 「받은 돈」 뜻인데 지급 분개(차 외상매입금)로 기표된다(종전과 같음). 뺄지 PM 결정.
2. ⚠️ **수금 `delivery`·`tax_invoice`(DDL 주석에만 있는 값)는 거절** — 쓰는 코드·읽는 코드 0 이라 목록에서 뺐다. 외부 연동이 보낸다면 400 이 된다.
3. ⚠️ **44 잠금 범위·교착** — 잠금 읽기가 거래처 하나로 좁혔어도 파생표 em(`collections ec` ⋈ 명세서)·rm 은 테넌트 범위 인덱스를 훑을 수 있어 S 틈 잠금이 넓다. 같은 회사에서 **다른 거래처 두 건을 동시에 이월 매칭**하면 INSERT 끼리 교착(1213)으로 한쪽이 롤백(500)될 수 있다 — 자료는 안 틀어지고 재시도하면 된다. **⚠️미실측**(동시 교착 재현 안 함). 정합성 우선으로 받을지, 트랜잭션을 READ COMMITTED 로 여는 대안(호출자 파일 · R2/R4 계약 영향)으로 갈지 PM 결정.
4. 46 은 헬퍼(`ApprovalTriggerHelper`)에 tx 오버로드를 **추가하지 않았다** — 지시의 수정 파일 목록 밖. 같은 SQL 을 `CollectionService` private 로 복제(문구 동일). 헬퍼로 옮기려면 별 갈래.
5. 등록(`CreateCollectionAsync`·`CreatePaymentAsync`)의 월마감 검사는 여전히 트랜잭션 밖(종전 · 이슈46 은 삭제만 지적) — 같은 경합 창이 있다. 이번 범위 밖.
6. 이슈44 의 「매입 반품 확정(rm)은 이 잠금을 안 잡아 R<0 가능」은 R2 영역(반품 확정 경로) — 손대지 않음.
7. 픽스처 DB 접두어는 `hitpan_r1_g8_` 그대로(지시의 `hitpan_r1b_*` 로 바꾸지 않음 — 공용 게이트 이름 유지). 끝난 뒤 0개 확인.
8. push · CI GATES 등록 안 함.

> 상태(PM 후속 반영 뒤): 1 = ✅ 유지 승인 · 3 = ✅ 해소(RC 교체 · 교착 실측 재현 → w 로 0 확인) · 4 = ✅ 승인 · 2·5~8 = 기록 유지.

9. ⚠️ **RC 전제** — `binlog_format=STATEMENT` 서버는 RC 트랜잭션 쓰기를 거절(고객 PC 설정 실측 안 함 · 설치본에 binlog_format 지정 0건 → 기본 MIXED). [4] 확인 대상.
10. RC 가드는 `GetForUpdateAsync` 를 부르는 **모든** 호출자에 걸린다 — R2·R4 가 새로 부르면 트랜잭션을 `LegacyBalanceMatching.MatchIsolation` 으로 열어야 한다(안 열면 500 으로 바로 드러남). 현재 호출자 = `CollectionService` 등록 2곳 · G8 뿐.
11. 봉합 빼고 42·43·46 은 후속 2 전(`b3e91a7f` 22건 기준)에 확인했고 후속 2 뒤 다시 돌리지 않았다(해당 코드 무변경).
12. 교훈 기록 — 봉합 빼기 스크립트의 `git checkout --` 원복이 **커밋 안 한 후속 2 수정까지 되돌림**(FAIL 실행 자체는 수정 반영 상태에서 돎) → 다시 적용·커밋 뒤 커밋 위에서 재실행해 같은 결과(u·x) 확인. 봉합 빼기는 커밋 뒤에만.

#### 백로그
- 46 삭제 월마감 검사 SQL 이 `ApprovalTriggerHelper.EnsureNotClosedAsync` 와 `CollectionService.EnsureNotClosedInTxAsync` 두 곳에 중복 — 헬퍼에 트랜잭션 인자 버전을 추가하고 private 을 대체(헬퍼 파일 소유 갈래에서).
