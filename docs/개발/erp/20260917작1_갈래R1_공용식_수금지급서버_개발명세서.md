# 개발명세서 2026-09-17 — 20260915작1 개정 3판 갈래 R1 (공용 식 · 수금·지급 서버)

| 항목 | 내용 |
|---|---|
| 근거 작업지시서 | `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` §15-0~15-3 · §15-9 |
| 근거 설계 | `docs/설계/erp/20260915_설계_자료이관_머리없는줄_분류봉합.md` §20·§21·§22·§25(G8)·§27(P2~P4·S2) |
| PM 결재 | ✅ 2026-09-17 조건부 승인(작지 §15-9) · S2 = 사장님 여쭘(기본값 추천안으로 구현) |
| 개발팀 지휘 | PM 브라운킴 |
| 구현 담당 | hp-implementer (갈래 R1 · worktree) |
| 커밋 | 계약 뼈대 = (이 커밋) · 본체 = §진행 상태 표 |

## 0. 진행 상태

| # | 덩어리 | 상태 | 커밋 |
|---|---|---|---|
| 1 | 계약 뼈대 — `LegacyBalanceMatching.cs`(식·검사 본문 포함) · DTO · 취소 기표 2개 | ✅ 빌드 0/0 | 이 커밋 |
| 2 | `CollectionService` 배선 — 등록 검사 · P2 · P3 · 삭제 역분개 · P4 · 요약 `ListAsync` · 목록 `source_type` | ⬜ | |
| 3 | G8 `LegacyBalanceMatchGateTests` a~k · 봉합 빼고 FAIL · 기존 게이트 회귀 | ⬜ | |

## 1. 사실 (첫 턴 확인 · 격리 33306 `hitpan_e2e_gi` 실측)

| # | 확인 | 결과 |
|---|---|---|
| ③ 미실측 | `journal_entries` source UNIQUE | **있다** — `UNIQUE KEY uq_je_source (tenant_id, source_type, source_id)`. 취소 기표는 `source_type` 이 `collection_cancel`/`payment_cancel` 로 달라 원 분개와 충돌 0. 같은 수금 두 번째 취소는 `ShouldReverseAsync` 가 0건 처리 |
| 계약 | `ICollectionService` 시그니처 | **무변경** (파일 미수정 · 구현체 `CollectionService` 1개) |
| #13 | DESCRIBE | `collections`(ref_doc_type varchar(30) · source_type varchar(30)) · `payments`(payment_type varchar(20) NOT NULL · CHECK 없음 · ref_order_id varchar(36)) · `partner_legacy_balances`(UNIQUE tenant_id+partner_id) · `journal_entries` · `journal_lines` · `sales_deliveries` · `purchase_receipts`(is_deleted 칼럼 없음) · `purchase_returns` · `monthly_closing` |
| 인터페이스 | `AutoJournalHelper` | `internal static class` — 계약의 `public static` 메서드는 클래스 접근성(internal) 안에서 public. 클래스 접근성은 안 바꿨다(#1) |

## 2. 무엇을 만들었나 (작지 §15-3 대비)

(덩어리 2·3 완료 시 채움)

## 3. 변경 파일

## 4. 자체 확인 결과 (실측)

## 5. 안 한 것 / 못 한 것 / PM 결정 필요
