# 작업지시서 작8 — demo 6월 상태 정합 복원 (Web+API+DB 한 세트)

- 발행: 2026-06-26 / PM 브라운킴
- 결재 대기: CTO 1차 → 사장님 최종
- 근거: 사장님 지시 "챗봇 분석 잘 되던 6월 상태로 되살려, 실제 웹에 떠서 정상 동작해야"
- 헌법: #39(운영 직접수술 금지·검증된 단일 스크립트), #29(인프라 결재), #33(의중 재확인), #36(clean DDL 단일진실원), #28·#30(자가복구)

---

## 1. 사고 진단 (실측 확정)

| 조각 | 실측 상태 | 비고 |
|---|---|---|
| Web (5234) | **4월 28일 빌드** (`C:\Program Files\HitPan\web`) | 지금 뜨는 "4월 히트판" 화면 |
| API (5257) | 6/26 최신(오늘 PM swap) | Web과 짝 안 맞음 → 곳곳 오류 |
| DB | hitpan_erp (멀쩡) | 어제 실측 OK |
| 터널 cloudflared ×3 | 멀쩡 | origin(PC) 응답만 죽었던 것 |

**진범 = demo가 줄곧 "4월 Web + 최신 API" 짝짝이.** 502는 Web 정적서버(web-server.ps1) 프로세스가 꺼져서였고(임시 복구함), 화면이 4월인 건 web 폴더가 4월이라서. **6월 Web 빌드·키·정합 백업은 디스크에 없음.**

## 2. 복원 기준 (사장님 결재)

- **기준 커밋 = `05f3ad3` (2026-06-21 01:54)** — "AI 챗봇 실데이터 분석 완성" 지점
- 확인: clean DDL 존재(121테이블, develop과 동일), 보안격벽(85594a2)은 **이미 포함**(=망가짐 원인 아님)
- 진행: **작업지시서→결재 후** (오늘 즉흥수술 6회 사고 재발 차단)

## 3. 작업 범위 — Web·API·DB **한 세트** 정합 빌드/배포

> 한쪽만 손대지 않는다(헌법 #39 deploy-demo 5요건). 모두 `05f3ad3`에서.

### [0] 사전 (변경 0)
- 워크트리 격리: `git worktree add` 로 `05f3ad3` 체크아웃 (현재 develop 작업본 무손상)
- 현재 운영본 백업: `web`→`web.bak_20260626`, `api`→이미 `api-backup-fix` 있음, **DB 덤프 1회**

### [1] DB 정합 (사장님 결재 필수 — 운영 DB)
- **선택지 A (안전·추천)**: 현 hitpan_erp 유지. `05f3ad3` clean DDL과 현 DB diff만 떠서 보고 → 차이 있으면 그때 ALTER 결재
- 선택지 B: 별도 `hitpan_e2e`에 clean DDL fresh import 후 검증, 운영은 안 건드림
- ※ 운영 DB DROP/초기화 절대 금지(오늘 사고 재발원). 기본 = A.

### [2] API 빌드 (05f3ad3)
- `dotnet publish src/HitPan.API -c Release` → staging 폴더
- 키 출처 단일화: `hitpan-keys.conf` 하나만(db.conf 즉석생성 금지)

### [3] Web 빌드 (05f3ad3) — 핵심
- `dotnet publish src/HitPan.Web -c Release` → `wwwroot` 산출
- **appsettings.json 손대지 않음**(헌법 #21): ApiBaseUrl = `https://api-demo.hitpan.kr` 유지 확인만
- 산출 wwwroot → `C:\Program Files\HitPan\web\wwwroot` 교체

### [4] 기동 (포트 고정)
- API: 5257 / Web: 5234(web-server.ps1)
- ⚠️ `hitpan-start.bat` 금지 (API를 5234로 띄우는 옛 단일서버 방식 → 충돌)

### [5] 정합 게이트 스모크 (헌법 #39 5요건) — 하나라도 실패 시 롤백
1. demo.hitpan.kr 200 + index 수신
2. 로그인 200
3. `/api/me` 200 (복호화 정상 = 키 정합)
4. **AI 챗봇 분석 1건** (사장님 기준 = "챗봇 분석 동작")
5. 대시보드 200 / 캘린더 JSON
6. 재고·매입·판매 핵심 각 1건

## 4. 영구 안정화 (별건, #28·#30) — 이번 미포함, 후속 결재
- web-server.ps1 + API를 **작업스케줄러 자동시작/서비스 등록** → 창 닫힘·재부팅에도 자동 기동(오늘 502 근본원인 차단)
- 본 작업지시서는 "6월 화면 복원"까지. 자동기동은 작9로 분리.

## 5. 리스크
- 05f3ad3↔develop 106파일 차이 → 복원 후 demo는 **6/21 기능 수준**(오늘 C·D, 6/22~ 전수봉합 미반영). 사장님 의도("챗봇 분석 그때")와 일치.
- 운영 DB가 그간 16~19차 ALTER 흔적 있으면 05f3ad3 코드와 컬럼 불일치 가능 → [1]-A diff로 사전 적발.

## 6. 결재 요청
- [ ] 기준 05f3ad3 OK?  / [ ] DB는 A(유지+diff) OK?  / [ ] 빌드·배포 진행 OK?
- 결재 시 PM이 워크트리 빌드→staging까지(운영 무변경) 먼저, 배포 직전 재보고 후 swap.
