# 개발명세서 2026-09-15 — 작1 갈래 I · 통합 (D·E 호출 연결 · 트랜잭션 계약 · DOCFC 신선도 가드 · R-B4 · 거래명세서 전잔액 · PartnerBalanceRepository)

| 항목 | 내용 |
|---|---|
| 근거 작업지시서 | `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` §14-4 · §14-5 · §14-6 · §14-7 · §14-8 |
| 참조 개발명세서 | 갈래 B §5 발견1 · 갈래 D §1-3 · 갈래 E §0·§5 · 갈래 G §5 |
| PM 결재 | ✅ 2026-09-15 (§14-8 3차 투입) |
| 개발팀 지휘 | PM 브라운킴 |
| 구현 담당 | 갈래 I 구현 에이전트 (worktree `agent-a4558b73f0bec5327`) |
| 선행 커밋 확인 | `origin/feat/20260915-migration-posting` ff 병합 · `4c8a5fc0` 포함 확인 후 착수 |
| 커밋 | `2050de11`(뼈대) · `d1c45091`(항목1·2) · `942aabe9`(중간 기록) · `44566352`(항목3·4) · 이 문서·GATES 커밋 |

## 1. 무엇을 만들었나

| # | 요구 | 구현 | 상태 |
|---|---|---|---|
| 1 | D·E 호출 연결 · E tx 오버로드 · 실제 호출 경로 게이트 | `MdbMigrationService.cs` `RunLegacyPartnerBalanceStepAsync`(E 스텝 · 수금·지급 병렬 잡 뒤) · `RunLegacyStockFinalizeAsync`(①`legacy_final_stock` → ②`item_stock_rebuild` → ③`legacy_final_cost` · 같은 `posting.Docfc` 인스턴스 · 스텝 tx·`_logger` 오버로드) · `EnsureFinalStockItemAsync`(빈 키 → 폴백 품목 · 1단계 itemMap → MIG-AUTO) · `MdbLegacyPartnerBalance.ApplyAsync(db, tx, …)` 오버로드(SELECT·INSERT IGNORE·UPSERT 전부 `transaction: tx` · 기존 시그니처는 `tx=null` 위임) · 게이트 `MdbLegacyWiringGateTests.cs` WG-E1·E2·D1·D2 | ✅ `d1c45091` |
| 2 | DOCFC 신선도 가드 | `MdbLegacyFinalStock.StaleReason`·`LastValidLegacyDate`(yyyyMMdd 파싱 · 이관일 뒤 날짜 제외) · `LegacyPostingContext.DocfbLastDate`(`ReadLegacyPostingContext` 가 DOCFB DISTINCT IJ_DT 로 채움) · 가드 걸리면 ①·③ 안 부르고 ②만 · 게이트 WG-1·2·D3 | ✅ `d1c45091` |
| 3 | R-B4 `99999999` 명시 분기 | `MigratedDocumentLock.IssueLockReason`(① 이관 아님 null ② 99999999 「발행 안 함 확정」 ③ ≠0 「레거시 발행」 ④ 0 null ⑤ NULL → 상수) · `IsIssueLocked` = 사유 있음 · `IssueBlockedMessageFor` · 문구 `IssueNotIssuedMessage` · `TaxInvoiceService.cs:87-90` 문구 1줄 · 발행 목록 SQL `SalesService.GetDeliveriesAsync`(`legacy_tax_no = 99999999 OR …`) · G5-17 사례 `D-CMP-H-9`+사유 단언 · G5-18 | ✅ `44566352` |
| 4 | 거래명세서 전잔액 | `SalesService.GetDeliveryAsync` 뷰 조회 **뒤에 추가**: 이월잔액 행 있는 회사만 `legacyPrevSql`(이관 명세서·이관 수금 제외 + 이 거래처 `GREATEST(balance,0)` 1회 · `HasLegacyBalanceSql` 재사용) 로 덮음 · 없는 회사 NULL → 종전 뷰 값 · G5-19 | ✅ `44566352` |
| 5 | PartnerBalanceRepository 부분 반영 해소 | **코드 변경 없음** — §5 사유·대안 | ⚠️ 불가(사유 기록) |

### 결과 표 · 로그 키 (갈래 H+C 대사표가 읽는다)

| 키 | 뜻 |
|---|---|
| `MdbMigrationResult.LegacyFinalStockSkipReason` | null = 가드 통과 · `"최종재고 표가 최신이 아님"`(`MdbLegacyFinalStock.StaleFinalStockReason`) = 맞춤·끝전 안 함 |
| `MdbMigrationResult.LegacyFinalStockRows` / `LegacyFinalCostRows` / `PartnerLegacyBalances` | 맞춤 줄 새 행 수 / 끝전 고친 item_stock 행 / 이월잔액 거래처 행 (Total 미포함) |
| 경고 로그 | `"[MDB마이그레이션] 최종재고 표가 최신이 아님 — DOCFC 마지막 달 {DocfcYm} < DOCFB 마지막 날짜 {DocfbLast} … (LegacyFinalStockSkipReason)"` |
| 체크포인트/진행 카드 표 이름 | `partner_legacy_balances` · `legacy_final_stock` · `item_stock_rebuild` · `legacy_final_cost` (가드 skip 이면 앞뒤 둘은 카드가 안 뜬다) |

## 2. 변경 파일

| 파일 | 변경 | 왜 |
|---|---|---|
| `src/HitPan.Application/Services/MdbMigrationService.cs` | `// E:`·`// D:` 주석 블록 → 호출 · 스텝 메서드 3개 추가 · 리빌드 스텝 **본문 그대로** 새 메서드 안으로 이동(SQL 글자 동일 — G3 가 파일에서 읽음) · record 에 `DocfbLastDate` init 속성 · 결과 DTO 칸 4 | 1·2 |
| `src/HitPan.Application/Services/MdbLegacyPartnerBalance.cs` | tx 오버로드 · 폴백 거래처 private 메서드에 tx 인자 | 1 |
| `src/HitPan.Application/Services/MdbLegacyFinalStock.cs` | 가드 함수 2 + 상수 | 2 |
| `src/HitPan.Application/Services/MigratedDocumentLock.cs` | 사유 함수·상수·문구 · `IsIssueLocked` 본문을 사유 함수 위임으로 | 3 |
| `src/HitPan.Application/Services/TaxInvoiceService.cs` | 거부 문구 1줄 (지시 [파일] 목록 밖 — 결재 문구를 서버 응답에 싣는 유일한 자리) | 3 |
| `src/HitPan.Application/Services/SalesService.cs` | 목록 SQL 조건 1줄 · 전잔액 추가 조회 | 3·4 |
| 🆕 `src/HitPan.Tests/Integrity/MdbLegacyWiringGateTests.cs` | 7 Fact | 1·2 |
| `src/HitPan.Tests/Integrity/TaxInvoiceMigratedLockGateTests.cs` | G5-17 사례 · G5-18 · G5-19 | 3·4 |
| `.github/workflows/build-and-test.yml` | GATES `MdbLegacyWiringGate` | 게이트 |

- #1: 기존 경로 삭제 0 · 기존 시그니처 유지(위임) · 전잔액은 종전 뷰 조회를 남기고 뒤에 조건부 덮기. DDL 없음(#36 해당없음). MdbReconciliationService·MdbMigration.razor 무접촉.

## 3. 헌법 준수

| 조항 | 어떻게 | 근거 |
|---|---|---|
| #2 | tenantId 는 기존 호출자 값 · 새 SQL 전부 `tenant_id = @TenantId` | `legacyPrevSql` · 스텝 메서드 |
| #3 | stock_ledger 는 D 의 INSERT IGNORE 그대로 · UPDATE/DELETE 추가 0 | — |
| #4 | 금액 decimal · 전잔액 `decimal?` | — |
| #13 | 격리 33306 `hitpan_e2e_gi`(출하 DDL 적재) information_schema: sales_deliveries.source_type varchar(20)·status·is_deleted · collections.source_type varchar(30)·ref_doc_type · payments.source_type · partner_legacy_balances.balance_amount decimal(15,2) · **sales_orders 에 source_type 없음**(§5 항목5 근거) | 2026-09-15 실측 |
| #15 | 새 catch 0 (게이트의 MySqlException catch 는 G3 관용구 그대로 · 메시지 출력) | — |
| #16 | 병렬 추가 0 · D·E 스텝은 병렬 잡 뒤 순차 | — |
| #19 | Application·API·Tests 0/0 | §4 |
| #39 | 운영 3306 무접촉 · 격리 33306 임시 DB(`hitpan_wiring_*` 자동 DROP · `hitpan_e2e_gi` G5 용) | — |

## 4. 자체 확인 (실측)

| 항목 | 명령 | 결과 |
|---|---|---|
| 빌드 | `build-brief.ps1 -Project src/HitPan.Tests/HitPan.Tests.csproj` · `-Project src/HitPan.API/HitPan.API.csproj` | 둘 다 errors 0 / warnings 0 |
| 게이트 | `HITPAN_DB_HOST=127.0.0.1 HITPAN_DB_PORT=33306 HITPAN_DB_USER=root HITPAN_TEST_DB=hitpan_e2e_gi HITPAN_REQUIRE_DB=1` · `test-brief.ps1 -Filter TaxInvoiceMigratedLockGate\|MdbLegacyWiringGate\|MdbLegacyBalanceGate\|MdbLegacyFinalStockGate` | **39 passed · 0 failed · 0 skipped** (WG 7 · G5 19 · G4 2 · G3 11) |
| G5-19 1차 | 같은 | FAIL 9,100 ≠ 9,200 — **내 기대값 산수 실수**(1,100×2−100+7,000=9,100) · 코드 무수정으로 기대값만 정정 후 통과 |

### 무력화 시험 (봉합 빼고 FAIL) — scratchpad `gI/mut1.ps1` · 한 곳씩 바꿔 WG+G5 실행 → `git checkout` 원복

(결과 표 — 아래 §4-1)

## 5. 안 한 것 / 못 한 것 / 발견

| 항목 | 왜 | 후속 |
|---|---|---|
| ⚠️ 항목5 `PartnerBalanceRepository` 부분 반영 해소 | 뷰 `v_partner_balance` = `v_partner_sales_orders_total`(sales_orders) − `v_partner_receipts_total`(payments receipt) · `v_partner_purchase_orders_total` − payments payment. **sales_orders·purchase_orders 에 source_type 칸이 없다**(#13 실측) → 이관 수주·발주(`MdbMigrationService.cs` DOCFO/DOCFA INSERT 에 source 표시 없음)를 SQL 로 가를 방법이 없다. 이월잔액만 더하면(E 현재) 이관 수주가 남아 두 번 센다 | 대안 (가) 이 API 도 G5-19 처럼 이월잔액 회사는 명세서 기준 식으로 바꾼다(뷰 기준과 달라짐 — 화면 소비자 확인 필요) (나) 이관 수주·발주에 source_type 칸 추가 DDL(DB-124 번호 예약 필요) 후 뷰 밖 인라인 식 (다) 현행 유지 + 화면 설명. **PM·설계 판단 필요** |
| ⚠️ 전잔액 기준 차이 | 이월잔액 회사는 명세서 기준 · 없는 회사는 종전 수주 기준(뷰) — 같은 화면 칸이 회사에 따라 기준이 다르다 | PM 판단 (항목5 대안 (가)와 함께) |
| ⚠️ `LockWhenLegacyTaxNoMissing = false` 유지 | B 이후 이관은 NULL 을 안 쓴다. NULL 은 봉합 전 이관분(종전 99999999→NULL · 첫 줄만)에만 → 덮어쓰기 재이관 전까지 그 행은 발행 허용(종전 동작) | 봉합 전 이관 DB 가 실물에 있으면 R4 재이관 또는 PM 결정 |
| 🔴 발견1 · B `LegacyTaxNo` | 「첫 번째 비0」 — 묶음 안 첫 줄이 99999999 이고 뒷줄이 실제 번호면 99999999 를 돌려준다 → 잠금은 같지만 문구가 「발행 안 함」으로 틀리게 나간다 | 갈래 B 파일(수정 금지) — 실제 번호 우선 규칙 여부 PM |
| 🔴 발견2 · 옛 `// D:` 람다 | `(tid, legacyKey, t, c)` 는 D 계약 (품명, 규격, tx, ct) 과 뜻이 달랐다 · itemMap 을 안 보면 DOCFS 마스터와 같은 이름 MIG-AUTO 가 생긴다 | 봉합(WG-D1 새 품목 2개 단언) |
| ⚠️ 가드 한계 | 이관일 뒤 IJ_DT 는 뺐지만 이관일 이전의 잘못 친 날짜(예: 2026-08 에 2026-07 오타)는 못 가린다 · 기준일(B `LegacyBaseDate`)은 DOCFC 우선이라 가드가 걸린 MDB 는 기준일도 오래된 달 말일이다(날짜 없는 줄·맞춤 없음) | [4] · 설계 판단 |
| 공영정보 MDB 영향 | scratchpad `gA/docfb_groups.tsv` DOCFB 마지막 유효 20260220 · DOCFC 202602 → 가드 통과 | [4] 실물 재이관으로 최종 |
| 이관 서비스 전체(OleDb) 종단 | MDB 없이 못 부름 — 게이트는 스텝 메서드를 reflection 으로 부른다(MigrateCoreAsync 가 그 메서드를 부르는지는 글자로만) | [4] MDB 사본 재이관 |
| R-A3 이월잔액 수금 매칭 | 사장님 결재 대기 | 별도 투입 |
| push · INDEX.md | 지시 | — |

## 6. [3-V] 병렬검증 반영

| # | 지적 | 반영 |
|---|---|---|
| 37 | 리빌드가 avg_cost 덮음 | 끝전을 리빌드 스텝 뒤에만 부르는 한 메서드 · WG-D1 순서 대조군 |
| §14-8 PM 반증 | DOCFC 가 최신이 아니면 최신 거래를 0 으로 덮음 | 신선도 가드 · WG-D3 |

## 7. 검증팀에 요청

- 위험: reflection 으로 private 스텝 메서드를 부른다 — 이름을 바꾸면 게이트가 「메서드 없음」으로 빨간불(조용한 통과 아님).
- 위험: `ReadLegacyPostingContext` 는 DOCFB 가 없으면 `DocfbLastDate` null → 가드 안 함.
- 회귀: 이관 끝 단계에 스텝 3개가 늘어 진행 카드 표 이름이 새로 보인다(갈래 H+C 화면 확인). 이월잔액 회사의 거래명세서 전잔액 숫자가 바뀐다(의도).
