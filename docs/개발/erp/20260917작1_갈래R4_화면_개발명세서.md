# 개발명세서 2026-09-17 — 20260915작1 3판 갈래 R4 화면

| 항목 | 내용 |
|---|---|
| 근거 작업지시서 | `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` §15-6 · §15-10 · §15-11 |
| 근거 설계 | `docs/설계/erp/20260915_설계_자료이관_머리없는줄_분류봉합.md` §22 · §24(F2) |
| PM 결재 | ✅ 2026-09-17 (§15-9) |
| 개발팀 지휘 | PM 브라운킴 |
| 구현 담당 | 개발팀 R4 (화면) |
| 기준 | `feat/20260915-migration-posting` `bc1c1092` (R1·R3 머지) — worktree 는 `564ec5d7` 에서 시작해 `--ff-only` 로 맞춤 |
| 커밋 | 뼈대 `57f503f3` → 수금 `e572cbcc` → 지급·대사표 `32223b0b` → 명세서(이 커밋) |

## 진행 상태

| 덩어리 | 상태 |
|---|---|
| 뼈대 | ✅ `57f503f3` |
| Web 모델·서비스(문구 돌려주는 메서드) | ✅ `e572cbcc` |
| 수금 화면 | ✅ `e572cbcc` |
| 지급 화면 | ✅ `32223b0b` |
| 대사표 EXPLAINED · 재고 안내 줄 | ✅ `32223b0b` |

## 사실 (첫 턴 확인)

| 확인 | 결과 |
|---|---|
| Web 이 Application 을 참조하나 | ❌ `HitPan.Web.csproj:30` = Contracts 만 → 이름표·문구를 Web 한 곳(`ApprovalModels.cs` `CollectionPaymentLabels`, `MdbMigration.razor` `RollForwardNoticeFormat`)에 **같은 값으로 복사**. 서버 값과 일치 확인은 [4] |
| 서버 거절 응답 모양 | `GlobalExceptionMiddleware.cs:24-31` → 400 `{"error":"…"}` |
| 서버 목록이 SourceType 을 내보내나 | ✅ `CollectionService.cs:35·87·266` |
| 이월잔액만 있는 거래처도 요약 표에 뜨나 | ✅ `CollectionService.cs:615-639` `MergeLegacyIntoSummary` → 펼침 창이 열린다(전표 표는 빈 채로 이월잔액 한 줄만) |
| 이관 수금의 ref 모양 | `ref_doc_type='sales_delivery'` · `ref_doc_id` NULL (`MdbMigrationService.cs:3147-3150`) → 종전 칩 없음 → 이제 「이전 프로그램」 칩 |
| 화면에 수금·지급 **삭제 버튼**이 있나 | ❌ Web 전체 grep — `DeleteCollectionAsync`·`DeletePaymentAsync` 호출처 0 · 두 화면 이력 표에 삭제 버튼 없음 → 숨길 버튼 자체가 없다. 삭제 문구용 `Delete*WithMessageAsync` 는 추가만 해 둠 |
| bUnit / Web 테스트 | ❌ 없음 (`*.csproj` 에 bunit 0) → **화면 실측은 [4] 로** |

## 1. 무엇을 만들었나 (작지 §15-6 대비)

| 요구 | 구현 | 상태 |
|---|---|---|
| 수금 펼침에 「이전 프로그램 이월잔액」 한 줄(기준일 · 넘어온 미수금 · 그 뒤 수금 · 남은 금액) + 수금 버튼 | `CollectionPage.razor` 전표 펼침 창 머리 `_dialogLegacy` 블록 · `OpenLegacyCollectDialog` | ✅ |
| 금액 기본값 = 남은 금액 · RefDocType=`legacy_balance` · RefDocId=PartnerId | `OpenLegacyCollectDialog` · `ProcessLegacyCollectAsync` | ✅ |
| 지급 같은 모양 · PaymentType=`legacy_balance` · RefOrderId=PartnerId | `PaymentPage.razor` `OpenLegacyPayDialog` · `ProcessLegacyPayAsync` | ✅ |
| 서버 거절 문구 그대로 Snackbar | Web `CollectionPaymentService` `CreateCollectionWithMessageAsync`·`CreatePaymentWithMessageAsync`(+`Delete*WithMessageAsync`) → `SaveResultModel(Ok, ErrorMessage)` → 단건·이월잔액·일괄 처리 모두 문구 표시 | ✅ |
| 기존 bool 메서드 유지(#1) | `CreateCollectionAsync` 등 4개 무변경 | ✅ |
| 유형 칩 한글 · 원문 노출 0 | 수금 `거래명세서`/`이전 프로그램 이월잔액`/`기타` · 지급 `매입전표`/`이전 프로그램 이월잔액`/`기타` (`CollectionPaymentLabels.CollectionRefLabel`·`PaymentRefLabel`) | ✅ |
| 엑셀 「참조 전표」 칸 | 칩과 같은 말만 — 종전 수금 `"{영문유형} {내부번호}"` · 지급 내부번호(RefOrderId) 그대로 → **내부 번호 제거**(R-A6) | ✅ |
| 이관 줄 칩 「이전 프로그램」 | 두 화면 이력 표 `SourceType=="migration"` | ✅ |
| 이관 줄 삭제 버튼 숨김·비활성 | 화면에 삭제 버튼이 원래 없음(위 사실) — 서버 R1 거절이 최종 | ✅(해당 버튼 없음) |
| 레거시 수금·지급 줄 목록에서 안 빠짐 | 필터 추가 0 · 정렬 무변경 | ✅ |
| 대사표 `EXPLAINED` 아이콘·색·「사유 확인됨」 | `MdbMigration.razor` `GetReconIcon/Color/Label` (FactCheck · Info 색) · 요약 줄 「사유 확인됨 n」 | ✅ |
| 재고 안내 줄(`RollForwardNotice`) | 이관 결과 `LegacyFinalStockRollForward`(R3) 받아 대사표 머리에 Info 1줄(개월 수 + 수량 없이 금액만 있던 줄 건수·금액) · 서버 알림에 「이어 계산」 문구가 이미 있으면 중복 표시 안 함 | ✅ |
| ⑥ 3행 표시 | 기존 표가 행 단위라 R2 행이 그대로 붙는다 · 정보 행 Status 가 `INFO` 면 「참고」 | ⚠️ R2 Status 값 미확정(아래 5) |

## 2. 변경 파일 목록

| 파일 | 변경 |
|---|---|
| `src/HitPan.Web/Models/ApprovalModels.cs` | 추가 — `SourceType` 2 · `LegacyBalances` 2 · `LegacyBalanceRowModel` · `SaveResultModel` · `CollectionPaymentLabels` |
| `src/HitPan.Web/Services/CollectionPaymentService.cs` | 추가 — `*WithMessageAsync` 4 · `SendWithMessageAsync` · `ExtractServerError` · `FallbackSaveError` |
| `src/HitPan.Web/Pages/Finance/CollectionPage.razor` | 추가 위주 · 수정 = 칩 식 1 · 엑셀 칸 1 · 단건/일괄 등록 호출을 문구 메서드로 교체 |
| `src/HitPan.Web/Pages/Finance/PaymentPage.razor` | 같은 모양 |
| `src/HitPan.Web/Pages/Settings/MdbMigration.razor` | 추가 — 판정 분기 2값 · 안내 줄 · 결과 모델 필드 · 요약 접미 |

- 다른 갈래 파일·`appsettings*.json`·마이그 SQL·출하 DDL 무접촉.

## 3. 헌법 준수

| 조항 | 어떻게 | 근거 |
|---|---|---|
| #1 | bool 메서드 유지 · 새 메서드 추가 | `CollectionPaymentService.cs` |
| #2 | 화면은 tenant 를 보내지 않음(PartnerId 만) | 두 화면 모델 |
| #4 | 금액 전부 decimal | `LegacyBalanceRowModel` |
| #14 | 변경분 `"""` 0건 (`git diff` grep) | |
| #15 | 새 catch 는 `HttpRequestException`·`TaskCanceledException`·`JsonException` 만 잡고 로그(Console — Web 서비스 관례 `ItemMasterService.cs:62`) + 안내 문구 반환 | |
| #19 | Web 빌드 0/0 | 아래 4 |
| #23 · 개발용어 | 화면 문구에 영문 코드 0 · 모르는 유형은 「기타」 | |

## 4. 자체 확인 결과

| 항목 | 명령 | 결과 |
|---|---|---|
| 빌드 (#19) | `scripts/dev/build-brief.ps1 -Project src/HitPan.Web/HitPan.Web.csproj` | errors 0 / warnings 0 (두 덩어리 각각) |
| raw string | `git diff \| Select-String '"""'` | 0 |
| 화면 동작 | 서버 미기동(운영 3306·5257·5234 무접촉 지시) · bUnit 없음 | **[4] 실측으로 넘김** |

## 5. 안 한 것 / 못 한 것 · PM 결정 필요

1. **화면 실측 0** — [4] 에서: 이월잔액 한 줄 표시 · 남은 금액 초과 → 서버 문구 Snackbar · 기준일까지 날짜 → 문구 · 방향 반대 → 문구 · 이관 줄 칩 · 엑셀 칸 · 대사표 「사유 확인됨」·안내 줄.
2. **R2 대사표 Status 값 미확정** — R2 미머지. `EXPLAINED` 는 계약(§15-2)대로. ⑥ 「그 뒤 수금·지급」「남은 금액」 정보 행이 `NA` 면 「—」, `INFO` 면 「참고」로 보인다. **R2 가 다른 값(예 `WARN`)을 쓰면 R4 분기 1줄 추가 필요** → R2 머지 뒤 PM 확인.
3. **재고 안내 줄은 같은 화면에서 이관을 막 끝냈을 때만** 보인다(`_migrationResult` 에서 읽음). 나중에 「다시 맞춰보기」만 누르면 안 보임 → R2 가 대사표 `Warnings` 에 안내를 넣으면 그쪽이 늘 보인다(중복은 거름). R2 에 넣을지 PM 판단.
4. **문구 복사 2곳** — `LegacyBalanceMatching.Label`/`RefType` ↔ `CollectionPaymentLabels`, `MdbLegacyFinalStock.RollForwardNotice` ↔ `RollForwardNoticeFormat`. Web 이 Application 을 참조하지 않아 복사. 서버 문구가 바뀌면 같이 바꿔야 한다 → 게이트로 문자열 일치를 볼지(Z) PM 판단.
5. 삭제 버튼 — 화면에 원래 없어 숨길 것이 없다. 새로 만들지 않았다(범위 밖).
6. 이월잔액 수금 창은 화면에서 「남은 금액 초과」를 미리 막지 않는다 — 서버가 잠금 뒤 다시 계산한 값으로 거절·문구를 주는 것이 정본(동시 입력 대비). 0 이하만 화면에서 막음.
