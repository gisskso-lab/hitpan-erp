# 개발명세서 2026-09-15 — 작1 갈래 G · 이관 명세서·계산서 잠금 (R-B · R-B2 · R-B3)

| 항목 | 내용 |
|---|---|
| 근거 작업지시서(PRD) | `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` §11-1 · §11-3(G5) · §13(R-B2) · §14-1(R-B3) |
| 설계 | `docs/설계/erp/20260915_설계_자료이관_머리없는줄_분류봉합.md` §17 R-B |
| 선행검증 | `docs/검증/선행/20260915_선행검증서_자료이관_라안_표식_미수기준_실측.md` §5 ④ |
| 병렬이슈 | `docs/검증/병렬이슈/20260915_병렬이슈38_*.md` · `39_*.md` |
| PM 결재 | ✅ 2026-09-15 (§13) · R-B3 사장님 결재 (§14-1) |
| 구현 담당 | 개발팀 갈래 G (hp-implementer) |
| 커밋 | worktree 브랜치 `worktree-agent-a97dd2d4bc6d51f37` (반환 메시지의 SHA) |

## 1. 무엇을 만들었나

판정 함수는 한 곳 — `src/HitPan.Application/Services/MigratedDocumentLock.cs`
- `IsMigrated(source_type)` — 수정·확정취소·삭제·계산서 취소 잠금
- `IsIssueLocked(source_type, legacy_tax_no)` — 계산서 **발행** 잠금 (R-B3: 이관 AND 레거시 계산서 번호 ≠ 0). 발행 범위가 또 바뀌면 이 한 줄만 바꾼다.
- 문구(현장 용어): 「이전 프로그램에서 가져온 자료는 고칠 수 없습니다. 새 전표로 정정해 주세요.」 · 「…거래명세서는 계산서를 새로 발행할 수 없습니다.」 · 「…계산서는 취소할 수 없습니다. 새 전표로 정정해 주세요.」

| 요구 | 구현 (잠금 줄) | 상태 |
|---|---|---|
| ① 계산서 발행 요청 거부 (레거시 발행 이관분만 · R-B3) | `TaxInvoiceService.cs:87` (IssueAsync `:46` · SELECT 에 source_type·legacy_tax_no 추가) — 일괄 발행도 Web `TaxInvoiceApiService.cs:39` 가 건별 IssueAsync 호출이라 같이 막힘 | ✅ |
| ① 발행 대상 목록에서 빠짐 | 목록 원천 `SalesService.cs:878-880` (`IsMigrated`·`IsIssueLocked` 칼럼) → `DeliveryDtos.cs` · Web `DeliveryModels.cs` 속성 → `TaxInvoicePage.razor:197` `!d.IsIssueLocked` | ✅ |
| ② 이관 계산서 취소 거부 | `TaxInvoiceService.cs:470` (CancelAsync `:432` · 판정 = `tax_invoices.source_type`) | ✅ |
| ③ 판매 명세서 수정 거부 | `SalesService.cs:941` (UpdateDeliveryAsync `:916`) | ✅ |
| ③ 판매 명세서 삭제 거부 (병렬이슈39 ①) | `SalesService.cs:1113` (DeleteDeliveryAsync `:1102`) | ✅ |
| ③ 판매 명세서 확정취소 거부 | `SalesService.cs:1165` (CancelConfirmedDeliveryAsync `:1156`) | ✅ |
| ③ 매입 명세서 삭제 거부 | `PurchaseService.cs:1941` (DeletePurchaseReceiptAsync `:1930`) | ✅ |
| ③ 매입 명세서 수정·확정취소 | **서버 경로 없음** — `PurchaseController.cs` 매입명세서 엔드포인트 = 등록·삭제·확정·반품전환뿐 · `UPDATE/DELETE purchase_receipts` grep = `PurchaseService.cs` DELETE 1곳뿐 · `CancelReceipt·UpdateReceipt·UpdatePurchaseReceipt` grep 0건 | ✅(해당 경로 없음) |
| `DeliveryBatchService.cs:169` 확인 | 채번 SELECT(`MAX(delivery_no)`)이지 발행 경로 아님 — 수정 없음 | ✅확인 |
| ⑤ 사람 명세서·계산서 흐름 그대로 | G5 대조군 5개 초록 (§4) | ✅ |

## 2. 변경 파일 목록

| 파일 | 변경 | 왜 |
|---|---|---|
| `src/HitPan.Application/Services/MigratedDocumentLock.cs` | 🆕 | 판정 함수·문구 한 곳 |
| `src/HitPan.Application/Services/TaxInvoiceService.cs` | 추가 (SELECT 칼럼 2 · record 인자 2 · 검사 2곳) | ①② |
| `src/HitPan.Application/Services/SalesService.cs` | 추가 (검사 3곳 · 목록 칼럼 2) | ③ · ① 목록 |
| `src/HitPan.Application/Services/PurchaseService.cs` | 추가 (검사 1곳) | ③ 매입 |
| `src/HitPan.Application/DTOs/Sales/DeliveryDtos.cs` · `src/HitPan.Web/Models/DeliveryModels.cs` | 속성 2 추가 | 목록 표시 |
| `src/HitPan.Web/Pages/Sales/TaxInvoicePage.razor` | `.Where` 1줄 추가 (raw string 없음 #14) | 목록에서 빠짐 |
| `src/HitPan.Tests/Integrity/TaxInvoiceMigratedLockGateTests.cs` | 🆕 G5 12개 | 게이트 |
| `.github/workflows/build-and-test.yml` | GATES 등록 + 무력화 기록 | CI |

- 기존 코드 추가만 (#1) — ✅ (기존 검사·SQL 조건 삭제 0) · 새 칼럼 없음 (#36 해당없음) · 다른 갈래 파일 무접촉 (MdbMigrationService·LegacyMdbMapping·FinanceService·CollectionService·MdbReconciliationService)

## 3. 헌법 준수

| 조항 | 어떻게 | 근거 |
|---|---|---|
| #2 | tenant_id 는 기존 컨트롤러가 JWT(HttpContext.Items)에서 넘긴 값만 씀 · 잠금 SQL 모두 `tenant_id = @Tid` | `MigratedDocumentLock.cs` |
| #13 | `hitpan_e2e` information_schema 로 sales_deliveries·purchase_receipts·tax_invoices(source_type·legacy_tax_no) 확인 + 출하 DDL `hitpan_db_clean.sql:2982·3192·3757` | §4 |
| #15 | 새 catch 는 게이트 1곳(메시지 받아 단언·로그) — 빈 catch 0 | 게이트 G5-8 |
| #19 | 빌드 0/0 | §4 |
| #20 | 이관 명세서 기반 반품 마이너스계산서 흐름 대조군 G5-11 초록 | §4 |

## 4. 자체 확인 결과 (실측)

| 항목 | 명령 | 결과 |
|---|---|---|
| 빌드 | `scripts/dev/build-brief.ps1 -Project src/HitPan.sln` | `BUILD exit=0 errors=0 warnings=0` |
| G5 (봉합 있음) | `HITPAN_REQUIRE_DB=true` · 로컬 MariaDB `hitpan_e2e` · `dotnet test --filter TaxInvoiceMigratedLockGate` | **12/12 Passed · Skipped 0** |
| 관련 기존 시험 | filter `TaxInvoice\|CreditNoteChainGate\|PurchaseChainGuardGate` (G5 포함) | 36/36 Passed (R-B3 반영 전 기준 34→36) |
| **봉합 빼고** | `MigratedDocumentLock.IsMigrated` 를 항상 false 로 바꿔 실행 후 원복 | (1차) 12개 중 6 FAIL: G5-1 · G5-3 · G5-5 · G5-6 · G5-7 · G5-9 |
| G5 (후속 · 봉합 있음) | 같은 명령 | **17/17 Passed · Skipped 0** |
| **봉합 빼고 (후속)** | 같은 무력화 | **17개 중 7 FAIL**: G5-1 · 3 · 5 · 6 · 7 · 9 · 17 / 초록 10: 대조군 G5-2 · 8 · 10 · 11 · 12 · 13 · 14 · 15 · 16 + G5-4 |
| **무력화 (b)** | R-B4 상수 `LockWhenLegacyTaxNoMissing` 만 true (목록 SQL 그대로) | **G5-17 만 FAIL** — 규칙 한쪽만 바꾸면 게이트가 잡는다 |

G5 방식: 실제 서비스 메서드를 MariaDB **임시 표**(`CREATE TEMPORARY TABLE … LIKE {TestDb}.표` → 이름 변경)에 물려 호출, 표 행·상태로 판정. 실제 표 쓰기 0 · `Pooling=false`.
이관 draft 명세서(G5-5·7·9)는 봉합이 빠지면 실제로 고쳐지고/지워지는 모양이라 문구가 아니라 표 변화로 FAIL 한다.

## 5. 안 한 것 / 못 한 것

| 항목 | 왜 | 후속 |
|---|---|---|
| ⚠️ R-B3 판정 정확도 — 레거시 번호 저장 | 이관 코드 `MdbMigrationService.cs:4992` 가 **묶음 첫 줄**의 `IJ_TAXNO` 만 `legacy_tax_no`(int)에 넣고 `:5010` `99999999 → NULL`. 「묶음 안 한 줄이라도 번호 있으면 발행」 과 99999999 의미는 지금 칸으로 못 가린다 (NULL = 미발행으로 취급) | **갈래 B 가 저장 필요** — 후보: `sales_deliveries.legacy_tax_no` 에 묶음 내 0 아닌 번호(MAX) 저장 · 99999999 의 뜻 확정. 코드 수정 안 함(지시) |
| ✅(후속) 목록 판정 SQL 이 식을 따로 가짐 | `SalesService.cs:880` 식은 const raw SQL 이라 함수에서 조각을 받게 바꾸면 기존 SQL 덮어쓰기(#1) → 대신 **G5-17** 이 번호 칸 6경우(NULL·0·번호·99999999 × 이관/사람)를 깔고 함수 결과 = 목록 SQL 결과를 단언. 무력화 (b) 로 FAIL 확인 | 규칙 바꿀 때 두 곳 함께 |
| R-B4 (99999999) 상수 한 줄 확인 | `MigratedDocumentLock.LockWhenLegacyTaxNoMissing` (지금 false). 이관 코드는 빈값→0(`GetInt`), 99999999→NULL 이라 이관분 NULL = 99999999 → **상수 한 줄 + 목록 SQL `COALESCE(d.legacy_tax_no,0)` 한 곳**을 함께 바꾸면 된다(한쪽만 바꾸면 G5-17 FAIL) | 사장님 재결재 대기 |
| ⚠️ 발견 — 사람 명세서 **수정 저장 자체**가 트랜잭션 오류 | `SalesService.UpdateDeliveryAsync` 가 `_db.BeginTransaction()` 뒤 `LoadItemDefaultWarehousesAsync`(`:2890` 부근)를 트랜잭션 없이 조회 → MySqlConnector 「The transaction associated with this command is not the connection's active transaction」 (G5-8 실측). 운영 연결 설정에서도 같은지 ⚠️미확인 | 갈래 G 범위 밖 — PM 보고. G5-8 은 「잠금이 막지 않았나」(잠금 문구 아님 + 잠금 뒤 단계 도달)로 잰다 |
| ✅(후속) #20 대조군 | G5-13 이관 명세서 반품 등록 · G5-14 이관 명세서 참조 수금 등록 · G5-15 사람 명세서 확정취소(상태 cancelled + sales_cancel 원장) · G5-16 사람 계산서 취소 — 전부 동작 단언 초록. 이를 위해 G5 는 테스트 DB 기본 표 **전부**를 임시 표로 가린다(FULLTEXT 색인 표 `hitpan_knowledge` 1개는 임시 표 불가라 제외) | — |
| 매입 반품 전환(`ConvertReceiptToReturnAsync`)·지급 등록 대조군 | 이번 지시 범위(판매 반품·수금) 밖 | ⚠️미완 |
| 확정(ConfirmDelivery/ConfirmReceipt)·일괄확정 | 이관분은 모두 confirmed 라 기존 상태 검사가 거부 — 잠금 추가 안 함 | 범위 밖 |
| SalesListDialog 「계산서 발행」 버튼 | 세금계산서 화면으로 **이동만** 함 → 목록은 TaxInvoicePage 에서 빠짐 · 버튼 자체는 무수정 | — |
| 계산서 잠금 오류 코드 `migrated_locked` | 컨트롤러 switch 무수정 → 기본 400 + 문구 | — |
| CI 첫 실행 | 로컬 `hitpan_e2e` 실측만 · CI 서비스 컨테이너에서 임시 표 LIKE·RENAME 권한은 첫 실행이 최종 확인 | [4] |

## 6. [3-V] 병렬검증 지적

| # | 지적 | 봉합 | 이슈 문서 |
|---|---|---|---|
| 38 | 계산서 발행 잠금 범위 (레거시 미발행분) | ✅ R-B3 반영 `IsIssueLocked` · G5-1/G5-12 | `docs/검증/병렬이슈/20260915_병렬이슈38_*.md` |
| 39 | ① 삭제 경로 잠금 · ② 매입 확정취소 없음 기록 · ③ 흐름 대조군 | ✅① ✅② ⚠️③ 부분(G5-11만) | `docs/검증/병렬이슈/20260915_병렬이슈39_*.md` |

## 7. 검증팀에 요청

- 위험: `SalesService.cs:880` SQL 식과 `MigratedDocumentLock.IsIssueLocked` 이중 정의 · `legacy_tax_no` 첫 줄만 저장(§5).
- 위험: DeleteDeliveryAsync 는 잠금이 계산서 검사보다 먼저 — 이관 명세서에 사람이 끊은 계산서가 있어도 「가져온 자료」 문구가 나온다(삭제 불가는 같음).
- 회귀 우려: 계산서 발행 화면 목록(`TaxInvoicePage.razor:197`) · 매입 삭제 순서(잠금이 사슬 검사보다 앞 — `PurchaseChainGuardGate` 초록 확인).
