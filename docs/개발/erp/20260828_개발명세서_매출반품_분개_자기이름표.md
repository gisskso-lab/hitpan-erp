# 개발명세서 — 매출반품 분개 «자기 이름표» (20260828작12)

> **루틴 단계**: [3] 개발 (PM 지휘) — [4] 검증 **동시 착수** (2026-08-28 개정 루틴)
> **선행검증**: [`docs/검증/선행/20260828_선행검증서_매출반품_회계읽는쪽.md`](../../검증/선행/20260828_선행검증서_매출반품_회계읽는쪽.md)
> **사장님 결재**: 2026-08-28 *"모두결재"* (과거 분개 처리 + 정합성 화면 포함 2건 모두 승인)

---

## 1. 무엇을 고쳤나 — 한 줄

**매출반품 분개는 정상으로 만들어지고 있었다. 다만 「자기 이름표」가 없어 남의 것을 빌려 쓰고 있었다.**

## 2. 원인 (선행검증 실측)

| | 매입 | 매출 |
|---|---|---|
| 반품 키 | `purchase_return` | 🔴 **없음** |
| 반품취소 키 | `purchase_return_cancel` | `sales_return_cancel` |

`SalesService.cs:2487` 이 `RecordSalesDeliveryCancelAsync`(명세서 취소) 를 빌려 썼다.
⇒ 장부에서 **«매출반품»과 «명세서 취소»가 같은 키로 섞여** 구분 불가.
⇒ `FinanceService` 「확정전표 기표 누락」 검사가 **매출반품을 셀 키가 없어** 항목 자체가 없었다.

## 3. 변경 목록

| # | 파일 | 내용 |
|---|---|---|
| 1 | `AutoJournalHelper.cs` | `RecordSalesReturnAsync` 신설 — `source_type='sales_return'` |
| 2 | `SalesService.cs:2487` | 반품 확정이 신설 메서드를 부르도록 교체 (**1197 명세서취소는 무접촉**) |
| 3 | `FinanceService.cs` | 기표누락 검사에 `sales_returns` 항 추가 |
| 4 | `DB-115_*.sql` | 과거 분개 라벨 정정 (사장님 결재 ①) |
| 5 | `hitpan_db_clean.sql` | DB-115 시드 등록 (헌법 #36) |
| 6 | `SalesReturnJournalSourceTypeGateTests.cs` | 게이트 4종 |

### ⚠️ 매입 복붙 금지 — 계정이 다르다

사장님 지시대로 매입반품을 복사하지 않았다.

| | 차변 | 대변 |
|---|---|---|
| 매입반품 | 매입채무(`AccountsPayable`) | 매입 + 부가세대급금 |
| **매출반품** | **매출 + 부가세예수금** | **외상매출금(`AccountsReceivable`)** |

게이트 2번이 `AccountsPayable`·`VatReceivable` 혼입을 **금지 조건으로** 잡는다.

### 🟢 분개 금액·차대는 한 줄도 안 바꿨다

종전 분개도 금액·방향은 옳았다. 바뀐 것은 **분류 라벨과 적요 문구뿐**이다.
`journal_lines` 무접촉 ⇒ 헌법 #3(원장 INSERT ONLY) 저촉 없음.

## 4. 마이그레이션이 안전한 이유

과거분은 `source_id` 로 정확히 가려진다.

| | source_id |
|---|---|
| 매출반품 확정 | `returnId` |
| 명세서 취소 | `'{deliveryId}:cancel'` |

⇒ `sales_returns` 에 그 id 가 **실재하는 행만** 반품이다. 명세서 취소는 구조적으로 안 걸린다.

## 5. 실측 결과 (로컬 MariaDB `hitpan_e2e`)

시나리오 4건 — 반품 2 · 진짜 명세서취소 1 · 다른 테넌트 1

| 대상 | before | after | 판정 |
|---|---|---|---|
| JE-A·JE-B (반품) | `sales_delivery_cancel` | **`sales_return`** | 🟢 정정됨 |
| JE-C (명세서취소) | `sales_delivery_cancel` | 그대로 | 🟢 무접촉 |
| JE-D (다른 테넌트) | `sales_delivery_cancel` | 그대로 | 🟢 격리 유지 |

- **재실행 무해** — 2회차 no-op 확인
- **검산 쿼리 0건** — 마이그 주석의 leftover 쿼리 = 0

> ⚠️ **이 실측은 시험용 DB(`hitpan_e2e`)다.** 설치 실물 확인은 별도다.

## 6. 게이트 4종 — 반증까지 확인

| # | 검사 | 반증 |
|---|---|---|
| 1 | 전용 메서드가 `sales_return` 사용 | — |
| 2 | 매출 계정 사용 · 매입 계정 혼입 금지 | — |
| 3 | 반품확정이 전용 메서드 호출 | 🔴 되돌리니 **FAIL** ✅ |
| 4 | 정합성 검사가 매출반품 셈 | 🔴 항 빼니 **FAIL** ✅ |

**3·4번은 봉합을 빼고 실제로 빨간불을 확인했다.** 메서드 **본문만 잘라** 검사하므로
주석·다른 메서드의 낱말이 거짓 초록불을 만들지 못한다.

## 7. 빌드·시험

- 빌드 **errors 0 / warnings 0** (헌법 #19)
- 전체 시험 **1093/1093 통과**
- 🔴 기존 게이트가 DB-115 시드 누락을 **실제로 잡았다** — 등록 후 통과
