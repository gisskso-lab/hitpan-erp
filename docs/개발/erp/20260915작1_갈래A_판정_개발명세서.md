# 개발명세서 2026-09-15 — 작1 갈래 A · 장부 반영 판정 · F3 잔액 · 기준일 · 재고 품목 키

| 항목 | 내용 |
|---|---|
| 근거 작업지시서 | `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` §11-1 · §11-2(A) · §11-3(G1) · §13(R5-2) |
| 근거 설계 | `docs/설계/erp/20260915_설계_자료이관_머리없는줄_분류봉합.md` §14 · §16 · §17 |
| 병렬검증 반영 | [3-V] 병렬이슈36 (순번 0 연결 키 제외) — `docs/검증/병렬이슈/20260915_병렬이슈36_*.md` (이 worktree 에는 파일 없음 · 지시 메시지로 받음) |
| PM 결재 | ✅ 2026-09-15 (작업지시서 §13) |
| 개발팀 지휘 | PM 브라운킴 |
| 구현 담당 | 갈래 A 구현 에이전트 (worktree `agent-a231487360b0697dc`) |
| 선행 커밋 확인 | `d299e479` 포함 확인 후 착수 |

## 1. 무엇을 만들었나

| 요구 | 구현 (`src/HitPan.Application/Services/LegacyMdbMapping.cs`) | 상태 |
|---|---|---|
| ① 판정 = 머리표 OR 거래처원장 연결 · 종류 맞춤 · 거래처 키 금지 | `LegacyPostingStatus`:255 · `LegacyDocKey`:266 · `LegacyLedgerLinkKey`:277 · `IsPostedToBooks`:290 · `ClassifyPosting`:302 | ✅ |
| ① DOCFE 없음/0행 → 전부 반영(R5·R5-2) | `ClassifyPosting` 첫 줄 → `Unclassified` · `BuildHeaderKeySet`:330 (null/0행 → null) | ✅ |
| ① DOCF5 없음 → 머리표로만 | `BuildLedgerLinkKeySets`:345·365 (null/0행 → (null,null)) | ✅ |
| ① 병렬이슈36 순번 0 제외 | 집합 쪽 `:356` (S_SSUN 0 skip) + 판정 쪽 `:319` (IJ_SEQ 0 연결 불가) — 두 겹 · 머리표 판정은 불변 | ✅ |
| ① 이름표 표시만 | `UnpostedReason`:517 (danga·assembly·no_name·test_zero_amount·test·other) · `UnpostedReasonText`:529 — 판정 함수는 이 값을 안 본다 | ✅ |
| ② 기준일 | `LegacyBaseDate`:453(목록) · :477(DataTable) · `ResolveLegacyDate`:494 · `TryParseLegacyDate`:445 | ✅ |
| ③ 재고 품목 키 | `StockItemKey(name, spec, warehouse=null)`:506 · 구분자 `StockKeySeparator`(U+001F):498 | ✅ |
| ④ F3 | `IsCarryOverRow`:385 · `ReadPartnerLedgerRows`:371 · `PartnerLegacyBalances`:398 · `SummarizeBalances`:431 | ✅ |
| G1 게이트 + CI 등록 | `src/HitPan.Tests/Integrity/MdbPostingClassifyGateTests.cs` (14 Fact) · `.github/workflows/build-and-test.yml` GATES `MdbPostingClassifyGate` | ✅ |

### 공개 API (갈래 B·C·D·E 가 부를 것)
```
LegacyMdbMapping.LegacyPostingStatus { Posted, Unposted, Unclassified }
LegacyMdbMapping.LegacyDocKey(string Dt, string Io, int Seq, int Buy)      .Of(dt, io, seq, buy)  // Trim
LegacyMdbMapping.LegacyLedgerLinkKey(string Ymd, int Seq)                   .Of(ymd, seq)
LegacyMdbMapping.LegacyPartnerLedgerRow(int Buy, string Ymd, string Gu, decimal Bal, decimal Suk, int SSun)
bool   IsPostedToBooks(bool headerTableOk, bool hasHeader, bool hasLedgerLink)
LegacyPostingStatus ClassifyPosting(LegacyDocKey doc, IReadOnlySet<LegacyDocKey>? headerKeys, IReadOnlySet<LegacyLedgerLinkKey>? salesLinks, IReadOnlySet<LegacyLedgerLinkKey>? purchaseLinks)
HashSet<LegacyDocKey>? BuildHeaderKeySet(DataTable? docfe)                  // IJA_DT·IJA_IO·IJA_SEQ·IJA_BUY
(HashSet<LegacyLedgerLinkKey>? Sales, …? Purchase) BuildLedgerLinkKeySets(DataTable? docf5 | IEnumerable<LegacyPartnerLedgerRow>?)
List<LegacyPartnerLedgerRow> ReadPartnerLedgerRows(DataTable? docf5)       // S_BUY·S_YMD·S_GU·S_BAL·S_SUK·S_SSUN
bool   IsCarryOverRow(string? sGu, string? sYmd)
Dictionary<int, decimal> PartnerLegacyBalances(IEnumerable<LegacyPartnerLedgerRow> rows)   // + 미수 · − 미지급
(decimal Receivable, int ReceivableCount, decimal Payable, int PayableCount) SummarizeBalances(IReadOnlyDictionary<int, decimal>)  // Payable = 절대값
bool   TryParseLegacyDate(string? yyyymmdd, out DateTime date)
DateTime? LegacyBaseDate(IEnumerable<string?>? docfcYm, IEnumerable<string?>? docf5CarryYmd, IEnumerable<string?>? docfbDt)
DateTime? LegacyBaseDate(DataTable? docfc, DataTable? docf5, DataTable? docfb)   // IM_YM · S_GU+S_YMD · IJ_DT
DateTime  ResolveLegacyDate(string? ijDt, DateTime baseDate)
string StockItemKey(string? name, string? spec, string? warehouse = null)
string UnpostedReason(string? dt, int buy, int seq, decimal groupAbsAmount, decimal groupAbsVat) · UnpostedReasonText(string?)
```
- 호출 규칙: DOCFB 묶음 키는 `(IJ_DT, IJ_IO, IJ_SEQ, IJ_BUY)` 를 `LegacyDocKey.Of` 로 만든다(설계 §1 DOCFE 조인 키). IJ_IO 는 문자열 "1"/"2".
- `LegacyBaseDate` 가 null(세 표 모두 날짜 없음)이면 호출자가 정한다 — 이 함수는 오늘 날짜로 채우지 않는다.

## 2. 변경 파일

| 파일 | 변경 | 왜 |
|---|---|---|
| `src/HitPan.Application/Services/LegacyMdbMapping.cs` | 추가 350줄 · 수정/삭제 0 (`using` 2줄 + 클래스 끝에 덧붙임) | 갈래 A 순수 함수 |
| `src/HitPan.Tests/Integrity/MdbPostingClassifyGateTests.cs` | 신규 | G1 |
| `.github/workflows/build-and-test.yml` | 추가 8줄 | GATES 등록 |
| `docs/개발/erp/20260915작1_갈래A_판정_개발명세서.md` · `docs/개발/erp/INDEX.md` | 신규 · 1줄 | #42 · #41 |

- #1 추가만: `git diff --stat` 358 insertions / 0 deletions. 기존 `ItemKey`·`MdbMigrationService.BuildItemKey` 무수정.
- DDL 없음(해당없음 #36).

## 3. 헌법 준수

| 조항 | 어떻게 | 근거 |
|---|---|---|
| #1 | 덧붙임만 | diff 0 deletions |
| #2 | tenant 무관 순수 함수 | — |
| #4 | 잔액·금액 전부 decimal · DataTable 읽기도 decimal | `:398` · `RowDec` |
| #13 | 새 SQL 없음 (MDB 는 사본 읽기 전용 실측만) | — |
| #15 | catch 없음 (TryParse 로 처리) | — |
| #19 | 빌드 0/0 | §4 |

## 4. 자체 확인 (실측)

| 항목 | 명령 | 결과 |
|---|---|---|
| 빌드 | `scripts/dev/build-brief.ps1 -Project src/HitPan.Tests/HitPan.Tests.csproj` | errors 0 / warnings 0 |
| 전체 테스트 | `scripts/dev/test-brief.ps1` | 1412 passed · 0 failed · 0 skipped |
| G1 | 위 결과 중 `MdbPostingClassifyGateTests` 14/14 | 초록 · DB 없음 · SKIP 없음 |
| MDB 실물 대조 | 사본 DOCFE·DOCF5·DOCFB(묶음)·DOCFC 를 TSV 로 뽑아(scratchpad `gA/export.ps1`) **이 파일을 그대로 링크 컴파일한** 콘솔(`gA/probe`)로 실행 · 병렬이슈36 반영 후 재실행 | 아래 표 |

| 항목 | 설계 기대 | 실측 (병렬이슈36 반영 후) |
|---|---|---|
| 판매 머리O·연결O / 머리O·연결X / 머리X·연결O / 둘 다 X | 115,148 / 2 / 0 / 353 | 115,148 / 2 / 0 / **353** (414줄) |
| 매입 같은 표 | 1,962 / 0 / 0 / 210 | 1,962 / 0 / 0 / **210** (235줄) |
| DOCFB 묶음 합 | 117,675 | 117,675 (Unclassified 가정 시 전부 반영) |
| F3 미수 / 미지급 | 142,062,113 (1,059) / 51,577,479 (530) | **142,062,113 (1,059) / 51,577,479 (530)** |
| F1(함정) | 다름 | 141,931,113 (1,058) / 51,522,479 (529) — 다름 확인 |
| 기준일 | 2026-02-28 | 2026-02-28 · DOCFC 뺌 → 2026-02-28 · DOCFB 만 → 2026-02-20 |

→ 병렬이슈36 순번 0 규칙 추가 전후 판매 353 · 매입 210 **변동 없음**.

### 무력화 시험 (봉합 빼고 FAIL) — 이 파일 사본을 한 줄씩 되돌려 G1 만 링크 실행 (scratchpad `gA/mut/run.ps1`)

| # | 무력화 | 결과 |
|---|---|---|
| M0 | 원본(대조) | 14/14 초록 |
| M1 | 수금·지급 줄도 연결 집합에 넣음(종류 안 가림) | 3 FAIL |
| M2 | 판매↔매입 집합 뒤바꿈 | 4 FAIL |
| M3 | 연결에 거래처 코드 조건 | 4 FAIL |
| M4 | 머리O·연결X → 미반영(AND) | 7 FAIL |
| M5 | DOCFE 없음 → Unposted | 1 FAIL |
| M6 | F3 마지막 이월만(그 뒤 줄 누락) | 2 FAIL |
| M7 | F1 (이월 무시 전 줄 합) | 2 FAIL |
| M8 | 기준일 DOCF5 폴백 끊음 | 1 FAIL |
| M9 | 잘못된 날짜 → 기준일 아님 | 1 FAIL |
| M10 | 품목 키 대소문자 구분 | 1 FAIL |
| M11 | 품목 키 Trim 안 함 | 1 FAIL |
| M12 | 이월행도 연결 키 | 1 FAIL |
| M13 | 순번 0 집합 쪽 가드 제거 | 1 FAIL |
| M14 | 순번 0 판정 쪽 가드 제거 | 1 FAIL |

- 🔴 무력화 시험이 게이트를 두 번 고쳤다: ① 첫 G1n 은 판정 쪽 가드가 겹쳐 막아 **M13 이 초록**이었다 → 집합 직접 단언(`DoesNotContain`) 추가. ② G1c(이월행) 가 순번 0 이라 병렬이슈36 가드에 가려 **M12 가 초록**이 됐다 → 순번 5 로 바꿔 단독 방어선 확인.

## 5. 안 한 것 / 못 한 것

- **서비스 배선 0** — `MdbMigrationService`·보관 표·대사표·재고/잔액 맞춤에서 이 함수를 부르는 것은 갈래 B·C·D·E 몫. G1 은 판정만 잰다(배선은 G2·G4·G6).
- `UnpostedReason` 이름표 분포(설계 §2 표)는 MDB 로 **대조하지 않았다** — 표시용이라 판정 기대값에 영향 없음. 대사표·보관 표 화면 기대값이 필요하면 갈래 B/C 에서 [4] 실측.
- 이름표 코드 문자열(`danga` 등)과 화면 글자는 설계에 고정 문구가 없어 구현자가 정했다 — 갈래 B(reason 칸)·H(화면)가 다르게 원하면 PM 결재로 조정.
- `StockItemKey` 는 Trim + `ToUpperInvariant` 까지만. `utf8mb4_unicode_ci` 의 전각/반각·악센트 동일시는 흉내내지 않았다(설계 §16 요구 범위 밖) — 갈래 D 가 DB 쪽 GROUP BY 와 섞어 쓰면 차이 가능 ⚠️[4].
- 같은 날 이월행 중복 시 첫 줄 유지(실측 0건이라 규칙 미결) — 다른 MDB 에서 나오면 결재 필요.
- `BuildItemKey`(이관 매핑 · 대소문자 보존)와의 관계: 건드리지 않았고, 재고 합산 키만 새로 뒀다. 이관이 대소문자만 다른 품목을 **별개 품목으로 등록**하는 현행 동작은 그대로 — 갈래 D 가 재고 맞춤 때 두 품목 중 어디에 맞춤 줄을 넣을지는 D 설계 몫 ⚠️.
- 병렬이슈36 문서 원문은 이 worktree 에 없어 읽지 못했다 — 지시 메시지의 규칙대로만 구현.
- push 안 함(지시).
