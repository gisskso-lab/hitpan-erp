# 작지서 3건 — 백오피스 P0 + 랜딩 가입 + 자식 계정 (W2 가도)

> 박제: 2026-05-31 19:00 | PM 브라운킴
> 사장님 결재: "A" (옵션 A 6종 PM 가능 영역 All-In)
> 발진: W2 (2026-06-04~) — W1 게이트 6/3 통과 후
> 헌법: #29 (PM 작지서 박제만, 실제 코드는 매니저 가도)

---

## 작8 — 백오피스 P0 3축 (백엔드 매니저 + 프론트 매니저)

### 1. 산출 산출
**3축 CRUD + 1 페이지**
1. **라이선스 발급 UI** — 본사 admin이 신규 가입자 라이선스 키 발급·갱신·만료
2. **고객 회사 정보 관리** — 가입 회사 CRUD (사업자번호·대표·주소·연락처)
3. **구독 결재 상태 관리** — 가입·갱신·해지·연체·환불 상태 박제

### 2. 신규 영역
- **백엔드**:
  - `src/HitPan.API/Controllers/Admin/AdminLicenseController.cs` (CRUD + 발급)
  - `src/HitPan.API/Controllers/Admin/AdminCustomerController.cs` (회사 CRUD)
  - `src/HitPan.API/Controllers/Admin/AdminSubscriptionController.cs` (구독 상태)
- **DTO**: 각 영역 Request·Response·List·Detail
- **DDL**: `licenses` 박제 박제 + `customers` + `subscriptions` 신규

- **프론트** (Platform 영역 확장):
  - `Platform/AdminLicensesPage.razor` (라이선스 발급·관리)
  - `Platform/AdminCustomersPage.razor` (고객 회사 CRUD)
  - `Platform/AdminSubscriptionsPage.razor` (구독 결재 관리)

### 3. xUnit
- 영역별 5~10건 (CRUD + 권한 + tenant_id 검증)

### 4. 게이트 (6/10 W2 D7)
- 신규가입 가도 = 본사 admin 1분 안 라이선스 발급
- 회사 CRUD = 사업자번호 검증 박제
- 구독 = 토스 webhook 수신 시 자동 상태 갱신

### 5. 헌법 정합
- #2 tenant_id JWT
- #7 SaaS ↔ ERP 권한 혼용 0 (PlatformOnly 정책)
- #22 본사 데이터 메타만 (업무 데이터 0)
- #19 errors 0 + warnings 0

---

## 작9 — 랜딩 가입 폼 동작 (프론트 매니저 + 백엔드 매니저)

### 1. 산출 산출
**랜딩 → 백오피스 → ERP 신규가입 흐름 박제**
1. 가입 폼 동작 (HTML → API 호출)
2. 사업자번호 검증 (외부 API 또는 자체 검증)
3. 토스페이먼츠 결재 연동
4. 이메일 OTP 인증
5. 라이선스 자동 발급 + 메일 발송
6. EXE 다운로드 페이지 (라이선스 키 인증 후)

### 2. 신규 영역
- **백엔드**:
  - `src/HitPan.API/Controllers/LandingController.cs` 확장 (가입 폼 수신)
  - `src/HitPan.API/Controllers/PaymentTossController.cs` (토스 webhook + 결재)
  - `src/HitPan.API/Controllers/OtpController.cs` (이메일 OTP)
  - `src/HitPan.API/Services/LicenseIssueService.cs` (자동 발급 흐름)
  - `src/HitPan.API/Services/SignupEmailService.cs` (안내 메일 발송)

- **프론트**:
  - `src/HitPan.Web/wwwroot/landing-web.html` 폼 동작 (JS fetch)
  - `src/HitPan.Web/wwwroot/landing-sns.html` 동일
  - `src/HitPan.Web/wwwroot/download.html` 신규 (라이선스 키 인증 후)

### 3. 외부 의존
- **사업자번호 검증 API**: 국세청 또는 외부 (PM 결재 의뢰 영역)
- **토스페이먼츠 키**: 사장님 박제 영역
- **SMTP**: 사장님 7월 박제 영역 (별도 가도)

### 4. xUnit
- 가입 흐름 E2E 5건
- 토스 webhook 검증 5건
- 라이선스 발급 5건

### 5. 게이트 (6/10 W2 D7)
- 가입 폼 → 결재 → 라이선스 메일 = 10분 이내
- 이메일 OTP 5분 이내 도착
- EXE 다운로드 라이선스 키 검증 통과

### 6. 헌법 정합
- #22 본사 데이터 = 메타만 (사업자번호·이메일만, 업무 데이터 0)
- #18 v3 본사 ↔ 고객 ERP 분리
- #25 3대 원칙 (쉽게·정확하게·안전하게)

---

## 작10 — 자식 계정 발급 UI (프론트 매니저 + 백엔드 매니저)

### 1. 산출 산출
**2계층 계정 = 부모(대표) ↔ 자식(직원)**
1. 부모 계정 (대표) = 첫 로그인 후 자식 계정 발급 권한
2. 자식 계정 = 권한별 화면 차단
3. 자식 계정 비활성·재활성·삭제

### 2. 신규 영역
- **백엔드**:
  - `src/HitPan.API/Controllers/UserController.cs` 확장 (자식 계정 발급 endpoint)
  - `POST /api/users/children` (자식 계정 발급)
  - `PUT /api/users/children/{id}/status` (활성·비활성·삭제)
  - 권한 매핑 영역 (UserPermissionService 확장)

- **프론트**:
  - `src/HitPan.Web/Pages/Users.razor` 확장
  - 자식 계정 발급 모달 (이메일·역할·권한 박제)
  - 권한별 사이드바 표시·차단 (이미 일부 박제, 강화)

### 3. xUnit
- 자식 계정 발급 5건
- 권한 매핑 검증 5건
- 부모 계정 ID 정합 5건

### 4. 게이트 (6/10 W2 D7)
- 대표가 자식 계정 1분 안 발급
- 자식 계정으로 로그인 시 권한별 화면 차단 정합
- 부모 계정 ID 자식과 분리 정합 (헌법 #22)

### 5. 헌법 정합
- #2 tenant_id JWT
- #7 SaaS ↔ ERP 권한 혼용 0
- #11 권한 어드민 직접 설정

---

## 발진 일자 박제

| 일자 | 작지서 | 담당 |
|---|---|---|
| 6/3 (수) W1 통과 | 작8·9·10 정식 발행 | PM → 매니저 4인 |
| 6/4~6/7 | 작8 백오피스 P0 발진 | 백엔드 + 프론트 매니저 |
| 6/4~6/8 | 작9 랜딩 가입 폼 발진 | 백엔드 + 프론트 매니저 |
| 6/5~6/8 | 작10 자식 계정 발진 | 백엔드 + 프론트 매니저 |
| 6/10 (수) W2 D7 | 3 작지서 게이트 통과 + 검증 | 검증팀 |

---

**문서 끝.** 6/3 W1 통과 후 매니저 가도 정합 박제.
