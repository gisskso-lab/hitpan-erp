# 틀 — 인수인계서 (AI수석 정의)

> **쓰는 때**: SOP [★] 세션·컨텍스트 만료 전. **다음 세션이 끊김 없이 이어받는 게 유일한 목적이다.**
> **저장 위치**: `docs/운영기록/` — 저장 위치는 사장님 결재(헌법 #41).
> **파일명**: `{YYYYMMDD}_인수인계서_{주제}.md`
>
> ⚠️ **이 문서의 독자는 사람이 아니라 "기억이 지워진 다음 세션의 AI"다.**
> 그가 **이것만 읽고 바로 일을 시작할 수 있어야** 통과다. 배경 설명보다 **다음 행동**이 먼저다.

---

```markdown
# 인수인계서 {YYYY-MM-DD} — {주제}

> 🟢 **진실원**: 이 문서가 {주제} 계열의 현재 진실원이다. {대체한 옛 문서}는 ⬛흡수.
> 세션 시작 즉시 §0 → §1 → §9 순으로 읽어라.

## §0. 30초 요약 (이것만 읽어도 방향은 잡힌다)
- 지금 무엇을 하고 있나: {한 줄}
- 어디까지 됐나: {한 줄}
- **다음 첫 행동**: {한 줄 — 명령어까지}

## §1. 현재 상태 (실측)
| 항목 | 상태 |
|---|---|
| 브랜치 | {develop / 작업브랜치} |
| 최신 커밋 | `{해시}` {제목} |
| 미커밋 | {N}건 (있으면 무엇인지) |
| 빌드 | errors {N} / warnings {N} |
| 배포 | {버전} {게시 여부} |

## §2. 오늘 한 일 (커밋 단위)
| 커밋 | 내용 | 검증 |
|---|---|---|
| `{해시}` | | |

## §3. 결정된 것 (사장님 결재 완료)
- {결정 내용} — 근거: 사장님 "{원문 인용}"
- ⚠️ 이건 **다시 논의하지 마라**. 결재가 끝났다.

## §4. 미결·보류 (사장님 결재 대기)
| 안건 | 왜 보류 | 누가 결정해야 하나 |
|---|---|---|

## §5. 함정 — 다음 세션이 밟을 지뢰
> **가장 중요한 절.** 오늘 내가 헛수고한 것을 다음 세션이 반복하지 않게.
- {함정}: {왜 그런지} → {어떻게 피하나}

## §6. 건드리면 안 되는 것
- {파일·설정·데이터} — 이유: 헌법 #{NN} / 사고 이력

## §7. 관련 문서
- 설계: `docs/설계/.../{파일}`
- 작업지시서: `docs/개발/.../{파일}`
- ⬛흡수된 옛 문서: {목록} — 읽지 마라, 이 문서가 대체했다

## §8. 남은 백로그
- [ ] {항목} — 우선순위 {P0/P1/P2}

## §9. 다음 세션 첫 행동 (복사해서 바로 실행)
```bash
{실제로 칠 명령어}
```
그다음: {무엇을 확인하고 무엇을 판단하는지}
```

---

## AI수석 작성 지침

1. **§0과 §9를 가장 공들여 써라.** 다음 세션은 이 둘만 보고 움직인다.
2. **§5(함정)가 이 문서의 진짜 값어치다.** 오늘 겪은 실패·헛수고를 반드시 남긴다.
   (예: "PowerShell 5.1은 BOM 없는 스크립트를 ANSI로 읽어 한글이 깨진다")
3. **§3(결정된 것)에 근거 인용을 반드시 넣어라.** 없으면 다음 세션이 결정을 다시 뒤집는다.
4. **옛 문서를 ⬛흡수로 명시하라**(헌법 #41). 안 하면 다음 세션이 옛 문서를 최신으로 오판한다.
5. 길게 쓰지 마라. **읽는 데 3분 넘으면 안 읽힌다.** 상세는 링크로.
