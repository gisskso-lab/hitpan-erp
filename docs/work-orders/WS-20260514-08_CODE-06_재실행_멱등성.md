# WS-20260514-08 — CODE-06 partners·collections 재실행 멱등성 봉합

**발행:** 2026-05-14 (수) / PM
**결재:** 사장님 (임원 합의)
**담당:** 백엔드 매니저 + DB 매니저 (듀얼 리뷰)
**헌법:** #3 (INSERT ONLY 멱등), #20 (워크플로우 끊김 0)
**마감:** 5/14 11:00

---

## 배경

- **현상**: `MdbMigrationService.cs:605` partners INSERT, `:1403` collections INSERT는 **INSERT IGNORE 아님** + GUID PK 매번 새로 발급
- **위험**: 재실행 시 partner_code UNIQUE 충돌 또는 collections 614K씩 중복 누적 → Aging/매출잔액 KPI 왜곡
- **재현**: PYOJUN 단계 정상 → PANDATA 실패 → 재실행 클릭 → 즉시 throw 또는 데이터 증식
- **현재 614,212행 보존 상태** — 재실행 1회 = 1,228K로 두 배

## 봉합 범위

1. partners INSERT → `INSERT ... ON DUPLICATE KEY UPDATE updated_at = VALUES(updated_at)` 또는 멱등 키 기반 SELECT 후 skip
2. collections INSERT → 멱등 키 `(tenant_id, source_type, source_id)` 기반 INSERT IGNORE
3. **collections 멱등 키 UNIQUE 인덱스 추가는 WS-20260514-10에서 처리** (선행 의존성)
4. source_id 결정: `"mig-{S_YMD}-{S_BUY}-{S_SEQ}"` 패턴

## 의존성

- ⚠️ WS-20260514-10 (collections UNIQUE 추가) 먼저 완료 필요
- partners는 기존 `uq_tenant_code (tenant_id, partner_code)` 활용 → 추가 DDL 불필요

## 구현 위치

- 수정: `src/HitPan.Application/Services/MdbMigrationService.cs`
  - `MigratePartnersAsync` (L:573~)
  - `MigrateCollectionsAsync` (L:1399~)

## 검증 게이트

- [ ] errors 0 + warnings 0
- [ ] CodeQL SAST 통과
- [ ] 단위 테스트: 같은 MDB 2회 실행 → partners·collections 카운트 변화 0
- [ ] V3 dry-run 60K 1차 PASS 후 2차 재실행 → 카운트 변화 0 검증

## 산출물

- 봉합 커밋 1건
- 단위 테스트 2건
- dry-run 재실행 검증 로그
