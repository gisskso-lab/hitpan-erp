# 2026-06-18 ERP 계층제거 완료 + 운영 워크플로우 봉합 진행상태 (compact 전 저장)

> 작성: PM 브라운킴. 사장님 /compact 직전 박제. 받아쓰기 아님 — 실제 빌드·import·검증으로 증명된 사실만.

---

## 0. 한 줄 요약
**히트판 ERP에서 본사·대리점·테넌트 계층구조를 전부 제거(보안 격벽), ERP는 부모/자식 + 업무만. 완료·검증됨. 다음 = 운영 워크플로우(가입→시리얼→설치·로그인) 끝김점 봉합.**

---

## 1. ✅ 완료된 것 — ERP 계층 제거 (전부 빌드 0/0 + 검증)

### 왜 했나 (사장님 배경)
- 원래 ERP 하나로 본사/대리점/고객사 로그인별 화면 분기하려던 옛 설계 잔재.
- 보안 이유로 백오피스로 계층 이관, ERP는 업무 플랫폼만: **"고객사 하나라도 뚫리면 본사·다른 고객사까지 위험."** (헌법 #7·#22·#35)

### 코드 제거 (완료, API+Web 빌드 0/0)
1. **JWT 클레임** platform_id·reseller_id 제거 — `AuthService.cs:185-196`
2. **TenantMiddleware** reseller_admin/platform_admin 분기 제거 — 부모/자식은 tenant_id로만 통과
3. **User 엔티티** PlatformId·ResellerId 속성 + EF 매핑(UserConfiguration) 제거
4. **HitPanControllerBase** IsPlatformAdmin 제거
5. **BackofficeAuth 하위시스템 전체 삭제**: BackofficeAuthService.cs, IBackofficeAuthService.cs, DTOs/Backoffice/*(6개), BackofficeRepositoryBase.cs, JwtClaimsBuilder.cs, Program.cs:190 DI
6. **프론트 잔재**: Web/Services/BackofficeService.cs, Web/Models/Backoffice/BackofficeModels.cs, wwwroot/css/backoffice-dashboard.css, Web/Program.cs:73 DI, index.html:28 링크, .bak 2개, AccountTypeHelper의 IsPlatformAdmin/IsResellerAdmin+상수
7. **잔여 죽은 정책**: Program.cs PlatformOnly/ResellerOnly/OwnerOnly/PlatformManagerOrAbove/PlatformOnlyV2/ResellerSelfOnly 6개 제거 (ERP 소비 0건 확인)
8. **PermissionService.cs:227** platform_admin 바이패스 절 제거 (tenant_admin 사수)
9. **DeviceController.cs:35,60** platform_admin 절 제거 (tenant_admin만, 더 엄격해짐)

### DB 제거 (완료, 빈DB import 에러0)
- `installer/hitpan_db_clean.sql`: 계층 16개 테이블 블록 DROP (137→121 테이블)
  - resellers, reseller_accounts/applications/commission_policies/commissions/payouts/promotions/revenues, platforms, platform_admins, commission_settlements, bo_permissions, backoffice_refresh_tokens, landing_signups, beta_signups, tenant_payments + 동반 트리거 5개
- **tenants 테이블은 보존** (ERP 회사 식별 본체) — resellers FK(fk_tenants_reseller)만 끊음
- **import 실측**: hitpan_trgtest DB에 에러 0줄, 핵심12개(tenants/subscriptions/users/local_company/local_subscription/ai_usage_logs/billing/partners/items/sales_orders/stock_ledger/tax_invoices) 보존, ERP 트리거 3개(trg_psp_ratio_insert/update, trg_tax_invoices_issued_lock) 사수 확인
- ⚠️ **인코딩 주의**: 첫 시도에서 Get-Content(cp949)로 한글 깨뜨림 → 백업에서 `[System.IO.File]::ReadAllText(UTF8)`로 재작업해 복구. 백업: `installer/hitpan_db_clean.sql.bak-before-layer-drop`

### 🔴 보존 (PRESERVE — 손대지 마라)
- **local_subscription.reseller_id/reseller_tier** + CompanyBootstrapController:125,145 + WebhookInboundController:104,121 — 이건 계층 아니라 **백오피스→ERP 연결축 캐시**(헌법 #35). 제거 시 부트스트랩·웹훅 500.
- tenants/subscriptions DbSet, ERP 트리거 2개, TenantConfigReader

### 검증 (3중 워크플로우, 전부 통과)
- ERP 영향(로그인·6단계·AI·약관·기기): 4/4 **NO_IMPACT**
- 프론트(매니저+서브3): 동작 OK
- **418개 버튼 전수**: 깨진 버튼 0

---

## 2. 🔄 다음 작업 — 운영 워크플로우 봉합 (전수조사 완료, 끝김점 발견)

사장님 명세: `①랜딩가입→도메인+EXE다운로드 ②백오피스 시리얼→이메일 ③ERP 정상설치→로그인→작동`

### 전수조사로 발견한 끝김점
**① 랜딩가입** (src/HitPan.Backoffice.API, src/HitPan.Landing) — 폼·사업자검증(BizNoVerifyController)·도메인중복검사(DomainAliasController)·가입처리(LandingSignupController) 전부 WORKS.
- 🔴 **가입→도메인 자동생성 끊김**: 가입 땐 tenants.domain_alias 저장만, 실제 Cloudflare DNS는 백오피스 승인(SignupsAdminController.Approve→CloudflareDomainService.IssueAsync) 시점.
  - **사장님 결재 2026-06-18: 승인 게이트 유지 (가입→승인→도메인). 현 설계 존중.** → 이건 끝김 아니라 의도된 설계로 확정. 봉합 불필요.
- EXE 다운로드: updates.hitpan.kr/packages/HitPan-ERP-Setup-1.2.15.exe 하드코딩 링크. dist/에 로컬 EXE 존재하나 NCP 업로드 여부 UNKNOWN.

**② 백오피스 시리얼·이메일** — 시리얼 발급(SerialIssueService/SerialVerifyController)·license_key_hash 매핑 골격 있음. 이메일 발송은 메모리상 7월 예정 — 현재 구현상태 추가 확인 필요.

**③ ERP 설치·로그인** (installer/HitPan-Universal.iss, CompanyBootstrapController) — 부트스트랩(POST api/installer/bootstrap) 골격 있음.
- ⭐ **핵심 미확인**: 방금 tenants FK 16개 제거가 "빈 DB 부모계정 INSERT 1452(처음 미션 진범)"를 고쳤는지 — import는 통과했으나 **bootstrap→create-parent 부모계정 실제 INSERT 작동은 미실측.** 다음 최우선 확인.
- 1033 터널: CLOUDFLARE_API_TOKEN/ZONE_ID/ACCOUNT_ID 환경변수 미설정 시 skip(UNKNOWN).

### ⭐ 부모계정 1452 해소 — 실측 완료 (2026-06-18, 증명됨)
빈 DB(hitpan_trgtest, 121테이블)에서 직접 실측:
- users의 fk_users_tenant FK = 없음 (제거 확인)
- 빈 상태: users 0건, tenants 0건
- **부모계정 INSERT → 성공, ERROR 1452 없음!** users 0→1 (account_type=tenant_admin, is_parent=1)
→ **처음 미션 "로그인 500"의 진범 봉합 증명.** 이전엔 빈 DB에 tenants 시드 0건인데 users.tenant_id가 tenants FK 참조 → 부모계정 INSERT 1452 → 로그인 영구 불가였음. FK 제거로 해소.
- 남은 실측: 실제 API 경로(bootstrap→create-parent HTTP)로도 0→1 되는지(SQL 직접 INSERT는 증명됨, API 레이어는 추가 확인 권장)

### 다음 작업자 순서
1. ✅ **부모계정 1452 해소 실측 — 완료(위 참조)**. SQL 레벨 증명됨. (API HTTP 경로 실측만 잔여)
2. ✅ **② 백오피스 시리얼·이메일 구현상태 확정 — 완료 (2026-06-18 전수조사)** ↓아래 §2-A
3. ✅ **③ ERP 설치·터널·EXE 상태 확정 — 완료 (2026-06-18 전수조사)** ↓아래 §2-B
4. 🔄 운영 워크플로우 끝김점 봉합 계획 → 사장님 결재 ↓아래 §2-C

---

### §2-A ② 백오피스 시리얼·이메일 — 전수조사 결과 (코드 증거)
**시리얼 발급·검증·ERP 연결 = 완성. 실제 돌아감.**
- 발급: `SignupsAdminController.Approve:328-340` `GenerateLicenseKey()` → `HITP-XXXX-XXXX-XXXX-XXXX`. 승인 1번에 시리얼+해시+DNS+웹훅+이메일 자동 트리거(:91-217).
- 저장: `tenants.license_key_plain`(평문) + `license_key_hash`(HMAC) — :142-143
- 검증: `SerialVerifyController:38-171` + 설치 EXE용 `InstallerBootstrapController:45-207`(시리얼 HMAC→DNS·터널 발급→bootstrapToken 응답)
- 이메일 코드: `EmailSender.cs:46-110` SmtpClient 실구현 완성 + 호출 `SignupsAdminController.cs:196`

🔴 **확정된 미완 2건 (코드로 증명, 추측 아님)**:
1. **이메일 현재 발송 안 됨(NOOP)** — `Backoffice.API/appsettings.json:28-34` SMTP Host/User 빈 문자열 → `EmailSender.cs:58-63` 로그만 찍고 return false. 승인 흐름은 emailSent=false로 계속 진행(:206), 관리자가 응답 JSON의 평문 키 수동 전달. → **SMTP 자격증명 주입만 하면 즉시 자동 발송**(메모리 "이메일 7월 예정"과 정합).
2. **DDL 형상관리 누락(클린배포 500 위험)** — `tenants.license_key_plain` 컬럼 + `serial_verify_attempts`/`serial_verify_locks` 테이블 + `serial_verified` 컬럼의 CREATE/ALTER가 레포 .sql에 없음(운영서버 수동 ALTER로만 존재). 백오피스 Program.cs에 마이그 자동실행기 없음. → 신규 클린 DB로 백오피스 세우면 승인·검증 시 Unknown column/table 500.
- 부수: 미사용 죽은코드 `SerialIssueService`(DI 등록됐으나 호출 0건) — 실발급은 인라인 함수가 담당.

### §2-B ③ ERP 설치·터널·EXE — 전수조사 결과 (코드 증거)
- **EXE 파일 존재**: `dist/HitPan-ERP-Setup-1.2.15.exe` 292,454,423 bytes(279MB) 2026-06-17. 링크 `DownloadPage.razor:31` updates.hitpan.kr/.../1.2.15.exe 와 버전 일치. (NCP 업로드 여부는 코드로 판정 불가 — 미확인)
- **설치 부트스트랩**: EXE(.iss)는 `back.hitpan.kr/api/installer/bootstrap`(백오피스 C-1)만 호출(:399). 시리얼 검증·터널은 **백오피스가** 처리, EXE는 받은 토큰으로 `cloudflared service install` 실행만(:1015).
- **터널 환경변수**: `CLOUDFLARE_*`는 백오피스 서버에서만 읽음(`CloudflareDomainService.cs:54-56`). 미설정 시 `IsConfigured=false`(:60) → 터널 발급 블록 통째 skip → tunnelToken=null → EXE가 cloudflared 등록 조용히 스킵(:993). **에러 없이 터널 없는 설치 "성공"**.
- **1033 코드상 위치**: 백오피스 ingress/DNS PUT(`InstallerBootstrapController:120,132`)이 try/catch silent(:122-142) — 실패해도 부트스트랩 성공처리. env 미설정이거나 ingress PUT 실패가 1033 진범. EXE엔 1033 유발코드 없음.

✅ **③ "설치만으로 부모계정 안 생김"은 끊김 아님 — 의도된 설계로 확정**:
- 컨트롤러 헤더 주석 `CompanyBootstrapController.cs:14-20` + `LicenseSetupPage.razor:19-23`이 설계 명시.
- 흐름: 설치 → ERP 첫 화면 `/setup/license`(4단계 마법사) → Step1 시리얼검증(`api/landing/license/claim`:239) → Step3 회사정보(`api/setup/bootstrap`:289) → Step3→4 부모계정(`api/setup/create-parent`:348) → 로그인.
- **3단계 razor 모두 실구현 확인.** EXE가 직접 create-parent 안 부르는 건 사용자가 설치 후 라이선스 입력하는 의도된 동선. (사장님 ①승인게이트 "의도된 설계 존중"과 동일 성격)

### §2-C 운영 워크플로우 — 최종 끝김점 정리 (사장님 결재 대상)
전수조사 결론: **운영 워크플로우 골격은 ①②③ 전부 코드로 완성·연결돼 있음.** 진짜 막힌 끊김은 2건뿐:
| # | 끊김점 | 성격 | 봉합 주체 |
|---|---|---|---|
| 1 | 이메일 NOOP (SMTP 빈값) | 설정 주입 | 사장님(자격증명) — 코드는 완성 |
| 2 | 시리얼 DDL 형상관리 누락 | 코드/형상 | **우리가 봉합 가능** (clean.sql에 DDL 추가) |
| — | 가입→도메인 승인게이트 | 의도된 설계 | 봉합 불필요(결재 완료) |
| — | 설치→/setup/license 부모계정 | 의도된 설계 | 봉합 불필요 |
| — | 터널 1033 / env | 헌법 #29 | 사장님 영역 |

→ **우리가 코드로 봉합할 건 #2(DDL 형상관리) 하나.** 나머지는 설정·설계·사장님 영역.

---

## 3. 작업 방식 (사장님 지시)
- 전 직원(서브에이전트) 붙어서 + 검증팀·CTO·PM 교차검증 계속
- 정공법, 임시방편 0, 빌드 errors 0 + warnings 0 (헌법 #19)
- 받아쓰기·거짓말 절대 금지 — 추측 말고 실측·코드로 증명. 안 된 건 안 됐다고.
- 최종 수정은 사장님 결재. 전부 미커밋 (원복 가능).

---

## 4. 미커밋 상태
- 모든 변경 미커밋 — git stash에 초기 tenants-채우기 시도분 1건 있음(폐기 가능).
- 백업: installer/hitpan_db_clean.sql.bak-before-layer-drop
