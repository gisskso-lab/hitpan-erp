# 🔍 영역 1 — 마이그 코드 심도 분석 v2 (PM 단독 자율가동)

> **작성**: 2026-05-18 22:00 PM 브라운킴
> **대상**: `MigrateDeliveriesAndReceiptsAsync` 핵심 로직 (line 3900~4080)

---

## 🎯 진범 #21 정확한 봉합 안 박제 (분석만, 수정 0)

### 현재 코드 (line 3955·3960)
```csharp
var sourceId = $"mig-docfb-{dtStr}-{io}-{seq}-{buyCode}";  // buyCode 포함 ✅
var docNo    = $"DOCFB-{dtStr}-{io}-{seq:D4}";              // buyCode 누락 🚨
```

### 결과 (9차 박제)
| 패턴 | DISTINCT 수 | 평가 |
|---|---|---|
| sourceId | 115,147 (100%) | ✅ 멱등 |
| docNo | 16,099 (14%) | 🚨 99,048건 중복 |

### 봉합 옵션 매트릭스

| 옵션 | 변경 | 변경량 | 효과 | 위험 |
|---|---|---|---|---|
| **A** | `docNo`에 buyCode 추가 | 2줄 | 99,048건 봉합 | docNo 가독성 ↓ |
| **B** | `docNo = sourceId` 통일 | 2줄 | 100% 멱등 | docNo 가독성 ↓↓ |
| **C** | DB uq_delivery_no/uq_receipt_no DROP | DDL 2줄 | 변경 최소 | 운영 시 docNo 중복 위험 |
| **D** | INSERT IGNORE → ON DUPLICATE KEY UPDATE | INSERT 패턴 변경 | 덮어쓰기 | 헌법 #3 INSERT ONLY 위반 |

### PM 1차 추천: **옵션 A**
- 변경 최소 (2줄)
- delivery_no = `DOCFB-20250101-1-0001-12345` 형식 → 운영에서도 식별 가능
- 봉합 후 예상 결과: sales_deliveries 1,962행 / purchase_receipts 115,147행 → INSERT 100%

---

## 🎯 진범 #34 정확한 봉합 안 박제

### 현재 코드 (line 3988~3998)
```csharp
var itemKey = $"{pum}|{ku}";
if (itemMap.TryGetValue(itemKey, out var mappedId) && !string.IsNullOrWhiteSpace(mappedId))
{
    itemId = mappedId;
}
else
{
    skipItem++;
    if (itemMissSamples.Count < 5) itemMissSamples.Add(itemKey);
}
// itemId가 NULL일 수도 있는 상태로 salesItems.Add (line 4000)
```

### 9차 결과
- 매핑 실패 92,646건이 itemId = NULL로 INSERT (DOCFB 라인의 75%)
- legacy_pum / legacy_ku에 원본 보존
- **그러나 헌법 #20 워크플로우 끊김 = 재고 출고 흐름 끊김**

### 봉합 옵션 매트릭스

| 옵션 | 변경 | 효과 | 위험 |
|---|---|---|---|
| **A** | items에 'LEGACY_UNKNOWN_ITEM' fallback 추가, itemId null 시 사용 | 라인 보존 100% | items 마스터에 1 row 신규 |
| **B** | 동적 itemMap 보강 — 등장한 (pum, ku) 조합 모두 items에 자동 INSERT | items 마스터 폭발 | 마이그 시간 ↑ |
| **C** | 라인 skip 유지 (현재 코드) | 헌법 #20 위반 영구화 | 베타 게이트 미통과 |
| **D** | items 사전 빌드 — DOCFS + DOCFB 등장 합집합 | 정확도 최고 | 복잡도 ↑↑ |

### PM 1차 추천: **옵션 A**
- 변경 5줄
- 마이그 시간 +0 (fallback 1 row INSERT)
- 정합성: 라인 100% 보존 + 운영에서 fallback item 식별 가능
- 헌법 #20 정합

---

## 🎯 진범 #40 회계 차/대 불균형 봉합 안 박제

### 현재 코드 (MigrateJournalAsync, DOCF7)
- DOCF7 한 row → journal_lines 한 row (1:1 변환)
- SC_DR(차변) 또는 SC_CR(대변) 한 컬럼만 0 아닌 값
- entry_id 그룹 안에 차변+대변 합산이 안 맞는 case 23,166건

### 가설
- DOCF7 데이터 자체가 불완전 (레거시 데이터)
- 또는 마이그 그룹화 키가 다름 (한 entry 안에 차/대가 분리된 SC_DT가 필요할 수도)

### 봉합 옵션

| 옵션 | 변경 | 효과 |
|---|---|---|
| **A** | DOCF7 그룹화 키 재검토 (SC_DT + SC_JEN + ?) | 정확도 ↑↑ |
| **B** | 마이그 예외 영역 유지 + 운영 시점부터 정합성 절대 | 베타 가능 |
| **C** | journal_lines에 한정 사장님 결재 *"마이그 차/대 불균형 허용"* 박제 | 회계 화면 0점 |

### PM 1차 추천: **옵션 B (사장님 격언 정합)**
- 5/14 사장님 결재: 마이그 거래명세서·세금계산서 정합성 무시 → 회계도 동일 영역 확장
- 운영 시점부터 AutoJournalHelper 마이그 분개 중복 가드 + 헌법 #3 INSERT ONLY로 정합성 보장
- 단, 회계 화면에서 마이그 데이터 차/대 표시 시 별도 경고 라벨 필요

---

## 📋 영역 1 5/19 풀가동 추가 안건

1. `MigrateTransactionsAsync` 상세 분석 (진범 #24)
2. `MigrateBusinessCardsAsync`·`MigrateServiceTicketsAsync`·`MigrateDeliveryTrackingAsync` 진단 (진범 #25)
3. `MigrateSalesOrdersFromIOAsync`·`MigratePurchaseOrdersFromIUAsync` 품목 skip 71/88% 봉합 (진범 #32·#33)

---

**작성**: PM 브라운킴 (CTO 1인) 2026-05-18 22:00
**상태**: 1차 심도 분석. 5/19 09:00 본부장 회의 시 진범 #21·#34·#40 봉합 옵션 발표.
