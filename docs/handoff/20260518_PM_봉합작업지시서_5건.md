# 5/18 PM 봉합 작업지시서 5건 — 전수조사 후속 P0·P1

> **작성:** 2026-05-18 PM 브라운킴
> **수신:** 본부장 + 백엔드·DB·ERP·프론트·보안 매니저
> **근거 문서:** `20260518_PM_전수조사_수정항목보고서.md`
> **사장님 결재 필요 항목 = ⚠️ 표시**

---

## 📋 작업지시서 색인

| ID | 제목 | 우선순위 | 담당 | 예상 | 사장님 결재 |
|---|---|---|---|---|---|
| **WS-A** | 세금계산서 BulkCopy 봉합 | **P0** | 백엔드 + DB | 90~120분 | 불필요 |
| **WS-B** | 시드 잔재 일괄 삭제 (1,295,418행) | **P0** | DB | 30~60분 | ⚠️ 필요 |
| **WS-C** | 헌법 #26 v3 + Timeout 정책 변경 | **P0** | PM + 백엔드 + 프론트 | 1~2일 | ⚠️ 필요 |
| **WS-D** | 회계 journal 마이그 신규 (5종) | **P1** | 백엔드 + DB | 2~3일 | 불필요 |
| **WS-E** | 발주·수주 소량 진단·봉합 | **P1** | 백엔드 + DB + ERP | 2~3일 | 불필요 |

---

# 📄 WS-20260518-A — 세금계산서 BulkCopy 봉합

## 진범 ID: #4 (5/14·5/17 미해소)

### 배경
- 5/17 진범 #10 봉합 (schema 7군) = ✅ 운영
- 진범 #4 = BulkCopy 미적용 = ❌ 미해소
- 현재 132,582행 중 NULL 시드 66,603 + migration 65,979 = **마이그 자체는 작동 중이나 row-by-row로 느림**

### 증거 (PM 전수조사)
- 위치: `src/HitPan.Application/Services/MdbMigrationService.cs`
- line 2880~3028 `MigrateTaxInvoicesAsync`
- line 2973 header INSERT (Db.ExecuteAsync row-by-row)
- line 3002~3014 items INSERT (최대 4 × row)
- = **66,603건 × 5 = 332,015 INSERT 왕복**

### 작업 내용
1. `MigrateTaxInvoicesAsync` 내부에 BulkCopy 분기 신설
   - 패턴: stock_ledger·collections 동형 (`if (Db is MySqlConnection mysqlConn && tx is MySqlTransaction mysqlTx)`)
   - `BulkCopyTaxInvoicesAsync` + `BulkCopyTaxInvoiceItemsAsync` 신규 (또는 통합)
2. staging 테이블 2개 (`tax_invoices_stage_<guid>` + `tax_invoice_items_stage_<guid>`)
3. DataTable 빌드 + MySqlBulkCopy (BulkCopyTimeout = 1800)
4. UNIQUE 인덱스 사전 DROP (BulkCopy 무손실)
5. INSERT IGNORE SELECT로 본테이블 적재
6. legacy fallback 경로 보존 (factory 모드 아닐 때)

### 기존 코드 영향
- ✅ **추가만**, 기존 row-by-row 경로 보존
- ⚠️ ON DUPLICATE KEY UPDATE 동작 → INSERT IGNORE SELECT로 의미 변경 (멱등 키 source_id 검증 필수)
- ⚠️ tax_invoice_items header_id FK 보존 (Guid 일치 보장)
- ⚠️ tax_invoices.invoice_no UNIQUE 인덱스 DROP/복원 필요 확인
- 검증: 5/17 진범 #10 봉합과 schema 정합 유지

### 효과
- 332,015 왕복 → 단일 BulkCopy 2회 (header + items)
- **수십 분 → 1~2분 추정**
- 사장님 격언 *"끝 숫자"* 4축 (세무 영역) 가속

### 담당·일정
- 백엔드 매니저 + DB 매니저
- 5/18 09:45~12:00 (점심 전 완료)
- 검증: 5/18 13:00 사장님 PC 재마이그 실측

### 산출물
- commit 1건 (`fix(migration): tax_invoices BulkCopy 봉합 — 진범 #4`)
- 실측 로그 박제 (BulkCopy 적재 시간 ms)

---

# 📄 WS-20260518-B — 시드 잔재 일괄 삭제 ⚠️

## 진범 ID: 시드 광역 오염

### 배경
- 5/13 시드 데이터 + 5/14~5/17 마이그 = source_type NULL 잔존
- "끝 숫자" 검증 불가 (시드 + 마이그 혼재)
- 사장님 격언 *"99% + 1% 강제조정"*의 99% 명확화 불가

### 증거 (PM 전수조사)
```
tax_invoices:        NULL 66,603 + migration 65,979 = 132,582
sales_deliveries:    NULL 0 + migration_tx 66,603 + migration 89 = 66,692
collections:         NULL 614,212 + migration 614,212 = 1,228,424
─────────────────────────────────────────────────────
합 NULL 잔재:        680,815행 + sales_deliveries migration_tx 66,603 = 추적 필요
```

### 작업 내용 ⚠️ 사장님 결재 필수
1. **백업 우선** (필수, 회복 불가 방지)
   ```bash
   mysqldump -uhitpan -p hitpan_erp tax_invoices tax_invoice_items collections \
     sales_deliveries > backup_seed_cleanup_20260518.sql
   ```
2. **트랜잭션 시작** + 시드 NULL 삭제
   ```sql
   START TRANSACTION;

   -- tax_invoice_items가 tax_invoices.invoice_id를 참조하는지 사전 확인
   DELETE FROM tax_invoice_items
     WHERE invoice_id IN (SELECT invoice_id FROM tax_invoices WHERE source_type IS NULL);
   DELETE FROM tax_invoices WHERE source_type IS NULL;
   DELETE FROM collections WHERE source_type IS NULL;

   -- sales_deliveries 'migration_tx' = 시드인지 마이그인지 추적 후 결정 (보류)

   -- 검증 쿼리
   SELECT 'tax_invoices' AS tbl, source_type, COUNT(*) FROM tax_invoices GROUP BY source_type
   UNION ALL SELECT 'collections', source_type, COUNT(*) FROM collections GROUP BY source_type;

   COMMIT;
   ```
3. ERP UI 진입 검증 (대시보드·재무·세무 화면 정상)

### 기존 코드 영향
- ✅ 코드 영향 0 (DELETE만)
- ⚠️ FK 연결 검증 = tax_invoice_items 의존 행 동시 삭제 필요
- ⚠️ ERP 화면이 시드 데이터에 의존하는 페이지가 있다면 깨질 수 있음 (대시보드·통계)
- ⚠️ 백업 후 작업 = 회복 가능

### 효과
- 시드 + 마이그 혼재 해소
- 마이그 결과만 남아 "끝 숫자" 검증 가능
- 사장님 4축 검증 도구 가동 전제 충족
- 약 680,815행 정리

### 담당·일정
- DB 매니저 (백업) + PM (실행) + 사장님 결재
- 5/18 13:00~14:00 (점심 후, WS-A 완료 후)

### 산출물
- backup SQL 파일
- DELETE 로그 박제
- 검증 쿼리 결과

---

# 📄 WS-20260518-C — 헌법 #26 v3 박제 + Timeout 정책 변경 ⚠️

## 사장님 결재 필요 항목 5/18 토론 결과 반영

### 배경
- 전임 PM 닥터스트레인지 오박제: *"데이터 양과 무관하게 최대 10분 이내"*
- 5/18 사장님 토론 결정: *"10분은 목표, Timeout은 안전망"*
- 사장님 2GB 시나리오: Timeout으로 막으면 사업 실패

### 작업 내용

#### Part 1: CLAUDE.md 헌법 #26 v3 정정 (PM)
```
26. 마이그·대량처리 목표-Timeout 분리 (사장님 v3 2026-05-18)
- 목표 시간: 10분 러프 (소형 표준 지향) — 정공법 5축+α로 달성
- Timeout: 물리적 한계 위 (24시간 또는 무제한) — 마이그 가능성 보장
- Y/n 다이얼로그: 10분 경과 시 "데이터 양이 많아 작업이 지연되고 있습니다. 계속 진행하시겠습니까?"
- 전임 PM 닥터스트레인지 v2 *"10분 절대"* 오박제 회수
```

#### Part 2: Command Timeout 코드 일괄 변경 (백엔드)
- 위치: `MdbMigrationService.cs` 7곳 (line 1747·1763·1784·1818·1836·2019·2034·2051·2083·2100·2256·2271·2288·2323·2340·2508·2523·2540·2575·2592·3315·3330·3347·3382·3399)
- 1800초 → **24시간 (86400초)** ⚠️ 사장님 결재 (또는 무제한)
- BulkCopyTimeout 동일 적용

#### Part 3: Y/n 다이얼로그 (프론트)
- 위치: `Migration.razor` 추정 (마이그 페이지)
- SignalR 또는 polling으로 10분 시점 감지
- MudDialog로 Y/n 표시
- n 선택 시 마이그 중단 요청 → 백엔드 CancellationToken
- Y 선택 시 30분·1시간 시점 추가 표시 (정책 결재 필요)

### 기존 코드 영향
- Part 1: CLAUDE.md 텍스트만 (코드 영향 0)
- Part 2: 7곳 Timeout 일괄 = 검색·치환 단순
- Part 3: ⚠️ **프론트 진행률 SignalR/Polling 신규** = 새 의존성
- ⚠️ CancellationToken 전파 = 기존 코드 검증 (이미 지원됨, line 1929 등)

### 효과
- 사장님 2GB 시나리오 마이그 가능
- 대형 도소매 고객 사업 성립
- 헌법 #29 (전임 PM 무허가 박제) 사고 #2 정정
- 사장님 격언 *"100%가 목표지만 현실 부분 인정"* 정합

### 담당·일정
- PM (헌법 박제) + 백엔드 매니저 (Timeout) + 프론트 매니저 + 디자이너 (다이얼로그)
- 5/18 14:00~ 1~2일

### 산출물
- CLAUDE.md commit
- 코드 Timeout commit
- Migration.razor commit + 다이얼로그 스크린샷

---

# 📄 WS-20260518-D — 회계 journal 마이그 신규 (5종)

## 진범 ID: #12 (PM 신규 발견)

### 배경
- 사장님 보고: *"회계, 세금계산서 느리거나 타임아웃으로 일부 막힘"*
- PM 전수조사: **journal_lines = 0행, journal_entries = 0행**
- = 회계 마이그 코드 **존재 자체가 없음**

### 증거 (PM 전수조사)
- `MigrateJournalEntriesAsync` 메서드 부재
- `MigrateJournalLinesAsync` 메서드 부재
- Grep 결과: `AutoJournalHelper.cs`·`SalesService.cs`·`PurchaseService.cs`에만 INSERT (운영 자동 분개)
- 마이그 결과: 0행

### 작업 내용
1. MDB 원천 식별 (DB 매니저)
   - DOCJA·DOCJL·DOCJB 등 회계 영역 DOC 테이블 추적
   - 컬럼 매핑 표 작성
2. `MigrateJournalEntriesAsync` 신규 (백엔드)
3. `MigrateJournalLinesAsync` 신규 (백엔드)
4. BulkCopy 패턴 적용
5. `RunTableStepAsync` 2개 신규 (pandataJobs 또는 별도 단계)
6. `ResultDto` 필드 2개 신규 (`JournalEntries`·`JournalLines`)

### 기존 코드 영향
- ✅ **추가만** (신규 메서드 2개)
- ⚠️ `AutoJournalHelper.cs` 운영 자동 분개와 충돌 검증
  - 마이그 분개 = source_type='migration'
  - 운영 분개 = source_type 다른 값
  - 멱등 키로 중복 방지
- ⚠️ stock_ledger·tax_invoices 분개와 연결 정합 (5/14 마이그 예외 적용 여부)
- ⚠️ ResultDto 2 필드 = 호출처 컴파일 영향 0 (struct 추가)

### 효과
- journal 0행 → 수만~수십만 건 마이그
- 사장님 격언 *"끝 숫자"* 4축 (회계 금액) 정합 가능
- 회계관리자 화면 = 레거시와 끝 숫자 일치
- 헌법 #20 워크플로우 끊김 0 (매입·매출 → 분개)

### 담당·일정
- 백엔드 매니저 + DB 매니저
- 5/19 09:00 ~ 5/20 18:00 (2일)

### 산출물
- commit 1건
- journal_entries·journal_lines 실측 행수 박제
- AutoJournalHelper 정합 검증 로그

---

# 📄 WS-20260518-E — 발주·수주 소량 진단·봉합

## 진범 ID: #13 (PM 신규 발견)

### 배경
- PM 전수조사: **sales_orders 178행, purchase_orders 642행**
- 추정: DOCF 수만건이 정상 → **99% 누락**
- 사장님 보고: 미언급이나 잠재적 영역

### 증거 (PM 전수조사)
- `MigrateSalesOrdersFromIOAsync` 메서드 존재 (line 330)
- `MigratePurchaseOrdersFromIUAsync` 메서드 존재 (line 323)
- `MigrateTransactionsAsync` 메서드 존재 (line 286, 본문 헤더·라인)
- 결과 행수 = 비정상 소량

### 작업 내용
1. **진단 단계 (1일)** — 백엔드 + DB
   - 로그 강화: `_logger.LogInformation` 추가 (각 메서드 입력 행수 / skip 행수 / INSERT 행수)
   - MDB 원천 행수 측정 (`COUNT(*) FROM IOMASTER`, `IUMASTER` 등)
   - partnerMap·itemMap·employeeMap 매핑 실패 skip 카운트
   - DB INSERT 실패 로그 (catch 블록)
   - row-by-row vs BulkCopy 분기 진입 여부
2. **봉합 단계 (1~2일)** — 진단 결과별
   - 옵션 1: 매핑 실패 누적 시 → 매핑 정정 (partner·item·employee 등록 누락 추적)
   - 옵션 2: row-by-row 시 → BulkCopy 패턴 적용
   - 옵션 3: MDB SELECT 쿼리 누락 시 → 쿼리 정정
   - 옵션 4: PK 충돌 ON DUPLICATE 시 → 검증

### 기존 코드 영향
- 진단 단계: 로그만 추가 = 영향 0
- 봉합 단계: 진단 결과별 분기
- ⚠️ ERP 매니저 영역 = 6단계 워크플로우 (매입·판매 흐름) 정합

### 효과
- 발주·수주 → 거래명세서·세금계산서 워크플로우 끊김 0 (헌법 #20)
- 사장님 격언 *"끝 숫자"* 4축 (영업·구매 미수금·미지급) 정합
- 매출 발생 0% 해소

### 담당·일정
- 백엔드 매니저 + DB 매니저 + ERP 매니저
- 5/19 09:00 ~ 5/21 12:00 (2~3일)

### 산출물
- 진단 로그 박제
- commit 1~2건
- 봉합 후 실측 행수

---

## 🎯 사장님 결재 요청 항목 (정리)

| # | 항목 | 결재 사항 |
|---|---|---|
| 1 | WS-B 시드 삭제 | 백업 후 1,295,418행 DELETE 실행 OK? |
| 2 | WS-C Timeout 값 | 24시간 vs 무제한 |
| 3 | WS-C Y/n 다이얼로그 | 10분 1회 / 30분·1시간 추가 / 다른 정책 |
| 4 | WS-C 헌법 #26 v3 박제 | 문구 OK? (위 Part 1 초안) |
| 5 | 5/14 마이그 예외 결재 변경 (P2 거래명세서) | 보류 vs 5/18 재결재 |

---

## 📅 5/18~5/22 통합 일정

| 일자 | 작업 | 담당 |
|---|---|---|
| **5/18 (월) AM** | WS-A (BulkCopy) + WS-B (시드 삭제) | 백엔드·DB |
| **5/18 (월) PM** | WS-C (헌법 + Timeout + 다이얼로그) | PM·백엔드·프론트 |
| **5/19~5/20 (화·수)** | WS-D (journal) + WS-E (발주·수주) | 백엔드·DB·ERP |
| **5/21 (목)** | 실측 + 끝 숫자 4축 검증 | 사장님 PC 재마이그 |
| **5/22 (금)** | 본런 결재 (P2 미달 시 연기) | 사장님 + 본부장 |

---

**작성: PM 브라운킴 2026-05-18**
**문서 ID:** worksheets-20260518-fivepack
**다음 액션:** 사장님 결재 → P0 3건 즉시 가동 또는 우선순위 조정
