# 통신 무결성 17시나리오 — 측정표 (PASS 기준 + 측정 방법)

> 작성: PM 브라운킴 / 2026-06-30 / 헌법 #27(5분 내 자동복구=PASS), #32(받아쓰기 금지·부풀림 금지)
> **정직 고지**: 17건 중 **자동 계측 스크립트는 현재 4건(S-A~S-D)뿐.** 나머지 13건은 수동 절차거나 미구현.
> 이 표는 슬롯이 선 뒤 사장님이 **직접 측정하고 숫자로 PASS를 박는** 도구입니다.

---

## 측정의 절대 기준 (헌법 #27)
- **PASS** = 대상을 죽인 뒤 **5분(300초) 이내** 자동 복구
- **FAIL** = 5분 초과 미복구
- **SKIP** = 슬롯 미구성으로 측정 불가(아직 안 됨 ≠ 실패)
- 측정 = **운영(demo) 절대 금지**, 오직 C:\HitPanTest 슬롯에서만 (헌법 #39, 화이트리스트 마커 fail-closed)

---

## A. 자동 계측 4건 (Run-CommsScenarios.ps1 -Confirm)

| ID | 시나리오 | 죽이는 대상 | 워치독 복구 | PASS 기준 | DB슬롯만으로? |
|---|---|---|---|---|---|
| **S-A** | cloudflared 종료 | `cloudflared-test` 서비스 | sc.Start() 재기동 | 5분 내 서비스 Running | ❌ 터널·서비스 등록 후 |
| **S-B** | MariaDB 중지 | `MariaDB-test` 서비스 | sc.Start() 재기동 | 5분 내 서비스 Running | ✅ **DB슬롯+서비스 등록 후** |
| **S-C** | API kill | `HitPan.API`(C:\HitPanTest 경로) | schtasks /Run | 5분 내 /health 200 | ❌ API 슬롯 기동 후 |
| **S-D** | 터널시크릿 무효화 | test 터널 자격증명 | WS28C 재생성 | 외부도메인 200 복귀 | ❌ 터널 발급 후 (반자동) |

### 자동 4건 실행법 (슬롯·서비스 선 뒤)
```powershell
cd "c:\Users\소순근\Desktop\hitpan-erp\tests\scenarios\comms-integrity"
powershell -ExecutionPolicy Bypass -File Run-CommsScenarios.ps1 -Confirm
```
- 결과 리포트: `tests/scenarios/comms-integrity/reports/comms-scenarios-{날짜}.md`
- 안전망: test 마커(ENV=TEST) 없으면 **실행 거부**. demo cloudflared Running이면 **차단**.

### 현재 측정 가능 여부 (오늘 DB슬롯까지 섰을 때)
- **S-B만 부분 측정 가능** — 단, `MariaDB-test` 서비스 등록은 [6]단계(터널과 함께 SKIP)라
  서비스 등록까지 마쳐야 진짜 측정. **오늘 DB슬롯만으로는 4건 모두 SKIP 나올 수 있음** (정상).
- 자동 4건 본격 실측 = 서비스·터널 등록(다음 결재) 후.

---

## B. 수동/미구현 13건 (8대 영역 — 자동 스크립트 없음)

> 헌법 #27의 "8대 영역 17시나리오" 중 위 자동 4건을 뺀 나머지. **현재 자동 계측 미구현 = 수동 절차 또는 설계만.**
> 부풀리지 않습니다 — 이건 아직 "할 일"이지 "된 것"이 아닙니다(헌법 #32).

| 영역 | 예상 시나리오(설계) | 현재 상태 |
|---|---|---|
| 인프라 | 디스크 풀 / UPS 정전 복구 | 🔴 미구현 |
| 네트워크 | 회선 단절 → 재연결(WS-04) / Windows Update 후 터널복구(WS-28) | 🟡 워치독 설계 있음, 실측 0 |
| 보안SW | 백신 5종 격리 → 1클릭 봉합(WS-08) | 🔴 미구현(메모리상 워치독 미감지 P0) |
| 자격증명 | 토큰 회전 / 인증서 만료 | 🟡 IssueTunnel 설계 |
| 물리 | 메모리 ECC / 디스크 SMART | 🔴 미구현 |
| 인적 | 오조작 / 실수 삭제 복구 | 🔴 미구현 |
| 응용 | 마이그 중단 재개 / 동시성 | 🟡 부분 |
| 외부공격 | DDoS / 비정상 접근 차단 | 🔴 미구현 |

→ 13건은 **별도 작업지시서 + 결재**로 한 건씩 자동화. 오늘 범위 아님.

---

## 측정 결과 기록란 (사장님 또는 검증팀이 채움)

| ID | 측정일 | 결과 | 복구 시간 | 비고 |
|---|---|---|---|---|
| S-A | | | | |
| S-B | | | | |
| S-C | | | | |
| S-D | | | | |

---

## 종합 (정직한 현 위치)
- **통신 무결성 베타 게이트(헌법 #27)**: 17시나리오 5분내 자동복구 실측 = **현재 실측 0건**
- 자동 스크립트 준비: 4/17 (S-A~S-D)
- 오늘 가능: DB슬롯 구축 → (서비스 등록 후) S-B부터 측정 개시
- 이 게이트 미통과 시 베타 출시 금지 — 부풀림 없이 4건부터 PASS 쌓아 17건까지.
