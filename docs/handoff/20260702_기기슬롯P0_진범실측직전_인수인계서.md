# 인수인계서 — 2026-07-02 접속 기기 슬롯 P0 (진범확정+핵심봉합 완료)

> PM 브라운킴 / 세션 만료 대비 기록. 다음 세션 0초 재개용.
> ⭐ **업데이트(2026-07-02 봉합완료)**: 진범 실측확정 → 사장님 6항목 결재 → 1·2차 봉합 완료·커밋 → **봉합 후 실측으로 origin 격리 극복 실증**. 아래 §8이 최신 상태. §1~7은 진범 규명 기록(참고).

## 8. ⭐ 최신 — 봉합 완료 상태 (2026-07-02)
### 진범 (실측 확정)
지문 `device-fingerprint.js`가 localStorage 난수(crypto.randomUUID)라 origin(scheme+host+port)별 격리 → 같은 PC도 접속주소(localhost↔127.0.0.1↔{id}.hitpan.kr) 다르면 새 지문=새 기기. **호스트 실측 확정**: localhost=`01a7075d…` ≠ 127.0.0.1=`40ba83fe…`.

### 사장님 최종결재 (6항목 전부 승인 "모두 결재!! 진행해!!")
①진범=origin격리 인정 ②우선순위=도메인일원화 1순위·A단독반려 ③도메인일원화 {id}.hitpan.kr 단일고정 ④메인PC=loopback 면제 ⑤F3 안내 포함 ⑥마이그 재인식방지 포함. (작지서 20260702작1 §6)

### 봉합 완료·커밋 (develop)
- **1차 `a74d487`**(백엔드): 메인PC loopback 면제(AuthController) + 빈지문 NOT NULL 가드(주석 "제거금지") + F3 기반(RegisterOrRefreshAsync에 newlyRegistered 4튜플, 인터페이스·구현·호출 전수정합 헌법#12, LoginResponse DeviceNewlyRegistered/DeviceNotice, 한도초과="등록된 기기가 아닙니다"). 빌드 0/0 실측.
- **2차 `5f41dfb`**(진범 근본): 지문 origin비의존 보강(device-fingerprint.js — 난수→기기 환경특성 FNV-1a 해시, localStorage 캐시 병행, 캐시없어도 환경해시 복원, 빈문자열 금지) + 도메인 일원화(web-server.ps1 — db.conf PRIMARY_DOMAIN 읽어 엉뚱host면 정식도메인 301, loopback·LOCAL·이미정식·빈host 스킵 겹겹가드). node실측+PS문법 PASS.
- **검증 `e3834a2`**(실측 기록).

### ✅ 봉합 실증 (봉합 후 브라우저 실측, 동일 도구 tests/device-fp-probe)
- 봉합 후 양쪽 캐시 clear 새 값: localhost=`HFPv2-502cf0e2` **=** 127.0.0.1=`HFPv2-502cf0e2`. 같은 PC·다른 origin인데 지문 동일 → **진범 극복 실증**(추측 아닌 실측). 핵심 봉합 완료.

### ⬜ 남은 것 (다음, 상황 봐서 — 오늘 미완)
1. **도메인 일원화 실환경 검증** — web-server 리다이렉트 문법만 확인, 터널 실환경 미검증(사장님 "나중" 승인).
2. **메인PC loopback 면제 실동작** — 코드정합만, ERP 전체기동 로그인 흐름 검증 미완.
3. **F3 "등록하시겠습니까?" UI 다이얼로그** — 백엔드 기반만, 프론트 2단계 상호작용 미착수(2차 프론트 봉합).
4. **마이그레이션 안전(검증팀 우려)** — 기존기기 새지문 재인식 유예·재매핑. 설계만, 미구현. 현 운영 7건(데모)이라 경미하나 정식 전 필수.
5. **SOP 검증팀 독립검증(SoD)** — 만든사람(PM)≠검증. 제3자 반증 미실시.

### 다음 세션 첫 판단
핵심 진범은 봉합·실증됨. 남은 5항목은 (a)검증환경(터널) 필요하거나 (b)별도 규모라 오늘 무리 안 함. 재개 시 사장님께 5항목 중 우선순위 확인 후 진행. 서두르면 두더지잡기 재발.

---
(이하 §1~7 = 진범 규명 당시 기록, 참고용)

# (원본) 진범 실측 직전 기록
> 한 줄: **접속 기기 슬롯 오작동(사장님 실측 "같은 PC·같은 브라우저인데 매번 다른 기기로 잡힘") = 별개 P0로 분리. 진범을 코드로 좁혔고(지문=localStorage 난수, origin 격리 유력), CTO 조건부GO·검증팀 반증까지 끝.**

---

## 0. 오늘(7/2) 전체 맥락 (두 갈래로 갈라짐)

### 갈래 1 — 3시스템 연결축 정합 (오전, 일단락)
- 사장님이 정식 3시스템 흐름 그림(랜딩 OCR·도메인 → 백오피스 시리얼 → ERP설치 자동등록·수정금지). 시리얼로 통일.
- **코드 정합검증 완료**: 랜딩→백오피스→ERP 계약 필드 대조 결과 **연결축 코드상 이어져 있음**(G5 웹훅 수신측 WebhookInboundController 실존 확인, 시리얼 형식 2종 HITP/HP는 별개 용도라 정합). 상세=docs/design/20260702_3시스템_연결축_정합설계서_전수조사기반.md §3 "코드 정합검증 완료 결과".
- **메인PC 정의 확정(사장님)**: "데이터가 들어가 있고 히트판 설치마법사(EXE)가 설치된 PC". 1도메인 1대. 이유 3가지=①무단사용금지 ②CS 단일지점 ③DB 무결성. 메인PC는 설치로 이미 등록→기기인증 면제. 설계서 §3-A에 기록.
- **G3(1PC 고정)·G4(재등록 본사승인경유) 결정**: 설계서 §3-A. 착수는 아직(연결 우선).
- **클라이언트PC 대수제한 = 넷플릭스식 영업 축**(사장님): 메인PC 1대 고정(보안축)과 별개. 이 논의 중 사장님이 "현재 기기슬롯 문제 많다"고 실측버그 지적 → 갈래 2로 전환.

### 갈래 2 — 접속 기기 슬롯 P0 (오후, 현재 진행 = 이게 지금 메인)
아래 §1~§5 전부 이 건.

---

## 1. P0 정의 — 사장님 실측 버그
- 증상: **같은 PC·같은 브라우저로 접속해도 매번 다른 기기(처음 보는 기기)로 잡힌다.** 슬롯이 계속 소모돼 금방 한도초과 → 로그인 차단 위험(헌법 #20).
- 사장님이 그린 정상 흐름: 접속→(메인PC면 면제 통과)→등록기기면 통과 / 처음기기면 슬롯여유시 "등록하시겠습니까?" · 슬롯초과시 "등록된 기기가 아닙니다".

## 2. 진범 (코드로 좁힘, 실측으로 최종확정 남음)
- **지문 생성** `src/HitPan.Web/wwwroot/js/device-fingerprint.js:7-22` = `localStorage['hitpan_fp_id']`에 **`crypto.randomUUID()` 난수**를 심어 재사용. **기기 특성(UA·OS·해상도) 0개 사용.** 주석은 "환경값 기반 해시"라 써놓고 실제 난수(설계-구현 불일치). DDL `tenant_devices.fingerprint` COMMENT는 "UA+OS+해상도+시간대 SHA-256"(원설계).
- **정확한 진범 재기술(검증팀)**: "난수라 매번 다름"은 부정확(난수는 재사용됨). 정확히는 **난수 + 저장소 리셋 무방비 + origin(scheme+host+port) 격리**. localhost/127.0.0.1/터널도메인/demo가 각각 다른 origin=다른 localStorage → 접속 주소 바뀌면 새 난수=새 기기.
- **반증 완료(재조사 금지)**:
  - ❌ "지문 JS 미로드"(PM 1차 가설) = 거짓. index.html:47 정상 로드, 파일 실존.
  - ❌ "index.html:55-61이 지문 지운다"(CTO 의심) = 부분거짓. 그 코드는 ServiceWorker+Cache Storage만 삭제, **localStorage(지문)는 안 건드림**(PM·검증팀 코드 확인 일치).
  - ❌ "암호화저장소 vs 평문fp 생명주기 차이"(CTO 의심) = 기각. hitpan_fp_id는 device-fingerprint.js에만, HitPanProtectedLocalStorage·storage.js와 접점 0.
  - ✅ 슬롯 로직 본체(`TenantDeviceService` 등록·매칭·한도차단·revoked차단)는 **정상 작동**. UNIQUE(tenant_id,fingerprint) 정확 매칭. **봉합 대상은 지문 안정성이지 슬롯 로직 아님.**
- **부수 결함**: F2 빈지문(`getFingerprint` catch→''→`TenantDeviceService.cs:102-105` 등록스킵) / F3 "처음 기기 등록하시겠습니까?" 안내 코드에 아예 없음(조용히 자동 approved INSERT).

## 3. 결재 진행 상황 (SOP)
- **작업지시서 20260702작1** 발행 완료: docs/design/20260702작1_접속기기슬롯_P0_지문근본결함_봉합작업지시서.md. 봉합 방향 후보 A(환경해시)/B(저장소이중화)/C(혼합), 결재 5항목.
- **CTO 1차 결재 = 조건부GO**: 진범 인정, 봉합은 6조건 후. 최대 우려=마이그레이션 폭발(지문방식 바꾸면 기존 tenant_devices 전기기 재인식→로그인 일괄차단). A단독 반려권고(동일기종 충돌). 도메인 일원화가 근본이라 지적. 샌드박스 단일origin 거짓PASS 경고.
- **검증팀 독립반증(데이비드 박)**: CTO 대비 판정 — 마이그폭발=**과장**(운영 tenant_devices 7건뿐, 전부 demo-tenant-0001, 실고객 0=베타전이라 폭발모수 없음. 단 이 확인 위해 **운영 demo 3306 직접 SELECT함 → 헌법 #39 경계선. 앞으론 샌드박스/테스트DB에서만**). A충돌=타당하나 치명아님(최악=덜소모, 차단보다 안전). 샌드박스 거짓PASS=옳음(단 localhost↔127.0.0.1로 origin 2개 재현 가능). **CTO·PM 둘다 놓친것 2개**: ①빈지문+NOT NULL 충돌(봉합시 빈값방어 제거하면 ''INSERT→500) ②revoked×지문교체 상호작용(폐기기기 부활/정상기기 차단 가능).
- **검증팀 결정적 실측**: 운영 tenant_devices에 **같은 데모PC가 몇시간 간격 5개 기기로 증식**(전부 UUID지문)해 있음 = 사장님 실측증상이 DB에 그대로 남음.

## 4. 🎯 다음 행동 (지금 여기 — 사장님 "기동 도와줘")
**샌드박스 origin 재현 실측 1회로 진범 최종확정.** 대본=docs/design/20260702_기기지문_origin격리_실측대본_샌드박스.md.
- 사장님 결정: **샌드박스 새로 띄워 클린 재현** + 관찰=**F12 Network(login payload의 deviceFingerprint) + Application(localStorage hitpan_fp_id)**.
- STEP1 `localhost:5234` 로그인→지문 A / STEP2 재로그인(A유지?) / **STEP3 `127.0.0.1:5234`(주소만 바꿈)→지문 B**.
- **판정**: B≠A → origin 격리 진범 확정(봉합핵심=도메인 일원화) / B=A → 다른원인 재조사 / 지문 ''빈값 → 알고리즘 아닌 전송경로 문제(봉합 가벼움, 마이그폭발 소멸).
- 로컬 기동: Web(5234) WASM은 ApiBaseUrl 빈값이면 HostEnvironment.BaseAddress 폴백(Program.cs:35). 로그인=admin@hitpan.kr/Admin1234!.
- **다음 세션 할 일**: 샌드박스 기동 도움(M4 셋업) → 사장님이 STEP1~3 F12값 A/B 알려주면 PM 즉석 판정 → 대본 결과란·작지서 §진범 보완 → 사장님 최종결재(사장님이 직접 하겠다 명시).

## 5. 절대 보존/금지
- 이 PC = demo 운영(3306, 129만행). **운영 직접수술·검증 금지(헌법 #39)**. 실측은 샌드박스(휘발성). 검증팀이 운영 SELECT한 건 경계선 — 재발 금지.
- 헌법 #33: 봉합 착수는 사장님 최종결재 후. 지금까지 **코드 변경 0건**(전부 정독·문서).
- 커밋 대상(기록물): docs/design/20260702작1_*.md, docs/design/20260702_기기지문_*.md, docs/design/20260702_3시스템_*.md(정합검증 갱신분), 본 인수인계서.
- M4 샌드박스 = [[project_sandbox_m4_20260630]]. slot빌더 운영격리 사고 교훈 [[project_slotbuilder_isolation_20260630]].

## 6. 갈래 1(3시스템) 미결 — 기기슬롯 P0 끝난 뒤 복귀
- G1 OCR / G2 시리얼발송 신뢰성 / G3 1PC고정(위 P0와 지문 인프라 공유) / G4 재등록 본사승인 / G5 웹훅 소비측 확인(R2) / G6 백오피스 DB분리. 우선순위=연결먼저 종단검증. 상세=설계서+docs/handoff/20260702_3시스템정합_전수조사완료_코드정합검증_다음지점.md.
