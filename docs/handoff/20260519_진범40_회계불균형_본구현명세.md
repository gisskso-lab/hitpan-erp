# 🔍 진범 #40 (P0+) 회계 차/대 불균형 본구현 명세

> **작성**: 2026-05-19 04:30 PM 브라운킴 (CTO 1인)
> **사장님 결재 필요**: 5/20 10:00 (그룹화 키 자문 포함)

---

## 🚨 진범 #40 진짜 본질

### 9차 박제
```
DOCF7 그룹화 — 엔트리=23170 라인=27661 미사용 KCODE=78
| 차/대변 불균형=23166 (99.98%)

journal_lines 차변합계: 10,559,060원
journal_lines 대변합계: 9,897,817,071원
차-대 차이: -9,887,258,011원 (-98억)
```

### 현재 코드 (MdbMigrationService.cs line 4527)
```csharp
var groups = dt.AsEnumerable().GroupBy(r => new
{
    Dt = GetStr(r, "SC_DT"),
    Sun = GetInt(r, "SC_SUN"),
}).ToList();
```

### 본질 진단
SC_DT + SC_SUN 그룹화가 **23,170 entry로 분할** → 각 entry 안에 1~2 라인만 → 차변 또는 대변 한쪽만 존재 → 불균형 99.98%

**정상 회계 분개**: 한 거래 = entry 1개 안에 **차변 N개 + 대변 N개 동시 존재** (차변 합 = 대변 합)

### 가설 매트릭스

| 가설 | 설명 | 검증 방법 |
|---|---|---|
| **A** | SC_SUN이 라인 순번 (entry 내부) | SC_SUN 분포 보면 1,2,3,... 연속 |
| **B** | SC_JEN 컬럼이 진짜 entry 키 | DOCF7 컬럼 목록 확인 |
| **C** | SC_DT만으로 그룹화하면 정상 | 일별 1개 entry 가정 |
| **D** | SC_SAWON + SC_DT (담당사원별 일별) | 사원별 분개 |
| **E** | DOCF7 자체가 라인 테이블 + 별도 헤더 테이블 존재 | DOCF6 헤더? |

---

## 📋 사장님 자문 #7 (NEW — 자문 6건에 추가)

### 사장님께 물어볼 5문
1. **레거시 회계 entry 구분 컬럼**: SC_SUN? SC_JEN? SC_SAWON? 다른?
2. **DOCF7 행 1개 = 라인 1개? 헤더+라인?**:
   - 옛 ERP에서 SC_CR 또는 SC_DR 한쪽만 채워지면 → 한 거래 = 여러 row
3. **별도 헤더 테이블**: DOCF6 또는 DOCF8 등?
4. **차/대 균형 검증 시점**: 레거시 ERP는 입력 시 강제? 또는 사후?
5. **마이그 차/대 불균형 정책**: 무시? 강제 균형? legacy_imbalance 컬럼?

### 사장님 결재 후 본구현 옵션

| 옵션 | 변경 | 효과 |
|---|---|---|
| **A** | 그룹화 키를 사장님 답변에 따라 변경 (예: SC_JEN) | 차/대 균형 회복 (정확도 ↑↑) |
| **B** | 차/대 불균형 시 자동 균형 INSERT (차액을 imbalance_account에 INSERT) | 강제 균형, 운영 화면 가독성 ↓ |
| **C** | 마이그 예외 영역 유지 + 운영 시점부터만 정합성 (5/14 사장님 결재 유지) | 회계 화면에서 마이그 데이터 표시 시 경고 |

### PM 1차 추천: **옵션 A (그룹화 키 재설계)**
- 정확도 회복이 최우선
- 사장님 답변 = SC_JEN 또는 SC_SAWON+SC_DT 가능성 高
- 변경량: line 4527 그룹화 키 1줄

---

## 🎯 본구현 명세 (사장님 옵션 A 결재 후)

### 변경 위치
`MdbMigrationService.cs` line 4527

### 변경 전
```csharp
var groups = dt.AsEnumerable().GroupBy(r => new
{
    Dt = GetStr(r, "SC_DT"),
    Sun = GetInt(r, "SC_SUN"),
}).ToList();
```

### 변경 후 (예: 사장님이 SC_JEN 결재 시)
```csharp
var groups = dt.AsEnumerable().GroupBy(r => new
{
    Dt = GetStr(r, "SC_DT"),
    Jen = GetStr(r, "SC_JEN"),  // 분개번호 (사장님 자문 #7 결재)
}).ToList();
```

### 변경량
**1줄**

### 봉합 후 예상 효과 (10차 마이그)
- entry 23,170 → 약 5,000~8,000 (실제 거래 수에 맞게 감소)
- 각 entry 안에 차/대 양쪽 라인 존재 → 균형 회복
- **차/대 균형 entry 비율 99.98% → 0~5%** (전체 정합 가능)

### 추가 검증 (5/20 PM 즉시 가능)
```sql
-- DOCF7 컬럼 목록 + SC_SUN/SC_JEN 분포 확인
-- OleDb 진단 콘솔로 즉시 실행
SELECT TOP 100 SC_DT, SC_SUN, SC_JEN, SC_KCODE, SC_DR, SC_CR
FROM DOCF7
ORDER BY SC_DT, SC_SUN;
```

---

## 🚨 진범 #40 vs 5/14 사장님 마이그 예외 결재

### 5/14 결재
*"마이그 예외: 거래명세서·세금계산서 정합성 무시하고 레거시 그대로 이관 (운영은 무결성 절대)"*

### PM 해석
- 거래명세서·세금계산서는 예외 명시
- **회계(journal_lines)는 예외 영역 미명시** → 헌법 §회계 절대 정합성 영역

### 5/19 사장님 자문 #7 필요
- 회계도 마이그 예외에 포함? (옵션 C)
- 또는 회계는 정확도 100% 절대 필요? (옵션 A·B)

### PM 1차 추천 (헌법 #25 정합)
- **회계도 정확도 100% 절대 필요** — 옵션 A 우선
- 옵션 A 불가 시 (사장님 답변 미확정) → 옵션 B 자동 균형

---

**작성**: PM 브라운킴 (CTO 1인) 2026-05-19 04:30
**상태**: 사장님 자문 #7 추가 → 자문 7건. 5/19 09:30 회의 시 자문.
