# 인수인계서 — UPDATE 자동화 근본틀·게시인증 합의·하이브리드 구조 정의 (2026-07-24 세션압축)

> 작성: PM 브라운킴 / 2026-07-24
> **다음 세션 세션시작 즉시 이 문서부터.** (오늘 다수 결정·설계·합의가 여기 응축됨)
> 미커밋 1건(B-2) 존재 — §5 상태표 참조.

---

## 0. ⭐⭐ 히트판 구조 = 하이브리드 (사장님 못박음 2026-07-24, 다음세션 필독)

> **PM이 오늘 "웹이냐 윈도우냐" 둘 중 하나로 가르려다 헷갈림. 사장님이 정정: "웹버전과 윈도우 애플리케이션의 장점을 섞은 하이브리드 구조."**

### 하이브리드 정의 (절대 — 다음세션 오판 방지)
| 요소 | 어느 쪽 장점 | 효과 |
|---|---|---|
| 브라우저로 띄움 | **웹** | 클라이언트 PC 설치·업데이트 **0**, 어디서나 접속 |
| 로컬 설치(EXE·데이터·터널) | **윈도우앱** | 데이터 주권(고객 PC 보관, 본사 부담 0 = 히트판 정신·#22) |
| cloudflared 터널 | 하이브리드 접착제 | 로컬인데 외부 접속 가능 |
| 워치독 자동업데이트 | **윈도우앱** | 회사당 "서버역할 1대"의 로컬 프로그램 자동 최신화 |

### 사장님 4문장 (원문)
1. 히트판 기본정신 = 본사 데이터관리부담 0, 고객 데이터관리 책임 → 데이터가 고객 로컬에.
2. 웹브라우저에 띄운다는 의미에서 웹버전.
3. 로컬에 데이터 있지만 외부연결 가능.
4. 웹브라우저에서 띄우기에 **클라이언트 PC 프로그램 업데이트 불필요.**

### 이게 뜻하는 것 (업데이트 범위 오판 차단)
- **업데이트 대상 = 회사당 "서버역할 1대"뿐**(히트판 웹서버·API·데이터·터널 깔린 그 PC). 그 1대 최신화 = 회사 전체 완료.
- 클라이언트 PC들 = 브라우저로 붙어 **새로고침만으로 자동 최신**. 걔들은 아무것도 안 함.
- **"PC마다 업데이트·인증" 같은 건 존재하지 않음.** PM이 이걸 헷갈렸음(사장님 "왜 모든 PC가 인증받냐 ㅋㅋ").
- 순수 SaaS 아님(로컬 프로그램 파일교체 필요 → 워치독·서명 필요) + 순수 윈도우앱 아님(회사당 1대·브라우저 클라이언트).

---

## 1. 오늘의 큰 흐름 (사장님 우선순위 재편)

```
사장님: "보안(비특권화)을 최우선 두지 마라. UPDATE 자동화 워크스페이스가 가장 기본·먼저다."
근거(사장님): "한 건 수정→NCP 수동업로드→재설치→테스트 무한반복 = 비효율.
             UPDATE 서면 → ERP 웹에서 딸깍 한 번으로 수정 테스트."
  → UPDATE = 나머지 모든 수정의 작업속도를 결정하는 인프라. 그래서 최우선.
  → 어벤져스 9인 만장일치 지지(설계·검증·안상무·CTO·네트워크·DB·보안매니저·풀스택·PM).
```

### 베타 전 미완 6개+α (사장님 제시) — UPDATE로 대부분 해결
1.마이그 100% / 2.풀스택 종단 테스트 / 3.데이터 무결성 / 4.백오피스 CS 재설계 / 5.ERP CS(챗봇·매뉴얼) / 6.보안 / α.신규설치 clean DDL(CF-1·2)
- **코드 수정형 = UPDATE로 딸깍 / 스키마 변경형 = 고리5 선행 필요 / 신규설치 무결성 = UPDATE로 못 고침(별도)** (DB매니저·CTO 확정)

---

## 2. UPDATE 자동화 근본 틀 — 설계 완료 (11인 워크플로우)

문서: `docs/design/20260724_UPDATE자동화_근본틀_설계종합_11인워크플로우.md`
- REFUTED 0 = 방향 견고. **17구멍(B-1~B-17) 봉합 후 구현**(재설계 아님).
- **관통결함 급소 B-4**: 게이트가 "러너가 보낸 값"을 신뢰 → 침해러너가 위조. 처방 = **NCP 래퍼가 GitHub API/git 독립 재조회**. 이거 하나가 서명오라클·전송변조 동시 방어.
- 신뢰경계 3원칙: A(GitHub→NCP)=데이터만 / B(NCP)=서명·교체·롤백 / C(NCP→고객)=서명본.
- 뼈아픈 실측: **NCP 디스크 96.8%**(B-16, 곧 full) + **gisskso-lab 단일User라 2인서명 구조 미성립**(B-10, Org전환 선결).

---

## 3. ⭐ 게시(publish) 인증 = 4인 협의 합의 (2026-07-24 사장님 채택)

> 사장님 아이디어("금융앱 2차인증처럼 OTP·토스·카카오") → CTO·ERP매니저·안상무·PM 4인 협의 → 단일 수렴.

### 합의안 (사장님 "채택 — 합의안대로 간다")
| 구성 | 내용 |
|---|---|
| **게시 게이트** | **GitHub `environment:production` Required reviewers = 폰 Approve 1탭(≈3초)**. 별도 OTP앱·토스·카카오 **불필요**(새 공격면·SPOF·#34 역행). GitHub 폰승인이 그 역할. |
| **B-4 병행** | NCP 독립재조회 상시(신원위조 차단). 사람게이트와 직교, 둘 다 필수. |
| **부-승인자** | 사장님 + 부승인자 1인, **베타 1-of-2(OR)**. 감시자 아니라 P0 핫픽스 백업 게이트키퍼(사장님 부재 SPOF 제거). |
| **마이그 표시** | 승인화면에 버전·마이그동반여부·변경요약·해시 강제표시. 마이그건은 2-of-2 상향. |
| **개인키** | NCP-only, 러너 상주 금지(#22). |

### 균형 근거 (전원 서명)
- 고객 편의 불변(손0 딸깍). 본사 편의 = 게시당 3초(주1회라 병목 아님). 신규코드0(설정만).
- 보안 = 러너·계정 뚫려도 사장님/부승인자 폰 없이 게시 물리불가 → 서명오라클 방아쇠 봉인.
- **하이브리드 구조 정합**: 인증 주체 양쪽 다 1명(본사 게시=사장님 / 고객 수신=회사당 담당자 1명). 회사당 1대라 인증 번거로움 애초에 작음.
- **기각**: 조건부(마이그일때만) — 폭발반경 마이그로 안갈림·플래그가 공격표적·편의이득0. 커스텀OTP/토스/카카오 — 새 공격면.

### ✅ 사장님 최종 결정 4건 — 전부 결재됨 (2026-07-24, "4가지 모두 결재")
1. **[결재] NCP sudo — ✅ 승인.** .disabled 제거 전 필수(§4). 헌법 #29 사장님 승인 확보.
2. **[지명] 부-승인자 — ✅ "지금은 사장님 1인만"(1-of-1).** 부승인자 공란 유지, **정식 전 지명**. 베타는 사장님 폰 1개가 게시 게이트 = SPOF → 4번(비상경로)으로 메움(설계정합).
3. **[승인] 단계론 — ✅ P0 먼저.** P0(reviewer 사장님1인 등록→백지 실측) → P1(승인화면 표시·마이그2-of-2·로그DB) → P2정식(HSM·2인서명·Org전환). 이 순서 확정.
4. **[확인] 비상경로 — ✅ 넣음.** 사장님 폰 막힘 대비 **1회용·감사로그·사후통지** 봉인 복구코드. 부승인자 없어 SPOF라 이게 유일 백업.

> **⚠️ 2번이 1-of-1이 되면서 4번 비상경로가 필수 안전망이 됨.** 백업 승인자 부재 = 사장님 폰이 유일 게이트 = 그 폰 하나 막히면 P0 핫픽스 영구차단 → 비상경로가 정확히 그 구멍을 막음.

---

## 4. 착수 순서 (트랙 분리 — CTO 정합)

```
[레포 코드 = 지금 가능, NCP 무관]
 ✅ webroot SSOT 정정 커밋(197c534)
 △ B-2 봉합(미커밋) — publish-update.sh sudo 오버라이드 차단
 ▢ 17구멍 트랙1 구현(래퍼2종·deploy-update.yml·publish가드·trufflehog·문서) → SoD 재검증
[NCP/GitHub = 검증 통과 후 사장님 결재]
 ▢ 계정 hitpan-deployer·sudoers·Secret분리·staging·백업retention·디스크
 ▢ GitHub: environment reviewer 2인·branch protection·SHA핀·dependabot
[활성화·실측]
 ▢ deploy-update.yml .disabled 제거(17구멍 전량 PASS 후)
 ▢ 2대 시나리오 검증게이트(작2) G-1 신규진입 → G-2 재설치 → G-3 UPDATE 종단
```

### 오전 A-3 NCP 실측 완료분 (신뢰)
- ✅ 접속 = root + `C:\Users\소순근\Downloads\hitpan-key.pem` (drumer_ssk_0는 없는유저)
- ✅ G-A 상주배치·G-C webroot `/var/www/updates` 정정 완료. /opt 트리 root:root 755.
- ✅ NCP 백오피스 환경변수 **전부 SET**(Cloudflare3종·Bootstrap·Pepper·BizNo·NTS·BackofficeDb·서명개인키). back.hitpan.kr 200. updates 피드 200+no-cache.
- ✅ 자동배포계정 = **root 단일**. sudoers.d 청정.

---

## 5. 현재 파일 상태 (커밋/미커밋)

| 상태 | 내용 |
|---|---|
| ✅ 커밋 197c534 | publish-update.sh:41 WEBROOT `/var/www/updates` 정정(SSOT) |
| △ **미커밋** | publish-update.sh B-2 가드(sudo 컨텍스트 오버라이드 die) — 작3 승인 시 커밋 예정 |
| 📄 문서 | 로드맵·작2(2시나리오게이트)·설계종합·작3(17구멍구현)·본 인수인계서 |

**미커밋 B-2 처리**: 다음세션이 작3(17구멍 구현) 착수 시 함께 커밋하거나, 사장님 지시로 별도 커밋. **워킹트리에 보존 중.**

---

## 6. 2대 시나리오 검증게이트 (작2, 확정·CTO승인·사장님승인)

문서: `docs/handoff/20260724작2_2대시나리오_검증게이트_작업지시서.md`
- 시나리오1 신규진입 종단 / 시나리오2 재설치 종단. 둘 다 UPDATE·워치독으로 닫힘.
- 진단: 코드 7고리 다 이어짐. 급소=NCP 환경변수(전부 SET 확인). 신규-재설치 시리얼 충돌 없음.
- 통과기준 = **백지 Sandbox 실측만 인정**(개발PC 정상=검증 아님). 실제 접속 200으로만(응답코드 신뢰금지).

---

## 7. 정식 숙제 (베타 밖 — 소유자 안철수 상무)
1. 개인키 HSM/KMS. 2. 릴리스 2인서명(**Org 전환 선결** — 현 단일User). 3. environment 수동승인(베타 채택·정식 필수). 트리거=정식 출시 전 BLOCKING.

---

## 8. 참조 문서
- 로드맵: `20260724_베타전_전체로드맵_UPDATE최우선확정_9인종합.md`
- 게이트: `20260724작2_2대시나리오_검증게이트_작업지시서.md`
- 설계종합: `docs/design/20260724_UPDATE자동화_근본틀_설계종합_11인워크플로우.md`
- 구현작지서: `20260724작3_UPDATE자동화_17구멍봉합_구현작업지시서.md` (CTO 부분수정후승인, 4건 보완 필요: B-4 토큰조달·sign-manifest 가드·B-10·deploy-web 골격)
- 이전 진실원: `20260724_최종인수인계서_자동배포영구CD_트랙A코드봉합완료_스케줄표포함.md`

---

## 8.5 트랙2 진행현황 (2026-07-24 — 세션 이어받기용)

> 트랙1(코드) 완료·커밋 c1b917b·develop push 완료. 아래는 트랙2(NCP/GitHub) 실행 진행분.

### ✅ PM이 완료한 것 (NCP 실측·조작)
- **디스크 정리**: 90%→73%(2.6GB 여유). 백업 최근1개·zip 최신2개만 남김. 운영본·개인키 무손상.
- **1단계 래퍼 상주**: `/opt/hitpan/deploy/{deploy-web.sh,publish-run.sh}` 755, bash -n OK.
  (SSH 파이프로 전송 — sftp 서브시스템 실패 우회. fail2ban 간헐차단 → 각 명령 텀 두고 진행.)
- **2단계 계정·sudoers**: `hitpan-deployer`(uid999·nologin) + `/etc/sudoers.d/hitpan-deployer`
  (visudo -c 통과·전체정합). 래퍼 2개만 NOPASSWD:SETENV. env_keep=HITPAN_EXPECT_SHA/GH_SHA/
  GH_RUN_ID/GH_REPO/RELEASE_VERSION(ALLOW_UNVERIFIED 제외=러너 우회차단·검증팀 M-2봉합). 과잉권한 0 검증.
- **3단계 git 미러**: `/opt/hitpan/repo` clone 완료(origin/main·develop 조회됨). 토큰만 남음(사장님 A작업).

### ✅ 브랜치 전략 (어벤져스 3관점 검토 — 사장님 "전직원 검토" 지시)
- **실측 진실**: origin/main=빈껍데기(초기+워크플로4커밋)·origin/develop=실개발768커밋. 공통조상 4/9.
- **결론 만장일치**: 고객게시 진실원=**main**(보안 #19·#23·#34). B-4 MAIN_REF=origin/main 유지.
- **⚠️ PM 2회 오판→검증팀 정정**: (1)"768머지 위험" (2)풀스택"로컬main=1커밋 FF가능" → 진실=**원격
  main 4커밋·add/add 충돌 3건 진짜**(blob해시 다름·PR#1 이미 CONFLICTING). 로컬≠원격 혼동 주의.
- **타이밍 결정(사장님 승인)**: main 정렬은 **첫 실측 성공 후 PR로**(지금 강행=불필요위험, 252자파일·
  force금지·미백업). 지금은 오늘커밋 push 백업만 완료(c1b917b 원격 안전).
- **평시 운영규칙**: develop=개발/main=릴리스승격PR. 게시 직전 develop→main 정렬.

### ⬜ 남은 것
- **5단계 비상경로**: 설계중(사장님 폰 막힘 구제 vs 뒷문 금지 딜레마 — 보안설계 진행).
- **4단계 사장님 GitHub 작업**(몰아서): 토큰발급(A)·environment reviewer(B)·main protection(C).
  매뉴얼=`docs/handoff/20260724_트랙2_사장님_GitHub작업_클릭매뉴얼.md`. 사장님 손 필수(PM 계정접근 불가).
- **6단계 활성화**: 사장님 GitHub 후 `git mv deploy-update.yml.disabled → .yml` + push(PM 실행).
- **첫 실측**: 이행기 우회(HITPAN_B4_ALLOW_UNVERIFIED=1·NCP서만)로. 게시대상=검증된 커밋. 폰승인 실측.

### 참조 매뉴얼
- 트랙2 전체: `docs/handoff/20260724_트랙2_사장님_복붙매뉴얼_UPDATE자동화_활성화.md`
- GitHub작업: `docs/handoff/20260724_트랙2_사장님_GitHub작업_클릭매뉴얼.md`

---

## 9. 다음 세션 첫 행동
1. 본 문서 §0(하이브리드) + §3(게시합의) + §8.5(트랙2 현황) 읽기
2. 5단계 비상경로 설계 확인 → 사장님 GitHub 작업(A·B·C) → 토큰 NCP저장 → 6단계 활성화 → 첫 실측
3. 첫 실측 성공 후: develop→main 정렬 PR(검증팀 5전제: force금지·longpath·충돌3육안·backup원격push·PR경유)
