# 2026-06-26 2차 종합 인수인계서 — 헌법#39 + C·D 봉합 완료 / B-1(demo배포) 대기

> 작성: PM | 오늘 2차 세션. 1차 인수인계서(022e3db) 이후 진행분 전체.
> 다음 세션 첫 작업 = **B-1 demo 배포**(사장님 승인 게이트). 이 문서로 끊김 없이 이어받는다.

---

## 0. 한눈에 — 오늘 2차 세션 커밋 6건 (전부 develop, demo 무손상)

| 커밋 | 내용 | 검증 |
|---|---|---|
| `cda0942` | 헌법 #39 신설(운영 직접수술 금지) + deploy-demo 설계·초안 | — |
| `6bc2f47` | demo 미해결 2건(CORS·approval 403) 전수조사 — 옛빌드 단일근원 | — |
| `f2cc15d` | deploy-demo.ps1 초안 완성(스모크 6종 + BOM/한글 BOM 봉합) | 파싱0·dry-run✅ |
| `2ef9027` | C-2 BOM 멱등 + D 대시보드 D-1~5 근본해결 | 빌드0/0 + ddl-smoke PASS |

**오늘 코드 변경은 전부 테스트/정적 검증만. demo 운영 환경 0회 접근(헌법 #39 준수).**

---

## 1. 헌법 #39 신설 (CLAUDE.md 절대원칙 #39)

운영(demo) 직접 수술 절대 금지 — 운영=읽기만 / 변경=검증된 deploy-demo.ps1 / 검증=테스트환경.
어제 새벽 demo 손수술 두더지잡기 사고 재발방지. 헌법 #29(인프라·터널)와 별개(=운영환경 자체 수술).
메모리: feedback_constitution_39_no_live_surgery.md.

---

## 2. C 단계 — ERP 무결성 (완료)

### C-1 quotations DDL 갭 → 봉합 불필요로 규명
- 코드 ↔ clean DDL 완전 정합(헤더16+items12+enum폴백). discount_rate도 clean DDL에 있음.
- **진짜 갭 = clean DDL vs demo 운영DB**(메모리 project_clean_ddl_vs_live_db_gap). → B-1 배포가 정합 반영.

### C-2 BOM 멱등 → 근본해결 완료
- 전수조사+SoD반증: 판매·매입·반품 9경로는 status 가드(SalesService:273 등)로 **이미 멱등**(보존).
- BOM 생산(AssembleAsync)·해체만 무방비(서버 GUID source_id라 중복호출 시 재고 2배).
- **근본해결(새 테이블 0건)**: 이미 있던 멱등 인프라(IdempotencyMiddleware + idempotency_keys 테이블 clean DDL:1414) 적용.
  - 서버: `BomController.cs` assemble·disassemble 에 `[IdempotencyKey]`.
  - 클라이언트: `HitPan.Web/Services/BomService.cs` 두 메서드에 `Idempotency-Key` 헤더(없으면 400). 새 클릭=새 키(반복생산 보존), 재시도=같은 키(중복 차단).
  - productionRunId·DDL·App서비스 무변경.

---

## 3. D 단계 — 대시보드 D-1~5 (완료, 근본해결)

### 전수조사 반증 (받아쓰기 차단)
- 어제 리스트 목적지(/deliveries·/purchases)는 **편집화면**이라 오답. 진짜 목록 = `/sales/summary`(SalesSummaryPage)·`/purchase-status`(PurchaseStatusPage).
- 이 목록들이 ?from=&to= **미수신** → "링크만 박으면 필터 안 먹음" 함정. 기존 GoToTransaction도 반쪽이었음.

### 봉합 (Dashboard.razor + 목적지 2페이지)
- **D-1**: 멀티캘린더 시각화 라디오 4뷰 복원(캘린더/매출·매입추이/거래처순위/외상연체순위). 기존 _chartOptions·_partnerChartOptions·MonthlyTrend·TopPartners·_aging 재사용 + _agingChartOptions 신설.
- **D-2·3**: 오늘매출·이번달매출 카드 → `/sales/summary?from=&to=`(GoTodaySales·GoMonthSales).
- **D-4**: 이번달매입 카드 신설(KPI 3→4장, _data.MonthPurchase) → `/purchase-status?from=&to=`(GoMonthPurchase).
- **D-5**: 미수금 → `/collections` 유지(이미 정합).
- **근본**: SalesSummaryPage·PurchaseStatusPage 에 `[SupplyParameterFromQuery] from/to` 수신 추가 → GoToTransaction 반쪽도 완결.

---

## 4. ★ 다음 세션 첫 작업 = B-1 demo 배포 (사장님 승인 게이트)

### 무엇
develop 최신 코드(오늘 C·D 포함)를 demo에 1회 정합 배포 → **CORS·approval 403·discount_rate 운영DB갭 + C·D 신기능 전부 동시 해소.**

### 어떻게 (헌법 #29·#39 절차 — 건너뛰면 어제 사고 재발)
1. **사장님 승인** — demo 운영 프로세스 정지·기동(헌법 #29)은 PM 단독 금지.
2. **테스트환경 dry-run** — deploy-demo.ps1 을 hitpan_e2e + 테스트포트에서 먼저(헌법 #39).
3. **demo 1회 배포** — dry-run 통과 후에만. `installer/deploy-demo.ps1`(스모크 6종 + 실패 시 자동 롤백).
4. 스모크 6종이 CORS·approval(6번째)·캘린더(버전정합)·복호화(키정합) 전수 검증.

### deploy-demo.ps1 상태
- 초안 완성(정지·키정합·기동 실동작 + 스모크 6종). UTF-8 BOM 저장(한글 파싱 사고 봉합).
- `-Execute` 없으면 dry-run만(demo 무손상 코드 보장). 경로·포트·커밋핀은 배포 시 확정.
- demo 좌표: Web 5234 / API 5257 / DB hitpan_erp / 키 `C:\Program Files\HitPan\hitpan-keys.conf` ERP_ENCRYPTION_KEY / 터널 무변경.

### demo 미해결 2건 = 봉합할 코드 없음(작5 규명)
- CORS·approval 403 둘 다 소스 정상, 옛빌드가 원인 → B-1 배포로 해소. 별도 코드봉합 작업지시서 없음.

---

## 5. 미커밋 보존분 (건드리지 말 것)
- `appsettings.json` — 헌법 #21로 수정·커밋 보류(어제부터). 월요일 3시스템 연결 때 사장님 승인 후 별도. 메모리 project_appsettings_backoffice_url_pending.

---

## 6. 잔여 스케줄 (오늘 못 간 것)
- **B-1** (위 4번, 다음 세션 첫 작업)
- E-1 P2 메시지 둔갑 / E-2 BOM UX (베타 비차단, 후순위)
- 통합 스케줄표: docs/handoff/20260626_통합스케줄표_대시보드포함_PM.md (작업번호 충돌 정리 필요 — 어제 작4·작5 = DDL·멱등 / 오늘 작4~7 = deploy·전수조사·BOM·대시보드. 다음 발행 시 재정렬).

---

## 7. PM 메모
- 오늘 받아쓰기 3번 직전 차단(C-1 봉합불필요 / C-2 새테이블→기존인프라 / D 목적지 편집화면→진짜목록). 전수조사·SoD·에이전트 교차가 일관 차단.
- demo 한 번도 안 건드림 = 어제 사고 정반대로 일함. B-1도 같은 규율로(승인→dry-run→1회).
