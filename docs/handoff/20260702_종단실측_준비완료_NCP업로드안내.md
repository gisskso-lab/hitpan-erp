# 종단 실측 준비 완료 — 랜딩→백오피스→ERP 설치 (2026-07-02)

> PM 브라운킴 / 사장님 명령 "실측 바로 가능하도록 모든 준비" 이행 결과.
> 제 손으로 되는 건 다 했고, **사장님 손이 꼭 필요한 것(NCP 업로드 = 헌법 #29)** 만 아래 [사장님 실행]에 정리.

---

## ✅ PM이 완료한 준비 (제 손)

### 1. 봉합 코드 반영한 최신 설치마법사 EXE 빌드
- 파일: `dist/HitPan-ERP-Setup-1.2.23.exe` (280 MB, 2026-07-02 15:41)
- SHA256: `ed40ffd3511d114b03875e7b525daafa00f31105c0521bfd88482ebf6613244c`
- 반영 봉합: 접속 기기 슬롯 P0 **1차(a74d487)** + **2차(5f41dfb, 지문 origin 비의존)** + **I3(F3 안내 UI)**.
- 출하 4게이트 통과: 124테이블·트리거3·**데이터0·백도어0**(헌법 #18/#22/#36 — 남의 데이터 미혼입).

### 2. 랜딩 다운로드 링크 정합
- `DownloadPage.razor`: 다운로드 링크·버전 표기를 **1.2.15 → 1.2.23** 으로 수정.
  - 링크: `https://updates.hitpan.kr/packages/HitPan-ERP-Setup-1.2.23.exe`
- ⚠️ 이 변경 때문에 **NCP 업로드 파일명은 반드시 `HitPan-ERP-Setup-1.2.23.exe`** 여야 링크가 맞는다(404 방지).
- 랜딩 빌드 0/0 확인. 단 **랜딩도 NCP 재배포**해야 이 링크 수정이 반영됨(아래 실행 2).

### 3. 빌드/정합 검증
- API·Web·Landing 각각 빌드 0/0(헌법 #19). 지문 보강 node 실측 PASS(origin 무관 동일).

---

## 🔴 사장님 실행 (NCP = 헌법 #29 인프라, PM이 원격 조작 불가)

> NCP 접속·업로드는 사장님 PC·사장님 계정에서만. 프로젝트에 NCP 접속 스크립트가 없어(사장님 별도 관리 영역) PM이 명령을 추측 생성하지 않음.

### 실행 1 — 설치마법사 EXE를 NCP에 업로드
- 올릴 파일: `dist/HitPan-ERP-Setup-1.2.23.exe`
- 올릴 위치: `updates.hitpan.kr` 서버의 `packages/` 디렉터리 (랜딩 링크가 가리키는 곳)
- 업로드 후 확인: 브라우저에서 `https://updates.hitpan.kr/packages/HitPan-ERP-Setup-1.2.23.exe` 접속 시 다운로드 시작되면 OK.

### 실행 2 — 랜딩 재배포 (링크 수정 반영)
- `HitPan.Landing` 을 NCP(landing.hitpan.kr)에 재배포해야 1.2.23 링크가 반영됨.
- (사장님 기존 랜딩 배포 방식대로)

### 실행 3 — 백오피스 살아있는지 확인
- 종단 흐름(가입→시리얼 발급)이 되려면 백오피스(back.hitpan.kr, NCP)가 기동 중이어야 함.
- 시리얼 발급 API가 응답하는지 확인.

---

## 🎯 실측 순서 (준비 완료 후 사장님이 브라우저에서 직접)
```
1. landing.hitpan.kr 접속 → 가입(사업자정보·희망도메인)
2. 시리얼 넘버 발급 확인 (메일 또는 화면)
3. 다운로드 → HitPan-ERP-Setup-1.2.23.exe 설치
4. 설치 중 시리얼 입력 → 자동 설정·설치 완료
5. 부모계정 로그인 확인
6. ⭐ 기기 슬롯 실측: 브라우저로 접속하며 — 같은 기기 재접속 시 기기 1건 유지되는지,
   새 기기(다른 PC/브라우저) 접속 시 "새 기기 등록" 안내 뜨는지, 슬롯 한도 초과 시 차단되는지
7. 데이터 직접 입력해보며 실동작 확인
```
- 이 종단 실측이 곧 작2 검증(V2·V5)을 겸한다 — 봉합이 실제 설치·로그인 경로로 도는지 확인.
- 막히는 화면 나오면 그 지점이 "진짜 끊김" — 캡처해서 PM에게.

---

## 미완 (실측 후 판단)
- **I4 지문 재매핑**: 기존 고객 업데이트 배포 시 필요(신규 설치 종단엔 불필요 — 옛 지문 없음). 이번 실측은 신규 설치라 I4 없이 진행 가능. 업데이트 배포 전 구현.
- EXE는 git 커밋 안 함(대용량, 기존 관례).
