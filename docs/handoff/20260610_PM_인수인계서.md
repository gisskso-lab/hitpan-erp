# 2026-06-10 (수) PM 인수인계서 — 브라운킴

> 새벽 작업까지 박힘. 사장님 컨디션 우선 종료.
> 마지막 명령: "내일하자. 커밋, 인수인계까지 하고"

---

## 🔴 내일 아침 첫 작업 — 사고 #6 봉합 (가입 승인 흐름)

오늘 저녁 사장님이 시험하시면서 발견:
1. 백오피스에 「승인」 버튼 자체가 없음 (T-001 status=pending인데 승인 액션 누락)
2. `test1.hitpan.kr` 접속 시 DNS_PROBE_FINISHED_NXDOMAIN — 도메인 자동 발급 미작동

### 봉합 시도한 영역 (3번 헛바퀴)
AdminTenantsV2.razor에 「승인」 버튼 박았는데, **MudBlazor Provider 위치 사고로 다이얼로그 호출 시 500 떨어짐**.

시도한 위치:
- ❌ PlatformLayout (그 안에 박혀있으면 dialog가 못 봄)
- ❌ Routes.razor (Router 안에 박혔는데도 dialog 영역 분리)
- ❌ AdminTenantsV2 페이지 내부 (이중 등록 → ObjectDisposed)

### 내일 정공법 (제안)
1. AdminSignups.razor 같이 잘 작동하는 페이지의 Provider 박힘 패턴 비교
2. ConfirmDialog 우회 — 다이얼로그 없이 곧장 PUT 호출 (자바스크립트 confirm() 사용 또는 단순 클릭)
3. App.razor 또는 다른 Root 영역에서 InteractiveServer 모드로 Provider 박는 패턴 적용

---

## 오늘 (2026-06-10 수) 진짜 박힌 봉합

### ✅ 사고 #1 — 백오피스 「고객사 목록」 안 보임
**진범 3중 사고:**
1. `bo_permissions` 테이블 비어있음 → `tenants.list` 권한 매핑 0건 → 403
2. .NET JwtBearer 기본 클레임 매핑이 `role` → `http://schemas.microsoft.com/.../role` URL로 변환 → `[BoPermission]`이 못 찾음
3. PlatformLayout에 MudPopoverProvider/MudDialogProvider/MudSnackbarProvider/MudThemeProvider 누락 → 500

**봉합:**
- DB: `bo_permissions`에 4건 INSERT (`tenants.list/detail/suspend/activate`)
- 코드: `Program.cs`에 `o.MapInboundClaims = false;` 박음 (`src/HitPan.Backoffice.API/Program.cs`)
- 코드: PlatformLayout에 Provider 4개 박음 (`src/HitPan.Backoffice/Components/Layout/PlatformLayout.razor`)

**검증:** 사장님 화면에 T-001~T-004 4건 정상 표시 확인됨

### ✅ 사고 #5 — 라이선스 키 4건 동일
**진범:** Approve 흐름의 UPDATE 조건이 `WHERE company_name AND status='pending'` → 같은 회사명의 여러 pending tenant에 같은 키 박힘

**봉합:** 
- `WHERE tenant_id` 단건 매칭으로 변경 (`src/HitPan.Backoffice.API/Controllers/SignupsAdminController.cs`)
- signup.submitted_at에 가장 가까운 pending tenant 1건만 매칭

**검증:** 새 가입 → 승인 흐름이 막혀서 아직 미검증 (사고 #6 봉합 후 검증 가능)

### ✅ 가입 데이터 클린
사장님 명령으로 백오피스 운영 데이터 통째로 삭제:
- `landing_signups`·`tenants`·`tenant_*`·`serial_verify_*`·`webhook_outbox` 다 비움
- 보존: `platform_admins`(master 계정)·`bo_permissions`·`pricing_*`·`reseller_*`

### ✅ 인프라 봉합 — nginx WebSocket
NCP `/etc/nginx/sites-available/back.hitpan.kr.conf`:
- `Connection keep-alive` → `Connection $connection_upgrade` (Map 변수)
- `proxy_read_timeout 3600s` / `proxy_send_timeout 3600s` 추가
- `/etc/nginx/conf.d/websocket-map.conf` 신규 생성
- 백업: `/etc/nginx/sites-available/back.hitpan.kr.conf.bak-20260610`

---

## 🚧 보류 사고

### 사고 #4 — 설치 후 ERP 미작동
**증상:** `test1.hitpan.kr` 접속 시 `DNS_PROBE_FINISHED_NXDOMAIN`. 도메인 자동 발급이 일어나지 않음.

**진단 시작도 못함.** 사고 #6 (승인 흐름) 봉합 후, 승인 시점에 Cloudflare API 호출이 실제 일어나는지부터 봐야 함.

### 사고 #6 — 가입 승인 흐름 (위 참조)

### 사이드 사고 — 웹훅 무한 재시도
백오피스 API가 `https://api-demo.hitpan.kr/api/internal/webhook/subscription` 으로 1분마다 POST → 530 응답 → 재시도 무한 반복. 사장님 개발 PC ERP가 꺼져있거나 터널 끊긴 게 원인. 가입 사고와는 무관.

**봉합안:** webhook_outbox 비움 (오늘 데이터 클린 시 이미 삭제됨) → 무한 재시도 자동 멈춤. 다시 발생 시 outbox에 박힌 row만 삭제.

---

## 환경·자격증명 영역

### NCP 서버 (변경 없음)
- IP: `211.188.58.140`
- SSH 키: `C:\Users\소순근\Downloads\hitpan-key.pem` (권한 조정 완료, 비번 안 묻음)
- root 비번: `P6-eUEJuiFFi`
- 서비스: `hitpan-backoffice-api`(5258), `hitpan-backoffice-web`(5259), `hitpan-landing`(5260)

### 핵심 파일
- 권한 필터: `src/HitPan.Backoffice.API/Filters/BoPermissionAttribute.cs`
- 권한 서비스: `src/HitPan.Backoffice.API/Services/BoPermissionService.cs`
- JWT 발급: `src/HitPan.Backoffice.API/Controllers/BackofficeAuthController.cs`
- JWT 검증: `src/HitPan.Backoffice.API/Program.cs` (라인 49~)
- 쿠키→Bearer 핸들러: `src/HitPan.Backoffice/Services/JwtFromCookieHandler.cs`
- 가입 승인: `src/HitPan.Backoffice.API/Controllers/SignupsAdminController.cs`
- 고객사 목록 UI: `src/HitPan.Backoffice/Components/Pages/Platform/AdminTenantsV2.razor`
- PlatformLayout: `src/HitPan.Backoffice/Components/Layout/PlatformLayout.razor`

---

## NCP 배포 상태

| 영역 | 배포됨? | 비고 |
|---|---|---|
| 사고 #1 봉합 (DB 권한) | ✅ | mysql INSERT 직접 박음 |
| 사고 #1 봉합 (MapInboundClaims) | ✅ | API dll 배포 완료 |
| 사고 #1 봉합 (Provider) | ✅ | Web dll 배포 완료 |
| nginx WebSocket | ✅ | systemctl reload nginx 완료 |
| 사고 #5 봉합 (Approve WHERE) | ✅ | API dll 배포 완료 |
| 사고 #6 임시 봉합 (승인 버튼 숨김) | ❌ | 로컬 코드에만 박음, 배포 안 함 |
| 디버그 로그 제거 | ⏳ | 로컬 코드 정리 완료, 재빌드·배포 필요 |

**내일 첫 작업:** 디버그 로그 제거된 클린 빌드로 API+Web 한 번 더 배포 → 그 후 사고 #6 정공법 봉합

---

## 헌법 정합 체크

- ✅ 헌법 #29 — 인프라 조작 모두 사장님 사전 결재 (nginx·DB 데이터 삭제 직전 결재 받음)
- ✅ 헌법 #33 — 사장 의중 100% 이해 + 재확인 후 진행 (A안/B안/C안 결재 흐름)
- ✅ 헌법 #35 — 3시스템 분리·연결 유지 (백오피스만 봉합, 랜딩/ERP 미건드림)
- ✅ 헌법 #15 — 빈 catch 없음, 디버그 로그 다 ILogger 또는 Console.WriteLine
- ⚠️ PM 자성 — 사고 #1 진단에 헛바퀴 너무 많이 돌림 (3시간+). 정공법 1차 시도 실패 시 즉시 다른 방향 가야 함

---

## 사장님 격언 (오늘 박힘)

> **"백오피스 가동이 안되면 베타 출시 자체가 안되"** (2026-06-10 21시경)

PM이 우선순위 잘못 짚었을 때 사장님이 정정. 가입→설치→ERP 작동 한 흐름 중 백오피스가 막히면 전체 다 막힘.

> **"렌딩페이지로 가입된 고객사 데이터를 삭제하라고"** (2026-06-10 22시경)

PM이 삭제 범위를 너무 넓게 잡아서 사장님이 정정. 운영 데이터(가입·tenant)만 삭제, 마스터(권한·가격·대리점)는 보존.

---

## 내일 진행 순서 (제안)

1. **클린 빌드 + 디버그 로그 제거 코드 배포** (10분)
   - API + Web 둘 다 재빌드 → SCP → cp → restart
2. **사고 #6 정공법 봉합** (30~60분)
   - AdminSignups.razor 등 다른 페이지가 다이얼로그를 어떻게 띄우는지 패턴 비교
   - ConfirmDialog 우회 — 단순 confirm()이나 즉시 PUT 방식
   - 검증: T-001 「승인」 버튼 클릭 → 시리얼 발급 알림 → DB에 license_key_plain 박힘 확인
3. **사고 #2 검증** (10분)
   - 승인 시점에 사장님 이메일로 시리얼 키 메일 도착 확인
4. **사고 #4 진단** (30~60분)
   - 승인 후 Cloudflare API 호출이 일어나는지 코드 추적
   - DNS 레코드 자동 발급 흐름 (가능성: 환경변수 누락 / Cloudflare API 토큰 사고)
5. **사이드 사고 — 웹훅 무한 재시도** (5분)
   - webhook_outbox 비우면 자동 봉합

베타1까지 D-54.

— 브라운킴 (2026-06-10 수 23:30 작성)
