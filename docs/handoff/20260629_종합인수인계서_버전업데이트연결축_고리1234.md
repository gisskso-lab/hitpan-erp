# 종합 인수인계서 — 버전 업데이트 연결축(4번) 고리1~4 진행
> 작성: PM 브라운킴 / 2026-06-29 마감
> 다음 세션은 이 문서부터 읽으면 0초 이어받기. SOP [★] 정합.

---

## 0. 오늘 한 줄 요약
버전 업데이트 워크플로우(4번 = 본사 게시→로그인 Y/N→백업→적용→마이그) 의 **코드 골격을 거의 다 세웠다.** 고리1·2·3 + 고리4①단계 완료(전부 검증 PASS·커밋). 남은 건 고리4②③ + 종단 실측 — **테스트 환경(B갈래) 필요**.

---

## 1. 오늘 커밋 (develop, 6건)
- `b7731fe` 매니저 3인 영입(마이클·존·안철수) + AI CS자동화 기획서
- `38ee821` 테스트환경 A갈래 준비물(Build-TestSlot.ps1·로컬 manifest 피드·작1/작2 작업지시서)
- `ae6bccf` 고리1·3(Orchestrator 연결+백업실패 물리적차단) + 고리4 설계서
- `c1221f9` 고리2(로그인 Y/N 동의 자가완결 A안, 전체6단계)
- `6381c70` 고리4①(스키마버전 추적 schema_migrations + MigrationRunner)
- (appsettings.json은 6건 전부 제외 — 헌법 #21 보류건 [[project_appsettings_backoffice_url_pending]])

---

## 2. 완료(검증 PASS) — 무엇이 봉합됐나
| 고리 | 내용 | 검증 |
|---|---|---|
| **1** | UpdateOrchestrator를 워치독 Worker에 연결(DI+하루1회 평가) + 채널 직렬화(JsonStringEnumConverter, 문자열·정수 둘다) | 8관문 PASS |
| **2** | 로그인 Y/N 동의 **고객PC 자가완결(A안, 헌법#30)**: 워치독 새버전 발견→local_update_status 적재→ERP 로그인 읽기→팝업→동의 local_update_consents 기록→워치독 읽기→영업시간외 적용. 본사엔 메타ping만 | 전체6단계 일치도100% PASS |
| **3** | 백업→업데이트 + **백업 실패 물리적 차단**(우회경로0). WatchdogBackupRunner(mysqldump 자족) | PASS |
| **4①** | schema_migrations(DB-84) + MigrationRunner(번호순·멱등·실패시중단·timeout86400·자동실행배선0) | 14반증 PASS |

핵심 신규 파일:
- 워치독: WatchdogBackupRunner.cs, WatchdogConsentReader.cs, WatchdogStatusWriter.cs
- API: AuthController(EnrichUpdateConsentInfoAsync + /update-consent), MigrationRunner.cs, IMigrationRunner.cs
- DB: DB-82(local_update_consents), DB-83(local_update_status), DB-84(schema_migrations) — 셋 다 로컬, clean DDL 편입 121→124, .iss/build 게이트 6곳 124 정합
- ERP Web: Login.razor 팝업, AuthModels/AuthService/LoginResponse 필드

---

## 3. 🔴 남은 것 (다음 세션) — "코드까지"는 했어도 "봉합완료" 아님
### 3-1. 고리4 ② Velopack PoC (코드 미착수)
- VelopackUpdaterStub → 실제 NuGet 통합(원자적 EXE 교체·재시작)
- ⚠️ **핵심 미지수**: Velopack이 "EXE 떴는데 DB마이그 실패" 자동롤백 지원하나? = **코드로 답 안 나옴, B갈래 실측 필수**(설계서 R2)
- MigrationRunner DI 배선·호출 연결·csproj SQL CopyToOutput = ②에서

### 3-2. 고리4 ③ 롤백 상태머신 (코드 미착수)
- 마이그 실패 시 DB+EXE 전체덤프 복원(스냅샷 교체, 사장님 R6 결재 = §#3 위반 아님)
- 적용~검증 구간 ERP read-only 잠금(R4 결재)
- update-state.json 영속(전원차단 대비), MariaDB DDL 암묵커밋→전체덤프복원이 유일(R1)

### 3-3. 종단 실측 0 (제일 중요)
- 오늘 PASS는 전부 **코드검증+빌드0/0**. 실제 업데이트 돌린 실측 0회.
- 진짜 봉합완료 = **B갈래 테스트환경에서 종단 실측** 후.

### 3-4. 운영DB 갭
- 새 테이블(DB-82·83·84) clean DDL엔 있으나 운영(demo/hitpan_erp)엔 미반영(#36 한계). demo 적용은 사장님 결재 후 ALTER(#39). [[project_clean_ddl_vs_live_db_gap_20260623]]

---

## 4. B갈래 = 사장님 손 (헌법 #29, 아직 안 세움)
테스트 환경 = ②③ 실측 + 17시나리오 통신검증 공용(1석2조). 준비물(A갈래)은 다 됨. 사장님이 실행하실 순서:
```
[B-1] cd tools/IssueTestDomain; dotnet run -- --issue --alias test   (DNS+터널+ingress 자동, 도구 완성됨)
        ↓ 출력 터널토큰을 환경변수로
[B-2] $env:HITPAN_TEST_TUNNEL_TOKEN="(토큰)"; powershell -File tests/scenarios/comms-integrity/Build-TestSlot.ps1 -Confirm  (DB·마커·서비스)
        ↓
[B-3] 반증4건 실증(데이비드 박 대본) → 4/4 PASS
        ↓
[B-4] 실측: 통신17시나리오 + 업데이트 종단(고리2~4)
```

---

## 5. 이번 작업 결재·교훈 박제
- 사장님 결재: 고리4 6건(단계분할·테스트환경검증·R6원장스냅샷·R1/R4·묶음·베타1전), 게이트122/123/124 수정, A안(로컬 자가완결)
- 교훈: **단계분할이 폭탄 캐냄** — DB매니저가 PM "clean DDL 넣어라" 반박(본사테이블→로컬 전환, 헌법#18·#22 사고 차단) / 고리4 설계가 "ALTER 자동주체 부재·스키마버전 부재·Velopack 롤백 불확실" 발견. 바로 박았으면 대형사고. **사장님 "안전하게"가 매번 옳았음.**
- 관련 메모리: [[project_4workflow_status_20260628]] [[project_ring4_migration_rollback_decisions_20260629]] [[project_team_three_managers_20260628]] [[feedback_constitution_39_no_live_surgery]]

---

## 6. 다음 세션 첫 행동
1. 본 인수인계서 + [[project_ring4_migration_rollback_decisions_20260629]] 읽기
2. 사장님께: "오늘 ②③ 코드 더 박을지 / B갈래(테스트환경) 세우고 실측부터 갈지" 의중 확인(#33)
3. ②③ 코드 가면: 결재된 단계순 ②Velopack→③롤백. 단 Velopack 롤백지원은 실측대기로 정직 표시(거짓봉합 금지)
