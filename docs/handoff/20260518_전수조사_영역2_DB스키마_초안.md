# 🔍 영역 2 — DB 스키마 전수조사 (초안)

> **책임**: DB 매니저 + DB 개발자 3명
> **PM 1차 분석**: 2026-05-18 17:00
> **대상**: 마이그 관련 18개 테이블 UNIQUE 인덱스

---

## 📊 UNIQUE 인덱스 매트릭스

### sales_deliveries (1,560행 / DOCFB 5,284 매출)
| 인덱스 | 컬럼 | 9차 충돌 |
|---|---|---|
| PRIMARY | delivery_id | 0 (GUID) |
| **uq_delivery_no** | tenant_id + delivery_no | **402건 충돌** 🚨 |
| uq_sd_source | tenant_id + source_id | 0 ✅ |

### purchase_receipts (16,099행 / DOCFB 118,868 매입)
| 인덱스 | 컬럼 | 9차 충돌 |
|---|---|---|
| PRIMARY | receipt_id | 0 (GUID) |
| **uq_receipt_no** | tenant_id + receipt_no | **99,048건 충돌** 🚨 |
| uq_pr_source | tenant_id + source_id | 0 ✅ |

### tax_invoices (65,979행 / DOCF6+TX 66,631)
| 인덱스 | 컬럼 | 9차 충돌 |
|---|---|---|
| PRIMARY | invoice_id | 0 |
| uk_tax_invoices_invoice_no | tenant_id + invoice_no | 652건 |
| uq_tax_invoices_io_no | tenant_id + direction + tax_no | (확인 필요) |
| uq_tax_invoices_source | tenant_id + source_type + source_id | 0 ✅ |
| uq_tax_invoices_source_hash | tenant_id + migrated_source_hash | (확인 필요) |

### journal_entries (23,152행 / DOCF7 23,170)
| 인덱스 | 컬럼 | 9차 충돌 |
|---|---|---|
| PRIMARY | entry_id | 0 |
| uq_je_source | tenant_id + source_type + source_id | 0 ✅ |
| uq_je_tenant_entryno | tenant_id + entry_no | 18건 |

### journal_lines (23,388행 / DOCF7 27,661)
| 인덱스 | 컬럼 | 9차 충돌 |
|---|---|---|
| PRIMARY | line_id | 0 |
| uk_journal_line_dedup | tenant_id + entry_id + account_code | 4,273건 (한 entry 안에 같은 account 다중) |
| uq_jl_source | tenant_id + source_id | 0 ✅ |

### collections (614,212행)
| 인덱스 | 컬럼 | 9차 충돌 |
|---|---|---|
| (PRIMARY 없음?) | (확인 필요) | |
| uq_collections_source_hash | tenant_id + migrated_source_hash | 0 ✅ |

### stock_ledger (115,460행 / 116,420)
| 인덱스 | 컬럼 | 9차 충돌 |
|---|---|---|
| PRIMARY | ledger_id | 0 |
| uq_stock_ledger_source | tenant_id + source_type + source_id + item_id + move_type | 960건 |
| uq_stock_ledger_source_hash | tenant_id + migrated_source_hash | (확인 필요) |

### quotations / sales_returns / purchase_returns
| 테이블 | 인덱스 |
|---|---|
| quotations | PRIMARY (quote_id) — UNIQUE 인덱스 1개만 |
| sales_returns | PRIMARY + uq_sret_tenant_returnno (tenant_id + return_no) |
| purchase_returns | PRIMARY만 |

### 마스터 (partners·items)
| 테이블 | UNIQUE |
|---|---|
| partners | uq_tenant_code + uq_partners_source_hash |
| items | uq_tenant_code |

---

## 🚨 진범 후보 종합

### 진범 #21 (확정 박제)
**purchase_receipts uq_receipt_no 충돌 99,048건**:
- `receipt_no = "DOCFB-{dtStr}-{io}-{seq:D4}"` 패턴 중복
- DOCFB 118,868행 중 86.4% 같은 (dtStr, io, seq) 조합 발견
- 가설: `seq`(IJ_SEQ)가 한 거래 안의 라인 순번이라 같은 거래 묶음에서 중복

**sales_deliveries uq_delivery_no 충돌 402건**:
- 동일 패턴 (DOCFB IO=1 5,284행 → staging 1,962 → INSERT 1,560)

### 진범 #21 봉합 옵션 매트릭스 (CTO 결재 대상)

| 옵션 | 변경 | 효과 | 위험 |
|---|---|---|---|
| **A** | docNo에 `-{buyCode}` 추가 | `DOCFB-{dtStr}-{io}-{seq:D4}-{buyCode}` | buyCode 중복 시 여전히 충돌 |
| **B** | docNo를 source_id 그대로 사용 (`mig-docfb-{dtStr}-{io}-{seq}-{buyCode}`) | 멱등성 100% | delivery_no 가독성 ↓ |
| **C** | DB UNIQUE 인덱스 변경 — uq_delivery_no/uq_receipt_no DROP, source 기반만 유지 | 변경 최소 | 일반 ERP 운영 시 delivery_no 중복 위험 |
| **D** | INSERT IGNORE → ON DUPLICATE KEY UPDATE | 덮어쓰기 | INSERT ONLY 헌법 #3 위반 위험 |

**PM 1차 추천: B**. source_id가 이미 `-buyCode` 포함이라 중복 0 입증. delivery_no 가독성은 부차적 (legacy 데이터).

### 진범 #28 (NEW)
**journal_lines uk_journal_line_dedup 충돌 4,273건**:
- (tenant_id, entry_id, account_code) 같은 entry 안에 같은 account 다중 line
- DOCF7 27,661행 → 23,388 INSERT (4,273 손실)
- 가설: 한 전표 안에 같은 계정과목 차/대변 분리 line 존재 — 정상일 수 있으나 dedup 정책 점검 필요

### 진범 #29 (NEW)
**stock_ledger uq_stock_ledger_source 충돌 960건**:
- 5개 컬럼 복합 UNIQUE — (tenant_id, source_type, source_id, item_id, move_type)
- 정상 범위로 추정 (item_id 다중 movement)

---

## 📋 영역 2 5/19 풀 분석 계획

| 시간 | 작업 | 담당 |
|---|---|---|
| 5/19 09:30 | 진범 #28·#29 정상성 확인 | DB 매니저 + ERP |
| 5/19 12:00 ~ 18:00 | 18개 테이블 FK 매트릭스 + Collation 통일 확인 | DB 개발자 3명 |
| 5/19 18:00 ~ 22:00 | 진범 #21 봉합 옵션 A/B/C/D CTO 결재 안건 정리 | DB 매니저 + PM |
| 5/19 22:00 ~ 5/20 02:00 | 5중 검증 (헌법 #23) | DB + 보안 + AI수석 |

---

**작성**: PM 브라운킴 2026-05-18 17:00
**상태**: 1차 초안. 5/19 09:00 DB 매니저 풀가동.
