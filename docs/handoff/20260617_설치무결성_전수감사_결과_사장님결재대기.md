# 20260617 설치 무결성 전수 감사 결과 — 사장님 결재 대기

> PM 브라운킴 / 2026-06-17 사장님 퇴근 후 작성
> 전 직원 13개 에이전트 워크플로우 감사 (매니저5 + 4병렬적대검증 + 디자인3 + PM종합)
> **결론: canShip = FALSE (4/4 만장일치 출시 불가). 사장님 직감이 옳았음.**

---

## 0. 한 줄 — 사장님이 맞았습니다

"이 설치파일을 어떻게 신뢰하냐" = 정확. 지금 설치파일은 **신뢰 불가**. 실측으로 확정.
1.2.6~1.2.15 12시간+ 봉합은 전부 증상(로그인 500)만 쫓은 것이고, 진짜 근본은 따로 있었음.

## 1. 진짜 근본 원인 (하나)

**"개발 DB를 그대로 떠서 설치본에 넣는 빌드 파이프라인 자체가 잘못 설계됨."**
- 설치본 DB는 *히트판ERP 전용 빈 스키마 + 회사 관리자 1명*이어야 하는데, 데모/개발 DB가 통째로 흘러들어감.
- **개발=설치본 정합을 보장하는 게이트가 없어서**, 137이든 91이든 87이든 어떤 덤프든 빌드가 통과해버림.
- 실측: 소스 137테이블 / 출시번들 91 / installer-build 87 — **3종이 다 다르고 셋 다 출시 불가.**

## 2. P0 (반드시 봉합)

### 🔴 P0-1 보안 백도어 (최우선) — 담당: 보안매니저1 + DB매니저
- 출시 번들에 `admin@hitpan.kr / Admin1234!` **platform_admin 계정 시드** + 그 **bcrypt 해시가 git 공개**(`$2a$12$j8KtWNN7I...`).
- `AuthService.FindUserByEmailAsync`(AppDbContext.cs:154-159)가 `.IgnoreQueryFilters()`로 테넌트 무시·email+IsActive만 매칭 → **모든 고객 PC에 본사 platform_admin 원격 로그인 가능 = 치명 백도어.**
- reseller@hitpan.kr·tenant@hitpan.kr도 동일 해시로 시드됨.
- 봉합: 출시 시드에서 platform_admin/reseller_admin 전면 제거, 회사 최초 관리자 1명만(랜딩 가입정보 기반). git 히스토리 정리·해시 폐기.
- **사장님 결재 대상** (DB 시드 변경).

### 🔴 P0-2 핵심 테이블 46개 누락 — 담당: DB매니저
- 출시 번들(91테이블)에 `tax_invoice_items·bank_transactions·card_payments·card_payment_lines·bills·form_templates·item_specs·partner_contacts·stock_monthly_snapshot` 등 MISSING.
- catch로 안 가려지는 직접 쿼리 → 매입·판매·재무 화면 실제 500 (헌법 #20 워크플로우 끊김).
- 봉합: 히트판ERP 전용 화이트리스트 빈 스키마 신규 작성. **137 통째 복사 금지**(HQ 22개·백도어·본사데이터 따라옴).
- **사장님 결재 대상** (시드 스키마 확정).

### 🔴 P0-3 API DLL 1.2.12 고착 — 담당: 백엔드매니저 (결재 불요, 빌드 영역)
- 번들 `HitPan.API.dll` = 1.2.12(01:49). HEAD는 1.2.15. `-SkipErpBuild` 가드로 1.2.13 CSP 정정(폰트·다음우편)이 미반영된 옛 DLL이 EXE마다 동봉됨.
- 봉합: `-SkipErpBuild` 없이 빌드 + DLL ProductVersion==HEAD 게이트. **코드 변경 0건.**

### 🔴 P0-4 멀티사업자 슬롯2 터널 라우팅 불가 — 담당: 네트워크매니저
- `InstallerBootstrapController.cs:132` ingress origin `localhost:5257` 하드코딩. 슬롯2~5는 5357/5457... → 슬롯2 터널이 5257(슬롯1)로 라우팅 → 타사 데이터/502 (헌법 #35·멀티사업자 위반).
- 봉합: bootstrap 요청에 apiPort 포함 or ERP 측에서 슬롯 확정 후 호출. **컨트롤러+iss 코드 변경 → 결재 대상.**

### 🔴 P0-5 터널 실패 "완료" 위장 — 담당: 네트워크매니저
- `InstallerBootstrapController.cs:101` CF 미설정 시 skip·tunnelToken=null인데 :172 success:true → iss:1111 "완료/5~10분 후 접속" 위장 (헌법 #15·#27). 현 운영은 CF 설정돼 미발현이나 잠재 구멍.
- 봉합: tunnelProvisioned=false+사유 명시, iss는 토큰 빈 경우 명시 실패 분기. **코드 변경 → 결재 대상.**

### 🔴 P0-6 가입화면 거짓 안내 — 담당: 프론트매니저+수석디자이너
- WelcomePage "결제가 완료되었습니다"(베타1 무결제). VerifyPage 인증 전 "인증 완료" 단정 + "데모"·"시뮬레이션" 개발용어 노출.
- 봉합: 결제 카피 제거, "가입 신청 접수"로 정정, 개발용어 제거. **랜딩 Razor 변경 → 결재 대상.**

## 3. P1 (중요)
- stock_monthly_snapshot 부재 → 과거기간 재고현황 500 (DDL 없음, 137·91 둘다)
- user_terms_consent 부재 — TermsConsentMiddleware가 catch로 안 막아 **약관 미동의 통과**(컴플라이언스 우회). ※CTO가 백엔드매니저 "로그인 500 진범" 오판을 반증함
- 빌드 게이트가 "테이블 수" 매직넘버라 잘못된 구성 통과 → 화이트리스트/블랙리스트 게이트로 교체
- 선택 요금제가 가입완료에서 모두 basic으로 소실(VerifyPage:62 하드코딩)
- 사업자등록증 'OCR' 영어약어·베타2 기능 베타1 노출 / ERP 로그인 로고 'HP' 영문

## 4. 통과 기준 (이게 다 돼야 "온전히 작동")
1. 출시 번들에 HQ/백오피스/대리점 테이블 0건, 본사 데이터 0건, platform_admin/reseller_admin 시드 0건 (#22·#35)
2. 히트판ERP 필수 화이트리스트 테이블 전수 존재 (tax_invoice_items·bank_transactions·card_payments·bills·form_templates·item_specs·partner_contacts·user_terms_consent·stock_monthly_snapshot 등)
3. 번들 API DLL ProductVersion==HEAD 커밋, CSP jsdelivr·daumcdn·cloudflareinsights·wss 반영
4. 신규 PC 설치 후 실제 웹 스모크: 로그인 → 6단계 대표화면(발주·매입·세금계산서 라인·경비·회계·과거 재고현황) 1146/500 0건
5. 멀티슬롯2 ingress가 슬롯2 포트(5357)와 일치, 타사 라우팅 0건
6. CF 미설정/터널 실패 시 success 위장 0건
7. 가입 카피에 거짓 결제완료·인증전 단정·데모/OCR 0건, 선택 요금제 유지
8. errors 0 + warnings 0 (#19)

## 5. 올바른 빌드 계획
1. **출시 전용 시드 스키마 확정** — 137도 91도 그대로 안 씀. 137에서 ①HQ 22테이블+트리거5 제거 ②본사 운영/감사/세션 데이터 제거 ③platform/reseller/공개해시 users 제거 ④stock_monthly_snapshot DDL 추가 ⑤user_terms_consent 포함. 결과 = ERP 전용 빈 스키마(데이터 0), 회사 관리자는 설치 시 랜딩 정보로 생성. **(결재 후)**
2. `-SkipErpBuild` 없이 빌드 — API·Web 재publish (CSP 반영)
3. DB 정합 게이트 강화 — 금지구문(USE/DEFINER) + 화이트리스트 전수존재 + HQ 블랙리스트 0건 + 시드 platform_admin 0건
4. DLL 버전 게이트 — ProductVersion==HEAD
5. 멀티슬롯·CF 위장 봉합 반영해 EXE 컴파일(1.2.16)
6. 신규 PC 1대 설치→로그인→6단계 스모크→슬롯2 라우팅 실측. 전부 PASS여야 출시.

## 6. 사장님 결재 필요 (출근 시)
1. **[최우선·보안]** 백도어 계정 제거 + git 공개 해시 폐기·히스토리 정리 승인. 회사 관리자는 랜딩 가입정보 1명만
2. 출시 DB 시드 정책: '137 통째' 금지 → 'ERP 전용 화이트리스트 빈 스키마' 채택 (헌법 #22)
3. HQ 22테이블+트리거5+본사 운영/감사/세션 데이터 고객 설치본 전면 제외 승인 (#35)
4. stock_monthly_snapshot: 신규 DDL+월마감 배치 vs stock_ledger 폴백
5. 멀티사업자 터널 ingress 봉합 방식
6. 랜딩 가입 카피·OCR·로고 정정 진행 승인 (베타1 무결제 전제)

---

## PM 자가 진단
- 12시간+ 증상만 쫓고 "개발=설치본" 근본을 사장님이 세 번 말씀하셔야 봄. 헌법 #19·#23·#33 위반.
- 특히 "137 통째로 떠낼까요?"는 **백도어를 전 고객에 뿌릴 뻔한 위험한 제안**이었음. 보안매니저가 잡음.
- 이번 워크플로우(4병렬 적대검증)가 1차 오판(약관/로그인 진범)까지 반증 — 받아쓰기 방지 작동.

작업 상태: hitpan_db.sql은 137 덤프 상태(USE/DEFINER/ALTER/BOM 제거됨)로 디스크에 있으나 **출시 부적합**(백도어·HQ테이블·본사데이터 포함). 코드·DB·git 변경 0건. 빌드 안 함. NCP 1.2.15가 최신 배포본(버그 있음). 모든 봉합은 사장님 결재 대기.
