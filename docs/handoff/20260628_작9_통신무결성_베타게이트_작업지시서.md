# 작업지시서 작9 — 통신 무결성 베타 게이트 (터널 오라우팅 P0)

- 발행: 2026-06-28 / PM 브라운킴
- 결재 체인: CTO 1차 → 사장님 최종 (헌법 SOP)
- 근거: 사장님 지시 "베타 출시 위해 통신 이슈 더 이상 있으면 안 됨" + "PowerShell이 고객사 PC에 노출 안 되고 정상연결"
- 헌법: #27(통신무결성 베타 게이트=17시나리오 5분내 자동복구), #28·#30(워치독 자가복구), #29(인프라 결재), #33(의중 재확인), #32(받아쓰기 금지)

---

## 1. 실측 전수조사 결과 (전 직원, 코드 무수정)

### 1-1. 통신 무결성 인프라는 정식 설치 EXE에 이미 구현됨
| 항목 | 정식 .iss 설계 | 근거 |
|---|---|---|
| Web 창 숨김 | `-WindowStyle Hidden` | HitPan-Universal.iss:1167 |
| 자동시작 | 작업스케줄러 ONSTART + 1분 keepalive | .iss:1129·1168·1151·1181 |
| 워치독 | Windows 서비스(sc create) + Guardian 2층 | InstallWatchdog.ps1:31·41-68 |
| 워치독 5단계 복구 | WS28A~E 전부 구현(재부팅·자격증명·터널재설치·헬스체크) | src/HitPan.Watchdog/* |
| 502/프로세스 죽음 | 60초 감지 + schtasks /Run 재기동(<90초) | Worker.cs:104-115, WS28I:92-139 |
| 토큰 401 | 자동 refresh 봉합됨 | HitPanApiAuthHandler |

→ **창 비노출·자동시작·워치독은 "만들어져 있다".** 단 demo가 정식설치 경로를 안 거쳐 보호를 못 받는 중(별건 작업).

### 1-2. 🚨 베타 게이트 미통과 — 2개 구멍 (냉정, 헌법 #32)
**현 통신 무결성 = 구현 60~70% / 검증 10% 미만.**

| 구멍 | 사실 | 위험 |
|---|---|---|
| **① 터널 오라우팅 (오늘 진범)** | 워치독은 `127.0.0.1:5257/health`(내부)만 감시. **외부 api-demo.hitpan.kr 이 5234(Web)로 새는 ingress 라우팅 오류는 감시 대상 아님.** | 오늘 사고가 고객 PC에서 재발. 워치독 있어도 못 막음 |
| **② 17 시나리오 미검증** | SCENARIO_20 설계서만 존재. 실측 PASS 기록·자동화 스크립트 **0건.** | 헌법 #27 게이트 "구현됨"이지 "검증됨" 아님 |

## 2. ⚠️ 진단 정정 (2026-06-28, ingress 조회 후) — 터널 오라우팅은 P0 아님

**CTO 가설(ingress 한 도메인만 등록)·PM 진단(터널 오라우팅 상시결함) 둘 다 틀림. 정정.**

실측(읽기 조회 승인 후):
- `~/.cloudflared/config.yml` ingress = **두 도메인 정확히 분리**:
  - `demo.hitpan.kr → localhost:5234` (Web)
  - `api-demo.hitpan.kr → localhost:5257` (API)
  - `service: http_status:404`
- api-demo.hitpan.kr/health **10회 연속 호출 → 10/10 application/json** (HTML 0회). 지금 라우팅 완벽 정상.

→ **오늘 낮 콘솔의 text/html 은 상시 결함이 아니라, PM 새벽 demo 수술(6회) 중 cloudflared 일시 불일치 1회성.** "터널 오라우팅 P0"는 PM 과잉진단. 회수.
→ 클라이언트 방어(GetJsonSafeAsync 커밋 4acbf97)는 그래도 유효 — 일시 불일치·느린 회선에도 콘솔 오류 안 나게 하는 정공법(헌법 #27).

## 2-2. 진짜 P0 (사장님 결재 2026-06-28): 17 시나리오 실측 PASS

### P0 = 헌법 #27 베타 게이트 = 17 강제 시나리오 5분내 자동복구 실측 PASS
- **현 상태**: SCENARIO_20_VERIFY_SKELETON.md(S01~S20)·COMMS_INTEGRITY_PLAN.md(17건) **설계서만 존재. 실측 PASS 기록·자동화 스크립트 0건.**
- **할 일**: 통신을 실제로 죽이고(cloudflared kill·API kill·MariaDB stop·터널secret무효화·재부팅 등) 워치독이 5분 내 복구하는지 **실측·로그 기록·PASS 판정**.
- **범위 주의**: 일부는 자동화 가능(프로세스 kill→복구), 일부는 물리(정전·SSD)·환경(백신격리)이라 수동 실측 필요. 자동화 가능분과 수동분 분리.
- **검증 환경**: 헌법 #39 — 운영(demo) 아닌 **별도 테스트 환경**에서. demo 깨뜨리지 않게.

### 8대 영역 현 판정 (실측, 헌법 #32 냉정)
| 영역 | 자동복구 | 검증 | 판정 |
|---|---|---|---|
| 자격증명(터널secret·토큰) | ✅ WS28C/D | 부분 | 🟢 |
| 응용(API·DB크래시) | ✅ WS28I schtasks | 부분 | 🟢 |
| 인프라(재부팅) | ✅ WS28A/B | ❌ | 🟡 |
| 네트워크(회선·DNS) | ✅ WS28E | ❌ | 🟡 |
| 보안SW(백신격리) | ❌ 미감지 | ❌ | 🔴 |
| 물리(정전·SSD) | ❌ 불가 | ❌ | 🔴 |
| 인적(삭제·Ctrl+C) | 부분 | ❌ | 🔴 |
| 외부공격 | CF WAF | ❌ | 🟡 |

**종합: 구현 60~70% / 검증 10% 미만. 베타 게이트 미통과.**

## 3. P1 (P0 후속)
- 17 시나리오 자동화 스크립트 + 실측 PASS (헌법 #27 게이트 통과 증거)
- demo 정식설치 경로 재적용 (창숨김·자동시작·워치독 — demo 데이터·키 보존 설계 필요)
- 마이그 중 통신 끊김 재개 검증

## 4. 검증 (SoD — 만든 사람 ≠ 검증)
- P0-1 봉합 후: 실제로 ingress 를 5234 로 틀어놓고 → 워치독이 감지·교정해 api-demo 가 다시 JSON 주는지 실측 (5분 내)
- 빌드 errors 0 + warnings 0
- 본사 전송 코드 0 확인(#18·#22)

## 5. 리스크
- 워치독이 외부 도메인을 주기적으로 때리면 트래픽·로그 증가 → 주기 조절(예: 5분)
- ingress 자동 교정이 무한루프 되지 않게 가드(N회 실패 시 메타 ping 보고만)
- cloudflared 조작 = 헌법 #29 → 사장님 승인 후에만

## 6. CTO 1차 결재란
- [ ] P0-1 접근 (a)재기동 / (b)ingress재적용 중 택
- [ ] WS28E 확장 vs 신규 모듈
- [ ] 트래픽·무한루프 가드 방식
- → CTO 의견 정리 후 사장님 보고

## 7. 사장님 최종 결재란
- [ ] P0-1 진행 OK?  [ ] 접근 (a)/(b)?  [ ] 17시나리오(P1) 우선순위?
