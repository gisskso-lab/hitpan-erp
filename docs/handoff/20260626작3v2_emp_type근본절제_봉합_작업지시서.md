# 작업지시서 20260626작3-v2 — emp_type 매핑 붕괴 **근본 절제** 봉합

> 발행: PM 닥터스트레인지 | 2026-06-26
> 사장님 지시: *"임시로 봉합하지 말고, 암세포 떼내듯 문제점 자체를 제거하고 모두 봉합. 단 다른 거에 문제 생기지 않도록."*
> v1(값 2곳만 교체) 폐기 → v2(근본 절제)로 대체.
> 프로세스: [3] 작업지시서 → [4] CTO 재결재 대기
> 트랙: **풀 트랙(코드+데이터). 헌법 #1(덮어쓰기 금지)·#12(grep 전수)·#29(운영DB 결재)·#36(출하DDL).**

---

## 1. 목적 — 진통제 아닌 절제

emp_type가 **단일 진실원 없이** 4개 값(`'fulltime'`/`'full_time'`/`'regular'`/`'Regular'`)으로 갈라져, EF enum 변환(`HasConversion<string>()`, 멤버명 'Regular' 기대)과 충돌한다. 값 2곳만 갈면 마이그 직원·기존 데이터·화면 기본값이 깨진 채 남는다(임시 봉합). **모든 쓰기·기존 데이터·기본값을 enum 정합값 'Regular'로 통일**해 다시는 안 깨지게 한다. 단 **읽기 무관 경로(raw SELECT)는 안 건드려** 회귀 0.

---

## 2. 암세포 전체 지도 (진단조 file:line 확정)

### 근원 (1줄)
**emp_type를 어떤 곳은 raw SQL 문자열로, 어떤 곳은 EF enum으로 써서 단일 진실원이 없다.** 4개 쓰기 경로가 제각각 다른 문자열 INSERT.

### 깨지는 읽기 경로 (enum 변환 거침 = 'Regular' 외 값 오면 로그인 불가)
- `AuthService.cs:74-76` (login FindAsync) 🔴
- `AuthService.cs:182-184` (refresh) 🔴
- `AuthService.cs:318` (backfill) 🔴
- ↳ EF `EmployeeConfiguration.cs:36` `HasConversion<string>()`가 멤버명 'Regular'만 인식

### 안 깨지는 읽기 경로 (raw SELECT 문자열 그대로 = **절대 안 건드림**)
- `EmployeeService.cs:98` 외 목록/상세 raw SELECT ✅
- `EmployeePage.razor.cs:190` 화면 표시 ✅

### 쓰기 경로 4곳 (암세포 본체 — 전부 'Regular'로 통일)
| # | 위치 | 현재값 | → |
|---|---|---|---|
| 1 | `CompanyBootstrapController.cs:289` | `'fulltime'` | `'Regular'` |
| 2 | `UserService.cs:171` | `'full_time'` | `'Regular'` |
| 3 | `MdbMigrationService.cs:1298` | `'regular'` | `'Regular'` |
| 4 | `MdbMigrationService.cs:2862` | `'regular'` | `'Regular'` |

### 기본값 오염 (사용자 입력 기본값도 소문자 → 'Regular'로)
- `EmployeeDtos.cs:40,87,103` 기본값 `"regular"` → `"Regular"`
- `EmployeeModels.cs:78,125` 기본값 `"regular"` → `"Regular"`
- `EmployeePage.razor.cs:86` 기본값 `"regular"` → `"Regular"`

---

## 3. 절제 범위 (모두 봉합)

### 절제 A — 쓰기 4경로 통일 (코드)
2.쓰기경로표 4곳 전부 `'Regular'`로. raw 문자열을 enum 멤버명과 일치시켜 단일 진실원 확립.

### 절제 B — 기본값 6곳 통일 (코드)
DTO·Model·Razor 기본값 `"regular"` → `"Regular"`. 신규 직원 추가 시에도 정합값 들어가게.

### 절제 C — 운영DB 기존 오염 정리 (데이터, **헌법 #29 사장님 결재 필요**)
```sql
UPDATE employees SET emp_type='Regular'
WHERE emp_type IN ('fulltime','full_time','regular');
```
운영 hitpan_erp 11건 + 기존 마이그 직원. **운영DB 조작이라 사장님 별도 결재 후 실행.** 작업지시서엔 SQL 명시, 실행은 결재 게이트.

### 절제 D — DDL 값 제약 (재발 원천 차단, 검증 후 채택 판단)
clean DDL `emp_type longtext NOT NULL` → 값 검증 장치 검토. 단 ENUM 전환은 마이그·기존데이터 회귀 위험이 있어 **CTO가 ENUM/CHECK/유지 중 안전한 안 판정 후 결정**. 무리하면 안 함(사장님 "다른 거에 문제 생기지 않도록").

### 안 한다 (정상 조직 보호)
- **raw SELECT 읽기 경로(EmployeeService·화면 표시) 절대 안 건드림** — 회귀 0.
- emp_type 기반 권한 로직 없음(Role/account_type 기준) → 그쪽 무관.
- 코드 구조 변경 금지 — 값 통일·기본값 통일·데이터 정리만.

---

## 4. 검증 동시 진행 [7-V] + 완료 3관문

- **작업**: 백엔드 매니저(코드 A·B) + DB 매니저(C SQL 작성, 실행은 결재 후).
- **독립 검증**: 검증팀장 데이비드 박 —
  - 절제 A·B 후 emp_type write 경로 **재 grep 전수**(헌법 #12) — 'Regular' 외 값 남았나 0 확인.
  - 정식 'Regular' 시드 부모/자식계정 로그인 → **employee_id 채워짐** + 결재·HR 200 실측.
  - **회귀 반증**: raw SELECT 읽기(목록/상세)·마이그·화면이 안 깨졌나(사장님 "다른 거 문제 없게").
  - 절제 C SQL이 enum 정합값만 만드는지, 실행 전 영향 row count.
- **완료 3관문**: ① 빌드 errors 0 + warnings 0 ② ddl-smoke PASS ③ 검증팀 독립 반증("emp_type 깨짐 재현 실패=절제 완료" + "다른 기능 회귀 0").

---

## 5. 리스크 / 정상조직 보호
- **회귀 핵심**: enum 변환 거치는 건 AuthService 3곳뿐. 거기에 'Regular'만 가게 하면 로그인 안전. raw SELECT는 문자열이라 무영향(진단 확증).
- **절제 C 미실행 시**: 기존 demo 직원은 봉합 후에도 깨진 채 → 그래서 C를 결재 게이트로 묶되 **반드시 같이 처리해야 진짜 절제**(사장님 "모두 봉합"). C 결재 안 나면 신규만 정상·기존 미정상의 절반 상태임을 정직 보고.
- DDL ENUM 전환(D)은 마이그 회귀 위험 → CTO 안전 판정 전 안 함.

---

## 6. 완료 기준
- 쓰기 4 + 기본값 6 = **코드 10곳 'Regular' 통일**, write 재grep 0
- 운영DB 절제 C SQL 준비(사장님 결재 후 실행) — 결재 전이면 안건 상정
- 정식 'Regular' 부모/자식 로그인 → employee_id 채워짐 → 결재·HR 200
- 회귀 0(목록/상세/마이그/화면)
- 빌드 0/0 + ddl-smoke + 검증팀 반증 통과
- PM 종합 보고

---

## 7. CTO 재결재란 [4]
- [ ] 절제 범위 적정(A·B 충분한가, D ENUM 전환 안전한가):
- [ ] 회귀 분석(raw SELECT·마이그·권한 무영향 확증):
- [ ] 절제 C 운영DB 결재 분리 적정:
- [ ] 결재 의견(승인/조건부/반려):
- [ ] 사장님 보고 요약:
