# 🚨 PM 작지서 — 회계 정밀 재진단 진범 #4·#5·#6 봉합

> **작성**: 2026-05-26 23:30 PM 브라운킴
> **사장님 결재**: A·B·C 모두 결재 (5/26 23:20)
> **마감**: 진범 #4 P0 = 5/29 본런 D-3 게이트 정합

---

## 🔥 진범 #4: 경비처리 27,640행 렌더 폭탄 — P0

### 영역
- **파일**: `src/HitPan.Web/Pages/Finance/ExpensesPage.razor`
- **코드 진단**:
  ```csharp
  private List<ExpenseModel> _items = new();
  protected override async Task OnInitializedAsync()
  {
      _items = await FinSvc.GetExpensesAsync(); // 27,640건 일괄 fetch
  }
  ```
- **MudTable**: `Items="_items"` — 페이지네이션·가상 스크롤 부재 → DOM 27,640행

### 측정 결과
- bodyLen = **4,020,664 bytes (4MB)**
- rowCount = **27,640건**
- Blazor WASM 모바일·저사양 PC → 사용자 화면 다운 위험

### 헌법 정합 위반
- **#19** "errors 0 + warnings 0이 정합성·무결성" — 4MB 렌더 = 정합성 사고
- **#25** 3대 원칙 (쉽게·정확하게·안전하게) — "안전하게" 영역 위반
- 사장님 격언: *"고객들은 컴퓨터 전문가가 아니고 사용자들이야. 최대한 보수적으로!!!"*

### 봉합 가도 (3축)
1. **백엔드 매니저**: `FinSvc.GetExpensesAsync()` API 페이지네이션 추가
   - `GET /api/finance/expenses?page=1&size=50&from=&to=&status=` 시그니처
2. **프론트 매니저**: `MudTable ServerData` 패턴으로 전환
   - 헌법 #16 정합 (단일 쿼리·서버 페이징)
3. **ERP 매니저**: 경비 처리 워크플로우 점검
   - 기본 표시 = 미결재만 / 결재 완료는 별 탭

### 5중 검증
- 검증팀장 SAST + 데이터 최소주의(#22) 통과
- 1만건·5만건·10만건 데이터셋 부하 테스트

### 마감
- 5/27 (화) 18:00: 백엔드 API 페이징 완료
- 5/28 (수) 18:00: 프론트 ServerData 봉합
- 5/29 (목) 09:00: 검증팀장 5중 검증 + 모바일 부하 테스트
- 5/29 (목) 18:00: 본런 D-Day 정합 확인

---

## 🟡 진범 #5: 어음 관리 API abort — P2

### 영역
- **파일**: `src/HitPan.Web/Pages/Finance/BillsPage.razor`
- **현상**: `/api/finance/profit?year=2026` ABORTED 1건

### 진단
- BillsPage 코드 자체에는 `profit` API 호출 없음 (코드 grep 확인 완료)
- 다른 페이지(`ProfitPage`)의 in-flight 요청이 BillsPage로 navigate 시 abort된 가능성
- **실 사용자 환경에서는 사고 영향 0** (audit 부수효과)

### 봉합 영역
- W2 (5/27~5/31) 정밀 재현 후 P2 처리
- 사용자 실 환경 진단 우선 (사장님 직접 demo 어음 관리 진입 + F12)

---

## 🟡 진범 #6: 계정과목 Bank.List abort — P1

### 영역
- **파일**: `src/HitPan.Web/Pages/Finance/AccountsPage.razor`
- **현상**: `[Bank.List] Failed to fetch` 콘솔 에러 + `/api/finance/bank-transactions` ABORTED

### 진단
- AccountsPage 코드 자체에는 `bank-transactions` 호출 없음 (코드 grep 확인 완료)
- BankTransactionsPage의 in-flight 요청이 AccountsPage로 navigate 시 abort 가능성
- **실 사용자 환경 영향 의심**: 콘솔 에러가 명시적으로 출력됐다는 점 = 의도된 호출일 수 있음

### 봉합 영역
- W2 (5/27~5/31) 정밀 재현
- 백엔드 매니저: 계정과목 페이지에서 BillsCardsBankService 호출 영역 추적
- 데이터 = rows=100 정상 표시 = P1 (P0 아님)

---

## 📋 매니저 가도 일정

| 시각 | 매니저 | 영역 |
|---|---|---|
| 5/27 (화) 09:00 | 백엔드 + 프론트 + ERP 매니저 호출 | 작지서 4건 통합 전달 (CRUD 3건 + 진범 #4) |
| 5/27 (화) 18:00 | 백엔드 매니저 | 진범 #4 API 페이징 완료 |
| 5/28 (수) 18:00 | 프론트 매니저 | 진범 #4 ServerData + CRUD 3건 봉합 |
| 5/29 (목) 09:00 | 검증팀장 | 4건 통합 5중 검증 + 모바일 부하 |
| 5/29 (목) 18:00 | **본런 D-Day** | 정합 확인 |
| W2 (5/30~5/31) | 백엔드 매니저 | 진범 #5·#6 정밀 재현 + 봉합 |

---

## 💎 PM 47번째 자가 박제

### 회계 8건 1차 박제 정정
- 1차 audit: 8건 모두 undefined (title/rows) → "P0 진입 실패"
- 정밀 재진단: **5건 정상 + 3건 ISSUE**
- 원인: audit 스크립트의 페이지 간 navigate 시 in-flight ABORTED + measurements undefined
- 봉합: 페이지별 독립 진입 + 6초 대기 + 명시적 컨소올·apiFail 수집

### 사장님 격언 정합
- *"DB 쿼리 0회"* (5/26) — Playwright 화면 100% 정합
- *"최대한 보수적으로"* — 27,640행 렌더 = 보수성 위반

---

## 🎯 종합 P0 4건 (5/29 본런 D-3)

| 진범 | 영역 | 마감 |
|---|---|---|
| #1 상품 등록 다이얼로그 미열림 | Items.razor OpenCreate | 5/27 18:00 |
| #2 수금 진입 버튼 부재 (헌법 #20) | CollectionPage.razor | 5/28 18:00 |
| #3 세금계산서 발행 진입 부재 (헌법 #20) | TaxInvoicePage.razor | 5/28 18:00 |
| **#4 경비처리 27,640행 폭탄 (헌법 #19·#25)** | ExpensesPage.razor + API | **5/29 09:00** |

---

**상태**: 작지서 발행 완료. 5/27 09:00 매니저 3명 호출 → 4건 통합 가도
**다음 가도**: 사장님 결재 시 5/27 매니저 호출 가도 즉시 발진
