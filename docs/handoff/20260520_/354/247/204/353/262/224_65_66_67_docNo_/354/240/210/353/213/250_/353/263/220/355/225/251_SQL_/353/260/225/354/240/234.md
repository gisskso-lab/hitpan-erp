# 🚨 진범 #65·#66·#67 docNo varchar(20) 절단 봉합 SQL 박제

> **발견**: 2026-05-20 PM 브라운킴 (10차 마이그 사후 DB 실측 + 로그 추적)
> **결재**: 사장님 2026-05-20 (옵션 A 즉시 ALTER 승인)
> **실행**: 2026-05-20 PM (ALTER 3건 즉시 가도 완료)

---

## 🎯 결정적 증거

### 로그 (5/20 14:46:00.862)
```
sales_deliveries 진범 #21 진단: staging 1962행 / DISTINCT source_id=1962행 (중복=0건)
sales_deliveries INSERT IGNORE 결과: 1560행 / staging 1962행 (손실 402건)
sales_deliveries BulkCopy: 1962행 warnings=64 1258ms   ← truncation warning
```

### DB 실측
```sql
SELECT delivery_no, source_id FROM sales_deliveries WHERE source_type='migration' LIMIT 5;
-- DOCFB-20000403-1-000 / mig-docfb-20000403-1-2-184
-- DOCFB-20000406-1-000 / mig-docfb-20000406-1-1-184
```

### 원인
- **코드 생성 docNo**: `DOCFB-{dtStr}-{io}-{seq:D4}-{buyCode}` = 최대 24자
- **DB 컬럼**: `delivery_no varchar(20)`
- **결과**: 24자 → 20자 절단 → `-{buyCode}` 통째로 잘림 → uq_delivery_no 충돌 → INSERT IGNORE 손실

### 5/18 봉합 #21 실패 원인
- 코드는 `buyCode` 추가했으나 DB 스키마 ALTER 누락 → 봉합 효과 0

---

## 📊 손실 규모

| 테이블 | staging | INSERT | 손실 | 손실률 |
|---|---|---|---|---|
| sales_deliveries | 1,962 | 1,560 | **402** | 20.5% |
| purchase_receipts | 115,147 | 16,099 | **99,048** | **86%** ⚠️ |
| journal_entries | 23,170 | 23,152 | 18 | 0.08% (잠재 위험) |

---

## ✅ 봉합 SQL 3건 (2026-05-20 실행 완료)

```sql
-- 진범 #65 봉합
ALTER TABLE sales_deliveries MODIFY delivery_no varchar(32) NOT NULL;

-- 진범 #66 봉합
ALTER TABLE purchase_receipts MODIFY receipt_no varchar(32) NOT NULL;

-- 진범 #67 봉합 (전수 점검 중 추가 발견)
-- 23,152 entry 전체 길이=20 한계 도달 → 잠재 절단 위험
ALTER TABLE journal_entries MODIFY entry_no varchar(32) NOT NULL;
```

### 실행 검증
```sql
SELECT TABLE_NAME, COLUMN_NAME, COLUMN_TYPE
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_SCHEMA='hitpan_erp'
  AND ((TABLE_NAME='sales_deliveries' AND COLUMN_NAME='delivery_no')
    OR (TABLE_NAME='purchase_receipts' AND COLUMN_NAME='receipt_no')
    OR (TABLE_NAME='journal_entries' AND COLUMN_NAME='entry_no'));
-- 결과:
-- sales_deliveries  | delivery_no | varchar(32) ✅
-- purchase_receipts | receipt_no  | varchar(32) ✅
-- journal_entries   | entry_no    | varchar(32) ✅
```

---

## 🔍 다른 마이그 영역 전수 점검 결과

| 테이블 | 컬럼 | 길이 | 생성 패턴 | 위험도 |
|---|---|---|---|---|
| purchase_orders | po_no | varchar(20) | MDB IU_NO 원본 그대로 | 낮음 (코드 생성 X) |
| sales_orders | order_no | varchar(20) | MDB 원본 | 낮음 |
| quotations | quote_no | varchar(20) | MDB 원본 | 낮음 |
| tax_invoices | invoice_no | varchar(32) | 이미 충분 | 안전 ✅ |
| card_payments | card_no | varchar(20) | 원본 | 낮음 |
| bank_transactions | account_no | varchar(30) | 원본 | 낮음 |

→ **마이그 영역 docNo 길이 절단 위험은 본 봉합 3건으로 전수 해소** (5/20 14:50 기준)

---

## 🎯 11차 마이그 예상 효과

| 영역 | 10차 | 11차 예상 |
|---|---|---|
| sales_deliveries | 1,560 (DB) | **1,962** (+402, 100% 복원) |
| purchase_receipts | 16,099 (DB) | **115,147** (+99,048, 100% 복원) |
| journal_entries | 23,152 | **23,170** (+18) |
| 4축 자동 검증 점수 | 약 40~50점 | **95+점** 예상 |

---

## ⚠️ PM 자가 점검

1. ❌ **5/18 봉합 #21**: 코드만 변경, DB 스키마 ALTER 누락 → 봉합 효과 0
2. ❌ **5/20 08:30 박제**: UI counter만 보고 "봉합 성공" 박제 → 받아쓰기 사고 (헌법 #25 정확하게 위반)
3. ✅ **5/20 14:50 정직 박제**: DB 실측 + 로그 추적으로 진짜 원인 발견
4. ✅ **헌법 #13 정합**: 새 SQL 작성 전 DESCRIBE 의무 — 5/18 위반 → 5/20 ALTER 후 SHOW CREATE TABLE 검증
5. ✅ **헌법 #29 정합**: ALTER는 사장님 결재 후 실행 (PM 단독 실행 0)

---

## 🚀 다음 액션 (11차 마이그)

1. 사장님 RESET_TRUNCATE 실행
2. 11차 마이그 가동
3. PM이 BulkCopy warnings 0 확인 + DB 실측으로 100% 복원 검증
4. 4축 자동 검증 점수 95+ 측정
5. 5/29 본런 시나리오 B 가도 확정

---

**작성**: 2026-05-20 PM 브라운킴
**상태**: ALTER 3건 즉시 가도 완료 + 11차 마이그 대기
