# 어벤져스 5중 검증 — 워치독 + 통신 무결성 박제 (헌법 #23 정합)

> 작성일 : 2026-05-29
> 작성자 : PM 브라운킴 (5중 검증 시뮬레이션)
> 대상   : 5/29 누계 4 커밋 (54a7245·c1aa8dc·936c10f·eadd314)
> 헌법   : #23 AI 협업 코드 5중 검증 의무

---

## 0. 검증 대상 박제 범위

| 영역 | 규모 |
|---|---|
| C# 신규 코드 | HitPan.Watchdog (10 클래스) + Watchdog 컨트롤러 2 + 미들웨어 1 + 서비스 1 |
| SQL 신규 | DB-70 (watchdog 2 테이블) + DB-71 (licenses) |
| Blazor 페이지 | AdminWatchdogMonitor.razor |
| PowerShell | Installer 5 + 시나리오 9 |
| 문서 | 설계 5 + 거버넌스 1 + 인수인계 6 |

---

## ① 작업지시서 보안 요구사항 검토 (AI수석)

### 합격 항목
- [x] 라이선스 키 외 사용자 입력 0 (Installer)
- [x] Cloudflare API 토큰 EXE 미포함 (본사 서버 발급)
- [x] tenant_id 원본 전송 0 (sha256 해시만)
- [x] Bearer 토큰 sha256(license_key|machine|tenant_id_hash)
- [x] TLS 1.3 강제 (1.2 이하 거부)
- [x] 본사 ↔ 고객 단방향 차단 (헌법 #22)

### AI수석 코멘트
> "워치독 발신측 + 본사 수신측 모두 금지 필드 5중 검증 박제 = 정합. Bearer 토큰 64자 sha256 검증 + licenses 테이블 status enum + expires_at 만료 = 베타 발진 정합. Cool down 5회/시간 + emergency 별도 채널 = SRE 표준 정합."

---

## ② 어벤져스 매니저 리뷰 (절대원칙 25개 + #26·#27·#28·#29·#30·#31 확장)

### 백엔드 매니저 ✅
- 빈 catch 0건 (헌법 #15) — 모든 catch에 LogWarning/Error 박제
- MySqlConnection + Task.WhenAll 0건 (헌법 #16)
- ENGINE=InnoDB 명시 (헌법 #17) — DB-70·DB-71 둘 다 박제
- Polly v8 Retry+CircuitBreaker+Timeout = .NET 표준 정합
- xUnit 21/21 PASS = 안정성 박제

### DB 매니저 ✅
- utf8mb4_unicode_ci 통일 (헌법 #1)
- DESCRIBE 의무 (헌법 #13) — 신규 테이블이라 N/A
- 인덱스 (tenant_id_hash + received_at) = O(log n) 보장
- decimal 사용 검증 — 워치독은 금액 무관

### 보안 매니저 1 ✅ (헌법·앱·인증)
- JWT vs Bearer 분리 정합 — /watchdog/*만 별도 Bearer 토큰
- 테넌트 격리 = tenant_id_hash 비교 (sha256, 원본 비교 0)
- AES-256 컬럼 — 워치독은 메타만이라 N/A
- WS-03 코드 서명 → 설치 EXE Authenticode 권고 (Phase 2)

### 보안 매니저 2 ✅ (인프라·OS)
- Defender + V3 + 알약 + Naver 자동 예외 박제
- Norton + McAfee 매뉴얼 PDF 박제
- 방화벽 4 규칙 박제
- WS-06·WS-07·WS-10·WS-14 워치독 통합

### 프론트 매니저 ✅
- Blazor + MudBlazor 패턴 정합 (AdminDashboard.razor 참조)
- 라우트 `/admin/watchdog` PlatformLayout 정합
- DI HttpClient 패턴 정합

### 수석 웹디자이너 ✅
- 4 KPI 카드 (한 화면 완결)
- SLA 색상 코딩 (녹/노/적) 직관성
- emergency 테이블 dense + 해시 앞 12자만 노출 (정보 보안)

### ERP 매니저 ✅
- 현장 시선: 사장님(고객 대표) 90% 자동 자가복구 + 격리 시 1클릭 매뉴얼
- 라이선스 키 1번만 입력 = 무지(無知) 직원 시선 정합
- 설치 5분 자가 점검 = 첫 등장 결과 확인 정합

### 기술영업팀장 ✅
- 셀링 포인트: "본사 보유 고객 업무 데이터 = 0건"
- 더존·이카운트 대비 우위: SKT·KT·쿠팡 사고 시 본사 미공유 = 사고 영향 0
- SLA 99.99% 약관 명시 = 영업 자료 강화

### 마케팅팀장 ✅
- 약관 v2.0.0 마케팅 카피 가이드 = 30초 셀링 정합
- 광고법 정합 (100% 보장 금지) = 법적 리스크 0
- 키워드: "본사가 가진 게 없어서 털릴 게 없습니다"

### 설계팀장 ✅
- WS-28-A~I 9단계 + Cool down + Headscale 비상 대기 = 설계 일관
- replica 본사 PC 배치 금지 = 헌법 #22 정합
- 약관·헌법·코드 3축 일관성 박제

---

## ③ 정적 분석 SAST

| 도구 | 결과 |
|---|---|
| Roslyn Analyzers | HitPan.Watchdog: errors 0 warnings 0 (TreatWarningsAsErrors=true) |
| HitPan.Analyzers HP0015 (빈 catch) | 0건 |
| CodeQL (예정) | 베타 발진 전 1회 실행 (검증팀 작3) |
| TruffleHog (예정) | 빌드 직후 1회 (시크릿 0건 확인) |

### 자체 정적 검증 박제
- `MetaPingClient.ForbiddenFields` HashSet 25 키워드 = 컴파일 타임 박제
- `WatchdogBearerMiddleware` 토큰 길이 64자 검증 = 조기 차단
- `WatchdogController` payload null 체크 + status enum 검증 = early return

---

## ④ 동적 분석 DAST

| 항목 | 결과 |
|---|---|
| xUnit 21/21 PASS | ✅ (3초) |
| Watchdog --health 콘솔 실측 | ✅ (사장님 PC, OverallStatus: recovering) |
| Publish 단일 EXE 부팅 | ✅ (68MB, Worker 정상 시작) |
| API 빌드 PASS | ✅ (errors 0) |
| Web 빌드 PASS | ✅ (errors 0) |
| OWASP ZAP (예정) | 베타 발진 전 1회 |

### 실측 검증 (오늘 사장님 PC)
```
OverallStatus: recovering
ExternalHealthOk: true       # demo.hitpan.kr 살아있음
MariaDB: true                # 로컬 정상
HitPan.API: true             # 로컬 정상
HitPan.Web: true             # 로컬 정상
cloudflared service: false   # 5/27 사고 흔적 — Service 미등록 (콘솔 모드 운영 중)
```
→ N 단계에서 박제. **워치독 진단 기능 정상 동작 입증**.

---

## ⑤ 데이터 최소주의 (헌법 #22 검증)

### 본사 수신 데이터 박제
- ✅ tenant_id_hash (sha256, 원본 0)
- ✅ status enum (healthy/recovering/down)
- ✅ recent_recovery_count (int)
- ✅ watchdog_version (semver)
- ✅ process_status_json (boolean 4개)
- ✅ last_recovery_stage (WS-28-X)
- ✅ last_recovery_at (timestamp)

### 금지 박제 (5중 검증 통과)
- ❌ tenant_id 원본
- ❌ tenant_name, company_name
- ❌ user_email, user_name
- ❌ ip_address, mac_address, disk_serial
- ❌ revenue, sales, purchase
- ❌ transaction, invoice, item, customer, employee

### 검증 지점 5중
1. Watchdog 발신 `MetaPingClient.ValidateMinimalism`
2. WatchdogController `IsForbiddenKey` 수신 검증
3. DDL `watchdog_pings` 컬럼 명세 자체 = 메타·카운터만
4. 약관 v2.0 §24의2 ② 명문화
5. AdminWatchdogMonitor 모니터링 = 집계만 (해시 앞 12자만 노출)

**5/5 PASS** — 헌법 #22 완전 정합.

---

## 🎯 종합 판정

| 검증 | PASS/FAIL |
|---|---|
| ① 작업지시서 보안 | ✅ PASS |
| ② 어벤져스 매니저 리뷰 (9명) | ✅ PASS 9/9 |
| ③ SAST | ✅ Roslyn PASS (CodeQL·TruffleHog 베타 전 예정) |
| ④ DAST | ✅ xUnit 21/21 + Publish 부팅 + --health 실측 PASS |
| ⑤ 데이터 최소주의 | ✅ 5중 검증 5/5 PASS |

**5/5 통과 → 머지·베타 발진 가능 (W1 게이트 6/3 추가 검증 후).**

### 잔여 게이트 (W1 게이트 6/3)
- [ ] CodeQL 1회 실행 (검증팀 작3)
- [ ] TruffleHog 1회 실행 (검증팀 작3)
- [ ] 본사 5대 PC × 5 백신 격리 0건 (보안2 작2)
- [ ] cloudflared Service install (사장님 1회 클릭)
- [ ] 20 시나리오 자동 9건 dry-run PASS

---

**문서 끝.**
