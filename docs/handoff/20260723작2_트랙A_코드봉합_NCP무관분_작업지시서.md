# 작업지시서 20260723작2 — 트랙A 자동배포 활성화 코드봉합 (NCP 무관분만)

> 발행: PM 브라운킴 / 2026-07-23 새벽 / 브랜치 develop
> 성격: 자동배포 활성화 사전 코드봉합. **NCP 조작·사장님 결정 필요분은 범위 밖(별도 결재)**.
> 사장님 지시(2026-07-23): "작지서 루틴대로 코드봉합. NCP 건드리는건 사장 결재 승인 후."
> SOP: [3]작지서(본문) → [4]CTO결재 → [5]사장님승인 → [6]코드 → [7-V]검증팀 SoD.

---

## §0. 범위 (엄격히 — 벗어나면 반려)

### IN (지금 작지서로 코드봉합 — NCP 무관·사장님 결정 무관)
| # | 항목 | 근거 | 코드량 |
|---|---|---|---|
| I-1 | **버전 1.2.39 → 1.2.40** (Directory.Build.props:33) | A-1. 실측하려면 새 버전 필수(같은버전 감지0) | 1줄 |
| I-2 | **deploy-update.yml 구멍② 봉합** — environment choice 에서 beta 선택 시 즉시 실패 가드(또는 production-only 잠금) | 검사2 구멍②: beta는 sudoers 2인자에 거부돼 혼란경로 | 가드 스텝 |
| I-3 | **deploy-update.yml 구멍③ 봉합** — 로드맵 규율 주석 명문화(dispatch 커밋이 build-and-test 초록인지) | 검사2 구멍③: warnings 커밋 자동게시 방지(#19) | 주석 |
| I-4 | **.github/CODEOWNERS 신설** — deploy-update.yml·publish-update.sh·sign-manifest.sh·서명키 파일 보호 | G-E, 검사3: 현재 0건 | 신규 파일 |

### OUT (범위 밖 — 별도 결재·별도 작지서)
| 항목 | 왜 밖 |
|---|---|
| **G-A·B·C (상주배치·sudoers·webroot정정)** | 🔴 NCP sudo = 사장님 결재·승인 후(헌법 #29). 지금 절대 안 건드림 |
| **.disabled 제거(활성화)** | NCP 게이트(G-A·B·C) 통과 후에만. 지금 하면 첫실측 100%실패(구멍①) |
| **G-E reviewer 등록** | GitHub 웹 설정 = 사장님 클릭 |
| **트랙B (licenses 발급경로·clean DDL 편입)** | 🔴 licenses INSERT 시점 = 사장님 결정 선결(받아쓰기 금지). CF-1·2 별도 작지서 |
| **트랙C (MetaPing)** | 사장님 아키텍처 결정3 후 |

---

## §1. I-1 — 버전 1.2.40

- **파일**: `Directory.Build.props:33` `<HitPanVersion>1.2.39</HitPanVersion>` → `1.2.40`
- **주의**: 이 1줄이 SSOT. FileVersion·manifest·설치EXE 전부 여기서 파생(W4-0). 하드코딩 회귀 금지.
- **DoD**: `git grep 1.2.39` 로 잔재 하드코딩 없나 확인(있으면 그것도 봉합 대상인지 판정 — 단 .iss 폴백 등은 릴리스시 오버라이드라 무관).

## §2. I-2 — 구멍② beta 가드

- **파일**: `.github/workflows/deploy-update.yml.disabled`
- **무엇**: environment input 이 `beta` 일 때, deploy 잡이 sudoers 2인자에 걸려 실패하기 **전에** 명확한 에러로 중단. 이유 메시지: "beta 는 nginx /beta/ location·sudoers beta 라인 별건 결재 전 실동작 불가(PRD §3.3)".
- **방식(택1, CTO 판정)**: (a) environment choice 를 production 만 남김 vs (b) beta 선택 시 deploy 잡 첫 스텝에서 `exit 1` + 안내. **(b) 권장** — beta 배선(§3.3 인자분기)은 미래 위해 남기되 실행만 막음.
- **주의**: 이건 `.disabled` 파일 수정이라 활성화와 무관(여전히 안 켜짐).

## §3. I-3 — 구멍③ 품질게이트 규율

- **파일**: `.github/workflows/deploy-update.yml.disabled` 주석 + PRD
- **무엇**: workflow_dispatch 로 임의 커밋 게시 가능하므로, "dispatch 전 해당 커밋이 build-and-test.yml 초록(빌드0/0+ddl-smoke+migsmoke) 통과했는지 운영자 확인" 규율을 주석으로 명문화. (자동 강제는 별건 — 지금은 규율 명시)

## §4. I-4 — CODEOWNERS 신설

- **파일**: `.github/CODEOWNERS` (신규)
- **무엇**: 다음 경로 변경 시 지정 리뷰어(사장님/PM) 필수 승인:
  - `.github/workflows/deploy-update.yml*`
  - `installer/updates/publish-update.sh` · `sign-manifest.sh` · `build-manifest.ps1`
  - `.github/workflows/deploy-ncp.yml`
- **근거**: 검사3 — 없으면 누구든 PR로 publish-update.sh 고쳐 게이트 우회. 서명·배포 파일 보호.
- **주의**: GitHub owner 계정(gisskso) 기준 리뷰어 지정. reviewer 실효는 branch protection 필요(별건).

---

## §5. 보안 불변식 (구현 중 위반 금지 — self-review)
1. NCP 절대 안 건드림 (G-A·B·C·활성화 전부 OUT)
2. .disabled 유지 (활성화 안 함 — 오배포 물리차단)
3. 개인키·서명 규격 무변경
4. clean DDL·licenses 안 건드림 (트랙B OUT)
5. 버전 SSOT 단조증가(1.2.39→1.2.40)

## §6. DoD (완료 정의)
- I-1: Directory.Build.props 1.2.40. 빌드 errors0+warnings0(헌법 #19).
- I-2: `.disabled` 에 beta 가드 존재. 여전히 `.disabled`(활성화 안 됨).
- I-3: 규율 주석 존재.
- I-4: CODEOWNERS 파일 존재, 대상 경로 5개 커버.
- 검증팀 SoD: NCP 무접촉·트랙B 무접촉·활성화 안 됨 확인. 회귀 0.

## §7. 결재란
- CTO 결재: ______________________ (OK/부분수정후승인/반려/보류)
- 사장님 결재: ______________________
