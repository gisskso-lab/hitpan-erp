# 20260922작2 — 메인PC 물리식별 「왕복 증명」 작업지시서

> **근거**
> · [1-V] [선행검증서](../검증/선행/20260922_선행검증서_메인PC물리식별_기기슬롯재설계.md)
> · [2] [설계문서](../설계/erp/20260922_설계_메인PC물리식별_왕복증명.md) — **결재 완료 D-1~D-7**
> **작성**: 설계팀장 존 · **결재**: PM 브라운킴 → 사장님
> **범위**: 🔴 **메인PC 물리식별만.** 계정 과금·계정관리·기기등록 절차 제거는 **이 지시서에 없다**

---

## 0. 이 작업이 끝나면 무엇이 되는가

| 지금 | 끝나면 |
|---|---|
| 도메인으로 열면 메인PC 인식 **100% 실패** | 🟢 도메인으로 열어도 **자동 인식** |
| 자료관리(백업·초기화·자료이관) **403** | 🟢 메인PC 에서 **열린다** |
| 외부 PC 가 메인PC 를 **자칭하면 통과** | 🟢 **자칭 불가** — 두드릴 히트판이 없다 |
| *"로컬호스트로 접속하세요"* 안내 필요 | 🟢 **안내 자체가 사라진다** |
| 브라우저 바꾸면 깨짐 | 🟢 **안 깨진다** (저장이 아니라 매번 증명) |

---

## 1. 절 나누기 — 순서를 지킨다

```
절A  P1  서버 — MainPcProofService + API 3개          (화면 무변경 · 무영향)
절B  P2  CSP 한 줄 + 브라우저 JS                      (왕복이 실제로 도는가)
절C  P3  [메인PC 등록] 팝업                            (최초 1회 · 키 생성 + 봉인 + 등록)
절D  P4  MainPcOnly 두 곳에 갈래 추가                  (자료관리가 열린다)
절E  P5  DeviceAuthGate 자칭 경로 차단                 (결함 봉합)
```

🔴 **절A → 절B 는 순서를 못 바꾼다.** 절B 가 절A 의 API 를 부른다.
🔴 **절C 는 절A·절B 가 돌아야 성립한다** — 팝업이 누르는 것이 곧 왕복의 첫 바퀴다.
🔴 **절D 를 절C 보다 먼저 하면 안 된다** — 등록되지 않은 상태에서 자료관리 판정만 바뀌면
   메인PC 도 클라이언트도 아닌 상태가 생긴다.

---

## 2. 절A (P1) — 서버

### 만든다

**`src/HitPan.Infrastructure/Services/MainPcProofService.cs`** (신규)

| 기능 | 내용 |
|---|---|
| 표 발급 | 1회용 랜덤값 N · **60초** 유효 · **세션(JWT `jti`)에 묶는다** · 메모리 보관 |
| 증표 생성 | `HMAC(봉인해제한 키, N + tenantId + 만료시각)` |
| 증표 검증 | 자기 키로 재계산해 대조 + N 미사용·미만료·세션 일치 |
| 키 보관 | [`ITpmKeyService`](../../src/HitPan.Application/Services/Security/TpmKeyService.cs) 의 `SealKey`/`UnsealKey` 를 쓴다 — **새로 만들지 않는다** |

⚠️ 표는 **메모리에 둔다**(DB 아님). 서버와 로컬이 같은 프로세스다.
   API 재시작으로 표가 사라져도 **다시 왕복하면 그만**이다.
⚠️ 표는 **쓰는 즉시 버린다**(1회용). 만료된 표도 주기적으로 버린다.

**`src/HitPan.API/Controllers/DeviceController.cs`** 에 3개 추가 (기존 코드 무변경 · 추가만)

| 엔드포인트 | 인증 | 하는 일 |
|---|---|---|
| `POST /api/devices/mainpc-challenge` | 로그인 필요 | 표 N 발급 |
| `POST /api/devices/mainpc-proof` | 로그인 필요 | 🔴 **「CF헤더 없음 + 루프백」일 때만** 증표를 준다. 아니면 거부 |
| `POST /api/devices/mainpc-verify` | 로그인 필요 | 증표 검증 → 통과 시 세션에 메인PC 표식 |

🔴 **`mainpc-proof` 의 로컬 판정은 `MainPcOnlyAttribute.IsMainPc` 와 같은 규칙을 쓴다.**
   규칙을 복붙하지 마라 — 한쪽만 고쳐지는 날이 온다.

### 하지 마라

- 🚫 요청 **본문**에서 `isMainPc` 같은 값을 읽지 마라. 읽는 순간 자칭이 가능해진다
- 🚫 `hardware_id`·`fingerprint` 를 건드리지 마라 — 이 작업은 그 칸을 **안 쓴다**
- 🚫 슬롯 계수 SQL 무접촉

### 완료 조건

- 빌드 **errors 0 · warnings 0** (#19)
- 화면에서 보이는 변화 **0** — 여기까지는 아무도 이 API 를 부르지 않는다

---

## 3. 절B (P2) — CSP + 브라우저

### 고친다

**`src/HitPan.API/Program.cs:548`** — `connect-src` 에 로컬 주소 **하나만** 보탠다

```
(현행) connect-src 'self' https: wss:;
(변경) connect-src 'self' https: wss: http://127.0.0.1:5257;
```

🔴 **`http:` 전체를 열지 마라.** 주소 하나만이다.
🔴 `CspFrameSrcGateTests` 가 깨지지 않는지 확인한다 (G-10).

### 만든다

**`src/HitPan.Web/wwwroot/js/hitpan-mainpc-proof.js`** (신규)
- `fetch('http://127.0.0.1:5257/api/devices/mainpc-proof', …)` 한 번
- 실패는 **조용히 삼킨다** — 클라이언트 PC 에서는 실패가 정상이다(콘솔 에러도 남기지 않는다)
- **한 번 재시도** 후 포기 (D-5)

### 실측 (여기서 처음 실제로 잰다)

🚨 **반드시 실제 브라우저 창으로 잰다. 헤드리스는 결과가 정반대로 나온다** ([1-V] §4-3).

| # | 절차 | 기대 |
|---|---|---|
| B-1 | 메인PC 에서 도메인 접속 → 왕복 3회 | 🟢 증표 받고 검증 통과 |
| B-2 | 클라이언트 PC 에서 도메인 접속 → 왕복 | 🔴 (2)에서 연결 실패 · **조용히 끝난다** |
| B-3 | Edge → Chrome 바꿔서 B-1 반복 | 🟢 **그대로 통과** (G-8) |

---

## 4. 절C (P3) — [메인PC 등록] 팝업

> 사장님: *"최초 로그인후 메인PC등록을 먼저 해"* · *"다음 접속부터는 팝업이 안보이고 바로 접속"*

### 동작 (설계 §3-4)

```
로그인 직후 — 화면 뒤에서 조용히 확인
  ├ 이미 메인PC 등록됨(is_main_pc=1 존재)  → 팝업 없음. 바로 들어간다
  ├ 미등록 + 왕복 성공                      → 🔵 [메인PC 등록] 팝업 1회
  └ 미등록 + 왕복 실패                      → 팝업 없음. 바로 들어간다
```

🔴 **클라이언트 PC 에서는 팝업이 절대 안 뜬다** (사장님 재확인). 누르고 실패하는 경험조차 만들지 않는다.
🔴 팝업 대상은 **부모계정만** (D-8 기본값 · 사장님 한 줄이면 바꾼다).

### [등록] 을 누르면

```
① MAINPC_PROOF_KEY 생성 (랜덤)
② TpmKeyService.SealKey() 로 봉인 → 저장          ← 🔴 그 PC 에서만 풀린다
③ tenant_devices 에 메인PC 등록 (is_main_pc=1)
④ 팝업 닫고 그대로 진행
```

⚠️ **이미 `is_main_pc=1` 줄이 있으면 새 줄을 만들지 않는다.** 그 줄을 쓴다
   (9/13 사장님 실측: `is_main_pc=1` 이 2줄이 된 사고 계통).
⚠️ 키가 이미 있으면 **덮어쓰지 않는다** (#1).

### 문구 — 고객이 읽는 말이다

- 🚫 "인증키", "왕복", "루프백", "HMAC", "토큰" 같은 개발 용어 금지
- 🟢 *"이 컴퓨터를 회사 자료가 들어 있는 컴퓨터로 등록합니다"* 같은 말로
- ⚠️ 문구 최종안은 **사장님 확인**을 받는다

---

## 5. 절D (P4) — 자료관리를 연다

### 고친다 — **두 곳 다**

| 파일 | 변경 |
|---|---|
| `src/HitPan.API/Security/MainPcOnlyAttribute.cs:38` | `기존 판정 OR 왕복증명 통과` |
| `src/HitPan.API/Controllers/DeviceController.cs:101` `IsMainPc` | 같은 규칙 |

🔴 **기존 줄을 지우지 마라**(#1). `localhost` 직접 접속은 **여전히 통과해야 한다**(G-9).
🔴 **한 곳만 고치면 "화면은 열리는데 저장이 안 된다"** — 기존 주석이 이미 경고하고 있다.
⚠️ 인터페이스·판정 규칙을 바꾸면 **부르는 곳 전부 grep** 한다 (#12).

---

## 6. 절E (P5) — 자칭 경로를 끈다

**`src/HitPan.Web/Components/Common/DeviceAuthGate.razor:618`** `JoinServerRowAsync`

🔴 **끈다. 지우지 않는다** (#1·#37).
- 호출부(버튼)를 막는다
- 함수 본문은 남기고, 진입 즉시 막고 사유를 주석으로 남긴다
- 주석에 **왜 껐는지**와 **무엇이 대신하는지**를 적는다

⚠️ 이 경로가 막히면 메인PC 에서 도메인 접속 시 **절C·절D 가 대신 열어 준다.**
   **절D 가 먼저 완료돼 있어야 한다** — 순서를 어기면 대표가 자기 PC 에서 갇힌다.

---

## 7. 게이트 — [4] 검증팀장이 잰다

설계 §8 의 **G-1 ~ G-13** 전부.

🚨 **G-3 (봉합을 빼면 FAIL 하는가)이 없으면 게이트가 아니다.** 누적 25회 지적된 자리다.
🚨 **DB 없는 환경의 PASS 는 SKIP 이다.** 초록을 통과로 읽지 마라.
🚨 **실측은 실제 브라우저 창.** 헤드리스 금지.

---

## 8. 🚫 이 작업에서 손대지 않는 것

| 대상 | 근거 |
|---|---|
| 워치독·업데이트·Major 팝업 흐름 | #43 — **불변** |
| `fingerprint` 컬럼 · `uq_tenant_fp` | #37 — 옛 기기 전부가 이 값으로만 찾힌다 |
| `hardware_id` 컬럼 | 이 작업은 이 칸을 **안 쓴다** |
| 슬롯 계수 SQL · 과금 | 별도 작업지시서 |
| `appsettings.json` · `appsettings.Development.json` | #21 |
| 설치본(`.iss`) · 설치 스크립트 | 🟢 **이번엔 안 건드린다** — 키를 등록 시점에 만들기 때문(D-4) |
| 운영 DB 직접 수술 | #39 — 테스트 환경에서만 |

---

## 9. 완료 정의

- [ ] 빌드 **errors 0 · warnings 0** (#19)
- [ ] G-1 ~ G-13 전부 PASS (**G-3 대조군 포함**)
- [ ] 사장님 PC 에서 **도메인 접속으로** 자료관리가 열리는 것 실물 확인
- [ ] 클라이언트에서 자료관리가 **안 열리는** 것 확인
- [ ] 개발명세서 → `docs/개발/`
- [ ] [4] 작업리뷰서 → `docs/검증/`
- [ ] [5] 결재 → [6] 커밋

---

## 10. ⚠️ 착수 전 한 줄이면 되는 것

| # | 질문 | 기본값 |
|---|---|---|
| D-8 | 팝업을 부모계정에게만 띄우나 | 🟡 **부모계정만** 으로 진행 |
| — | 팝업 문구 | 🟡 초안을 만들어 **사장님 확인** 받고 확정 |
