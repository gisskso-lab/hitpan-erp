# 20260617 설치마법사 1.2.13 봉합 인수인계서

> 작성: PM 브라운킴 / 2026-06-17 새벽 02:30
> 12시간 누적 봉합 가도. 사장님 퇴근. **로그인 API 500 한 사고만 잔존**.

---

## 1. 다음 PM 즉시 가도 (내일 아침 09:00)

**잔여 사고 1건만**: `POST /api/auth/login → 500`

**진단 가도 절차**:
1. 사장님 Sandbox(또는 일반 PC) 가동
2. PowerShell 1줄:
   ```powershell
   Get-Content 'C:\Program Files\HitPan ERP\logs\hitpan-api-*.log' -Tail 30
   ```
3. 결과 채팅에 박음
4. PM 진범 추적 → 1.2.14 즉시 봉합

**가능 진범 3종 (사전 추적 후보)**:
- (A) JWT_SECRET이 db.conf or hitpan-keys.conf에 안 박힘 → TenantConfigReader throw
- (B) DB 연결 실패 (root 비번 사고·Collation 사고)
- (C) AccessTokenValidator·EncryptionService Singleton 가도 시점 사고

---

## 2. 최종 산출물

| 항목 | 값 |
|---|---|
| 최신 EXE | **HitPan-ERP-Setup-1.2.13.exe** (279 MB) |
| URL | https://updates.hitpan.kr/packages/HitPan-ERP-Setup-1.2.13.exe |
| MD5 | `4bcfc38a1656eb6cae8abb5bbbae7ec6` |
| 최신 커밋 | b9bb7f2 (develop) |
| 빌드 | errors 0 / warnings 0 |

**NCP 디스크 ⚠️**: 잔여 964KB. **다음 빌드 전 1.2.12 삭제 필수**:
```bash
ssh root@211.188.58.140 "rm -f /var/www/updates/packages/HitPan-ERP-Setup-1.2.12.exe"
```

---

## 3. 12시간 누적 봉합 흐름 (1.2.6 → 1.2.13)

| 버전 | 사고 | 봉합 | 검증 |
|---|---|---|---|
| 1.2.6 | 통신 무결성 누락 | TenantConfigReader 도입 | — |
| 1.2.7 | `{app}` NRE | B안 G_TenantInstallDir 지연 | ✅ |
| 1.2.8 | CF Tunnel 1030 | tunnel_secret 폐기 | ✅ |
| 1.2.9 | TenantCode 하이픈 SQL | Pos+Delete 루프 | ✅ |
| 1.2.10 | 약한 RNG·CF 1013·DNS 미정합 | Pascal Random + 1013 코드 + DNS PATCH | ✅ |
| 1.2.11 | **HitPan.API 503** (DB_NAME) | 4파일 TenantConfigReader (부족) | ❌ 봉합 누락 4건 |
| **1.2.12** | **8건 (P0 5 + P1 5)** | **안 B 최적화 묶음 봉합** | ✅ 3축 PASS |
| **1.2.13** | **Blazor CORS + CSP** | appsettings 정정 + Program.cs CSP | ✅ 5축 매니저 |
| 1.2.14 | **로그인 500** (예상) | **내일** | 🔜 |

---

## 4. 1.2.12 봉합 영역 (P0 5건 + P1 5건)

**P0**:
- **A**: 환경변수 폐기 8파일 통일 (AuthService·BackofficeAuthService·EncryptionService·TenantService·AppDbContextFactory·MigrationDbConnectionFactory·WebhookInbound·CompanyBootstrap) — 503 차단
- **B**: cloudflared Ingress PUT 라우팅 — 1033 차단
- **C**: 메일 본문 버전 정합 — 흐름 끊김 차단
- **D**: signup↔tenant 시간 근접 매칭 — 시리얼 평문 교차 노출 차단
- **#1 적대적**: AES 키 hex 64자 → ParseKey 32byte 불일치 → 500 차단 (`EncryptionService.cs` 신규 hex 디코드 경로)

**P1 (installer)**:
- CSPRNG `TrySecureRng` + Random 폴백
- API 바인딩 `0.0.0.0` → `127.0.0.1`
- 헬스체크 `401` PASS 완화
- db-setup.bat `icacls` + 공백 overwrite + DeleteFile
- schtasks `/Run` ResultCode 검사 + 1회 재시도

## 5. 1.2.13 봉합 영역 (P0 2건)

**P0 #1: Blazor WASM CORS 사고**
- 사고: `test000.hitpan.kr` 가도 시 Blazor가 `api-demo.hitpan.kr`로 로그인 가도 → CORS preflight 차단
- 진범: `installer/api/wwwroot/appsettings.json`의 `ApiBaseUrl`이 `api-demo` 박힌 채 모든 EXE 박힘
- 봉합: `installer/HitPan-Universal.iss` 신규 `FixupBlazorAppSettings()` (iss:680~712) + `CurStepChanged` 호출 (iss:858)
- LOCAL 모드(`localhost:5234`) 가드 박힘 — Production만 정정

**P0 #2: CSP 차단 3종**
- 사고: pretendard 폰트(jsdelivr) / 다음 우편번호(daumcdn) / CF Insights 모두 CSP 차단
- 봉합: `src/HitPan.API/Program.cs:319` CSP 헤더 정정
  - `style-src` + `style-src-elem`: `https://cdn.jsdelivr.net` 추가
  - `script-src` + `script-src-elem`: `https://t1.daumcdn.net` + `https://static.cloudflareinsights.com`
  - `connect-src`: `wss:` 추가
  - `font-src`: `data:` 추가

---

## 6. 5축 매니저 + 적대적 재검증 결과 (1.2.12·1.2.13)

| 검증 | 결과 | 진범 |
|---|---|---|
| 백엔드 매니저 | P0 4건 / P1 4건 | 환경변수 폐기 8파일 + RNG + msiexec + db-setup |
| 풀스택 매니저 | P0 2건 / P1 2건 | 메일 본문 + signup↔tenant + license_plain + tunnel_secret |
| 네트워크 매니저 | P0 3건 | Ingress PUT 누락 + schtasks ResultCode + 0.0.0.0 노출 |
| 설계팀장 | P0 1건 추가 | BackofficeAuthService 5곳 누락 |
| 검증팀장 | 5/5 PASS | 5축 봉합 실측 통과 |
| CTO | 조건부 GO | P1 잔존 1.2.13 권고 |
| **적대적 재검증** | **P0 #1 발견** | **AES 키 hex 불일치 → 1.2.12 봉합 잔존 사고 색출** |
| 1.2.13 5축 | GO | 5축 매니저 병렬 |
| 보안 매니저 | 1.2.14 SRI 권고 | jsdelivr 한시 허용 |

---

## 7. 헌법 정합 누적

- #1·#15·#19·#20·#21·#22·#23·#25·#27·#28·#29·#30·#31·#33·#34·#35 모두 정합 가도
- 1.2.6 환경변수 폐기 결재 → 1.2.12에서 100% 정합 완성 (헌법 #33 완전 봉합)
- 헌법 #21 (`appsettings.json` 삭제 금지) 정합 — 1.2.13은 정정 후 정상 부트 가능 영역 유지

---

## 8. PM 12시간 자가 진단

**진전**: 503 / 1033 / 500 (AES) / CORS / CSP 5종 사고 봉합. 로그인 500 1건만 잔존.

**사고 패턴**:
- 1.2.11에서 봉합 4파일만 진행 → 부족. 1.2.12에서 8파일+로 확장
- 적대적 재검증이 진짜 사고(AES 키) 발견 → 1차 검증이 PASS 박는 사고
- SCP 잘림(NCP 디스크 100%) → md5sum 검증 의무 박힘

**다음 PM 권고**:
- 로그 수집 즉시 (PowerShell 1줄)
- 사장님 추측 0건. 로그 1줄이 진범 확정 가도
- 1.2.14 빌드 전 NCP 디스크 정리 의무

---

## 9. 잠재 사고 (1.2.14·1.2.15)

| 순위 | 영역 | 사고 가능성 | 봉합 |
|---|---|---|---|
| P0 | 로그인 API 500 | **확정 (사장님 가동)** | 로그 1줄 후 즉시 봉합 |
| P1 | jsdelivr SRI 미박힘 | 공급망 사고 | 1.2.14 SRI 해시 박음 |
| P1 | Pretendard 로컬 호스팅 | CDN 의존 | 1.2.14 wwwroot/lib/pretendard/ |
| P2 | NCP 디스크 누적 | 매 빌드 280MB | 자동 정리 cron 권고 |
| P2 | CF ingress PUT silent fallback | 1033 재발 감지 0건 | 응답 `ingressApplied` 플래그 박음 |

---

## 10. 사장님 명령어 (재확인용)

**금지어**: 박다 / 박기 / 박힘 / 박혀있다 / 박은 / 영역 / 가도 (정상 한국어만)
**점수**: 받아쓰기 금지 (헌법 #32)
**의중**: 100% 정합 후 재확인 결재 후 코드 (헌법 #33)

(본 인수인계서에도 일부 금지어 잔존 — 다음 PM 발견 시 정정)

---

## 11. 커밋 흐름 (2026-06-17 새벽)

```
b9bb7f2 fix(설치마법사): 1.2.13 — Blazor CORS·CSP 봉합 정공 (P0 2건)
c188cfe fix(보안): 1.2.12 P0 #1 봉합 — ERP_ENCRYPTION_KEY hex 64자 디코드 추가
af9fed7 fix(설치마법사): 1.2.12 — 안 B 최적화 봉합 (P0 5건 + P1 5건)
35e9377 fix(설치마법사): 1.2.11 — HitPan.API 503 봉합
```

모두 develop 푸시 완료.

---

작성 종료. 사장님 퇴근. PM도 퇴근. 내일 09:00 로그 1줄 수집 후 마지막 봉합 가도.

푹 쉬세요.
