# 인수인계서 2026-08-16 — 기기 식별·슬롯 과금 재설계 [설계~1차 구현~리뷰 2회차]

> 🟢 **진실원**: 이 문서가 현재 진실원이다.
> `20260815_인수인계서_설치P0닫힘_메신저봉합_1.2.80게시_참조식.md` 는 ⬛흡수 (단 **§5 함정은 여전히 유효**).
> 세션 시작 즉시 **§0 → §1 → §7** 순으로 읽어라.

---

## §0. 30초 요약

- **무엇을 했나**: 사장님 오더로 **기기 식별·슬롯 과금 구조를 재설계**하고 1차를 구현했다.
  설계 3건 → 작지서 3건 → 구현 2회 → 병렬검증 2회 → 검증 1회 → 코드리뷰 2회.
- **지금 상태**: 🔴 **전부 미커밋.** 워킹트리에 **598줄 + 신규 소스 6개 + 문서 24건**이 있다.
- **🔴 다음 첫 행동**: **§7 — 코드리뷰 2회차가 잡은 P0 1건 봉합** → 코드리뷰 → 커밋 → PR.
  사장님이 **코드리뷰·머지·PR 게시를 전결**로 위임하셨다(2026-08-16).

> ⚠️ **커밋이 0건이다.** 세션이 끊기면 이 작업 전체가 워킹트리에만 남는다.
> 🔴 **검증 중 `git checkout` 으로 380줄이 실제로 날아간 사고가 있었다**(백업으로 복원).
> **`git checkout` / `restore` / `stash` 를 쓰지 마라.**

---

## §1. 🔴 지금 워킹트리에 무엇이 있나 (실측 2026-08-16)

| 항목 | 값 |
|---|---|
| 브랜치 | `main` |
| HEAD | `4611f8c` (8/15 인수인계 커밋 — **그 뒤로 커밋 0건**) |
| 변경 | **598 insertions / 107 deletions · 9파일** |
| 신규 소스 | **6개** |
| 신규 문서 | **24건** |

**변경 파일**
```
src/HitPan.Infrastructure/Services/TenantDeviceService.cs        421 ++++
src/HitPan.API/Services/CompanyBootstrapProvisioner.cs           129 +++
src/HitPan.Backoffice.API/Controllers/DeviceRegistrationController.cs  57 ++
installer/hitpan_db_clean.sql                                     35 +-
src/HitPan.Application/DTOs/Device/DeviceDtos.cs                  30 +-
src/HitPan.API/Controllers/AuthController.cs                      14 +-
src/HitPan.Infrastructure/HitPan.Infrastructure.csproj             8 +
src/HitPan.Tests/HitPan.Tests.csproj                               5 +
docs/검증/erp/00_INDEX.md                                          6 +
```

**신규 소스**
```
src/HitPan.Infrastructure/Services/SlotPolicyDefaults.cs          ← 코드 쪽 유일 정의
src/HitPan.API/Migrations/SQL/DB-103_tenant_devices_hardware_id.sql
src/HitPan.API/Migrations/SQL/DB-104_device_slot_policy_settings.sql
src/HitPan.Tests/Integrity/DeviceSlotGuardTests.cs
src/HitPan.Tests/Integrity/DeviceSlotSeedGateTests.cs
scripts/_truncate-only.sql
```
⚠️ `tools/EncryptHelper.cs` · `tools/*.sql` 은 **이번 작업분이 아니다**(이전부터 있던 미추적 파일).

---

## §2. 무엇을 왜 바꾸고 있나 (구조)

> 🟢 **구조 진실원** → `docs/설계/erp/20260815_아키텍처명세서_기기식별_슬롯과금_재설계.md` (**2차 개정본**)
> 단계 PRD → `docs/설계/erp/20260815_개발스케줄_PRD_기기식별_슬롯과금.md`

**사장님 오더 한 줄**: *"기기슬롯 인증을 하면 그 기기는 **100번을 접속해도 한번만** 그 기기를 잡아야해"*

**진단** — 기기를 **생김새(지문)로 알아맞히려 해서** 브라우저·창·주소가 바뀌면 새 슬롯을 먹는다.
**고객이 쓰지도 않은 자리에 돈을 낸다.** 7/2 에도 같은 증상이 났고 지문을 정교하게 해 봉합했다 — **한 달 반 만에 재발했다.**

**해법 — 알아맞히기를 그만둔다.**
> 사장님: *"**자동화보단 물리적으로 수동으로.** 클라이언트 인증요청 → 메인PC 인증수락·슬롯추가 → 클라이언트 인증등록"*

**슬롯은 기기가 접속했다고 늘지 않는다. 대표가 수락 버튼을 누를 때 늘어난다.**
제어는 **로그인과 업무 화면 사이의 화면 하나**에서 한다(명세서 §14).

---

## §3. 사장님 결재로 닫힌 것 (다시 논의하지 마라)

| 안건 | 결재 |
|---|---|
| 과금 단위 | **기기 1대 = 슬롯 1개, 메인PC 포함.** 계정 무제한 |
| 추가슬롯 | **1만원 / PC 1 + 모바일 1** (현행 코드는 1+2 — 정정 대상) |
| 범위 | **베이직 · 프로.** 프리미엄 제외(클라우드·서버구축 = 별개 트랙) |
| 실사용 고객 | **없다** ⇒ 재매핑 위험이 지금은 실재하지 않는다. **지금이 구조를 깔끔하게 잡을 유일한 창** |
| 한도 초과 | **401 을 내지 않는다.** 로그인은 통과, **중간 화면에서 제어** |
| 승인 | **영구** — 대표가 슬롯을 지우기 전까지 |
| 메인PC | **설치 = 승인.** 앉은 사람은 구조적으로 ERP담당자·대표 |
| 인증번호 | **사람이 옮겨 적는다.** 알림에 싣지 않는다(옆에서 보면 샌다) |
| 게이트 | **양방향** — 요금 새는 쪽도 지킨다 |
| 전결 | **코드리뷰·머지·PR 게시 전결**(2026-08-16) |

**하지 않기로 한 것** (전부 PM 이 책상에서 만든 걱정 — 사장님이 접으심):
대표 부재 대응 · 반복요청 차단 · 메인PC 승인자 추가 검증

---

## §4. 어디까지 됐나

| SOP | 산출물 | 상태 |
|---|---|---|
| [2] 설계 | 아키텍처 명세서 · 스케줄 PRD | ✅ **2차 개정** |
| [2] 작지서 | `20260815작2` | 🔴 **폐기**(차수 재편) |
| [2] 작지서 | `20260815작3` 1차 | ✅ PM 결재 |
| [2] 작지서 | `20260816작1` R-1 봉합 | ✅ PM 결재 |
| [3] 개발 | 1차 구현(계수·설정화·DDL) | ✅ 완료 |
| [3] 개발 | R-1 봉합 + B-5 + 게이트 4 | ✅ 완료 |
| [3-V] 병렬검증 | 1·2회차 → PI-08~PI-21 **14건** | ✅ |
| [4] 검증 | 작업리뷰서 → **조건부 승인** | ✅ |
| 코드리뷰 | 1회차 6건 · **2회차 5건** | ✅ |
| **[5] 결재** | — | ⬜ **전결 위임됨** |
| **[6] 커밋** | — | 🔴 **0건** |

**3관문 실측**: 빌드 **0/0** · 시험 **509건** · `ddl-smoke` **PASS**

---

## §5. 🔴 함정 — 다음 세션이 밟을 지뢰

> ⚠️ 8/15 문서 §5 함정도 여전히 유효하다. 여기 것은 **추가분**이다.

### §5-1. 🔴🔥 `git checkout` 금지 — 실제로 380줄이 날아갔다
검증 중 게이트 반증을 하려고 `git checkout` 을 썼다가 **개발팀 미커밋 380줄이 통째로 사라졌다.**
백업을 떠 둔 덕에 복원했다. **커밋 0건 상태에서 되돌리기 명령은 전부 사고다.**
반증이 필요하면 **사본을 떠서** 하라.

### §5-2. 🔴 게이트가 오늘만 **네 번** 가짜였다
| # | 게이트 | 무엇이 가짜였나 |
|---|---|---|
| 1 | **G-8** | *"코드 리터럴에 없는지"* — **상수를 다른 파일로 옮기면 통과** |
| 2 | **G-13** | 진단 자체가 틀렸다(`tablet` 은 애초에 저장 안 됨) |
| 3 | **P25** | **인덱스 이름 문자열만** 검사 — `NOT NULL DEFAULT ''` 로 바꿔도 초록불인데 그 DDL 이면 2번째 기기부터 1062 |
| 4 | **G-21** | 시드에 닿기 전 `users`/`employees` 를 **DELETE 해서 조기 return 을 우회** — **결함을 게이트가 가렸다** |

🔴 **글자를 검사하면 게이트가 아니다. 동작을 검사해라.**
그리고 **게이트가 실제 운영 경로를 밟는지** 확인해라(G-21 이 그것을 안 밟았다).

### §5-3. 🔴 PM 설계가 **P0 를 세 번** 냈다
| 회차 | 무엇을 틀렸나 |
|---|---|
| 1 | 열쇠를 *"장비넘버 + 인증키"* 라고만 쓰고 **AND/OR 를 안 정함** · **DDL 을 아예 언급 안 함** |
| 2 | "닫았다"고 썼는데 안 닫힘 — **클라이언트가 기기번호를 아예 안 보낸다**(AND 의 오른쪽 항이 없다) |
| 3 | 계수를 4곳 → 5곳으로 정정했는데 **실제로는 6곳**(`SessionLimitMiddleware.cs:19`) |

🔴 **문서에 "닫았다"고 쓰는 것과 실제로 닫는 것은 다르다.** 반드시 실측으로 확인해라.

### §5-4. ⚠️ 판정이 세 번 엇갈린 자리가 있다 — `?? "pc"` 폴백
병렬검증(PI-20) *"한 곳만"* ↔ 개발팀 *"4곳 전부"* ↔ 검증팀 *"개발팀 승"* ↔ **코드리뷰 2회차 *"다시 문제"***
🔴 **아직 최종 판정이 안 났다.** §7-2 에서 정리해야 한다.

### §5-5. ⚠️ 백지 환경 실측이 **한 번도 안 됐다**
전부 개발 PC 와 격리 임시 DB 에서만 확인했다.
[[feedback_dev_pc_proves_nothing]] — **개발 PC 정상은 검증이 아니다.**
그리고 8/15 에 샌드박스가 PC 종료로 사라져 **`test1` 환경이 없다.**
랜딩 다운로드 경로는 살아 있다(실측): `updates.hitpan.kr/packages/HitPan-ERP-Setup-1.2.80.exe` · 259MB · HTTP 206

---

## §6. 남아 있는 지적 — 등급별

### 🔴 P0 (커밋 전 반드시)
| # | 내용 | 근거 |
|---|---|---|
| **CR2-1** | **R-1 봉합이 절반만 됐다.** 시드가 `CreateParentAsync` **맨 끝, 조기 return 5개 뒤**에 있어 **기존 테넌트는 도달 못 한다.** R-1 이 덮으려던 바로 그 집단 | `CompanyBootstrapProvisioner.cs:412` · 조기 return 은 `:206` |

### P1 — 2차로 넘긴 것
| # | 내용 |
|---|---|
| B-1 | 🔴 `UserInfoPage.razor.cs:125` 가 `deviceType="pc"` 하드코딩 → **폰이 PC(비싼) 칸을 먹는다** |
| B-2 | `P25` 가짜 게이트 |
| B-3 | `pricing_plans_seed.sql` 이 **세 번째 진실원**, basic 모바일 **0** |
| B-4 | `SessionLimitMiddleware.cs:19` 에 `["premium"]=100` 잔존 → `enterprise` 부재로 **429 가 먼저 뜬다** |
| B-6 | `uq_tenant_hardware` 가 `hardware_id` NULL 에만 안전 |
| CR2-2 | `?? "mobile"` 폴백 — `getDeviceType` 예외 시 **진짜 PC 가 모바일 칸으로**(`TenantDeviceService.cs:302`) |
| CR2-3 | 같은 폴백이 **라이선스 활성화 경로**에도(`DeviceRegistrationController.cs:93`) — 데스크톱 설치 경로다 |
| CR2-4 | `type=="tablet"` 이 **죽은 코드**(`:322`). `ApproveAsync:456` 의 똑같이 생긴 검사는 **살아 있다** |
| CR2-5 | `GetQuotaAsync` 가 `local_subscription` 을 **세 번** 읽는다(`:134`) |

### ✅ 코드리뷰가 안전하다고 확인한 것
계수 통합(`CountUsedSlotsAsync`/`GetLimitsAsync`/`ResolveLimits`) · QR 경로 모바일 전용 유지 ·
갱신 경로 한도 미검사 유지 · `enterprise`/`premium` 정정 · G-20 의 코드↔DB-104 15키 대조 ·
`DB-103` NULL 허용 UNIQUE(`MainPcRegistrationService` 가 `hardware_id` 를 INSERT 목록에서 빼므로 공존)

---

## §7. 🔴 다음 세션 첫 행동

### 7-1. CR2-1 봉합 (P0)
시드가 기존 테넌트에게도 닿게 한다. 두 가지를 함께:
1. **시드를 조기 return 앞으로** 옮기거나 별도 경로로
2. 🔴 **G-21 이 DELETE 없이 실제 운영 경로를 밟게** — 지금은 게이트가 결함을 가리고 있다

### 7-2. `?? "pc"` / `?? "mobile"` 폴백 최종 판정 (§5-4)
세 번 엇갈렸다. **코드로 갈라서 결론을 문서에 남겨라.**

### 7-3. 코드리뷰 3회차 → 커밋 → PR
사장님 **전결 위임**. 다만 CR2-1 이 닫히기 전에는 커밋하지 않는다 —
**이 작업이 존재하는 이유가 R-1 인데 그것이 절반만 됐다.**

⚠️ 커밋 시 [[feedback_git_commit_heredoc_title]] — 메시지는 **파일(-F)로**. here-string 쓰면 제목이 `@` 로 깨진다.
⚠️ main 은 보호 브랜치다. **PR 경유**(Ruleset). [[project_squash_rebase_verify]] — 스쿼시가 커밋을 삼킨 적이 있다.

### 7-4. 그 다음 (2차)
명세서 §14 **중간 화면** + 열쇠 AND + 메인PC 합류.
🔴 **1차만으로는 사장님 3증상이 하나도 안 닫힌다**(PI-12). **2차까지 가야 닫힌다.**

---

## §8. 건드리면 안 되는 것

- 🔴 **`git checkout`/`restore`/`stash`** — §5-1
- **DB-104 파일 수정** — 이미 적용된 고객이 있을 수 있다
- **clean DDL 의 `('DB-104','clean-ddl',1)`** — `ddl-smoke` 가 그 줄을 **요구**한다
- **`appsettings.json`·`appsettings.Development.json`** — 헌법 #21
- **워치독·업데이트 팝업 Y/N** — 헌법 #43
- **운영(demo 3306)·NCP** — 헌법 #39
- 마이그레이션은 **`src/HitPan.API/Migrations/SQL/DB-NN_*.sql` 에만**. 최신 = **DB-104**

---

## §9. 관련 문서 (주소만)

| 종류 | 주소 |
|---|---|
| 🟢 구조 진실원 | `docs/설계/erp/20260815_아키텍처명세서_기기식별_슬롯과금_재설계.md` |
| 단계 PRD | `docs/설계/erp/20260815_개발스케줄_PRD_기기식별_슬롯과금.md` |
| 작지서 1차 | `docs/운영기록/20260815작3_기기식별_슬롯과금_1차_작업지시서.md` |
| 작지서 R-1 봉합 | `docs/운영기록/20260816작1_기기슬롯_R1봉합_신규설치_기준값시드_작업지시서.md` |
| 현행 실측(P0) | `docs/개발/erp/20260815_P0_현행봉인_계수스냅샷_기기슬롯.md` |
| 개발명세서 1차 | `docs/개발/erp/20260815_개발명세서_1차_계수단일화_기준값설정화_DDL.md` |
| 개발명세서 봉합 | `docs/개발/erp/20260816_개발명세서_R1봉합_신규설치_기준값시드.md` |
| 작업리뷰서 | `docs/검증/erp/20260815_작업리뷰서_기기슬롯_1차_계수단일화.md` |
| 병렬이슈 14건 | `docs/검증/병렬이슈/20260815_PI-08` ~ `PI-21` + 요약 2건 |
| ⬛흡수(§5 함정 유효) | `docs/운영기록/20260815_인수인계서_설치P0닫힘_메신저봉합_1.2.80게시_참조식.md` |

---

## §10. 사장님 지시 중 아직 안 한 것

8/15 인수인계서 §6 의 5건 중 **①메신저 UI** 는 손도 못 댔다. 기기슬롯이 끼어들었다.
②계층권한 분리 · ④계정·사원관리 는 **이 작업과 인증 축을 공유**한다 — 2차와 묶어 보는 것을 권한다.

> 🔴 **새 추측 금지.** 이 작업에서 PM 설계가 P0 를 세 번 냈고, 전부 **실측 없이 문서에 "닫았다"고 써서** 났다.
> **증상이 조용하면 실측으로 갈라라.**
