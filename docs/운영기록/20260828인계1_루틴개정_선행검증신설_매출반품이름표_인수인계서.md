# 인수인계서 2026-08-28 (인계1) — 루틴 개정 · 선행검증 신설 · 매출반품 이름표

> **언제 보나**: 세션이 끝나 **새 창을 열 때** (같은 날 이어서).
> 다음날이면 → [`20260828일보1`](20260828일보1_루틴개정_매출반품_분개이름표_일일작업보고서.md) 먼저.

| 항목 | 상태 |
|---|---|
| 브랜치 | `docs/20260828-routine-verify-first` |
| PR | **#346** — 머지 대기 |
| 미커밋 | 🟢 **0** |
| 게시 | ⚠️ **1.3.29 그대로** |

---

## 🔴 0. 다음 세션 첫 행동 — 이것부터

1. **PR #346 머지됐나 — 🔴 파일 내용으로 대조하라**
   ```
   git show origin/main:docs/WORKFLOW.md | grep -c "1-V"    # 5 나와야 함
   git ls-tree -r origin/main --name-only | grep 검증/선행   # 2건 나와야 함
   ```
   🔴 **오늘 PR #344 가 `MERGED` 인데 커밋이 삼켜졌다.** 상태값 믿지 마라.

2. **화면 확인** — 정합성 검사 화면(`/accounting/integrity`)에 **매출반품 항목이 보이나**
   🔴 **이번 봉합은 서버·DB 층이다.** 8/27 작7 이 정확히 *"서버는 맞았는데 화면이 안 받았다"* 였다.

3. **게시 판단** — 1.3.30 을 사슬 나머지와 묶을지 **사장님 결재**

---

## 1. 🔴 업무 루틴이 바뀌었다 — 오늘부터 이게 정본

```
[1] 사장 오더 → [1-V] 🔴선행검증 → [2] 설계 → [3] 개발 ⟷ [3-V] 병렬검증
                                                       ⟷ [4] 검증 🔴동시착수
```

| 단계 | 누가 | 무엇 |
|---|---|---|
| **[1-V] 선행검증** ⭐신설 | 검증팀장 데이비드 박 | **설계보다 먼저.** 오더의 전제를 실측. 깨지면 **설계 착수 금지** → 사장님께 되묻는다 |
| **[4] 검증** | 검증팀장 | 🔴 **개발과 동시 착수.** 끝나기를 기다리지 않는다 |

- 정본: [`docs/WORKFLOW.md`](../WORKFLOW.md) §1 · 요약: `START_HERE.md` §4 · `CLAUDE.md`
- 산출 폴더: **`docs/검증/선행/`** (신설)

**왜 바뀌었나**: 8/27 작7 게이트 9건이 **전부 통과하고도 반려**났다.
게이트가 잰 건 *"서버가 만드는가"* 였고 **그건 사실**인데 화면이 안 받았다.
**틀린 전제 위에 설계문서를 쓰면 그 아래가 전부 틀린다.**

**🟢 첫 적용에서 바로 효과가 났다** — 아래 §3.

---

## 2. 🔴 주의사항 — 오늘 실제로 밟은 함정

| # | 함정 | 어떻게 됐나 |
|---|---|---|
| 1 | 🔴 **스쿼시가 커밋을 삼킨다** | PR#344 `MERGED` 인데 루틴 커밋이 main 에 없었다. **파일 대조로 잡음** |
| 2 | 🔴 **낱말 개수는 근거가 아니다** | 「참조 0회」를 두 번 다르게 오독 → 인계5·인계6 둘 다 틀림 (헌법 #32) |
| 3 | 🔴 **주석은 그 서브시스템의 것** | `:cancel` 은 `MonthlySummary` 얘긴데 회계로 옮겨 적음 → [4] 가 P2 로 반려 |
| 4 | **CRLF** | `docs/*.md` 는 CRLF. `\n` 앵커로 치환하면 **무조건 실패** |
| 5 | **`python` 은 Store 스텁** | `node` 를 써라. 단 **node 는 `C:/` 경로**, Bash 는 `/c/` |
| 6 | **`hitpan_erp` DB 는 없다** | 실재: `hitpan_e2e` · `hitpan_backoffice` · `hitpan_trgtest` |

---

## 3. 오늘 한 일 — 매출 파트

### 3-1. 🔴 선행검증에서 내 기록이 두 번 틀린 게 드러났다

| | 내가 쓴 것 | 실제 |
|---|---|---|
| 인계5 | *"매출반품이 회계에 아예 없다"* | ❌ **분개는 만든다** |
| 인계6 | *"구멍은 읽는 쪽이다"* | 🟡 절반 — **쓰는 쪽이 원인** |

**둘 다 코드를 안 읽고 낱말만 셌다.**

### 3-2. 진짜 원인 — 자기 이름표가 없었다

| | 매입 | 매출 |
|---|---|---|
| 반품 키 | `purchase_return` | 🔴 **없음** |
| 반품취소 키 | `purchase_return_cancel` | `sales_return_cancel` |

`sales_delivery_cancel`(명세서취소)를 빌려 씀 ⇒ 장부에서 **반품·취소 구분 불가**
⇒ `FinanceService` 기표누락 검사가 매출반품을 **셀 키가 없었다**

🔴 **8/27 내가 만든 정합성 검사 화면이 이 사각을 그대로 물려받았다.**

### 3-3. 고친 것

`AutoJournalHelper`(신설 메서드) · `SalesService:2487` · `FinanceService` ·
`DB-115`(과거 라벨 정정) · `hitpan_db_clean`(시드) · 게이트 4종

⚠️ **매입 복붙 안 함** — 매입반품=매입채무 / 매출반품=**외상매출금**

### 3-4. 검증

| 항목 | 결과 |
|---|---|
| 빌드 | 🟢 0/0 |
| 시험 | 🟢 **1093/1093** |
| DB 실측 | 🟢 4건 시나리오 · 재실행 no-op · 검산 0 |
| **대조군** | 🟢 **옛 검사 0 vs 새 검사 1** |
| 반증 | 🟢 4건 (게이트 3·4번 실제 FAIL 확인) |

> ⚠️ 시험용 DB(`hitpan_e2e`). **설치 실물 확인은 별도.**

---

## 4. 문서 (참조식 — 주소만)

| 단계 | 문서 |
|---|---|
| [1-V] | `docs/검증/선행/20260828_선행검증서_매출반품_회계읽는쪽.md` |
| [3] | `docs/개발/erp/20260828_개발명세서_매출반품_분개_자기이름표.md` |
| [4] | `docs/검증/erp/20260828_작업리뷰서_매출반품_분개_자기이름표_조건부통과.md` |
| [5] | `docs/운영기록/20260828결재1_매출반품_분개_자기이름표_CTO결재의견.md` |
| [7] | `docs/운영기록/20260828일보1_루틴개정_매출반품_분개이름표_일일작업보고서.md` |

---

## 5. 남은 것

| 건 | 상태 |
|---|---|
| **화면 확인** (정합성 화면 매출반품) | 🔴 **최우선** — 서버만 고쳤다 |
| 매출 사슬 — 마이너스 전표 | ⬜ 미착수 |
| 그리드 오더 | ⬜ 발주 enum 선행 |
| 계산서 2차 | ⬜ 미착수 · 상태 3단계가 DB 에 없음 |
| 그룹웨어 편집기능 제거 | 🟡 사장님 *"지금 하지마"* — **먼저 여쭙지 말 것** |
