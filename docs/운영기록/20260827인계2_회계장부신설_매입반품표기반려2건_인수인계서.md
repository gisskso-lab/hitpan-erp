# [★] 인수인계서 20260827인계2 — 회계장부 신설 · 매입반품 표기 반려 2건

| 항목 | 내용 |
|---|---|
| 작성 | PM 브라운킴 · 2026-08-27 |
| 세션 범위 | 용량정리 → 작1·작2 게시(1.3.24) → 작3·작4 게시(1.3.25) → **실측 반려 2건** |
| main | `44ebaedf` |
| 게시 | **1.3.25** (실물 확인) |
| 미커밋 | **0** |

---

## 🔴 다음 세션이 가장 먼저 할 일

### P0 — 실측 반려 2건 규명 (착수 전 사장님께 증상 확인부터)

1.3.25 실측 결과:

| # | 항목 | 판정 |
|---|---|---|
| 1 | 반품목록 「원 매입전표」 (직접작성 표기 포함) | 🟢 **통과** |
| 2 | 매입목록 「반품전표」 — **부분반품 2건이면 둘 다** | 🔴 **반려** |
| 3 | 매입현황·반품현황 **「전표별」 장부** | 🔴 **반려** |
| 4 | 회계장부 차변·대변 합계 일치 | 🟢 **통과** |

🔴 **게이트 6+7건이 전부 초록불인데 반려됐다. 또 같은 실수다.**

**원인 (PM 자기반증)**: 내 게이트는 **레포 코드**를 봤다 —
*"화면 파일에 「전표별」 글자가 있나"*, *"서비스가 값을 내려주나"*.
사장님이 보신 건 **게시된 1.3.25 화면**이다. 그 사이가 끊겼는지를 **내 게이트는 구조적으로 못 본다.**
⇒ [[project_fixed_vs_delivered_gap]] **"고쳤다 ≠ 갔다" 4차 재발.** 그 메모를 읽고도 같은 종류를 짰다.

**⚠️ 유력 가설 (미검증 — 추측으로 착수 금지)**
- 2·3번이 **증상 2개 · 원인 1개** 모양이다.
- 1·4번이 통과한 게 단서 — 그 둘은 **다른 파일**이라 갈릴 이유가 있다.
  - 1번 = `PurchaseReturnList.razor` (컴포넌트)
  - 2번 = `PurchaseReceiptList.razor` (컴포넌트)
  - 3번 = `PurchaseStatusPage`·`ReturnStatusPage` (페이지)
  - 4번 = `TrialBalancePage` (신규 페이지)
- 🔴 **신규 파일(4번)은 갔는데 기존 파일 수정분(2·3번)이 안 갔다면** 배포·캐시 축을 먼저 본다.

🔴 **착수 순서**:
1. **사장님께 화면 증상 먼저 확인** — 「전표별」 선택지 자체가 없나 / 선택은 되는데 표가 안 바뀌나
2. 그 다음 규명. **추측 착수 금지** — 8/25 에 추측으로 다섯 번 틀린 자리.

---

## 🟢 이번 세션 완료분

### 1. 개발PC 용량 — 3.0G → 1.6G (1.4G 회수) · 빌드 0/0 확인

| 지운 것 | 용량 |
|---|---|
| `src/**/bin`·`obj` 28개 | 980M |
| `히트판ERP_설치_v2.zip` (4/22) | 170M |
| `artifacts/doc-build`·`sec01-build` (4/12) | 95M |
| tar.gz 3종 (7/7) | 44M |
| 🔴 `installer/web-new` | 23M |
| installer sql 백업 4개 · `tools/screenshots` 448장 | 66M |

🔴 **`web-new` = 이월 #5 의 답이었다.** 이름이 "new" 라 안 지우셨는데,
**8/2 산출물**이고 실제 payload(8/26)보다 **오래된 사본**이었다. `api-demo` 가 5개 파일에 살아 있었고
세 문서가 **P1 「거짓증거」** 로 지목하던 것 — 조사할 때 이게 잡히면 헛다리를 짚는다.

🛡️ **무접촉**: `db-backup-*` 2개(유일본 `hitpan_erp.sql` **140테이블·Dump completed** 재확인) ·
`tests/sandbox-m4`(검증 도구).

### 2. 작1 — 매입↔반품 정합성 (PR#320 · 1.3.24)

**실측**: 같은 데이터·같은 30일에 세 화면이 **2 / 4 / 3 건**이었다.

| 화면 | 필터 | 건수 |
|---|---|---|
| 반품**현황**(집계) | `status='confirmed'` | 2 |
| 반품**목록** | **없음** | 4 ← 문제 |
| 매입목록 「반품」 | `status<>'canceled'` | 3 |

원인 ① 필터가 제각각 ② **날짜 컬럼이 다르다**(매입일 vs 반품일) —
7월 매입을 8월에 반품하면 매입 행이 창 밖이라 「반품」이 뜰 자리가 없다.
⇒ **표기 로직은 처음부터 정상이었다. 행이 화면에 없었을 뿐.**

봉합: `GetReturnsAsync` 에 취소분 제외 + **「반품포함」 옵션**(사장님 결재 B안, 기본 false).

### 3. 작2 — 그룹웨어 급여 조회전용 (PR#320 · 1.3.24)

사장님 결재 **「5개 전부 제거」** ⇒ 등록·수정·확정·지급완료·취소 + 편집 다이얼로그 제거(**-391줄**).
회계 「급여관리」에 5개 전부 실재 확인 후 착수 ⇒ **기능 손실 0**.
**부수 발견**: 발송 후 `CloseSendAsync` 가 `/hr/payroll`(조회전용)로 튕겨
담당자를 막다른 길에 떨어뜨리고 있었다 → 회계로 돌림(헌법 #20).

### 4. 작3 — 매입↔반품 연결 표기 (PR#322 · 1.3.25) 🔴 **2·3번 반려**

`receipt_id` 는 **DB에 진작 있었는데** DTO·SELECT 에 없어 **화면까지 값이 안 갔다.**
반품목록 「원 매입전표」 · 매입목록 「반품전표」(`GROUP_CONCAT`) · 전표별 장부 신설.

### 5. 작4 — 🟢 **회계장부 신설** (PR#322 · 1.3.25 · **통과**)

🔴 **핵심 발견**: `journal_entries`/`journal_lines` 를 **읽는 조회 화면이 0건**이었다.
13개 회계 화면이 전부 **원본 테이블을 각자 직독**한다.
⇒ **분개를 쌓기만 하고 여는 문이 없었다.** `/accounting/ledger` 가 그 첫 문.
`ledger_balance_snapshot` 은 **코드 참조 0건**(죽은 표) — 5차 재활용 예정.

**실측 검증**: 판매→회수 시 외상매출금이 **상계돼 0**, 차변합=대변합=220,000 ✅

---

## 🔴 급여·경비가 회계장부에 없다 — **고장 아님. 설계대로.**

사장님 질문: *"회계장부에 급여, 경비는 반영이 안되네??"*

**지금 장부에 모이는 것 = 매입·판매·생산·계산서 넷뿐.**
**수금·지급·경비·급여는 분개를 아예 안 만든다** — 화면·저장은 되는데 **회계로 넘기는 배선이 없다.**

| 업무 | 화면 | 저장 | 기표 | 실제 서비스 |
|---|---|---|---|---|
| 수금 | 🟢 | 🟢 | 🔴 | `CollectionService.cs:121` |
| 지급 | 🟢 | 🟢 | 🔴 | `CollectionService.cs:253` (같은 파일) |
| 경비 | 🟢 | 🟢 | 🔴 | `FinanceService.cs:265` |
| 급여 | 🟢 | 🟢 | 🔴 | `PayrollService.cs:506` |

⇒ 사장님이 **차수로 승인**하신 3차(수금·지급)·4차(경비·급여) 예정분.

🔴 **다만 내 누락 1건**: 화면 안내문에 수금·지급·경비만 넣고 **급여를 빠뜨렸다.**
사장님이 급여를 물으신 이유가 이것일 수 있다. ⇒ **다음 차수에 급여 추가.**

🔴 **착수 블로커**: 계정과목이 **8개뿐이고 「현금」이 없다**
(`CompanyBootstrapProvisioner.cs:395` 실측). 현금 계정이 없으면 수금을 기표할 **이름표 자체가 없다.**
⇒ **2차(계정과목 8→20여개)가 3·4차의 전제조건.**

---

## 📋 사장님 결재 완료 — 회계 워크플로우 (문서: `docs/설계/erp/20260827_회계장부_워크플로우_설계결재서.md`)

| # | 결재 |
|---|---|
| 1 | 1차 범위 = **한 화면씩** ⇒ 시산표 하나부터 ✅완료 |
| 2 | **회계장부 먼저** · 계정과목·기표는 기존 것 수정보완 |
| 3 | 경비분류 = 기본값 제안 + **반자동** |
| 4 | 어음 = 거의 안 쓰지만 **레거시 마이그 위해 존치** |
| 5 | 현금·카드·계좌 **수동기표** · 토스 파이프라인은 **설계에만** |
| ★ | *"saas결재는 지금 하지마"* ⇒ `billing_*` **무접촉** · 회계 축만 |

**사장님 7단계 흐름 판정 2건 정정**:
- **계정과목은 3번이 아니라 맨 앞(0번)** — 마이너스 전용이 아니고, 없으면 나머지가 작동 안 함
- **매입매출장 ≠ 수금+지급** — 매입매출장은 *팔았다/샀다*(세금계산서), 수금·지급은 *돈 받았다/줬다*.
  **수금+지급을 한번에 보는 자리가 「회계장부」** 다(사장님이 찾으시던 것).

### 남은 차수
| 차수 | 내용 | 상태 |
|---|---|---|
| 1차 | 시산표 | ✅ 완료 |
| **2차** | **계정과목 8→20여개** + 경비분류 매핑 | 🔴 3·4차 전제 |
| 3차 | 수금·지급 기표 | 대기 |
| 4차 | 경비·급여 기표 | 대기 |
| 5차 | 분개장·총계정원장·거래처원장 + 수동분개 | 대기 |

---

## 🔴 이번 세션 PM 오판 4건 (같은 뿌리)

**공통 원인 = 한 번 막히거나 숫자 하나 보고 「영구 제약/전체 상태」로 단정.**

| # | 오판 | 실제 |
|---|---|---|
| 1 | *"Actions 계정 정지"* | **큐 대기**였다. run 0건만 보고 단정 |
| 2 | *"게시 dispatch 못 한다"* | **됐다.** 사장님 *"전에 했잖아"* 로 규명 |
| 3 | *"main 에 워크플로 파일이 없다"* | Git Bash 가 `origin/main:경로` 의 `:` 를 백슬래시로 바꿔 조회 실패한 것. 파일은 정상 |
| 4 | *"PaymentService·ExpenseService 파일 자체 없음"* | **다른 파일 안에** 멀쩡히 있었다(파일명만 다름) |

🔴 **교훈**: 어제 NCP 오진(*"레포에 없다 ≠ 서버에 없다"*)과 **같은 종류**다.
⇒ **단정 전에 한 줄 더 확인한다.** `ls`·`cat-file`·재시도 한 번이면 갈린다.

⚠️ **Git Bash 경로 함정**: `git show origin/main:파일` 이 실패하면 `export MSYS_NO_PATHCONV=1` +
`git cat-file -p "origin/main:파일"` 로. 커밋 메시지는 `$TMPDIR` 말고 **scratchpad 절대경로**로.

---

## 🟢 인프라 상태

- **NCP 디스크**: `/dev/vda2 9.8G · 7.1G 사용 · 2.2G 여유 (77%)`
  packages = **현행+직전 2세대만**(회전 정상 동작 확인). 게시 1회 ≈ 240M ⇒ **여유 8~9회분**.
  ⚠️ 어제 *"2~3회분"* 은 회전을 계산에 안 넣은 **과소평가**였다.
- **CI**: 정상. 1.3.25 커밋 CI **전부 초록 · 실패 0건**(CodeQL 포함).
  ⚠️ `ZAP Baseline` 은 **8/26 이전부터 계속 실패** — 이번 변경 무관(실측 확인).

---

## ⏸ 이월 (미착수)

| # | 건 | 상태 |
|---|---|---|
| 1 | **반려 2건 규명** | 🔴 **P0 — 사장님 증상 확인 후 착수** |
| 2 | 회계 2차(계정과목) | 3·4차 전제 |
| 3 | 급여를 미기표 안내에 추가 | PM 누락분 |
| 4 | NCP 480M 정리 | 사장님 *"냅둬"* — **재개 지시 없으면 손대지 말 것** |
| 5 | NCP 디스크 증설 | 비용 판단 (급하지 않음 — 여유 8~9회분) |
