# 20260826작7 — 급여 화면 분리: 회계 「급여관리」 신설 · 그룹웨어 「급여조회」 원복

| 항목 | 내용 |
|---|---|
| 지시 | 사장님 2026-08-26 — *"전 세션에서 개판친거 다시 수정해"* |
| 결재 | 🟢 **사장님 직접 오더** (*"시키는데로 좀 하자"* · *"최대한 신속, 정확하게, 그리고 루틴대로 봉합해"*) |
| 상위 | [20260826작6](20260826작6_회계급여_명세서메일발송_작업지시서.md) — 이 문서가 **바로잡는 대상** |
| 상태 | 🟢 코드 완료 · 빌드 0/0 · **1011 통과**(+7) · DDL 0 · 🔴 **사장님 실측 대기** |

---

## 1. 사장님 지시 원문

> 회계관리에 "급여관리" 메뉴를 만들어서 **대표계정과 권한자만** 접근하여 급여명세서 생성, 대표에게 결재, 메일발송을 하고,
> 그룹웨어의 급여메뉴에선 **계정로그인자 개인의 급여명세서 확인·조회 정도** 기능만 하자고 했는데,
>
> 잘 돌아가던 그룹웨어 급여 화면을 회계 화면처럼 **개조**했음. **경리가 쓸 발송 기능을 직원 화면에 올려놨음.**
> 그리고 **화면 여는 로직을 갈아치웠음** — 원래 한 줄이던 걸 주소창 파라미터 읽는 코드로 바꿨음.
> **직원이 그냥 자기 급여 보러 들어오는 경로를 건드렸음.**
>
> 1. 회계관리에 "급여관리" 메뉴신설
> 2. 그룹웨어에 "급여관리" 메뉴 "급여조회"로 바꾸고, **원래대로 돌려놔.**

추가 지시:
> 그룹웨어의 급여메뉴는 **로그인 계정의 개인급여조회**야. **타인의 급여를 볼수없고, 내 급여만 볼수 있는 기능**

---

## 2. 🔴 무엇이 잘못됐나 — PM 자책

작6 은 회계 화면을 **만들지 않았다.** 대신 **그룹웨어 직원 화면 하나를 개조**해서 양쪽 일을 다 시켰다.

| | 작6 이 한 것 | 있어야 했던 것 |
|---|---|---|
| 화면 | `/hr/payroll` **하나** | 회계 · 그룹웨어 **둘** |
| 발송 | 직원 화면 툴바 | 회계 화면 |
| 진입 | 주소창 `?send=`·`?slip=` 읽고 API 1회 더 | `=> await LoadAsync()` 한 줄 |

### 🔴 사고의 크기 — 사장님이 전수로 세어 주셨다

- `PayrollPage.razor` **+359줄 / -2줄**(499→856)
- 붙은 것: 「명세서 메일발송」 단추 · **발송 확인 다이얼로그 통째로**(명단표·집계·결과표·재발송) · 행마다 「명세서 받기」 · **진입 로직 교체** · `OnAfterRenderAsync` 신설
- 서버 `PayrollController` **+125줄** — 엔드포인트 3개 + `IPayslipMailService` 의존성
- 결재화면 `ApprovalManagePage.razor` **+57줄** — 승인 팝업 + `IDialogService`

### 🔴 근거를 알면서 반대로 갔다

**같은 날 작3 문서에 PM 이 직접 적어 놓았다**:

> | **회계 급여** | — | 🔴 **없다** |

없다고 조사해 적어 놓고, 만들지 않고 남의 화면에 얹었다.

### 🔴 왜 못 잡았나

작6 게이트 4종은 **기능이 있는지**만 봤다. **어느 화면에 붙었는지**는 아무도 안 봤다. 그래서 전부 초록불이었다.
⇒ 이번 게이트는 **붙은 자리**를 본다(§4).

---

## 3. 한 일

### W1 — 그룹웨어 원복 (`/hr/payroll`)

`git checkout 44c3914c -- PayrollPage.razor` — **856 → 499줄**.

- 진입 로직 복귀: `protected override async Task OnInitializedAsync() => await LoadAsync();` **한 줄**
- 잔재 0 실측: `메일발송`·`SendQuery`·`SlipQuery`·`_sendOpen`·`OpenSendAsync`·`OnAfterRender`·`IJSRuntime`·`NavigationManager` **전부 검출 0건**
- 사이드바 `Sidebar.razor:140` 「급여 관리」 → **「급여 조회」**

### W2 — 🔴 "내 급여만" 은 서버가 이미 막고 있었다 (실측)

사장님 추가 지시를 **화면이 아니라 서버에서** 확인했다 — 감추는 것은 차단이 아니다.

`PayrollController.ResolveScopeAsync`:
```
if (await CanSeeOthersAsync()) return (true, requested);   // 권한자 → 전 직원
var me = CurrentEmployeeId();
return (true, me);                                          // 🔴 요청을 무시하고 본인으로 덮는다
```

⇒ 직원이 **주소창에 남의 id 를 넣어도 본인 것으로 덮인다.** 원복한 화면은 그대로 안전하다.
(사장님 2026-08-13 *"a직원 급여를 b직원이 볼수 없어야해"* 가 이미 서버에 들어가 있었다.)

### W3 — 회계 「급여관리」 신설 (`/accounting/payroll`)

`Pages/Finance/PayrollManagePage.razor` — 작6 이 만든 기능을 **제자리로 옮겼다**(기능 삭제 0).

- 라우트 `/accounting/payroll` · 네임스페이스 `FinanceUi` · 제목 「급여 관리」(회계)
- 사이드바 회계관리 그룹 `Sidebar.razor:296` — 「경비 처리」 위
- 그룹웨어 문맥 주석을 회계 문맥으로 정정

### W4 — 결재 팝업 목적지 교체

`ApprovalManagePage.razor:364` — `"/hr/payroll?send=1"` → **`"/accounting/payroll?send=1"`**

🔴 팝업 자체는 **사장님 결재사항**(대표이사 승인 → 발송 Y/N)이라 **살렸다.** 가는 곳만 바꿨다.

### W5 — 권한 확인 (실측)

`CanSeeOthersAsync` — **부모계정(`tenant_admin`) 또는 `PAYROLL` 권한자**만 통과.
⇒ 사장님 *"대표계정과 권한자만"* 과 일치. 화면 `_canManage` 는 서버 판정을 그대로 따른다(화면이 정하지 않는다).

---

## 4. 게이트 — `PayrollScreenSeparationGateTests` (7건 신설)

**작6 게이트가 못 본 것을 본다: 기능이 있는지가 아니라 ★어느 화면에 붙었는지★.**

| # | 게이트 | 막는 것 |
|---|---|---|
| ① | 화면 둘이 각각 제자리 | 라우트 합치기 |
| ② | 사이드바 진입점 2개 + **이름**(조회/관리) | 메뉴명 되돌리기 |
| ③ | **직원화면에 메일발송 없음** | 단추·다이얼로그·서비스 **3모양** |
| ④ | **직원화면에 발송대상 명단 없음** | 남의 수신주소 노출 |
| ⑤ | **직원 진입에 주소창 파라미터 없음** | 진입 로직 재개조 |
| ⑥ | 🔴 **대조군** — 회계화면엔 그 기능이 **있다** | "전부 삭제" 로 통과하는 가짜 |
| ⑦ | 결재팝업은 **회계**로 보낸다 | 목적지 되돌리기 |

### 🔴 반증 실측 — 초록불을 믿지 않았다 (가짜 게이트 누적 19번)

| 반증 | 방법 | 결과 |
|---|---|---|
| 1 | 직원화면을 **작6 버전으로 되돌림** | 🔴 **3건 FAIL**(③④⑤) |
| 2 | 메뉴명을 「급여 관리」로 되돌림 | 🔴 **1건 FAIL**(②) |

⇒ 봉합을 빼면 실제로 빨간불이 뜬다. **진짜 게이트다.**
⚠️ ⑥ 대조군이 없으면 급여 발송을 **통째로 삭제**해도 ③④⑤ 가 초록불이 된다.

---

## 5. 검증

| 항목 | 결과 |
|---|---|
| 빌드 | 🟢 **errors 0 · warnings 0**(헌법 #19) |
| 시험 | 🟢 **1011 통과 · 0 실패**(+7) |
| DDL | 🟢 **0건** — 표·컬럼 무접촉 |
| 기능 삭제 | 🟢 **0건** — 옮겼을 뿐 |
| 그룹웨어 잔재 | 🟢 **0건**(문자열 8종 검출 0) |
| 본인범위 | 🟢 서버 `ResolveScopeAsync` 실측 |

🔴 **개발PC 정상은 검증이 아니다.** 사장님 실측이 관문이다.

---

## 6. 사장님 실측 요청

1. **그룹웨어 → 급여 조회** — 직원 계정으로 들어가 **자기 명세서만** 보이는가. 발송 단추가 **없는가.**
2. **회계관리 → 급여 관리** — 대표계정으로 명세서 생성·결재 상신·발송이 되는가.
3. **결재 승인 팝업** — 대표이사 승인 시 「예」가 **회계 화면**으로 가는가.
4. 일반 직원이 회계 급여관리에 들어가면 **막히는가.**
