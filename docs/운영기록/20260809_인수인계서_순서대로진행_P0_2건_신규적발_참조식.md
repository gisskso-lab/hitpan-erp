# 인수인계서 — 2026-08-09 (일) · 순서대로 진행 · P0 2건 신규 적발

| 항목 | 내용 |
|---|---|
| 상태 | 🟢 **진실원** — 새 세션 **이것부터** |
| 작성 | PM · 2026-08-09 |
| 형식 | **참조식** — 상세는 링크로 |
| 브랜치 | `fix/watchdog-handoff-20260806` |
| 커밋 | 오늘 **5건** · **미커밋 0** (SOP [6] 준수) |
| 직전 | [8/07 인수인계서 2차](20260807_인수인계서_2차_사업구조_5대과제_전체그림_참조식.md) · [1차](20260807_인수인계서_배포종단검증_N10봉합_CS3축_참조식.md) |

---

## 0. 한 줄

> **사장님 지시 "순서대로 일 진행하자" 를 받아 4→2→3순위를 돌았고,
> 그 과정에서 P0 2건이 새로 나왔다. 둘 다 "고친 것이 고객에게 안 갔다" 는 같은 계열이다.
> 이후 사장님 "모두결재" 로 5건 전건 실행했고, N-10 미시험 C·D 까지 해소했다.**

---

## 0-1. ✅ 사장님 "모두결재" 이후 완료분 (세션 후반)

**§1-1 의 결재 5건이 전부 실행됐다.** 그 뒤 N-10 C·D 도 해소했다.

| # | 항목 | 결과 |
|---|---|---|
| 1 | **PR #68 승인·머지** | ✅ `cda11bd` — **소스 차이 0** (squash 라 해시 아닌 **내용**으로 검증) |
| 2 | 🔴 **백오피스 배포** | ✅ run `31301654467` — **404 → 401** 갈라짐 확인 |
| 3 | P0-2 빌드게이트 | ✅ `draft` + `environment: production` 2중 방어 |
| 4 | P0-1 디스크 검사 | ✅ `UpdateDiskSpaceGuard` 141줄 · 1.2.55 기준 5.3GB |
| 5 | P0-3 야간창 관측 | ✅ 하루 1줄 Information · **정책 무변경** |
| ➕ | **N-10 미시험 C·D** | ✅ 신규 테스트 14건 · **반증으로 유효성 증명** |

### 🟢 백오피스 배포 — 대조 실험으로 확정

| | 배포 전 | 배포 후 |
|---|---|---|
| `update-history` | 404 | 🟢 **401** |
| 없는 경로(대조군) | 404 | 404 |

**3개월간 허공으로 가던 워치독 보고가 도착한다.** `f934d34`(국세청 무응답 시 가입 전면차단 해소) 반영.

### ⚠️ 1차 배포는 실패했다 — 게이트가 정상 작동한 것

`여유 1967MB < 최소 2048MB`. 사장님 결재로 백업 2세대 + apt 정리 → **2,202MB**.
**8/02 백업 12개 전부 보존**, 서비스 3종 active.

> ⭐ 이 실패가 **오늘 워치독에 넣은 P0-1 과 같은 종류의 안전장치**였다. 서버엔 있었고 고객 PC 엔 없었다.

### 🟢 N-10 C·D 해소 — 통과를 반증으로 검증했다

**원인 실측**: `UpdateCheckStampFile` **전용 테스트가 0건**이었다. 시험할 도구가 없어서 미시험이었다.

**반증(mutation)으로 통과가 허수가 아님을 증명:**

| 주입 | 결과 |
|---|---|
| 미래 시각 방어 무력화 | `C_미래_시각` **1건만 정확히 실패** |
| 영속화 무력화(종전 인메모리 결함 재현) | **D 3건 전부 실패** |

주입은 즉시 되돌렸다 — **운영 코드 변경 0**(`git diff` 확인).

**채점표: 통과 5→7 / 미시험 4→2 / 실패 0.** → [검증리뷰서](../검증/20260809_검증리뷰서_N-10_미시험_CD_해소_실측.md)

### 🔴 별건 결함 2개 발견 (안 고쳤다 — 후속 대상)

| # | 결함 | 왜 미룸 |
|---|---|---|
| 1 | `deploy-web.sh` **순서 역전** — 세대정리(`:123`)가 여유검사(`:108`)보다 뒤라 도달 불가 | 별건. 막히면 **사람이 손으로 지워야만** 풀린다(오늘 실제로 그랬다) |
| 2 | `AddWatchdogServices()` 추출 미이행 | 병렬이슈 06 §7 — 안 하면 **3차 재발** |

---

## 1. 🔴 가장 중요 — 다음 세션이 **먼저 알아야 할 것**

### 1-1. 사장님 결재 없이는 더 못 나간다 (5건)

| # | 항목 | 왜 PM 이 못 하나 |
|---|---|---|
| **1** | 🔴 **PR #68 승인** (폰 1탭) | 1인 레포 self-approve 불가. **1순위 실측의 전제** |
| **2** | 🔴 **백오피스 NCP 배포 실행** | 인프라 조작(헌법 #29). **P0 4건이 서버에 없다** |
| 3 | P0-2 빌드게이트 방식 | (가)draft / (나)environment / **(가)+(나)** ← PM 권고 |
| 4 | P0-1 최소 여유공간 값 | 안전계수는 정책 |
| 5 | P0-3 관측 먼저 착수 | 🟢 정책 안 건드리는 범위 |

> ⭐ **2번이 가장 급하다.** `f934d34` *"국세청 무응답 시 가입 전면차단 해소"* 가 **서버에 없다.**
> 국세청 API 가 흔들리면 **신규 가입이 통째로 막힌다.** 베타 대상이 대리점 10곳인데.

### 1-2. 🔴 인수인계서 기록과 실제가 달랐던 것 3건

**받아쓰기 금지(헌법 #32)를 실제로 적용한 결과다. 다음 세션도 실측부터 할 것.**

| # | 기록 | **실측** |
|---|---|---|
| 1 | 병렬이슈 03 = 🔴미봉합 | ✅ **7/30 에 이미 해소** — 표기만 40일 낡음 |
| 2 | 백오피스 = *"미확인·가장 뒤처짐"* | **59화면·28컨트롤러로 규모는 있다.** 문제는 **미배포** |
| 3 | P0-3 야간창 | N-10 이 고친 게 **아니다** — §4-3 |

---

## 2. ✅ 오늘 한 것

### 2-1. 4순위 main 정렬 → **PR #68** ([작1](20260809작1_미반영분_main정렬_PR_작업지시서.md))

**순위를 뒤집은 게 아니다.** 1순위가 사장님 손을 필요로 하는 동안 대기하지 않았고,
**이것이 1순위의 전제**이기도 하다 — 실측할 신버전이 main 에서 빌드된다.

| 축 | 실측 |
|---|---|
| 미반영 | **21건** (소스 10파일 834줄 + 문서 11건) |
| 빌드 | ✅ **errors 0 · warnings 0** — 솔루션 **13개 프로젝트 전체**(PM 직접) |
| 충돌 | 우려한 squash 중복 → **없음** |
| CI | 🟢 **9/9 전건 통과 · `CLEAN`** |

> ⚠️ PR 본문에 **"N-10 실측 미완"** 을 명시했다. 통과로 위장하지 않았다.

### 2-2. 🔴 그 PR 에서 CI 가 결함을 잡았다 — [병렬이슈 06](../검증/병렬이슈/20260809_병렬이슈_06_워치독DI검증테스트_N10의존성_미추종_봉합완료.md)

```
Unable to resolve service for type 'UpdateCheckStampFile'
while attempting to activate 'HitPan.Watchdog.Worker'
```

| | |
|---|---|
| 운영 코드 | ✅ 정상(`Program.cs:80`) — **고객 영향 0** |
| 깨진 것 | 🔴 **워치독 조립 감시망** |
| 재발 | **2차.** 파일이 예고해 뒀고 1차는 **47일 방치** |
| 이번 | 🟢 **CI 가 그 자리에서** — 봉합 `aaf9bc7` · 실측 **105/105** |

> ⭐ **함께 드러난 것**: 검증팀이 `dotnet build` 로 0/0 을 봤으나 **빌드는 조립을 증명하지 않는다.**
> DI 그래프가 깊은 프로젝트는 **`dotnet test`** 까지 가야 한다.

### 2-3. 2순위 백오피스 → 🔴 [병렬이슈 07](../검증/병렬이슈/20260809_병렬이슈_07_백오피스_배포트리거_develop전용_main머지_미배포_미봉합.md) **(P0 · 미봉합)**

**뒤처진 이유가 화면 부족이 아니었다.**

```yaml
# deploy-ncp.yml
on:
  push:
    branches: [develop]     ← 🔴 develop 만 본다
```

**작업은 전부 `main` 머지다.** → 자동 배포가 **영원히 안 걸린다.**

| | |
|---|---|
| 마지막 배포 | **8/02** (전부 `workflow_dispatch` = 수동) |
| 수신부 작성 | **8/06** |
| 그 사이 자동 배포 | 🔴 **0회** — 아무도 안 눌렀다 |

**대조 실험으로 확정** (`TelemetryController.cs:19` 가 *"400 은 경로 고유 증상 아님"* 이라 경고해 둠):
```
404 <= /api/telemetry/update-history      ← 8/06 수신부
404 <= /api/telemetry/절대없는경로xyz      ← 존재하지 않음   ⇒ 동일. 라우트가 서버에 없다
```

**미배포 P0 4건**: `2fcada9`(업데이트기록 수신부) · `adbfa6b` · 🔴`f934d34`(가입 전면차단 해소) · `da77fec`

### 2-4. 3순위 P0 3건 원인규명 ([작2](20260809작2_P0_3건_원인규명_실측결과_작업지시서.md)) — **코드 무수정**

| # | 항목 | 실재 | 난이도 |
|---|---|---|---|
| 1 | 디스크 검사 (`grep` **0건**) | 🔴 | 중 |
| 2 | 빌드게이트 (draft·environment **전부 없음**) | 🔴 | **하** |
| 3 | 야간창 | 🔴 | **상 — 정책 재설계** |

---

## 3. 🔴 3차 재발 — 끊기는 자리만 옮겨간다

**오늘 나온 P0 2건이 같은 계열이다. 이것이 오늘의 가장 큰 발견이다.**

| 회차 | 구간 | 증상 |
|---|---|---|
| 1차 [병렬이슈 03](../검증/병렬이슈/20260730_병렬이슈_03_랜딩404_봉합코드_main미반영_봉합완료.md) | 커밋 ~ main | 코드는 정상인데 main 미포함 |
| 2차 [작1](20260809작1_미반영분_main정렬_PR_작업지시서.md) | 커밋 ~ main | `ab566a4` 21건 미반영 |
| **3차** [병렬이슈 07](../검증/병렬이슈/20260809_병렬이슈_07_백오피스_배포트리거_develop전용_main머지_미배포_미봉합.md) | **main ~ 서버** | main 에 있는데 **배포 안 됨** |

> 🔴 **"고쳤다" 와 "고객에게 갔다" 사이가 계속 끊긴다.**
> 1·2차를 커밋 구간에서 막았더니 **배포 구간에서 터졌다.**
> ⇒ **다음 세션은 "고쳤나"가 아니라 "갔나"를 먼저 물어야 한다.**

---

## 4. 다음 세션 첫 일감 (🔄 세션 후반 반영해 갱신)

| 순위 | 일감 | 상태 |
|---|---|---|
| **1** | 🔴 **N-10 A·B 백지환경 실측** | **유일하게 남은 미시험.** §4-4 — 사장님 손 3회 |
| **2** | PR #70 승인·머지 | 문서만 · CI `CLEAN` · 급하지 않음 |
| 3 | `deploy-web.sh` 순서 역전 봉합 | 별건 §0-1 |
| 4 | `AddWatchdogServices()` 추출 | 병렬이슈 06 §7 — 3차 재발 방지 |

> ✅ 종전 1·2순위(PR #68 · 백오피스 배포)와 4순위(P0 3건)는 **전부 완료**됐다(§0-1).

### 4-4. 🔴 A·B 실측은 게시를 두 번 해야 한다 (8/09 실측으로 확인)

```
updates.hitpan.kr/manifest.json  →  1.2.55 (8/06 게시)
N-10 봉합                        →  8/07 (e3342b9)
```

**현재 게시본에 N-10 코드가 없다.** 발견할 워치독 자체가 N-10 을 가져야 한다.

```
[1] 1.2.56 게시(N-10 포함) → 사장님 승인   ⏱ 약 25분
[2] 백지환경 설치           → 사장님 Sandbox
[3] 1.2.57 게시(미끼)      → 사장님 승인   ⏱ 약 25분
[4] 재시작 없이 60분 대기   → 로그에서 발견 확인
```

**총 2시간 반 · 사장님 손 3회.** ⚠️ 자정 넘기지 말 것.

> 🟢 **C·D 는 이미 끝났으므로 그 60분에 A·B 만 보면 된다.**

### 4-1. N-10 실측 — 전제 하나가 오늘 확인됐다

```
updates.hitpan.kr/manifest.json  →  200 · 1.2.55 · 서명 upd-v1
updates.hitpan.kr/packages/HitPan-ERP-Setup-1.2.55.exe  →  200
```

**게시 배선은 살아 있다.** 실측할 판은 준비돼 있다.

⚠️ **도메인 혼동 주의** (PM 이 오늘 한 번 헛짚음):
`landing.hitpan.kr/packages/*` 의 404 는 **Blazor SPA 폴백으로 정상**이다.
게시원은 🔴 **`updates.hitpan.kr`** 이다(`UpdateClient.cs:13`).

### 4-2. 실측 미시험 4건은 그대로다

[검증리뷰서 §6](../검증/20260807_검증리뷰서_작2_N-10_발견주기_4안혼합.md) — **A**(재시작 없이 발견) · B · C · D.
**A 를 도는 60분 동안 C·D 병행하면 1회 세션으로 전부 해소된다.**

### 4-3. ⚠️ 야간창 오해 금지

N-10 상한 60분이 야간창을 덮는 건 사실이나 **"PC 가 켜져 있으면"** 이다.
**사무실 PC 는 밤에 끈다.** 더 나쁜 건 침묵이다 —
`TrackUpdateStallAsync`(`Worker.cs:638-642`)는 **feed 조회 성공만 본다.**
고객이 옛 버전에 고정돼도 **카운터 0 → 7일 경보 안 울림.**

---

## 5. 오늘 커밋 5건

| 커밋 | 내용 |
|---|---|
| `695f1f0` | [2] 작1 main 정렬 작업지시서 |
| `aaf9bc7` | **워치독 DI 검증 테스트 봉합** (CI 적발) |
| `bf65936` | [3-V] 병렬이슈 06 신규 + 03 상태정정 |
| `e7105de` | [2] 작2 P0 3건 원인규명 |
| `1ea283e` | [3-V] **병렬이슈 07** 백오피스 미배포 |

---

## 6. ⚠️ PM 오류 기록 (오늘 1건)

| 오류 | 교훈 |
|---|---|
| 백오피스 실측 중 **`landing.hitpan.kr`** 에서 `packages/*` 를 찾아 404 를 오판 | **도메인마다 역할이 다르다.** 게시원은 `updates.hitpan.kr`. 문서에 남겨 다음 사람이 같은 오판을 안 하게 했다 |

> 🟢 **스스로 잡았고 문서에 남겼다.** 헌법 #32 — 실측이 추측을 이겼다.

---

## 7. 문서 성격

- §2 실측값은 **PM 이 직접 명령을 실행한 결과**다. 받아쓰기 0
- §3 재발 분석·§4 순위는 **PM 해석**
- 🔴 표시는 **사장님 결재 대기** 또는 **고객 영향 진행 중**

---

**PM · 2026-08-09**
