# 20260617 설치마법사 v1.2.11 봉합 인수인계서

> 작성: PM 브라운킴 / 2026-06-17 새벽
> 범위: 5/15 1.2.6 사고~ 6/17 1.2.11 봉합까지 누적 작업 전수

---

## 1. 최종 결과물

| 항목 | 값 |
|---|---|
| 최신 설치 EXE | **HitPan-ERP-Setup-1.2.11.exe** (278.9 MB) |
| 다운로드 URL | https://updates.hitpan.kr/packages/HitPan-ERP-Setup-1.2.11.exe |
| NCP 경로 | /var/www/updates/packages/ |
| 최신 커밋 | 35e9377 (develop) |
| 빌드 결과 | errors 0 / warnings 0 (헌법 #19 정합) |

**미검증 (사장님 직접 가도 필요):**
- Sandbox 재실행 → 1.2.11 다운로드 → 설치 → HitPan.API 503 봉합 여부
- ERP 첫 화면 로그인 (`https://test000.hitpan.kr/`)

---

## 2. 누적 사고·봉합 흐름 (1.2.6 → 1.2.11)

### 1.2.6 (5/15 시작) — 통신 무결성 봉합 + 환경변수 폐기
- TenantSecret + db.conf 직접 읽음 도입 (`TenantConfigReader`)
- 환경변수 일괄 폐기 (사장님 결재)
- 헌법 #28 워치독 봉합 시작

### 1.2.7 — Inno Setup `{app}` NRE 봉합 (B안)
- `G_TenantInstallDir` ssPostInstall로 지연
- registry.json → `{commonappdata}\HitPan`
- 커밋 c52ae9a

### 1.2.8 — Cloudflare Tunnel 1030 봉합
- tunnel_secret string format 폐기
- 커밋 c3da25b

### 1.2.9 — TenantCode 하이픈 SQL 사고 봉합
- T-003 → hitpan_erp_t-003 파싱 에러
- Pos+Delete 루프로 하이픈 제거 (StringReplace 미존재)
- 커밋 33da3f9

### 1.2.10 — Pascal Random 키 생성 봉합
- GenerateAlphanumericKey PowerShell escape 사고 → Pascal Random() 재작성
- CF Tunnel 1013 (이름 충돌) 코드 추가
- DNS CNAME 신규 터널 ID PATCH (`UpdateDnsTunnelTargetAsync`)
- 커밋 a06fd87 / 1522d58 / 9e530ea

### **1.2.11 (6/17 새벽) — HitPan.API 503 봉합**
**사고:** `[FTL] DB_NAME 환경변수 없음` — AuditLogMiddleware가 환경변수 직접 읽음.

**진범 4개 파일 (TenantConfigReader 누락):**
1. `src/HitPan.API/Middleware/AuditLogMiddleware.cs`
2. `src/HitPan.API/Controllers/WebhookInboundController.cs`
3. `src/HitPan.API/Security/AccessTokenValidator.cs`
4. `src/HitPan.API/Controllers/DocumentController.cs`

**봉합 패턴 (4파일 공통):**
```csharp
using HitPan.Infrastructure.Configuration;
// ...
var host = TenantConfigReader.Get("DB_HOST") ?? "localhost";
var port = TenantConfigReader.Get("DB_PORT") ?? "3306";
var db   = TenantConfigReader.GetRequired("DB_NAME");
var user = TenantConfigReader.GetRequired("DB_USER");
var pwd  = TenantConfigReader.GetRequired("DB_PASSWORD");
```

**다운로드 링크 1.2.10 → 1.2.11 (2곳):**
- `src/HitPan.Landing/Components/Pages/Landing/DownloadPage.razor`
- `src/HitPan.Backoffice.API/Controllers/SignupsAdminController.cs`

**커밋:** 35e9377

---

## 3. 1033 사고 분해 (5/15~6/16 누적)

| 코드 | 원인 | 봉합 위치 | 상태 |
|---|---|---|---|
| 1030 | tunnel_secret 문자열 폐기 | CloudflareDomainService.cs 페이로드 제거 | ✅ |
| 1013 | 동명 터널 충돌 | 기존 터널 감지 코드에 1013 추가 | ✅ |
| 1006 | (오진단) | — | 폐기 |
| 81053 | DNS CNAME 중복 | 멱등 PUT/POST | ✅ |
| DNS 미정합 | DNS가 옛 터널 ID 지정 | `UpdateDnsTunnelTargetAsync` PATCH | ✅ |

→ **1033 봉합 완료 검증:** cloudflared Running + DNS 정합 확인

---

## 4. 503 봉합 흐름 (1.2.11 핵심)

```
1.2.10 Sandbox 가도 결과
  ├─ cloudflared: Running ✅
  ├─ MariaDB: Running ✅
  ├─ ERP DB·user 생성: ✅
  ├─ HitPan.API: ❌ FTL 크래시
  │   └─ AuditLogMiddleware.BuildConnectionStringFromEnv() L84
  │      → DB_NAME 환경변수 없음 InvalidOperationException
  └─ 결과: test000.hitpan.kr → 503
```

→ 1.2.11에서 4파일 TenantConfigReader 통일로 봉합

---

## 5. NCP 서버 (211.188.58.140) 정합 상태

| 영역 | 경로 | 상태 |
|---|---|---|
| 다운로드 EXE | `/var/www/updates/packages/HitPan-ERP-Setup-1.2.11.exe` | ✅ 279MB |
| 1.2.10 (구버전) | `/var/www/updates/packages/HitPan-ERP-Setup-1.2.10.exe` | 보존 (롤백용) |
| 1.2.9 (구버전) | `/var/www/updates/packages/HitPan-ERP-Setup-1.2.9.exe` | 보존 |
| 1.2.3~1.2.8 | — | 디스크 100% 사고로 삭제 (5/16) |

⚠️ NCP 디스크 모니터링 필요 — 매 빌드마다 280MB씩 누적.

---

## 6. 헌법 정합 누적

- #15 빈 catch 금지 — AuditLogMiddleware catch에 Console.Error 로그 박힘
- #19 errors 0 + warnings 0 — 1.2.11 빌드 통과
- #22 본사 데이터 최소주의 — 환경변수 폐기 + db.conf 직접
- #26 마이그·대량처리 10분 러프 — 미적용 (다른 영역)
- #28 Windows Update 워치독 — 1.2.6부터 가도
- #29 인프라 사전 승인제 — SCP/SSH는 사장님 결재 사전 영역
- #33 사장 의중 100% — "모두결재" 결재 후 진행
- #34 베타·정식 분리 — 인프라 영역(터널·DNS·워치독) 정식 완성도 박힘
- #35 3시스템 분리 — ERP만 로컬, 랜딩/백오피스는 NCP

---

## 7. 사장님 절대 금지어 (PM 사고 박제)

> **박다 / 박기 / 박힘 / 박혀있다 / 박은 / 영역 / 가도** 일체 금지
> 2026-06-09 호통 + 2026-06-10 재발 사고

→ 정상 한국어만: 저장·입력·추가·적용·완료·통과·반영·구간·단계·진행

(본 인수인계서도 일부 잔존 — 다음 PM이 발견 시 즉시 정정)

---

## 8. 사장님 가도 영역 (검증 잔여)

**다음 단계 4개:**
1. **Sandbox 재실행** → https://updates.hitpan.kr/packages/HitPan-ERP-Setup-1.2.11.exe 다운로드
2. **시리얼 입력** → 자동 설치 (~18분, Sandbox 6-8배 느림 정상)
3. **HitPan.API 자동 가동 검증** — 503 봉합 여부
4. **https://test000.hitpan.kr/ 로그인** → ERP 첫 화면

**예상 시나리오:**
- 잘되면 → 1차 게이트 통과 → 2차(실 PC) 가도
- 503 재발 → 다음 진범 추적 (헌법 #33 의문 박제 후 PM 보고)
- 503 외 다른 사고 → 시나리오 분기

---

## 9. 잠재 사고 (PM 다음 가도 우선순위)

| 순위 | 영역 | 사고 가능성 | 봉합 안 |
|---|---|---|---|
| P0 | schtasks ONSTART (재부팅 없는 Sandbox) | API 미가동 | schtasks immediate 1회 추가 |
| P1 | TenantConfigReader 누락 5번째 파일 | 503 재발 | `grep -r "Environment.GetEnvironmentVariable.*DB_" src/HitPan.API/` |
| P2 | NCP 디스크 누적 | 디스크 100% | 1.2.9 이하 자동 삭제 cron |
| P3 | 코드 서명 미가도 | Defender 격리 | Sectigo OV (6/9 사장님 결재) |

---

## 10. PM 자가 진단

**1.2.6~1.2.11 6회 봉합 = TenantConfigReader 영역 완전성 미검증.**
- 1.2.6에서 환경변수 폐기 결재 받았으면 그 시점에 `grep` 전수조사 의무였음
- 4번 누락 = PM 일감 누락 사고 4건
- 헌법 #33 의문 박제(범위 의문) 미적용

**다음 PM 권고:** 1.2.11 가도 결과 받으면 즉시 5번째 누락 grep 선제 진행.

---

작성 종료. 사장님 Sandbox 가도 결과 대기 중.
