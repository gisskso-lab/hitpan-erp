# 20260826보고1 — 일일작업보고서 (8/25 세션 + 8/26 실측)

> **[7] 일일작업보고서** — **다음날 컴퓨터 켜고 여는 문서.**
> 같은 날 새 창 열 때는 [★] 인수인계서를 본다 → 이 보고서는 그 인계서들을 **주소로 참조**한다.
> 작성 PM 브라운킴 · 2026-08-26

---

## 1. 한 줄 요약

**매입반품이 확정될 길을 뚫고 창고분리를 되살려 1.3.19 를 게시했다. 8/26 사장님 실측 결과 — 재고 4/4·로그인 통과, 매입은 1/5(반품 목록 500 으로 전면 반려).**

---

## 2. 오늘의 판정 — 🔴 사장님 실측 (2026-08-26)

| 파트 | 결과 |
|---|---|
| 매입 | 🔴 **1/5** — 조회유형만 통과 |
| 재고 | 🟢 **4/4 전부 통과** |
| 로그인 | 🟢 통과 |

### 🔴 매입 반려 4건

| 건 | 증상 |
|---|---|
| ① 매입반품 목록 | `500 Internal Server Error` — `[작18] 매입반품 목록 조회 실패` |
| ② 반품 전환 | `400` *"이미 반품 전환된 매입명세서입니다 (매반-20260825-001)"* |
| ③ 확정 | **눌러볼 수조차 없음** (목록이 안 떠서) — 이번 차수 핵심이었는데 검증 불가 |
| ④ 담당자 열 | 발주서·매입처리·반품처리 그리드에 **미반영** |
| ⑤ 조회유형 | 🟢 통과 (5화면 날짜조회) |

> 🔴 **②번이 결정적 단서다.**
> "이미 전환됨 + 반품번호"가 떴다 = **반품서는 DB 에 실물로 있다.**
> ⇒ **쓰기는 성공했고 읽기가 죽었다.** ①③④가 **원인 하나**일 가능성이 크다.
> ⇒ 이 400 은 버그가 아니라 **중복가드 정상동작**일 수 있다. 다만 목록이 안 떠서
>    이미 만들어진 반품서에 **도달할 길이 없다** — 사장님 눈엔 "반품이 안 된다".

### 🟢 재고 4건 전부 통과
조회유형 · **창고분리(오류 없이 뜨고 KPI 4개 채워짐 — 두 번 죽었던 자리)** · 창고관리 우편번호·카카오내비 · 수불부종류 콤보 3화면

---

## 3. 8/25 세션에 한 일 (커밋 기준)

| 커밋 | 작업 | 내용 |
|---|---|---|
| `541e41eb` | 인계7 | 8/25 인수인계서 |
| `e657a1e2` | **작19** | **창고분리 부활** — 서버는 배열, 화면은 객체로 받아 `[` 에서 즉사. 증상 4개가 원인 하나. + 로그인 배너 제거·재고현황 조회유형·창고 우편번호(DB-110) |
| `9314eaee` | **작18** | **매입반품 확정경로** — 목록 「확정」만 분기 누락돼 **매입반품 ID 를 매출 API 로** 보냄. 확정될 길이 아예 없었다 |
| `67cbbc7c` | 인계6 | 1.3.18 게시 |
| `142d7753` | 작17 | 매입 리포트 18종·회계 6곳에 반품 반영 |
| `d03ca9e7` | 작16 | 매입을 판매와 같은 축으로 |
| `7945c2b7` | 작14·15 | 반품확인 메뉴 제거 + 500 원인 확정(bool→int) |

**빌드 0/0 · 944 통과(+29) · CI 전부 success(DDL Smoke 포함) · 미커밋 0**

---

## 4. 게시 — 1.3.19 실물 확인

| 검사 | 결과 |
|---|---|
| 매니페스트 version | **1.3.19** |
| channel | Major (팝업 Y/N — 헌법 #43) |
| requiresMigration | 🔴 **true** |
| zip | 206 · 104,867,292 bytes |
| sha256 실측 대조 | 🟢 일치 (`93fce492…`) |
| DB-110 패키지 실재 | 🟢 `api/Migrations/SQL/DB-110_warehouse_address.sql` |

"게시했다"가 아니라 **실제로 받아서 해시까지 맞췄다.**

---

## 5. 🔴 내가 틀린 것 (다음 세션이 되밟지 않게)

1. **작18 초안이 매니저 반증에 3개 다 깨졌다** — 또 **판매 코드를 근거로 매입을 판단**했다. 8/25 에만 이걸 4번째 반복.
2. **트랜잭션을 가짜로 만들었다** — 커넥션이 달라 트랜잭션이 성립 안 했다. **옆 함수에 정답이 있는데 다르게 짰다.**
3. **가짜 게이트를 한 세션에 두 번** — 주석 낱말 검사 · 이름 앞부분 검사. 누적 16번째.
4. **검증 중에 대상물을 고쳤다** — SoD 위반. 만든 사람이 검증하면 안 되는 이유를 내가 증명했다.

---

## 6. 참조 — 오늘 남긴 문서

| 문서 | 무엇 |
|---|---|
| [20260825인계7](20260825인계7_매입반품확정경로_창고분리_인수인계서.md) 🟢 | **[★] 8/25 인계 — 진실원** |
| [20260826작1](20260826작1_회계계산서2차_스케줄표.md) 🆕 | **8/26 스케줄표 — 매입2차·회계2차·계산서2차** |

---

## 7. 내일(8/26) 착수 — 스케줄표대로

```
A 매입 2차 마무리 ──▶ B 회계관리 2차 ──▶ C 계산서관리 2차 ──▶ (시간 남으면) D 자료관리 마이그
```

**A-1 착수 순서** — 🔴 추측 금지: **DB-110 마이그 적용여부 → 서버 로그 → 증상 모양 대조**
(콘솔은 "500"까지만 말한다. 8/25 에 다섯 번 틀린 자리 — 진범은 마이그 미적용이었다)

## 🔴 사장님 결재 대기

1. **급여관리** — 그룹웨어에 이미 있다(`/hr/payroll`). 회계는 **링크만**인가 **화면을 또 그리나**
2. **경비관리** — 경비 화면이 **두 개**(회계 139줄 · 그룹웨어). 정본이 어디이며 **회계 기표까지** 포함인가
3. (이월) 재고이송 2건 · 로그인 하단 버튼 · 작15 우회로
