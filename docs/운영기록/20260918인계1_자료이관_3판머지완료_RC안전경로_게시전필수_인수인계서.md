# 인수인계서 2026-09-18 (인계1) — 자료이관 장부반영 분류 3판 **main 머지 완료** · 게시 전 필수 1건 · 게시·재이관 결재 대기

> ⬛ **흡수** → `20260921인계1_…`(9/21 · RC 안전 경로 진행 중). 아래는 당시 기록 그대로.
> 세션 시작 즉시 §0 → §4 → §9. 상세는 작업지시서 `20260915작1_…` §14-11~§15-17 (절 단위로만).

## §0. 30초 요약
- 9/17 사장님 결재 R-C3·R-A4·R-A5·R-A6 · S2 · **전결 2회**("진행해. 전결!!" · "전결!!!! 모두 루틴대로") → [2] 설계 3판 → [3] R1·R1b·R2·R3·R4·Z → [3-V] 병렬이슈 42~46 봉합 → [4] 1단계·2단계A·A′·B → 병렬이슈 50(기대값 정정)·52(봉합) → [5] CTO 🟡조건부 → **PR #404 CI 전부 통과 → main 스쿼시 머지 `bf0f7c85`**.
- 사장님 원칙 추가(9/17): **레거시 수금·지급 줄은 하나도 빼지 않는다** — 목록 필터 0 · 이관 줄 삭제 거절 · 이월잔액에서 줄어드는 건 이관 뒤 입력분만.
- 🔴 **아직 게시 안 함 · 사장님 PC 재이관 안 함** — 둘 다 전결 밖(§4-2).

## §1. 현재 상태 (실측 2026-09-18)
| 항목 | 값 |
|---|---|
| main | `bf0f7c85` (#404) — PR head `f9025511` 과 트리 diff 0 확인 |
| CI (#404 head) | Build 0/0 · DB 게이트 291/291 skip 0(G8 24 · G9 6 · G10 · LabelParity 포함) · DDL Smoke · CodeQL · Watchdog · 번호 충돌 · TruffleHog 전부 pass |
| 로컬 전체 시험 | 1,513 중 실패 5 → 백업 게이트 4 = 비번 없는 root 환경(비번 계정 8/8) · `PurchaseReturnInReportsGate` 1 = 글자 게이트가 공용 식 이동을 못 따라감 → 게이트 수정 `f9025511` 99/99 |
| DB | DB-123(표 추가만) · 3판 DDL 0 · DB-121·122 메인PC 트랙 예약 유지 |
| 격리 DB | `C:/HitPanBuild/mariadb-iso-0917` 127.0.0.1:33306 root 비번 없음 · 계정 `v4`/`V4iso!2026` · `hitpan_e2e_gi`(출하 DDL) · `hitpan_v4_a`·`hitpan_v4_b`·`hitpan_v4_a2`(재이관 결과) · 세션 끝나면 프로세스 꺼짐 → `mariadbd --defaults-file=…/my.ini` 로 재기동 |
| MDB 사본 | `C:/HitPanBuild/mdb-0917/`(읽기 전용) · 조회 `scripts/readonly/20260917_mdb_select.ps1`(비번 `MdbMigrationService.cs:55` 주석) |

## §2. 한 일 (주소만)
- 결재: 작지 §14-12(R-C3·R-A5·R-A6) · §14-13(R-A4·전결) · §15-9(PM 3판 결재) · §15-10(S2) · §15-15(전결 확대) · §15-16(CTO 조건 조치)
- [2] 설계 §19~§27 · 작지 §15-0~15-8
- [3] 개발명세서 `docs/개발/erp/20260917작1_갈래{R1,R2,R3,R4}_*.md`(R1b 는 R1 §7) · Z 는 작지 §15-14
- [3-V] 병렬이슈 42~46 · [4] 작업리뷰서 §8~§11 · 병렬이슈 50·52 · 검증계획서 §8 · [5] 리뷰서 §12

## §3. 결정된 것 (다시 논의 금지)
- 이월 매칭 키 = partner_id · 지급 = `payment_type='legacy_balance'` + `ref_order_id=partner_id`(DDL 0)
- 남은 이월잔액 R = L0 − M(이관 뒤 매칭 · is_active) — 11곳 공용 · 거래처 잔액(#5)은 무수정(자동 1회 감소)
- 서버 거절: 초과 · 방향 · 기준일 **당일까지**(S2 `<=`) · 유형 값 목록 밖 · 금액 ≤0·소수 3자리 · 이관 수금·지급 삭제
- 재고 이어 계산 규칙③ · 입고 없는 달 S_IDAN 은 **그 달 움직인 키만** · S_IDANB 폴백 없음 · 기준일 min(달 말일, 이관일 로컬)
- 대사표: F2 「사유 확인됨」 · 정보 행 INFO「참고」 · 합계 비교 = 잔액 + M(#9 와 같은 규칙 · 병렬이슈52)
- 동시 수금: READ COMMITTED + 이월잔액 행 FOR UPDATE (SHARE MODE 는 교착 실측 → 폐기)

## §4. 🔴 미결 — 이것부터
| # | 항목 | 누가 |
|---|---|---|
| 4-1 | 🔴 **게시 전 필수 — RC 안전 경로**: 수금·지급 등록이 전부 READ COMMITTED → `log_bin=ON`+`binlog_format=STATEMENT` 서버면 **모든 수금·지급 등록 거절**(#20 P0). 설치본은 기존 MariaDB 재사용(`HitPan-Universal.iss:115` `NeedsMariaDB`). 이 PC 운영은 log_bin=0·MIXED. → [1-V](고객 설치 조합 실측 가능 범위) → [2] 감지·폴백 설계 → [3] + 게이트(STATEMENT 인스턴스로 동작 확인) → [4]. ⚠️ **다른 트랙이 main 에서 먼저 게시하면 이 위험이 같이 나간다** | PM → 루틴 |
| 4-2 | 게시(자동업데이트 · Major 팝업 흐름 #43) · 사장님 PC 덮어쓰기 재이관 시점 — **전결 밖 · 사장님께 여쭘** (4-1 끝난 뒤) | 사장님 |
| 4-3 | 사장님 PC 재이관 때 화면 확인 3가지: 수금·지급 삭제 버튼 · 기준일 이전 날짜 화면 입력 문구 · 대사표 「참고」 표시 | 사장님 PC |
| 4-4 | 후속 실측: 명세서 확정취소 뒤 partner_balance API 실경로(#11) · 병렬이슈52 봉합 뒤 API 재실측 · CTO 미확인 2(확정취소↔수금 등록 동시 · 이관 매입 새 반품 확정 시 이월잔액 검사) | [4] |
| 4-5 | 별건(9/17 인계1 §4-5 그대로): `localhost:5234` 자료이관 404 · 알림 연결 실패 · 사람 임시 매출 명세서 수정 저장 트랜잭션 오류 | [1-V] |

## §5. 함정 (이번에 새로)
1. **에이전트 턴 한도로 문서 0줄 멈춤 4번** — 「N턴마다 결과 먼저 문서에」「대기는 포그라운드 블로킹(모니터 알림은 서브에이전트를 못 깨운다)」를 착수 지시문에 넣어라. hp-judge 는 25턴 · hp-verifier 50턴.
2. **갈래는 지정 회귀만 돌려 전체 시험에서 글자 게이트가 깨졌다** — 머지 전 전체 시험 1회를 작업지시서에 넣어라.
3. **에이전트가 스스로 추가한 규칙(RC)이 영향 범위를 넓혔다** — 이월 매칭만이 아니라 모든 수금·지급 등록에 걸림. 봉합 방식 변경은 영향 경로를 PM 이 세라.
4. **검증자가 메인 레포를 치환**(몇 분 · 원복 확인) — 봉합 빼기는 detached worktree 에서만 · 커밋 뒤에만(R1b 는 `git checkout --` 로 미커밋 수정을 날림).
5. **기대값 오기**: 「재고금액 음수 행 0」 — 레거시에 음수 재고 19행이 원래 있다. 기대값도 레거시로 먼저 확인.
6. **환경 차를 회귀로 읽을 뻔** — 이관 989s 는 새 격리 DB 기본 설정 탓 · 같은 조건 대조군(base/new 교차 4회)으로 판정(3판 −7%).
7. 이관 도구는 DB 이름 `hitpan_e2e`·`hitpan_trgtest` 만 · 빈 비번 계정 거절 · `HITPAN_MDB_FOLDER` 미지정 시 **원본 MDB** 를 연다.
8. 실사용 경로 시험: 사원 0명 → users 1행 직접(BCrypt) · 약관 동의 · `is_main_pc=1` 없으면 전부 403 · Web publish 0바이트 `.br` 가 화면 97% 정지.
9. Git Bash `sed -i` 는 CRLF 파일을 LF 로 바꿔 「수정됨」만 남긴다 — `git diff` 0 확인 후 checkout.

## §6. 건드리면 안 되는 것
- 운영 DB `hitpan_erp_t004`(3306) · 이 PC `HitPan.API`·`HitPan.Watchdog` 서비스 · 원본 MDB `Desktop/공영정보DB` · appsettings(#21) · DB-121·122 번호.

## §8. 백로그
- **V7 이관 세션 튜닝 실패(기존)**: `SET SESSION innodb_flush_log_at_trx_commit` 거부 → 같은 try 의 `foreign_key_checks=0`·`unique_checks=0` 도 빠짐 → 이관 속도가 고객 DB 설정 의존(#26 10분).
- 수금 구분 이름 매핑(카드→은행이체 · 기타→카드 · 지급 B·F→현금) — 금액·분개 무관 · 기존 이관 규칙.
- 레거시 음수 재고 19행 ↔ 히트판 18행 행 단위 대조(합계는 일치).
- 확정취소 경로 수금일 달 마감 검사 없음 · 월마감 검사 SQL 중복(CollectionService private) · 기존 고객 partner_balance 옛 오차 치료.
- 로그인 응답 role `system_admin`(DB `TenantAdmin` · `AuthService.cs:500` 기존 매핑) — 판정 안 함.
- 설계문서 459줄(상한 300) 분할 · `.claude/worktrees/agent-*` 21개 정리(사장님 확인 후) · 9/17 인계1 §8 항목 그대로.

## §9. 다음 세션 첫 행동
```bash
git -C "C:/Users/소순근/Desktop/hitpan-erp" fetch origin --quiet && git -C "C:/Users/소순근/Desktop/hitpan-erp" log -3 --format="%h %s" origin/main
```
main 이 `bf0f7c85` 이후 이 인수인계서 커밋이면 그대로다. **그다음**: 사장님께 §4-1 RC 안전 경로 착수(루틴) 보고 → [1-V] 선행검증(hp-verifier · 격리 인스턴스에 `log_bin=ON`·`binlog_format=STATEMENT` 로 띄워 현재 코드의 수금 등록이 실제로 거절되는지부터 재현) → [2] 설계. 게시·재이관(§4-2)은 4-1 끝난 뒤 여쭙는다. **결재 없이 코딩 금지.**

---
**작성**: PM 브라운킴 · 2026-09-18 / **상태**: ⬛ 흡수(→ 20260921인계1)
