# 종합 인수인계서 — 3시스템 연결축 전수파악 + 끊김 전수판정 (2026-06-30)

> 작성: PM 브라운킴 / 다음 세션 0초 인계용
> 핵심 한 줄: **연결축 핵심 흐름에 새 끊김 0건. 진짜 미구현은 K3·K4(고리4②③)뿐 — 어제 기지·결재완료·B갈래 예정.**

---

## 1. 오늘 한 일 (SOP [1] 전수조사)

사장님 지시: *"개발진도 핵심축 파악부터 해"* → 3시스템 연결축을 개발팀 4병렬로 코드 전수 파악.
모든 결과는 추측 0, 파일:라인 근거. 코드 변경 0건(읽기만).

### 빌드 실측 (결재 불필요 읽기성)
- 3시스템 핵심 5개 빌드: **경고0/오류0**
- 솔루션 전체 14개(HitPan.sln): **경고0/오류0**
- → 컴파일 게이트(헌법#19) 완전 통과. 단 "빌드됨 ≠ 작동함".

### 위치 그림 확정 (사장님 5번 못박음)
- **NCP 클라우드**: 랜딩(landing/www.hitpan.kr) · 백오피스(back/api.back.hitpan.kr) · 업데이트(updates.hitpan.kr)
- **고객 PC 로컬+터널**: 히트판 ERP만 (demo/api-demo.hitpan.kr)
- ⚠️ PM 오판 2회(백오피스 로컬→NCP, "로컬 백오피스 구간"). 사장님이 잡음. **재발 금지: appsettings의 localhost는 개발 기본값일 뿐, 운영은 NCP 환경변수.**

### NCP+로컬 읽기 점검 (실측)
- 랜딩 200 / 백오피스 200 / ERP-Web(터널) 200 / ERP-API(터널) 200 → 핵심축 살아있음
- 업데이트 403(서버는 살아있음, 매니페스트 인증경로 추정) / www·api.back DNS 미해결(미발급 추정) → 단정 안 함

---

## 2. 연결축 핵심축 지도

상세: `docs/개발/erp/20260630_3시스템_연결축_핵심축지도_전수파악.md`

```
랜딩 가입(SignupPage:477) → 백오피스 signup(LandingSignupController:51)
 → 승인+시리얼발급(SignupsAdminController:131) + 도메인발급(CloudflareDomainService.IssueAsync)
 → 라이선스클레임(LandingPublicController:143) → 부트스트랩토큰
 → ERP 로컬반영(CompanyBootstrapController:42, local_company/local_subscription)
 → 부모계정 원자생성(:198, users+emp+wh+accounts)
 → 설치부트스트랩(BootstrapInstall.ps1:81 → 터널 자동발급)
 → 로그인(AuthController:35, JWT account_type=tenant_admin/tenant_user)
 → 업데이트(Watchdog → updates.hitpan.kr → 채널분기 → 동의 → 백업 → [Velopack 미구현])
```

헌법 정합 확인: #22(사업자정보 ERP로컬만, 백오피스 해시·메타만) / #29(터널토큰 EXE미저장) / #38(account_type 2종만) / #37(reseller_id 수신캐시 정상).

---

## 3. 끊김 의심 5건 → 전수 판정 (교차검증)

| # | 의심 | 판정 | 근거 |
|---|---|---|---|
| K1 | desired_domain 컬럼 누락 | 🟢 거짓경보 **완전종결** | 출하DDL `installer/backoffice/00_backoffice_core.sql:155` 컬럼 존재 + **NCP 운영DB 쐐기확인 완료(2026-06-30)** — `hitpan_backoffice.landing_signups.desired_domain varchar(255) YES NULL` 실측. 신규설치·운영 양쪽 정상. |
| K2 | 승인시 tenant_domains INSERT 없음 | 🟢 거짓경보 | domain_alias가 가입시점 tenants에 저장(LandingSignupController:249). tenant_domains는 보조 |
| K3 | Velopack 실적용 미구현 | 🔴 진짜 미구현 | UpdateOrchestrator.cs:102 Stub. **어제 기지, B갈래 예정** |
| K4 | MigrationRunner 배선 미등록 | 🟡 **P1 부분완료(2026-06-30)** | DI 등록 완료(Program.cs:75, 커밋 4fe93a3, 수동호출만·자동실행0). **실호출 배선(소비처)·실DB검증은 M4 후 고리4②③** |
| K5 | OTP/결제 stub | 🟢 의도된 정책 | 베타1 결제 제외(2026-06-08 결재). 가입→승인→시리얼은 결제없이 완결 |

**결론: 5건 중 거짓경보·정책 3건(K1·K2·K5), 진짜 미구현 2건(K3·K4)은 어제 이미 잡힌 고리4②③.**
→ **연결축 핵심 흐름에 새 끊김 0건.** 받아쓰기로 봉합했으면 멀쩡한 K1·K2·K5 건드려 두더지잡기 할 뻔(교차검증이 차단).

---

## 4. 다음 세션 할 일 (우선순위)

1. ~~**K1 NCP 쐐기**~~ ✅ **완료(2026-06-30)** — NCP(`211.188.58.140`)에서 `sudo mysql -e "SHOW COLUMNS FROM hitpan_backoffice.landing_signups LIKE 'desired_domain';"` → `desired_domain varchar(255) YES NULL` 실측. K1 완전종결.
2. **K3·K4 = 고리4②③ 구현**: 어제 설계서(`20260629_고리4_마이그롤백_원자적복원_설계서.md`)·작1·작2 기반. **B갈래(테스트 환경) 필요 = 사장님 Build-TestSlot.ps1 실행 선행**(헌법#29).
   - B갈래 실행 대본: `docs/개발/erp/20260630_사장님_실행대본_테스트슬롯구축_DB까지.md`
3. **종단 빌드테스트**: B갈래 슬롯 위에서 가입→시리얼→ERP로그인 실측 + EXE 빌드(빈데이터 게이트 4중 확인). 작업지시서: `20260630작1`.

---

## 5. 오늘 작성 문서 (커밋 대상)
- `20260630_사장님_실행대본_테스트슬롯구축_DB까지.md`
- `20260630_통신무결성_17시나리오_측정표.md`
- `20260630작1_빌드테스트_종단흐름_작업지시서_CTO결재용.md`
- `20260630_3시스템_연결축_핵심축지도_전수파악.md`
- 본 인수인계서

코드 변경 0건. appsettings.json 미터치(헌법#21).
