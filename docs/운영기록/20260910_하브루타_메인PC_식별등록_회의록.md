# 하브루타 회의록 — 메인PC 식별·등록 (2026-09-10)

| 항목 | 내용 |
|---|---|
| 상태 | 🟢 **결론 도출**(주심 불일치 0) · 🔴 **사장님 결재 대기** — 결론은 §7 |
| 사회·기록 | PM 브라운킴 |
| 주심(관리감독) | **CTO 래리 · 보안상무 안철수** |
| 코드 | **0건** (헌법 #33 — 결재 전) |

> **참조식**: 코드는 `파일:줄` 로만 가리킨다. 각 진영 발언은 요지를 옮기되 근거는 빼지 않는다.

---

## §0. 사장님 지시 (원문)

1. *"일단 마이그레이션 실측을 위해서라도, 메인컴퓨터 식별, 등록이 되어 있어야 겠어. 디바이스 등록에서 메인컴퓨터 식별, 등록에 대한 확실한 방안 설계팀, 개발팀, 검증팀, 세 축이 병렬로 브리핑 해봐."*
2. *"그럼 병렬3축으로 나온 의견으로 하부르타 토론 해서 최종 결론 도출해."*
3. *"임원들은 하부르타 토론 관리감독 하고"*

## §1. 진영·규칙·근거 문서

| 진영 | 누구 | 관점 |
|---|---|---|
| 방어 | 설계팀장 존 | 안 A 「로컬 되돌이 확인」 |
| 실현성 | 개발팀 (백엔드·프론트·보안2·네트워크 마이클) | 코드로 되는가 |
| 레드팀 | 검증팀장 데이비드 박 | 왜 안 되는가 · 수용기준 대입 |
| 주심 | CTO 래리 · 보안상무 안철수 | 근거 부실 각하 · 독립 반증 |

**규칙** (`docs/CS/20260715_로그인전업데이트버튼_하브루타_진영구성.md` 준용)
1. 근거강제 — 공격·방어 모두 `파일:줄` 또는 재현 절차. 없으면 주심이 각하, 각하된 주장은 다음 라운드 불가.
2. 인상비평 금지 · 추정은 🟡.
3. 받아쓰기 금지(#32) — PM 교차검토도 반증 대상.
4. 반증당하면 인정하고 입장을 바꾼다.
5. 🔴 8/18 교훈 — 하브루타 합의안이 [3-V]에서 P0 6건으로 깨졌다(`docs/검증/병렬이슈/20260818_3V_동시검증_하브루타봉합안_반증.md`). **합의가 아니라 살아남은 근거가 결론이다.**

**근거 문서 (세 축 브리핑)**
- 검증: `docs/검증/선행/20260910_선행검증서_메인PC_식별등록_전제실측.md`
- 설계: `docs/설계/erp/20260910_설계브리핑_메인PC_식별등록_확실한방안.md`
- 개발: `docs/개발/erp/20260910_개발브리핑_메인PC_판정코드전수_구현타당성.md`

**쟁점 (PM 상정)**
1. 판정축 — 안 A 가 확실한가 vs 개발 (b)/(a′)
2. 보안 — 증표 재생 · 무로그인 확인 페이지 · 번호 보여주기/입력하기 · Host 위조 구별 · LOCAL 모드
3. 신분·슬롯 — A-2 다중탑승 vs 합류 갈래 · 1.2.88~1.2.90 계약 · 기존 2~3줄 정리 · 메인PC 교체 경로 0
4. 순서·당장봉합 — 개발 (f) vs 설계 S1 · 8/18작4 철회 · 게시 순서
5. 검증 게이트 — 실경로 「갔나」 게이트 · DB 게이트 SKIP · 출하 DDL DB-89 누락 · DB 번호
6. 쉬움(#25) — 처음 보는 대표가 혼자 끝내나 · 권한 대표 전원 vs 부모계정(#40) · 교체 절차

---

## §2. 1라운드 — 공격·자기반증

### 2-1. 설계팀장 존 (방어)

**A. 공격**
- (개발) **쟁점4 · (f) 안2 「대기 관문으로 보냄」** — 줄은 이미 `TenantDeviceService.cs:711` 에서 approved. `Login.razor:309-312` forceLoad 로 메모리 표시가 지워지고(`DeviceAuthGate.razor:296-307`) `gate-status` 가 approved 를 받아(`DeviceController.cs:155`) `:381-407` 이 통과 처리 → 업무 403 → `_blocked`. pending 으로 되돌리면 G-40 이 깨지고 기본 버튼(`:94-105`)이 슬롯 +1.
- (개발) **쟁점2·3 · (d) 신규절 「is_main_pc=1 ∧ 기계비밀 일치면 MainPcOnly 통과」** — 증명 1회·권한 영구. `localStorage` 값을 옮기면 원격 초기화(AC-07 위반). 신규절은 동시 1명(G-39 `DeviceAndKeyGateTests.cs:1189-1195`) → 엣지+크롬 불통.
- (개발) **쟁점2 · (b) 발급 조건 「loopback 판정」** — 5234 프록시 경유는 Host=`localhost:{port}`·XFF 없음(`web-server.ps1:91-103`)으로 진짜 localhost 와 Host 위조 사내 PC 를 API 가 구별 못 함. 사내 인원이 위조로 발급 → (d) 로 영구 원격 삭제권.
- (개발) **쟁점3 · F-4 「다른 줄이 is_main_pc=1 이면 안 건드림」** — 8/18 표식 이동 결과를 영구화. 「회사 서버」 칩이 브라우저 줄에 붙음(`DeviceManagePage.razor:163-171`) · 교체 감지 차단.
- (검증) **쟁점5 · AC-02 vs AC-05b 모순** — API 가 받는 요청이 같아 동시 만족 불가 ⇒ AC-02 를 「localhost 탭도 127.0.0.1 직결 확인으로 열림」으로 수정해야.
- (검증) **쟁점1 · Q2 틀** — 직결 확인은 도메인 요청의 경로를 판정에 안 쓴다. 「localhost 로만」이 오히려 8/10 계열.
- (검증) **쟁점4 · Q4 오늘 2·3·5** — 실물 DB 는 이미 이관된 DB 라 보태기 재이관은 계산서 방향 짝을 흡수(병렬이슈15). 🟡 숫자 판정 오염 ⇒ 「화면 동작만, 숫자 판정 금지」 조건.

**B. 자기반증**
1. 🔴 **CORS 가 모든 출처 + 자격증명 허용**(`Program.cs:400-404`) ⇒ 메인PC 브라우저의 아무 웹페이지 fetch 도 「직결 로컬」 판정 참. 확인 엔드포인트에 Origin 동일출처 검사 필수 · 「직결 로컬 단독으로 MainPcOnly 통과」 항 **철회**.
2. 🔴 **자동 확인 + 폴링은 피싱에 열림** ⇒ **입장 변경**: 번호는 127.0.0.1 창에만 띄우고 사람이 도메인 탭에 **입력**(개발 (b) 방식 채택). 발급은 A 경로 유지.
3. 🔴 **A-2 다중탑승은 1.2.89 계약(G-39 동시 1) 위반** — §7 「K·G-41 무변경」은 틀림 ⇒ A-2 를 요금 결재로 분리.
4. 🔴 **S1 에 계정 조건 없음** — 직원·Host 위조 로그인에도 메인 줄 번호를 주고 축②(`DeviceAuthMiddleware.cs:186-194`)로 터널 너머 통과 ⇒ S1 은 tenant_admin 한정 + 축② 폐지와 묶어야.
5. 폴링 GET 도 `audit_logs` INSERT(`AuditLogMiddleware.cs:21-72`) — 폴링 제거로 해소.
6. 브라우저 로컬 네트워크 제한(R-A1) ⬜ — 실측 전 「확실한 방안」이라 쓰지 않는다.

**C. 입장**: ①127.0.0.1 창 발급 + 도메인 탭 입력 · 자동확인·폴링 철회 · 권한창·팝업·Host 실측이 출시 전 게이트 ②직결 = loopback ∧ 터널헤더 없음 ∧ Host=127.0.0.1:port ∧ Origin 동일출처 · 자리증표 메모리 10분 · (d) 반대 · 프록시 경유 판정 폐지 ③A-2 철회 · 현행 합류 + 입력코드 증명 + 옛 줄 정리 ④1순위 8/18 표식이동·revoke 철회 + F-2 + F-3 · 안2·F-4 반대 · S1 은 대표 한정일 때만 ⑤파이프라인 전체 + 터널헤더 도메인 요청 200 단언 · 대조군 복사자칭·Host위조·교차출처 · DB 번호 한 번만 ⑥처음 보는 대표 혼자 가능 🟡(문구 대본·화면 실측) · 부모계정 한정 권고

### 2-2. 개발팀 (실현성)

**A. 공격**
- (설계) **쟁점1 · 「API 가 주는 무로그인 정적 HTML 1장」** — `/local-proof` 는 `MapFallbackToFile("index.html")`(`Program.cs:625-628`)로 떨어져 **Blazor 앱 전체**가 뜨고 로그인 화면으로 간다 🟢 ⇒ `.html` 파일 또는 컨트롤러 응답 명시 필요.
- (설계) **쟁점1 · 「POST confirm 로그인 불필요」** — `TenantMiddleware.cs:71-75` 가 무로그인 `/api` 를 401, 통과목록(`:25-58`)에 `/api/devices/*` 없음 🟢. `POST api/devices/mobile-register`(`DeviceController.cs:304-306`)도 목록에 없어 코드상 401(실측 ⬜ · **별건 후보**).
- (설계) **쟁점1 · 포트** — API 가 `API_PORT` 를 어디서도 안 읽음(grep 0) 🟢 ⇒ localUrl 조립 코드 신설 필요. 이 PC 에 `HTTP://*:5357/{GUID}/` 등록(🟢 netsh · 정체 🟡 Windows 장치검색) — 두 번째 사업자 기본포트 5357(`iss:495`)과 겹칠 수 있음 🟡.
- (설계) **쟁점1 · 팝업** — `POST start` 응답 후 `window.open` 은 클릭 효력이 끝나 팝업 차단 🟡 ⇒ 클릭 즉시 빈 창 먼저.
- (설계) **쟁점2 · pairId 흐름** — CORS 전체허용(`Program.cs:396-405` · 전역 `:577`) 🟢 ⇒ 메인PC 브라우저의 아무 페이지가 confirm 단순 POST → Host·loopback·헤더 조건 전부 통과. 필요: `Origin`/`Sec-Fetch-Site` 동일출처 + 사람 클릭 + 번호 로컬→도메인 입력.
- (설계) **쟁점4 · S1** — 번호만 주면 비밀 없는 축②에 실린다. §4-4 축② 폐지 검토와 충돌 → S1 뒤 A-2 게시 시 403 회귀. `IsLocalConsole` 은 LOCAL 모드·Host 위조에서 참 → 새 줄 없이 ① 공유 → **한도 검사(`TenantDeviceService.cs:668-684`) 우회로 슬롯 무제한** 🟡.
- (설계) **쟁점3 · A-2** — 축①은 줄 하나의 해시만 봄(`:1072-1079`) ⇒ 미들웨어 새 축·G-35/G-36 기대 확대·탑승자 해제 화면까지 범위.
- (검증) **쟁점2 · §4-1 「로그인 계정만 있으면 초기화·복원 통과」 과장** — `BackupController.cs:16`·`DataResetController.cs:16`·`MigrationController.cs:21` 클래스 전체 `TenantAdminOnly` 🟢 ⇒ 등급 정정. 보강 실측: 정적 `/` 에 `Host: localhost:5234` → **200, 301 없음** 🟢(감사기록은 `/api/` 만 · 운영 DB 쓰기 0).
- (검증) **쟁점5·6 · AC-03 「엣지+크롬 서로 안 가둠」** — 1.2.89 재발급이 직전 비밀을 죽임(`DeviceAuthGate.razor:600`) ⇒ AC-03 = 다중탑승 DDL 강제 = 결재 전 설계 선택 선점(#33) ⇒ 「결재된 규칙대로」로 수정.

**B. 자기반증**
1. 🔴 (d) 신규절 틀림 — 기계비밀은 저장소에 오래 사는 값, 복사 시 원격 초기화 ⇒ 「판정 함수 한 곳」 구조만 남김.
2. 🔴 (e) 철회 — CSP `connect-src 'self' https: wss:`(`Program.cs:541`)가 `http://127.0.0.1` 를 막음.
3. 🔴 (f) 안1 은 S1 과 같은 자리(`TenantDeviceService.cs:269-309`·`:711`)를 반대로 고침 → 철회. 안2 도 보이는 2중 + 슬롯 +1 → S1 이 낫다.
4. ⚠️ (b)·(a′) 「DDL 0 · 1.2.89 재발급 재사용」은 엣지+크롬 번갈이 방치. 무로그인 발급 (a′) 는 구멍 ⇒ (b) 만 유지.
5. ⚠️ 「Y-2 가 유일한 갈래」는 문구 기준일 뿐 — 실제 갈래 ⬜.

**C. 입장**: ①운반은 안 A 수용 — .html/컨트롤러 · TenantMiddleware 통과목록 · 포트 조립 · 빈 창 먼저, 넷 없으면 반려 · 권한창 실측 전 보류 ②번호 로컬→도메인 입력 + confirm 동일출처 + 사람 클릭 · **CORS 전체허용 축소가 선결** ③A-2 동의(번갈이는 DDL 없이 못 풂 🟡) · 미들웨어 새 축·해제 화면·공존 규칙 범위 포함 ④안1·안2 철회 · 먼저 게시 = S1(Origin 조건 + LOCAL 제외) + S3 + 막힌 화면 대표 안내 + 서버줄 재표식 가드 → A-1 → A-2 · 8/18작4 철회 찬성 ⑤파이프라인 전체(`Program.cs:574-617`) + 터널헤더 + 로그인 직후 업무 200 + 익명 통과 · DB 게이트 CI 등록·DB-89 재정합 이번 범위 · DB 번호 하나로 합의 ⑥로컬 창 번호 크게·도메인 입력칸 하나 🟡 · 권한 결재 · 부모계정 DB 조회 추가 여부 ⬜

### 2-3. 검증팀장 데이비드 박 (레드팀)

**A. 공격**
- (설계) **A1 · 쟁점2 · 「확인은 직결 로컬에서만」** — CORS 전체허용(`Program.cs:400-404`)이 인증·기기 미들웨어보다 앞(`:577`) ⇒ 아무 사이트 스크립트가 확인 페이지를 읽고 confirm. 공격 순서: 탈취한 대표 JWT·기기비밀로 start → 사장님이 10분 안에 링크 하나 엶 → 증표는 공격자 탭 → 원격 초기화. 「4자리 보여주기」는 스크립트가 무시 🟡.
- (설계) **A2 · 쟁점2·3 · 자기 원칙 위반** — 「접속경로 식을 신분·슬롯 근거로 쓰지 마라」(설계:68)면서 S1(:283)·§4-1⑤(:204)에서 localhost 로그인에 메인 줄 번호·① 탑승. `IsLocalConsole` 은 LOCAL 모드 사내 전 PC(`web-server.ps1:32`)·Host 위조에서 참 ⇒ 대표 승인 없이·슬롯 0 으로 메인 번호 → 축②로 도메인에서도 영구 통행. 현행은 +1 슬롯·한도 검사라도 걸림.
- (설계) **A3 · 쟁점3 · A-2 다중탑승** — G-39 「무료 탑승 동시 1」(`DeviceAuthGate.razor:596-600`·`DeviceAndKeyGateTests.cs:1189-1195`) 해제 ⇒ 번호+비밀 복사 시 슬롯 0 원격 기기 동시 N대. 「K 와 동급」(:170) 틀림.
- (설계) **A4 · 쟁점3 · 옛 줄 ② 폐기 + 「캐시 삭제 → 1회 재합류」** — 캐시 삭제 시 지문으로 ② 찾음(`TenantDeviceService.cs:226-233`) → `revoked && !isMainPc` 면 pending 회귀(`:366-386`) → 대기 목록에 다시 뜨고 기본 버튼이 슬롯 +1.
- (설계) **A5 · 쟁점1 · LOCAL 모드 「localhost 탭도 같은 버튼」** — start 조건 「대표 JWT + 기기비밀 짝」인데 LOCAL 대표 탭은 비밀 없이 축②로만 통행 ⇒ start 자격 없음.
- (개발) **D1 · (f) 안2** — `Login.razor:311-312` forceLoad 로 표시 소멸, 관문 진실원은 행 status(`TenantDeviceService.cs:1631-1639`) → approved 라 통과 → 403 → `_blocked`.
- (개발) **D2 · (d)** — 위치 증명 1회 후 판정은 저장 비밀뿐(`:1063-1080`) ⇒ 복사 시 원격 초기화·복원.
- (개발) **D3 · (b) 발급 근거 `IsLocalConsole` 계열** — LOCAL 모드·Host 위조에서 발급. 「대표 비번 필요」는 대표 여럿·비번 공유 회사에서 방어 아님.
- (개발) **D4 · 「업데이트 전달·DDL 0」 반증** — 러너는 실패 마이그를 `success=0` 으로 두고 **이후 마이그 전부 정지**(`Program.cs:422-432`) ⇒ `is_main_pc=1` 2줄 고객에서 UNIQUE 추가 실패 시 이후 마이그가 영영 안 깔림(탐지는 마이그 파일 안의 조건이어야). 설계·개발 **둘 다 DB-120** → 러너가 번호로 건너뜀(`MigrationRunner.cs:90-94`) → 뒤 파일 통째 skip(8/25 DB-100 계통).
- (PM) **교차검토 반증** — `http://localhost:5257` 직결도 Host 는 `localhost:5257` ⇒ 구별되는 건 **`127.0.0.1` 리터럴뿐** · 같은 PC 악성 페이지는 못 거름.

**AC 대입표 (FAIL·조건부 행)**

| AC | 설계 A-1(자리) | 설계 S1·A-2⑤ | 개발 (f)안1/안2·F-2 | 개발 (b)+(d) |
|---|---|---|---|---|
| 01 도메인 | 조건부(권한창·팝업 ⬜ · A1) | FAIL | FAIL | 조건부 |
| 02 localhost 첫 로그인 | — | 조건부(05b·12 연쇄) | 안1 FAIL · 안2 FAIL · F-2 조건부 | (f) 없이 FAIL |
| 03 엣지+크롬 | — | 조건부(A3 요금) | F-2 합류 번갈이 FAIL | FAIL(D2) |
| 04 캐시 삭제 | FAIL(A4) | — | — | — |
| 05b Host 위조 | 🟡 PASS | FAIL(A2) | 안1 FAIL | FAIL(D3) |
| 07 복사 자칭 | 조건부(증표 보관처) | FAIL(A3) | — | FAIL(D2) |
| 10 메인PC 교체 | 조건부(§4-3 신설) | — | FAIL | FAIL |
| 12 LOCAL 모드 | 조건부(A5) | FAIL(A2) | FAIL | FAIL(D3) |
| 13 기존 고객 정리 | 조건부(A4) | 조건부 | FAIL | FAIL |
| 14 승인 직후 200 | 조건부(게이트 0) | 조건부 | 안2 FAIL | 조건부 |
| 16 문구 | — | S3 단독 FAIL | F-3 동일 | — |

**B. 자기반증**
1. 🔴 AC 16개에 127.0.0.1 창 방식이 여는 실패가 한 줄도 없다 ⇒ **AC-17**(다른 사이트 스크립트 confirm 불가) · **AC-18**(다중 사업자 두 번째 포트) · **AC-19**(팝업 차단·권한창 안내·재시도) 추가.
2. Host 위조 ⬜ → 개발 🟢 실측(301 우회)으로 정정 · API 판정은 🟡.
3. 「CI 미실행 54곳」 중 본문 직접 확인은 3파일 39곳, 나머지 15곳 🟡 · G-OW8 은 「정책 종속 게이트」로 재분류.
4. 「오늘 2·3·5 가능」은 **localhost 재로그인을 안내하지 않는다는 조건부** — 재로그인 시 ① revoke(`:274-283`) → 도메인 탭 합류 상태면 문 A 도 막힘(`:1072-1077`).

**C. 입장**: ①A-1(127.0.0.1 리터럴 + loopback + 헤더 0)만 사내PC·LOCAL 을 구조로 거름 · confirm 에 Origin·Sec-Fetch-Site + **로컬 창 안 입력** · (b)/(a′)·(d) 반려 ②`IsLocalConsole` 을 신분·슬롯·승인면제 근거로 쓰는 안(S1·A-2⑤·안1·(b)) 전부 반려 · 증표 메모리·짧은 수명 · **번호는 도메인 탭 값을 로컬 창에 입력** ③A-2 는 탑승 상한·목록 노출·개별 폐기 없으면 반려 · 옛 줄은 폐기가 아니라 흡수(A4) ④오늘 코드 0 · 도메인 2·3·5 만 · localhost 재로그인 안내 금지 · 1차 게시 = F-2(+F-4)만 · S1·F-1·안2·문구 단독 반려 · 8/18 C1 철회는 A-1 과 같은 게시 ⑤출시 게이트 AC-01·02·05b·07·12·13·14·17 · 파이프라인 게이트 3요청(CF 헤더/127.0.0.1 Host/타 Origin) · 127.0.0.1 창은 Playwright · 기기 게이트 CI `GATES` 등록 필수 · DB 번호 한 곳 배정 · DB-89 조건부 마이그 ⑥3동작 + 막힘 시 안내·재시도 없음 = #25 FAIL · 부모계정 한정(JWT 에 없어 DB 조회) · 교체 절차 없는 안은 「확실한 방안」 아님

---

## §3. 1라운드 정리 (PM)

### 3-1. 세 진영이 따로 낸 같은 판정 — 살아남은 근거 후보

| # | 판정 | 근거 |
|---|---|---|
| H-1 | 🔴 **API CORS 가 모든 출처 + 자격증명 허용** ⇒ 127.0.0.1 직결 판정은 **메인PC 브라우저 안의 다른 사이트 스크립트**를 못 거른다 | `Program.cs:396-405` · 전역 `:577` (세 진영 모두 적발) |
| H-2 | **자동 확인(번호 보여주기·폴링) 폐기** ⇒ 사람이 번호를 **입력** | 설계 B2 · 개발 A · 검증 A1 |
| H-3 | **개발 (d) 신규절 폐기** — 저장된 비밀로 자료관리 영구 통과는 복사 시 원격 초기화 | 설계 A · 개발 B1 · 검증 D2 |
| H-4 | **개발 (f) 안1·안2 폐기** — forceLoad 로 대기 표시 소멸 · 안1 은 도메인 갇힘 앞당김 | `Login.razor:309-312` · `TenantDeviceService.cs:711` |
| H-5 | **8/18작4 표식 이동·서버줄 revoke 철회** 찬성 (시점은 대립 — D3) | 세 진영 |
| H-6 | **DB 번호는 한 곳에서 배정** · DB-89 재적용은 **조건부 마이그** (실패 시 이후 마이그 전부 정지) | `Program.cs:422-432` · `MigrationRunner.cs:90-94` |
| H-7 | **PM 교차검토 정정** — Host 로 구별되는 것은 `127.0.0.1` 리터럴뿐 · 같은 PC 악성 페이지는 못 거름 | 검증 PM 반증 |
| H-8 | 무로그인 확인 페이지는 **Blazor fallback·TenantMiddleware 401** 에 걸린다 — 전용 응답·통과목록이 필요 | `Program.cs:625-628` · `TenantMiddleware.cs:25-75` |

### 3-2. 대립 — 2라운드 상정

| # | 대립 | 설계 | 개발 | 검증 |
|---|---|---|---|---|
| D1 | **번호 방향** | 로컬 창 표시 → 도메인 탭 입력 | 로컬 → 도메인 입력 | **도메인 탭 값 → 로컬 창 입력** |
| D2 | **A-2 다중탑승** | 철회(G-39 계약) · 요금 결재로 분리 | 유지(번갈이는 DDL 없이 못 풂) | 상한·목록·개별폐기 없으면 반려 |
| D3 | **1차 게시 묶음** | 8/18 철회 + F-2 + F-3 (+S1 대표 한정 시) | S1(Origin + LOCAL 제외) + S3 + 대표 안내 + 재표식 가드 | F-2(+F-4)만 · S1·문구 단독 반려 · 8/18 철회는 A-1 과 같은 게시 |
| D4 | **`IsLocalConsole` 을 신분·슬롯 근거로** | 대표 한정이면 허용 | Origin·LOCAL 제외면 허용 | 전부 반려 |
| D5 | **옛 줄 정리** | 폐기 | — | 폐기는 1.2.90 회귀와 충돌 → **흡수** |
| D6 | **오늘 마이그 실측 2·3·5** | 비어있지 않은 DB 보태기 → 숫자 판정 오염, 화면 동작만 | — | 가능 · 단 localhost 재로그인 안내 금지 조건 |
| D7 | **수용기준 수정** | AC-02 문구 수정 | AC-03 이 설계 선택 선점 → 「결재된 규칙대로」 | AC-17~19 추가 |

### 3-3. 별건 후보 (이번 결론 범위 밖 · 주심 등급 판정 요청)
- `POST api/devices/mobile-register` 가 TenantMiddleware 통과목록에 없어 **코드상 401** (개발 🟡 · 실측 ⬜)
- 이 PC 의 `*:5357` 등록이 두 번째 사업자 기본포트와 겹칠 가능성 (개발 🟡)
- `hitpan-keys.conf` 일반 권한 열람 · 터널 토큰 서비스 명령줄 노출 (검증 §10)

---

## §4. 주심 — 사전 독립반증 · 1라운드 판정

### 4-1. 보안상무 안철수 — 사전 독립반증 (1라운드와 동시 · 코드 직접)

**결론**: 안 A 는 **조건부 채택 가능**. 조건 셋 — 하나라도 빠지면 반려.
- ⓐ 확인·자료관리 판정을 **`Host ∈ {127.0.0.1:{API_PORT}, [::1]:{API_PORT}}`** 로 고정 (loopback 단독 판정 폐기)
- ⓑ **증표는 터널 너머로 인정 금지** (직결 로컬에서만)
- ⓒ 파괴급(초기화·복원·덮어쓰기)은 **`is_parent`** 로 축소

| 등급 | 사안 | 근거 |
|---|---|---|
| 🔴 P0 | **5234 프록시가 원격 요청을 loopback 으로 바꿔 넘긴다** — API 는 늘 `RemoteIp=127.0.0.1`·터널헤더 0 ⇒ `IsMainPc` 참 | `web-server.ps1:33·91-103` · `MainPcOnlyAttribute.cs:41-48` |
| 🔴 P0 | **LOCAL 모드 = 사내 전 PC 가 메인PC** — 301 이 꺼져 위조조차 불필요 | `web-server.ps1:32·75` |
| 🔴 P0 | **`hitpan-keys.conf` 일반 권한 열림**(서명키·암호화키 파일) — 설치기 의도(`iss:1913-1925` admin 전용)와 실측 불일치 | 선행검증 E4 (내용 미열람) |
| 🟡 P1 | CORS reflect-any + AllowCredentials — confirm 이 pairId 에만 기대면 위험 | `Program.cs:400-404` |
| 🟡 P1 | A-1 증표 복사·재생 — 파괴권을 원격으로 이동 | 설계 §2-3 · AC-07 |
| 🟢 방어 | 임의 웹페이지가 confirm 유발 — pairId 는 대표 JWT 로만 발급 + Chrome 142+ LNA 가 공인→loopback 요청을 권한창으로 게이트 | [Chrome LNA](https://developer.chrome.com/blog/local-network-access) |

**🔴 현행 1.3.39 Host 위조 구멍 — 열려 있다 (코드 판정 · 실측 안 함)**
사내 PC → `Host: localhost` 위조로 `{메인PC IP}:5234` → 301 건너뜀(`web-server.ps1:77-79`) → 프록시 loopback 재요청 → `MainPcOnly` 참.
대표 비번 보유 시 기기승인이 켜져 있으면 로그인 2회로 C1(`TenantDeviceService.cs:269-296`)이 **진짜 서버줄을 revoked, 공격자 줄을 `is_main_pc=1`** 로 올린다 ⇒ 원격 초기화·복원·덮어쓰기 + 진짜 메인PC 무력화. 8/11 사장님 지시(자료관리는 메인PC 에서만) 정면 위반.
⇒ 당장봉합은 **서버측**(프록시는 업데이트로 못 고친다): ⑴ `MainPcOnly` 를 Host 고정으로 ⑵ 파괴급 `is_parent`.

**PM 교차검토 판정 — 절반만 맞다.** Host 는 「직결 127.0.0.1」과 「5234 프록시」는 가르지만 **정상 프록시와 위조 프록시는 못 가른다**(둘 다 `localhost:5257` 로 도착). ⇒ Host 검사는 **5234 경로를 자료관리에서 폐기**할 때만 유효.

**번호 방향 판정** — 설계의 「도메인 개시 · 로컬 확인」은 사회공학에 열려 있다(원격자가 자기 pairId 로 시작하고 전화로 "확인 눌러주세요"). ⇒ **개시를 로컬 페이지에서, 번호는 로컬→도메인 방향.**

**별건**: `hitpan-keys.conf` 🔴 최우선 별트랙 · 터널 토큰 명령줄 노출 🟡(#29 인프라 트랙).

**강제 질문** ①「직결 로컬」을 Host=127.0.0.1:{API_PORT} 로 재정의하고 5234 경로를 자료관리에서 폐기하는가 ②번호를 로컬 개시·로컬→도메인으로 뒤집는가 ③증표를 터널 너머에 인정하는가 — 인정하면 AC-07 이 열린다, 직결 재요구하지 않을 근거를 대라.

### 4-2. CTO 래리 — 사전 독립반증 (1라운드와 동시 · 코드 직접)

**판정 기준 (입증 없으면 각하)**
1. 판정축 — 도메인 탭(P1)·localhost(P2)에서 확인 직후 업무 200·자료관리 200 · 바깥 PC·폰(P3·P4) 거부를 **미들웨어+필터 전체를 태우는 시험 하나**로 함께 입증. 문 하나만 여는 안은 각하.
2. 보안 — ①메인PC 브라우저의 제3자 사이트 ②사내 PC Host 위조·LOCAL 모드 ③원격 대표 계정 + 현장 클릭 1회를 막는 입증. **사람이 입력하지 않으면 확인이 성립하지 않아야.** "브라우저가 막아준다"는 근거로 불인정.
3. 신분·슬롯 — 계수 SQL 무접촉 · 1기계=1슬롯 · K·DP-1·1.2.89 서버줄 보존·`&& !isMainPc` 네 계약에 대해 **고칠 자리 전수 목록**.
4. 순서 — 당장봉합이 본안에서 **지워지거나 뒤집히는 코드·게이트·문구 목록**. 비어 있지 않으면 「임시」로 적고 사장님 결재.
5. 게이트 — CI `GATES` 등록 + 터널 헤더 붙인 상태의 「열려야 할 것이 열린다」 단언 + 무력화 FAIL 사유 확인. DB-120 계열은 **메인 줄 2개인 더러운 DB**에서도 실패하지 않음을 입증.
6. 쉬움 — 사장님이 주소를 바꾸지 않고 막힌 화면에서 몇 번 눌러 끝나는지 실측 시나리오. #40 은 결재 안건으로만.

| 등급 | 사안 | 근거 |
|---|---|---|
| 🔴 P0-1 | 안 A 확인 페이지 **사이트 간 위조 요청** — CORS 가 정적파일·전체 API 앞 · 「열리자마자 즉시 확인」이면 집 PC 대표가 만든 pairId 를 현장 링크 클릭 1회로 확인 → 원격 초기화(8/11 지시 `DeviceController.cs:89-91` 위반) | `Program.cs:394-405`·`:577` 🟢 · 브라우저 차단 🟡 |
| 🔴 P0-2 | **R-2 를 DB-120 으로 내면 이후 마이그 영구 정지** — UNIQUE 는 status 무관(`DB-89:47-65`) · 서버줄 서비스가 다른 메인 줄을 안 보고 표식을 되올림(`MainPcRegistrationService.cs:149-171`) ⇒ 메인 줄 2개 고객 존재 → ALTER 실패 → 이후 전부 막힘(`MigrationRunner.cs:143-161`) · 앱은 뜸(`Program.cs:467-478`) · 롤백은 DB 미복원(`UpdateOrchestrator.cs:800-801`) · **체크섬 없음**(`MigrationRunner.cs:91-108`) ⇒ 게시한 DB-120 은 고칠 수 없다 | 🟢 |
| 🔴 P0-3 | **S1·(f)안1·A-2⑤ 모두 프록시 경유 `IsLocalConsole` 로 메인 줄 신분 부여** — LOCAL 모드 301 꺼짐(`web-server.ps1:32`) · 계정종류 조건 없음(`TenantDeviceService.cs:269`·`:711`) · 현행 C2 는 +1 슬롯·한도검사(`:668-671`)로 요금은 지킴 → 세 안은 **사내 PC 전원 슬롯 0** · 설계 §3-3 과 §4-1⑤ 모순 | 🟢 코드 · LOCAL 실측 ⬜ |
| 🔴 P0-4 | **(f)안1 과 A-2(a) 는 서로 뒤집는 코드** — 게시 두 번에 두 번 갈아엎음 · 도메인 탭이 ① 합류 상태면 안1 게시 즉시 403(`:1076`) | 🟢 코드 · 합류 여부 ⬜ |
| 🟡 P1 | 익명 `/api/devices/local-proof/*` 는 TenantMiddleware 401(`TenantMiddleware.cs:71-75`) — 여는 목록(`:25-58`)이 #43 업데이트 항목(`:35`·`:46`)과 같은 목록 | 🟢 |
| 🟡 P1 | `/local-proof` 는 Blazor 로 떨어짐(`Program.cs:627`) → 127.0.0.1 에 4번째 저장소 · 로그인 시 새 줄 | 🟢 |
| 🟡 P1 | (f)안2 무효 — approved 면 첫 gate-status 에서 `MarkAllowed` 가 대기 표시를 지움(`DeviceAuthGate.razor:381-407`·`DeviceAuthState.cs:34-38`) → C2 pending 복귀 전제 = GC40 반전 | 🟢 |
| 🟡 P1 | F-2 는 2중을 늘림 — `_blocked` 에 [이 컴퓨터 승인](슬롯 +1) 노출 | 🟡 |
| 🟡 P1 | S1 은 GC41·GC42 기대값 정면 반전(`MainPcLocalConsoleGateTests.cs:339-358`·`:390-410`) · 두 게이트는 CI 에서 `return` 으로 조용히 통과(`:326`·`:379`) ⇒ 뒤집어도 CI 초록 · ⚠️ **G-41 이름 두 벌**(8/18작4 GC41 ≠ 1.2.89 G-41) | 🟢 |
| 🟡 P1 | S1 이후 localhost 탭은 축②(번호만 `DeviceAuthMiddleware.cs:186-194`)에 기댐 → 축② 폐지 게시에 재확인 버튼이 같이 없으면 모두 갇힘 | 🟢 |
| 🟡 P1 | A-2 탑승비밀 표는 네 자리 동시 수정 — 축①(`TenantDeviceService.cs:1072-1079`) · 재발급(`:1117-1125`) · K verify(`:1034-1044`) · 폐기 열쇠소거 | 🟢 |
| 🟡 P1 | A-1 증표 판정 — 미들웨어가 통과 축을 기록 안 함 · `MainPcOnly` 는 동기 정적(`MainPcOnlyAttribute.cs:37`) ⇒ 번호만으로 통과한 요청에도 증표 인정 구멍 또는 비동기 전환 · 승인제 스위치 꺼진 고객은 미들웨어 통째 건너뜀(`:83-87`) | 🟢 |
| 🟡 P1 | **다중 사업자 두 겹** — 포트 5257+100(N-1)(`iss:495`·`:2888`) · **터널 ingress 가 슬롯 무관 `localhost:5257` 고정**(`InstallerBootstrapController.cs:133`) · `web-server.ps1:4` 5234 고정 ⇒ 「localhost:5234 로 여세요」 안내는 슬롯2 이상에서 **다른 회사 API** 로 보냄 | 🟢 코드 · 슬롯2 실측 ⬜ |
| 🟡 P1 | **마이그 번호** — 최신 DB-119 · 번호충돌 게이트(`build-and-test.yml:305-317`)는 있으나 병렬 PR 2개가 각자 DB-120 이면 각 PR 초록 · 게시는 수동이라 CI 초록 미강제(`deploy-update.yml:43-47`) · 출하 DDL 시드 게이트 `.Distinct()`(`MigrationLocationGuardTests.cs:93`) ⇒ **작업지시서에서 번호 사전 배정** | 🟢 |
| 🟡 P1 | 개발 (d)가 `AuthController.cs:517-520`(로그인 전 업데이트 응답)을 통합 후보로 둠 — 증표 OR 절이 섞이면 #43 영역 변경 ⇒ **금지** | 🟢 |
| 🟡 P1 | 재부팅·Windows Update 후 — 메모리 증표 소멸(수용) · 메인 줄이 옮겨진 상태면 기동마다 서버줄 서비스가 표식 되올림 → DB-89 있으면 1062 3회(`DB-89:73-81`) · 없으면 메인 줄 2개 | 🟢 |
| 🟡 P1 | **4자리 입력은 대입에 약함** — POST 분당 600회(`RateLimitMiddleware.cs:67-80`) → 1만 가지 17분 ⇒ **시도 횟수 제한 필수** | 🟢 |
| 🟢 방어 | #43 통과 목록 — `/api/devices` 접두어 일치(`DeviceAuthMiddleware.cs:36-39`·`:98-104`) · 업데이트 경로(`:59-61`) 무접촉 · 정적파일은 인증 미들웨어 앞(`Program.cs:583-596` < `:601-613`) | 🟢 |
| 🟢 방어 | 프록시 경유 Host 는 항상 `localhost:{port}`(`web-server.ps1:33`·`:98`) ⇒ Host=127.0.0.1 조건으로 사내 PC·Host 위조 구별 | 🟢 |
| 🟢 방어 | bat 0.0.0.0 기동 + 방화벽 5257 허용(`hitpan-start.bat:79`·`FirewallRules.ps1:39`)은 loopback AND 조건이 막음(유지할 때만) | 🟢 |
| 🟢 방어 | (e) 127.0.0.1 fetch 는 CSP `connect-src` 가 차단(`Program.cs:541`) | 🟢 |

**강제 질문** ①(존) 사람 입력 없이는 확인이 성립하지 않는 절차를 코드 경로로 · 비밀 방향 · local-proof 경로를 CORS 에서 빼는지 ②(개발·존) LOCAL 모드 사내 PC 10대 로그인 시 S1/(f)안1/A-2⑤ 슬롯 수를 숫자로 ③(개발) 메인 줄 2개 DB 에서도 실패 않는 DB-89 재적용 SQL · 번호 확정 ④(PM·개발·존) 당장봉합 후 안 A 게시 때 지우거나 뒤집는 코드·GC40~42·문구 목록 · (f)안2 가 C2 pending 복귀 전제인지 ⑤(데이비드 박) DB 조회 없이 판정 가능한 쟁점/불가 쟁점 분리 · 슬롯2 이상이 수용기준 범위인지

**CTO 기울기 (판정 아님)**: 증명 원천을 「127.0.0.1 직결 순간」에 두는 안 A 의 **방향**이 가장 오래 버틴다. 조건 셋 — 즉시 확인을 버리고 사람이 입력하는 방향 · 당장봉합은 `IsLocalConsole` 로 신분을 주지 않고 대기 관문을 거칠 것 · 번호·DDL 은 더러운 DB 에서도 실패하지 않을 것.

### 🔴 4-2-1. 주심 사이의 긴장 (PM 기록)

| | CTO 판정기준 1 | 보안상무 조건 ⓑ |
|---|---|---|
| 자료관리를 여는 곳 | **도메인 탭**에서도 확인 직후 200 | **127.0.0.1 직결 창에서만** (증표를 터널 너머에 인정 안 함) |

⇒ 양립 불가. 두 주심 1라운드 판정에서 다룬다. 합의가 안 되면 **사장님 결재 안건**(선택지와 대가를 함께)으로 올린다.

### 4-3. 1라운드 판정 — 보안 주심 안철수

**각하** (2라운드 불가)

| 대상 | 사유 |
|---|---|
| PM 교차검토 원문 「Host 값으로 구별 가능」 | H-7 로 정정 — API 는 `127.0.0.1` 리터럴만 가른다. 정상 5234 프록시와 위조 5234 프록시는 못 가른다(둘 다 `Host: localhost:5257`) · `web-server.ps1:91` |
| 검증 §4-1 원문 「로그인 계정만 있으면 초기화·복원 통과」 | 과장 — `BackupController.cs:16`·`DataResetController.cs:16` 클래스 `TenantAdminOnly` ⇒ **대표 자격 필요**로 정정(개발 반증 채택). 등급 유지·문구 각하 |
| 검증 「CI 미실행 54곳」 중 본문 미확인 15곳 | 자기반증으로 🟡 강등 — 근거로 못 씀 |

그 외 §2 발언은 전부 `파일:줄` 근거가 있어 각하 없음.

**H 채택**: H-1·H-3·H-4·H-5·H-6·H-7·H-8 채택 · H-2 **재정의 후 채택**(아래 D1) · 보류 0.

**D1 번호 방향 — 확정**
- ⓐ **파괴급(초기화·복원·덮어쓰기)은 번호 릴레이를 쓰지 않는다** — `127.0.0.1` 직결 창 **안에서** 바로 실행.
- ⓑ 저위험 **찾아보기**만 페어링 허용 — **로컬(127.0.0.1) 창에 번호 생성·표시 → 사람이 도메인 탭에 입력**(설계·개발 방향 채택).
- 🔴 **검증 방향(도메인 값 → 로컬 입력) 반려** — 원격 공격자가 자기 도메인 탭 번호를 **불러주기만** 하면 되어 더 쉽다.
- ⓒ 방향만으로 사회공학이 안 막히므로 로컬 창에 **경고문**(*"전화 안내로 이 창을 열었거나 번호를 불러달라 하면 멈추세요"*) + **무엇을 승인하는지 표시** 필수.

**D4 — 검증 편 채택, 전면 반려**: `IsLocalConsole` 은 5234 프록시·LOCAL 모드·Host 위조에서 참(`AuthController.cs:136-140`·`web-server.ps1:32`) ⇒ 신분·슬롯·승인면제 근거 **금지**. 유일 예외 = `127.0.0.1` **리터럴 Host 고정** 신호(프록시가 못 만든다).

**D5 — 검증 편 채택, 「흡수」**: 폐기하면 `revoked && !isMainPc` → pending 회귀(`TenantDeviceService.cs:366-386`)로 되살아나 슬롯 +1 ⇒ 삭제·폐기가 아니라 **같은 기계로 흡수**.

**🔴 조건 ⓑ 귀결 — 확정**
- 증표를 터널 너머에 인정하면 AC-07(10분 원격 초기화)이 산다 ⇒ **파괴급은 `http://127.0.0.1:{API_PORT}` 직결 창에서만.**
- 새 origin = 4번째 줄 문제는 **「127.0.0.1 리터럴 Host 직결 로그인 = 서버줄 합류(새 줄·pending 없음)」로 허용 가능.** 근거: 원격에서 못 만든다 — 프록시는 `Host: localhost:5257` 을 보낸다(`web-server.ps1:98`) · 사내 PC 가 5257 을 직접 치면 RemoteIp 가 loopback 이 아니다(`FirewallRules.ps1:39` · `MainPcOnlyAttribute.cs:46`).
- **허용 4조건 (모두 AND)**: ① `Host ∈ {127.0.0.1:{PORT}, [::1]:{PORT}}` (`localhost` 불가 — 프록시 값이라 구멍 재개방) ② RemoteIp loopback ③ `CF-Connecting-IP`·`X-Forwarded-For` 0 ④ 파괴급은 `is_parent`(헌법 #40).
- **설계 A-1 반려 근거**: 파괴급에 「직결 OR 증표」를 통으로 걸어(설계 §3-1) 증표-터널을 연다 ⇒ 8/11 「원격 삭제 불가」 위반. 저위험 찾아보기에 한해 10분 증표(device_id+user_id+기기비밀 짝)는 가.

**refresh token — P0 격상 아님 🟢**: ERP Web 은 access·refresh 를 `HitPanProtectedLocalStorage` 에 둔다(`AuthService.cs:201·209`). 인증 쿠키 0 — API 는 JwtBearer(`AuthExtensions.cs:51-74`) · `/api/auth/refresh` 는 `[FromBody]`(`AuthController.cs:339-341`) ⇒ CORS 전체허용이어도 임의 사이트가 새 JWT 를 읽어가지 못한다. H-1 의 위험은 **토큰 탈취가 아니라 부작용 있는 POST 의 도달**에 한정.

**현행 Host 위조 P0 — 당장봉합 판정**
- 🔴 **Host 고정 단독을 당장봉합에 넣지 마라** — 넣으면 사장님은 127.0.0.1 창에서만 자료관리가 되는데 직결 합류가 같이 안 가면 그 창이 기기 문에 걸린다 ⇒ **마이그 덮어쓰기 실측이 새로 막힌다.**
- 당장봉합 = ⑴ 파괴급(복원·초기화)에 **`is_parent` DB 게이트**(호출 위치 불변 → 사장님 경로 안 막음) ⑵ **`is_main_pc` 이동 감사알림**.
- Host 고정 + 127.0.0.1 직결 합류는 **안 A 와 한 게시**로.
- 사장님이 여실 주소: 안 A 게시 후 자료관리·덮어쓰기는 **`http://127.0.0.1:5257`**(이 PC API_PORT) 직접 창.

**별건 등급**: mobile-register 401 = 보안상 이롭다 🟢(무로그인 `/api/devices` 차단) · 기능 정합은 🟡 별트랙 ▸ `*:5357` 🟡 P1 — localUrl 은 **그 테넌트 API_PORT** 로 조립 필수 ▸ **`hitpan-keys.conf` 🔴 별트랙 최우선** — ACL 목록만 확인(내용 금지) · 사장님 직보 후보 ▸ 터널 토큰 명령줄 🟡 P1 #29 트랙

**2라운드 강제 질문 (보안)**
- 설계: (a) A-1 을 「파괴급 = 127.0.0.1 직결 창 안 / 찾아보기 = 10분 증표」로 쪼개는가 — 코드로 (b) 직결 합류를 4조건으로 짤 때 서버줄 흡수 SQL 자리(`TenantDeviceService.cs:269-309` 반대로 안 가게)
- 개발: (a) `127.0.0.1` 리터럴 Host 게이트의 대조군 3요청 단언 방법 (b) 당장봉합 `is_parent` DB 조회 시 `MainPcOnly` 동기→비동기 전환 범위 · `DeviceController.cs:101-114` 복제 동시 정리
- 검증: (a) AC-17/18/19 에 파괴급/찾아보기 분리 반영 + CI `GATES` 등록 명시 (b) `is_main_pc` 이동 감사알림의 무력화 시험을 「왜 FAIL 인지」까지

#### 4-3-1. PM 추가 질문 → 보안 주심 정정 (2라운드 발송 전)

**Q-A (PM)**: 허용 4조건에 Origin 검사가 없다 — 메인PC 브라우저의 악성 사이트가 `fetch('http://127.0.0.1:5257/api/auth/login')` 에 대표 자격을 넣으면 ①②③ 참 → 합류 발동 → 응답을 CORS reflect 로 읽고 → `Bearer` 파괴급 POST 까지 참.

**판정 — 🔴 4조건은 구멍이다. 5조건으로 정정.**
- 코드상 성립: `Program.cs:400-404` `SetIsOriginAllowed(_=>true)+AllowAnyHeader+AllowAnyMethod+AllowCredentials` · 전역 `:577` · `/api/auth/login` 은 `[AllowAnonymous]` + DeviceAuth 통과목록(`DeviceAuthMiddleware.cs:38`) · 판정 `MainPcOnlyAttribute.cs:41-48` 참. LNA 는 CTO 기준 2 로 불인정.
- **⑤ `Origin` 부재(주소창 탐색) 또는 `Origin == http://127.0.0.1:{PORT}` AND `Sec-Fetch-Site ∈ {same-origin, none}`** — 합류 로그인·파괴급·번호 발급 경로에 필수.
- 선후: **⑤(요청단 게이트)가 선결** · 미들웨어/필터 코드라 업데이트로 전달된다. CORS 정책 축소는 옳지만 **단독 불충분**(SignalR `AllowCredentials` 의존 `Program.cs:403` · 동일출처 요청엔 CORS 가 안 붙음) ⇒ **⑤ 필수 + CORS 축소 병행 권고**. 둘 다 #43 무접촉(`AuthController.cs:517` · `/api/app-version` · `/api/watchdog` 통과목록 불변).

**Q-B (PM)**: 「안 A 게시 전까지 현행 도메인 유지」는 도메인이 지금도 자료관리 403 이라 모순.

**판정 — 오문 인정. 임시 경로는 열지 않는다.**
- (나) localhost:5234 재로그인은 서버줄 ① revoked(`TenantDeviceService.cs:274-283`) → 도메인 합류 탭까지 막히고 재표식 충돌 ⇒ **반려**.
- 마이그 실측은 **찾아보기 대신 경로 직접 입력 + 「없는 것만 보태기」**(도메인에서 가능 · 파괴급 아님 · `MigrationController.cs:300-328`)로 진행.
- **덮어쓰기·초기화 실측은 안 A(127.0.0.1 직결 합류 + 5조건) 게시까지 대기.** 사장님께 임시 우회 안내 금지(추측 안내 재발 방지).

### 4-4. 1라운드 판정 — CTO 래리

**§0. 두 주심 사이의 긴장 — 판정**
- **기술 판정: 보안상무 ⓑ(127.0.0.1 창에서만)가 나중에 안 터진다.** 도메인 탭 자료관리는 터널을 건너는 증표가 필수인데, 도메인 출처 CSP 가 `script-src 'unsafe-inline' 'unsafe-eval'`(`Program.cs:535`) 🟢 ⇒ 화면 스크립트 취약점 하나면 증표 탈취 → 원격 초기화. 127.0.0.1 창은 **매 요청의 TCP 연결 자체가 증명**이라 훔칠 값이 없다.
- **CTO 판정기준 1 의 「도메인 탭 자료관리 200」 철회**(규칙 4 는 주심에게도). 대체: *"도메인 탭이 막힐 때 127.0.0.1 주소와 할 일이 화면에 뜨고, 그 창에서 첫 세션부터 자료관리 200."* 새 페이지 없이 된다 — 설치본 ApiBaseUrl 빈 값(`iss:1255-1266`) → 화면이 현재 출처로 API 호출(`HitPan.Web/Program.cs:35`) 🟢. 포트는 API 가 안 읽는 `API_PORT` 대신 **`Connection.LocalPort`** 와 대조.
- **사장님 결재 선택지 (CTO 원안)**
  - **(가)** *"자료관리 4기능(찾아보기·모두 지우고 새로 가져오기·초기화·백업복원)은 자료보관 컴퓨터에서 주소창에 http://127.0.0.1:5257 을 직접 연 창에서만 열립니다. 도메인 주소에서는 그 주소를 안내합니다."* — 대가: 자료관리 때마다 주소를 바꿔 한 번 더 로그인(첫 1회 [회사서버 컴퓨터] 선택) · LOCAL 고객도 같음 / 이득: 바깥에서 지울 길 0 · DDL 없이 1차 게시
  - **(나)** *"(가)에 더해, 127.0.0.1 창에 뜬 번호를 도메인 탭에 넣으면 그 탭에서 10분간 자료관리가 열립니다."* — 대가: 10분 동안 증표가 새면(화면 취약점·원격제어) 바깥 초기화 · 10분마다 번호 / 선결: 익명 확인 API·CORS 축소·권한창 실측 ⇒ 2차 이후
  - ⇒ **(나)는 (가) 위에 얹힌다. 어느 쪽이든 1차 게시는 같고 서로 뒤집는 게시가 없다.**

**§1. 각하·정정**
1. 개발 「5357 겹침」 — 충돌은 🟡 재현 없음 ⇒ **범위 밖 보류**. 슬롯2 이상은 터널 ingress 5257 고정(`InstallerBootstrapController.cs:133`)이 선행 결함.
2. 설계 「Q4 숫자 오염」 — 전제 「실물 DB 는 이미 이관된 DB」 근거 없음 ⇒ **전제 각하**(⬜). 오염은 4번(계산서 66,631)에 한해 병렬이슈15 §3 으로 따로 성립 · 2·3(화면)·5(INSERT IGNORE 멱등)는 근거 없음.
3. 검증 AC 표 — AC-04·13 의 A-1 칸은 **열 오기**(A4 는 A-2 공격) · AC-03 (b)+(d) 사유 정정(재발급이 직전 비밀을 죽임 `DeviceAuthGate.razor:600`) · AC-05b A-1 🟡PASS → 🟢(`web-server.ps1:33`·`:98`).
4. 설계 B3·검증 A3 「A-2 가 G-39 동시1 을 깨서 복사 N대」 — 축②가 메인 줄 번호만으로 통과(`DeviceAuthMiddleware.cs:186-194`) 🟢 ⇒ 「동시 1」은 **현행에서도 요청 단에서 강제되지 않는다.** 「A-2 가 새로 여는 구멍」 논거 각하.
5. 검증 D4·H-6 근거 `Program.cs:422-432` 는 주석 ⇒ `MigrationRunner.cs:143-161` + `Program.cs:467-478` 로 교체(결론 유지).
6. 개발 C④ 「S1 + Origin 조건 + LOCAL 제외」 — Origin 은 **비브라우저 요청이 임의로 붙일 수 있고** API 는 도메인 유무를 모른다 ⇒ **위치 증명 근거로서** 각하.
7. 보안상무 §4-1 🟢방어(pairId 는 JWT 로만 + LNA) — 공격자는 JWT 가진 원격 대표일 수 있고 브라우저 정책은 불인정 ⇒ 기각 · (나)면 P0 그대로.

**§2. H 판정**: H-1 채택(등급 (가) P1 / (나) P0 · refresh 는 응답 본문 `AuthService.cs:484`, 쿠키 설정 0 🟢) · H-2·H-8 (나) 한정 · H-3·H-4·H-7 채택 · H-5 채택(시점 **1차**) · H-6 채택(근거 교체).

**§3. D 판정**
- **D2 보류** — A-2 는 축② 폐지와 한 몸. 2차 이후 · 사장님 요금 결재.
- **D3 — 1차 게시 묶음 (DDL 0 · 선택 무관)**
  - **K1** 자리 판정 한 함수: 터널헤더 0 ∧ loopback ∧ Host∈{127.0.0.1, [::1]} ∧ Host.Port = LocalPort. 부르는 곳 `MainPcOnlyAttribute.cs:37-49` · `DeviceController.cs:101-114` · `MigrationController.cs:684`. **`AuthController.cs:136-140`·`:517-520` 무접촉(#43).**
  - **K2** 8/18작4 철회 — 표식이동·revoke(`TenantDeviceService.cs:269-309`)와 자동승인 갈래(`:711`) 제거 → 로컬 로그인도 대표 버튼 있는 대기 관문(`DeviceAuthGate.razor:80-126`).
  - **K3** `_blocked` 화면에 대표용 **[회사서버 컴퓨터] 합류 버튼만**(채워진 [승인] 제외) — 127.0.0.1 창이 지문으로 기존 approved 줄에 붙으면(`:226-233`) 대기 관문이 안 떠 막다른 길.
  - **K4** 안내문 *"이 컴퓨터가 자료보관 컴퓨터면 주소창에 http://127.0.0.1:{포트}"* · localhost:5234 안내 금지.
  - **K5** 게이트 — 요청 3종(CF 헤더 / Host localhost:port / Host 127.0.0.1:LocalPort) 시험 신설(DB 불필요 → build 잡에서 실제로 돈다) · 메인PC 양성 대조군 · GC40·41 기대 반전 · GC42 「왜 PASS」 확인 · GC44 폐기 또는 대상 변경.
  - **1차에서 빼는 것**: S1·F-1·안2·F-4·A-1·A-2·DB-89 재적용·is_parent·CORS 축소.
  - K2 대가 🟢: 합류가 ① 키를 재발급해도 도메인 탭이 ① 번호면 축②로 계속 통행 · 도메인 탭이 ② 면 영향 없음 · 신규 설치 대표는 첫 로그인에 대기 관문 1회.
  - 보안상무 「127.0.0.1 리터럴 직결 로그인 → ① 합류(새 줄 0)」: LAN·터널·LOCAL 사내 PC 가 못 만드는 조건이라 D4 반려 사유에 안 걸림 🟢 — **채택하면 K3 를 대신할 수 있다. 단 지문 조회(`:212-233`)보다 먼저 판정해야.**
- **D6 기술 허용 조건(최종 사장님 결재)** — 도메인 탭 + 경로 직접 입력 + 보태기만(G-OW9 `MdbOverwriteReseedGateTests.cs:305-322`) · 2·3 은 화면 동작, 5 는 표시 여부만 · **9,167 을 기대값으로 쓰지 않음** · 4·7·8 금지 · localhost 재로그인 안내 금지.
- **D7** — AC-02 재작성(「127.0.0.1 창 첫 로그인 → 첫 세션 업무·자료관리 200 · 도메인 탭 안 갇힘」) · **AC-02′ 신설**(「localhost:5234 자료관리 거부 + 안내」) · AC-03 「결재된 규칙대로, 어느 규칙이든 업무 403 0」 · AC-17·19 (나) 한정 · AC-18 「슬롯2 이상 ingress」 전제.

**§4. 1.3.39 Host 위조 봉합을 1차에 넣는 안전성**
- 새로 막는 것 = localhost:5234 자료관리 🟢 — 사장님은 이미 기기 문에 막혔고 여는 길(재로그인)은 금지 ⇒ 실제 손실 0. 🔴 **단 K1 단독 게시는 덮어쓰기 실측 길을 0 으로 만든다 ⇒ K1~K4 한 게시.**
- #43 — 워치독 업데이트 백업은 mysqldump 직접(`WatchdogBackupRunner.cs:9-15`) 🟢 무영향.
- 게이트 — 메인PC 검사(`MigrationController.cs:684`)가 확인문구 검사(`:695`)보다 먼저라 **G_OW5 3건(`:224`·`:234`)이 400 → 403 빨간불**(보이는 반전) → 픽스처(`:451-455`)에 Host·LocalPort 추가. G-OW8·9·W13-e 불변.
- LOCAL 설치 — 바로가기가 여는 `localhost:5257`(`hitpan-start.bat:61`)도 자료관리 거부 ⇒ 안내 필요.
- **is_parent 1차 제외** — JWT 에 없음(`AuthService.cs:446`) → 필터 비동기·DB 조회 필요 · **사장님 계정 값 ⬜ → 모른 채 넣으면 사장님을 잠글 수 있다** ⇒ 조회 후 2차.

**§5. DB 번호** — 1차는 DDL 0 · **DB-120 = DB-89 조건부 재정합**(메인 줄 2개면 UNIQUE 안 만들고 기록만 · 실패 0 · 같은 PR 에 출하 DDL·시드) · A-2 표는 결재 후 배정(예약 금지) · 규칙: 번호는 작업지시서 한 곳 · 게시 후 수정 불가(체크섬 없음 `MigrationRunner.cs:91-108`) · 번호 든 PR 은 머지 직전 최신 main 기준 CI 재실행(충돌 게이트는 병렬 PR 을 못 봄 🟡).

**§6. 2라운드 강제 질문 (기술)**
- 존: ①K3 흐름 — 127.0.0.1 창이 새 대기 줄일 때 / 기존 approved 줄에 붙을 때 둘 다 막다른 길 0? ②K2 이후 신규 설치 대표가 3분 안에 끝내는 관문 문구 대본 ③(가) 안내문이 LOCAL·슬롯2 이상에서 맞는 주소를 내나
- 개발: ①K1 시그니처·부르는 곳 전수·#43 두 자리 무접촉 증명 ②DB-120 SQL — 메인 2줄 DB 에서 success=1·UNIQUE 미생성? 출하 DDL 트리거를 C# 게이트 적재기(`DeviceAndKeyGateTests.cs:112`)가 읽나 ③K2 이후 GC42·GC44 PASS 이유 ④나중에 (나)로 갈 때 K1~K5 중 뒤집히는 줄 0?
- 검증: ①(가) 기준 AC 재작성본(02/02′·05b·12·13·14) ②K5 무력화 FAIL 사유까지 ③사장님 조회로 도메인 탭 신분(①/②)·이관 이력·is_parent 확정 — D6·is_parent 편입이 여기서 갈림 ④테스트 환경 Playwright 로 127.0.0.1 창 지문이 기존 줄과 일치하는지

**별건**: mobile-register 🟢코드·실행⬜ → 🟡 P1(QR 모바일 등록) · `hitpan-keys.conf` — 설치기가 icacls 결과 코드를 확인 안 함(`iss:1924-1926`) 🟢 · 실제 ACL ⬜(권한 분류기 거부 · 우회 안 함) → 보안상무 별트랙 유지 · 5357 보류.

### 🔴 4-5. 주심 판정 대조 — 2라운드에서 정리할 5곳 (PM)

| # | 자리 | 보안상무 | CTO | PM 메모 |
|---|---|---|---|---|
| R-1 | **K1 조건 수** | 5조건 — ⑤ `Origin` 부재 또는 = `http://127.0.0.1:{PORT}` ∧ `Sec-Fetch-Site ∈ {same-origin, none}` (§4-3-1) | 4조건 — 터널헤더 0 ∧ loopback ∧ Host 리터럴 ∧ **Port = LocalPort** | CTO 판정은 보안상무 Q-A 정정 전에 작성됐다. CTO 각하 6(Origin 은 비브라우저가 위조 가능)은 **위치 증명** 용도에 대한 것이고, ⑤ 는 **브라우저 교차사이트 요청 거부** 용도라 충돌하지 않는다 ⇒ **통합 후보 = 5조건 + Port=LocalPort** |
| R-2 | **127.0.0.1 창의 기기 신분** | 리터럴 직결 로그인 = **서버줄 자동 합류**(새 줄·pending 0) | K2 대기 관문 + K3 `_blocked` 화면 **[회사서버 컴퓨터] 합류 버튼** · 자동 합류는 K3 대체 가능(지문 조회보다 먼저) | K3 버튼 자체가 K1 판정 없이 뜨면 1.2.89 「증명 없는 자기 선언」(설계 기각안 D) 그대로 — **버튼도 K1 로 묶어야** |
| R-3 | **is_parent 시점** | 당장봉합에 포함(파괴급 DB 게이트) + 표식 이동 감사알림 | **1차 제외** — 사장님 계정 값 ⬜ → 잠김 위험 · 조회 후 2차 | K2 가 표식 이동 자체를 없애면 감사알림 대상은 서버줄 서비스 재표식만 남는다 |
| R-4 | **(나)의 범위** | 파괴급은 **어떤 경우에도 터널 너머 금지** · 찾아보기만 10분 증표 가 | (나) = 도메인 탭 10분간 **자료관리(4기능)** | 보안상무 선을 따르면 (나)는 **「찾아보기만」** 으로 좁혀야 한다 ⇒ 결재 문장 수정 필요 |
| R-5 | **D6 오늘 실측** | 도메인 + 경로 입력 + 보태기 · 덮어쓰기는 안 A 게시까지 대기 · 임시 우회 안내 금지 | 동일 + 2·3 화면 동작만 · 5 표시만 · 9,167 기대값 금지 · 4·7·8 금지 | **두 주심 일치** → 사장님 결재 안건 |

### 4-6. 주심 대조 — 보안상무 최종 입장 (2라운드와 동시)

- **R-1 — 포트 조건 추가 동의. 자리 판정 = 5조건, 계정 조건은 따로.**
  - 🟢 API 는 `API_PORT`·`LocalPort` 를 읽는 곳이 없다(grep 0) ⇒ 믿을 포트는 `Connection.LocalPort` 뿐. 다중 사업자에서 다른 회사 API 로 증명이 옮겨지는 것을 막는다.
  - **자리 함수 = Host 가 `127.0.0.1`·`[::1]` 리터럴 ∧ Host.Port = LocalPort ∧ 소켓 loopback ∧ 터널헤더 0 ∧ 교차사이트 거부.**
  - 교차사이트 거부식: `Origin` 이 있으면 자기 출처와 같아야 · `Sec-Fetch-Site` 가 있으면 same-origin·none · **둘 다 없으면 통과**(주소창 탐색·구형 브라우저·그 PC 의 비브라우저 도구뿐). **파괴급은 POST 로만.**
  - CTO 각하 6 과 충돌 없음(용도가 위치 증명이 아니라 교차사이트 차단) · CTO 각하 7 수용.
- **R-2 — 입장 변경: 자동 합류 철회 · CTO K3 버튼 채택. 단 서버에서 K1 로 묶는다.**
  - 조건 1: 버튼은 `is-main-pc`(K1) 참일 때만 표시.
  - 🔴 조건 2: **서버가 `is_main_pc` 줄의 `reissue-key` 에 K1 강제**(`TenantDeviceService.cs:1100-1113`) · 대기 관문의 1.2.89 합류 버튼에도 동일. 화면 감추기는 차단이 아니다(`MainPcOnlyAttribute.cs:11-14`) ⇒ 바깥 폰 합류 경로(M4) 닫힘.
  - 바꾼 이유: 로그인 경로(`AuthController.cs:136-140`) 무접촉 · 로그인 순간 신분이 조용히 바뀌는 계통 재발 방지 · 로그인 POST 는 교차사이트 요청을 받는 자리라 자동 동작을 얹으면 피해 범위 최대.
  - 대가 🟡(요금 결재 표시): 도메인 탭의 **신규 합류가 닫힌다** — [승인](+1) 또는 기존 합류 유지뿐.
- **R-3 — 조건부 동의: 사장님 조회로 `is_parent=1` 확인 후 2차.**
  - 🟢 **Host 위조 P0 는 K1 만으로 닫힌다**(리터럴 Host 는 프록시가 못 만든다 `web-server.ps1:33·98`) ⇒ is_parent 는 P0 봉합이 아니라 파괴 권한을 좁히는 방어층.
  - 조회 결과 is_parent 줄이 0 이면 사장님 잠김 — 탈출구 설계 선행(8/19 §4-2).
  - 감사알림 대상 재정의: ⓐ 메인 줄 `reissue-key`(🟡 `device_key_reissue` 기록 추정) ⓑ 기동 시 서버줄 재표식·신규 등록(`MainPcRegistrationService.cs:149-213`) ⓒ K1 거부(403) 반복 — 탐침.
- **R-4 — (나) 결재 문장 수정:**
  > *"(가)에 더해, 127.0.0.1 창에 뜬 번호를 도메인 탭에 넣으면 그 탭에서 10분간 **찾아보기(폴더 고르기)만** 열립니다. 모두 지우고 새로 가져오기·초기화·백업복원은 어떤 경우에도 127.0.0.1 창에서만 됩니다."*
  - 허용 가능하나 **가치가 낮다**(유출 피해는 파일 목록 노출 · 경로 직접 입력으로 같은 일을 한다). 선결: CORS 축소·익명 확인 API·권한창 실측. 등급 (가) P1 / (나) P0.

### 4-7. 주심 대조 — CTO 최종 입장 (2라운드와 동시)

- **R-1 — 동의. K1 = 5조건 + Port=LocalPort.**
  - 식: 터널헤더 0 ∧ loopback ∧ Host∈{127.0.0.1, [::1]} ∧ Host.Port = `Connection.LocalPort` ∧ (Origin 부재 ∨ Origin = 같은 127.0.0.1·[::1] 주소) ∧ `Sec-Fetch-Site ∈ {same-origin, none}`.
  - ⑤ 는 **API 요청에만** — 자료관리 판정·is-main-pc·메인 줄 합류·번호 발급. 로그인 자체엔 안 걸고 **로그인이 합류를 일으키는 갈래에만**(로그인은 터널로도 쓰인다).
  - 문서 탐색 GET 엔 안 건다 — 화면 파일은 인증 흐름보다 앞에서 나가 필터에 안 닿는다(`Program.cs:583-596`) 🟢 · 걸면 도메인 탭 안내 링크로 연 창이 `cross-site` 로 와서 K4 경로가 스스로 막힌다 🟡.
  - 🔴 **`Sec-Fetch-Site` 가 없으면 거부**(옛 브라우저·비브라우저는 파괴급 불가) · 게이트에 cross-site·헤더 부재 2종 추가. ← **보안상무(둘 다 없으면 통과)와 갈림 — 최종 판정에서 정리**
- **R-3 — 조건 둘을 달고 조건부 편입 수용.**
  - ① 사장님 조회에서 `is_parent=1` 확인 ② **부모계정 0명인 회사는 tenant_admin 으로 판정하는 절 + 그 분기 게이트.**
  - ②가 필요한 이유 🟢: `is_parent=1` 을 쓰는 곳은 부모계정 생성기 한 곳(`CompanyBootstrapProvisioner.cs:244-256`)뿐이고 옛 설치를 메우는 마이그는 0건 ⇒ 사장님 값만 보고 넣으면 **다른 고객**의 파괴급 기능을 잠글 수 있다 ⬜.
  - JWT 에 없으므로(`AuthService.cs:446`) 필터는 비동기 DB 조회로.
- **R-4 — 좁히는 데 동의.** 수정 문장 **(나′)**: *"(가)에 더해, 자료보관 컴퓨터 앞에서 127.0.0.1 창에 뜬 번호를 도메인 탭에 넣으면, 그 탭에서 10분간 「찾아보기(폴더 고르기)」만 열립니다. 모두 지우고 새로 가져오기·초기화·백업복원은 어떤 경우에도 127.0.0.1 창에서만 됩니다."*
  - 대가: 10분간 증표가 새면 바깥에서 그 컴퓨터의 **모든 드라이브·폴더 목록**이 보인다(`MigrationController.cs:536` `DriveInfo.GetDrives()`) 🟢 · 10분마다 번호 · 경로 직접 입력으로 이미 이관이 되므로 편의가 작다 · 2차 이후. **CTO 권고는 (가).**
- **R-2 — 기울기: 자동 합류가 K3 버튼보다 낫다.** ← **보안상무는 반대로 K3 버튼으로 옮겼다 — 교차 · 최종 판정에서 정리**
  - 127.0.0.1 창은 5조건 로그인 시 자동 합류(지문 조회 `TenantDeviceService.cs:212-233` 보다 먼저) · 버튼은 채워진 [승인]을 잘못 누를 자리를 남기고, 서버 결속이 같다면 자동 쪽이 #25 에 맞다.
  - 「K3 도 K1 로 묶어야」 동의 — **묶는 자리는 서버.** 지금 재발급 API 는 대표면 어느 줄이든 어디서든 발급(`DeviceController.cs:412-427` · 위치·메인줄 조건 0) 🟢 ⇒ **「메인 줄 재발급은 K1 요청에서만」**.
  - 🔴 그러면 같은 API 를 쓰는 **기존 대기 관문의 도메인 합류**(`DeviceAuthGate.razor:112-123`·`:597-600`)도 닫힌다 — 이미 합류한 탭은 축②로 계속 통행 🟢 · **새 도메인 탭은 별도 줄(+1 슬롯) ⇒ 사장님이 지적한 「2중」이 요금으로 되살아난다 — 결재 선택지에 대가로 적어야.**
  - 기술 메모: 번호 입력 수단은 자료관리보다 **도메인 탭 신분 합류(슬롯 0)** 에 쓸 때 위험이 작다(새도 업무 API 까지 · 파괴급은 K1 로 막힘).
- **번호·시도 제한 기준 (확정)** — 현행 쓰기 제한 사용자당 분당 600(`RateLimitMiddleware.cs:67-71`)이라 4자리는 17분에 소진 ⇒ **6자리 · 페어당 틀림 5회 즉시 폐기 · 페어 수명 5분 1회용 · 사용자당 활성 페어 1 · 신규 발급 시간당 10 · 127.0.0.1 창이 실제로 활성화한 페어에만 유효(메인PC 없이 원격 대입 불가) · 고정시간 비교 · 실패 감사 기록** ⇒ 페어 하나 적중 확률 5/10⁶.

## §5. 2라운드 — 강제 질문 답변·최종 입장

### 5-1. 설계팀장 존

1라운드 각하 2건(Q4 숫자 오염 전제 · A-2 동시1 논거) 수용 · 재상정 없음.

**[CTO-1] K3 흐름** (전제: K2 로 `TenantDeviceService.cs:269-309`·`:711` 제거)
- (a) 127.0.0.1 창이 **새 대기 줄** → 막다른 길 0 🟢 — pending INSERT → `AuthController.cs:162·176` → `Login.razor:309-312` → `DeviceAuthGate.razor:308-309` gate-status false → `:368-379` 대기 관문 → 대표 두 버튼 `:80-126` → [회사서버] `:601-689` → ① 재발급·확인 → `:670` reload → `:381-407` 통과. ① 탑승 도메인 탭은 축②(`DeviceAuthMiddleware.cs:186-194`)로 계속 통행. 남는 것: **보이는 2중**(대기 줄 잔존 `DeviceAndKeyGateTests.cs:1204`) · 채워진 [승인] 오클릭 +1.
- (b) 지문으로 **기존 approved ②** 에 붙음 → K3 없으면 막다른 길 🟢 — `:226-233` → `:588` → `AuthService.cs:127-135` 가 이 창에 ② 번호 → 업무 API 축① 키 없음 · 축② 메인 줄 아님 → 403 → `HitPanApiAuthHandler.cs:86-89` → `_blocked`(`:179-227`) 대표 버튼 0. K3 있으면 0 이지만 ② approved 잔존 → 슬롯 2.
- ⇒ K3 로 막다른 길은 없어지나 보이는 2중은 그대로. **자동 합류면 (a)(b) 갈래 자체가 안 생긴다.**

**[CTO-2] 신규 설치 대표 관문 문구 대본**
- 화면1 대기 관문(도메인) — 제목 「이 컴퓨터를 히트판에 등록합니다」 · 본문 「지금 이 화면을 연 컴퓨터가 히트판을 설치한 컴퓨터(자료보관 컴퓨터)인가요?」 · **채운 버튼** 「네, 히트판을 설치한 이 컴퓨터입니다」(작은 글씨 「기기 수가 늘지 않습니다.」) · **외곽선 버튼** 「아니요, 다른 컴퓨터입니다」(「기기 1대가 추가로 사용됩니다.」) — 🔴 1.2.89 대비 채움/외곽선 **반전 = 요금 방향 변경 → 결재**.
- 화면2 자료관리가 막힌 도메인 탭 — 제목 「자료관리는 자료보관 컴퓨터 전용 창에서 열립니다」 · 본문 「백업·초기화·자료 찾아보기·모두 지우고 새로 가져오기는 회사 자료를 지키기 위해 히트판을 설치한 그 컴퓨터에서만 열립니다. 지금 그 컴퓨터 앞이라면 아래 버튼을 누르고 같은 아이디로 한 번 더 로그인하세요.」 · 버튼 **[이 컴퓨터에서 열기]** + 주소 글자 `http://127.0.0.1:5257` · 작은 글씨 「다른 컴퓨터나 휴대폰에서는 열리지 않는 것이 정상입니다.」
- 화면3 127.0.0.1 창 첫 로그인 뒤 1회 — 「이 창은 자료보관 컴퓨터 전용 창입니다. 자료관리는 이 창에서, 평소 업무는 원래 주소에서 하세요.」
- 시간 🟡 약 2분(로그인 30초 + 화면1 20초 + 새 창 로그인 40초 + 메뉴 이동 30초) · **실측 전 「3분 통과」 금지** · 새 창에 돌아갈 주소가 안 실려(`RedirectToLogin.razor:6`) 메뉴 이동 1회 추가 🟢.

**[CTO-3] 안내 주소**
- 슬롯1 정식 도메인: ingress `localhost:5257`(`InstallerBootstrapController.cs:133`) → 맞음.
- 슬롯2 이상: 두 번째 회사 도메인도 5257 로 들어가 **슬롯1 API 가 응답** — 안내문이 아니라 도메인 전체의 선행 결함 ⇒ **이번 범위 밖 · AC-18 전제 별트랙** · 1차에 슬롯2 보증 문구 금지.
- LOCAL 슬롯1: 프록시는 db.conf API_PORT(`web-server.ps1:9-33`) → 맞음 · 바로가기 `localhost:5257`(`hitpan-start.bat:61`)은 K1 거짓 → 안내가 뜨는 게 맞다 · LOCAL 슬롯2 는 웹 5234 고정(`web-server.ps1:4`) → 범위 밖.
- 포트 조립: K1 판정은 요청별 `LocalPort` · 안내 주소는 「이 프로세스가 연 포트」(`IServerAddressesFeature` · src grep 0 → 신설) 🟡.

**[보안-a]** A-1(도메인 탭 증표) 전체를 **(가)로 대체 — 동의.** (나) 「찾아보기만」에도 동의하나 **보류 권고** — 경로 직접 입력이 이미 도메인에서 된다(`MdbMigration.razor:33-36` · `MigrationController.cs:300-328`) · 찾아보기 응답은 드라이브·폴더 목록(`:445-449`)이라 저위험 아님 · 선결 비용(`TenantMiddleware.cs:71-75` · CORS 축소 · `RateLimitMiddleware.cs:67-80`)이 이득보다 크다.

**[보안-b] 127.0.0.1 직결 합류 SQL 자리**
- 자리: `RegisterOrRefreshAsync` **맨 앞** — 장비넘버 조회(`:212-223`)·지문 조회(`:226-233`)보다 먼저.
- 입력: AuthController 가 K1 함수로 채우는 새 필드 · `:136-140` 무접촉 · `DeviceDtos.cs:119-123` 옆에 추가.
- SQL: 읽기 `SELECT device_id … WHERE tenant_id=@T AND is_main_pc=1 AND status='approved' LIMIT 1` · 갱신은 그 줄 **`last_seen_at` 만**(`:560-579` 보존 규칙) → 그 번호 반환.
- 🔴 금지: `is_main_pc`·`status`·`auth_key_hash` 쓰기 0 · 다른 줄 revoke 0 · 재발급 0(G-39 무접촉) · 지문 조회 0(② 흡수 시 도메인 탭 갇힘) · 메인 줄 없으면 합류 안 하고 종전 대기 흐름(메인 줄 INSERT 0).
- ⇒ `:269-309` 의 두 UPDATE 와 쓰기 대상이 안 겹쳐 되돌릴 자리 없음 · K2 삭제와 같은 PR · 번호는 127.0.0.1 저장소에만 · 로그인 POST 는 ⑤ 로 교차사이트 거부.

**[PM R-2] — 자동 합류 선택 · K3 버튼 추가 안 함**
- 이유: ①K1 은 원격이 못 만드는 신호(D4 반려 밖) ②클릭 0 = #25 ③새 줄 0 → 보이는 2중 안 늘어남 ④재발급 0 → 도메인 탭 탑승 비밀을 안 죽임.
- 🔴 **도메인 대기 관문의 기존 1.2.89 [회사서버] 버튼은 K1 로 못 묶는다** — 도메인은 K1 이 항상 거짓 → 묶으면 도메인 합류 소멸 → +1 슬롯 강제. 현행 유지 근거: K1 은 기기 신분을 안 읽으므로 자기 선언 피해는 **요금 1대**로 한정(8/20 [3-V] 8회차 §3) · 자료관리 위험 0 ⇒ **요금 1대 수용 여부를 사장님 결재로 명시.** ← **보안상무 R-2 조건 2(1.2.89 합류 버튼에도 K1 강제)와 정면으로 갈림**

**[PM #25]**
- 주소 타이핑만 요구하면 🟡 FAIL 위험(`127.0.0.1:5257` 은 설명 없이 치기 어렵다).
- **[이 컴퓨터에서 열기] 버튼은 막히지 않는다** — 새 창 문서 GET 은 cross-site 로 올 수 있으나 정적 파일·fallback 은 인증·기기 미들웨어보다 앞(`Program.cs:579-597` < `:601-613` · `:625-628`) · ⑤ 는 API 요청에만.
- 그 창의 로그인은 same-origin 🟢 — ApiBaseUrl 빈 값(`src/HitPan.Web/wwwroot/appsettings.json:3` · `iss:1255-1266`) → 현재 출처로 호출(`HitPan.Web/Program.cs:35`) · Origin `http://127.0.0.1:5257` + same-origin → ⑤ 통과 · CSP `connect-src 'self'`(`Program.cs:541`) 허용.
- 조건 셋: 클릭 즉시 await 없이 `window.open`(주소는 화면이 뜰 때 미리 받음) · 로컬 네트워크 권한창 ⬜ → 주소 글자 병기 · 돌아갈 주소 없음 → 메뉴 이동 1회(또는 returnUrl 가산).
- 판정: 버튼 안은 #25 **후보** · AC-19 화면 실측 전 「통과」 금지.

**바꾼 것 (11)**: A-1 전면 철회→(가) · (나) 찾아보기만+보류 · **S1 철회**(D4 수용) · §4-1⑤ 철회 · 폐기→흡수(흡수는 2차) · 5조건+Port · localhost:5234 안내 철회 · A-2 D2 보류 · K3 대신 자동 합류 · is_parent 1차 제외 수용 · 교체 절차 2차

**최종 입장**: ①(가) — 자료관리 4기능은 127.0.0.1 직결 창에서만 · 도메인 탭은 [이 컴퓨터에서 열기] 안내 · (나) 보류 ②증표 0 · ⑤ 는 로그인·합류·파괴급 API 필수 · CORS 축소 병행 · 번호 릴레이 없음 ③127.0.0.1 로그인 = 쓰기 없는 자동 합류 · 도메인 1.2.89 합류 유지(요금 1대 결재) · A-2·흡수·교체는 2차 ④1차 = K1+K2+자동합류+K4+K5 한 게시 · 오늘은 D6 조건만 ⑤요청 3종 + 127.0.0.1 로그인 직후 업무·자료관리 200 + 교차사이트 로그인 거부 대조군 · build 잡 · GC40·41 반전 ⑥버튼 + 주소 글자 + 대본 3화면 · 3분은 실측으로만 · 관문 버튼 방향·is_parent 는 결재

### 5-2. 개발팀

**[CTO-1 + R-1] K1 판정 함수**
- 시그니처(안): `HitPan.API.Security.MainPcSeat.IsDirectConsole(HttpContext) → bool` · 기존 `MainPcOnlyAttribute.IsMainPc` 는 이름을 남기고 위임 ⇒ `MigrationController.cs:684`·G-OW 게이트 수정 0.
- 부르는 곳 전수 🟢: 필터 `MainPcOnlyAttribute.cs:53` · `MigrationController.cs:684` · 복사본 `DeviceController.cs:106-113` · 속성 부착 `DataResetController.cs:19`·`BackupController.cs:23`·`MigrationController.cs:438` · 화면 `MainPcOnly.razor:51`·`MdbMigration.razor:633` · 게이트 `MdbMigrationV2ModeGateTests.cs:108`·`MdbOverwriteReseedGateTests.cs:451-455`.
- **AuthController 무접촉** 🟢 — grep 에 주석만 걸림 · `:136-140`·`:517-520` 은 따로 쓴 인라인 식 ⇒ PR 조건 「`AuthController.cs` 변경 0줄」 + 두 식이 원문과 같고 `MainPcSeat` 미참조를 build 잡 울타리 시험으로.
- 조건: ①터널헤더 없음 ②RemoteIp loopback ③Host 리터럴 `127.0.0.1`/`[::1]` ④`Host.Port == Connection.LocalPort`(Port 비면 거짓) ⑤ **`Origin` 이 있는데 자기 출처가 아니거나 · `Sec-Fetch-Site` 가 있는데 {same-origin, none} 이 아니면 거부 · 헤더가 없으면 통과**(비브라우저는 ①~④가 판정).
- ⑤ 범위: K1 안에 넣고 K1 은 `/api` 필터·액션에서만 불림 · 문서 탐색은 인증 앞(`Program.cs:583-596`) 서빙이라 **자동 제외** 🟢 · 조회성 GET(찾아보기·목록·is-main-pc) **포함** — 같은 출처 fetch 는 GET 에도 `Sec-Fetch-Site: same-origin` 🟡.
- Blazor WASM 실제 값 🟡(표준 · 실측 ⬜): 같은 출처 POST = `Origin: http://127.0.0.1:5257` + same-origin · 같은 출처 GET = Origin 없음 + same-origin. SignalR: negotiate 같은 출처 POST · WebSocket 은 Origin 항상(RFC 6455) · 허브엔 K1 없음(`Program.cs:619-622`) → 영향 0.
- `Sec-Fetch-*` 미지원: 구형 브라우저 교차출처 POST 는 Origin 이 붙어 거부 · 교차출처 GET 은 ⑤ 를 지나도 Bearer 가 없어 401.
- 각하 6 과의 차이: 각하 6 은 Origin 을 **허가** 근거로 쓴 것 · ⑤ 는 **거부만** · 위조로 얻는 건 거부 회피뿐이고 위조 가능한 비브라우저는 ①~④에서 걸림.

**[CTO-2] DB-120 초안 (2차용)**
- 구성: ①생성컬럼 `main_pc_key` 조건부 추가 ②메인 줄 2개 이상 회사 수·UNIQUE 존재 확인 → **중복 0 일 때만 UNIQUE** ③중복 회사는 `audit_trail` 에 `db120_mainpc_dup` 1줄(재실행해도 1줄) ④BEFORE UPDATE 트리거는 **「폐기로 넘어가는 순간」만 막고 이미 폐기된 줄은 되살리지 않음**.
- 🔴 **DB-89 와 다른 점 = 결재 사항**: DB-89 의 「revoked 메인 줄 → approved 복구」 UPDATE(`DB-89:96-99`) 제거(2줄 DB 에서 되살리면 슬롯 +1) · 트리거 의미 변경(아무 UPDATE 가 되살림을 일으키지 않게).
- success=1 근거 🟢: 러너가 파일 한 덩어리 실행(`MigrationRunner.cs:113-137`) · 사용자 변수 허용(`MigrationDbConnectionFactory.cs:58`) · `CREATE TRIGGER` 는 PREPARE 불가라 DROP+CREATE 🟡 · 러너 경유 트리거 생성 실적 ⬜(사장님 조회에서 DB-89 행 `checksum` NULL 여부로 판별).
- 입증 게이트: 메인 2줄 DB 에 러너로 2회 적용 → success=1 · UNIQUE 없음 · 감사 1행 · approved 수 불변.
- 출하 DDL 트리거를 게이트 적재기가 읽나 — **읽는다** 🟢(`DeviceAndKeyGateTests.cs:123-143` 이 `mysql.exe` 표준입력 · CI `build-and-test.yml:141` · 설치기 `iss:2204` 동일 · 이미 DELIMITER 3곳 `clean.sql:2626·2641·3820`).

**[CTO-3] K2 이후 게이트**
- **GC42** PASS 🟡 — 단 이유가 바뀐다(브라우저 줄 pending 잔존). 🔴 **표식이동(C1)을 되살려도 1/1 로 PASS — 못 잡는다** ⇒ 단언 추가: 서버줄 `is_main_pc=1 ∧ approved` · 브라우저줄 `is_main_pc=0 ∧ pending`.
- **GC44** — K2 이후 `IsLocalConsole` 을 읽는 곳 0 → 지키는 대상 없음 ⇒ **폐기** · 취지는 K5 N6 으로 이관.
- GC40·41 예정된 반전 → 재작성 · GC43 유효 · ⚠️ GC40~43 은 CI 에서 `return` 통과(`MainPcLocalConsoleGateTests.cs:281`) → db-gate `GATES` 미등록이면 반전이 CI 에 안 보인다.

**[CTO-4] (나)로 갈 때 뒤집히는 줄 0 — 조건 3개를 1차에 넣으면** 🟡
1. 필터를 1차부터 `OnActionExecutionAsync`(비동기) 형태로 — (나) 증표·is_parent 는 갈래를 더하기만.
2. `is-main-pc` 응답의 `isMainPc` 는 K1 만 뜻함 · 이후 필드(`browseSeat`·`canDestroy`)는 추가만 · 덮어쓰기 라디오(`MdbMigration.razor:90`)는 `isMainPc` 유지.
3. K4 문구에 「…에서만」을 쓰지 않는다.
- 예외: 자동 합류는 축②에 기댄다 → 2차 축② 폐지 시 합류 갈래에 비밀 발급을 **추가**(뒤집기 아님).

**[보안-a] DB 없이 build 잡에서 파이프라인 전체**
- 현황 🟢: `Mvc.Testing`/`TestHost` 없음(`HitPan.Tests.csproj:11-25`) · `InternalsVisibleTo` 없음 ⇒ 테스트 패키지 추가 + API csproj 에 `InternalsVisibleTo HitPan.Tests`.
- `WebApplicationFactory<Program>`: 환경 Production(Development 는 DB 시더 `Program.cs:489-494`) · 환경변수 `JWT_SECRET` 32자+(`AuthExtensions.cs:17-39`)·`DB_*` 더미(`InfrastructureExtensions.cs:24`) · `MainPcRegistrationService` 제거 · DB 없어도 끝까지 🟢코드/⬜실행(`Program.cs:480-486` · 약관·세션·기기 미들웨어 통과로 떨어짐 `TermsConsentMiddleware.cs:107-111`·`SessionLimitMiddleware.cs:143-149`·`DeviceAuthMiddleware.cs:252-258`).
- 요청: 같은 발급자 JWT(`AuthService.cs:441-464`) · `Server.SendAsync(ctx => Host·RemoteIpAddress·LocalPort·헤더)` 🟡 · 대상 `GET /api/devices/is-main-pc`(K1 단독) · `GET /api/migration/legacy-mdb/browse`(미들웨어→필터 전 구간 `MigrationController.cs:445-455`).
- 사례: **P0 양성**(127.0.0.1:5257·LocalPort 5257·loopback → 200) · **N1** +CF-Connecting-IP → 403 · **N2** Host localhost:5257 → 403 · **N3** +Origin evil·Sec-Fetch-Site cross-site → 403 · 보강 N4 Port≠LocalPort · N5 RemoteIp 192.168.0.50 + Host 127.0.0.1 · N6 본문·헤더 자칭.
- 단언: 상태코드·오류코드 정확히 · **401 이면 FAIL** · K1 조건을 하나씩 빼면 짝 사례만 FAIL 하는지.
- ⚠️ **정직 기재**: DB 없으면 기기 미들웨어(문 A)가 통과로 떨어진다 ⇒ **이 시험은 문 B 만 증명** · 문 A 는 DB 게이트를 `GATES` 에 등록해 따로.

**[보안-b + R-3] is_parent**
- 비동기 전환: `MainPcOnlyAttribute` → `OnActionExecutionAsync` + `RequireParent` 속성(파괴급 액션만) · `MigrationController.cs:684` 은 `PrepareOverwriteAsync` 에서 await · `DeviceController.cs:101-114` 복사본 → K1 호출 + `canDestroy` 동시 정리 · Backup/DataReset 클래스 속성 → 액션별 · 픽스처(`MdbOverwriteReseedGateTests.cs:444-458`) 의존성 추가 · 요청마다 인덱스 조회 1회(`clean.sql:4221`).
- 🔴 **대표인데 `is_parent=0` 이 생기는 경로 — 실재** 🟢: ①`UserService.CreateAsync` — TenantAdmin 권한 계정이 `account_type='tenant_admin'`(`UserService.cs:111-113`)인데 INSERT 에 is_parent 없음(`:130-145`) → 기본값 0(`clean.sql:4194`) ②`UpdateAsync` — 자식을 대표로 올려도 0 그대로(`:235-249`) · 부모를 `tenant_user` 로 내려도 막는 장치 없음(삭제만 `:285-290`) ③마이그 is_parent 백필 0건 ④1 은 프로비저너만(`CompanyBootstrapProvisioner.cs:245-252`) ⇒ **1차 제외 동의** · 조회 후 2차.
- 덤 🟢: 초기화 비번 재확인 문구는 「부모계정」인데 실제 판정은 `account_type='tenant_admin'`(`DataResetService.cs:80-83`) → is_parent 도입 PR 에서 맞춰야.

**[PM R-2] 자동 합류 vs K3**

| | 자동 합류 | K3 버튼 |
|---|---|---|
| 자리 | `AuthController` 로그인 초기화(`:111-141`)에 K1 속성 1줄(`:136-140` 무접촉) + `DeviceDtos.cs` 1속성 · `TenantDeviceService` `:210` 앞에서 K1 참이면 `existing = 메인 줄(is_main_pc=1 ∧ approved)` → 기존 흐름(`:366` 건너뜀 · `:560-579` 정체 보존 · `:588` 승인 반환) | `DeviceAuthGate.razor:179-228` 대표 구역 + K1 조회 · 서버 묶음 = `DeviceController.ReissueKey :412-433` 「대상이 메인 줄이면 K1 참일 때만」 |
| 규모 | **S** (≈3파일 20줄) | S~M · 화면 게이트는 xUnit 불가 → Playwright |
| 기존 계약 | K·G-41·`&& !isMainPc`·계수 SQL·신분유지 가드 **전부 무접촉** · 새 줄 0 | 서버 조건 없으면 화면 숨김(설계 기각안 D) |
| 통행 | 127.0.0.1 탭이 ① 번호로 축② 통과 → 첫 세션부터 업무 200 · 메인 줄 없거나 폐기면 K2 대기 | 서버 조건 넣으면 **도메인 탭 1.2.89 합류(`:112-126`)가 끊긴다 = 결재** · 재발급은 직전 비밀을 죽임(`:600`) |

⇒ **개발 판정: 자동 합류가 K3 를 대신한다** — 클릭 0 · 줄 증가 0 · 1.2.89 무변경.

**바꾼 것**: S1 철회·K2 찬성 · 1R 묶음 → CTO K1~K5(+K3 자리에 자동 합류) · 번호 방향 보안 D1 · A-2 동의 → 보류 · CORS 축소 선결 → ⑤ 선결·CORS 병행 · DB 번호 작업지시서 배정 · GC44 폐기 · is_parent 2차 · 5357 보류 수용

**최종 입장**: ①K1 한 함수(5조건+Port) · 파괴급 127.0.0.1 창만 · (가) · (나)는 찾아보기 한정 2차 ②⑤ 선결·CORS 병행 · (나) 번호는 로컬 생성→도메인 입력 + 시도 제한 ③127.0.0.1 리터럴 로그인 = 서버줄 자동 합류(새 줄 0) · A-2·흡수·교체 2차 ④1차 = K1(비동기 필터 형태)+K2+자동합류+K4+K5 한 게시 · is_parent·DB-120·CORS·A-1 2차 · 오늘 D6 만 ⑤build 잡 파이프라인 7사례 · GC40~43 재작성·GC42 단언 추가·GC44 폐기 · `GATES` 등록 · AuthController 울타리 · DB-120 은 2줄 DB success=1 입증 후 ⑥127.0.0.1 창은 로그인 1회로 끝 · 막힘 화면에 주소·할 일 · is_parent 는 조회 후 결재

### 5-3. 검증팀장 데이비드 박

**[CTO-4] 127.0.0.1 창 지문 — 🟢 측정 완료** (실물 0 · 격리 정적서버 18234/18257 · Playwright 1.62.1 · 지문 스크립트만 · 프로세스·포트 정리 확인)
- 🔴 **같은 브라우저·같은 화면이면 origin 이 달라도 지문이 같다** — `127.0.0.1:18257` · `localhost:18234` · `localhost:18257` · `[::1]:18257` 전부 **`HFPv2-b2e88f45`**. 원인: `hitpan_fp_id` 캐시는 origin 격리지만 `_envSeed`(플랫폼·언어·화면·TZ·UA 계열)가 origin 과 무관하게 같은 값을 재계산(`device-fingerprint.js:29-77`) 🟢.
- `hitpan_device_id` 는 origin 격리 → 127 창 로그인은 `DeviceId=null` → 서버가 **2순위 지문 조회**(`TenantDeviceService.cs:226-233`)로 **도메인 탭의 기존 줄**에 붙는다.
- ⇒ 🔴 **K3 단독은 막다른 길 확정** — pending 이 안 생겨 대기 관문·[회사서버] 버튼이 안 뜬다 → 축②(`is_main_pc=1`) 불충족 → 업무 403. 새 pending 줄이 되는 경우는 **다른 브라우저**(edge `ee0d14bd` ≠ chrome `3b819084`)·**화면 변경**(`5e372dc9`)뿐. ⇒ **127 직결은 지문 조회보다 먼저 「합류」로 판정해야**(K1 5조건으로 묶어서).

**[CTO-1] (가) 기준 AC 재작성** (실경로만 인정 · loopback 호출 불인정)

| AC | 기대값 | 슬롯/보이는 줄 | 재는 방법 | 대조군 |
|---|---|---|---|---|
| 02 | 메인PC 앞 `http://127.0.0.1:{API_PORT}` 첫 로그인 → 그 창 **첫 세션 업무 200 + 자료관리 4기능 200** · 도메인 탭 안 갇힘 | 1/1 | 백지 Sandbox + 실터널 · 네트워크탭 403=0 | K2 반전(자동승인 되살림) → 02′ 뚫려 FAIL |
| 02′ | 메인PC `http://localhost:5234` 자료관리 **403 + 「127.0.0.1:{포트} 로 여세요」** | 불변 | 5234 탭 찾아보기 403 문구·링크 | 127 창은 200 |
| 05b | 사내 PC `Host: localhost` 위조 `IP:5234` → 자료관리 거부 · 메인 표식 이동 0 | +0 | 다른 PC curl `…/api/devices/is-main-pc` = false | 미위조(301) |
| 12 | LOCAL 모드 사내 PC `IP:5234` → 메인PC 인정 0 | +0 | 백지 LOCAL + 2번째 VM | 메인PC 자기 127 창 |
| 13 | 이미 2~3줄 test1234 → DB 수술 0 · 127 창 합류 1회로 표식 1·승인 1·보이는 1 · 도메인 합류 탭 안 갇힘 | 1/1 | 업데이트 전 조회 → 조작 → 재측정 | 신규 백지(02) |
| 14 | 「합류/승인」 직후 조회·저장 200 (공통) | — | 네트워크탭 | — |
| 17′ | 메인PC 브라우저의 **타 사이트 스크립트**가 `fetch('http://127.0.0.1:{PORT}/api/auth/login' 또는 파괴급)` → 거부 | +0 | 격리서버에 악성 origin 페이지 · 교차 fetch 응답 코드 | 같은 origin 요청 통과 |

**[CTO-2] K5 무력화** (DB 불필요 → build 잡 · `GATES` 등록 필수)
- 요청 3종: ①CF 헤더 도메인 → 파괴급 403 ②Host `localhost:{port}` → 403 ③Host `127.0.0.1:{LocalPort}` → 통과. 죽이면: ①배제절 제거 → 터널 통과(8/10 항상참) ②loopback 단독 복귀 → 프록시·위조 통과(현행 P0) ③통과 조건 제거 → 사장님 자료관리 길 0.
- ⑤ 교차출처: 죽이면 타 사이트 요청 통과 → AC-17′ 뚫림.
- **양성 대조군**: 정당한 127 창 요청은 반드시 200 — 없으면 「전부 거부」가 초록(G-OW9 계통).
- GC40/41: 옛 기대 유지 = 8/18 표식 이동 생존 → FAIL 이 정상 · GC42 는 「왜 PASS」(계수 SQL 무접촉)까지 로그.
- 🔴 4파일은 CI 에서 `if(!ServerAvailable()) return` SKIP(3파일 39곳 🟢) ⇒ K5 신설분은 DB 없이 + `GATES` 등록. 안 하면 8/28 W1 재발.

**[CTO-3] 사장님 조회 문항 (SELECT 전용 · 최소 4문항)**
```sql
-- ① 기기 줄 전부 (도메인 탭 신분 ①/② · 슬롯)
SELECT device_id, LEFT(fingerprint,14) fp, device_type, is_main_pc, status, user_id,
       (auth_key_hash IS NOT NULL) has_key, last_seen_at FROM tenant_devices ORDER BY registered_at;
-- ② 표식이 옮겨졌나 (K2 철회 근거 · H1)
SELECT created_at, action_type, entity_id FROM audit_trail
 WHERE entity_type='device' AND action_type LIKE 'device_%' ORDER BY created_at;
-- ③ 실물이 이미 이관된 DB 인가 (D6 전제)
SELECT (SELECT COUNT(*) FROM migration_jobs) jobs,
       (SELECT COUNT(*) FROM tax_invoices WHERE source_type='migration') mig_inv,
       (SELECT COUNT(*) FROM hr_reports) hr;
-- ④ 대표 계정 · is_parent (헌법 #40 · 2차 편입 시점)
SELECT account_type, is_parent, COUNT(*) n FROM users WHERE is_deleted=0 GROUP BY account_type, is_parent;
```
- ① 에서 `is_main_pc=1·MAINPC-` 줄 vs `HFPv2-·approved` 줄로 도메인 탭 신분이 갈린다 · ② 에 `device_mainpc_adopt` 0건이면 H1 성립.
- ③ 전부 0 = 빈 DB → D6 걱정 없음 · >0 이면 4번(66,631) 숫자 판정 오염 확정.
- 접속정보는 사장님 권한(`db.conf` 거부 · 우회 안 함) · `--default-character-set=utf8mb4`.
- (개발 추가 요청: `migrations` 표 DB-89 행의 `checksum` NULL 여부 — 러너 경유/출하 DDL 시드 판별)

**[보안-a]** AC-17′ = 파괴급만(찾아보기 제외) · AC-18 = 슬롯2 이상도 그 테넌트 API_PORT 로 확인 성립 — ingress 5257 고정이 선행 결함이면 ⬜ · AC-19 = 파괴급 127 창 안 실행(릴레이 0) · 찾아보기만 (나) 10분 증표 · 팝업/권한창 안내·재시도 · **증표 소지 요청도 터널 헤더 있으면 무효** · CI: K5·AC-17′ 게이트는 `GATES=` 등록 · Playwright 는 build 밖 별 스텝.

**[보안-b] 감사알림 — 먼저 판정**
- K2 가 C1 을 없애면 로그인發 `is_main_pc` 쓰기 경로 0 ⇒ 감사 대상: **① 서버줄 서비스 재표식**(`MainPcRegistrationService.cs:149-171` — 다른 메인 줄이 있어도 되올림) **② 🔴 백업 복원**(`BackupService.cs:335·343` — mysqldump 가 `--ignore-table` 없이 `tenant_devices` 통째 덤프·복원 🟢 → **옛 백업 복원이 `is_main_pc=1` 줄을 되살려 2줄**).
- 무력화: 알림을 `if(false)` 로 죽이고 ①다른 메인 줄 존재 상태 재기동 ②옛 백업 복원 → 알림 0건이면 FAIL · 대조군: 표식 변화 없는 재기동은 알림 0(과알림 방지).

**[PM] R-1~R-5 레드팀 판정**: R-1 **5조건+Port**(4조건은 AC-17′ FAIL) · R-2 **자동합류**(K3 단독은 실측대로 AC-02·13 FAIL) · R-3 **2차**(⬜ 로 넣으면 AC-01 사장님 잠김) · R-4 **찾아보기만**(파괴급 넣으면 AC-07 FAIL) · R-5 동의 — 단 **Q③ 이 빈 DB 로 나와야 2·3 도 숫자 판정 가능**.

**바꾼 것**: AC-02 재작성·AC-02′ 신설 · 번호 방향 철회(로컬 생성→도메인 입력, 찾아보기 한정) · K1 5조건+Port · 54곳 → 39곳 🟢 + 15곳 🟡 · G-OW8 정책 종속 · AC-05b 정정 · 「오늘 2·3·5」 → Q③ 빈 DB 조건부 · 각하 수용

**최종 입장**: ①K1 5조건+Port · 127 직결 자동합류(지문 조회보다 먼저) · (b)/(a′)/(d) 반려 ②파괴급 127 창 안 · 찾아보기만 10분 증표 · ⑤ 필수 + CORS 병행 · 로컬 창 경고문 · `IsLocalConsole` 신분 근거 전부 반려 ③A-2 2차 · 옛 줄 흡수 · **백업 복원이 `tenant_devices` 를 덮는 것을 AC-13 정리 설계에 반영** ④1차 = K1~K5 한 게시(DDL 0) · is_parent·DB-120 은 조회 후 2차 ⑤출시 게이트 AC-02·02′·05b·12·13·14·17′ · K5 요청3종+⑤+양성대조군 · `GATES` 등록 · 127 창 Playwright · DB 번호 한 곳 ⑥주소를 클릭 가능 링크+할 일로 · 팝업/권한창 재시도 · 확인권한 `is_parent` 한정 · 교체 절차 없는 안은 반려

### 🔴 5-4. 2라운드 정리 (PM)

**수렴 — 세 진영 + 두 주심 (반대 0)**

| # | 수렴 사항 |
|---|---|
| C-1 | **(가) 방향** — 자료관리 4기능(찾아보기·모두 지우고 새로 가져오기·초기화·백업복원)은 **메인PC 에서 `http://127.0.0.1:{포트}` 로 연 창에서만**. 도메인 탭은 주소·할 일 안내. (나)는 「찾아보기만」으로 좁혀 **보류·2차** |
| C-2 | **K1 = 한 함수** — 터널헤더 0 ∧ loopback ∧ Host 리터럴 `127.0.0.1`/`[::1]` ∧ `Host.Port = Connection.LocalPort` ∧ ⑤ 교차사이트 거부 · `AuthController.cs:136-140`·`:517-520` 무접촉(#43) |
| C-3 | **K2 — 8/18작4 철회**(표식이동·revoke `TenantDeviceService.cs:269-309` · 자동승인 `:711`) |
| C-4 | **`IsLocalConsole` 을 신분·슬롯·승인면제 근거로 쓰는 안 전부 폐기**(S1·F-1·안2·(b) 발급) |
| C-5 | **is_parent 는 2차** — 사장님 조회 후 + 부모계정 0명 회사 대체절. 🔴 대표인데 `is_parent=0` 계정이 생기는 경로가 코드에 실재(`UserService.cs:111-145`·`:235-249`) |
| C-6 | **DB 변경은 1차 0** · DB-120 조건부 재정합은 2차 · 번호는 작업지시서 한 곳 |
| C-7 | **K5 게이트** — build 잡에서 DB 없이 도는 파이프라인 시험(양성 1 + 거부 6) · GC40/41 반전 · GC42 단언 추가 · GC44 폐기 · `GATES` 등록 · 「문 B 만 증명」 정직 기재 |
| C-8 | **오늘 실측** — 도메인 + 경로 직접 입력 + 보태기만 · 덮어쓰기·초기화는 게시 후 · 임시 우회 안내 금지 · 숫자 판정은 조회 ③ 이 빈 DB 일 때만 |

**남은 갈림 — 최종 판정 안건**

| # | 안건 | 입장 |
|---|---|---|
| F-1 | **127.0.0.1 창의 기기 신분** | 설계·개발·검증·CTO = **자동 합류**(지문 조회보다 먼저 · 쓰기는 `last_seen_at` 만) / 보안상무 = K3 버튼(서버 K1 강제). 🔴 **검증 실측**: 같은 브라우저면 origin 이 달라도 지문이 같다(`HFPv2-b2e88f45` ×4) → 127 창이 도메인 탭 기존 줄에 붙어 **K3 버튼이 안 뜬다 = 막다른 길** |
| F-2 | **도메인 대기 관문의 기존 1.2.89 [회사서버] 합류 버튼** | 보안상무 = 서버 K1 강제(바깥 폰 합류 차단) / 설계 = 유지(자기 선언 피해는 요금 1대 · 자료관리 위험 0) → 결재 / CTO·개발 = K1 강제하면 **새 도메인 탭은 +1 슬롯 → 사장님 지적 「2중」이 요금으로 부활** → 결재 대가 명시 · CTO 메모: 번호 입력은 도메인 탭 **신분 합류(슬롯 0)** 에 쓸 때 위험이 작다 |
| F-3 | **⑤ 헤더 부재 처리** | CTO = `Sec-Fetch-Site` 없으면 거부 / 보안상무·개발 = 둘 다 없으면 통과(비브라우저는 ①~④가 판정 · 구형 브라우저 교차 POST 는 Origin 으로 거부 · 교차 GET 은 Bearer 없어 401) |
| F-4 | **CORS 축소 시점** | CTO = 1차 제외 / 보안상무 = ⑤ 필수 + 병행 권고 |
| F-5 | 🆕 **백업 복원이 `tenant_devices` 를 통째 덮는다** — 옛 백업 복원 시 메인 줄 2개 부활(`BackupService.cs:335·343`) | 검증 발견 — 1차 영향·감사 대상·2차 설계 반영 분류 필요 |
| F-6 | 🆕 **초기화 비번 재확인 문구 「부모계정」 ≠ 판정 `tenant_admin`**(`DataResetService.cs:80-83`) | 개발 발견 — 2차 is_parent PR 과 묶을지 분류 |
| F-7 | 🆕 **DB-120 초안이 DB-89 의미를 바꾼다**(복구 UPDATE 제거 · 트리거 전이만 차단) | 2차 결재 안건 분류 |
| F-8 | **관문 채움 버튼 반전**(「네, 설치한 이 컴퓨터」를 채운 버튼으로 = 요금 방향 변경) | 설계 제안 — 결재 안건 분류 |

## §6. 주심 최종 판정

### 6-1. 보안 주심 안철수 — 최종 판정

**F-1 — 입장 변경: 자동 합류 채택.** 철회했던 이유 둘이 이 형태에선 불성립 🟢
- 「신분이 조용히 바뀐다」 — 127 창은 `DeviceId=null` 로 들어와 바뀔 기존 신분이 없다(`device-fingerprint.js:29-77`) · 메인 줄 갱신은 `:560-579` 보존 규칙으로 `last_seen_at` 만 · 표식 이동·revoke(`TenantDeviceService.cs:269-309`) 없음.
- 「로그인 POST 는 교차사이트를 받는 자리」 — ⑤가 합류 갈래에 걸려 악성 origin 이면 합류 0 · 악성 사이트가 로그인 응답을 읽어도 공격자 자기 자격으로 받은 것이라 새 권한 0.
- K3 단독은 지문으로 기존 줄에 붙어 막다른 길(검증 실측) → 반려.
- 🔴 **조건 3개 추가**: ① **메인 줄이 정확히 1개일 때만 합류** — 0개·2개 이상이면 종전 흐름 + 감사 1줄(2줄은 출하 DDL `hitpan_db_clean.sql:3502` 과 F-5 복원으로 실제로 생긴다) ② **K1 판정·합류 SQL 이 예외면 합류 안 함**(`DeviceAuthMiddleware.cs:252-258` 「예외면 통과」 복사 금지) ③ AuthController 울타리 = 「`:517-520`·`:576-577` 원문 동일 + 로그인 쪽 K1 호출 1줄」(개발 안의 「0줄」과 「1줄」 충돌 정리).

**F-2 — 1차 유지(무변경) · 2차 번호 증명 합류 · 내 R-2 조건 2(1.2.89 버튼 K1 강제) 철회.**
- 유지 🟡 — 피해는 요금 1대(대표 자격이면 자기승인으로 어차피 업무 API 가 열리고 파괴급은 K1 이 막는다 · `reissue-key` 위치 조건 0 `DeviceController.cs:412-427`).
- K1 강제 — 보안 🟢 · 사업 🔴(2중이 요금으로 부활).
- 번호 증명 합류 🟢 — 바깥 폰이 합류하려면 메인PC 앞에 사람이 있어야 한다. 번호 기준(6자리·5회·5분·1회용·활성 1·시간당 10·고정시간 비교·감사) 그대로 + **발급은 로그인된 127 창 API(K1+⑤)에서만**(익명 API 불필요 → H-8 해소) + **소비는 발급한 user_id 와 같은 사용자의 도메인 탭만** + 127 창 사기 경고문.

**F-3 — CTO 안 채택(입장 변경): `Sec-Fetch-Site` 없으면 거부.** 설치 PC 는 최신 브라우저 전제(Blazor WASM) → 헤더 없는 요청은 비브라우저·구형뿐 · 정상 사용자 비용 ≈0 이면 막는 쪽이 기본. 조건: 거부 응답에 사유 코드 · **백신 웹실드가 헤더를 지우는지 게시 전 Sandbox 1종 이상 실측(#31)** ⬜.

**F-4 — CTO 안 채택: CORS 축소 1차 제외.** ⑤가 K1 경로 전부에 있고 인증 쿠키 0(refresh 는 본문 `AuthController.cs:339-341` · 저장 ProtectedLocalStorage `Web/Services/AuthService.cs:201·209`) → 잠재 P1. **단 2차 번호 합류·(나) 게시 전 필수 선결.**

**F-5 — 🔴 실재 보안 문제(P1).** 덤프가 DB 전체(`BackupService.cs:335` · `--ignore-table` 없음) → 옛 백업 복원 시 ①**폐기한 기기 부활**(폐기는 해시를 지우지만 `TenantDeviceService.cs:810`, 복원하면 옛 해시·approved 복귀 → 축① 통과 `:1076`) ②로그아웃으로 무효화한 refresh 부활(`AuthController.cs:327`) ③유출 뒤 바꾼 비번 원복 ④메인 줄 2개 ⑤마이그 이력·트리거 옛 상태. 함께: 복원 원천 임의 경로(`:259-261`) · DB 비번 명령줄 인자(`:335·343`). 분류: **1차는 F-1 ①로만 막고** · 2차 복원 후 보안 재정합 설계·결재 · 명령줄 비번은 별트랙.

**F-6 — 2차 is_parent PR 과 묶음.** 단 🟢 현재 문구는 거짓(「부모계정이 아닙니다」인데 판정은 `tenant_admin` `DataResetService.cs:77-92`) → **1차 게시노트·사장님 보고에 기재.**

**F-7 — 2차 결재.** 방향 찬성(폐기된 메인 줄 자동 부활 권한 제거) · 조건: 복원이 옛 DDL·이력으로 되돌리므로 **복원 뒤 멱등 재적용 게이트**.

**F-8 — 결재 · 보안 조건부.** 번호 증명 없이 채운 버튼을 「기기 수 안 늚」으로 돌리면 자기 선언이 기본 경로가 된다 ⇒ **F-2 번호 증명 합류와 같은 게시일 때만 수용.**

**1차 게시 보안 승인 조건 (#23 — 하나라도 빠지면 게시 불가)**
1. **K1 한 함수** — 터널헤더0 ∧ loopback ∧ Host 리터럴 ∧ Port=LocalPort ∧ Origin(있으면 자기 출처) ∧ `Sec-Fetch-Site`∈{same-origin,none}(없으면 거부) · 예외 시 거부 · 복사본(`DeviceController.cs:101-114`)은 위임.
2. **K2 와 자동 합류는 한 PR** · 자동 합류는 F-1 제약(쓰기 `last_seen_at` 만 · 메인 줄 1개일 때만 · 지문 조회보다 먼저 · 예외면 합류 안 함).
3. **#43 울타리 시험** — `AuthController.cs:517-520`·`:576-577` 원문 동일(build 잡).
4. **K5 게이트** — 양성 1 + 거부 7종(CF헤더·Host localhost·Origin evil·cross-site·헤더 부재·Port≠LocalPort·원격 IP) · 자동 합류 DB 게이트(K1 거짓 3종이면 합류 0 · K1 참이면 전 줄 is_main_pc/status/auth_key_hash 불변 · 메인 2줄이면 합류 0) · 전부 `GATES` 등록 · 무력화 「왜 FAIL」 기록.
5. **K4 안내** — 127.0.0.1 주소 + 할 일 · localhost:5234 안내 금지 · 외부 노출 문구 금지어 0.
6. **게시문에 사장님 실측 주소 명시** — `http://127.0.0.1:5257`.

**2차 목록**: 번호 증명 합류(F-2) · CORS 허용출처 목록화(번호 합류 선결) · is_parent + 부모계정 0명 회사 대체절 + `UserService.cs:111-145·235-249` 정합 + F-6 문구 · DB-120(F-7) · 복원 보안 재정합 + 복원 경로 백업 폴더 제한(F-5) · 축② 폐지(G-32-d) · A-2·흡수·교체 절차 · (나) 찾아보기 증표(보류 권고) · F-8(번호 합류와 같은 게시)

**사장님 결재 안건 (보안 문장)**
- Ⓐ 자료관리 위치 — *"회사 자료를 지우거나 되돌리는 기능은 자료보관 컴퓨터에서 127.0.0.1 주소로 연 창에서만 됩니다."* 대가: 주소를 바꿔 로그인 1회 / 이득: 바깥·사내 다른 PC 에서 지울 길 0
- Ⓑ 도메인 [회사서버] 합류 버튼 — ①유지(대표 비번만 있으면 바깥 폰도 회사서버로 등록돼 기기 1대 요금이 샐 수 있다 · 자료는 안전) ②위치 강제(메인PC 도메인 탭이 기기 1대로 추가 = 2중이 요금으로 남는다) ③번호 확인 후 합류 · 2차(처음 한 번 6자리 입력 · 개발 기간)
- Ⓒ 관문 버튼 방향 반전 — Ⓑ③과 함께일 때만 권고
- Ⓓ 백업 복원 정책 — *"옛 백업을 복원하면 그 뒤에 폐기한 기기·바꾼 비밀번호·로그아웃도 함께 되돌아갑니다. 이 셋을 복원 대상에서 뺄지 결정해 주십시오."* 대가: 빼면 완전 복원이 아니다
- Ⓔ is_parent — 사장님 조회(부모계정 수) 후 결재

**1차 후에도 안 닫히는 위험 (정직)**
- 축② — 메인 줄 번호는 비밀이 아니다 · 대표 JWT 를 가진 사람은 어디서든 그 번호로 업무 API 통과(`DeviceAuthMiddleware.cs:186-194`) · 1차는 넓히지도 막지도 않는다.
- 메인PC 자체가 악성코드에 장악되면 로컬 프로세스가 K1 5조건을 모두 만족 — 막을 수 없는 영역.
- 메인PC 앞 사람이 전화 사기에 속아 127 창에서 초기화 — 남는 방어는 비번 재확인뿐.
- F-5 복원 부활 · 1.2.89 자기 선언 요금 누수(유지 시) · CORS reflect 잠재 · 슬롯2 이상 도메인이 슬롯1 API 로(`InstallerBootstrapController.cs:133` · 테넌트 격리 🟡 별트랙) · 백신 헤더 변형·팝업·로컬 네트워크 권한창 ⬜.

**사장님 직보 문장 (별트랙)**
> "설치 폴더의 보안키 파일(hitpan-keys.conf)이 일반 권한 창에서 열렸습니다(실제 권한 목록은 아직 확인하지 못했습니다). 설치기는 권한 설정 결과를 확인하지 않습니다(`iss:1924-1926`). 이 파일에는 로그인 증명을 만드는 키와 자료 암호화 키가 들어 있어, 그 컴퓨터에 들어온 악성 프로그램이 로그인 증명을 위조할 수 있습니다. 메인PC 작업과 별도로 ①파일 권한 목록 확인(내용은 열지 않음) ②권한 교정 방식(#29 사전 승인) ③키 교체 필요 여부를 결재받겠습니다. 터널 토큰과, 백업 중 DB 비밀번호가 명령줄에 보이는 문제도 함께 올립니다."

**CTO 와의 불일치: 없음** (F-1·F-3·F-4 는 CTO 쪽으로 이동). 단 **CTO 확인 필요 신규 조건 4개**: F-1 ①(메인 줄 2개면 합류 금지) · F-1 ②(예외 시 합류 안 함) · F-1 ③(AuthController 울타리 정의) · F-3 백신 헤더 실측 — CTO 가 F-1 ①을 받지 않으면 불일치로 기록.

### 6-2. CTO 래리 — 최종 판정

> ⚠️ 보안상무 최종 판정(§6-1)을 보기 전에 작성됐다. 그래서 아래 §8 불일치 목록 중 F-1·F-2·F-4 는 **이미 해소**됐다(보안상무가 CTO 쪽으로 이동). 남은 교차는 §6-3 에서 정리.

**F 판정**
- **F-1 — 자동 합류(J) 채택 · K3 버튼 불채택.** 검증 실측(`HFPv2-b2e88f45` ×4) → K3 단독은 막다른 길 🟢. 🔴 **CTO 조건 추가: 대표계정만 합류** — 직원은 메인 줄 번호를 볼 수 없는데(`DeviceController.cs:36-42`) 합류시키면 그 번호를 집 PC 에 붙여 번호만으로 통행(`DeviceAuthMiddleware.cs:186-194`) → 구멍 확대.
- **F-2 — 1차 현행 유지 · 결재 2.** 재발급만 K1 으로 묶어도 대표는 목록의 메인 줄 번호를 붙여 계속 통행 ⇒ 바깥 경로는 안 닫히고 새 도메인 탭만 +1. 도메인 탭은 터널로만 들어와 스스로 위치를 증명 못 한다 · 127 창 값이 넘어가는 통로는 사람(번호)과 창 간 메시지뿐인데 API 가 정식 도메인을 몰라 후자는 불가 ⇒ **도메인 탭까지 「메인PC 한 대 = 한 줄」을 증명으로 지키는 현실적 유일안 = 번호 입력 신분 합류 + 브라우저별 비밀 + 번호만 통행 폐지, 한 묶음(2차)** 🟡.
- **F-3 — CTO 입장 철회: 「둘 다 없으면 통과」 수용.** 브라우저가 토큰을 실으려면 교차출처가 되어 Origin 이 붙는다 · 인증은 쿠키가 아니라 본문·로컬저장(`AuthService.cs:484`) ⇒ 헤더 부재로 통과하는 교차사이트 요청엔 토큰이 없다. 불변식 게이트 2개: **인증 쿠키 0 · 파괴급은 POST 만.**
- **F-4 — CORS 축소는 2차 첫 항목.** ⑤가 1차에 들어가 위치 통제와 CORS 는 무관 · 쿠키 0 이라 위생 문제 · 1차 축소는 모르는 소비처 단절 위험만.
- **F-5 — 🟢 확인.** 백업은 DB 통째(`Application/Services/BackupService.cs:335`) · 복원은 `mysql < 파일`(`:301`·`:342`) · 초기화는 기기표 보존(`DataResetService.cs:54`) ⇒ 옛 백업 복원 시 폐기 기기 줄·열쇠·옛 메인 줄 부활. 1차 차단 사유 아님 · 남는 위험 · 2차에서 복원 시 기기표 제외 또는 재정리.
- **F-6** 2차 is_parent PR 과 묶음 · **F-7** 2차 결재 5 — 개발 초안 찬성(`DB-89:96-99` 복구 UPDATE 를 2줄 DB 에 돌리면 폐기 줄 부활·슬롯 +1) · **F-8** 결재 3 — 1차 반전 금지(자기 선언이 기본 선택), 2차 번호 합류 뒤 가능.

**1차 게시 범위 (DDL 0 · 한 게시)**
- **K1** 자리 판정 한 함수 · 비동기 필터 형태 · 터널헤더 0 ∧ loopback ∧ Host 리터럴 127.0.0.1/[::1] ∧ Host.Port = LocalPort(비면 거짓) ∧ ⑤ · 부르는 곳 `MainPcOnlyAttribute`(위임)·`DeviceController.cs:101-114`(복사본 제거)·`MigrationController.cs:684`.
- **K2** 8/18작4 철회 — 표식이동·revoke(`TenantDeviceService.cs:269-309`) · 로컬 자동승인(`:711`).
- **J (K3 대체)** 127 직결 자동 합류 — **대표 ∧ K1** 이면 번호·지문 조회보다 먼저 메인 줄 번호 반환 · 쓰기 `last_seen_at` 만 · 재발급·표식·상태 쓰기 0 · 메인 줄 없으면 K2 대기.
- **K4** 안내 — 막힌 도메인 탭 [이 컴퓨터에서 열기](클릭 즉시 새 창) + 주소 글자 + 할 일 · 127 창 첫 로그인 안내 · localhost:5234 안내·슬롯2 보증 문구 금지.
- **K5** 게이트 — build 잡 파이프라인(양성 1 + 거부 N1~N6 + 헤더 부재) · J DB 게이트(대표/직원/터널/localhost Host/메인 줄 없음) `GATES` 등록 · GC40·41 재작성 · GC42 단언 추가 · GC44 폐기 · G_OW5 픽스처 Host·LocalPort · **#43 울타리 = 업데이트 두 액션(`AuthController.cs:504-539`·`:561~`) 식 불변 + K1 미참조**(J 가 로그인 초기화에 1줄 필요).
- **1차에서 빼는 것**: CORS 축소 · is_parent · DB-120 · (나′) · 번호 합류·탑승비밀·번호만 통행 폐지 · 1.2.89 버튼 K1 강제·반전 · 옛 줄 흡수 · 교체 절차 · 감사알림 · 복원 기기표 처리.

**2차 순서**: ①CORS 축소(교차출처 소비처 전수 먼저) ②사장님 조회 + DB-89 checksum → DB-120 조건부 재정합 + 복원 기기표 처리 + 감사알림(재표식·메인 줄 재발급·K1 거부 반복·복원 후 메인 줄 수) ③is_parent(부모 0명 대체절 + `UserService.cs:111-145`·`:235-249` 정리 + F-6 문구) ④**도메인 탭 증명 합류 묶음**(번호 입력·탑승비밀 DB-121·번호만 통행 폐지·1.2.89 대체·F-8 반전·옛 줄 흡수 — **요금 결재 먼저**) ⑤메인PC 교체 절차 ⑥(나′) — 고르실 때만 · 별트랙: `hitpan-keys.conf` ACL(최우선) · 슬롯2 ingress 5257 고정 · mobile-register 401 · 5357 · 터널 토큰 명령줄.

**사장님 결재 안건 (CTO 문안)**
- **결재 1 — 자료관리 주소** (주심 둘·세 진영 권고 = 가) · **(가)** *"자료관리 4기능은 자료보관 컴퓨터에서 http://127.0.0.1:5257 로 연 창에서만 열립니다. 도메인에서는 [이 컴퓨터에서 열기] 버튼으로 안내합니다."* — 대가: 자료관리 때마다 창 하나 + 로그인 1회 · **(나′)** *"(가)에 더해, 127 창에 뜬 번호를 도메인 탭에 넣으면 10분간 찾아보기만 열립니다."* — 대가: 증표가 새면 드라이브·폴더 목록 노출(`MigrationController.cs:536`) · 2차 이후 · 편의 작음.
- **결재 2 — 도메인 탭 신분 (사장님 「2중」 지적 자리)**
  - **(2-A)** *"도메인 관문의 [회사서버 컴퓨터] 버튼을 지금처럼 둡니다."* — 대가: 대표가 휴대폰·집 PC 에서 눌러도 기기로 안 셈(대표 판단에 기댐) · 자료관리 위험 0.
  - **(2-B)** *"그 버튼을 자료보관 컴퓨터 전용 창에서만 되게 합니다."* — 대가: 도메인 탭은 합류 불가 → 기기 1대 추가(「2중」이 요금으로 남음) · 번호 붙여넣기 통행이 남아 보안 이득 작음.
  - **(2-C, 2차 목표 — 위 둘 중 하나와 함께)** *"전용 창의 6자리 번호를 도메인 탭에 넣으면 그 탭은 회사서버 자리로 합쳐지고, 번호 없는 합류와 번호 붙여넣기 통행은 없앱니다."* — 대가: 브라우저마다(캐시 삭제 시 다시) 번호 1회 · DB 표 1개 · 규모 M.
  - **(2-D, 1차 직후 가능)** *"자료보관 컴퓨터에서는 평소 업무도 127 창으로 하고, 도메인 줄은 기기관리에서 해제합니다."* — 대가: 바로가기는 계속 도메인을 엶(업데이트로 못 바꿈 `hitpan-start.bat:62`) · 도메인을 다시 열면 그 줄이 대기 목록에 재등장(`TenantDeviceService.cs:366-386` · 슬롯은 안 셈).
- **결재 3 — 관문 버튼** — 지금 순서 유지 / 「네, 설치한 이 컴퓨터」를 채운 버튼으로(대가: 자기 선언이 기본 선택)
- **결재 4 (2차) — 파괴급 권한** — 부모계정만(부모계정 없는 회사는 대표 전원) / 대표 전원
- **결재 5 (2차) — DB-120** — 폐기된 옛 메인 줄은 자동으로 되살리지 않음 / DB-89 방식(대가: 기기 1대 추가·충돌)
- **결재 6 — 오늘 마이그 실측** — 도메인에서 경로 직접 입력 + 없는 것만 보태기로 2·3·5 를 화면만(숫자는 조회 ③ 이 빈 DB 일 때만) / 게시 후 한 번에

**오늘 할 일** — 사장님: ①조회 4문항 + DB-89 checksum(관리자 PowerShell · SELECT 만) ②결재 1·2·6(3 은 선택) ③결재 6 앞쪽이면 도메인 보태기 실측 · localhost 재로그인·임시 우회 금지 / 팀(코드 0): PM 최종 결론 + 1차 작업지시서(K1·K2·J·K4·K5) → PM → CTO → 사장님 결재 · 검증 AC 게이트·Playwright 시나리오 + 병렬이슈 기록(F-5·is_parent=0 경로·슬롯2 ingress) · 보안 keys ACL 별트랙.

**1차 게시 후에도 안 닫히는 위험**
1. 번호만 통행 — 메인 줄 번호(대표 목록에 보임)만 있으면 어느 기기에서든 업무 API · 파괴급만 K1 로 닫힘.
2. 결재 2-A 면 자기 선언 합류 잔존.
3. 🔴 **사장님 「2중」은 1차로 풀리지 않는다** — 도메인 탭이 자기 줄이면 두 줄 그대로(1차가 해소하는 것은 127 창뿐).
4. 옛 백업 복원이 폐기 기기·열쇠를 되살린다(F-5).
5. 기동 시 서버줄 재표식(`MainPcRegistrationService.cs:149-171`) — 이미 표식이 옮겨진 고객은 기동마다 충돌 또는 메인 줄 2개 · 출하 DDL 에 DB-89 방어선 없음.
6. 그 PC 의 악성 프로그램·전화 사회공학은 K1 통과(대표 자격 필요).
7. 슬롯2 이상은 도메인이 슬롯1 API 로(`InstallerBootstrapController.cs:133`).
8. CORS 전체허용 + CSP unsafe-inline/eval.
9. is_parent 없어 대표 전원이 파괴급 가능.
10. 팝업·로컬망 권한창 ⬜ · keys ACL ⬜.

**출시 게이트 (AC)**: 01 도메인 첫 세션 200·막힘 시 버튼으로 127 창 · 02 대표 127 첫 로그인 업무·자료관리 200·새 줄 0·도메인 탭 안 갇힘 · 02′ localhost:5234 403+안내 · 04 캐시 삭제 후 127 재로그인 새 줄 0 · 05 사내 PC 도메인 대기·자료관리 거부 · 05b Host 위조 거부·표식 이동 0 · 06 바깥 폰 자료관리 거부·127 합류 불가 · 07 복사 자칭 자료관리 거부(업무 API 통과는 위험 1 로 명기) · 09 재부팅·재기동 후 막힘 0·표식 줄 수 불변 · 12 LOCAL 사내 PC 인정 0 · 13 test1234 DB 수술 0·127 합류 1회·도메인 탭 안 갇힘 — **보이는 줄·슬롯은 결재 2 결과로 기록, 1차에 1/1 약속 금지** · 14 공통 첫 세션 200 · 15 직원 기기 무회귀 · 16 문구·주소 · 17′ 타 사이트 스크립트의 127 로그인 합류·파괴급 거부 · 19 127 창 열기 Playwright · 🆕20 직원 계정 127 로그인은 메인 줄 번호를 받지 않음 · 🆕21 로그인 전 업데이트 안내·동의 무회귀(#43) · 2차로: 03·08·10·11·18·복원.

### 6-3. 주심 교차 정리 (PM)

| 자리 | 보안상무 최종 | CTO 최종 | 상태 |
|---|---|---|---|
| F-1 자동 합류 | 채택 + 조건 ①메인 줄 정확히 1개 ②예외면 합류 안 함 ③울타리 정의 | 채택 + **대표계정만** | 🟡 서로의 추가 조건 수용 확인 중 |
| F-2 1.2.89 버튼 | 1차 유지 · 2차 번호 증명 합류 | 1차 유지 · 결재 2 · 2차 번호 합류 묶음 | 🟢 **일치** |
| F-3 헤더 부재 | 「Sec-Fetch-Site 없으면 거부」로 **이동** | 「둘 다 없으면 통과」로 **철회** | 🔴 **서로 자리를 바꿨다** — PM 병합안 발송: 기본 거부 · 게시 전 백신 실측에서 헤더가 지워지면 통과 규칙 + 불변식 게이트 2개로 전환 |
| F-4 CORS | 1차 제외 · 2차 번호 합류 선결 | 2차 첫 항목 | 🟢 **일치** |
| #43 울타리 | `:517-520`·`:576-577` 원문 동일 + 로그인 K1 1줄 | 업데이트 두 액션 `:504-539`·`:561~` 식 불변 + K1 미참조 | 🟡 줄 범위 확정 중 |
| 결재 2-D | — | 추가 | 🟡 보안 반대 사유 확인 중 |

**보안상무 대조 답**
- **F-3 PM 병합안 — 수용.** 기본은 막는 쪽 · 전환은 Sandbox 백신 실측이라는 증거에 건다 · CTO 근거도 코드상 맞다(인증 쿠키 0 · 로그인 `[FromBody]` JSON 이라 폼 POST 는 415) ⇒ 두 규칙 모두 안전, 차이는 기본값뿐. 조건: ①불변식 게이트 2개(인증 쿠키 0 · 파괴급 POST 만)는 **어느 규칙이든 `GATES` 등록**(F-4 도 쿠키 0 에 기댐) ②규칙 전환 시 실측 근거 문서화(#42).
- **CTO 조건 「J 는 대표계정만」 — 수용.** 대표 아닌 계정은 목록에서 자기 기기만 본다(`DeviceController.cs:38-42` 🟢) → 직원을 합류시키면 메인 줄 번호가 127 창 저장소에 남고 집 PC 로 옮겨 번호만으로 통행 → G-32-d 가 직원 전원으로 확대. 제외된 직원은 일반 승인 흐름(막다른 길 아님).
- **(2-D) — 조건부 반대.** 파괴급이 열리는 origin 을 매일 쓰는 origin 으로 만든다 · CSP `'unsafe-inline' 'unsafe-eval'`(`Program.cs:535`)라 화면 스크립트 취약점 하나가 같은 출처 요청으로 ⑤ 통과 → 백업 복원은 **회사명만 확인**(`BackupService.cs:279` 🟢 · 비밀 아님) · 덮어쓰기 확인문구(`MigrationController.cs:695`)도 비밀 아님 🟡 ⇒ **채택 조건: 파괴급 3종(초기화·복원·덮어쓰기) 모두 비밀번호 재확인**(재확인 API 기존 `AuthController.cs:359-372` · 현재 재확인은 초기화만 `DataResetService.cs:87`).
- ⇒ CTO 에게 추가 질문: 파괴급 3종 비밀번호 재확인을 (2-D) 조건으로만 둘지, **(가) 기준 1차에도 넣을지**(127 창도 같은 CSP).

**CTO 대조 답**
1. **F-3 병합안 — 수용.** 기본 「`Sec-Fetch-Site` 없으면 거부」 · 거부 응답 `error` 에 사유 코드 · 게시 전 Sandbox 에서 백신 웹실드 1종 이상 켜고 실제 엣지·크롬으로 127 창 AC-02 실측 → 헤더가 지워지면 「둘 다 없으면 통과」로 전환 · 불변식 게이트 2개(인증 쿠키 0 · 파괴급 POST 만)는 **어느 규칙이든 1차** · 🔴 **그 실측 전에 기본 거부를 게시하는 것은 금지**(사장님 127 창이 막히면 AC-02 FAIL).
2. **메인 줄 정확히 1개일 때만 합류 — 수용 + 보탬.** ⓐ 개수는 status 무관 `is_main_pc=1` 줄 수 · 정확히 1줄이고 그 줄이 approved 일 때만 · 한 SQL 로 판정 ⓑ 0줄·2줄 이상이면 종전 흐름 → 지문으로 도메인 줄에 붙어 대표 버튼 없는 `_blocked` 로 끝남(검증 실측) ⇒ **127 창 전용 안내 화면 필요**(감사 1줄). 🔴 2줄을 화면에서 정리할 방법이 없다(메인 줄 폐기 거부 `TenantDeviceService.cs:779-783`) ⇒ **사장님 조회 ①이 2줄로 나오면 1차로도 사장님이 못 들어간다 — 게시 전에 판정해야.**
3. **예외면 합류 안 함 — 수용.** `DeviceAuthMiddleware.cs:252-258` 의 「예외면 통과」는 기존 문의 가용성 장치 · 신분을 주는 자리에 복사하면 DB 오류 하나가 메인 신분 발급이 된다 · 예외 시 종전 흐름 + `LogWarning`(#15).
4. **#43 울타리 — 줄 번호가 아니라 메서드 단위로 확정.** J 의 로그인 1줄이 `Login`(`AuthController.cs:46~`) 안에 들어가 아래 줄번호가 전부 밀린다 🟢 ⇒ ①`GetUpdateStatusLocal`(`:504~`)·`UpdateConsentLocal`(`:561~`) 두 메서드 본문이 1.3.39 main 과 **글자 동일**(본문 해시 게이트 · 보안상무 `:517-520`·`:576-577` 포함 상위 범위) ②두 메서드에서 K1 미참조 ③K1 호출은 `Login` 안 1곳뿐 ④로그인 전 업데이트 통과목록 3자리 글자 존재 — `TenantMiddleware.cs:35`·`:46` · `DeviceAuthMiddleware.cs:59-61` · `RateLimitMiddleware.cs:52-65`.
- 보안상무 F-5·F-6·F-7·F-8·F-2 요지 — 전부 수용(F-2·F-8 은 CTO 판정과 동일).

**⇒ 주심 불일치: 0** (F-1 대표 한정은 보안상무 수용 · F-3 병합안은 양쪽 수용). 남은 확인 1건 = 파괴급 3종 비밀번호 재확인의 1차 포함 여부(CTO).

**CTO 마지막 답 — 파괴급 비밀번호 재확인**
- **판정: (2-D) 조건으로만 두지 말고 1차에 넣는다. 새로 넣을 곳은 「백업 복원」 한 곳뿐.**
- 🔴 보안상무 전제 정정: **덮어쓰기는 이미 비밀번호를 재확인한다** — 빈 값이면 400(`MigrationController.cs:699-702`) · `ResetAllAsync` 로 넘겨 BCrypt 대조(`:705-711` · `DataResetService.cs:78-93`) 🟢 ⇒ 초기화·덮어쓰기는 기존.
- 빠진 곳은 **복원** — 회사명만 확인(`Application/Services/BackupService.cs:271-280`) · 복원 파일로 임의 경로도 받음(`:259-262`) · F-5 까지 겹쳐 셋 중 가장 위험.
- 1차에 넣는 이유: 127 창도 같은 앱·같은 CSP(`Program.cs:535`) — 노출 빈도만 다르고 구멍 모양이 같다.
- 규모 **S**: `RestoreRequest` 에 `Password`(`BackupDtos.cs:50-57`) · `RestoreAsync` 에서 초기화와 같은 대조문 재사용 · 복원 화면 입력칸 1 · 게이트 1(비번 틀리면 복원 호출 0).
- 사장님 실측 영향 0(덮어쓰기는 지금도 비번 요구 · 복원은 실측 8항목에 없음) · G-OW 계열 영향 0.
- ⇒ 보안상무 (2-D) 채택 조건이 1차에서 충족 → **주심 불일치 최종 0.**

---

## §7. 최종 결론

> **주심 판정**: CTO 래리 · 보안상무 안철수 — **불일치 0** · 세 진영(설계·개발·검증) 수렴
> **성격**: 방향·범위 결론이다. 🔴 **코드는 사장님 결재 → 작업지시서 → PM·CTO 결재 후** (헌법 #33·#42).

### 7-0. 한 줄 결론

> **메인PC 는 「어느 주소로 열었나」가 아니라 「그 컴퓨터 안에서 `http://127.0.0.1:{포트}` 로 직접 연 창」으로만 증명한다.**
> 1차는 **그 창에서 자료관리가 첫 세션부터 열리고, 그 창이 새 줄 없이 메인PC 한 줄에 합류**하는 것까지다.
> 🔴 **도메인 탭까지 한 줄로 합치는 것(사장님 「2중」의 완전 해소)은 2차 「번호 증명 합류」다.** 1차로 약속하지 않는다.

### 7-1. 왜 이 결론인가 — 살아남은 근거

| # | 근거 | 누가 · 어떻게 |
|---|---|---|
| 1 | 접속 경로(loopback·터널 헤더)로는 못 푼다 — 사장님 경로에선 거짓, 빼면 누구나 참 | 8/19 §1-4 · 1라운드 재확인 |
| 2 | 🔴 **현행 1.3.39 에 구멍** — 5234 프록시가 원격 요청을 loopback 으로 넘겨, 사내 PC 가 Host 를 위조하면 자료관리 판정이 참 | 보안상무 P0 · 코드 판정(`web-server.ps1:77-79·91-103` · `MainPcOnlyAttribute.cs:41-48`) |
| 3 | `127.0.0.1` **리터럴 Host 는 프록시·사내 PC·터널이 만들 수 없다** | `web-server.ps1:33·98` · API 5257 loopback 바인드 |
| 4 | API 가 **모든 출처 + 자격증명 허용** → 같은 PC 악성 사이트가 127.0.0.1 로 요청 가능 ⇒ 교차사이트 거부(⑤) 필수 | 세 진영 독립 적발 · `Program.cs:396-405` · PM 지적으로 4조건→5조건 |
| 5 | 도메인 탭에 증표를 주면 화면 스크립트 취약점 하나로 탈취 → 원격 초기화 ⇒ **파괴급은 터널 너머 금지** | CTO · CSP `unsafe-inline/unsafe-eval`(`Program.cs:535`) |
| 6 | 🔴 **같은 브라우저면 주소가 달라도 지문이 같다**(`HFPv2-b2e88f45` ×4) → 127 창이 도메인 줄에 붙어 버튼이 안 뜬다 ⇒ **자동 합류가 지문 조회보다 먼저** | 검증팀 격리 실측 · `device-fingerprint.js:29-77` |

### 7-2. 1차 게시 — DDL 0 · 한 게시 · 결재 선택과 무관

| # | 무엇 | 핵심 조건 |
|---|---|---|
| **K1** | 메인PC 자리 판정 **한 함수** | 터널헤더 0 ∧ loopback ∧ Host `127.0.0.1`/`[::1]` ∧ Host.Port = `Connection.LocalPort` ∧ Origin(있으면 자기 출처) ∧ `Sec-Fetch-Site` ∈ {same-origin, none} — **없으면 거부**(게시 전 백신 실측으로 확정) · 예외면 거부 · 비동기 필터 형태 · 복사본(`DeviceController.cs:101-114`) 위임 · 부르는 곳 `MainPcOnlyAttribute`·`MigrationController.cs:684` |
| **K2** | **8/18작4 철회** | 로그인 시 표식 이동·서버줄 revoke(`TenantDeviceService.cs:269-309`) · 로컬 자동승인(`:711`) 제거 |
| **J** | **127 직결 자동 합류** | **대표계정만** ∧ K1 ∧ **메인 줄 정확히 1개(approved · 한 SQL)** · 번호·지문 조회보다 먼저 · 쓰기는 `last_seen_at` 만(표식·상태·열쇠·재발급·revoke 0) · 예외면 합류 안 함(+LogWarning) · 조건 불충족 시 **127 창 전용 안내** + 감사 1줄 |
| **K4** | 안내 | 도메인 막힘 화면 **[이 컴퓨터에서 열기]**(클릭 즉시 새 창) + 주소 글자 + 할 일 · 127 창 첫 로그인 안내 · localhost:5234 안내·슬롯2 보증 문구 금지 · 외부 노출 금지어 0 |
| **P** | **백업 복원 비밀번호 재확인** | 초기화·덮어쓰기는 기존 · 복원만 추가(규모 S) |
| **K5** | 게이트 | build 잡 파이프라인(양성 1 + 거부 7) · J DB 게이트(대표/직원/터널/localhost Host/메인 줄 0·2) · **불변식 2개**(인증 쿠키 0 · 파괴급 POST 만) · GC40/41 재작성 · GC42 단언 추가 · GC44 폐기 · G_OW5 픽스처 Host·LocalPort · **#43 울타리**(업데이트 두 메서드 본문 해시 동일 · K1 호출은 `Login` 1곳 · 통과목록 3자리) · 복원 비번 게이트 · **전부 `GATES` 등록** · 무력화 FAIL 사유 기록 |

**1차에서 빼는 것**: CORS 축소 · is_parent · DB-120 · (나′) · 번호 증명 합류 · 1.2.89 버튼 변경 · 관문 버튼 반전 · 옛 줄 흡수 · 메인PC 교체 절차 · 감사알림 · 복원 시 기기표 처리

**🔴 게시 전 필수 — 하나라도 없으면 게시 금지**
1. **사장님 조회 ①** — 메인PC 표식 줄 수. **2줄 이상이면 1차로도 사장님이 못 들어가신다**(화면에서 정리할 길 없음 `TenantDeviceService.cs:779-783`) → 게시 전 판정·대책.
2. **Sandbox 백신 1종 이상 + 실제 엣지·크롬**으로 127 창 AC-02 실측 → `Sec-Fetch-Site` 규칙 확정(헤더가 지워지면 「둘 다 없으면 통과」 + 불변식 게이트로 전환 · 근거 문서화).
3. 보안 승인 조건 6개(§6-1) + 출시 게이트 AC(7-7).

### 7-3. 2차 — 순서

1. **CORS 허용출처 목록화** — 교차출처 소비처 전수 먼저 · 번호 합류의 선결
2. **DB-120 조건부 재정합** + 복원 시 기기표 처리 + 감사알림(재표식·메인 줄 재발급·K1 거부 반복·복원 후 메인 줄 수) — 사장님 조회·DB-89 checksum 후
3. **is_parent** — 부모계정 0명 회사 대체절 + 대표인데 `is_parent=0` 이 생기는 경로(`UserService.cs:111-145`·`:235-249`) 정리 + 초기화 문구 정합
4. **도메인 탭 증명 합류 묶음** — 127 창 6자리 번호 · 브라우저별 비밀 · 번호만 통행 폐지 · 1.2.89 버튼 대체 · 관문 버튼 반전 · 옛 줄 흡수 — 🔴 **요금 결재 먼저**
5. **메인PC 교체 절차**
6. (나′) — 사장님이 고르실 때만

**별트랙**: 🔴 `hitpan-keys.conf` 권한(최우선 · 사장님 직보 §6-1) · 한 PC 두 번째 회사부터 도메인이 첫 회사 API 로(`InstallerBootstrapController.cs:133`) · 휴대폰 QR 등록 코드상 401 · 5357 포트 · 터널 토큰·백업 DB 비밀번호 명령줄 노출

### 7-4. 🔴 사장님 결재 안건 — 한 줄씩 골라 주십시오

| 결재 | 안건 | 선택지 | 주심 권고 |
|---|---|---|---|
| **1** | 자료관리를 어느 주소에서 여나 | **(가)** 자료보관 컴퓨터에서 `http://127.0.0.1:5257` 로 연 창에서만 · 도메인에선 [이 컴퓨터에서 열기] 안내 — 대가: 자료관리 때마다 창 하나 + 로그인 1회 / **(나′)** (가)에 더해 번호를 넣으면 도메인 탭에서 10분간 **찾아보기만** — 대가: 새면 드라이브·폴더 목록 노출 · 2차 이후 · 편의 작음 | **(가)** |
| **2** | 도메인 탭 신분 — **「2중」이 걸린 자리** | **(2-A)** [회사서버 컴퓨터] 버튼 지금처럼 — 대가: 대표 비번만 있으면 바깥 폰도 회사서버로 등록돼 기기 1대 요금이 샐 수 있음(자료는 안전) / **(2-B)** 그 버튼을 전용 창에서만 — 대가: 도메인 탭이 기기 1대 추가(**2중이 요금으로 남음**) · 보안 이득 작음 / **(2-C)** 전용 창 6자리 번호를 도메인 탭에 넣으면 회사서버 자리로 합침 · 번호 없는 합류·번호 붙여넣기 통행 폐지 — 대가: 브라우저마다 번호 1회 · DB 표 1 · 규모 M · **2차** / **(2-D)** 메인PC 에선 평소 업무도 127 창으로 · 도메인 줄은 기기관리에서 해제 — 대가: 바로가기는 계속 도메인을 엶 · 도메인을 다시 열면 대기 목록에 재등장 | **1차 2-A 유지 → 2차 2-C** |
| **3** | 등록 화면 버튼 순서 | 지금 순서 유지 / 「네, 설치한 이 컴퓨터」를 채운 버튼으로 — 대가: 자기 선언이 기본 선택 | **유지**(반전은 2-C 와 함께) |
| **4** (2차) | 파괴급 권한 | 부모계정만(부모계정 없는 회사는 대표 전원) / 대표 전원 | 조회 ④ 후 |
| **5** (2차) | DB-120 | 폐기된 옛 메인 줄을 자동으로 되살리지 않음 / DB-89 방식(대가: 기기 1대 추가·충돌) | **되살리지 않음** |
| **6** | 오늘 마이그 실측 | 도메인에서 경로 직접 입력 + 없는 것만 보태기로 2·3·5 **화면만**(숫자는 조회 ③ 이 빈 DB 일 때만) / 게시 후 한 번에 | 사장님 판단 |
| **7** (2차 설계 입력) | 백업 복원 정책 | 옛 백업을 복원할 때 **폐기한 기기·바꾼 비밀번호·로그아웃**까지 되돌릴지 / 빼고 복원(대가: 완전 복원이 아님) | 사장님 판단 |

### 7-5. 오늘 할 일

- **사장님**
  1. 관리자 PowerShell 에서 **조회(SELECT 만)** — §5-3 4문항 + `migrations` 표 DB-89 행 checksum
  2. **결재 1·2·6** (3 은 선택)
  3. 결재 6 앞쪽이면 도메인에서 보태기 실측 — 🔴 **localhost 재로그인·임시 우회는 하지 마십시오**
- **팀 (코드 0)**
  - PM: 1차 작업지시서(K1·K2·J·K4·P·K5) 작성 → PM 결재 → CTO → 사장님
  - 검증: AC 게이트·Playwright 시나리오 · 병렬이슈 기록(현행 Host 위조 P0 · 복원 부활 F-5 · `is_parent=0` 경로 · 두 번째 회사 ingress)
  - 보안: `hitpan-keys.conf` 권한 별트랙 직보

### 7-6. 1차 게시 후에도 안 닫히는 것 (정직)

1. 🔴 **사장님 「2중」** — 도메인 탭이 자기 줄이면 두 줄 그대로. 1차는 127 창만 한 줄로 만든다 → 2차 2-C.
2. 메인 줄 번호만 있으면 어느 기기에서든 업무 API 통행(번호는 대표 목록에 보임) — 파괴급만 K1 로 닫힘.
3. 옛 백업 복원 시 폐기 기기·열쇠·비밀번호·로그아웃 부활.
4. 기동 시 서버줄 재표식 · 출하 DDL 에 DB-89 방어선 없음.
5. 그 PC 안의 악성 프로그램 · 전화 사기(대표 자격 필요).
6. 한 PC 두 번째 회사부터 도메인이 첫 회사 API 로.
7. CORS 전체허용 + CSP unsafe-inline/eval.
8. is_parent 가 없어 대표 전원이 파괴급 가능.
9. 팝업·로컬망 권한창 ⬜ · keys 파일 권한 ⬜.

### 7-7. 출시 게이트 (AC)

01 도메인 첫 세션 200·막힘 시 버튼으로 127 창 · **02 대표 127 첫 로그인 → 업무·자료관리 200 · 새 줄 0 · 도메인 탭 안 갇힘** · 02′ localhost:5234 자료관리 403 + 안내 · 04 캐시 삭제 후 127 재로그인 새 줄 0 · 05 사내 PC 도메인 대기·자료관리 거부 · **05b Host 위조 거부·표식 이동 0** · 06 바깥 폰 자료관리 거부·127 합류 불가 · 07 복사 자칭 자료관리 거부 · 09 재부팅·재기동 후 막힘 0·표식 줄 수 불변 · 12 LOCAL 사내 PC 인정 0 · **13 test1234 DB 수술 0·127 합류 1회·도메인 탭 안 갇힘 — 보이는 줄·슬롯은 결재 2 결과로 기록, 1차에 1/1 약속 금지** · 14 공통 첫 세션 200 · 15 직원 기기 무회귀 · 16 문구·주소 · **17′ 타 사이트 스크립트의 127 로그인 합류·파괴급 거부** · 19 127 창 열기(팝업·권한창·재시도) Playwright · 20 직원 계정 127 로그인은 메인 줄 번호를 받지 않음 · 21 로그인 전 업데이트 안내·동의 무회귀(#43)
2차로: 03·08·10·11·18·복원

### 7-8. 이번 하브루타가 뒤집은 것 — 코드 한 줄 전에 걸렸다

| 뒤집힌 것 | 누가 잡았나 |
|---|---|
| 설계 원안 A-1(도메인 탭 10분 증표로 자료관리) | 보안상무 · CTO(CSP) |
| 자동 확인·폴링(번호 보여주기) | 세 진영 · 보안상무(사회공학) |
| 개발 (d) 저장 비밀로 자료관리 · (e) 127.0.0.1 fetch · (f) 첫 로그인 안1·안2 | 세 진영 교차 · CTO |
| 당장봉합 S1(localhost 로그인에 메인 번호) | 검증 · CTO(슬롯 무제한) |
| 보안상무 「4조건」 | **PM**(Origin 누락 — 악성 사이트 경유 원격 초기화) → 5조건 |
| **PM 교차검토 「Host 로 구별 가능」** | 보안상무·검증 — 절반만 맞음, 각하 |
| CTO 판정기준 「도메인 탭 자료관리 200」 | CTO 자기 철회 |
| K3 [회사서버] 버튼으로 127 창 구제 | **검증 지문 실측** — 버튼이 안 뜬다 |
| 「1.2.89 버튼에 K1 강제」 | CTO·설계 — 2중이 요금으로 부활 → 보안상무 철회 |
| 「실물 DB 는 이미 이관된 DB」(오늘 실측 숫자 오염) | CTO — 전제 각하 · 조회 ③ 으로 판정 |
| 「덮어쓰기는 비밀번호를 안 묻는다」 | CTO — 이미 묻는다 · 빠진 곳은 복원 |

---
**기록**: PM 브라운킴 · 2026-09-10 / **주심**: CTO 래리 · 보안상무 안철수 / **토론**: 설계팀장 존 · 개발팀 · 검증팀장 데이비드 박 / **상태**: 🟢 결론 도출 · 🔴 사장님 결재 대기 · 코드 0
