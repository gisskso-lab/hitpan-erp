# PM 스케줄표 — 2026-06-30 전수조사 수정 리스트

> SOP [1]전수조사 → **[2]PM 스케줄표** → [3]작업지시서 → [4]CTO결재 → [5]사장님승인
> 작성: PM 브라운킴 / 오늘 발견·결정된 수정거리 전수 취합 + 우선순위

---

## 1. 오늘 전수조사·작업에서 나온 수정거리 (전수)

| ID | 항목 | 출처 | 상태 |
|---|---|---|---|
| **M1** | demo db.conf 테스트값 오염 복구 | slot빌더가 운영 db.conf 덮음 발견 | 🔵 사장님 복구대본 전달, 실행 대기 |
| **M2** | slot빌더 db.conf 경로 구멍 봉합 | C:\Program Files\HitPan\db.conf(운영경로)에 씀 | 🔴 작업지시서 필요 |
| **M3** | slot빌더 demo보호 가드 구멍 봉합 | "MariaDB 서비스명"만 체크 → 같은 인스턴스 demo DB 못 막음 | 🔴 작업지시서 필요 |
| **M4** | hitpan_e2e 운영 인스턴스서 격리(별도 인스턴스/PC) | 운영(3306)에 테스트 DB 공존 | 🟡 결재 필요(인프라 #29) |
| **M5** | hitpan_e2e·hitpan_trgtest 잔재 정리(DROP) | 운영 인스턴스 내 테스트 DB | 🟡 사장님 결재 후 |
| **K3·K4** | 고리4② Velopack·MigrationRunner배선 | 연결축 전수파악(어제 기지) | ⏸️ 작2 결재완료, B갈래 대기 |
| **K1 잔여** | desired_domain NCP 쐐기 확인 | 거짓경보 유력, NCP 확인만 | 🟢 사장님 한 줄 |

> 참고: 오늘 빌드테스트(컴파일 5+14 = 0/0), 연결축 전수파악(끊김 거짓경보 3/진짜 2), 고리4 결재 4건은 **완료**.

---

## 2. 우선순위 (의존관계·워크플로우 영향 기준)

### 🔴 P0 — 운영 안전 직결 (즉시)
- **M1 db.conf 복구** — demo 재시작 시 인증 깨짐 잠복. 사장님 복구대본 실행하면 종결. (현재 demo 정상이라 급하나 안 터진 상태)
- **M2·M3 slot빌더 봉합** — **재발 차단의 근본.** 봉합 안 하면 다음 슬롯 구축 때 또 운영 db.conf 덮고 또 demo 옆에 테스트DB 만듦. M1을 또 일으킴.

### 🟡 P1 — 격리 정합 (M2·M3 봉합 후)
- **M4 테스트 인스턴스 분리** — slot빌더 봉합되면, 테스트는 별도 인스턴스/포트로. 이게 서야 K3·K4(고리4) B갈래 검증 안전.
- **M5 잔재 DROP** — M4로 새 격리 환경 서면, 운영 인스턴스의 hitpan_e2e·trgtest 정리(사장님 결재).

### 🟢 P2 — 기능 (격리 환경 선 후)
- **K3·K4 고리4②** — M4 격리 환경 위에서 P1 구현·검증.
- **K1 잔여** — 사장님 NCP 한 줄(독립, 언제든).

---

## 3. 의존관계 (순서가 왜 이런가)

```
M1(db.conf복구) ──┐
                  ├─→ M2·M3(slot빌더 봉합) ──→ M4(테스트 인스턴스 분리) ──→ K3·K4(고리4 구현·검증)
M3(가드구멍) 봉합 ─┘         재발차단              안전한 검증환경            B갈래 본작업
```

- **M2·M3을 먼저 안 하면** M4(격리 환경 구축)도 또 운영을 덮을 수 있음 → 근본 봉합이 선행.
- **M4를 먼저 안 하면** K3·K4(고리4 검증)가 운영 인스턴스서 돌아 헌법#39 위반.
- 즉 **M1·M2·M3 → M4 → K3·K4** 순서 불변.

---

## 4. 이번 작업지시서 범위 (PM 제안)

**우선 작업지시서 1건 = M2+M3 (slot빌더 근본 봉합)** 으로 묶음.
- 이유: 둘 다 같은 파일(Build-TestSlot.ps1)의 같은 사고(운영 미터치 실패)라 한 봉합으로 묶는 게 맞음.
- M1은 사장님 복구대본으로 별도 진행(운영 파일이라 사장님 손).
- M4·M5는 M2·M3 봉합 후 별도 결재(인프라 #29).

→ 다음: [3] M2+M3 작업지시서 작성 → [4] CTO결재 → [5] 사장님 승인.

---
**상태: [2] 스케줄표 완료. [3] 작업지시서로 진행.**
