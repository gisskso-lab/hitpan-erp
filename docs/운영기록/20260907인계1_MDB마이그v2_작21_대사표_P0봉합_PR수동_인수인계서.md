# 인수인계서 2026-09-07 — MDB 마이그 v2 · 작21(대사표 + P0 봉합) 완료 · PR 수동 대기

> 🟢 **진실원**: 이 문서가 MDB 마이그 v2 계열의 현재 진실원이다. `20260904인계1_BOM창고축_…` 은 ⬛흡수(BOM 창고축 건은 그 문서 §8 백로그만 유효).
> 세션 시작 즉시 §0 → §1 → §9 순으로 읽어라.

## §0. 30초 요약
- 지금 무엇을 하고 있나: MDB 마이그 v2 **작21(W1 대사표 + W2 P0 봉합)** — 구현·[3-V]·[4] 검증·[5] 결재까지 끝났고 **커밋 `45e94d92` 를 브랜치에 push** 했다.
- 어디까지 됐나: **PR 생성만 남았다** — `gh pr create` 가 권한 분류기에 막혀 사장님 수동. 게시는 **작22 전 금지**(결1 §3-④).
- **다음 첫 행동**: §9 — PR 이 머지됐는지 확인 → 안 됐으면 사장님께 PR 생성을 여쭙고, 됐으면 `git show origin/main:<파일>` 대조 후 작22 작지서 착수.

## §1. 현재 상태 (실측 2026-09-08 03:50)
| 항목 | 상태 |
|---|---|
| 브랜치 | `feat/20260904-jak21-mdb-mig-v2` (origin push 완료) · main 은 `ce13dd89` 그대로 |
| 최신 커밋 | `45e94d92` feat([6]): MDB 마이그 v2 — 대사표 + P0 봉합 · 28.5분→6.5분 (34 files · +4,414/−108) + 본 인계서·커밋해시 반영 docs 커밋 1건 |
| 미커밋 | 0 (레포 밖 잡파일 제외) |
| 빌드 | errors 0 / warnings 0 · 게이트 66/66 · 전체 1,217/1,217 |
| 배포 | **게시 안 함** — 1.3.37 그대로. 작22(두 모드) 전 게시 금지 |
| PR | ⬜ **미생성** (권한 분류기 차단) — 본문 초안 scratchpad `pr_body.md` · 제목 = 커밋 제목 |

## §2. 오늘 한 일 (커밋 단위)
| 커밋 | 한 줄 | 근거 | 검증 |
|---|---|---|---|
| `45e94d92` | 작21 — 판정 순수함수 7 · P0 5건 봉합 · 대사표 서비스·API·화면 · payments 이관 · DB-118 · [3-V] 5건 · 실측 도구 | `docs/운영기록/20260904작21_…작업지시서.md` · 명세서 `docs/개발/erp/20260907_개발명세서_MDB마이그v2_대사표_P0봉합.md` | `docs/검증/erp/20260907_작업리뷰서_MDB마이그v2_대사표_P0봉합_조건부.md` ⚠️조건부→✅통과(문서 정정 3건 반영) |

## §3. 결정된 것 (결재 완료 — 다시 논의하지 마라)
- Q1~Q12 설계 판단 — 사장님 *"1~12 모두 전결"* → `docs/운영기록/20260904전결1_…PM전결서.md`
- 작21 결과 결재·커밋 승인 — `docs/운영기록/20260907결1_MDB마이그v2_작21_최종결재_CTO반증_사장님결재3.md` (CTO 반증 10)
- 작지서 오기 2건은 코드가 아니라 **읽는 쪽 코드**가 정답 — 병렬이슈 9·10 (`'purchase'` · `ref_doc_type='sales_delivery'`)

## §4. 미결·보류 (사장님 결재 대기) — 결1 §3
| 안건 | 왜 보류 | 누가 |
|---|---|---|
| ① 서비스 품목(유지보수 관리비 등)이 재고로 잡힘 — 재고관리 대상에서 뺄지 | 레거시 DOCFC 는 74품목만 센다 · DOCFS 플래그 확인 필요 | 사장님 |
| ② 대사표 ⑤⑧⑨(재고 기말·미수·미지급) 정답 칸 식 | H1 식은 미수 303억을 내 정답 아님 — 레거시 화면 식이 필요 | 사장님 1줄 / VB6 |
| ③ DOCME 242K 이관 여부 | 전결1 Q6 재확인 | 사장님 |
| ④ 게시 시점 | 작22(두 모드) 전 게시하면 봉합 전 이관 DB 위 병합 시 옛 방향 행 잔존 | 사장님 |
| PR 생성·머지 | 권한 분류기 차단 | 사장님(수동) |

## §5. 함정 — 다음 세션이 밟을 지뢰
- **PR 을 만들 때 스쿼시 머지 확인**: 브랜치에 커밋이 2개(코드 + docs)다. 머지 후 `git show origin/main:src/HitPan.Application/Services/LegacyMdbMapping.cs` 등으로 **파일 존재를 직접** 대조(8/23 스쿼시 사고).
- **재이관은 덮어쓰기 후**: 봉합 전 코드로 이관된 DB(예: 사장님 PC 실물) 위에 병합 재이관하면 옛 sales/purchase 행이 **다른 표에 남는다**. 작22 두 모드가 그 자리 — 그 전엔 실물 DB 에 돌리지 마라(#39 도 걸린다).
- **비번은 코드 주석에서 셸 변수로만**: `grep -m1 "사장님 MDB(비번 "` → env. 어떤 문서·로그에도 값 금지. 줄번호로 뽑지 마라(요약 줄이 54, 비번 줄이 55 — 한 줄 빗나갔었다).
- **perl -0pi 로 UTF-8 파일에 decoded 문자열을 끼우면 파일 전체 이중 인코딩** — rows 파일을 `<:raw` 로 읽어 바이트끼리. 백틱·따옴표 많은 perl 은 heredoc 도 깨진다 → **.pl 을 Write 로 쓰고 실행**.
- **게이트 기대값을 작지서에서 베끼면 작지서 오기를 지킨다**(W5 사고). 기대값은 그 값을 읽는 화면·식에서.
- **`dotnet test --no-build` 의 빈 출력**: 테스트 DLL 이 stale 이면 grep 이 아무것도 안 잡는다 — 초록으로 오해하지 마라.
- 15잡 병렬은 conn 발급이 24초에 걸쳐 늦어질 수 있다(병렬이슈 12) — 잡을 더 늘리면 `MaximumPoolSize=20`·`ConnectionTimeout=60` 부터.
- `hitpan_e2e` 는 지금 2차 이관 + 검증팀장 재이관 결과가 들어 있다(tenant `e2e-mig-baseline-tenant`). 새 실측은 scratchpad `reset_e2e.sh` 로 초기화 후.

## §6. 건드리면 안 되는 것
- `LegacyMdbMapping` 시그니처 7개 — 게이트 W10·대사표 서비스가 같은 시그니처를 부른다.
- `ApplyJobSessionTuningAsync` 의 `unique_checks=0` — 첫 SET 실패로 지금은 전부 미적용인데 **일부러 안 고쳤다**(INSERT IGNORE 멱등과 충돌 가능). 작22 재검토 항목.
- `EnsureMigrationWarehouseAsync` 의 MAIN 우선 — 9/4 사장님 *"미지정 = MAIN"*.
- `installer/hitpan_db_clean.sql` payments 3컬럼 + `uq_payments_source` + 시드 DB-118 — #36.

## §7. 관련 문서 (주소만)
| 종류 | 주소 | 언제 |
|---|---|---|
| 작업지시서 | `docs/운영기록/20260904작21_MDB마이그v2_대사표_P0봉합_작업지시서.md` | 범위·DoD 확인 |
| 전결 | `docs/운영기록/20260904전결1_MDB마이그v2_Q1-Q12_PM전결서.md` | 판정 근거가 필요할 때 |
| 설계 | `docs/설계/erp/20260904_설계_MDB마이그v2_대사표_덮어쓰기병합.md` | 두 모드·W3 설계(작22 착수 시) |
| 선행검증 | `docs/검증/선행/20260904_선행검증서_MDB마이그v2_8개전제_실측.md` | P0 5건의 실측 근거 |
| 개발명세서 | `docs/개발/erp/20260907_개발명세서_MDB마이그v2_대사표_P0봉합.md` | 파일:줄 · §5 안 한 것 |
| 실측 전/후 | `docs/검증/erp/20260907_검증_MDB마이그v2_베이스라인_봉합전_실측.md` · `…봉합후_실측_대사표.md` | 숫자 |
| 작업리뷰서 | `docs/검증/erp/20260907_작업리뷰서_MDB마이그v2_대사표_P0봉합_조건부.md` | 반증 10건 · P1 |
| 병렬이슈 | `docs/검증/병렬이슈/20260907_병렬이슈9~13_*.md` | 봉합 5건의 원인 |
| 결재 | `docs/운영기록/20260907결1_MDB마이그v2_작21_최종결재_CTO반증_사장님결재3.md` | 사장님 결재 항목 · 작22 목록 |

- ⬛ 흡수: `20260904인계1_BOM창고축_봉합_창고미사용_1337게시_인수인계서.md`(진실원 자리만 넘김 — BOM 건 백로그는 그 문서 §8)

## §8. 남은 백로그 — 작22(W3) 작지서 대상 (결1 §5 번호 고정)
- [ ] 1 계산서 `invoice_no` 방향 토큰 + stage `ORDER BY` — **P1**
- [ ] 2 두 모드(덮어쓰기=DataReset / 병합) · 찾아보기·업로드 · 1/2단계 · 체크포인트 — P1
- [ ] 3 대사표 ⑤⑧⑨ 정답식(사장님 ②) · ⑩ 자리표시 사원 제외 · ⑤ 품목 키 — P1
- [ ] 4 서비스 품목 재고관리 여부(사장님 ①) — P1
- [ ] 5 `preview`·`reconcile` POST — P2
- [ ] 6 cashbook·bank·expenses·orders 읽는 식·`total_amount` 대조 — P2
- [ ] 7 재이관 진행 행수 2배 · `JournalSides` 양쪽 비영 카운트 · 비활성 MAIN · bank 85→214s · 세션 튜닝 — P2
- [ ] BOM 창고축 건 사장님 화면 실측(9/4 인계1 §8) — 별도 계열

## §9. 다음 세션 첫 행동
```bash
cd "C:/Users/소순근/Desktop/hitpan-erp" && git fetch origin && git log --oneline -3 origin/main && gh pr list --head feat/20260904-jak21-mdb-mig-v2 --state all
```
- PR 이 **없으면**: 사장님께 *"작21 PR 을 만들어 주십시오(브랜치 feat/20260904-jak21-mdb-mig-v2 → main · 본문은 scratchpad pr_body.md 또는 결1 §1)"* 1줄. 만들면 CI(build·db-gate) 를 SHA 로 확인 후 머지.
- PR 이 **머지됐으면**: `git checkout main && git pull` → `git show origin/main:src/HitPan.Application/Services/LegacyMdbMapping.cs | head -3` · `git show origin/main:installer/hitpan_db_clean.sql | grep -c uq_payments_source` 로 대조 → 결1 §3 사장님 결재 4건 답을 받은 뒤 **작22 작지서**(설계 문서 §W3 + 결1 §5 번호대로) 착수. 게시는 작22 와 묶는다.
