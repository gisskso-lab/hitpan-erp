# 20260825인계6 — 매입 오더 5건 완주 · 반품이 매입 숫자에 반영 · 1.3.18 게시 인수인계서

| 항목 | 내용 |
|---|---|
| 문서번호 | 20260825인계6 |
| 일자 | 2026-08-25 |
| 세션 | 작16(매입 오더 5건) → 작17(반품 정합성) |
| main | `142d7753` |
| PR | **#297**(작16) · **#299**(작17) — 둘 다 머지 완료 |
| 게시 | **1.3.18** (`requiresMigration:true` — DB-109 포함) · 🟢**실물확인**(매니페스트·zip 200·**sha256 실측 일치**·**게시빌드 커밋 = main**·DB-109 패키지 실재) |
| 미커밋 | **0** |

---

## 0. 🔴 다음 세션이 가장 먼저 볼 것

### ① 사장님 실측 대기 — 1.3.18

**매입관리 오더 5건이 다 들어갔다.** 확인 요청 드릴 것:

| # | 화면 | 무엇을 보나 |
|---|---|---|
| 1 | 발주현황·매입현황·반품현황·매입순위표·매입통계 | **조회유형 콤보를 바꾸면 바로 다시 조회되는지** |
| 2 | 매입 처리 목록 | 행마다 **「매입확정」 버튼**(초록 체크) · **확정된 건도 체크가 되는지** |
| 3 | 매입 처리 목록 → 확정 건 선택 → **「반품으로 전환」** | 되는지 |
| 4 | 발주서·매입처리·반품처리 목록 | **「작성자」 칸** (⚠️**오늘 이후 작성분만** 채워진다) |
| 5 | 반품 처리 → 반품 사유 | **직접 타이핑**이 되는지 · 쓴 말이 다음에 선택지로 뜨는지 |
| 6 | 매입현황·순위표·통계 | **반품한 만큼 금액이 빠졌는지** |

### ② 🔴 마이그레이션이 들어가는 게시다

`DB-109`(매입반품 작성자 컬럼)가 포함돼 **`requiresMigration:true`** 로 나갔다.
⚠️ **API 가 재기동돼야 마이그가 돈다** — 작12 에서 *"게시했다 ≠ 적용됐다"* 를 겪었다.
사장님 실측에서 **반품 저장이 안 되면** `schema_migrations` 에 DB-109 가 있는지부터 본다.

---

## 1. 오늘 한 일

### 작16 — 매입관리 오더 5건 (PR #297)

| 오더 | 결과 |
|---|---|
| ① 조회유형이 날짜로만 나옴 | 🟢 **콤보 한 줄**이 원인 — `@bind-Value` 는 값만 담고 재조회를 안 건다 |
| ② 매입확정 버튼 + 확정건 반품전환 | 🟢 버튼 신설 + **차단 3곳 해제**(전결) |
| ③ 담당자(작성자) 행 | 🟢 3목록 + **DB-109** |
| ④ 반품사유 자유입력 | 🟢 `MudAutocomplete` — 마이그 불요(DB 가 `varchar` 였다) |
| ⑤ 재고·미지급금 차감 | 🟢 **이미 되고 있었다** — 손댈 게 없었다 |

### 작17 — 반품이 매입 숫자에 안 빠지던 것 (PR #299)

매입 리포트 **20종이 `purchase_returns` 를 아예 안 봤다**(참조 SQL **0건**).

🔴 **사장님 설명이 원인을 짚었다** — *"불량100개가 들어왓다고 해도, **100개를 반품하고 재주문 하겠지**"*
⇒ 실제로 산 건 **100개**인데 화면엔 **200개**.

**바꾼 것**: 리포트 18종(현황5+순위5+통계8) `UNION ALL` 음수합산 + 회계 6곳
(🔴부가세 신고 · 🔴미지급금 · aging · 대시보드 · 손익추이 · 6개월추이)
⚠️ **매입매출장만 빼지 않고 반품을 음수 「행」으로** — 집계가 아니라 **전표 목록**이라.

---

## 2. 🔴 실측 (로컬 MariaDB · 매입 SQL 20종 전부 실행)

**불량 100개 매입 → 100개 반품 → 100개 재주문**

| 항목 | 봉합 전 | 봉합 후 |
|---|---|---|
| 매입현황 | **200개 / 200,000원** 🔴 | **100개 / 100,000원** 🟢 |
| **부가세 매입세액** | 20,000원 🔴 | **10,000원** 🟢 |
| 미지급금 | 220,000원 🔴 | **110,000원** 🟢 |
| YoY 전년 | 300,000원 🔴 | **200,000원** 🟢 |
| **재고** | 100개 🟢 | 100개 🟢 |

🟢 **SQL 20종 실행 성공 20 / 실패 0**

⚠️ **재고는 원래 맞았다** — 사장님 *"재고에만 영향이 있겠지"* 그대로. 틀린 건 **화면 숫자**뿐이었다.
🔴 **부가세가 제일 무거웠다** — 반품해서 안 산 물건의 매입세액을 **국세청에 공제 신청**하던 상태.

### 실측 아니었으면 못 찾았을 2건
① **전량 반품 건이 목록에서 통째로 사라졌다** — 순액0·건수0(매입1+반품−1)이라 `HAVING` 을 못 넘음
  ⇒ 사장님 헌법 *"숨기면 안 보이는 채로 쌓인다"* ⇒ **절대값 기준**으로 0원 표시
② **YoY 는 당해·전년 양쪽** 다 빼야 — 한쪽만 빼면 **증감률이 거짓**

---

## 3. 🔴 이번 세션에 내가 세 번 틀렸다 — 공통점이 하나다

| # | 내 판단 | 사장님 정정 |
|---|---|---|
| 1 | 판매도 같이 고치자(양쪽 공통 문제) | *"**매출에 영향이 있나?? 매입반품인데?**"* — 축이 다르다 |
| 2 | 판매가 유형 추가니 매입도 유형 추가 | *"**매입을 매출에 맞춰서 하지 않잖아.**"* |
| 3 | (되돌린 뒤) 이제 맞나 | *"100개를 반품하고 **재주문** 하겠지"* — 현장 흐름이 답 |

🔴 **세 번 다 판매 코드를 근거로 매입을 판단했다.**
*"판매가 이렇게 돼 있으니 매입도"* 는 근거가 아니다 — **매입 업무가 근거다.**

⚠️ 작16 의 교훈(*"판매에서 고친 걸 매입에 옮겨라"*)과 헷갈리면 안 된다.
그건 **같은 병일 때**만 맞다. **업무 축이 다르면 옮기면 안 된다.**

---

## 4. 🔴 사장님 전결 2건 (작16)

**① 확정된 매입도 반품으로 전환** — *"반품은 조회전용이 아니라 워크플로우의 한 축이지."*
2026-04-26 지시(*"확정 행은 조회 전용"*)로 선택 자체를 무시해 **확정건은 반품으로 갈 길이 없었다.**
⚠️ **선택만 열고 위험한 동작은 그대로** — 일괄확정·삭제는 각자 `draft` 로 거른다.

**② 작성자는 오늘 이후 건만** — 매입은 `created_by` 를 **한 번도 안 썼다**(컬럼 있던 2테이블조차 전부 NULL).
**없던 기록을 지어내지 않는다.** 과거 전표의 작성자 칸은 **영구 공란**이다.

---

## 5. 검증

| 항목 | 작16 | 작17 |
|---|---|---|
| 빌드 | 0/0 | 0/0 |
| 전체 테스트 | 816 통과 | **915 통과**(+99) |
| 신설 게이트 | `PurchaseParityGateTests` 12건 | `PurchaseReturnInReportsGateTests` 99건 |
| 게이트 반증 | ✅ 3건 | ✅ 4건 |
| SQL 실행 실측 | — | ✅ **20종 성공/0 실패** |
| DDL | **DB-109** | 없음 |

🔴 **게이트를 짜면서 또 가짜를 만들었다** — 마이너스가 *"하나라도"* 있으면 통과하게 짜서
`qty` 부호만 떼도 **초록불**이 났다 ⇒ **집계 컬럼 수만큼 세도록** 수정.
⚠️ 반증 4건 중 2건이 안 잡혔을 때 **하나는 게이트 허술, 하나는 내 반증 정규식이 CRLF 로 매칭 실패**였다.
**반증이 적용됐는지부터 확인**하고 게이트를 탓해야 한다.

---

## 6. 🔴 남은 것 — 다음 차수

| # | 무엇 | 왜 안 했나 |
|---|---|---|
| 1 | **판매쪽 대칭** — 매출반품이 **매출** 순위표·통계에 안 빠진다 | 같은 유형·다른 축. 사장님이 *"매입만 하고 판매는 다음에"* 로 정하심 |
| 2 | 🔴 **작15 우회로** — `Pages/Purchase/ReturnPage.razor:45` 「반품유형」에 **「판매반품」이 살아 있다** | 매입 메뉴로 판매반품 작성 가능(`api/sales/returns` 로 **실제 저장**). 작15 는 사이드바 2줄·라우트 2개만 지웠다 |
| 3 | 재고 `StockLedgerPage:19`·`Stock.razor:45` **같은 조회유형 병** | 재고 대메뉴 — 작업 영역 밖 |
| 4 | `GetReturnsAsync` 의 `partners` 조인에 **`tenant_id` 누락** | 범위 밖 |
| 5 | `PurchaseSummaryPage`(`/purchases/summary`) 빈 껍데기 | 링크 없는 화면 |

### 이전부터 보류 중 (사장님 지시)
- **로그인창 업데이트 버튼 2개 · 눌러도 진행 안 됨** — 사장님 *"추후에 변경"*.
  🔴 착수 시 **내가 넣은 멱등 가드부터** 본다(승인 행이 남아 있으면 200 만 주고 아무 일도 안 함).
- **상태 전파**(반품확정 → 명세서·수주) — 판매 메뉴 제거로 보류. P0 위험 11곳 조사 기록은 작15 문서에.

---

## 7. 다음 세션 시작 지점

1. **사장님 1.3.18 실측 결과**를 받는다 (§0 ①의 6가지)
2. 반품 저장이 안 되면 → **`schema_migrations` 에 DB-109 있는지** (§0 ②)
3. 통과하면 → **§6 의 1번(판매쪽 대칭)** 또는 사장님 다음 오더

⚠️ **개발PC 통과는 검증이 아니다.** 빌드 0/0 · 915 통과 · SQL 20종 실행 성공은
**만드는 쪽이 맞다는 것**이지 **사장님 화면에 갔다는 것**이 아니다.
