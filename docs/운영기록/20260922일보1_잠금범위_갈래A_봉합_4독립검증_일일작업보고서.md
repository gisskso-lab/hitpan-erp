# 일일작업보고서 2026-09-22 — 잠금 범위 갈래 A · **[4] 독립검증 · 반려 · 봉합**

> 🟢 **완료** — [4] 재검증까지 반영. 1차 반려 → 봉합 → **조건부 통과**.
> 상위 트랙 정본 = `20260921작2_…` **§18 · §19** · [4] 리뷰서 = `docs/검증/20260922_작업리뷰서_…`
> 별건 = `20260922별건1_…` · 앞 진실원 = 9/21 인계3

## §0 한 줄

**어제 못 닫은 미결 4건을 닫으러 들어갔다가, [4] 가 내가 만든 게이트를 반려했고, 그 반려가 옳았다.**

## §1 사장님 오더 (2026-09-22)

| # | 오더 | 처리 |
|---|---|---|
| 1 | **`.gitignore` 고쳐** | 🟢 `c03c2178` — 🔴 2차 봉합 `c8cdb9d3`([4] 가 1차의 구멍을 찾았다) |
| 2 | **빨간불 5건 → 게시 조건 + 별건 · 전결** | 🟢 `a93e901d` — 별건 A·B · **P-25·P-26** |
| 3 | **CI 트리거 → 별건** | 🟢 `a93e901d` — 별건 C · **P-27** |
| 4 | **4-3 · 4-4 봉합해** | 🟢 **둘 다 닫음** — 4-4 는 [4] **반려 → 봉합 → 재검증 조건부 통과** |
| 5 | **봉합 시작해** | 🟢 `c8cdb9d3` — 재상신 1·2·3·5 닫음 |

## §2 커밋 (전부 origin 동기)

| 커밋 | 무엇 |
|---|---|
| `ae3697f2` | A-1 봉합 — `innodb_trx` 캐시 지연 |
| `00d65972` | G-RC16 신설 — W-3 |
| `cc61c84a` | A-4 회귀 전수 결과 |
| `c03c2178` | `.gitignore` 1차 |
| `a93e901d` | 별건 3건 + P-25~P-27 + 작지 §18 |
| `142c2075` | A-4 봉합 — 직렬화 |
| `f868e5d6` | A-4 확정(9→5) + [4] 지시서 |
| `9ca448fe`·`65f97274`·`636f8283`·`277172d7` | **[4] 리뷰서**(에이전트) |
| `c8cdb9d3` | **[4] 반려 봉합** — 설계 §15-7 절대 조건 |
| `db17ac8e`·`f4270a73` | **[4] 재검증 §8**(에이전트) |

## §3 오늘 닫은 것

| 미결 | 결과 |
|---|---|
| **§4-1** 게이트 빨간불 | 🟢 원인 = **`innodb_trx` 내부 캐시 지연**. 🔴 **어제 PM 진단(「다른 연결」)은 틀렸다** — 처음부터 같은 연결 |
| **§4-2** W-3 증명 | 🟡 G-RC16 신설 → **[4] 가 반려** → 설계 §15-7 절대 조건으로 **다시 만듦** → **무력화로 우는 것까지 [4] 확인** |
| **§4-3** 기존 회귀 | 🟢 **9건 → 5건**(병렬 간섭 제거) · **[4] 가 독립 재실행해 같은 값 확인** |
| **§4-4** [4] 독립검증 | 🟡 **반려 → 봉합 → 조건부 통과 · [5] 상신 가능** |

## §4 🟢 전수 회귀 (봉합 후) — **[4] 가 직접 돌렸다**

> 🔴 [4] §7-1 이 「안 했다」고 적은 자리를 이번에 채웠다. **PM 이 인용만 하던 수치를 [4] 가 독립 확인했다.**

| 항목 | 값 |
|---|---|
| 실패 | 🟢 **5** — **별건 A(P-25) 1 + 별건 B(P-26) 4 뿐** |
| 통과 | 🟢 **1,527** |
| 건너뜀 | 🟢 **0** |
| 시간 | 15분 12초 |
| `TaxInvoiceMigratedLockGateTests` 4건 | 🟢 **통과** — 직렬화 봉합이 유지된다 |
| 🔴 EXIT 코드 | **0 이었다** — [4] 도 결과 줄을 읽었다 |

🟢 **4% 칸을 새로 넣었는데 실패가 안 늘었다.** PM 값과 정확히 일치 ⇒ **V-5 · V-6 · W-2 · W-5 를 [4] 가 독립 확인**했다.

## §5 🟢 flaky red 확인 — **6회 중 0회**

> 🔴 [4] 가 지적한 **내가 만든 새 위험**. 절대 조건 `if (s.Blocked)` 가 비결정적 간극 잠금을 **하드 실패**로 바꿨다.

| 측정 | 결과 |
|---|---|
| 회차 | `G_RC16` 단독 **4회** + 전수 회귀 속 1회 + 최종 확인 1회 = **6회** |
| 봉합 쪽 ①②③⑤ 막힘 | 🟢 **6회 중 0회** — 어느 칸에서도 한 번도 안 막혔다 |
| 신설 **4% 칸** | 🟢 **6회 전부 초록** |
| 여유 | **26배** (타임아웃 3,000ms vs 봉합 쪽 최대 **114ms**) |
| ⚠️ 산포 | 봉합 쪽 소요가 **2~114ms(57배)** — 🔴 **「여유 26배」이지 구조적 불가능은 아니다** |
| 🟢 완화 | `DisableParallelization` 로 **혼자 돈다** |
| 🔴 잔여 | **CI 는 33406 으로 폴백하는데 거기선 아무도 안 쟀다** → **게시 조건** |

🟢 **곁다리 성과**: **기준선** 막힘은 여전히 회차마다 1건/3건으로 뒤집히는데 **이제 판정에 영향이 없다**(절대 조건이 기준선과 무관). **봉합이 정확히 그 문제를 무해화했다.**

### 5-1. 🟢 무력화 재반증 — **[4] 가 직접 돌렸다**

| # | 결과 |
|---|---|
| **N-8** | 🟢 **울었다**(`실패: 1`). 🔴 **이 한 회차가 봉합 둘을 증명한다** — ① **기준선 막힘 0건인데도 §15-7 위반이 발화**(옛 판은 회차 의존이라 침묵했다) ② **실패가 이미 있는데 살아있음이 2건째로 발화**(§4-4 수정 실증) |
| **A-1′** | 🟢 **상한 1 로 4회 돌려 4회차에 0% 칸이 우는 것을 잡았다** — `합계 0.00 · s.Locked -1`. **옛 판이면 `s.Sum != 0` 에 걸려 건너뛰고 `비 -0.0000 · 통과`였을 바로 그 조합**이다 |
| 정리 | 🟢 전부 원상 복구 · 최종 확인 **통과 4 · 실패 0** |

🔴 **여기서 내가 또 틀렸다**: 내가 사장님께 *「0%·2%·4% 세 칸 모두 울었다」*고 보고했는데 **[4] 실측은 4회 중 1회 재현**이다. **또 1회 관측을 일반화했다** — §6-10.

## §6 🔴 오늘 내가 틀린 것 — 숨기지 않는다

| # | 무엇 | 누가 잡았나 |
|---|---|---|
| **1** | **어제 A-1 원인 진단을 「규명됨」으로 문서에 적었다** — 처음부터 같은 연결이었다 | **오늘의 나**(어제 같이 적어 둔 「단정하지 마라」 한 줄이 막았다) |
| **2** | **G-RC16 이 설계 §15-7 의 절대 조건을 빠뜨렸다** — 0% 칸에서 **발화 자체가 불가능**했고 봉합을 되돌려도 초록이었다 | **[4]** |
| **3** | **G-RC17 0% 칸이 측정 실패(-1)를 통과로 셌다** — 「0% 는 합계가 0」이라고 **내가 주석까지 달아 놓고** 가드를 그대로 뒀다 | **[4]** |
| **4** | **「무력화 확인했다」가 독립 실행에서 재현 안 됐다** | **[4]** |
| **5** | **1회 실측을 「실측」으로 올렸다**(§10-2 보고표) | **[4]** |
| **6** | **「설계 변경이 필요할 수도」라고 설계를 안 읽고 단정했다** — 설계는 이미 정해 두었다 | **나**(설계를 열어 보고) |
| **7** | **`.gitignore` 1차가 폴더 이름만 막았다** — `hitpan_erp.sql` 이 안 막혔다 | **[4]** |
| **8** | **`backup/`·`Backup/` 로 소스를 막을 뻔했다** | **나**(커밋 전 `--no-index` 실측) |
| **9** | **봉합에서 또 1회 실측을 통과로 올렸다**(4/4 초록) | **[4]** |
| **10** | **「A-1′ 가 0%·2%·4% 세 칸 모두 울었다」고 사장님께 보고했다** — [4] 실측은 **4회 중 1회 재현** | **[4] 재검증** |

🔴 **5·9·10 은 전부 같은 실수다 — 1회 관측을 일반화했다.**
**지적받고 고치면서 또 했고, 그걸 고친 뒤 보고하면서 또 했다. 하루에 세 번이다.**
⇒ 🔴 **「몇 회 쟀나」를 숫자로 안 적으면 나는 1회를 실측으로 올린다.** 보고·문서에 **회차를 반드시 병기**한다.

## §7 🔴 배운 것 (메모리에 남길 것)

1. 🔴 **진단도 실측 전에는 가설이다** — 문서의 「원인은 ~다」는 다음 사람에게 **사실**이 된다
2. 🔴 **`EXIT=0` 은 통과가 아니다** — 결과 줄을 읽어라
3. 🔴 **회귀 실패는 「격리 실행 → 이전 커밋 대조」 순서로 가른다** — 두 번에 갈린다
4. 🔴 **`git check-ignore` 는 추적 파일을 안 보여 준다** — **`--no-index`** 없이는 거짓 안심
5. 🔴 **넓은 무시 패턴이 소스를 지운다** — 막은 뒤 **「안 막혀야 할 것이 살아있나」**를 같이 재라
6. 🔴 **간헐적이면 구조가 아니라 타이밍·캐시** — 다섯 칸 중 한 칸만 다르면 연결·권한·SQL 이 아니다
7. 🔴 **1회 실측을 「실측」으로 올리지 마라** — 🔴 **오늘 세 번 걸렸다.** 보고·문서에 **회차를 병기**한다
8. 🔴 **게이트를 오더 문구에 맞추지 말고 설계에 맞춰라** — 오더는 **무엇을 지킬지**, 설계는 **어떻게 잴지**를 정한다

## §8 🟡 [4] 재검증 판정 — **조건부 통과 · [5] 상신 가능**

> 1차 **반려** → 봉합 → 2차 **조건부 통과**. 🔴 리뷰서 **§1~§7(1차 반려 기록)은 보존**되고 **§8 이 재검증**이다.

### 8-1. 달고 올리는 조건 4건

| # | 조건 | 성격 |
|---|---|---|
| **㉮** 🔴 | **P-28 결재** — C2 감시가 **4% 한 칸의 2.4행 여유**에 매달려 있다 | **사장님·CTO** |
| **㉯** 🔴 | **CI 33406 미측정** — 🔴 **새 절대 조건은 하드 실패라 틀렸을 때 비용이 옛 판보다 크다.** CI 는 33406 으로 폴백하는데 **거기선 아무도 안 쟀다** | **게시 조건**(별건 C·P-27 과 한 몸) |
| **㉰** | **별건 A(P-25) · B(P-26) 유지** | 게시 조건 |
| **㉱** | 🆕 **곁다리 6건** — `tmp-build/*.sql` 4 · `installer-build/*` 2 가 **추적 중인데 무시된다**(기존 규칙 탓 · 이번 봉합과 무관) | **별건 권고** |

### 8-2. 🔴 [4] 가 「못 쟀다」고 적은 7건

33406 전량 · 순서 바꾼 TaxInvoice · **지급 축 보유자 프로브** · ⑤ 서비스 계층 경유 · **W-1 8문장** · 백업 재측정 · 대조 커밋.
🔴 **이 중 「지급 축」과 「W-1 8문장」은 오더 증명에 직접 걸린다** — 후속으로 살려 둔다.

### 8-3. 사장님 오더(작2 §16-3) 최종 현황

| 오더 | 상태 |
|---|---|
| **W-1 금액 불변** | 🟡 **부분** — 비영 기대값 동등 확인 · **8문장 전수는 아직** |
| **W-2 3흐름 불변** | 🟢 **[4] 독립 확인** — 1,527 통과 · SKIP 0 · 빨간불은 별건 2건뿐 |
| **W-3 등록 경로** | 🟡 **「위반을 관측하지 못했다」** — 🔴 **「증명했다」로는 못 쓴다**(1차 반려의 핵심). 단 **게이트는 이제 발화 가능**하고 **무력화로 우는 것을 [4] 가 확인**했다 |
| **W-4 화면·응답** | 🟢 제품 변경 SQL 1줄 + 워치독 내부 |
| **W-5 없던 실패 0** | 🟢 **[4] 독립 확인** |
| **W-6 · W-7 · W-8** | 🟢 유지 (운영·`appsettings` 접촉 0) |

🔴 **「침해 없음」을 아직 단정하지 않는다** — W-1 이 부분이고 W-3 은 「관측하지 못했다」다.

### 8-4. 다음

1. **P-28 결재**(사장님·CTO) · **㉯ CI 33406** 을 게시 조건에 편입
2. **[5] CTO 1차 → 사장님 최종**
3. 게시 조건 **7 + P-25·P-26·P-27·P-28 = 11건** + ㉯
4. 후속: **W-1 8문장** · **지급 축 보유자 프로브** · 별건 A~J′

## §9 사장님 결재 대기

| # | 안건 |
|---|---|
| **P-28** 🔴 | **C2 감시 여유 0.0004** — 2% 는 눈이 없고 4% 는 2.4행 차. 🔴 **게이트를 고쳐 초록 만들지 않는다**(#32). ㉮수용+명시 / ㉯별건 감시 신설 / ㉰**C2 제외는 반대** |
| **P-22** | 게시 순서 — 차단 장치가 자기 릴리스를 못 지킨다 |
| **P-16′** | L5 잔존 위험 수용 (C-C2 실측이 먼저) |
| 게시 조건 | 기존 7 + **P-25·P-26·P-27·P-28 = 11건** |
