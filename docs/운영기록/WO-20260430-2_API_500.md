# 작20260430-2 — /api/chatbot/quota·/api/tenants/me 500 에러 (P0 빠른 트랙)

## 🔴 우선순위
P0 (빠른 트랙)

## 발생 증상
- `GET /api/chatbot/quota` → 500 (반복)
- `GET /api/tenants/me` → 500 (반복)
- 대시보드·챗봇 진입 시 즉시 에러

## 진범 추정 (데이비드 박)
오늘 오전 통신 봉합 작업의 부수효과 의심:
- CORS 정책 단순화 (`SetIsOriginAllowed`)
- appsettings ApiBaseUrl 변경 (`https://api-demo.hitpan.kr`)
- 둘 중 하나에서 인증·테넌트 컨텍스트 깨졌을 가능성

## 담당
- 메인: 백엔드 매니저 (직접 처리, 어려운 영역 = 트랜잭션·미들웨어)
- 보조: 보안 매니저 (JWT·테넌트 컨텍스트 검토)
- 검증: CTO

## 작업 범위
1. API 서버 로그 확인 (`logs/`)
2. 두 엔드포인트 컨트롤러·서비스 추적
3. JWT 클레임에서 tenant_id 추출 단계 확인 (절대원칙 #2)
4. 통신 봉합 커밋(`867f636`) 직전 상태와 비교
5. **회귀 검사: 다른 API 5개 이상 200 OK 확인 (헌법 4조 #4 끊김 0)**

## 검증 보고서 양식
- DB 계층: tenants 테이블 데이터 정상 확인
- 백엔드 계층: 두 엔드포인트 200 OK 응답 + 정상 payload
- 프론트 계층: 대시보드·챗봇 진입 정상 표시
- 연결 끊김 0: 다른 API 5+ 동시 정상
- 고객 시선: 에러 메시지 0건

## SLA
4~12h 이내 핫픽스
