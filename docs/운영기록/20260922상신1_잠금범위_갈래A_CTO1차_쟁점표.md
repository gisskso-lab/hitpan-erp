# [5] 상신 쟁점표 — 잠금 범위 갈래 A (2026-09-22)

> **상신**: PM 브라운킴 → **CTO 래리 1차 결재**
> **사장님**: *"전결 바로 진행해"* (2026-09-22)
> **대상**: `feat/20260920-rc-safe-path` HEAD **`b10a48a9`** · **미머지·미게시**
> 근거: 작지 **§18·§19·§20** · 개발명세서 **§9~§13** · [4] 리뷰서(**§1~§7 반려 · §8 재검증**) · 별건 등록부 · 일보 `20260922일보1`

## §0 한 장 요약

| 항목 | 값 |
|---|---|
| 제품 변경 | **L2 에 `AND ec.ref_doc_id IS NOT NULL` 1줄** + DB-125 인덱스 2개 + 워치독(938 추가 / 1 삭제) |
| 이번 세션 제품 변경 | 🟢 **0줄** — 바뀐 것은 **시험 코드와 문서와 `.gitignore`** 뿐([4] 6커밋 전수 확인) |
| 빌드 | 🟢 errors 0 / warnings 0 |
| 전수 회귀 | 🟢 **실패 5 · 통과 1,527 · 건너뜀 0** (**[4] 가 직접 실행**) — 실패 5는 **전부 별건 A·B** |
| [4] 판정 | 🟡 **1차 반려 → 봉합 → 2차 조건부 통과 · [5] 상신 가능** |
| 게시 조건 | **12건**(CTO 7 + P-25·P-26·P-27·P-28·P-29) |

## §1 🔴 CTO 가 물어야 할 쟁점 (PM 이 먼저 꺼낸다)

> **PM 이 숨기지 않고 전부 올린다.** 아래 중 **하나라도 답이 안 서면 반려가 맞다.**

| # | 쟁점 | PM 의 답 | 남는 의문 |
|---|---|---|---|
| **Q1** 🔴 | **flaky red — 산포 57배** | 6회 중 0회 막힘 · 여유 **26배**(3,000ms vs 최대 114ms) · `DisableParallelization` 으로 혼자 돈다 | 🔴 **확률적 보장이지 구조적 보장이 아니다.** 느린 PC·부하 높은 CI 에서 114ms 가 3,000ms 를 넘을 수 있나? |
| **Q2** 🔴 | **하드 실패로 바꾼 대가** | 절대 조건 `if (s.Blocked)` 는 **설계 §15-7 이 정한 것**이고, 이걸 안 넣어서 1차 반려였다 | 🔴 **틀렸을 때 비용이 옛 판보다 크다**([4] §8 ㉯). 거짓 빨간불이 배포를 막는다 |
| **Q3** 🔴 | **W-3 을 「증명」에서 「관측하지 못했다」로 내렸다** | [4] 1차 반려의 핵심. **게이트는 이제 발화 가능**하고 **무력화로 우는 것을 [4] 가 확인**했다 | 🔴 **사장님 오더 「침해 절대 금지」가 「위반을 관측하지 못했다」로 충족되나?** |
| **Q4** 🔴 | **척도 문제** — `trx_rows_locked = 1` 을 보고하면서 이웃을 **3,011ms 막았다** | 잠금 행 수는 **간극 잠금을 안 센다** ⇒ **보조 지표**로 내리고 **개선 단언은 G-RC16 절대 조건**이 한다 | 🔴 **계수 0.80(P-20)은 피해와 단조 관계가 증명되지 않은 척도 위에 있다.** 그대로 둬도 되나? |
| **Q5** 🔴 | **CI 는 33406(STATEMENT)으로 폴백하는데 아무도 안 쟀다** | **P-29** 로 게시 조건에 올렸다 | 🔴 **게이트가 CI 에서 무엇을 재는지 모르는 채 상신하는 것**이다 |
| **Q6** 🟡 | **C2 감시 여유 0.00043(≈2.4행)** · 2% 칸은 눈이 없다 | 사장님 결재 **P-28** — 명시+수용(㉮) + **별건 K**(㉯). 🔴 **게이트를 고쳐 초록 만들지 않는다**(#32) | 🟡 **C2 가 조용히 빠져도 아무도 모른다**는 상태를 수용하는 것이 맞나 |
| **Q7** 🟡 | **지급 축(L4·L5) 보유자 프로브 0회** | 이번 변경이 **L2 만** 건드린다(DB-125 는 `payments` 무접촉 · 파일 확인) | 🟡 **「안 건드렸으니 안 잰다」가 성립하나?** 인덱스 추가가 다른 경로에 주는 영향은? |
| **Q8** 🟡 | **W-1 8문장 전수 값 동등 미완** | 비영 기대값 동등은 확인. 8문장 전수는 후속 | 🟡 **금액 불변을 부분만 증명하고 상신하는 것** |
| **Q9** ⚪ | **별건 A(G-RC13) 빨간불을 안고 상신** | L4 는 **§17-4 B-3 으로 범위 밖**. **초록으로 만들지 않았다** | ⚪ 게시 조건 P-25 로 닫는다 |
| **Q10** ⚪ | **게시 순서**(차단 장치가 자기 릴리스를 못 지킨다) | 사장님 결재 **P-22** — 1차 차단 장치만 → 2차 DB-125 묶음 | ⚪ 결재됨 |

## §2 🟢 이번에 닫힌 CTO 조건

| 조건 | 상태 |
|---|---|
| **C-D1** — *"이웃 프로브를 2%·4% 시드에서도"* | 🟢 **충족** — 판정 구간에 **4% 를 넣었고 6회 전부 초록**([4] 확인) |
| **C-A1** — *"상한을 하드코딩 숫자로 걸지 마라 · 쌍 비로 판정"* | 🟢 유지 — 0.80 은 **같은 회차 쌍의 비**다 |
| **C-B1** — *"L2 조인이 INNER 임을 코드로"* | 🟢 확인 — 평문 `JOIN` |
| **C-A2** — 부분 적용 구멍 | 🟢 갈래 B 차단 장치 + G-RC18-b·c·d |

🔴 **C-C1(L5 를 미해결 P0 로 등재) · C-C2(실제 막힘 지속시간 실측)는 미완** — 게시 조건.

## §3 🔴 PM 이 이번 트랙에서 틀린 것 (판정 재료)

> **CTO 가 「이 PM 의 판단을 믿어도 되나」를 재는 데 쓰라.** 일보 §6 에 10건 전부 있다.

| 핵심 | 내용 |
|---|---|
| **진단을 확정으로 적었다** | 어제 「원인은 다른 연결」을 **문서에 규명됨으로** 적었고 틀렸다. 막은 것은 같이 적어 둔 「단정하지 마라」 한 줄 |
| **게이트를 오더 문구에 맞췄다** | 설계 §15-7 의 절대 조건을 빠뜨려 **0% 칸이 발화 불가**였고 **되돌려도 초록**이었다 ⇒ [4] 반려 |
| **1회 관측을 일반화했다 — 하루에 세 번** | §10-2 보고표 · 「4/4 초록」 · 「세 칸 모두 울었다」. **지적받고 고치면서 또 했다** |
| **설계를 안 읽고 「설계 변경 필요할 수도」라고 단정** | 설계는 이미 정해 두었다 |

🔴 **그러나 이번 세션의 봉합은 전부 [4] 가 독립 확인했다** — 무력화 2건 · 전수 회귀 · flaky 6회 · `.gitignore` `--no-index` 전수.

## §4 🔴 [4] 가 「못 쟀다」고 적은 7건

33406 전량 · 순서 바꾼 TaxInvoice · **지급 축 보유자 프로브** · ⑤ 서비스 계층 경유 · **W-1 8문장** · 백업 재측정 · 대조 커밋.

## §5 요청

**[5] CTO 1차 결재** — 승인 / 조건부 / 반려. 🔴 **조건부면 조건을 게시 조건(현재 12건)에 편입할지 명시해 주십시오.**

**상신**: PM 브라운킴 · 2026-09-22
