# 인수인계서 — 자동업데이트 영구 CD 자동화 (골격 착수 완료 / 사장님 NCP 게이트 대기)

> 작성: 2026-07-23 밤 / PM 브라운킴
> **다음 세션 세션시작 즉시 이 문서부터.** (오늘 밤 CICD 자동화 진실원)
> 커밋 `0821c13` (develop). 아직 push 안 함(사장님 지시 시 push).

---

## ★ 다음 세션 첫 행동 (2026-07-23 밤 마지막 갱신 — 이것부터)

**트랙A 코드봉합(NCP무관분) 완료. 남은 건 사장님 손 + 활성화.**

### 지금 상태 (커밋 0e07bac 까지)
- ✅ 버전 1.2.40 (props+iss) / beta가드 / 품질규율 / CODEOWNERS 봉합 (작지서 20260723작2, 검증팀 SoD 3관문 PASS)
- ✅ deploy-update.yml 골격+배선 완료, **여전히 .disabled**(오배포 물리차단)
- ✅ CI 초록(DDL스모크·Build·TruffleHog) / G-D 게이트 소멸(no-cache 이미걸림)
- ✅ P0-검사: CF-1·2는 백지실측 안막음 → **트랙A 바로 진행 가능**

### 🔴🔴★ 결재벽 (다음세션 클로드 필독 — 2026-07-23밤 사장님 못박음)
**NCP sudo 실행 = 반드시 사장님 사전결재 후.** PM은 PowerShell로 NCP SSH 붙을 능력이 되지만(deploy-ncp 키), **능력≠권한. 헌법 #29 절대.** "SSH 되니 결재없이 실행"은 전임PM 해고사고 재발. NCP=운영서버(#39). 결재 후 방식 2택: A(PM PowerShell SSH 직접) / B(PM 대본→사장님 복붙, 더안전). dispatch=PM 트리거→사장님 승인버튼. 백지실측=사장님 Sandbox.

### 다음 세션 순서 (트랙A 활성화 → 백지 UPDATE 첫통과)
1. **🔴 사장님 NCP sudo 1회 세션** (헌법 #29 사전결재 필수, 순서엄수):
   (a) G-A: publish-update.sh + sign-manifest.sh → `/opt/hitpan/installer/updates/` 동일디렉토리 배치
   (b) G-C: 배치본 publish-update.sh:41 webroot `/var/www/updates.hitpan.kr`→`/var/www/updates` 정정 (**구멍①, 안하면 첫실측 100%실패**)
   (c) sign-manifest.sh 상주본==레포 최신본 확인
   (d) G-B: sudoers `/etc/sudoers.d/` — `publish-update.sh --zip * --manifest *` 2인자만 NOPASSWD
2. **🔴 사장님 GitHub 웹 설정** (NCP아님, 클릭): production environment에 required reviewer=사장님 등록 + branch protection "Require review from Code Owners" (CODEOWNERS 실효화)
3. **활성화 작지서** (SOP): deploy-update.yml.disabled → deploy-update.yml (파일명 .disabled 제거). CTO→사장님 결재 후
4. **1.2.40 dispatch 실측**: build→reviewer승인→deploy→curl 서빙 1.2.40 확인
5. **백지 Sandbox 종단**: 1.2.39 설치본이 1.2.40 자동감지·교체 = **UPDATE 사상 첫통과** (원래 목표 달성점)
   - ⚠️ 판독오염방지: 성패=local_update_apply_status+/health+FileVersion으로만. MetaPing400·로컬/watchdog 500은 정상증상. 1.2.40에 새마이그 싣지말것.

### 병행(목표 안막음, 사장님 결정 후)
- 트랙B: CF-1·2·3 봉합(헌법#36 별건P0, licenses 발급경로=사장님 결정 선결)
- 트랙C: MetaPing 봉합(수신 백오피스 이식, 아키텍처 결정3 후)

**스케줄표**: `docs/운영기록/20260723밤_PM작업스케줄표_자동배포_MetaPing_잠복사고.md`

---

## 0. 한 줄 상태

**사장님 지시 "빌드→NCP 배포 완전 자동화(영구 CD)" → 전수조사·4인회의·PRD·CTO결재·검증까지 SOP 완주. NCP sudo 게이트 무관분(4파일) 착수·커밋 완료. 남은 것 = 사장님 NCP sudo 1회 세션 + deploy-update.yml 활성화 배선.**

근본 문제(사장님 못박음): **"UPDATE 실측에서 한 번도 통과된 적 없음"** — 진범의 절반이 **게시가 손작업**(CI→NCP 전송 다리 부재)이라 매번 게시함정 3개에 걸림. 자동화가 이걸 영구 제거.

---

## 1. 오늘 완주한 SOP 체인

```
사장님 지시(빌드→NCP 자동화, 영구 CD)
 → 전수조사 4인(풀스택·네트워크·백엔드·설계)  → 20260723_전수조사_CICD자동화_4인_종합보고.md
 → 4인 회의·협의                              → 20260723_4인회의_CICD자동화_협의종합_PM조율.md
 → AI수석 PRD                                 → 20260723작1_deployupdate_영구CD_작업지시서.md
 → CTO 결재: 부분수정후승인(blocking 없음)
 → 사장님 "모두결재"(이슈 있으면 보고 후 진행)
 → 착수(게이트 무관분) + 검증팀장 SoD 동시    → 3관문 PASS, blocking 없음
 → 커밋 0821c13
```

---

## 2. 커밋 0821c13 — 착수 4파일 (게이트 무관, 지금 안전)

| 파일 | 내용 | 상태 |
|---|---|---|
| `.github/trufflehog-config.yaml` | EC PRIVATE KEY 룰 추가 — 서명 개인키 오커밋 안전망 (기존 3룰 무변경) | ✅ 활성 |
| `scripts/ddl-smoke-test.sh` | env 파라미터화(5개). 로컬 하위호환 100%. MYSQL=실행파일 하나(host/port 분리) | ✅ 활성 |
| `.github/workflows/build-and-test.yml` | DDL 스모크 CI 잡 추가(MariaDB 11.4 컨테이너). 헌법 #36 게이트 CI화 | ✅ 활성 |
| `.github/workflows/deploy-update.yml.disabled` | 자동업데이트 CD 골격. 게이트 통과 전 안 켜짐 | ⏸ 비활성 |

검증팀장 SoD: 3관문(빌드영향0/하위호환/보안불변식) 전부 PASS. 회귀·거짓봉합·blocking 없음.

---

## 3. 🔴 다음 행동 — 2갈래 (둘 다 사장님 손 필요)

### 갈래 A — 사장님 NCP sudo 1회 세션 (deploy-update.yml 활성화 선결, 헌법 #29)
착수 게이트 G-A·B·C를 **한 세션으로 묶음**. 상세: PRD `20260723작1...md` §2.
1. **G-A**: `publish-update.sh` + `sign-manifest.sh` 를 NCP `/opt/hitpan/installer/updates/` **동일 디렉토리** 상주 배치 (publish 가 sign 을 source). 현재 `/root/` 임시본.
2. **G-B**: sudoers 화이트리스트 — `publish-update.sh --zip * --manifest *` 2인자만 NOPASSWD. 오버라이드(`--webroot`·`--private`·`--feed-url`·`--allow-republish`) 차단.
3. **G-C**: `publish-update.sh:41` webroot 기본값 `/var/www/updates.hitpan.kr` → `/var/www/updates` 정정본을 G-A 배치 시 함께.
- **G-D**(별도): Cloudflare 엣지 경유 `curl -I https://updates.hitpan.kr/manifest.json` → no-cache 헤더 확인. (CTO: 이미 nginx 설계에 있을 개연성 높음, 1회 확인으로 해제 가능)
- **G-E**(레포설정): GitHub environment `production` required reviewer 등록 + `.github/CODEOWNERS` 신설(현재 0건).

### 갈래 B — deploy-update.yml 활성화 배선 ✅ **완료** (커밋 예정, `.disabled` 유지)
검증팀장 SoD 2회차가 짚은 활성화 blocker 3건을 **배선 완료**(2회차 검증 통과, 인자 단위 정합):
1. ✅ **build 잡 산출 배선** — publish 3종(api single-file·web·watchdog self-contained)+wwwroot 복사(터널404 봉합 승계)+build-manifest.ps1 호출. build-installer.yml:76-236 정합. EXE·번들·Release 제외(G2 별건).
2. ✅ **environment·channel 소비** — `environment: ${{ inputs.environment }}` + download-url-base 분기(production=`updates.hitpan.kr/packages`, beta=`.../beta/packages`) + verify 피드 분기 + channel `-Channel` 상류 주입.
3. ✅ **verify python3 → jq** — 오탐 제거, 캐시버스터+no-cache 정합.

### 🔴 남은 활성화 blocker — F-1 (검증팀장 SoD 2회차 신규 발견, `.disabled` 해제 전 필수)
**beta environment 보호규칙 미생성 함정**: GitHub 은 존재하지 않는 environment 를 **보호규칙 0으로 즉석 생성** → beta 배포 시 required reviewer 게이트 없이 실행될 수 있음(전 고객 코드교체 방아쇠에 승인 부재). 지금은 beta 가 nginx 403 이라 실피해 제한적이나 불변식 위반 소지.
→ **활성화 전 G-E 에 "beta environment 보호규칙 생성"도 추가**. production-only 활성화라면 무관.
(build version gate 는 SemVer 형식만 보나 -p:HitPanVersion 주입이 input==FileVersion 동치 인과보장 = gap 아님.)

---

## 3.5 별건 규명 완료 — MetaPing 400 진범 (2026-07-23 밤 조사, 코드 0)

**진범 = 스키마 불일치 아님. "받을 문이 없음".** 워치독 송신 URL=`back.hitpan.kr/watchdog/ping`(백오피스 NCP)인데, `/watchdog/*` 수신 코드는 `HitPan.API`(고객PC ERP)에만 있고 **백오피스엔 0건**. 호스트 3자 불일치(송신 back / 수신코드 HitPan.API / 설계문서 api.hitpan.kr 오기). 라우트 없음→프록시/인증 파이프라인 400.
- payload 스키마는 정합(원본 8필드). 자동업데이트 3필드(latest_version·update_channel·consent_message)만 백오피스 DTO·테이블 미반영이나 nullable이라 400 원인 아님.
- 잠복 이유: MetaPingClient.cs:80-82이 400을 LogWarning으로 삼킴+고객무영향+팝업 0회. Sandbox 종단(19:18)서야 첫 관측.
- **🔗 자동화 선행 게이트**: 자동배포 켜지면 MetaPing이 "게시성공률·고객버전 가시성" 유일 소스. 죽어있으면 깜깜이 배포(1.2.40 몇대 적용됐는지 못봄). deploy-update.yml 활성화 전 이 400 봉합 필요.
- **봉합 방향(코드 아님, 작지서 재료)**: 수신을 백오피스로 이식(송신 정상, 안 건드림). watchdog 수신 컨트롤러+watchdog_pings/emergencies 테이블+CsAutoDispatchService를 HitPan.API→HitPan.Backoffice.API로. 워치독 전용 인증경로(라이선스 해시 Bearer) 백오피스에 신설. 문서·URL 3자 정합.
- **🔴 사장님 결정 필요(아키텍처)**: "MetaPing 수신 주체=백오피스 맞나?" (헌법 #35·선제CS 목적상 백오피스가 정답이나 확정 필요)
- 근거: WatchdogOptions.cs:12-13 / MetaPingClient.cs:74-82 / Backoffice.API Program.cs:110-111(watchdog 0건) / HQ_META_PING_SCHEMA.md:11(호스트 오기)

## 3.6 검사2 — 자동배포 활성화 준비상태 (2026-07-23 밤, 존, 코드0)
활성화 로드맵: [0]버전1.2.40 →[1]NCP sudo(G-A·B·C)→[2]GitHub(G-E reviewer+CODEOWNERS)→[3].disabled제거→[4]dispatch→[5]백지종단.
**구멍 3건(활성화 작지서 반영):**
- 🔴**구멍① G-C를 blocking 승격**: production은 CI가 --webroot 안넘김→publish 기본값(`/var/www/updates.hitpan.kr` 틀림) 씀→G-C 정정 빠뜨리면 첫실측 100%실패(옛버전→롤백). "G-A와 함께"로만 적혀 경중저평가. 독립 blocking 명시. NCP세션 순서=G-A배치+G-C정정→G-B sudoers.
- 🟡**구멍② beta는 production-only 잠금**: beta는 --webroot 넘겨야하나 sudoers 2인자에 거부→dispatch시 build성공/deploy실패 혼란. environment choice에서 beta 잠금이 F-1보다 깔끔.
- 🟡**구멍③ 품질게이트 규율**: deploy-update build잡에 test·warnaserror 없음→warnings커밋 자동게시 가능(#19우회). 로드맵[0]에 "dispatch커밋 build-and-test 초록확인" 명문화.
- **MetaPing 위치=병행(선행blocking 아님)**: 자동배포 파이프라인 MetaPing 의존0. 죽어있어도 1.2.40 배포·백지교체 성립. "선행게이트"는 운영가시성 의미. **첫실측 판독=백지Sandbox 직접관측으로 MetaPing 없이 UPDATE 첫통과 확인가능.**

## 3.7 검사3 — MetaPing 봉합 영향범위 (2026-07-23 밤, 백엔드+보안, 코드0)
- **헌법 #22 판정=합헌** ✅: watchdog_pings 컬럼=tenant_id_hash·status·버전·프로세스뿐(매출·거래처·IP 0건). 송신측 금지필드 해시셋+익명화검증 코드有. 헌법#18 "SaaS 텔레메트리" 경계 안. 백오피스 수신이 #35 정합.
- **봉합=옮기기 아니라 새로짓기**: 수신컨트롤러(ping INSERT) ERP에도 없음(미들웨어만 반쪽). admin조회API 없어 백오피스 모니터화면 현재 404. 백오피스에 (a)수신컨트롤러 (b)admin조회API (c)테이블 (d)라이선스발급경로 전부 신규.
- 🔴🔴**별건 잠복사고 2건(사장님 보고필수)**: ①**licenses 테이블 비어있음**(INSERT 0건)→봉합해도 전고객 401=봉합 진짜 선결과제. ②**licenses·watchdog_pings·watchdog_emergencies가 clean DDL 본문 없음**(schema_migrations 시드행만, 헌법#36위반)→신규설치 고객PC ERP에 이 테이블 안생김=MetaPing과 별개 잠복사고.
- **인증**: 백오피스 JWT ≠ 워치독 라이선스해시Bearer. `[AllowAnonymous]`+자체검증(SerialVerifyController 선례). CORS 무관(서버간 호출).
- **위험**: 이미배포된 워치독 스키마 못바꿈→수신을 송신에 100%맞춰야. licenses 발급경로 없으면 봉합해도 401.
- **사장님 결정**: ①수신주체=백오피스? ②licenses 언제/누가 INSERT? ③tenant_id_hash 역매핑 보유가 #22 충돌?

## 4. 확정 우선순위 (PM 조율)

```
트랙1(핵심): [갈래A 게이트] → [갈래B 배선] G1 활성화 → 실측(1.2.40 dispatch) → G5 트리거정책(별건)
트랙2(병렬): G3 DDL스모크 ✅완료(커밋됨, CI 첫 실행 관측 필요)
편입:        trufflehog EC룰 ✅완료
별건(후속):  G7 베타피드 물리생성(nginx) / G2 EXE배포 / MetaPing 400 규명
```

---

## 5. 절대 잊지 말 벽 (구현 시)

- **개인키 GHA 반입 0** — 서명은 NCP publish-update.sh 안에서만. 웹 CD(deploy-ncp.yml) **광역 sudo 복붙 금지** = 개인키 열람 우회로(백엔드 SoD 발견).
- **복붙 금지 3구간**: rsync 위치이동(→/opt) / 광역 sudo(임의 셸) / GHA 롤백 스텝(이중경합).
- **webroot 실서빙 = `/var/www/updates`** (`.hitpan.kr` 아님. 작4 line8 확인은 틀렸고 진실원 실측이 뒤집음. CTO가 nginx 셋업 문서 `20260609:36`로 재확증).
- **재빌드 방식**(아티팩트 승계 아님) — "게시할 커밋=빌드한 커밋" SSOT.
- **실측하려면 새 버전 필요** — 현재 SSOT 1.2.39. 같은 버전 재게시는 다운그레이드 게이트로 감지 0. 예: 1.2.40 dispatch.

---

## 6. 실측 결과 · 대기

### ✅ CI 실측 통과 (2026-07-23 밤, 커밋 7378a09 push 후)
- **DDL 스모크 잡 = 초록(36s)** — 실제 ubuntu MariaDB 11.4 서비스 컨테이너에서 clean DDL 빈DB 단일 import + 테이블125 + 핵심컬럼 + schema_migrations 시드 자기참조 게이트 전부 PASS. env 파라미터화(host/port 분리·비번 주입)가 CI 실동작 확인. (개발PC bash -n 아니라 진짜 CI 첫 통과)
- **Build & Test = 초록(3m35s)** — 기존 빌드 errors 0 + warnings 0 무손상(헌법 #19). 하위호환 확인.
- **TruffleHog = success** — EC 개인키 룰 추가가 오탐으로 자기레포 안 막음.
- 참고: `actions/upload-artifact@v5` Node20 deprecation 경고(비치명, 별건).

### ✅ 갈래A 사전 실측 완료 (2026-07-23 밤, 조회만·코드0) — NCP 세션 크게 좁혀짐
| 게이트 | 실측 | 판정 |
|---|---|---|
| **G-D no-cache** | `curl -I updates.hitpan.kr/manifest.json` → `Cache-Control: no-store, no-cache, must-revalidate` **이미 걸림**. CF 헤더 없음(캐시 안 함) | ✅ **해제 — nginx 작업 불필요, 게이트 소멸** |
| **G-E reviewer** | GitHub production environment 존재하나 `protection_rules: []` = **reviewer 0개**, can_admins_bypass:true. deploy-ncp(웹)도 reviewer 없이 도는 중 | 🔴 GitHub 웹에서 production 에 reviewer(사장님) 등록 (NCP 아님, **설정 클릭**) |
| **CODEOWNERS** | `.github/CODEOWNERS` 없음 확인 | 🔴 신설(작지서 루틴) |
| **F-1 beta** | `curl -I .../beta/manifest.json` → **403**(검증팀장 예측 정확) | 🔴 활성화 전 beta env 보호규칙 or production-only |

**결론: 갈래A 실제 NCP sudo 필요분 = G-A·B·C(상주배치·sudoers·webroot정정)뿐.** G-D 소멸, G-E는 GitHub 클릭, CODEOWNERS·beta는 작지서.

### 실측 대기 (갈래A 착수 때)
- sign-manifest.sh 미열람 — 상주 배치 시 동반 확인
- **코드 수정은 작지서 루틴(SOP)으로만** (사장님 지시 2026-07-23)

---

## 7. 참조 문서

- PRD: `docs/개발/업데이트배포/20260723작1_deployupdate_영구CD_작업지시서.md` (§2 게이트·§3 설계·§4 대조표·§5 불변식)
- 조율: `docs/운영기록/20260723_4인회의_CICD자동화_협의종합_PM조율.md`
- 전수조사: `docs/운영기록/20260723_전수조사_CICD자동화_4인_종합보고.md`
- 승계 원본: `docs/개발/업데이트배포/20260720작4_자동업데이트_GHA배포워크플로우_작업지시서.md`

---

## 8. 원래 목표(백지 실측)와의 관계

사장님 원래 지시 = **백지 Sandbox 새 계정 설치 + UPDATE 실측**. 이 자동화는 그 실측을 **손게시 사고 없이 반복 가능하게** 만드는 인프라. 자동화 활성화(갈래A·B) 완료 후 → 1.2.40 dispatch → 백지 1.2.39 설치본이 자동 감지·교체 실측 = **UPDATE 사상 첫 통과** 목표.
