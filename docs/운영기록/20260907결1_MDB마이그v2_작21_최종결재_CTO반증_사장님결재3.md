# 최종 결재 — MDB 마이그 v2 · 작21(W1 대사표 + W2 P0 봉합) · CTO 반증 + PM 전결 + 사장님 결재 항목 3

- **문서번호**: 20260907결1
- **단계**: 작업 루틴 [5] — CTO 래리 → 사장님. 사장님 오더(9/4) *"1~12 모두 전결 · 루틴대로"* 에 따라 설계 판단은 PM 전결(전결1), 본 결재는 **작업 결과의 결재**다.
- **근거(주소만)**: 작지서 `20260904작21_MDB마이그v2_대사표_P0봉합_작업지시서.md` · 전결 `20260904전결1_…PM전결서.md` · 개발명세서 `docs/개발/erp/20260907_개발명세서_MDB마이그v2_대사표_P0봉합.md` · 실측 `docs/검증/erp/20260907_검증_…봉합전_실측.md`(검1)·`…봉합후_실측_대사표.md`(검2) · 작업리뷰서 `docs/검증/erp/20260907_작업리뷰서_MDB마이그v2_대사표_P0봉합_조건부.md` · 병렬이슈 `docs/검증/병렬이슈/20260907_병렬이슈9~13`
- **판정**: ✅ **결재 — 커밋 승인** (검증 ⚠️조건부 → 문서 정정 3건 반영으로 통과 · P0 0 · P1 1 은 5월 잠복 결함이라 작22 1번으로) · 🔴 **사장님 결재 항목 3 + 게시 여부 1** (§3)

---

## 1. 한 줄

레거시와 **같은 값**(오더 1)에 처음으로 도달한 자리 — 원장 124,152행·순수량·공급가, 회계 27,661행·차변·대변, 매입 공급가, BOM 이 **0원 차**. **속도**(오더 2) 28.5분 → 6.5분. 5월 *"99.7%"* 는 코드 출력을 코드 출력과 맞춘 것이었고, 이번엔 **레거시가 스스로 계산한 값**(DOCFE·DOCFC·DOCF5)과 맞췄다.

## 2. CTO 래리 반증 — "진짜 되나, 나중에 안 터지나"

| # | 반증 질문 | 답 (근거) | 판정 |
|---|---|---|---|
| 1 | 게이트 66/66 초록이 **글자검사** 아닌가 | G1~G8 51케이스는 `LegacyMdbMapping` 함수를 **값으로 호출**. 무력화 4자리 실측: 방향 스왑 G2 5 FAIL · `purchase`→`payment` W5 FAIL · `total_amount` 되돌림 W11 FAIL · 발급 재시도 제거 W12 FAIL → 원복 66/66 (`chain2.log`) | ✅ |
| 2 | 대사표 초록불은 **어디서 오나** | 검증팀장 (a): `hitpan_e2e` 원장 1행 삭제 → 대사표 즉시 OK 9/DIFF 17 「재고원장 행수 −1」. 측정이다 | ✅ |
| 3 | 고객이 **두 번** 돌리면(병합 재이관) 중복·유실 없나 | 검증팀장 (b): 채워진 DB 위 재이관 255.8s → 21표 행수·합계 동일, 지운 1행 INSERT IGNORE 복귀, 중복 0. **단** 계산서 21쌍은 승자가 바뀜(5월 잠복 · P1 · 작22 1번) | ⚠️ 작22 |
| 4 | 봉합 **전** 코드로 이관된 고객 DB 위에 돌리면 | 방향이 바뀌어 옛 sales/purchase 행이 **다른 표에 남는다**(명세서 §5). **덮어쓰기(DataReset) 후 이관이 전제** — 작22 두 모드가 그 자리. 지금 게시하면 안 되는 이유 1 | 🔴 §3-④ |
| 5 | 잡 1개가 죽으면 이관 전체가 죽나 | 1차 실측에서 **그랬다**(`sales_returns` 발급 15초 타임아웃 → 전체 사망). 봉합: 발급을 try 안 + 재시도 2·4·8s + `ConnectionTimeout=60` → 2차 완주. 게이트 W12 | ✅ |
| 6 | 헌법 #1 — 삭제된 −108줄에 **기존 기능**이 있나 | 검증팀장 (h) 전수: 전부 결재된 판정식의 교체(`io=="I"` · `io==1 //매출` · S_BAL 대체 · `isExpense=true` · WH-MIG · supply+vat · try 밖 발급) | ✅ |
| 7 | #2·#5·#15·#13·#36·#39 | 검증팀장 (f)(g): tenant 파라미터 0 · PII SELECT 0 · 빈 catch 0 · DESCRIBE 18표 대조 · DB-118 clean DDL + 시드 · 실측 DB 는 `hitpan_e2e` 만(도구가 스스로 차단) | ✅ |
| 8 | 작지서대로 짰는데 **작지서가 틀린** 자리는 | 2건([3-V] 병렬이슈 9·10) — `payment_type` 어휘 · 수금 `ref_doc_type`. 둘 다 **DDL 주석·"넣었나" 를 믿은 PM 오기**. 게이트 W5 가 오기를 기대값으로 품어 초록이었다 ⇒ **기대값은 읽는 쪽 코드에서** | ✅ 봉합·기록 |
| 9 | 남은 DIFF 16 은 무엇인가 | 정의 차이 3(DOCFC 74품목 기말 · 미수/미지급 H1 식은 매출 총액보다 큰 값을 내 정답 아님) · 히트판이 더 보존 4(DOCFB↔DOCFE 레거시 내부 불일치 353+209 · 폴백 1 · 자동등록 858 · 자리표시 사원 1) · 잔차 5(미매핑 4+1 · 반올림) · 계산서 21쌍 | ✅ 숫자로 기록(Q11) |
| 10 | 속도 6.5분이 **고객 PC** 에서도 그런가 | 아니다 — 개발PC 실측이다. 고객 PC(느린 디스크·백신)는 다르며 conn 발급 지연이 그 증거. 작22 에서 체크포인트·1/2단계 분리 | ⚠️ 작22 |

**CTO 판정**: 리스크는 전부 **기록되고 작22 로 번호가 붙었다.** 결재하지 않을 이유가 없다. 단 **게시는 작22(두 모드) 전에는 금지** — 반증 4.

## 3. 🔴 사장님 결재 항목 (PM 이 못 정하는 것 4)

| # | 물음 | PM 권고 | 근거 |
|---|---|---|---|
| ① | **서비스 품목이 재고로 잡힌다** — "유지보수 관리비" 재고 24,933,238 · "전자세금계산서 중계서비스 N월수수료" 20~30만. 레거시 DOCFC 는 이들을 **안 센다**(74품목만). 자동등록 858 품목·서비스 품목을 재고관리 대상에서 빼야 하나 | **빼는 쪽** — 단 DOCFS 에 재고관리 플래그가 있는지 먼저 확인(있으면 그 값을 따른다) | 검2 §3 품목 차이 상위 |
| ② | 대사표 ⑤⑧⑨(재고 기말 · 미수 · 미지급)의 **정답 칸 식** — 레거시 재고현황·미수금현황 **화면이 쓰는 식**이 무엇인가 | 사장님 1줄 또는 VB6 소스 확인 후 작22 에서 정답 칸을 다시 세운다. 지금 H1 식(ΣS_BAL−ΣS_SUK)은 미수 303억을 내 정답이 아니다 | 전결1 Q2 "약함" 명기 |
| ③ | **DOCME 242K 이관 여부** (전결1 Q6 재확인) | 대사 대상 아님 · 이관 안 함 유지 | 전결1 Q6 |
| ④ | **게시 시점** — 이번 커밋을 고객 PC 업데이트로 내보낼 것인가 | **작22(두 모드) 전에는 게시 금지** — 봉합 전 코드로 이관한 고객 DB 위에 병합 재이관되면 옛 방향 행이 남는다(§2-4). 커밋·머지만 하고 게시는 작22 와 묶는다 | 명세서 §5 |

## 4. PM 전결 (사장님 결재 항목 밖)

- 검증팀장 조건 3건(문서 정정) 반영 → **조건부 → 통과**. 코드 무접촉이라 재검증은 문서 대조로 갈음.
- 검증팀장 P2 5건은 명세서 §5 표에 작22 항목으로 등록.
- [6] 커밋: `45e94d92` (브랜치 `feat/20260904-jak21-mdb-mig-v2` · 34 files). PR 생성은 권한 분류기에 막혀 **사장님 수동**(gh pr create 또는 GitHub 화면) → 머지 후 `git show origin/main:<파일>` 대조.
- 게시 없음(§3-④).

## 5. 작22 로 넘어가는 목록 (번호 고정)

1. 계산서 `invoice_no` 방향 토큰(또는 UNIQUE `(tenant_id, direction, invoice_no)`) + stage SELECT `ORDER BY` — P1
2. 두 모드(덮어쓰기=DataReset 기반 / 병합) · 찾아보기·업로드 · 1/2단계 분리 · 체크포인트 재개
3. 대사표 ⑤⑧⑨ 정답식 재정의(사장님 ②) · ⑩ 자리표시 사원 제외 · ⑤ 품목 키 불일치
4. 서비스 품목 재고관리 여부(사장님 ①)
5. `preview`·`reconcile` POST 통일(병렬이슈 11)
6. cashbook·bank·expenses·orders 읽는 식·`total_amount` 의미 대조
7. 재이관 진행 카드 행수 2배 · `JournalSides` 양쪽 비영 카운트 · 비활성 MAIN 경계 · bank_transactions 85→214s · 세션 튜닝 재검토

---
**결재**: CTO 래리 ✅ (반증 10) · PM 브라운킴 ✅ 전결 · **사장님 ⬜ 항목 ①~④** · 2026-09-07
