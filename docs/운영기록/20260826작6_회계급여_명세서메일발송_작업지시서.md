# 20260826작6 — 회계 급여명세서 일괄 메일발송 작업지시서

| 항목 | 내용 |
|---|---|
| 지시 | 사장님 2026-08-26 — *"회계에서 급여명세서 작성 후 **일괄 급여명세서 메일발송** 할 수 있도록 해"* |
| 결재 | 🟢 **전결** — 4건 (§2) |
| 상태 | 🟢 **W1·W2·W3 완료** — 빌드 0/0 · 971통과 · DDL 0 · 🔴사장님 실측 대기 / 🟡 W4~W7 진행 예정 |

---

## 1. 사장님이 정한 축 (2026-08-26)

| | 회계 | 그룹웨어 |
|---|---|---|
| **급여** | 급여명세서 **작성** | 내 명세서 **조회·다운로드** |
| **경비** | 전건 **조회·결재·반려** | 내 경비 **올림** |

🔴 **같은 데이터를 양쪽이 다른 역할로 본다.** 표를 가르지 않는다
(가르면 **직원이 자기 명세서를 영영 못 본다** — [작4](20260826작4_그룹웨어경비_본인범위_작업지시서.md) 에서 정리된 축).

### 🚨 "경리" 는 직급이 아니라 **권한**이다 (사장님 2026-08-26)

> *"단 **경리라고 정의한건 경리라는 직급에만 권한부여가 되는게 아니라, 권한부여를 대표계정이 정할 수 있도록**"*

🔴 **이 문서에서 「경리」는 설명을 위한 이름일 뿐이다.** 코드는 직급을 보지 않는다.

| 무엇 | 누가 정하나 |
|---|---|
| 급여명세서 **작성·발송** | **대표계정이 권한설정에서 지정** (`PAYROLL` 쓰기) |
| **결재자**(대표이사) | **대표계정이 결재선 설정에서 지정** |
| 본인 명세서 조회 | 전 직원 (본인 것만) |

⚠️ **`position`·`emp_type`·직급명으로 분기하는 코드를 만들지 않는다.** 헌법 #11
(*"권한은 어드민이 직접 설정 — 업종·규모별 템플릿 제공 금지"*) 과 같은 축이다.
회사마다 급여를 만지는 사람이 다르다 — 총무일 수도, 대표 본인일 수도 있다. **우리가 정하지 않는다.**

## 2. 결재 4건 + 축 2건

| # | 건 | 결재 |
|---|---|---|
| ① | 명세서 PDF 양식 | **양식 자유롭게 붙일 수 있게** + **기본 템플릿 제공** (양식템플릿 수정은 **다음 차수**) |
| ② | 메일 본문 | **A안** — 🔴 *"히트판에서 발송되는 **사내메일은 무조건 본문에 자료내용을 공개하지 않음**"* · `"000 발송드립니다"` 정도 |
| ③ | PDF 비밀번호 | **안 건다** — *"어차피 개인메일로 가는데"* |
| ④ | 급여 자동계산 | **안 넣는다** — 수동입력 원칙(사장님 헌법) |

> 🔴 **②는 급여만이 아니라 사내메일 전체 원칙이다.** 다음 문서 메일에도 적용된다.

### 🔴 추가 축 (2026-08-26)

> *"급여명세는 **대표이사 결재**로 넘길거임."*
> *"이메일 뿐 아니라, **그룹웨어에도 급여명세서가 올라갈거고**!!"*
> *"급여명세서는 **이메일로도 받고, 본인것만 그룹웨어에서도 확인, 다운로드 가능**함."*

| 축 | 내용 |
|---|---|
| **⑤ 결재** | 경리 작성 → **대표이사 결재** → 그 다음에 발송·게시. **결재 없이는 안 나간다.** |
| **⑥ 2경로** | **메일**(개인메일 PDF) **＋** **그룹웨어**(본인 것만 조회·다운로드) — **같은 명세서** |

⇒ 표를 가르지 않는 이유가 여기서 또 확인된다. 갈랐으면 **직원이 자기 명세서를 못 본다.**

## 3. 조사 실측 — 있는 것과 없는 것

### 🟢 이미 있다 (새로 만들 필요 없음)
| 자산 | 실물 |
|---|---|
| 메일 인프라 | `IEmailService` — SMTP 설정·발송·**테스트**·이력 (4/29 결재분) |
| **건별 발송이력** | `email_send_history` — `recipient_email`·`status`·**`error_message`**·`smtp_response` **컬럼 완비** |
| 직원 이메일 | `employees.email` + **사원관리 화면에 입력칸 실재** |
| 급여 데이터 | `payroll_slips`(총지급·총공제·실수령) + `payroll_slip_lines`(지급/공제 항목) |
| **양식 분기** | `PdfRenderService` 가 `form_templates` 로 **양식용지/순백지·좌표·여백** 이미 분기 |
| 권한 축 | `PAYROLL` menu_code · `ResolveScopeAsync`(본인만/전직원) |

🔴 **건별 이력표가 이미 있다는 것이 중요하다** — 일괄발송의 핵심인 *"누가 성공하고 누가 실패했나"* 를
**새 표 없이** 남길 수 있다. **DDL 0 · 마이그 불요.**

### 🔴 없다 (이번에 만든다)
| 없는 것 | 왜 |
|---|---|
| **급여명세서 PDF** | 지원 문서 8종이 전부 **거래문서**(견적·수주·명세서·계산서)다. `DocumentSnapshot` 이 거래처+품목+금액 구조라 **급여명세서(지급·공제·실수령)와 형태가 아예 다르다** |
| **일괄 발송** | 지금은 `SendDocumentAsync` = **문서 1건 → 수신자 1명**. N명 루프·부분실패 처리가 0건 |

⇒ *"메일 기능에 급여만 얹으면 되는"* 일이 **아니다.** **PDF 신설 + 일괄발송 신설** 두 개다.

---

## 4. 🔴 이 기능의 위험 — 되돌릴 수 없다

**잘못 나가면 전 직원에게 남의 연봉이 간다. 회수 불가다.**

| 위험 | 대응 |
|---|---|
| 이메일 **없는** 직원 → 조용히 누락. 경리는 "전체 발송했다" 고 알고 **그 직원만 영영 못 받음**(=되는 척) | **발송 전 확인 화면**에서 *"20명 중 18명 발송 가능 / 2명 이메일 없음(홍길동·김철수)"* 를 **이름까지** 보여준다 |
| 이메일 **오입력** → 남의 메일함에 그 직원 연봉 | 확인 화면에 **수신 주소를 그대로 노출**해 경리가 눈으로 본다 |
| 중복 발송 | 발송 버튼 **잠금** + 건별 이력으로 재발송 여부 판단 |
| 미확정 명세서 발송 | **`confirmed`·`paid` 만** 발송 대상 (아래 §5) |

🔴 **100% 자동 금지**(사장님 반자동 원칙) — **제안 → 사람이 확인 → 확정**. 경리가 **보고 나서** 누른다.

---

## 5. 할 일

### W1 — 급여명세서 PDF (`payslip`)
- `IPdfRenderService` 에 `payslip` 문서타입 추가. `payroll_slips` + `payroll_slip_lines` 로 렌더.
- 구성: 회사·귀속연월·사원명 / **지급 항목**·**공제 항목** 2단 / **실수령액** / 지급일
- ① 결재대로 `form_templates` 분기에 **`payslip` 매핑 추가** — ⚠️ `TryLoadTemplateAsync` 의 `documentType→form_type` 표에 **빠지면 조용히 기본 모양으로 인쇄된다**(그 표 주석에 8/12 적발 기록 있음).
- **기본 템플릿 제공** — 양식 미설정 고객도 바로 쓸 수 있게.

### W2 — 🔴 대표이사 결재 배선 (⑤)
- 결재 문서타입 **`payslip` 신설** — 지금 18종에 **급여명세서가 없다**.
- 🚨 **doc_type 은 한 곳만 고치면 조용히 죽는다.** 14차 P2 사고 기록:
  *"결재선 설정 화면에서 켜도 트리거 조회가 lineCount=0 → 결재가 **영영 미생성**"*
  ⇒ `DocTypeLabels`(라벨) · **결재선 설정 화면 목록** · 알림 라벨 · 상신 코드 **전부**에 넣는다.
  기존 게이트 `ApprovalDocTypeWiringGuardTests` 가 이 정합을 검사한다 — **거기에 payslip 도 추가**.
- 상신 주체 = **경리**(작성자), 결재자 = **대표이사**(결재선 설정에서 고객이 정함 — 헌법 #11, 역할을 코드에 넣지 않는다).

> 🔴 **급여표 무접촉 원칙과 충돌하지 않는다** — §6 확인 참조.

### W3 — 🔴 승인 직후 발송 팝업 (사장님 2026-08-26)
```
"급여명세서 결재가 승인되었습니다."
 급여명세서를 발송하시겠습니까?   [Y] [N]
```
- **Y** → 곧바로 발송하지 **않고** §4 확인 화면으로 간다(누가 받고 누가 못 받는지 보여준 뒤 확정).
  🔴 되돌릴 수 없는 발송이라 **Y 한 번으로 나가게 하지 않는다**(반자동 원칙).
- **N** → 나중에 급여 화면의 「명세서 메일발송」 버튼으로 발송할 수 있어야 한다.
  ⚠️ **N 이 막다른 길이 되면 안 된다** — 거절했더니 영영 못 보내는 상태가 되면 워크플로우가 끊긴다(#20).


#### 🔬 착수 전 조사 (2026-08-26) — 팝업을 띄울 자리가 지금은 **알 수가 없다**

| 실측 | 자리 |
|---|---|
| 승인 처리 | `ApprovalController.Process` → `ApprovalService.ProcessAsync` |
| 화면 | `ApprovalPendingPage.razor:61` 「승인」 버튼 → `ProcessAsync(id,"approved")` |
| 최종승인 판정 | `ProcessAsync` 안 **`request.Action=="approved" && doc.CurrentSeq >= doc.TotalLines`** |

🔴 **결재선이 2단이면 1단 승인은 아직 승인이 아니다.** 부장이 승인해도 `current_seq++` 만 되고
문서는 여전히 `pending` 이다. 그런데 **API 응답은 두 경우 모두 `"승인되었습니다."`** 한 문장이고,
화면 서비스(`Web/Services/ApprovalService.ProcessAsync`)는 **응답 본문을 버리고 `bool` 만 돌려준다.**

⇒ 지금 구조에서 팝업을 붙이면 **부장이 승인한 순간에도 "발송하시겠습니까?" 가 뜬다.**
   대표이사 결재 전에 급여명세서가 나가는 길이 열린다 — ⑤결재(*"결재 없이는 안 나간다"*) 정면 위반.

**⇒ 응답에 「최종승인됨」 여부를 실어 보낸다.** `leave`·`absence` 가 이미 쓰는
`CurrentSeq >= TotalLines` 판정을 **그대로** 쓴다(새 규칙을 만들지 않는다).

#### 설계 (확정)

| | 내용 |
|---|---|
| API 응답 | `{ message, isFinalApproved, docType, refId }` — 기존 `message` **유지**(다른 화면이 쓴다) |
| 팝업 조건 | `isFinalApproved == true` **그리고** `docType == "payslip"` **둘 다** |
| **Y** | 급여 화면으로 이동(`/hr/payroll?send=1&slip={refId}`) — **발송하지 않는다** |
| **N** | 아무 일도 안 일어남. 급여 화면 버튼으로 언제든 발송 가능(#20) |

🔴 **팝업은 「발송」이 아니라 「발송 화면으로 가기」다.** 실제 발송은 §4 확인 화면에서만 일어난다.
🔴 **N 은 상태를 아무것도 안 바꾼다** — 아무 표에도 "거절함" 을 적지 않는다. 적으면 그게 막다른 길이 된다.

#### ✅ W3 완료 (2026-08-26) — 빌드 0/0 · 971통과(+18) · DDL 0

**고친 자리 6개** (전부 «추가», 헌법 #1 — 기존 것은 하나도 안 지웠다)

| 자리 | 무엇 |
|---|---|
| `ApprovalDtos.cs` | `ProcessApprovalResult` 신설 (`IsFinalApproved`·`DocType`·`RefId`) |
| `IApprovalService.cs` | `Task` → `Task<ProcessApprovalResult>` |
| `ApprovalService.cs` | 최종승인 판정을 결과로 반환 |
| `ApprovalController.cs` | 응답에 3칸 **추가** — `message` 는 **그대로** |
| `Web/Services/ApprovalService.cs` | `ProcessDetailedAsync` **신설** — 기존 `bool` 메서드 **존치** |
| `ApprovalManagePage.razor` | `MaybeOfferPayslipSendAsync` — 팝업 |
| `PayrollPage.razor` | `?send=1`·`?slip=` 수신 + **귀속 연·월 정렬** |

##### 🔴 실측 — 진짜 `ApprovalService.ProcessAsync` 를 실제 DB(`hitpan_e2e`)에 대고 돌렸다

게이트가 규칙을 **다시 적은 것**이 아니라, **그 코드가 실제로 그렇게 도는지** 봤다.
2단 결재선(부장 → 대표이사)을 실제로 깔고 두 번 승인시켰다.

| # | 상황 | DB 실제 상태 | 반환값 | 팝업 |
|---|---|---|---|---|
| ① | **부장이 1단 승인** | `status=pending` `seq=2` | `IsFinalApproved=False` | ✅ **안 뜸** |
| ② | 대표이사 2단(최종) 승인 | `status=approved` | `IsFinalApproved=True` | ✅ 뜸 |
| ③ | 대조군 — **연차** 최종승인 | — | `docType=leave` | ✅ 안 뜸 |
| ④ | 대조군 — 급여명세서 **반려** | — | `IsFinalApproved=False` | ✅ 안 뜸 |

🔴 **①이 이 작업의 전부다.** DB 가 아직 `pending` 인데 반환값도 `False` 로 일치한다 —
   즉 **부장이 승인한 순간에는 발송 팝업이 안 뜬다.** 대표이사 결재 전에 급여명세서가
   나가는 길이 막혔다(⑤결재).

##### 🔴 반증 3건 — 봉합을 빼고 **FAIL 을 눈으로 확인**했다

| 뺀 것 | 결과 |
|---|---|
| `CurrentSeq >= TotalLines` 조건 | ❌ FAIL `최종승인_판정에_두_조건이_모두_있다` |
| 화면을 옛 `ProcessAsync`(본문 버림)로 되돌림 | ❌ FAIL `화면은_최종승인과_문서종류를_모두_본다` |
| 귀속 연·월 정렬(`_year = slip.PayYear`) | ❌ FAIL `급여화면이_발송요청과_명세서id를_받는다` |

⇒ 셋 다 되돌리니 **18/18 통과**. 게이트가 **동작을 잡고 있다**(글자가 아니라).

##### 결재 지킨 것

- **Y 는 발송이 아니다** — `/hr/payroll?send=1&slip=` 로 **보낼 뿐**이다. 게이트가
  결재관리 화면에서 `send-mail`·`SendMail` 을 부르는 것을 **금지**한다(반자동 원칙).
- **N 은 아무것도 기록하지 않는다** — 게이트가 그 분기에서 `SaveAsync`·`PostAsJsonAsync` 등을
  **금지**한다. 적어두면 그 값이 다음 발송을 막는 자물쇠가 된다(헌법 #20).
- **급여표 무접촉** — 이번에도 `payroll_*` 에 한 글자도 안 썼다. 기존 게이트
  `결재승인은_급여표를_건드리지_않는다` 통과 유지.

##### ⚠️ 남긴 것

- `_sendRequested` 는 **W6 이 쓸 값**이다. 지금은 담아만 두고 **아무 동작도 안 한다** —
  발송 확인 화면(W4·W6)이 아직 없기 때문이다. **W6 에서 이 값을 반드시 소비할 것.**
  안 그러면 Y 를 눌러도 화면만 열리고 확인 창이 안 뜬다.

### W4 — 일괄 발송 API
- `POST /api/payroll/slips/send-mail` — 연·월 + 대상 사원 목록.
- **미리보기**: `GET .../send-mail/preview` — 발송가능/불가(사유: 이메일없음·미결재) 집계 + **명단**.
- 건별로 `email_send_history` 기록(성공/실패 각각). **뭉뚱그린 "발송 완료" 금지**(헌법 #15 정신).
- 실패분만 **재발송** 가능.
- 권한: `PAYROLL` + **쓰기** 축 (조회 권한만으로 발송 못 함).

### W5 — 그룹웨어 게시 (⑥)
- 직원이 **본인 명세서**를 그룹웨어에서 조회·다운로드.
- 🟢 **조회는 이미 된다** — `PayrollController.ResolveScopeAsync` 가 일반 직원을 본인으로 좁힌다(8/13).
- **없는 것 = PDF 다운로드 경로** ⇒ W1 의 `payslip` PDF 를 본인 것만 받도록 연결.
- ⚠️ **결재 승인 전 명세서는 안 보인다** — 메일과 같은 기준. 한쪽만 열면 축이 갈린다.

### W6 — 화면
- 회계 급여 화면에 **「명세서 메일발송」** 버튼 → 확인 다이얼로그(§4 표) → 발송 → **결과 표**(성공 N / 실패 M + 사유).
- ⚠️ 다이얼로그는 **인증 캐스케이드 밖**이다 — `AuthorizeView` 쓰면 화면이 통째로 죽는다(작11).

### W7 — 게이트
- 본문에 **금액·항목이 안 들어가는지**(②결재) — **동작으로** 검사.
- 이메일 없는 직원이 **조용히 누락되지 않는지**.
- **미결재 명세서가 발송·게시 대상에서 빠지는지** (메일·그룹웨어 **양쪽**).
- `payslip` doc_type 이 **모든 등록처에 있는지**(W2 의 조용한 죽음 방지).
- 🔴 짜고 나서 **봉합 빼고 FAIL 확인** 후에만 통과로 친다.

## 6. 🔴 착수 전 확인 2건

### ① 발송 대상 상태 범위

급여 상태는 `draft → confirmed → paid` 인데, 여기에 **결재**가 붙었다.

- **권고**: **결재 승인된 명세서만** 발송·게시.
  결재를 거치라는 것이 곧 *"승인 전에는 나가지 않는다"* 는 뜻이므로, 상태값보다 **결재 여부가 관문**이다.
- `draft`·미결재는 **제외** — 금액이 바뀔 수 있는데 명세서가 나가면 **직원이 틀린 금액을 받는다.**
- ⚠️ **메일과 그룹웨어가 같은 기준**이어야 한다. 한쪽만 열면 축이 갈린다.

### ② 🚨 기존 게이트와의 관계 — `결재승인은_급여표를_건드리지_않는다`

**이미 있는 게이트**가 결재 승인이 `payroll_*` 를 쓰는 것을 **금지**한다
(사장님 2026-08-21: *"급여는 수동입력원칙이야… 급여가 휴직을 모르고 사원만 알면 됨"*).

🔴 **이번 작업이 그 게이트에 걸릴 수 있다.** 다만 **성격이 다르다**:

| | 게이트가 막는 것 | 이번 작업 |
|---|---|---|
| 무엇 | 결재·휴직·근태가 **급여 금액을 자동 생성** | 사람이 손으로 넣은 명세서를 **승인 표시** |
| 왜 | 금액을 우리가 만들면 안 된다 | 금액은 **안 건드린다** |

⇒ **금액에는 손대지 않는다.** 승인 여부는 `approval_documents.ref_id = slip_id` **조회로 판정**하고,
`payroll_slips` 에는 **아무것도 쓰지 않는 방향**을 우선 검토한다(게이트 무수정).

⚠️ 그래도 상태 컬럼 하나가 필요해지면 **게이트를 고쳐야 하는데, 그건 사장님 결재 사항이다.**
🚫 **게이트를 임의로 완화하지 않는다** — 그 게이트는 사장님이 못박으신 원칙을 지키는 물건이다.

## 7. 안 하는 것

- 🚫 **급여 자동계산**(④결재) · 🚫 **PDF 비밀번호**(③결재) · 🚫 **본문에 금액 표기**(②결재)
- 🚫 **표 신설** — `payroll_slips` 그대로. 그룹웨어와 **같은 데이터**를 본다.
- 🚫 **그룹웨어 급여 화면 수정** — 직원 조회·다운로드는 이미 된다(`ResolveScopeAsync`).
- 🚫 **양식템플릿 편집 UI** — ① 결재대로 **다음 차수**.
- 🚫 **직급·직책으로 분기** — 회사마다 조직구조가 다르다. 권한은 대표계정이 정한다(헌법 #11).

---

## 8. 검증 기준

- 빌드 errors 0 + warnings 0 (#19)
- 게이트 **반증(FAIL) 확인**된 것만 인정
- **DDL 0** 목표 — `email_send_history` 재사용
- 🔴 **최종 판정은 사장님 실측** — 실제 메일 수신 확인까지
