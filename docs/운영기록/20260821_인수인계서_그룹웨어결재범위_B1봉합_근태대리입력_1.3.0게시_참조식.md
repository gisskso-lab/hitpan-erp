# [★] 인수인계서 — 그룹웨어 결재범위 확정 · B1 봉합 · 근태 대리입력 · 1.3.0 게시 (20260821)

> **작성** PM 브라운킴 · 2026-08-21 (3번째 세션) · **참조식**(헌법 #41 — 내용 복사 금지, 주소만)
> **읽는 때** 세션이 끝나 **새 창을 열 때**(같은 날). 다음날이면 일일작업보고서를 먼저.

---

## 0. 🟢 한 줄 상태

```
main  d6186016  ·  미커밋 0  ·  게시 1.3.0 (실측 3종 확인)
남은 것: B 연장·조퇴·외출 → C 경비 화면통합 → 작9 → ④ 메뉴정리 → ⑤ 메신저
막는 것: U1 (경비 옵션 표시 문구) — C 만 막는다
🔴 실측 0건 — 1.2.97~1.3.0 전부 화면에서 확인 안 됨
```

---

## 1. 🔴 다음 세션이 **가장 먼저** 알아야 할 것

### 1-1. 실측이 0건이다

코드 2건(B1·A)이 게시까지 갔는데 **화면에서 한 번도 안 돌려봤다.**
오늘 잡은 결함이 전부 *"빌드도 시험도 통과하는데 화면에서 아무 일도 안 일어나는"* 종류였다.
⇒ **여기서 더 쌓으면 깨졌을 때 어느 작업 탓인지 못 가린다.**

### 1-2. 🔴 이번 세션에서 [3-V]가 **내 게이트를 두 번 뚫었다**

| # | 무엇 | 1차 | 문서 |
|---|---|---|---|
| ① | B1 — `if (false)` 로 코드를 죽였는데 **10/10 통과** | 뚫림 | [병렬이슈5](../검증/병렬이슈/20260821_병렬이슈5_B1가드시험이_글자만_봤다.md) |
| ② | A — `tenant_id` 를 빼 **테넌트 격리를 뚫었는데 5/5 통과** | 뚫림 | [개발명세서 §3](../개발/erp/20260821_개발명세서_A_근태대리입력.md) |

🔴 **②는 ①을 겪은 직후에 형태만 바꿔 재발했다.** 이게 이번 세션의 가장 무거운 사실이다.

- ①의 원인: **블록 통째 제거**로 대조실험을 해서 성립한 것처럼 보였다.
  실제 회귀는 **조건 한 줄이 죽는** 식으로 온다.
- ②의 원인: 시험이 **파일 전체**에서 패턴을 찾아, `HrService.cs:199` 의 **무관한 SQL** 이
  초록불을 켜주고 있었다. **검사 대상을 아예 안 보고 있었다.**

> ⇒ **다음 세션 규율**: 가드시험을 짜면 반드시
> ① 조건을 `if(false)`/`if(true)` 로 바꿔 보고 ② **초록불이 어느 줄에서 오는지** 확인한다.
> *"봉합 뺐더니 FAIL"* 은 **어떻게 뺐느냐**에 따라 의미가 다르다.

---

## 2. 사장님 결재 — 이번 세션에 확정된 것

> 원문·근거는 [설계 20260821설5](../설계/erp/20260821_설계_그룹웨어결재_범위확정_ERP분리_회계기표.md) §0 · [설6](../설계/erp/20260821_설계_근태축_경비통합_옵션모듈화.md) §0 에 **원문 그대로** 있다.

| # | 확정 | 무엇이 바뀌나 |
|---|---|---|
| **결재 범위** | **ERP = 확정이 곧 승인 / 그룹웨어 = 결재함.** 예외 0 | 결재함 **8종** |
| **ERP 7종** | *"모두 빼"* — 견적 포함 | **작9**(ERP 파트로 분리) |
| **회계** | 시점은 사람 · **금액은 자동** | ③-회 |
| **S2 휴직** | 자동 배치 **폐기**, 관리자 수동 | ② |
| **S4 필터** | **해소** — 8종이라 2단 필터 불요 | ④ |
| **S5 근퇴** | **연장·조퇴·외출 = 결재 맞음** + 자동근퇴 + 대리입력 | 작10 |
| **예외처리** | 🔴 **권한이 예외. 기본 OFF, 고객사가 켬** | A·U1·U3 |
| **경비** | 화면만 합치고 **표는 그대로** · 옵션 2개 + 모듈화 | C |

### 🔴 사장님이 잡아주신 PM 오판 4건 (반복 금지)

1. ERP 결재 제거를 *"기능 제거"* 로 브리핑 → 실제는 **중복 승인 제거**(확정이 이미 승인)
2. 회계를 *"금액도 수동"* 처럼 브리핑 → *"안그러면 데이터 틀어지니"* — **금액은 자동**
3. *"계정 없는 직원이 앞으로 계정 받나"* 를 물음 → **"우리가 정할게 아님"**
   ⇒ 우리가 못 정하는 것을 **코드가 이미 전제**(자기 것만 입력)한 게 결함의 원인이었다
4. *"발주 결재 원하는 고객"* 을 리스크로 남기려 함 → **SI 사고**(#11 계열). 항목 삭제

---

## 3. 이번 세션에 한 일

### 3-1. 문서 (루틴 [2])

| 문서 | 무엇 |
|---|---|
| [설계 20260821설5](../설계/erp/20260821_설계_그룹웨어결재_범위확정_ERP분리_회계기표.md) | 결재 범위·ERP 분리·회계 기표 |
| [설계 20260821설6](../설계/erp/20260821_설계_근태축_경비통합_옵션모듈화.md) | 근태 축·경비 통합·예외처리 축 |
| [작8](20260821작8_그룹웨어결재_8종확정_회계기표_결재함일원화_작업지시서.md) | 그룹웨어 본선 (B1·③회·②·①-3·④) |
| [작9](20260821작9_ERP결재_중복승인제거_7종_작업지시서.md) | **ERP 파트** — 7종 제거 (P2) |
| [작10](20260821작10_근태대리입력_경비화면통합_옵션모듈화_작업지시서.md) | A 근태·B 연장조퇴외출·C 경비 |

### 3-2. 코드 (루틴 [3]~[6])

| 커밋 | 무엇 | 문서 |
|---|---|---|
| `2dec69e2` | **B1** 경비 결재 원본반영 — **이미 출하된 P0** | [개발명세서](../개발/erp/20260821_개발명세서_B1_경비결재_원본반영.md) · [검증리뷰서](../검증/erp/20260821_검증리뷰서_B1_경비결재_원본반영.md) |
| `d6186016` | **A** 근태 대리입력 + **화면까지** | [개발명세서](../개발/erp/20260821_개발명세서_A_근태대리입력.md) |

**빌드 0/0 · 시험 630/630**(619 → 630, 신규 11)

### 3-3. 게시 4회

```
1.2.97  결재 배선 P0 3건 + 퇴사자 정리   (앞 세션)
1.2.98  결재 범위 확정 문서
1.2.99  근태 축·경비 통합 설계
1.3.0   B1 + A                          ← 코드
```

🔴 **1.2.100 을 돌리다 사장님 지시로 취소했다.** 다음은 **1.2.99 → 1.3.0** 이다.
파일 404 로 안 나간 것 확인 완료. ⚠️ **`1.3.01` 같은 표기 금지** — `System.Version` 이 `1.3.1` 로 읽는다.

---

## 4. 🔴 다음 세션이 이어서 할 일

### 4-1. 권고 순서

```
0. 실측 (M1 · B1 · A)          ← 쌓기 전에
1. B 연장·조퇴·외출 결재문서    ← 작10, 결재 불요
2. C 경비 화면통합              ← U1 답 필요
3. 작9 ERP 7종 제거             ← ④ 선행 · 착수조건 있음
4. ④ 메뉴정리                   ← 화면·문서종류 확정 후
5. ⑤ 메신저
```

### 4-2. B 착수 시 반드시 지킬 것

- 신규 문서유형 `attendance_request`
- 🔴 **라벨 두 곳** — `ApprovalService.cs:15` + `ApprovalTriggerHelper.cs:229`
- 🔴 **`ProcessAsync` 원본 반영 블록 필수** — 없으면 `expense` 와 같은 병(오늘 봉합한 그것)
- 결재함 **8종 → 9종**

### 4-3. ④ 착수 전 주의 (두 번 만지지 않으려면)

⚠️ **④ 필터 목록을 `DocTypeLabels` 에서 뽑지 마라.** 작9 이전에는 **ERP 7종이 살아 있다.**
⇒ **작9 를 ④ 보다 먼저** 하는 편이 낫다. 아니면 그룹웨어 8종을 **별도 정의**한다.

### 4-4. 작9 착수 조건

🔴 **확정 API 권한 통제 실측이 먼저다.** 확정에 권한 검사가 없는데 결재까지 빼면
**아무 통제 없이 확정되는 자리**가 생긴다 ⇒ 별건 P0.

---

## 5. 실측 항목 (⚠️ 헌법 #39 — **테스트 환경만**)

### 5-1. 1.2.97 분 (앞 세션 · 아직 0건)

[실측리스트](20260821_실측리스트_그룹웨어_결재배선_퇴사자정리_1.2.97.md) M1~M3 · R1~R5
⚠️ **M3(퇴사→미결 회수)는 되돌리기 어렵다** — 운영에서 하지 마라.

### 5-2. B1 (경비)

- [ ] 직원 경비신청 → 결재함 승인 → `hr_expense_requests.status` = `approved`
- [ ] 경리 경비등록 → 결재함 승인 → `expenses.approval_status` = `approved`
- [ ] 반려 → 양쪽 `rejected`
- [ ] 🔴 **2단 결재선에서 1단계만 승인 → 원본이 아직 안 바뀜**(헌법 #6 · 문자열 시험이 못 보는 영역)

### 5-3. A (근태 대리입력)

- [ ] `HR_PROXY` 권한 **켜기 전** → 카드 안 보임 · API **403**
- [ ] 권한 **켠 뒤** → 카드 보임 · **계정 없는 직원**이 목록에 뜸
- [ ] 대신 출근 → `attendance` 1행 · `memo` 에 **처리자 남음**
- [ ] 🚨 **다른 테넌트 사원 `employee_id` 지정 → 거부**
- [ ] 자동 근퇴(로그인/로그아웃) **여전히 정상** — 회귀 없음

---

## 6. 🔴 사장님 확인 대기 1건

| # | 질의 | 막는 것 |
|---|---|---|
| **U1** | 경비 옵션 **2개가 무엇과 무엇**인가 | **C 만** |

*"법인카드"* 는 `ExpensesPage.razor:38-41` 에 **이미 있다**(법인카드·현금·개인카드).
*"지출결의"* 는 성격이 다르다 — 앞은 **어떻게 결제했나**, 뒤는 **어떤 절차로 올리나**.
⇒ PM 해석은 **별도 컬럼**이고 사장님 *"옵션컬럼을 붙여"* 와 맞는다. **표시 문구만 대기.**

---

## 7. 별건 이월 (그룹웨어 밖)

| # | 항목 | 근거 |
|---|---|---|
| **B7** | **ERP 결재 7종 제거** | 작9 — ERP 파트 |
| **B8** | `expenses` 에 **반려 사유를 남길 자리가 없다** | 검증리뷰서 §3 |
| **B9** | 🔴 기존 `ApproveExpenseAsync` 가 `approved_by` 에 **테넌트 ID(회사)** | 검증리뷰서 §3 — 감사 추적 불가 |
| B10 | `expense` 블록 `else-if` 사슬 일관성 | P2 |
| B1~B6 | 앞 세션 이월분 | [스케줄](20260821_스케줄_그룹웨어_남은업무.md) §6 |

---

## 8. ⚠️ 다음 세션이 헷갈릴 수 있는 것

- **문서번호 `작10` 이 중복이다** — `20260513작10`(items ALTER)이 이미 있다. 날짜 접두로 구분.
- **급여 메뉴는 이미 있다**(`/hr/payroll`). *"오더 15 에서 빠진 건 실수"* 라는 앞 기록은 **틀렸다.**
- **자동 근퇴는 이미 있었다**(`AuthController.cs:236-258`·`:313`). 다시 만들지 마라.
- **Scan secrets fail 은 기존 119건**이다. 이번 커밋들과 무관(매번 확인함).
- 그룹웨어 사이드바 메뉴는 **15개**다(실측). ④ 에서 정리 대상.

---

## 9. 참조 지도

```
지금 상태          → 본 문서 §0
사장님 결재 원문   → 설계 20260821설5 §0 · 설6 §0
작업 범위          → 작8 · 작9 · 작10
왜 뚫렸나          → 병렬이슈5 · A 개발명세서 §3
무엇을 실측하나    → 본 문서 §5
남은 일정          → 스케줄 20260821 + 본 문서 §4
```
