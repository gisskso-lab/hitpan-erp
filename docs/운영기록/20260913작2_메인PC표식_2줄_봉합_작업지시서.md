# 작업지시서 20260913작2 — 메인PC 표식 2줄 봉합 (재기동 되올림 차단 · 정리 · DB 방어선)

## §0 [카드]

| 항목 | 내용 |
|---|---|
| 작성 | 설계팀장 존 · 2026-09-13 · **초안** |
| **결재** | ⬜ PM 브라운킴 (작성자≠결재자 · 결재 없이 코딩 금지 · #42) |
| 사장님 결재 | ✅ 2026-09-13 — 세 축만 착수 (되올림 차단 · 2줄 정리 · 기존 고객 DB 방어선) · 앞선 반려 「수정안됨. 반려」(작5 §8-1 순서 2) |
| 우선순위 · 규모 | **P0** — 업데이트마다 재발(F4) / **M** — 코드 1 + 마이그 1 + 출하 DDL 1 + 게이트 1 + CI 1줄 |
| 진행 | ⬜[2] PM 결재 → ⬜[3] + ⬜[3-V] + ⬜[4] 동시 → ⬜[5] CTO·사장님 → ⬜게시 전 대본(§6-4) → ⬜[6] |
| 🔴 무효 조건 | **§2 전제 P1~P6 중 하나라도 다르면 이 지시서는 무효** — [3] 착수 금지, [2] 다시 쓴다 |
| 근거 | 설계 [`docs/설계/erp/20260913_설계_메인PC표식_재기동_되올림_차단.md`](../설계/erp/20260913_설계_메인PC표식_재기동_되올림_차단.md) §2·§3·§4·§5 · 선행검증 [A §8](../검증/선행/20260911_선행검증서_슬롯관리전수_A_서버DB.md) · [B §9](../검증/선행/20260911_선행검증서_슬롯관리전수_B_화면집계.md) · 회의록 [§7](20260910_하브루타_메인PC_식별등록_회의록.md) |

**오더 범위 (사장님 결재 문구 — 이것만)**
1. 재기동이 표식을 되올리지 않게 (정당한 목적은 죽이지 않는다)
2. 이미 2줄인 상태를 정리 (사장님 사용 줄 ⓐ 유지 · #1 · 감사기록 보존)
3. DB 안전장치가 없는 기존 고객 PC 에 방어선 투입 (#36 과 충돌 없이)

**넘기는 것 (이번에 안 함)**: 증상 ③ 자료관리 막힘(원인 미확정 B §9-4) · 증상 ① 칩 안 보임(재현 안 됨 B §9-3) · **X5 화면 시각 +9h(P2 별건)** · 2차 2-C 번호 증명 합류 · X4 폐기줄 자물쇠(화면) · 워치독·업데이트(#43) · appsettings(#21)

**갈래**: 1 — `hp-implementer` 1명 · 격리 worktree · 파일 소유표 §5-2
**브랜치**: 🟡 추천 `origin/main` 기준 `fix/20260913-mainpc-mark` (1차 `feat/20260910-mainpc-seat` 와 분리) — **게시 순서는 쟁점 K6**
**게이트**: `MainPcRestartMarkGate` G-M1~G-M7 (§6) — 실제 격리 DB + 출하 DDL + 실제 서비스·실제 마이그 실행 · 무력화 시험 · CI 등록(K4)
**불변**: 되올림 경로 밖 무접촉 · 줄 삭제 0 · `status` 부활 0 · 요금 계수 무이동(PC 4/5 불변) · 운영 DB·5257·5234 접속 0(#39)

**결재 필요 쟁점 (§9)**: K1 트리거 제외 · K2 마이그 1회 · K3 「진짜 줄」 규칙 · K4 게이트 CI 자리 · K5 번호·감사기록 · K6 1차와의 게시 순서

**산출물**: 개발명세서 `docs/개발/erp/20260913_개발명세서_메인PC표식_2줄_봉합.md` · 병렬이슈 `docs/검증/병렬이슈/` · 작업리뷰서 `docs/검증/erp/20260913_작업리뷰서_메인PC표식_2줄_봉합.md`

---

## §1 [오더]

> 사장님 결재 2026-09-13 — 원인 보고(선행검증 A §8 · B §9) 뒤 **세 축**으로 착수:
> ① 재기동이 표식을 되올리지 않게 ② 이미 2줄인 상태를 어떻게 정리하나 ③ DB 안전장치가 없는 기존 고객 PC

**이 아래 모든 내용은 위 세 축을 명문화한 것이다.** 오더에 없는 요구를 넣지 않는다(#33). 해석이 갈리는 곳은 §9 쟁점으로 올린다.
직전 맥락: 1.3.40 게시 후 사장님 확인 순서 2 = 🔴 **「수정안됨. 반려」** (작5 §8-1) — 1.3.40 은 이 결함을 고치는 버전이 **아니었다.** 이 지시서가 그 반려를 받는 트랙이다.

---

## §2 [전제] — 하나라도 다르면 이 지시서는 무효

| # | 전제 | 확인 | 근거 |
|---|---|---|---|
| P1 | 실물에 `is_main_pc=1` 이 **2줄** — ⓐ `HFPv2-a341d087`(승인 · 사장님 사용) · ⓑ `MAINPC-9c1163c`(폐기 · 사유 「…(20260818작4)」) | 🟢 실측 | A §8-1 Q1·Q1b |
| P2 | 진범 = `MainPcRegistrationService.cs:156-164` 의 되올림(`status` 안 봄) · `alreadyHasMain`(`:179-189`)은 신규 INSERT 경로에만 · `BackgroundService` 라 **기동마다 1회** | 🟢 코드 | A §8-2 ②·§8-4 6 |
| P3 | 표식 이동 코드(`TenantDeviceService.cs:269-309`)는 정상 동작 · H1-d **배제** | 🟢 코드·사유문자열 전수 | A §8-2 ① |
| P4 | DB 방어선(`main_pc_key`·`uq_tenant_main_pc`·트리거)이 **실물에 없다** · DB-86·89·103·104 = `clean-ddl` ⇒ 영원히 안 돈다 | 🟢 실측 Q6·Q7·Q8 | A §8-1 · `MigrationRunner.cs:104-108` |
| P5 | 마이그 최대 번호 **119** · 출하 DDL 시드는 **DB-02~119 전부** `clean-ddl` ⇒ `DB-120` 은 번호 충돌 0 이고 기존 고객에게 **실제로 돈다** | 🟢 실측 | `Migrations/SQL/` · `hitpan_db_clean.sql:3487-3502` |
| P6 | `ddl-smoke-test.sh:89-103` 이 **「소스 DB-*.sql 개수 == 시드 success=1 행 수」** 를 자동 대조 ⇒ 파일만 더하면 CI 빨간불 | 🟢 코드 | 설계 §1 F13 |
| P7 | 요금 영향 0 — 폐기 줄은 계수에서 빠진다(화면 PC 4/5 = Q2) | 🟢 실측 | A §8-1 Q2 · B §9-1 |
| P8 | 🟡 **착수 전 재확인 필요** — 8/11 「폐기된 메인PC 로그인 구제」가 코드(`TenantDeviceService.cs:363-366` `&& !isMainPc`)로 살아 있는지 **그 구간 전체 흐름**. K1 판단의 근거 | 🟡 부분 확인(주석·조건문) | 설계 §1 F16 |

---

## §3 [범위]

### 3-1. 하는 것

| # | 무엇 | 어디 |
|---|---|---|
| ① | 되올림 UPDATE 의 `WHERE` 에 **(A) 그 줄이 `approved`** + **(B) 회사에 `is_main_pc=1` 0줄** 을 더한다 — **한 UPDATE 문 안에서**(파생표로 자기 표 읽기 · 별도 선행 `SELECT` 금지) | `MainPcRegistrationService.cs:156-164` |
| ② | 되올리지 않았을 때 `LogWarning` 1줄(이유 + 표식을 든 줄 수) | 같은 파일 `:166-169` 인접 |
| ③ | `DB-120` **①단** — 표식 2줄 이상인 회사에서 `approved` 중 `last_seen_at` 최신 1줄만 남기고 **나머지 `is_main_pc` 만 0 으로**. `approved` 표식 줄이 0개면 **무처리** | `Migrations/SQL/DB-120_main_pc_single_row_guard.sql` (신규) |
| ④ | `DB-120` **②③단** — `main_pc_key` 생성컬럼 + `uq_tenant_main_pc` UNIQUE (멱등 · DB-89 패턴). **①단 뒤에** | 같은 파일 |
| ⑤ | 출하 DDL 본문에 ②③ 을 직접 싣고 시드에 `('DB-120','clean-ddl',1)` 추가 · `is_main_pc` 컬럼 주석 정정 | `installer/hitpan_db_clean.sql:3876-3905`(본문·주석) · `:3502`(시드) |
| ⑥ | 게이트 G-M1~G-M7 + CI 등록 | `Integrity/MainPcRestartMarkGateTests.cs`(신규) · `build-and-test.yml` |

### 3-2. 하지 않는 것 (넣으면 반려)

- 신규 INSERT 경로 ②`alreadyHasMain`·③`INSERT`(`:173-213`) · 지문 생성 · 재시도 루프 · 회사 조회 — **무변경**
- `TenantDeviceService` 표식 이동(`:269-309`)·폐기→대기 회귀(`:366`)·`&& !isMainPc` — **무접촉** (1차 작2 K2 의 몫)
- **DB-89 파일 수정 금지** · DB-89 꼬리 UPDATE(폐기 줄 `approved` 부활) **이식 금지** · 트리거 이식 **금지**(K1 결재 전)
- 줄 `DELETE` · `status`·`revoked_at`·`revoked_reason` 쓰기 · 계수 SQL 에 `AND is_main_pc = 0` (요금 샌다 — `TenantDeviceService.cs:339-340` 경고)
- `DeviceManagePage.razor` · `DeviceAuthMiddleware` · 목록 SQL · X5 시각 표시 · 워치독·업데이트 · appsettings

---

## §4 [헌법]

| # | 자리 | 이 작업에서 |
|---|---|---|
| #1 | 덮어쓰기 금지 | 줄 삭제 0 · 쓰는 컬럼은 `is_main_pc` 하나 · `status`·`revoked_*` 원문 보존 · DB-89 파일 무수정 |
| #9 | DB 는 미리 다 | 방어선을 코드가 아니라 DB 에 둔다 + 출하 DDL 본문에 싣는다 |
| #12 | 인터페이스 확장 시 전수 grep | `is_main_pc` 를 쓰는 자리 전수(계수·목록·미들웨어)에서 **읽기 의미가 바뀌지 않음**을 grep 으로 보인다 |
| #13 | 새 SQL 전 DESCRIBE | `tenant_devices` 실제 컬럼·인덱스 확인 후 SQL 작성 (격리 DB) |
| #17 | 신규 표 InnoDB | 신규 표 0 (컬럼·인덱스만) |
| #19 | 빌드 | errors 0 + warnings 0 |
| #21 | 설정 파일 | appsettings 두 파일 무수정 |
| #23 | 고객 노출 문구 | 새 고객 문구 0. 로그는 내부용 |
| #26 | 마이그 Timeout 분리 | `DB-120` 은 러너 기본 타임아웃(86400s) 그대로 · 마이그 실패로 업데이트가 멈추지 않게 §8 순서 준수 |
| #29 | 🚨 원문 확인 | 서비스·방화벽·레지스트리·cloudflared 무접촉 |
| #33 | 🚨 원문 확인 | 범위 = 세 축 · 갈리는 해석 K1~K6 **결재 전 [3] 착수 금지** |
| #36 | 출하 DDL 단일 진실원 | 시드만 넣고 본문을 빼면 **DB-89 와 같은 죽음** — G-M6 가 잰다 |
| #37 | 수신 캐시 컬럼 제거 금지 | 컬럼 제거 0 |
| #39 | 🚨 원문 확인 | 게이트·재현은 CI 컨테이너 또는 **격리 MariaDB**(별도 포트·데이터 폴더) · 3306 운영·5257·5234 접속 0 · 사장님 PC 는 화면으로만(§6-4) |
| #42 | 문서 | 설계문서 · 이 지시서 · 개발명세서 · 병렬이슈 · 작업리뷰서 |
| #43 | 업데이트 흐름 | 워치독·업데이트 코드 무접촉 · 게시는 기존 루틴(팝업 규칙 무변경) |

---

## §5 [구현]

### 5-1. 순서 (🔴 이 순서가 설계의 심장이다)

1. `origin/main` 기준 브랜치 · 격리 worktree · §2 P8 재확인(⬜이면 PM 보고)
2. **게이트 먼저**(§6) — 봉합 전 코드·DDL 에서 돌려 **FAIL 사유를 개발명세서에 원문 기록**
3. ① 코드(축1) → ② `DB-120` ①단 정리 → ③ `DB-120` ②③단 방어선 → ④ 출하 DDL 본문+시드+주석
   🔴 **축3 을 축1 보다 먼저 넣지 않는다** — 되올림이 UNIQUE 에 부딪혀 매 기동 예외가 난다(설계 §6)
   🔴 **`DB-120` 안에서 정리(①)가 UNIQUE(③)보다 먼저** — 2줄 상태에서 UNIQUE 를 먼저 걸면 마이그 실패 = 고객 업데이트 멈춤
4. 빌드 0/0 · 게이트 PASS · **무력화 시험**(§6-3) · CI 등록(K4)
5. 개발명세서 · 병렬이슈 대응

### 5-2. 파일 소유표 (갈래 1 = `hp-implementer` 1명 전부 소유)

| 파일 | 변경 | 비고 |
|---|---|---|
| `src/HitPan.API/HostedServices/MainPcRegistrationService.cs` | 수정 | **`:156-164` 한 UPDATE + 로그 1줄만.** 그 밖 무변경 |
| `src/HitPan.API/Migrations/SQL/DB-120_main_pc_single_row_guard.sql` | 신규 | 번호 119 다음 · ①정리 → ②컬럼 → ③UNIQUE · 멱등 |
| `installer/hitpan_db_clean.sql` | 수정 3곳 | `tenant_devices` 본문(컬럼+UNIQUE) · `:3895` 주석 정정 · `:3502` 시드 1항 |
| `src/HitPan.Tests/Integrity/MainPcRestartMarkGateTests.cs` | 신규 | `[Collection("DeviceAndKeyGate")]` · 격리 DB · `DbGateEnvironment.SkipOrFail` |
| `.github/workflows/build-and-test.yml` | GATES 1줄 + 등록 근거 주석 (+ `:212` 「물지 않는 것」 줄 정정) | 기존 줄 무수정 · K4 결재 후 |
| `docs/개발/erp/20260913_개발명세서_메인PC표식_2줄_봉합.md` | 신규 | 봉합 전 FAIL 사유 원문 · 무력화 결과 · 격리 실측 |

이 표 밖 파일을 건드려야 하면 **중단 → PM 보고**.

### 5-3. 구현 주의

- (B) 조건은 **UPDATE 한 문장 안**에서 판정한다. MariaDB 는 UPDATE 대상 표를 직접 서브쿼리로 못 읽으므로 **파생표로 감싼다**. 별도 선행 `SELECT` 로 옮기면 검사와 쓰기 사이에 로그인 경로가 끼어든다(TOCTOU).
- `DB-120` ①단의 「최신 1줄」 선택도 **결정적**이어야 한다 — `last_seen_at` 동값·NULL 동률에서 무엇을 고르는지 SQL 이 정해 둔다(사람·랜덤 판정 금지). 게이트에 동값 변형을 넣는다.
- 마이그는 러너가 **Dapper `ExecuteAsync` 한 번**으로 문장들을 보낸다(`MigrationRunner.cs:135-137`) — DB-89 와 같은 `PREPARE`/`EXECUTE` 멱등 패턴을 따르고, 러너에서 실제로 통과하는지를 G-M3 이 **파일을 실행해** 잰다(글자 검사 금지).
- 출하 DDL 은 생성컬럼·트리거 구문을 이미 담고 있다(`:2469`·`:2543-2544`·`:2626-2643`) — 새 형식을 발명하지 말고 그 모양을 따른다.

---

## §6 [게이트] — `MainPcRestartMarkGate` (글자가 아니라 동작)

### 6-1. 환경

- 격리 DB(`hitpan_mainpc_mark_*`) + **출하 DDL 적재** + **실제 `RegisterAsync` 호출 · 실제 `DB-120` 파일 실행**. 반환값을 판정 근거로 쓰지 않고 **표를 읽는다**(선례 `MainPcLocalConsoleGateTests.cs:44-47`).
- 🔴 **옛 고객 모양 재현**: 출하 DDL 에 UNIQUE 가 들어간 뒤에는 2줄을 만들 수 없다 ⇒ 격리 DB 에서 `DROP INDEX uq_tenant_main_pc` 로 DB-120 이전 모양을 만든 뒤 시험한다. **격리 DB 안에서만**(#39).
- DB 없는 로컬 PASS = **SKIP**(통과로 세지 않는다 · `DbGateEnvironment`). CI 는 `HITPAN_REQUIRE_DB` 로 SKIP 금지.

### 6-2. 판정표 (봉합 전 FAIL 사유는 실제 출력 원문으로 채운다)

| # | 동작 | PASS 조건 (봉합 후) | 봉합 전 기대 FAIL | 봉합 전 실제 |
|---|---|---|---|---|
| G-M1 | 옛 모양 · 서버줄 `revoked ∧ is_main_pc=0` + ⓐ `approved ∧ is_main_pc=1` → `RegisterAsync` 1회 | 서버줄 `is_main_pc` **0 유지** · 회사 표식 줄 수 **1** · ⓐ 무변경 | 서버줄이 1 로 되올려져 **표식 2줄**(= 실물 P1 재현) | ⬜ |
| G-M2 | 🟢 **대조군** — 서버줄 `approved ∧ is_main_pc=0` · 회사 표식 **0줄** → `RegisterAsync` 1회 | 서버줄 `is_main_pc` **1 이 된다**(정당한 목적 생존) | 봉합 전에도 PASS — **FAIL 로 바뀌면 조건을 과하게 건 것** | ⬜ |
| G-M3 | 옛 모양에 2줄(approved ⓐ + revoked ⓑ) → **`DB-120` 파일 실제 실행** | ⓑ `is_main_pc=0` · `status='revoked'` **유지** · `revoked_at`·`revoked_reason` **원문 그대로** · ⓐ 무변경 · 줄 수 불변 · 실행 후 `uq_tenant_main_pc` 존재 | 파일 없음/정리 없음 → 2줄 유지 또는 UNIQUE 생성 실패 | ⬜ |
| G-M4 | 🔴 **순서 시험** — 같은 2줄에서 ①정리를 건너뛰고 ②③만 실행 | ~~UNIQUE 생성이 **실패**한다(중복)~~ → **§9-3 대체: ⓐ실패 안 함 ⓑUNIQUE 안 생김 ⓒ감사 1줄** | 역시험 — 실패하지 않으면 순서 보장이 허구 | ⬜ |
| G-M5 | `DB-120` 적용 후 두 번째 줄에 손으로 `is_main_pc=1` UPDATE | **DB 가 거부**(중복 키) — 코드를 되돌려도 2줄 불가 | UNIQUE 없음 → 2줄이 그냥 생긴다 | ⬜ |
| G-M6 | **출하 DDL 만** 적재한 새 DB | `main_pc_key`·`uq_tenant_main_pc` **존재** · 시드에 `DB-120`=`clean-ddl`·success=1 · `ddl-smoke` 게이트 5 통과(파일 수 == 시드 수) | 컬럼·인덱스 0 (= 지금 신규설치 고객 · P4) | ⬜ |
| G-M7 | 🟢 **대조군(요금)** — 정리 전후 `status='approved' ∧ device_type='pc'` 줄 수 | **동일**(4 → 4) · 부활 0 | DB-89 꼬리 UPDATE 를 넣으면 4 → **5** | ⬜ |

변형(같은 게이트에 넣는다): 표식 3줄 · `approved` 2줄 · `last_seen_at` 동값/NULL · `approved` 표식 0줄(**무처리** 확인).

### 6-3. 무력화 시험 (CI 등록 근거 — 「초록불이라서」 금지)

| 되돌린 것 | 빨간불이어야 할 게이트 |
|---|---|
| 되올림 조건 (A) `approved` 제거 | G-M1 |
| 되올림 조건 (B) 「표식 0줄」 제거 | G-M1 |
| `DB-120` ①단(정리) 제거 | G-M3 · G-M4 |
| `DB-120` ③단(UNIQUE) 제거 | G-M5 |
| 출하 DDL 본문 변경 되돌림(시드만 유지 = **DB-89 와 같은 모양**) | G-M6 |
| DB-89 꼬리 UPDATE(부활)를 DB-120 에 넣음 | G-M3 · G-M7 |

**대조군 둘**: G-M2(정당한 목적) · G-M7(요금 무이동). 하나라도 깨지면 **봉합이 과한 것**이다.
CI 주석에 결과를 적고 `GATES="$GATES|MainPcRestartMarkGate"` 1줄 추가 · `:212` 「물지 않는 것」 줄 정정 · CI 로그 `[SKIP]` 0 확인.

### 6-4. 게시 전 사장님 확인 대본 (확인은 사장님 손 · 화면으로만 · #39)

| 순서 | 할 일 | 통과 | 실패 시 |
|---|---|---|---|
| 0 | **업데이트 전** 읽기전용 조회(`scripts/readonly/20260910_mainpc_query.ps1`) + **localhost 창** 기기·슬롯 화면 캡처 — 「회사 서버」 칩 개수 · 상단 PC n/5 · 🔴 도메인 탭 로그인·인증번호 금지(작5 C-1) | 캡처 확보 | 중단 (작5 §8-1 에서 0번이 없어 숫자로 못 쟀다 — 이번엔 먼저 받는다) |
| 1 | 새 버전 업데이트(기존 흐름 · #43) · 로그인 화면 버전 확인 | 새 버전 표시 | 중단 |
| 2 | **localhost 창** 기기·슬롯 화면 재확인 | 🔴 「회사 서버」 칩 **1개**(「Windows · Edge」 줄) · 「회사 서버 (자료 보관 컴퓨터)」 줄은 **폐기 그대로 · 칩 없음** · 상단 **PC 4/5 불변** | 중단 · 보고 |
| 3 | 한 번 더 API 재기동(또는 다음 업데이트) 후 같은 화면 | 칩 **1개 유지**(되올림 0) | 중단 — 축1 미봉합 |
| 4 | 자료관리 진입 | 🟡 **이번 범위 아님** — 열리면 기록만, 막혀도 이 봉합의 실패가 아니다(B §9-4) | 문구 캡처만 (③ 트랙 입력) |

---

## §7 [검증] — SoD (만든 사람 ≠ 검증하는 사람)

**[3-V] 보안상무 안철수 — [3] 도는 중에 동시**
- 되올림 조건이 **권한·신분 축을 새로 만들지 않았나**(이 서비스는 접속 경로를 보지 않는다 — `MainPcRegistrationService.cs:32-41` 축 보존 확인)
- UNIQUE 거부가 **고객 화면 500·기동 실패로 번지지 않나** 실측(`BackgroundService` 라 기동 무방해 `:90-91`)
- 표식을 내린 줄이 **다른 판정(미들웨어·계수·목록)에서 신분을 잃어 새 막힘을 만들지 않나** — 특히 `TenantDeviceService.cs:366` `&& !isMainPc` 경로
- 적발 → `docs/검증/병렬이슈/` 1건 1파일

**[4] 검증팀장 데이비드 박 — [3] 착수와 동시**
- [ ] 빌드 errors 0 + warnings 0 (`dotnet build src/HitPan.sln`)
- [ ] CI 잡에서 `MainPcRestartMarkGate` G-M1~G-M7 PASS · `[SKIP]` 0 · **등록 안 됐으면 반려**
- [ ] 봉합 전 FAIL 사유가 **증상 모양과 맞나** — 「표식 2줄」이 실제로 재현됐나(모양 대조 · 「재현했다고 원인이 아니다」)
- [ ] 무력화 시험 6행 전부 실제 빨간불 기록 · **대조군 2개 초록 유지**
- [ ] 독립 반증 3건 이상: ⓐ 표식 3줄·approved 2줄 ⓑ `DB-120` 을 **두 번** 돌려도 같은 결과(멱등) ⓒ 출하 DDL 신규설치 DB 에서 `ddl-smoke` 게이트 5 통과
- [ ] `is_main_pc` 사용처 재grep(#12) · 계수 SQL 에 `AND is_main_pc = 0` 유입 0 · 소유표 밖 변경 0(`git diff --stat origin/main`)
- 산출 → `docs/검증/erp/20260913_작업리뷰서_메인PC표식_2줄_봉합.md`

---

## §8 [리스크·롤백]

| 리스크 | 확률 | 대응 |
|---|---|---|
| 🔴 축3 만 먼저 들어가 되올림이 UNIQUE 에 부딪힘 → 매 기동 예외 로그 | 중간 | **한 게시에 축1+축2+축3** · §5-1 순서 · G-M4 |
| 2줄 고객에게 UNIQUE 생성 실패 → 업데이트 멈춤 | 중간 | `DB-120` 안에서 정리가 먼저 · 3줄·동값 변형 게이트 · 실패 시 러너가 기록하고 중단(`MigrationRunner.cs:119`) |
| `approved` 표식 0줄 고객은 무처리 → 표식 0줄 유지 | 낮음 | 의도. 다음 기동에 (A)(B) 둘 다 참이면 서버줄에 붙는다 · CS 식별 축은 로그로 드러남 |
| 표식을 내린 줄이 `&& !isMainPc` 구제 밖으로 나감 | 낮음 🟡 | 그 줄은 서버줄(`user_id NULL`·`MAINPC-`)로 브라우저 로그인 경로를 타지 않는다 — **[3-V] 실측 확인 항목** |
| 출하 DDL 수정이 신규설치를 깨뜨림 | 낮음 | `ddl-smoke-test.sh` 전체 + G-M6 · 테이블 수 게이트(125) 불변 확인 |
| 1차 작2 와 같은 파일 충돌 | 낮음 | 소유 파일 겹침 0(1차는 `TenantDeviceService`·인증 축) · `build-and-test.yml` GATES 줄만 근접 → K6 |
| 사장님 PC | — | 이미 DB-120 대상 ⇒ 다음 게시 업데이트에서 **자동 정리** · 직접 수술 0(#39) |

**롤백**: 코드는 되돌림 커밋 + 다음 버전 게시(재설치 아님). 🔴 **DDL 은 파일을 고쳐 되돌리지 않는다** — 새 번호 마이그로만(시드 정합 P6). 표식 내림은 `status`·감사기록이 온전하므로 절차로 복구 가능(화면으로는 못 치운다 — X4 범위 밖 적발).

---

## §9 [쟁점] — PM 결재 (⬜ 비워 둔다 · 필요 시 사장님)

| # | 쟁점 | 추천 | 대안 | 왜 결재인가 | 결재 |
|---|---|---|---|---|---|
| K1 | DB 트리거를 뺄지 | **UNIQUE 만 · 트리거 0** | 방향 뒤집기(`revoked` 면 표식 내림) / DB-89 원본 | 8/11 P0 「메인PC 폐기 시 로그인 막힘」 보호를 무엇이 대신하나. 근거는 `TenantDeviceService.cs:363-366` 이고 **P8 재확인 전제** | ⬜ |
| K2 | 2줄 정리 위치 | **`DB-120` 마이그 1회** | 기동 시 자가정리 | 고객 데이터를 언제·몇 번 만지나. 이번 사고 자체가 「조용한 기동 쓰기」였다 | ⬜ |
| K3 | 「진짜 줄」 규칙 | `approved` 중 `last_seen_at` 최신 · **approved 0개면 무처리** | 서버 지문 줄 우선 / 사람 판정 대기 | 잘못 고르면 사장님이 갇힌다(8/11·8/16·8/18 3회 재발 계통) | ⬜ |
| K4 | 게이트 CI 자리 | `db-gate` 잡에 **기기·승인 계열 축을 연다**(임시 DB 권한 실측 후) | 별 잡 신설 / 등록 보류 | 그 잡이 기기 계열을 **의도적으로 안 문다**(`build-and-test.yml:164-168·212`) ⇒ 범위 확장 성격. **등록 못 하면 안 돈 게이트** | ⬜ |
| K5 | 번호·감사기록 | `DB-120_main_pc_single_row_guard.sql` · 감사표 기록 없음(마이그 기록·로그로) | 다른 번호 / `audit_logs` 1줄 | 회의록 §7-3 2차 항목이 「DB-120 조건부 재정합」으로 같은 번호를 이름처럼 쓴다 — **내용은 다르다** | ⬜ |
| K6 | 1차 작2 와 게시 순서·브랜치 | `origin/main` 기준 별 브랜치 · **1차보다 먼저 게시** (P0 · 재발이 매 업데이트) | 1차 브랜치에 합류 / 1차 뒤 | 1차 K2 가 표식 이동 코드를 철회하면 2줄이 **새로 생기지는** 않으나, 이미 생긴 2줄과 되올림은 그대로 남는다 | ⬜ |

### §9-1 [PM 결재] — 2026-09-13 · 브라운킴 (작성자 ≠ 결재자)

**판정: 조건부 결재** — [3] 착수 가능. 단 아래 조건 5개가 빠지면 [4] 반려.

| # | 결재 | 근거 · 조건 |
|---|---|---|
| **K1** | 🟢 **승인** — UNIQUE 만 · 트리거 0 | **P8 을 PM 이 실측해 해소**: `TenantDeviceService.cs:366` = `if (status == "revoked" && !isMainPc)` ⇒ 표식 있는 메인PC 는 폐기여도 이 차단 분기를 **빠져나간다** · `:408` = `if (status == "rejected" && isMainPc)` 가 반려에서 `approved` 로 **자가 회복**시킨다 ⇒ 8/11 보호는 **트리거가 아니라 코드에 실재**한다. §2 P8 🟡→🟢. 🔴 **조건 C-1**: 축2가 폐기 서버줄의 표식을 0으로 내리는 순간 **그 줄이 `:366` 분기에 들어간다**(revoked ∧ !isMainPc). 그 줄이 `MAINPC-` 서버 지문·`user_id NULL` 이라 브라우저 로그인 경로를 타지 않는다는 것을 [3-V] 가 **실측**해야 한다(§8 리스크 4행·§7 [3-V] 3항에 이미 있음 — 빠지면 반려) |
| **K2** | 🟢 **승인** — `DB-120` 마이그 1회 | 이번 사고 자체가 「조용한 기동 쓰기」였다. 같은 성질의 자리를 하나 더 만들지 않는다 |
| **K3** | 🟢 **승인** — `approved` 중 `last_seen_at` 최신 · approved 0개면 무처리 | 🔴 **PM 추가 반증**: `last_seen_at` 은 **되올림 UPDATE 가 `NOW()` 로 덮는 값**이다 — 실물 ⓑ 의 `9/11 23:05` 이 바로 그 흔적(A §8-2 ③). 따라서 **폐기 줄이 「최신」이 될 수 있다.** 규칙이 `approved` 로 **먼저** 거르므로 안전하지만, 이 순서가 뒤집히면 사장님이 갇힌다. 🔴 **조건 C-2**: 게이트에 **「폐기 줄의 `last_seen_at` 이 승인 줄보다 최신인」 변형**을 넣어 그래도 ⓐ 가 선택되는지 잰다(§6-2 변형 목록에 추가) |
| **K4** | 🟡 **조건부** — 실측 후 등록 | 🔴 **조건 C-3**: `db-gate` 잡 `root` 로 `CREATE DATABASE` 가 되는지 **실측 원문**을 개발명세서에 남기고 `GATES` 에 등록한다. 실패하면 별 잡 신설은 **범위 확장**이므로 **PM 재결재**. **등록 안 된 게이트는 안 돈 게이트** — 등록 없이 [4] 통과 금지 |
| **K5** | 🟡 **번호 유지 · 단 「흡수」를 명시** (반려 아님) | PM 실측: 마이그 파일 최대 **119** · 출하 DDL 시드 최대 **119** · 양쪽 `DB-120` **0건** ⇒ 번호는 비어 있다(P5·P6 🟢). **그러나** 회의록이 그 번호를 2차 항목으로 **이미 이름 지어 뒀다** — §7-3 2번 「DB-120 조건부 재정합」 · **사장님 결재 5** 「폐기된 옛 메인 줄 자동 부활 안 함」 · [CTO-2] 초안. 🔴 **새 번호를 따면 오히려 두 갈래가 생겨 D4 사고**가 난다: `MigrationRunner.cs:91-94·104` 가 `migration_id` **문자열만** 보고 skip 하고 **checksum 은 판정에 안 쓴다**(`:130` 계산·기록만) ⇒ **같은 번호 다른 내용이면 먼저 성공한 쪽만 돌고 나머지는 영구 skip** · `:143-162` 는 실패 시 `return` 이라 **이후 마이그 전부 중단**(회의록 P0-2 실물 확인). ⇒ **판정: `DB-120` 유지.** 단 지시서에 **「이 작업이 회의록 §7-3 2번·결재 5 를 흡수한다」**를 명시하고, **DB-121 은 「탑승비밀」로 이름이 나가 있으므로 침범 금지**를 적는다. 🔴 **조건 C-4**: [CTO-2] 초안 ②(**중복 0 일 때만 UNIQUE**)와 이번 설계(**①정리로 중복을 없앤 뒤 ③UNIQUE**)는 **접근이 다르다** — 어느 쪽이 2줄 고객에게 안전한지는 **[5] CTO 래리 결재 안건으로 올린다**(게시 후 수정 불가이므로 이 판단은 되돌릴 수 없다) |
| **K6** | 🟢 **승인** — `origin/main` 기준 별 브랜치 · 1차보다 먼저 게시 | P0 · 매 업데이트 재발 · 소유 파일 겹침 0 |

**🔴 조건 C-5 (전 갈래 공통)**: §6-4 대본 **순서 0 선(先)캡처**를 받지 못하면 게시하지 않는다. 지난번(작5 §8-1 순서 2)에 이것이 없어 「줄 수·표식 동일」을 숫자로 재지 못했다.

**PM 이 반증한 것 (통과)**: ① 되올림 조건 (A)(B) 가 F5 정당 목적을 죽이지 않는가 → §2-2 표 4행이 네 경우를 다 덮는다 ② 요금 이동 0 → 계수는 `approved` 로 세고 정리는 `is_main_pc` 만 건드린다(G-M7 대조군) ③ 삭제·부활 0 → #1 정합 ④ 대조군 2개(G-M2·G-M7)가 「봉합이 과한지」를 잰다 — 게이트가 글자가 아니라 동작이다.

**PM 이 결재하지 않은 것**: 증상 ③·① · X5 · X4 — 범위 밖(§0 「넘기는 것」 그대로).

### §9-2 [CTO 판정 접수 + PM 반증] — 2026-09-13

**접수**: [5] CTO 래리 1차 결재 → [`20260913결1`](20260913결1_메인PC표식_V4_마이그정지_CTO결재.md) · 🟡 **조건부 결재 — (가) ③단 조건부 UNIQUE 채택**([CTO-2] 초안 ②③ 복원) · (나)·(다) **반려** · 게시 전 **D-1~D-4** + **사장님 결재 1건**.

| 조건 | 요지 |
|---|---|
| **D-1** | ①단 뒤에도 `is_main_pc=1` 이 2줄 이상인 테넌트가 **하나라도** 있으면 UNIQUE 를 만들지 않는다 — 판정은 **DB 전체**로(UNIQUE 가 표 전체 제약이므로) |
| **D-2** | ②단 생성컬럼 `main_pc_key` 는 **무조건 유지**(무해 · 다음 재정합 발판) |
| **D-3** | 건너뛴 경우 `audit_trail` 에 **1줄**(멱등) — 「누가 다시 보나」의 답이 그 고객 DB 안에 |
| **D-4** | **V4 단언 무수정** + 조이는 단언 추가 · G-M5·V5 초록 유지 · 무력화 ④ 재실측 |

**PM 반증 (받아쓰지 않는다 · #32)**

| # | CTO 가 뒤집은 것 | PM 판정 |
|---|---|---|
| 1 | 「게시 후 수정 불가」를 **현재 제약처럼** 다룬 내 C-4 문구 — `DB-120` 은 `origin/main` 에 없고(최대 DB-119) **지금 고치는 비용은 0** | 🟢 **수용 · 내 오기.** 다만 「게시하면 못 고친다」는 **여전히 유효한 미래 제약**이며, 그래서 게시 전에 정하라는 뜻이지 판단이 가벼워진 것은 아니다 |
| 2 | 남는 위험 2 — 「같은 DB 두 번째 회사가 더러우면 정상 회사도 UNIQUE 누락」 | 🔴 **PM 이 반증을 시도했고 실패했다.** 실측: `C:\ProgramData\HitPan\registry.json` **tenants 1개**(`hitpan_erp_t004`) · `local_company` INSERT 경로는 `CompanyBootstrapProvisioner.cs:81` **한 곳**. ⇒ 실물은 단일 테넌트다. **그러나 행이 2개 생기는 것을 코드가 물리적으로 막지 않는다** — 「실물이 1개」와 「구조가 1개를 보장」은 다르다. ⇒ **D-1(DB 전체 판정)이 옳다.** 내 예상(해당 없음)을 철회한다 |
| 3 | 설계 §6 리스크 3행 *「UNIQUE 는 걸린다(중복 없음)」* 이 **사실오류**이고, 그것이 내 K3 승인에서 충돌을 가린 단일 원인 | 🟢 **수용.** 그 줄은 「approved 표식 0줄」과 「표식 0줄」을 혼동했다. 표식은 **2줄로 남는다**. 내가 §9-1 에서 K3 를 승인하며 이 한 줄을 걸러내지 못했다 — **PM 반증 실패로 기록한다** |
| 4 | K3 명분 「사람이 판정할 자리로 남긴다」의 **그 자리가 화면에 없다**(X4 · `DeviceManagePage.razor:198-204` 자물쇠) | 🟢 **수용.** X4 를 부수 적발로만 두고 K3 근거와 연결하지 못했다. K3 는 유지하되 **근거는 2차까지 공백** — 사장님 안건의 「대가」에 그대로 적힌 것이 맞다 |
| 5 | 요금 무이동 독립 확인(`TenantDeviceService.cs:1699-1702` 계수 SQL 에 `is_main_pc` 절 없음) | 🟢 내 §9-1 반증 ②와 **같은 결론에 독립 경로로 도달** — 교차 확인 성립 |

**PM 이 추가로 남기는 것 (CTO 남는 위험 3 관련)**
- **F-5**(옛 백업 복원이 `tenant_devices` 를 통째 덮음)는 UNIQUE 가 생긴 뒤 **복원 거부**로 번질 수 있다 — CTO 적발대로 **이번 게시가 그 위험을 키운다**. 🟢 다만 **복원은 지금 막혀 있다**(작5 · 「복원 잠시 막기」 사장님 결재) ⇒ **당장 발현하지 않는다.** 🔴 **복원을 다시 여는 설계에서 이 항목을 반드시 함께 본다** — 그 트랙의 필수 입력으로 남긴다.

**다음 순서 (🔴 착수 금지 선)**
1. **사장님 결재(결1 §5)** — (가)/(나) 선택은 사장님 안건이다. **결재 전 [3] 재작업 착수 금지** — (나)를 고르시면 재작업이 헛일이 된다.
2. 결재 후 (가)로 `DB-120` 수정 → D-1~D-4 실측 → 개발명세서 §7 갱신 → [4] 재확인(V4 가 **단언 무수정으로** 초록이 되는지 · G-M5·V5 초록 유지) → K4 CI 등록 · C-1 · C-5 → [5] → 게시.

### §9-3 [G-M4 기대값 변경 결재] — 2026-09-13 · 사장님 결재 (PM 브라운킴 기안)

**배경**: 2차(`5a46b269`)에서 G-M4 가 🔴 빨간불. 원래 단언 = 「①단(정리)을 건너뛰고 ②③단만 돌리면 UNIQUE 생성이 **실패한다**」. (가) 채택으로 ③단이 중복을 보고 **스스로 건너뛰므로** 실패하지 않는다. 결1 §4 는 V4 만 다뤘고 **G-M4 칸이 비어 있었다(계약의 빈칸)**. 구현자는 고치지 않고 빨간불로 올렸다(개발명세서 §7-2 — 옳은 판단).

**판정 (사장님 결재 · A안)**: 단언을 무르는 것이 아니라 **재는 대상이 바뀐 것**이다. (가)에서 「정리가 먼저여야 한다」는 순서 의존이 사라졌고, 대신 **「③단이 ①단의 성공에 기대지 않는다」**를 잰다.

| 구분 | 내용 |
|---|---|
| 새 질문 | **①단을 건너뛰어 중복이 남아도** → ⓐ 마이그가 **실패하지 않는다** ⓑ 방어선(`uq_tenant_main_pc`)이 **생기지 않는다** ⓒ `audit_trail` `db120_mainpc_dup` 기록이 **1줄** 남는다 |
| 🔴 조건 | **ⓐⓑⓒ 세 단언을 한 시험에 전부** 건다. 하나라도 빠지면 이전보다 약한 시험이므로 **반려** (ⓐ만 보면 「UNIQUE 를 걸다 조용히 실패」를, ⓑ만 보면 「기록 누락」을 놓친다) |
| 추가 유지 | 표식 2줄 그대로 · 폐기 줄 `status`·`revoked_reason` 원문 그대로 · `main_pc_key` 컬럼 존재(D-2) |
| V4 와의 차이 | V4 = **①단이 규칙대로 무처리**한 모양(승인 표식 0줄). G-M4 = **①단 자체가 없어도**(승인 1 + 폐기 1 · 원래 ①단이 정리했을 모양) ③단이 스스로 지킨다 ⇒ **겹치지 않는다** |
| 무력화 의무 | ⅰ ③단 조건에서 `@dup_tenants = 0` 제거 → G-M4 **ⓐ에서 FAIL** ⅱ ④단(감사기록) 제거 → G-M4 **ⓒ에서 FAIL** · 둘 다 원복 후 **바이트 동일**(sha256) — 개발명세서에 원문 기록 |
| 불변 | V4 단언 무수정 · G-M5·V5 초록 유지 · 판정표 §6-2 G-M4 행의 옛 기대값은 **지우지 않고 대체 표시** |

§6-2 G-M4 행 **대체**: ~~UNIQUE 생성이 실패한다(중복)~~ → **§9-3 ⓐⓑⓒ** (2026-09-13 사장님 결재).

### §9-4 [병렬이슈33 봉합 방향 · 범위 확장 결재] — 2026-09-14 · 사장님 결재 (PM 브라운킴 기안)

**배경**: [4] 작업리뷰서 `docs/검증/erp/20260913_작업리뷰서_메인PC표식_2줄_봉합.md` **반려** — 조건 C-1 전제가 실측으로 깨졌다([병렬이슈33](../검증/병렬이슈/20260914_병렬이슈33_표식내린_폐기서버줄을_합류한_화면이_다시_세우고_사장님줄을_폐기한다.md)). `DB-120` ①단이 폐기 서버줄의 표식을 0 으로 내리면, 합류 화면(`DeviceAuthGate.razor` [회사서버 컴퓨터] · 장비넘버 = 서버줄)의 로그인이 `TenantDeviceService.cs:269`(localhost → 서버줄 부활 · 사장님 줄 폐기 · 핑퐁) · `:366`(원격 → 대기 전환 · 사유 소거)을 탄다.

**판정 (사장님 결재 · (a))**: 로그인 코드에서 **서버가 만든 줄(지문 `MAINPC-`)이면 부활·대기 전환을 하지 않고 거부만** 한다. (b) DB-120 이 기기 비밀을 지우는 안은 **반려**(DB-120 이 만진 줄만 막고 8/18 모양의 옛 줄은 열린 채).

| 구분 | 내용 |
|---|---|
| 🔴 범위 확장 | §3-2 「`TenantDeviceService` 무접촉」을 **이 한 자리에 한해 푼다** — `RegisterOrRefreshAsync` 기존 기기 분기 입구의 **가드 추가만**(#1 · 기존 갈래 무수정 · `&& !isMainPc` 유지) |
| 가드 ① | 서버줄 ∧ `is_main_pc=0` ∧ `status ∈ {revoked, rejected}` → **표 쓰기 0 · 거부**(`:366` 대기 전환 · `:269` 표식 이전 · 1-4 재신청 모두 안 탄다) |
| 가드 ② (PM 해석 · 사장님 확인 요청) | 서버줄 ∧ `is_main_pc=0` ∧ `approved` ∧ localhost → **표식 이전(`:269`)만 건너뛴다**(로그인은 종전대로). ①단이 승인 서버줄의 표식을 내린 모양(V1)에서 같은 핑퐁이 난다 — 원칙 「서버가 만든 줄은 브라우저 로그인으로 표식·상태가 안 바뀐다」의 같은 적용 |
| 불변 | 표식 든 서버줄(`is_main_pc=1`)의 8/11 구제(`:366` 통과) · DP-2 반려 자가회복(`:408`) — **무변경** · 브라우저 줄(`HFPv2-`) 경로 무변경 · 요금 계수 무변경 |
| 게이트 (G-M8 계열 · 실제 `TenantDeviceService`) | ⓐ DB-120 적용 **전** 대조군: 표 무변화 ⓑ 적용 후 localhost·원격: 거부 + **표 무변화**(status·is_main_pc·revoked_reason 원문) ⓒ 사장님 줄 로그인 이어서 → **핑퐁 0** ⓓ 승인 서버줄(표식0) localhost 로그인 → 표식 이전 0 · 사장님 줄 유지 ⓔ 대조군: 표식 든 서버줄 폐기 상태 로그인 = 종전(8/11 구제) |
| 무력화 의무 | 가드 ① 제거 → ⓑⓒ FAIL · 가드 ② 제거 → ⓓ FAIL · 원복 바이트 동일 |
| 함께 봉합 | [4] P2 — `DB-120` ④단 `reason` 고객 노출 문구에서 개발·내부 용어 제거 · [4] 추정 P2 — V5·G-M3 에 「감사기록 0줄」 단언 추가 · M4·M5 재실측 |

### §9-5 [병렬이슈34 ②③ 이번 게시에 포함 · 게시 지시] — 2026-09-14 · 사장님 지시 「남은것 까지 다 작업하고 최종 게시해」

**배경**: [4] 재검증 `e72f0c10` 🟡 조건부 통과 — ① 반려 갈래 게이트(✅ `e76510ef` G-M8e) · ② 거부 문구 미도달·대표 알림 반복 · ③ 표식 없는 서버줄 승인 시 슬롯 +1. PM 은 ②③ 별건을 추천했으나 **사장님이 이번 게시에 포함을 지시.**

| # | 봉합 | 자리 |
|---|---|---|
| ② | 가드 거부 문구를 공용 상수(`DeviceMessages.StaleServerRow`)로 · **AuthController 가 그 사유면 대표 `device_pending` 알림을 보내지 않는다** · `gate-status` 가 `staleServerRow` 를 실어 **관문이 전용 안내**를 띄우고 [이 컴퓨터 승인]을 숨긴다(합류 버튼만) | `DeviceDtos.cs` · `ITenantDeviceService`(+1 메서드 · 구현체 1 · #12 grep) · `TenantDeviceService.cs` · `AuthController.cs` · `DeviceController.cs` · `DeviceAuthGate.razor` |
| ③ | `ApproveAsync` — 서버줄(`MAINPC-`) ∧ 표식0 이면 **승인 거부**(대기·반려 모두) · 슬롯 무이동 | `TenantDeviceService.cs` |
| 게이트 | **G-M8f** 대기·반려 서버줄 승인 → 예외 · 승인 PC 수 불변 / **G-M8g 대조군** 일반 대기 브라우저 줄 승인 → 성공 / **G-M8h** `IsServerRowWithoutMarkAsync` 판정(서버줄 표식0 참 · 표식1 거짓 · 브라우저 줄 거짓) | `MainPcRestartMarkGateTests.cs` |
| 무력화 | ③ 승인 가드 제거 → G-M8f · 판정 메서드 항상 거짓 → G-M8h·G-M8b | |
| ⚠️ 미실측으로 남김 | 관문 화면 문구·버튼 숨김·알림 미발송은 **브라우저·컨트롤러 종단 시험 없음** — 게시 후 사장님 화면 확인 대본에 넣지 않는다(실물에 해당 화면이 있는지 모름 · `auth_key_hash` 조회 미확보) | |

**게시**: 봉합 → 빌드 0/0 · 게이트 · 무력화 → [4] 재확인 → [5] CTO 체크리스트(메인 · 규모 M) → **단일 커밋 PR**(TruffleHog 이력 함정) → CI 전 체크 초록 → 머지 → `deploy-update` dispatch `1.3.41` · Major · production · **requiresMigration=true**(DB-120 신규) → manifest 확인 → 사장님 업데이트 뒤 §6-4 순서 1~3.

### §9-6 [게시 기록] — 2026-09-14
- PR #398 단일 커밋 → main `57010118` (CI 전 체크 초록 · 브랜치 push 비밀검사·ZAP 은 종전 판정 제외) · CTO 최종 `20260914결1`
- `deploy-update` run 34857820112 — 1회차 `archive.mariadb.org` 504 로 빌드 실패(게시 skipped · 고객 영향 0) → 2회차 빌드 성공 → 사장님 production 승인 → **서명·NCP 게시 성공**
- **manifest `1.3.41` · Major · requiresMigration=true · releasedAt 2026-09-14T14:58:05Z** · 패키지 URL 200
- 부수: PR #400 번들 다운로드 재시도(최대 4회) 머지 — 다음 게시부터
- ⬜ 남은 것: 사장님 PC §6-4 순서 1~3 (팝업 [예] · 버전 1.3.41 · 칩 1개 · 폐기 줄 칩 없음 · PC 4/5 · 재기동 뒤 유지) · 실물 서버줄 `auth_key_hash` 조회

**C-5 순서 0 (부분 확보 · 2026-09-14 사장님 화면 캡처 · 1.3.40 · localhost)**: PC **4/5** · 모바일 1/3 · 6줄 — `Windows·Edge`(9/10 · 승인 · **회사 서버 칩**) · `회사 서버 (자료 보관 컴퓨터)`(8/21 · **폐기 · 회사 서버 칩** · 최근 접속 9/12 08:05) · `Windows·Chrome`(8/29 승인) · `Android·Chrome`(8/25 승인) · `Windows·Edge`(8/25 승인) · `Windows·Edge`(8/21 승인). ⇒ **「회사 서버」 칩 2개 = P1 증상 그대로.** ⬜ 읽기전용 조회 txt 미확보 · ⬜ 실물 서버줄 `auth_key_hash` 유무 미확인(PM 직접 조회가 도구 권한에서 차단됨).

---

**확인 필요 (결재 아님 · 기록)**
- 증상 ③ 자료관리 막힘은 이 세 축으로 **설명되지 않는다**(B §9-4) — 봉합 뒤에도 남을 수 있다. **「고쳤다」로 보고하지 않는다.**
  - 🔴 **갱신 (사장님 확인 2026-09-13)**: 「자료관리 화면 안막혀」 ⇒ **현재 재현되지 않는다**(B [§9-5](../검증/선행/20260911_선행검증서_슬롯관리전수_B_화면집계.md)). 그 사이 1.3.40 업데이트(API 재기동)·자료 초기화가 있었고 **어느 것이 상태를 바꿨는지 재지 않았다** — 우리가 고친 것이 아니므로 **해결로 세지 않는다.** 이 지시서 범위 밖 유지 · 재발하면 **문구 원문 + 메뉴**부터 받는다(K5 · 문구가 코드에 없는 말일 수 있다 X3).
- X5 화면 시각 +9h(B §9-2)는 **P2 별건**. 시각 대조는 DB 값으로 한다.
- 사장님 오더 증상 ①(칩 0개)은 현재 재현되지 않는다(B §9-3) — 범위에 넣지 않았다.

---

## §10 [완료]

① §3-1 ①~⑥ 구현 · 빌드 0/0 ② §6 게이트 G-M1~G-M7 PASS + 무력화 6행 실측 + **CI 등록** + `[SKIP]` 0 ③ [3-V] 적발 0 또는 봉합 · [4] 통과 ④ 개발명세서·병렬이슈·작업리뷰서 ⑤ [5] CTO 래리 → 사장님 결재 ⑥ §6-4 대본 **순서 0 캡처를 먼저 받고** 2·3 통과 ⑦ [6] 커밋·게시.
**하나라도 빠지면 완료 아님.** 특히 ②의 **CI 등록**과 ⑥의 **순서 0 선(先)캡처** — 지난번엔 이 둘이 없어 「줄 수·표식 동일」을 숫자로 못 쟀다(작5 §8-1 순서 2).
