# 작업지시서 20260824작2 — 그룹웨어 메뉴 정리 4건

| 항목 | 내용 |
|---|---|
| 작성 | PM 브라운킴 · 2026-08-24 |
| **결재** | ✅사장님 직접 지시 + 착수 전 2건 결재 |
| 우선순위 | **P1** |
| 진행 | ✅[3]개발 ⟷ ✅[3-V] → ⬜[4]검증 → ⬜[5]결재 → ⬜[6]커밋 |

---

## 0. ⭐ 사장님 오더 (원문)

> *"메뉴 더 정리 하자."*
> *"1. 휴가연차+연차신청 통합"*
> *"2. 메신저 그룹웨어 메뉴에서 삭제, 아이콘으로 동작제어."*
> *"3. 연차부여 삭제 = 사원관리에 연차부여 넣기 = 권한에 연차부여 권한 추가."*
> *"4. 전자근로계약서 = 입사/퇴사 로 메뉴변경 전자근로계약서 작성, 전자 퇴직서 작성"*

### 착수 전 여쭌 것 — 결재 완료

| 물음 | 사장님 결재 |
|---|---|
| 퇴직서를 오늘 만드나 | 🔴 **"퇴직서까지 오늘 다 만든다"** |
| 연차부여를 어떤 모양으로 | ✅ **사원관리 안 탭으로** |
| 초과근무 메뉴 | ✅ **별도 메뉴 유지** |

⚠️ **메뉴 순서 변경은 이 작업에 넣지 않았다** — 사장님이 *"지금 작성중"* 이라 하셨다.
별건으로 받는다.

---

## 1. 무엇을 했는가

### [1] 휴가·연차 + 연차신청 통합

- [x] `HrLeavePage` 첫 탭 **「내 연차」** 신설 — 옛 `/hr/leave-request` 가 하던 그대로
- [x] 🔴 **탭 인덱스가 한 칸씩 밀렸다** — 옛 주소 매핑도 같이 옮겼다
      (안 옮기면 `/hr/leave-status` 가 엉뚱한 탭을 연다)
- [x] 🔴 `/hr/leave-request` **주소 생존** — 결재관리 [결재 올리기] 안내가 그리로 보낸다
- [x] 옛 화면 파일·내용 그대로. `@page` 한 줄만 뗐다 (헌법 #1)

**왜 합쳤나**: 두 화면이 **같은 KPI(총·사용·잔여 연차)를 각각** 보여주고 있었다.
직원은 내 잔여를 보러 갔다가 남들 것을 보려면 또 다른 화면으로 갔다.

### [2] 메신저 메뉴 삭제

- [x] 사이드바 줄 제거
- [x] 🔴 **실측 확인** — 상단바 `TopHeader.razor:26` `<ChatButton>` 이 **실제로 있다**
      (알림 아이콘 옆 · 사장님이 8/13 에 지정하신 자리)
- [x] `/chat` 주소 생존

⚠️ 아이콘이 없는데 메뉴만 지웠으면 **들어갈 길이 사라진다.** 그래서 먼저 실측했다.

### [3] 연차부여 → 사원관리 탭 + 권한 신설

- [x] 사원관리를 **탭 2개**로 — [사원 목록] · [연차 부여]
- [x] 🔴 **복사가 아니라 호출** — `<AnnualLeaveGrantPage />` 컴포넌트로 얹었다(373줄)
      복사하면 두 벌이 되고 한쪽만 고쳐지는 날이 온다. **연차는 숫자가 갈리면 사고다**
- [x] 🔴 **`ANNUAL_LEAVE_GRANT` 권한 신설** (기본 OFF, 헌법 #11)
- [x] 🔴 **`TenantAdminOnly` → `TenantOnly` + 권한** — 종전엔 **대표만** 연차를 줄 수 있었다
- [x] 🔴 **`confirm` 만 `update`** — 보는 것과 주는 것을 가른다(연차는 돈이다)
- [x] `/hr/annual-leave` 주소 생존

### [4] 전자근로계약서 → 입사/퇴사 + 전자 퇴직서 **신규**

- [x] 메뉴·화면 제목 **「입사/퇴사」**
- [x] 탭 3개 — [계약서] · **[전자 퇴직서]** · [서명 이력]
- [x] 🔴 **`resignation_letters` 표 신설** (DB-100)
- [x] `ResignationService` · `ResignationController` · `ResignationPanel.razor` 신규
- [x] 🔴 **`RESIGNATION` 권한 신설**
- [x] 🔴 **결재 배선** — `doc_type='resignation'` 은 ①에서 등재만 돼 있었다. 트리거를 붙였다
- [x] `/hr/labor-contracts` 주소 그대로

---

## 2. 🔴 안 건드린 것

| 대상 | 이유 |
|---|---|
| **관리자 퇴사 처리** (`EmployeeResignDialog`·`ResignAsync`) | **이미 멀쩡하다.** 사직서가 수리되면 **그것을 부른다** — 다시 구현하지 않는다 |
| 옛 화면 3개 (연차신청·연차부여 등) | `@page` 한 줄만 뗐다. 파일·내용 그대로 (헌법 #1) |
| 옛 주소 전부 | 살아 있다 — 즐겨찾기·메신저 링크 404 금지 |
| `labor_contracts` 표 | 무접촉. 사직서는 **별도 표**다(담는 칸이 다르다) |

---

## 3. [3-V] 동시검증 결과

| # | 재는 것 | 결과 |
|---|---|---|
| V-1 | 빌드 errors 0 + warnings 0 | 🟢 **PASS** |
| V-2 | 🔴 **DB-100 실제 실행** | 🟢 **PASS** — 표 생성 확인 |
| V-3 | 🔴 조회 SQL 컬럼 정합 | 🟢 **PASS** — 실제 DB 에 돌렸다 |
| V-4 | 🔴 INSERT · UPDATE 4종 | 🟢 **PASS** — 한글 정상 |
| V-5 | 🔴 **권한 메뉴 정합** (CI 그것) | 🟢 **PASS** — 23개 일치 |
| V-6 | 🔴 메신저 아이콘 실재 | 🟢 **PASS** — `TopHeader.razor:26` |

### 🔴 [3-V] 가 적발한 것 2건 — 둘 다 봉합

**① `menu_master` 라는 표는 없다**
DDL 에 권한 메뉴 `INSERT` 를 쓸 뻔했다. 실측하니 **그런 표가 없고**,
권한 목록은 **코드 두 곳**(`MenuList` · `ErpMenus`)이 진실원이었다.
⇒ 그대로 뒀으면 **마이그레이션이 통째로 실패**했다. 헌법 #13 이 막았다.

**② `HasPermissionAsync` 인자 순서가 `(userId, tenantId)` 다**
반대로 썼다. 그대로 뒀으면 **권한이 있는 사람도 "없다" 로 판정**된다 —
빌드는 통과한다(둘 다 string).

---

## 4. ⚪ 못 잰 것 — 정직 기록

| # | 이유 |
|---|---|
| 화면 실제 동작 | 게시 후 사장님 확인 자리 |
| 사직서 결재 종단 | `resignation` 결재 설정을 켜고 실제로 돌려야 한다 (게시 후) |
| 권한 켜고 끄기 | 계정 2개 필요 — 터널 실측 대기 |

🔴 **게시 전에는 [5] 상신하지 않는다.**
