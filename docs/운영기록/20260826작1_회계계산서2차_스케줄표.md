# 20260826작1 — 8/26 작업 스케줄표 (매입 2차 마무리 · 회계 2차 · 계산서 2차)

> **[1] 사장 오더** 2026-08-26 · **작성** PM 브라운킴 · **결재** CTO 래리 → 사장님
> 목적: **업무가 다른 데로 새지 않게** 트랙을 고정한다. 이 표 밖의 일은 하지 않는다.

## 🔴 대원칙 — 이번 차수 범위 밖 금지

- 작업 중인 대메뉴 영역 밖은 **묻지도 않는다** (8/25 사장님 지적: 매입 하다 판매 갈아엎음)
- **전수조사 결과로 나온 것도 즉시 고치지 않는다** — 조사서에 적고 결재받는다
- 트랙 A(매입)가 안 끝나면 트랙 B로 안 넘어간다. **동시 착수 금지**

---

## 트랙 A — 매입파트 2차 마무리 (최우선 · 8/26 실측 반려 4건)

> 근거: 8/26 사장님 실측 = 매입 **1/5 통과**. 상세 → `project_purchase_return_list_500_20260826`

| 순 | 건 | 실측 증상 | 판정 |
|---|---|---|---|
| A-1 | 🔴 **매입반품 목록 500** | `[작18] 매입반품 목록 조회 실패 · 500` | 반려 |
| A-2 | 확정 불가 | 목록이 안 떠서 **눌러볼 수조차 없음** | 반려(A-1 종속) |
| A-3 | 반품현황 공백 | 4탭 전부 안 뜸 | 반려(A-1 종속) |
| A-4 | **담당자 열 미반영** | 발주서·매입처리·반품처리 그리드 | 반려(별건) |
| — | 조회유형 무관 날짜조회 | 5화면 | 🟢 **통과** |

### A-1 착수 순서 — 🔴 추측 금지, 이 순서 그대로

1. **DB-110 마이그가 실제 적용됐는지부터 본다.** 1.3.19 는 `requiresMigration:true` 로 나갔다.
   8/25 반품확정 500 을 **다섯 번** 틀린 진범이 `Unknown column 'is_loss'` = **마이그 미적용**이었다.
2. **서버 로그를 본다.** 브라우저 콘솔은 "500"까지만 말한다 — 어느 컬럼·어느 SQL인지 **안 알려준다**.
3. 🔴 **증상 모양 대조** — 재현한 게 사장님이 받은 500 과 같은 모양인지 확인. 다르면 진범이 아니다.
4. 원인 1개가 A-1·2·3 을 **다 풀었는지 각각 확인**. 하나 고치고 나머지를 "당연히 됐겠지" 로 넘기지 않는다.

> 🔴 **핵심 단서 — ②번이 알려준 것**
> 매입목록 반품전환 시도 → `400` *"이미 반품 전환된 매입명세서입니다. (매반-20260825-001)"*
> ⇒ **반품서는 DB 에 실물로 있다. 쓰기는 성공했고 읽기가 죽었다.**
> ⇒ 이 400 은 **버그가 아니라 중복가드 정상동작**일 수 있다. 다만 목록이 안 떠서
>    **이미 만들어진 반품서에 도달할 길이 없다** — 사장님 눈엔 "반품이 안 된다".

### A-4 — 담당자 열
판매에서 한 걸 매입에 안 옮긴 사고가 8/25 에 있었다(**게이트가 `Pages/Sales` 만 봐서 초록불**).
같은 재발인지부터 확인 → 게이트 검사 범위에 `Pages/Purchase` 포함.

**A 완료 기준**: 빌드 0/0 + 목록·확정·현황 3개 각각 실측 + 담당자 열 그리드 표시 + 게이트(동작검사)

---

## 트랙 B — 회계관리 2차 (메뉴 재편 + 전수조사)

### B-0 🔴 **선행 — 진입점 전수 세기** (코드 손대기 전)

이름 하나가 **사이드바·`@page` 라우트·`PageTitle`·`PageHeader`·권한 `menu_code`·검색 인덱스**
여러 곳에 산다. 8/25 반품 메뉴 제거 때 진입점이 **6곳**에 흩어져 있었다.
**세지 않고 바꾸면 어딘가에 옛 이름이 남는다.** → 조사표 먼저.

### B-1 메뉴명 변경 (3건)

| 현재 | 변경 | 라우트 | 비고 |
|---|---|---|---|
| 수금 | **수금관리** | `/collections` | |
| 지급 | **지급관리** | `/payments` | |
| 은행 거래내역 | **계좌관리** | `/accounting/bank-transactions` | 카드결제와 동일 취급(준비중) |

### B-2 메뉴 신설 2건 — 🔴 **배치 아님. 설계 결재 필요**

| 신설 | 연결 | 🔴 걸리는 것 |
|---|---|---|
| **급여관리** | 그룹웨어 급여관리 | 🔴 사장님 **수동입력 원칙** 못박음. `/hr/payroll` **이미 존재**(23,095 byte) — 신설이 아니라 **회계에서 참조** |
| **경비관리** | 그룹웨어 경비처리 | 🔴 **경비가 회계에 기표되지 않는다**(`journal` 호출 0건 = 예정트랙 그대로). `/accounting/expenses`(139줄) 와 `/hr/expense-request` 가 **따로 있다** |

> 🔴 **PM 판단 — 사장님 결재 필요 2건**
> ① **급여**: 그룹웨어에 이미 있다. 회계에 **또 만들면 두 화면이 생긴다**.
>    → *"연결"* 이 **회계에서 그룹웨어 급여로 보내는 것**인지, **회계에 급여를 다시 그리는 것**인지 확인 필요.
> ② **경비**: 경비 화면이 지금 **두 개**(회계 139줄 · 그룹웨어). 어느 쪽이 정본인지 결재 필요.
>    그리고 **경비는 회계 기표가 아직 없다** — "연결"에 기표까지 포함인지가 작업량을 가른다.
> 접근권한(대표이사 + 권한부여 직원)은 `menu_code` 서버 판정으로 — 기존 급여 방식과 동일.

### B-3 카드결재 / 계좌관리 — 카카오페이·토스페이먼츠
**파이프라인 설계만. 비활성화 「준비중」 표기.** 실연동 금지.

### B-4 🔴 전수조사 — 사장님이 준 기준

> **"설정관리→사용환경설정의 셋팅에 맞게 매입·매출·계산서·그룹웨어 등
>   히트판 웹서비스의 그룹웨어와 ERP 워크플로우 정합성이 **숫자 끝전까지** 모두 맞아야 됨"**

- 축: **사용환경설정 값** → 매입 / 매출 / 계산서 / 그룹웨어 각각에 **실제로 반영되는가**
- 🔴 **실측 필수** — 단위시험은 SQL 을 실행하지 않는다. 8/25 매입 리포트 20종도
  **로컬 DB 에 넣고 돌려서야** 2건이 잡혔다(전량반품이 `HAVING` 에 걸려 통째로 사라짐 등)
- 조사 결과는 **조사서에 적고 결재** — 즉시 수정 금지

**B 완료 기준**: 진입점 조사표 + 이름 3건 전 진입점 반영 + 신설 2건 결재완료 + 전수조사서

---

## 트랙 C — 계산서관리 2차

### C-0 🔴 선행 — 상태 3단계가 DB 에 없다

DDL 실측(`tax_invoices`): `status` = `issued | canceled` · `etax_status` = `pending | issued | failed`
사장님 요구 = **미발행 / 계산서발행(임의) / 전자발행(국세청)** 3단계.
⇒ 🔴 **"미발행"은 `tax_invoices` 에 행이 없는 상태**다. 세 상태가 **한 컬럼에 안 산다.**
⇒ 조회는 **거래명세서(`sales_deliveries`) 기준 LEFT JOIN** 으로 짜야 미발행이 보인다.
   → 마이그 필요 여부 **DDL 보고 판정**(추측 금지). 필요하면 DB-111.

### C-1 계산서 발행 (기존 개편)
- 거래명세서 판매목록 → **미발행 건수 조회** → 발행 → 전자발행 **2단계**
- 하단 그리드 콤보 = 미발행 / 계산서발행 / 전자발행 **조회·변경**
- 행 클릭 → **계산서 양식으로 미리보기**

### C-2 **발행현황 (신설)** — 🔴 현재 **없음**(실측 확인)
조회유형 필터 + 시작일·종료일 DatePicker + 계산서 상태 필터. 판매·매입현황과 동형.

### C-3 발행이력 — **기존 유지**(손대지 않음)

### C-4 발행통계 — C-2 와 동일 필터 3종 추가 (`/tax-invoice-stats` 존재)

### C-5 **발행순위표 (신설)** — 🔴 현재 **없음**(실측 확인). C-2 와 동일 필터

### C-6 범용인증서 관리 — **메뉴 겹침 통합**
`/tax/certificate`(범용인증서) + `/settings/tax-invoice-cert`(전자세금계산서 인증서) **2개가 계산서관리에 동시 존재**.
→ 통합. 이세로·메이크빌 전송은 **파이프라인만 설계 · 비활성화 「준비중」**

### C-7 홈택스 연동설정 / C-8 전자세금계산서 관리 — **지금처럼 비활성화 유지**(작업 없음)

### C-9 🔴 판매 워크플로우 전수조사
거래명세서 → 계산서 → 회계 기표 → 부가세 신고자료 **숫자 일치**까지.

**C 완료 기준**: 상태 3단계 조회 동작 + 신설 2화면 + 인증서 통합 + 전수조사서

---

## 트랙 D — 자료관리 마이그레이션 (조건부)

🔴 **시간이 남을 때만.** 사장님: *"시간이 된다면… 아니면 계산서까지 하고 마무리"*
⇒ **C 가 끝나기 전엔 쳐다보지 않는다.**

---

## PM 게이트 — 트랙 이동 조건

```
A 완료(실측 3건 + 담당자열) ──▶ B 착수
   ⛔ A-1 원인 미규명 상태로 B 넘어가지 않는다

B-0 진입점 조사표 ──▶ B-1 코드 착수
   ⛔ 세지 않고 이름 바꾸지 않는다

B-2 신설 2건 ──▶ 🔴 사장님 결재 대기(급여 중복 · 경비 정본)
   ⛔ 결재 전 코드 금지 (#33)

B 완료 ──▶ C 착수
C-0 상태 3단계 판정 ──▶ C-1 착수
C 완료 ──▶ (시간 남으면) D
```

## 🔴 사장님 결재 대기 — 착수 전 여쭐 것

1. **급여관리**: 그룹웨어에 이미 있다 → 회계는 **링크만**인가, **화면을 또 그리나**
2. **경비관리**: 경비 화면이 **두 개**다 → 정본은 어디이며, **회계 기표까지** 포함인가
3. (이월) 재고이송 2건 · 로그인 하단 버튼 · 작15 우회로
