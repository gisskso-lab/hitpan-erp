# 20260924작1 — 메인PC 봉인 공급자 불일치 봉합 작업지시서

- 일자: 2026-09-24 · 작성 설계팀장 존 · **결재 PM 브라운킴** (작성자 ≠ 결재자 · ⛔ 결재 전 코딩 금지)
- 설계문서: `docs/설계/erp/20260924_설계문서_메인PC봉인_공급자불일치_출입증전달.md`
- 선행검증: `docs/검증/선행/20260924_선행검증서_메인PC출입증_전달끊김_전제실측.md` (R-1 확정)
- 오더: 사장님 2026-09-24 *"팝업 메인pc 등록 해도 메인pc에서 실행할수 있는 기능이 메인pc로 인식이 안되서 안됨"*

---

## 카드 (≤40줄)

### 고칠 것 한 줄

봉인은 **TPM 금고**, 해제는 **소프트웨어 금고**를 연다 ⇒ 출입증이 **발급된 적이 없다**.

### 갈래 (겹치지 않는다)

| 절 | 담당 | 주 대상 파일 | 완료 판정 |
|---|---|---|---|
| **A** | 보안 매니저 1 + 백엔드 | `MainPcSealService.cs`(신규) · `MainPcProofService.cs:127,235,48` | G-21a · G-21b · G-22 · G-23 |
| **B** | 백엔드 + 프론트 | `MainPcProofOutcome` · `MainPcProofService.cs:109-147` · `MainPcGate.razor:92,101,128-133` | G-24 · G-25 |
| **C** | 프론트 매니저 | `HitPanApiAuthHandler.cs:47-70` | G-26 |
| **D** | 프론트 2 + 퍼블리셔 | `MainPcOnly.razor:41-65` · `BackupPage.razor:39-50` · `DataResetPage.razor` · `MdbMigration.razor:646` | G-27 |
| **E** | 검증팀장 데이비드 박 | `src/HitPan.Tests/Integrity/MainPcSealProviderGateTests.cs`(신규) | §5 표 8건 전부 |

착수 순서: **A → B → (C ∥ E) → D.** A 없이 B~D 만 하면 증상은 **안 사라진다**(선행검증 §8).

### 🚨 절대 금지

- `TpmKeyService.cs` **한 글자도 수정 금지** — 세금계산서 경로다(#1 · 트랙 밖 #33).
- 실물 `TpmKeyService.SealKey` **실행 금지** — 이 PC 의 TPM 키를 덮는다.
- 게이트가 `HitPan.MainPc.SealKey` · `HitPan.TaxInvoice.MasterKey` 를 만들거나 덮는 것 **금지**(§5-0).
- 운영(사장님 PC) 직접 수술 금지(#39) · 워치독·업데이트·Major 팝업 무접촉(#43) · `appsettings*` 무접촉(#21).

### 마이그레이션

🟢 **불필요.** 봉투 5바이트는 기존 `VARBINARY(1024)` 안에 들어간다.
⇒ `DB-127` 만들지 않는다 · 출하 DDL `installer/hitpan_db_clean.sql` **무접촉**(#36 판정 완료 · 현재 최대 번호는 DB-126).
🔴 다른 판단이 필요해지면 **PM 결재를 먼저 받는다.** 번호 겹치면 뒤 파일이 skip 된다.

### 빌드

#19 — errors 0 + **warnings 0**. 각 절 완료 시 `dotnet build src/HitPan.sln -c Release` 로 확인한다.

---

## §1 절A — 봉인/해제 공급자 + 키 분리

### §1-1 신규 파일

`src/HitPan.Application/Services/Security/MainPcSealService.cs` · `IMainPcSealService`

| 멤버 | 계약 |
|---|---|
| `byte[] Seal(byte[] raw)` | 갈래를 **재서** 고르고 그 공급자로 봉인 → 봉투를 씌워 반환 |
| `MainPcUnsealResult Unseal(byte[] envelope)` | 매직 확인 → 갈래 읽기 → **공급자를 명시해** 해제 |
| `bool IsPlatformKspUsable()` | 임시 키 이름으로 `CngKey.Create(..., MicrosoftPlatformCryptoProvider)` 를 **실제로 시도**하고 성공하면 즉시 삭제 |

`MainPcUnsealResult` = `Status` (`Ok` / `LegacyFormat` / `NotThisPc`) + `Key`(Ok 일 때만).
🔴 `Unseal` 은 **예외를 밖으로 던지지 않는다.** 상태로 답한다 — 지금 예외가 `DifferentPc` 로 위장되는 통로(`MainPcProofService.cs:137-146`)를 끊는다.

### §1-2 봉투 포맷

```
[0..3] 'H' 'P' 'M' '1'
[4]    갈래  'P'=Platform(TPM) · 'L'=DPAPI LocalMachine · 'U'=DPAPI CurrentUser
[5..]  본문  그 갈래가 만든 바이트 그대로
```

- 매직 불일치 · 길이 5 미만 → `LegacyFormat`. **해제를 시도하지 않는다**(TPM 접근 0회).
- 갈래 문자가 말하는 방법 **하나만** 쓴다. 실패해도 **다른 방법으로 폴백하지 않는다** → `NotThisPc`.

### §1-3 키 이름

- 상수 `HitPan.MainPc.SealKey` (사장님 C-1 결재 2026-09-24).
- 🔴 생성자에 **키 이름을 주입할 수 있게** 둔다(기본값 = 위 상수). 게이트가 실물을 안 덮고 진짜 왕복을 도는 **유일한 통로**다.
- 🚫 `HitPan.TaxInvoice.MasterKey` 를 이 파일에서 **참조하지 않는다.**

### §1-4 결선 (`MainPcProofService.cs`)

- `:48` 생성자 — **오버로드를 더한다.** 기존 3인자 생성자를 **지우지 않는다**(#1 · 기존 시험 무회귀).
- `:43` `_tpm` 필드 **그대로 둔다.** `_seal` 을 **더한다.**
- `:235` `_tpm.SealKey(raw)` → `_seal.Seal(raw)`
- `:127` `_tpm.UnsealKey(sealedKey)` → `_seal.Unseal(sealedKey)` (§2 판정표로 이어진다)
- `src/HitPan.API/Program.cs:174` 부근 — `IMainPcSealService` DI 등록을 **한 줄 더한다**. `ITpmKeyService` 등록은 **그대로 둔다**.

### §1-5 #12 의무 — 인터페이스 확장 금지 확인

`ITpmKeyService` 구현체는 4개다(`TpmKeyService` · `NeverCalledTpm` · `LocalSealTpm` · 라운드트립 시험의 가짜).
⇒ **확장하지 않는다.** 새 인터페이스를 쓰는 이유가 이것이다. 착수 전 `grep -rn "ITpmKeyService" src` 로 재확인한다.

---

## §2 절B — 옛 봉인값 판별 + 재등록 UX

### §2-1 판정표 (`MainPcProofService.JudgeRegistrationAsync` `:109-147`)

| `Unseal` 결과 / 칸 상태 | 판정 |
|---|---|
| 칸이 NULL/빈값 | `NotRegisteredYet` (종전 `:122-123` 그대로) |
| `Ok` | `MainPcConfirmed` (종전) |
| `NotThisPc` | `DifferentPc` (종전) |
| **`LegacyFormat`** | **`ReRegisterRequired`** (신규) |

- `MainPcProofOutcome` 에 `ReRegisterRequired` 를 **더한다**(#1 추가만 · 기존 값 순서·이름 무변).
- 🔴 `:137-146` 의 catch 는 **남긴다.** `Unseal` 이 안 던져도 DB 오류 등은 여전히 올 수 있다. 다만 로그 문구에 *"봉인키가 이 컴퓨터에서 풀리지 않는다"* 를 쓰는 갈래가 **`NotThisPc` 하나로 좁혀진다.**

### §2-2 팝업 (`MainPcGate.razor`)

- `:101` `enum Prompt` 에 `Reissue` 를 **더한다.**
- `:128-133` switch 에 `("ReRegisterRequired", true) => Prompt.Reissue` 를 **더한다.**
  🔴 **더하지 않으면 `_` 로 떨어져 팝업이 안 뜬다 = 막다른 방**(8/16 P0 전례).
- `:92` 버튼 글자 — `Reissue` 일 때 **"다시 인증"**.
- 본문 문구(고객어 · 개발용어 금지 · 챗봇 지식과 같은 말투):
  *"자료보관 컴퓨터 인증을 한 번 더 받아야 합니다. 이 컴퓨터가 회사 자료가 들어 있는 그 컴퓨터가 맞으면 [다시 인증] 을 눌러 주세요."*
- 🔴 `RegisterAsync`(`:184-230`) 를 **그대로 쓴다.** 새 API 를 만들지 않는다.
- 🔴 대표에게만 뜬다 — `:126` `account_type == "tenant_admin"` 규칙 무변. 직원 화면 변화 0.

---

## §3 절C · 절D — 출입증이 끊기는 자리

### §3-1 절C — 403 `main_pc_only` 1회 자동 재왕복 (주 봉합)

대상: `src/HitPan.Web/Services/HitPanApiAuthHandler.cs` — `:54` 의 **401 재시도 바로 옆**에 같은 모양으로 더한다.

- 조건: 응답 `403` **이고** 본문 `error == "main_pc_only"` 일 때만. 다른 403 은 손대지 않는다.
- 동작: 왕복 1바퀴(①`mainpc-challenge` → ②`hitpanMainPc.probe` → ③`mainpc-verify`) → 출입증을 받았으면 원요청 **1회** 재시도.
- 🔴 **요청당 1회** + 실패 시 **60초 쿨다운**(정적 타임스탬프). 클라이언트 PC 에서 매 403 마다 왕복을 돌면 소음이다.
- 🔴 왕복 3콜 자신은 **재시도 대상에서 제외**한다(무한 고리 금지) — `skipBearer` 와 같은 방식으로 경로를 뺀다.
- #15 — 빈 catch 금지. 최소 `LogDebug(ex, ...)` 한 줄.
- 🟢 이 절 하나가 **경주(C-3) · 30분 만료(C-5) · SPA 이동 중 만료**를 함께 덮는다.

### §3-2 절D — 화면은 통과한 뒤에 부른다 (소음·깜빡임 제거)

- `MainPcOnly.razor:41-65` — `[Parameter] public EventCallback OnAllowed { get; set; }` 를 **더한다.**
  `:52` 에서 `_isMainPc == true` 로 판정된 뒤 `await OnAllowed.InvokeAsync()`. `ChildContent`·문구·잠금 기본값은 **무변**.
- `BackupPage.razor:39-50` — `OnInitializedAsync` 의 `LoadAsync()` 호출을 **지우고** `<MainPcOnly OnAllowed="LoadAsync">` 로 옮긴다.
  `LoadAsync` 본문·마크업·저장 동작은 **무변**.
- `DataResetPage.razor:18` · `MdbMigration.razor:646` — 같은 방식으로 init 조회를 `OnAllowed` 로 옮긴다.
- 🔴 **서버 4곳은 손대지 않는다**(`BackupController.cs:23` · `DataResetController.cs:19` · `MigrationController.cs:438,684`).
  화면을 감추는 것은 차단이 아니다 — 서버 판정은 그대로 둔다.
- 🟢 절C 가 있으면 절D 없이도 기능은 된다. **순서는 C 먼저.**

---

## §4 검증 기준 — 동작으로만 판정한다

### §4-1 실물 실측 (사장님 PC 아님 · 이 개발 PC 정식설치 실물)

| V | 무엇을 하나 | 통과 조건 |
|---|---|---|
| V-1 | 도메인(터널)으로 로그인 | 대표 화면에 **[다시 인증]** 팝업이 뜬다 (옛 봉인값을 알아봤다는 증거 = `ReRegisterRequired`) |
| V-2 | [다시 인증] 1회 클릭 | 팝업이 닫히고 **자료관리(백업) 화면이 그 자리에서 열린다.** 재로그인·F5 불필요 |
| V-3 | F12 Network — 백업 설정/이력/복원이력 3콜 | **200**. `X-MainPc-Pass` 헤더가 실려 있다 |
| V-4 | 같은 브라우저에서 **31분** 뒤 백업 화면 재진입 | 첫 요청이 403 → 왕복 → **재시도 200**. 화면에 오류가 안 보인다 |
| V-5 | **클라이언트 PC**(다른 컴퓨터)에서 백업 화면 | "자료보관 컴퓨터에서만" 안내가 뜬다 · **콘솔 403 이 0건** (절D 효과) |
| V-6 | 재등록 직후 API 로그 | `"TPM 2.0 봉인 성공"` 이 **안 찍힌다**(그 줄은 `TpmKeyService` 것이다) · 새 서비스의 봉인 성공 줄이 찍힌다 |
| V-7 | 🚨 재등록 전후 `CngKey.Exists("HitPan.TaxInvoice.MasterKey", Platform)` | **값이 바뀌지 않는다** (세금계산서 키 무접촉 실증) |

🔴 **V-1 이 [이 컴퓨터로 바꾸기] 로 뜨면 절B 가 안 붙은 것이다** — 그대로 진행 금지.

### §4-2 회귀

- `dotnet build src/HitPan.sln -c Release` → **errors 0 · warnings 0** (#19).
- 기존 게이트 **G-1a ~ G-20 전건 통과**. 특히 `G-14`(세 갈래를 가른다)는 갈래가 넷이 되므로 **깨지면 시험을 고치되, 기존 세 갈래의 판정은 바꾸지 않는다.**
- `MainPcProofRoundTripGateTests` · `MainPcSealRegistryGateTests` 의 **가짜 구현체는 그대로 둔다**(#1). 새 진짜 왕복은 §5 의 새 파일에서 돈다.

### §4-3 CI

- `build` 잡(`windows-latest` · `.github/workflows/build-and-test.yml:20`)의 `dotnet test src/HitPan.sln` 이 **필터 없이** 새 게이트 클래스를 문다 ⇒ **yml 수정 불필요.**
- 🔴 그래도 **CI 로그에 새 클래스 이름이 뜨는지 눈으로 확인한다.** 안 뜨면 안 돈 것이다(9/22 N-1 교훈).
- 🚫 `db-gate` 잡(`:93` `ubuntu-latest`)에 **넣지 않는다** — Linux 에는 CNG 가 없다.

---

## §5 게이트 목록 — 봉합을 빼면 FAIL 하는가

### §5-0 격리 규칙 (🚨 어기면 실물 키가 덮인다)

신규 파일 `src/HitPan.Tests/Integrity/MainPcSealProviderGateTests.cs`.

- 시험이 쓰는 키 이름은 **`HitPanTest.MainPcSeal.<GUID8>`** — 매번 새로 만든다.
- 매 시험 `finally` 에서 그 키를 **삭제**하고, **삭제됐는지 확인**한다(잔재 0).
- 🚫 `HitPan.MainPc.SealKey` · `HitPan.TaxInvoice.MasterKey` 를 **만들지도 열지도 삭제하지도 않는다.**
- 🚫 실물 `TpmKeyService` 를 **부르지 않는다.**

### §5-1 게이트 8건

| ID | 무엇을 동작으로 묻나 | 봉합을 빼면 (= FAIL 확인 방법) |
|---|---|---|
| **G-21a** | **Software 금고로 진짜 왕복**: 격리 이름으로 `Seal` → `Unseal` → 원문이 **바이트까지 같다** | `Unseal` 의 공급자 인자를 빼 1인자 오버로드로 되돌리고, `Seal` 을 Platform 으로 두면 → `NotThisPc` ⇒ **FAIL**. 어느 Windows 에서나 돈다 |
| **G-21b** | **TPM 금고로 진짜 왕복** (`IsPlatformKspUsable()` 이 참일 때만) | 같은 위. TPM 이 없는 러너에서는 안 돌지만 **G-21a 가 본질을 물므로 건너뛰기가 아무것도 가리지 않는다** |
| **G-22** | **공급자 분리 음성 대조군**: Software 금고에만 만든 키를 `Exists(name, Platform)` 으로 찾으면 **False** | 이 명제가 거짓이면 R-1 자체가 틀린 것이다. PM 실측(선행검증 §PM)을 게이트로 굳힌다 |
| **G-23** | **봉투가 갈래를 말한다**: `Seal` 결과의 `[0..3]=="HPM1"` 이고 `[4]` 가 `P`/`L`/`U` 중 하나 · 그 바이트를 **다른 갈래 문자로 바꾸면 `Unseal` 이 `NotThisPc`** | 갈래 문자를 무시하고 폴백을 살리면 → 바꿔도 풀린다 ⇒ **FAIL** |
| **G-24** | **옛 값을 알아본다**: 봉투 없는 임의 바이트를 넣으면 `LegacyFormat` 이고 **TPM 을 열지 않는다**(격리 키가 생성되지 않았음을 확인) | `LegacyFormat` 갈래를 지우면 해제를 시도해 `NotThisPc` 가 된다 ⇒ **FAIL** |
| **G-25** | **재등록으로 보낸다**: `LegacyFormat` 인 기기줄로 판정하면 결과가 **`ReRegisterRequired`** 이고, 그 값이 `MainPcGate` 의 팝업 갈래에 **매핑되어 있다**(`Prompt.Reissue`) | 매핑을 빼면 `Prompt.None` 이 되어 팝업이 안 뜬다 ⇒ **FAIL** (막다른 방 재현) |
| **G-26** | **403 이면 왕복하고 다시 간다**: 첫 응답 403 `main_pc_only` → 두 번째 요청이 **출입증 헤더를 달고** 나가고 **200**. 그리고 **같은 실패가 60초 안에 두 번 왕복하지 않는다** | 재시도 블록을 빼면 두 번째 요청이 없다 ⇒ **FAIL**. 쿨다운을 빼면 왕복 횟수가 1을 넘는다 ⇒ **FAIL** |
| **G-27** | **통과 전엔 안 부른다**: `MainPcOnly` 가 `_isMainPc=false` 로 끝나면 `OnAllowed` 가 **0회** 호출된다 · true 면 **정확히 1회** | `OnAllowed` 를 `OnInitializedAsync` 로 되돌리면 false 에서도 1회 ⇒ **FAIL** |

### §5-2 등록·계측 의무

- 🔴 위 8건은 **초록불이 아니라 무력화 시험으로 등록한다**(#32). 담당자는 **봉합을 실제로 빼 보고 FAIL 을 확인**한 뒤 개발명세서 §4 에 그 표를 적는다.
- 🔴 CI 로그에 `MainPcSealProviderGate` 라는 이름이 **실제로 뜨는지** 확인한 run 번호와 SHA 를 명세서에 적는다.
- 🚫 `--verbosity normal` 로그의 한글 DisplayName 만 보고 판정하지 않는다 — FQN 으로 확인한다.

---

## §6 산출물 · 다음 단계

| 단계 | 낼 것 | 위치 |
|---|---|---|
| [3] | 개발명세서 (절A~E 각 1 또는 통합 1) | `docs/개발/erp/20260924작1_*_개발명세서.md` |
| [3-V] | 병렬검증 (보안상무 안철수) — 키 분리·출입증 결속 관점 | `docs/검증/병렬이슈/` |
| [4] | 작업리뷰서 (검증팀장) — §4 실측 V-1~V-7 · §5 무력화 시험 | `docs/검증/기기/` |
| [5] | CTO 1차 → 사장님 | `docs/운영기록/` |
| [6] | 커밋 — 결재 + 검증 통과 시 예외 없이 | — |

🔴 **열린 쟁점 4건(설계문서 §8 O-1~O-4)은 이 작업으로 닫히지 않는다.** 특히 **O-1(세금계산서도 같은 고장일 수 있다)** 은 사장님 결재 사항이며 이 작지서 범위 **밖**이다.

---

`docs/운영기록/INDEX.md` 추가할 줄 (#41 · 60자 이내):

`| [20260924작1_메인PC봉인_공급자불일치_봉합_작업지시서.md](20260924작1_메인PC봉인_공급자불일치_봉합_작업지시서.md) 🆕 | 메인PC 봉인 공급자 불일치 봉합 | 2026-09-24 |`

---

## §7 PM 결재 (2026-09-24 · 브라운킴 · 작성자 ≠ 결재자)

### 🟢 승인 — 절A~E 전부. [3] 착수를 허가한다.

승인 근거 (내가 실제로 본 것):
- §5 게이트 8건이 **전부 동작 판정**이고, **봉합을 빼면 FAIL 하는 방법**이 ID 마다 적혀 있다 ⇒ 가짜게이트(#32) 재발 방지가 문서에 박혀 있다.
- **G-22 가 PM 동작 실측(선행검증 §PM)을 게이트로 굳힌다** ⇒ R-1 이 뒤집히면 게이트가 먼저 운다.
- §5-0 격리 규칙이 **실물 `KEY_NAME` 무접촉**을 명시하고, **V-7** 이 세금계산서 키 불변을 실증한다 ⇒ 사장님 결재 ②(키 분리) 이행 + M-12 추가 피해 0.
- `TpmKeyService.cs` 무접촉 ⇒ 트랙 밖(세금계산서)을 안 넘는다(#33 · 9/22 지적).
- 마이그레이션 불필요 판정에 **근거**(봉투 5바이트 ⊂ `VARBINARY(1024)`)와 **현재 최대 번호 DB-126** 이 같이 적혀 있다(#13·#36).

### 🔴 PM 이 닫는다 — O-3 (TPM 없는 PC 의 봉인 갈래)

**결정: 봉인에 `U`(DPAPI CurrentUser) 갈래를 쓰지 않는다. `P` 또는 `L` 만이다.**

- **왜**: `U` 로 봉인하면 **지금 사고와 똑같은 모양**이 된다 — 그 순간엔 성공으로 보이고, 프로필이 바뀌는 **나중에 조용히 못 푼다.** API 는 SYSTEM 으로 돈다. 🔴 **조용히 나중에 터지는 경로를 새로 만들지 마라.**
- **왜 안전한가**: `L`(DPAPI LocalMachine)은 **TPM 이 없어도 된다.** 설계가 걱정한 *"TPM 없는 고객이 등록 자체를 못 한다"* 는 `L` 이 받는다. `U` 까지 내려가는 건 `L` 마저 실패하는 **비정상 상황**이다.
- **그때는**: 봉인 실패로 처리하고 **등록을 거절하되, 화면이 사유를 말한다.** 🚨 **막다른 방을 만들지 마라**(8/16 P0 전례) — 재시도 경로를 반드시 남긴다.
- 🔴 **게이트로 굳혀라**: `U` 갈래로 봉인이 만들어지지 않는지 묻는 게이트를 **G-23 에 한 줄 덧붙인다**(봉투 `[4]` 가 `U` 인 값은 **봉인 쪽에서 생성되지 않는다**). 해제 쪽은 옛 값 호환을 위해 `U` 를 **읽을 수만** 있게 둔다(#1).

### ⏸ 보류 — 트랙 밖 (PM 이 정하지 않는다)

| # | 내용 | 처리 |
|---|---|---|
| **O-1** | 세금계산서도 같은 `UnsealKey` 를 쓴다(`DoubleEncryptionService.cs:111`) ⇒ **인증서도 안 풀릴 수 있다** | 🔴 **사장님 결재 사항.** 본 트랙에서 손대지 않는다. 별건 작업지시서 |
| **O-4** | 9/23~24 등록이 세금계산서 키를 이미 덮었는가 | ⏸ PM 권한으로 못 잰다(선행검증 §PM) · 사장님 지시 전까지 열지 않는다 |
| **O-2** | 출입증 테넌트 미결속 | 🟡 별건 P1 — 별도 트랙. 본 작업에서 **넓히지 않는다** |

### 🔴 [3] 개발팀에 붙이는 단서

1. **착수 순서 A → B → (C ∥ E) → D 를 지킨다.** A 없이 B~D 만 하면 증상은 안 사라진다.
2. **절A~E 는 파일이 서로 물린다**(`MainPcProofService` · `MainPcGate` · 판정 enum) ⇒ **갈래를 쪼개 병렬로 돌리지 않는다.** 한 줄기로 간다.
3. 🚨 **실물 `SealKey` 실행 금지 · 실물 `KEY_NAME` 무접촉 · `TpmKeyService.cs` 한 글자도 금지.**
4. **빌드 0/0**(#19) · **빈 catch 금지**(#15) · **`.razor` 에 raw string `"""` 금지**(#14).
5. 🚫 `git checkout --` 류 되돌리기 금지 — 커밋 안 한 봉합까지 지운다(9/23 전례).

**결재**: PM 브라운킴 · 2026-09-24 · ⛔ 이 줄 위의 O-3 결정과 단서 5개는 **작업지시서 본문과 같은 효력**을 갖는다.

---

## §8 [3-V] 병렬검증 반증 접수 — PM 처리 (2026-09-24 · 브라운킴)

출처: `docs/검증/병렬이슈/20260924_병렬이슈_메인PC봉인_보안반증.md` (보안상무 안철수 관점)

### 🔴 [3] 정지 범위 — **절A 만 멈춘다. 절B~E 는 계속.**

키 이름 규칙은 **첫 줄을 쓰기 전에** 정해야 한다. 쓰고 나면 고칠 자리가 **이미 키가 심긴 고객 PC** 다.

### 접수 판정

| S | 반증 | PM 처리 |
|---|---|---|
| **S-4** | `ReRegisterRequired` 가 서버 화이트리스트에 **없다** — `src/HitPan.API/Controllers/DeviceController.cs:609` 는 `NotRegisteredYet`·`DifferentPc` 만 받는다 ⇒ **[다시 인증] 누르면 400 · 막다른 방** | 🟢 **PM 이 닫는다 — 설계 누락 확정.** PM 이 `:609` 를 직접 읽어 확인했다. 작지서 §2 수정 대상에 **`DeviceController.cs` 를 추가한다.** 🚨 8/16 P0 와 같은 모양(막다른 방)이므로 **절B 완료 판정에 이 경로를 넣는다** |
| **S-5** | 키는 **PC 당 하나**인데 봉인값 칸은 **회사당 하나**(`DB-120_..._guard.sql:107,179`) ⇒ 한 PC 에 회사가 둘이면 `OverwriteExistingKey` 로 뒤가 앞을 죽인다. 또 `MainPcProofService.cs:235` 봉인 → `:259-267` DB 쓰기 **순서** 때문에 **등록 실패 1회가 옛 봉인값을 영구히 죽인다** | 🔴 **설계로 되돌린다.** 🚨 *"한 PC 에 여러 사업장"* 은 **사장님이 이미 결재한 시나리오**다(6월 `multi_business_per_pc`) ⇒ **가정이 아니라 실재**. **M-12 와 같은 모양이 새 키에서 재발**하는 것이므로 그냥 못 넘긴다 |
| **S-2** | 출입증이 회사·사용자·기기 **무결속 30분 bearer** 인데, 절C 가 발급을 「대표 로그인 1회」→「누구든·어느 화면이든 403 이면 자동」으로 **넓힌다** | 🔴 **설계로 되돌린다.** O-2(별건) 본체는 그대로 두되, **이 봉합이 표면을 넓히는 것은 허용하지 않는다.** 절C 범위를 좁힐 방법을 설계가 답한다 |
| **S-6** | O-3 결론(`U` 배제)은 지지하나 **근거가 틀렸다** — `L` 은 하드웨어 미결속이라 DB+MachineKeys 를 함께 복사하면 다른 PC 에서 풀린다 | 🟡 **결론 유지 · 근거 정정.** §7 의 *"`L` 이라 안전하다"* 는 **틀렸다**. `L` 은 *"TPM 없는 고객을 받는 최소 수단"* 일 뿐 **하드웨어 결속이 아니다.** 이 한계를 설계문서에 명시하고, 사본 복제 위협은 **별건**으로 연다 |
| **S-3** | 쿨다운이 실패 경로만 덮는다 · `RefreshLock` 같은 직렬화가 없어 init 3콜이 왕복을 3중으로 돈다 | 🟡 설계가 직렬화를 넣는다 |
| **S-1** | 갈래 강등은 성립 안 함(G-23 이 문다) · 다만 봉투에 **MAC 이 없고** 판정이 「복호 성공 여부」뿐인 **오라클** | 🟡 설계 판단 |
| **S-7** | 게이트 8건이 전부 초록이어도 ①서버 화이트리스트 ②두 번째 봉인 ③다른 회사 출입증 ④등록 실패 뒤 옛 값 사망이 **그대로 통과** | 🔴 **게이트를 늘린다.** 위 넷을 동작으로 무는 게이트가 없으면 [4] 에서 반려한다 |
| **S-8** | 기기 Pull 은 3컬럼 화이트리스트(`SyncService.cs:51-58`) · 새 컬럼 0 | 🟢 #18·#22 위반 없음 — 확인됨 |

### 🔴 설계팀장에게 돌려보내는 것 (작지서 개정 요구)

1. **S-5 키 이름 규칙** — 한 PC 에 여러 회사가 공존할 때 **서로 죽이지 않는** 이름 체계. 🚨 `OverwriteExistingKey` 가 **언제 무엇을 죽이는지** 명시하라.
2. **S-5 순서** — **등록이 실패해도 옛 봉인값이 살아 있어야 한다.** 봉인 → DB 쓰기 순서를 그대로 둘지, 뒤집을지, 세대를 둘지 답하라.
3. **S-2 절C 범위** — 자동 재왕복을 **누가·어느 화면에서** 쓸 수 있는지 좁혀라.
4. **S-7 게이트 4건 추가** — 위 ①~④.
5. **S-3 직렬화 · S-1 MAC** — 넣을지 말지와 그 이유.
6. **S-6 근거 정정**을 설계문서에 반영.

⛔ **절A 는 위 개정에 PM 이 다시 결재할 때까지 착수 금지.**

**기록**: PM 브라운킴 · 2026-09-24

---

## §9 절A 개정 — 키 이름 · 순서 · 범위 (2026-09-24 · 설계팀장 존 · ⛔ PM 재결재 전 착수 금지)

> 근거: 설계문서 `## §개정1`. §8 의 개정 요구 6건에 대한 답이다.
> 🔴 이 절은 §1~§5 를 **지우지 않는다.** 갈리는 줄은 §9-0 에 적었고, **그 줄만 이 절이 이긴다**(#1).

### §9-0 앞 절의 어느 줄이 갈렸나

| 앞 절 | 갈린 줄 | 개정 |
|---|---|---|
| §1-2 | 봉투 `HPM1` · 4+1 바이트 | **`HPM2` · 4+1+1 바이트**(세대) — §9-1 |
| §1-3 | 키 이름 상수 **하나** | **회사별 + 세대 이름** — §9-1 |
| §1-1 | `Seal(byte[] raw)` | `Seal(tenantId, deviceId, raw)` · `Unseal(envelope, tenantId, deviceId)` — §9-2 |
| §1-4 | `:235` 봉인 → `:259-267` 쓰기 | **순서 유지 + 6단계 세대 절차** — §9-3 |
| §2 | 수정 대상 파일 | 🔴 **`src/HitPan.API/Controllers/DeviceController.cs` 추가**(S-4) — §9-4 |
| §3-1 | 「403 이면 자동 왕복」 | **대표 + 3경로 + 단일 왕복 잠금** — §9-5 |
| §5-1 | 게이트 8건 | **게이트 13건** — §9-6 |

### §9-1 키 이름 · 봉투 (S-5 ①)

```
이름  HitPan.MainPc.SealKey.{T16}.{GG}
       T16 = SHA-256( tenant_id 를 소문자화·앞뒤공백제거 ) 앞 8바이트 → 16자리 소문자 16진
       GG  = 세대 2자리 16진 (00~FF · 순환)

봉투  [0..3]='H','P','M','2'  [4]=갈래('P'|'L')  [5]=세대  [6..]=본문
       본문의 평문 = tenantId ‖ deviceId ‖ 난수32
```

- 🚫 **`CngKeyCreationOptions.OverwriteExistingKey` 를 쓰지 않는다.** 이 상수가 새 코드에 나타나면 [4] 반려.
- 🔴 이름은 **`P` 갈래에만** 있다. `L` 갈래(`ProtectedData.Protect`)는 이름 있는 키를 안 만든다 ⇒ `optionalEntropy` 에 `T16` 바이트를 넣어 회사를 가른다(지금 실물은 `null` — `TpmKeyService.cs:93`).
- 🚫 `HitPan.TaxInvoice.MasterKey` 를 이 파일에서 **참조하지 않는다**(§1-3 유지).
- 🚫 봉인은 `U` 갈래를 **만들지 않는다**(PM O-3). 해제만 옛 값 호환으로 `U` 를 읽는다.

### §9-2 인터페이스 (S-1 이름표)

| 멤버 | 계약 (개정) |
|---|---|
| `byte[] Seal(string tenantId, string deviceId, byte[] raw)` | 갈래를 재서 고르고 · 세대를 정하고 · 이름표를 넣어 봉인 · 봉투 반환 |
| `MainPcUnsealResult Unseal(byte[] envelope, string tenantId, string deviceId)` | 매직·갈래·세대를 읽어 **그 이름 그 공급자로만** 해제 → **꺼낸 이름표를 인자와 대조** |
| `bool IsPlatformKspUsable()` | §1-1 그대로 (임시 이름으로 실제 시도 · 즉시 삭제) |

- 🔴 이름표가 다르면 `NotThisPc` 다. **"풀렸다"만으로 통과시키지 않는다**(오라클 차단).
- 🔴 `Unseal` 은 예외를 밖으로 던지지 않는다(§1-1 유지).
- 🔴 생성자 키 **접두사** 주입은 유지한다 — 게이트 격리의 유일한 통로(§5-0).

### §9-3 등록 절차 6단계 (S-5 ②) — `MainPcProofService.RegisterThisPcAsync`

| # | 하는 일 | 실패 처리 |
|---|---|---|
| 1 | 현재 봉투를 읽어 세대 `g` 파악 (없음/옛 값이면 미정) | 읽기 실패 → 중단 · 아무것도 안 만든다 |
| 2 | `g' = (g+1) mod 256` · **그 이름이 있으면 한 칸 더**(최대 3회) | 3회 다 참 → 거절 + 사유 안내(막다른 방 금지) |
| 3 | `g'` 이름으로 키 생성 — **덮어쓰기 옵션 없이** | 중단 · 옛 키 무접촉 |
| 4 | 봉인 → 봉투 조립 | 3의 키를 **삭제**하고 중단 |
| 5 | DB `UPDATE`(기존 `:259-267` · SQL 무변) | 🔴 `rows==0` → **`g'` 키 삭제** 후 `false`. **옛 키 `g` 무접촉 ⇒ 옛 봉투 그대로 산다** |
| 6 | `rows==1` 뒤 **옛 키 `g` 삭제**(best-effort · `LogDebug` 한 줄) | 실패해도 무해 |

- 🔴 `:251-257` 의 「옛 메인PC 줄 내리기」 UPDATE 는 **그대로 둔다**(DB-120 UNIQUE 충돌 방지 · 순서 무변).
- #15 — 빈 catch 금지. 삭제 실패도 한 줄 남긴다.

### §9-4 S-4 — `DeviceController.cs` 수정 (PM 이 닫은 항목)

- `src/HitPan.API/Controllers/DeviceController.cs:609` — 화이트리스트에 **`ReRegisterRequired` 를 더한다.**
  현재 `NotRegisteredYet`·`DifferentPc` 둘뿐이라 [다시 인증] 이 **400 · 막다른 방**이다(8/16 P0 와 같은 모양).
- 🔴 **절B 완료 판정에 이 경로를 넣는다** — §4 에 **V-2b** 를 더한다:
  *"[다시 인증] 클릭 → `POST /api/devices/mainpc-register` 가 **200** 이고 `registered=true` 다. 400 이면 절B 미완."*
- 🚫 `account_type == "tenant_admin"` 검사(`:604`)는 **무변**.

### §9-5 절C 범위 (S-2) · 직렬화 (S-3) — `HitPanApiAuthHandler.cs`

**실측**: `mainpc-challenge`/`mainpc-verify` 호출처는 `MainPcGate.razor:152,164,190` **뿐**이고 그 컴포넌트는 `:126` 대표에게만 돈다 ⇒ **오늘 출입증을 받는 사람은 대표뿐이다. 그 표면을 그대로 유지한다.**

| 겹 | 조건 |
|---|---|
| C-① 사람 | `account_type == "tenant_admin"` 일 때만 자동 왕복 |
| C-② 경로 | 403 `main_pc_only` **이면서** 요청 경로가 `api/backup/` · `api/data-reset/` · `api/migration/` 로 시작할 때만. **다른 403 · 다른 경로는 무접촉** |
| C-③ 횟수 | 요청당 1회 · 실패 시 60초 쿨다운 · 🔴 **`SemaphoreSlim(1,1)` 단일 왕복 잠금** — 먼저 들어온 하나만 왕복하고 나머지는 그 결과를 쓴다 |

- 🚫 왕복 3콜 자신은 재시도 대상에서 제외(§3-1 유지).
- ⚠️ **Q-1 확인 필요** — 서버(`mainpc-challenge`/`mainpc-verify`)에 `account_type` 검사를 더할지는 **PM 결재 전까지 손대지 않는다.**
- ⚠️ **Q-2 확인 필요** — 출입증에 **회사 1축** 결속(`IsPassValid(pass, tenantId)`)은 **PM 결재 후** 착수. 결재 전에는 G-30 을 **봉투 이름표 쪽으로만** 세운다(§9-6).

### §9-6 게이트 — 🔴 총 **13건** (기존 8 + 신규 5)

§5-0 격리 규칙은 그대로다. 격리 이름은 `HitPanTest.MainPcSeal.<GUID8>.{T16}.{GG}` 로 접두사만 갈아 끼운다.

| ID | 무엇을 동작으로 묻나 | 봉합을 빼면 (FAIL 확인) | 출처 |
|---|---|---|---|
| G-21a · G-21b · G-22 · G-24 · G-25 · G-27 | §5-1 그대로 | §5-1 그대로 | 기존 |
| **G-23**(개정) | §5-1 + 🔴 **봉인 결과의 `[4]` 가 `U` 인 값이 생성되지 않는다** | `U` 폴백을 살리면 생성된다 ⇒ FAIL | PM O-3 |
| **G-26**(개정) | §5-1 + 🔴 **동시 403 3건이 왕복을 1회만 돈다** · 🔴 **`api/employee/` 403 에는 왕복 0회** | 잠금을 빼면 3회 ⇒ FAIL · 경로 조건을 빼면 왕복 1회 ⇒ FAIL | S-2 · S-3 |
| **G-28** 🆕 | **서버가 재등록을 받는다**: 판정 `ReRegisterRequired` 로 `mainpc-register` 를 부르면 **200 · `registered=true`** | `:609` 목록에서 `ReRegisterRequired` 를 빼면 **400** ⇒ FAIL (막다른 방 재현) | S-7 ① |
| **G-29** 🆕 | **두 번째 봉인이 첫째를 안 죽인다**: 같은 PC 에서 회사 A 봉인 → 회사 B 봉인 → **A 의 봉투가 여전히 `Ok`** | 이름에서 `T16` 을 빼 공유 이름으로 되돌리면 A 가 `NotThisPc` ⇒ FAIL | S-7 ② |
| **G-30** 🆕 | **다른 회사 봉투는 안 통한다**: 회사 A 봉투를 회사 B 로 `Unseal` → **`NotThisPc`** (이름표 대조) | 이름표 대조를 빼면 「복호 성공」만으로 `Ok` ⇒ FAIL | S-7 ③ · S-1 |
| **G-31** 🆕 | **등록 실패가 옛 값을 안 죽인다**: DB `UPDATE` 를 `rows==0` 으로 만든 뒤 **옛 봉투가 여전히 `Ok`** · 새 세대 키가 **남아 있지 않다** | 세대를 빼고 덮어쓰기로 되돌리면 옛 봉투 `NotThisPc` ⇒ FAIL | S-7 ④ · S-5 ② |
| **G-32** 🆕 | **표면이 안 넓어진다**: 비대표(`account_type != "tenant_admin"`)가 403 `main_pc_only` 를 받아도 **왕복 0회 · 출입증 요청 0건** | C-① 을 빼면 왕복 1회 ⇒ FAIL | S-2 |

- 🔴 13건 모두 **무력화 시험으로 등록**한다(#32). 담당자는 **실제로 빼 보고 FAIL 을 확인**해 개발명세서 §4 에 표로 적는다.
- 🔴 CI `build` 잡(`windows-latest`)이 필터 없이 문다 ⇒ yml 무수정. **로그에 클래스 이름이 뜬 run 번호·SHA 를 명세서에 적는다**(9/22 N-1 교훈). 🚫 한글 DisplayName 으로 판정 금지 — FQN 확인.
- 🚫 `db-gate`(`ubuntu-latest`)에 넣지 않는다 — Linux 에 CNG 가 없다.
- ⚠️ **Q-2 미결재 시** G-30 은 **봉투 이름표 축만** 문다. 출입증 축(다른 회사 출입증으로 API 호출)은 **별건 O-2** 로 남고, 이 작지서는 그것을 초록이라 주장하지 않는다.

### §9-7 마이그레이션 · 무접촉 (변동 없음)

- **마이그레이션 불필요.** 봉투 6바이트 + 이름표는 `mainpc_sealed_key VARBINARY(1024)` 안이다. 현재 최대 번호 **DB-126** 확인 · `DB-127` 안 만든다 · `installer/hitpan_db_clean.sql` 무접촉(#36).
- 🚫 `TpmKeyService.cs` 한 글자도 금지 · 실물 `SealKey` 실행 금지 · 실물 `KEY_NAME` 무접촉.
- 🚫 `is_main_pc` · `main_pc_key` · `uq_tenant_main_pc`(DB-120) · `fingerprint`(#37) · 워치독·업데이트(#43) · `appsettings*.json`(#21) 무접촉.
- 🚫 `.claude/worktrees/` 무접촉. 🚫 `git checkout --` 류 되돌리기 금지(9/23 전례).
- 착수 순서 **A → B → (C ∥ E) → D** 유지 · 한 줄기로 간다(§7 단서 1·2 유효).

### §9-8 PM 결재란 (⛔ 비어 있다 — 절A 착수 금지)

| 결재 항목 | 상태 |
|---|---|
| §9-1~§9-3 (키 이름 · 세대 · 순서) | ⏸ 대기 |
| §9-4 (S-4 `DeviceController.cs` 추가 · V-2b) | ⏸ 대기 |
| §9-5 (절C 3겹 · 직렬화) | ⏸ 대기 |
| §9-6 (게이트 13건) | ⏸ 대기 |
| **Q-1** 서버 `account_type` 검사 | ⚠️ 확인 필요 |
| **Q-2** 출입증 회사 1축 결속 | ⚠️ 확인 필요 |
| **O-5** `L` 갈래 사본 복제 위협 (별건) | ⚠️ 별건 개설 여부 |

**작성**: 설계팀장 존 · 2026-09-24 · **결재**: PM 브라운킴 (작성자 ≠ 결재자)

---

## §10 PM 재결재 — 절A 잠금 해제 (2026-09-24 · 브라운킴)

개정안 `## §9`(작지서) · `## §개정1`(설계문서) 검토 완료.

### 🟢 절A 착수를 허가한다. §8 의 정지를 푼다.

승인 근거 (내가 본 것):
- **S-5 키 이름** — `HitPan.MainPc.SealKey.{T16}.{GG}` + **`OverwriteExistingKey` 를 한 번도 안 쓴다.** 🟢 M-12 의 뿌리(덮어쓰기)를 **새 키에서 되풀이하지 않는다**는 뜻이다. 이게 핵심이다.
- **S-5 순서** — 뒤집기가 불가한 이유(DB 에 쓸 바이트가 봉인 결과물)를 **먼저 밝히고** 세대로 푼 점. `rows==0` 이면 **새 키만 지운다** ⇒ 옛 봉투 생존. 🟢 *"등록 실패 1회가 옛 값을 죽인다"* 가 닫힌다.
- **S-5 실측** — 이름 충돌이 `P` 갈래에만 있고 `L` 은 이름 없는 키(`TpmKeyService.cs:93`)라 `optionalEntropy` 로 가른다는 **갈래별 구분**. 뭉뚱그리지 않았다.
- **S-1 MAC 거절 + 대안** — MAC 키 순환 문제를 이유로 거절하고, 봉인 평문을 `tenantId‖deviceId‖난수32` 로 바꿔 **이름표 대조**로 「풀렸나」오라클을 닫은 것. 🟢 *"안 넣는다"* 로 끝내지 않고 **다른 축으로 닫았다.**
- **S-3 직렬화 승인 근거** — *"쿨다운은 실패 뒤에만 걸려 G-26 의 주장이 동시요청에서 거짓이 된다"* ⇒ **게이트가 거짓말하게 되는 조건**을 스스로 찾아냈다.
- 게이트 **13건**(8+5) 전부 무력화 FAIL 방법 병기 · 마이그레이션 불필요 재확인(DB-126 최대 · 출하 DDL 무접촉).

### 🟢 PM 결재 3건

| # | 안건 | 결재 |
|---|---|---|
| **Q-1** | 서버 `mainpc-challenge`/`verify` 에 `account_type` 검사 추가 | 🟢 **승인.** 지금은 **클라이언트만 대표를 거르고 서버는 안 본다** — 화면 말을 믿는 구조다. 서버가 강제하는 것은 **표면을 넓히는 게 아니라 좁히는 것**이고, *"자료관리는 부모계정 + 메인PC 에서만"*(사장님 2026-08-11 지시 · `BackupController.cs:17`)과 정합한다 |
| **Q-2** | 출입증을 **회사 1축**에 결속 | 🟢 **승인.** 최소한 회사는 묶는다. 🔴 단 **"묶었다"고 적지 마라** — 기기·사용자 축은 여전히 열려 있고 그건 **O-2(별건)** 다. 거짓봉합 금지 |
| **O-5** | `L` 갈래 사본 복제(DB+MachineKeys 동시 복제) 위협 | 🟢 **별건 개설 승인.** 본 작업에서 **손대지 않는다.** 설계문서 갈래 한계표에 남은 것으로 명시된 상태 유지 |

### 🔴 [3] 재개 단서

1. **이전 작업물을 이어받아라** — `.claude/worktrees/agent-aa63a9120cd114e2c` 에 9개 파일이 살아 있다(신규 3: `MainPcSealService.cs` · `MainPcAccessState.cs` · `MainPcRetryCoordinator.cs`). 🔴 **버리고 다시 쓰지 마라.**
2. 🚨 **그 작업물은 개정 전 설계 기반이다.** **절A 는 §9 대로 다시 맞춘다**(키 이름 체계 · 세대 · 이름표 평문). 절B~E 는 §9 단서만 얹는다.
3. **절E 미착수** — 게이트 파일이 아직 없다. **13건 전부**가 대상이다.
4. 🚫 `OverwriteExistingKey` 를 쓰면 **[4] 에서 반려**한다(§9-1).

### ⚠️ 에이전트 상한 초과 사유 (토큰 매뉴얼 §2)

규모 **M** 상한 5인데 이번 작업은 **6개째**([1-V] 1 · [2] 1 · [3] 1 · [3-V] 1 · [2 개정] 1 · [3 재개] 1).
사유: **[3-V] 가 절A 를 정지시켜 설계가 한 바퀴 더 돌았다.** 반증이 옳았으므로(S-4 는 PM 이 직접 확인) 되돌리지 않는다.
🔴 이후 [4]·[5] 로 2개가 더 든다 ⇒ **총 8**. **L 규모 상한(8)** 에 해당한다 ⇒ **규모 판정을 M → L 로 정정한다.**

**결재**: PM 브라운킴 · 2026-09-24

---

## §11 [4] 반려 접수 — PM (2026-09-24 · 브라운킴)

리뷰서: `docs/검증/메인PC/20260924_작업리뷰서_메인PC봉인_공급자불일치_봉합.md` (검증팀장 데이비드 박)

### 🔴 반려를 그대로 받는다. 되돌리지 않는다.

### 🔴 PM 결재의 허점 — 내가 틀렸다

§10 에서 나는 이렇게 적었다:

> *"`rows==0` 이면 **새 키만 지운다** ⇒ 옛 봉투 생존. 🟢 *'등록 실패 1회가 옛 값을 죽인다'* 가 닫힌다."*

🔴 **닫히지 않았다. 키 축에서만 참이다.**
`rows==0` 일 때 **①의 강등 UPDATE 가 되돌아가지 않아** 그 회사의 `is_main_pc=1` 줄이 **0개가 된다**(트랜잭션 없음 · 판정 SELECT 는 `is_main_pc=1` 을 본다).
⇒ **열쇠는 살아 있는데 자물쇠를 가리키는 줄이 사라진다.** 결과는 같다 — **메인PC 가 없어진다.**
⇒ 자료관리가 영영 안 열린다 = **막다른 방**. 🚨 8/16 P0 와 같은 모양이다.

**왜 놓쳤나**: 설계가 *"새 키만 지운다"* 라고 답했고 나는 **키 축만 보고 닫았다.** DB 축을 따로 묻지 않았다.
**다음에 할 것**: *"X 가 산다"* 는 답을 받으면 **X 가 몇 축인지 먼저 센다.** 한 축만 보고 닫지 않는다.

### 접수 판정

| # | 조건 | PM 처리 |
|---|---|---|
| **C-1** | P1-1 봉합 — `rows==0` 이면 ①의 강등을 되돌리거나 ①②를 **한 트랜잭션**으로. + `RegisterThisPcAsync` 를 **실제로 부르는** 게이트(가짜 `IDbConnection` 으로 `rows=0` 강제) | 🔴 **필수.** 위 허점의 본체다 |
| **C-2** | P1-2 게이트 — DI 로 뽑은 `IMainPcProofService` 가 `LegacyTpmSealAdapter` 를 **안 쓴다**를 무는 게이트 | 🔴 **필수.** 🚨 **이 트랙의 존재 이유(M-12)가 코드에 살아 있는데 무는 게이트가 0건**이다. `Program.cs` 한 줄이 유일한 방벽인 상태는 **누가 그 줄을 건드리는 날 조용히 재발**한다 |
| **C-3** | G-30 이 무는 축은 **기기**다 — 이름을 고치고 회사 축은 **별도 시험**으로 | 🟡 **동반.** 🔴 이름과 실제가 다른 게이트는 **다음 사람을 속인다** |
| **C-4** | G-31 뒷절반 — `UnsealAfterRollback` 의 **자기충족 단언** 제거 | 🟡 **동반** |
| **C-5** | 실물 실측 V-1·V-2b·V-3·V-7 | 🔴 **필수 · 단 순서가 바뀐다** — 아래 |
| **C-6** | 커밋 직후 CI `build` 잡에서 **FQN** 확인 → run·SHA 기입 | 🟡 **동반.** CI 0회를 이 건 한해 허용한 근거(검증자가 같은 OS·같은 명령으로 직접 확인)를 리뷰서 §8 이 적어 두었다 |

### 🔴 C-5 는 PM 혼자 못 한다 — 순서를 바꾼다

검증자 지적이 옳다. 실물 실측은 **실제 브라우저 창 · 터널 경유**여야 한다.
🔴 **로컬(`localhost`)로 재면 `IsLocalConsole`=true 라 무조건 통과한다 — 재는 의미가 없다.**
그리고 지금 코드는 **미커밋**이라 배포된 곳이 없다.

⇒ **순서**: C-1~C-4 봉합 → [4] 재검증 → **커밋·PR·CI(C-6)** → **배포** → **실물 실측(C-5)** → **[5] 최종 결재**
⇒ 🔴 **[5] 는 C-5 전에 올리지 않는다.** 직전 M-22 가 *"게이트 18건 통과 + 실물 400"* 이었다. 같은 사고를 반복하지 않는다.
⇒ ⏸ **사장님 협조가 필요한 자리다.** 절차서: `docs/검증/20260922_실측절차서_메인PC왕복증명_실물.md`
  🚨 **V-7 은 읽기만 한다** — `SealKey` 를 부르는 순간 그것이 곧 M-12 다.

### 🟢 반려에도 불구하고 기록할 것

- 개발자 주장은 **검증자가 다시 잰 것 전부가 사실**이었다 — 빌드 0/0 · 회귀 1549/1549 · 게이트 17/17 · `TpmKeyService.cs` 0줄 · `OverwriteExistingKey` 0회 · Q-2 과장 없음. **거짓봉합 0건.**
- 실물 0회 · CI 0회 · G-30 축 불일치는 **개발자가 명세서에 스스로 신고**했다. **신뢰 감점 없음.**
- 🔴 그럼에도 반려인 이유: **P1-1 은 신고되지 않은 동작 결함**이고, **P1-2 는 게이트가 0건**이며, 작지서 §4 가 스스로 정한 기준이 **0/7** 이다.

**결재**: PM 브라운킴 · 2026-09-24 · ⛔ C-1·C-2 는 코드, C-5 는 실물. **셋 다 닫히기 전 [5] 금지.**

---

## §12 🔴 사장님 결재 — C-5 순서 정정 (2026-09-24)

> **사장님**: *"게시가 되야 실측검증이 가능해. 최종 실물확인은 게시후에!!"*

### 🔴 PM 이 순서를 거꾸로 잡았다

§11 에서 나는 *"[5] 는 C-5(실물 실측)가 닫히기 전에 올리지 않는다"* 고 적었다.
🔴 **그러면 아무것도 못 한다.** 게시를 하려면 [5] 결재가 있어야 하고, 실측을 하려면 게시가 되어야 한다 — **서로를 기다리는 고리**다.
⇒ **사장님이 그 고리를 끊으셨다.** 실측은 **게시 후**다.

### 🟢 확정 순서

```
C-1~C-4 봉합 → [4] 재검증 → [5] 결재(실물 조건부) → [6] 커밋 · PR · CI(C-6)
   → 🚀 게시(1.3.44) → 🔬 실물 실측 C-5(V-1·V-2b·V-3·V-7) → 최종 확인
```

- **[5] 는 "실물 조건부 통과"로 올린다.** 결재문에 **실물 0/7 임을 명시**하고, 무엇을 게시 후 잴지 항목으로 적는다.
- 🚫 **"실물까지 끝났다"고 쓰지 않는다.** 게시 후 실측 결과가 나오기 전까지 이 건은 **미완**이다.
- 🚨 **V-7 은 읽기만 한다** — `SealKey` 를 부르는 순간 그것이 곧 M-12 다.
- 🔴 **실측에서 실패하면 되돌린다.** 게시 전 manifest 백업 주소를 [6] 기록에 남긴다(9/23 전례: `bak-1.3.42-...`).

### ⚠️ 게시 전 확인 의무 (PM)

- **NCP 디스크 여유** — 9/23 실측 2223MB · **1200MB 밑이면 게시가 막힌다** ⇒ 게시 직전 재측정. 부족하면 **정리 후 게시**.
- **게시 경로** — CI/CD 영구자동화. `production` 환경 승인 1회. 🚫 사장님 PC 직접 수술 금지(#39).
- **#43** — Major 팝업 Y/N 흐름 무접촉.

**기록**: PM 브라운킴 · 2026-09-24 · 사장님 결재 반영

---

## §13 [4] 재검증 🟢 실물조건부 통과 — PM 접수 (2026-09-24 · 브라운킴)

리뷰서 `## §9` (검증팀장 데이비드 박 · 257줄)

### 🟢 C-1~C-4 전부 닫힘 — 검증자가 **직접 빼서 FAIL 을 봤다**

| 무엇 | 검증자가 잰 값 |
|---|---|
| 빌드 | `--no-incremental -t:Rebuild` **1분39초 · 경고 0 · 오류 0** |
| 회귀 | **1553/1553 · 실패 0 · 건너뜀 0** (+ Watchdog 119) |
| 새 게이트 | **FQN 21/21** |
| 무력화 | 이번 판 **3/3 FAIL** + 컴파일 실측 2회 · **누적 22/22 · STILL-GREEN 0** |

- **C-2 덫이 진짜**로 확인됐다 — 3인자 생성자를 부르면 `error CS0619` 로 **빌드가 깨지고**, `#pragma warning disable CS0619` **로도 여전히 깨진다**(검증자 실측). ⇒ 🚨 **M-12 재발 경로에 자물쇠가 섰다.**
- **C-3 가짜게이트가 진짜가 됐다** — 1판 `K2b` STILL-GREEN 구멍을 `G-30b` 가 **실제 기전(키이름+entropy)** 으로 문다.
- **C-4 자기충족 소멸** — `UnsealAfterRollback` grep **0건**. `G-35` 는 진짜 `DbConnection` 대역에 Dapper 를 태워 `RegisterThisPcAsync` 를 **실제로 부른다**.

### 🔴 남은 누수 — 등급 P1 → **P2** (PM 이 받는다)

C-1 은 **트랜잭션이 아니라 보상 UPDATE** 다. 누수 2개:
① 보상 자체가 실패하면 `LogError` 한 줄뿐 ② 강등~보상 사이 **프로세스 사망은 탐지 불가**.

🟢 **다만 막다른 방은 아니다** — 줄 0개 → `NotRegisteredYet` → `Prompt.Register` → **등록 1회로 자가복구**(검증자 경로 추적 확인).
⇒ **P2 로 받는다.** 🚨 단 **게시 후 `되돌리기에 실패했다` 로그 감시가 누수의 유일한 탐지 수단**이다(P-6).

### 🔴 D-1 — 커밋 전 반드시 정정 (PM 지시 완료)

개발명세서 §8 의 *"공유 `IDbConnection` 이라 트랜잭션 불가"* 가 **사실과 다르다.**
같은 scoped 연결로 `BomService.cs:563·823·1156` · `AuthService.cs:243` 가 **이미 `BeginTransaction` 한다.**
🔴 **문서의 「~라서 못 한다」는 다음 사람에게 사실이 된다.** 보상을 고른 것 자체는 선택지지만 **근거가 틀렸다** ⇒ 정정 지시함(D-2 `[Obsolete]` 메시지의 안 통하는 `#pragma` 안내 · D-3 동시성 ⚠️ 포함).

### ⏸ D-4 — 별건 후속으로 연다 (본 작업에서 안 한다)

| # | 내용 |
|---|---|
| **N-1** | `G-33` 이 판정(`Unseal`) 경로는 안 문다 |
| **N-2** | `G-34` 는 속성값만 읽고 **컴파일을 안 잰다**(검증자가 대신 쟀다) |
| **P2-5** | `G-27` 이 `.razor` 가 아니라 **상태 클래스**를 잰다 |
| ⚠️ | 동시 등록 시 **1062** 는 `FakeDevicesDb` 가 UNIQUE 를 강제 안 해 **미측정** |

### 🚀 게시 후 필수 측정 6건 (사장님 결재 §12)

| P | 무엇 |
|---|---|
| **P-1** | V-1 — 팝업이 **[다시 인증]** 으로 뜨는가 |
| **P-2** | V-2b — `mainpc-register` **200** |
| **P-3** | V-3 — 백업 3콜 **200** + `X-MainPc-Pass` 헤더 |
| **P-4** | V-7 — 🚨 **세금계산서 키 불변**(읽기만 · `SealKey` 호출이 곧 M-12) |
| **P-5** | CI `build` 잡 **FQN** `HitPan.Tests.Integrity.MainPcSealProviderGateTests` → run·SHA |
| **P-6** | 🚨 **`되돌리기에 실패했다` 로그 감시** — 위 누수의 **유일한 탐지 수단** |

**접수**: PM 브라운킴 · 2026-09-24 · 다음 [5] CTO 1차 결재

---

## §14 [5] CTO 조건부 승인 — PM 조건 처리 (2026-09-24 · 브라운킴)

결재문: `docs/운영기록/20260924_결재문_메인PC봉인_공급자불일치_봉합.md` (CTO 래리 · 🟡 조건부 승인 A-1~A-6)

### 🔴 CTO 가 새로 찾은 터질 자리 2건 — PM 이 못 본 것

| # | 무엇 | PM 처리 |
|---|---|---|
| **L-1** | TPM 자리 3칸이 고아 키로 차면 그 PC 는 **등록 영구 불가**인데 화면은 *"잠시 후 다시 시도해 주세요"* 라고 **거짓 안내** | 🟢 **A-1 정정 완료**(아래) |
| **L-2** | 🚨 **되돌리면 M-12 가 되살아난다** — 옛 판이 새 봉투를 못 읽어 `DifferentPc` → 고객이 [이 컴퓨터로 바꾸기] 를 누르면 `4a009013:MainPcProofService.cs:235 _tpm.SealKey` = **세금계산서 키 덮어쓰기 재점화** | 🔴 **A-3 — 되돌림 금지. 전진 수정만.** 아래 |

### 🔴 A-3 — 되돌림 방침 변경 (§12 정정)

§12 에서 나는 *"실측에서 실패하면 되돌린다. 게시 전 manifest 백업 주소를 남긴다"* 고 적었다.
🔴 **되돌리면 안 된다.** 되돌림이 **M-12 를 재점화**한다(L-2). 봉투를 비워도 [등록] 버튼이 같은 곳으로 간다.

⇒ **확정 방침**: 게시 후 실측이 실패하면 **되돌리지 않고 앞으로 고친다**(전진 수정 · 1.3.45).
⇒ manifest 백업은 **여전히 남긴다**(다른 사고 대비) — 🚫 **다만 이 건으로는 쓰지 않는다.**
⇒ 🔴 **사장님께 사전 고지 의무** — 게시 승인 전에 *"문제가 나와도 되돌리지 않는다"* 를 말씀드린다.

### 🟢 A-1 — 문구 정정 완료 (PM 직접 · CTO 허용)

`src/HitPan.Application/Services/Security/MainPcSealService.cs:377`
```
종전: "자료보관 컴퓨터 인증 자리가 모두 차 있습니다 — 잠시 후 다시 시도해 주세요."
정정: "자료보관 컴퓨터 인증 자리가 모두 차 있습니다. 기다려도 저절로 풀리지 않으니 히트판에 문의해 주세요."
```
🔴 **기다려도 안 풀리는데 기다리라고 말하는 것이 가장 나쁘다.** 고객은 영원히 다시 시도만 한다.
⚠️ 문자열 1줄 · **동작 무변** · 고객 노출 글이라 내부 용어 없음(#23 정신).
⇒ **[4] 는 diff 1줄만 확인하면 된다**(CTO 지정).

### 🔴 A-4 — 세금계산서(O-1·O-4)를 **오늘 같이 상신한다**

CTO 판정: *"O-1·O-4 는 남길 것이 아니다 — 이번 게시 뒤 그 경로가 그 금고를 안 건드리므로 **이미 덮였다면 영영 조용해진다**."*
🔴 **맞다.** 이번 봉합은 *"앞으로 안 덮는다"* 일 뿐 *"이미 덮였는지"* 는 **아무도 모른다.**
⇒ 별건 상신: `docs/운영기록/20260924_상신_세금계산서봉인_피해범위확인_별건.md`

### 🟡 A-2 · A-5 · A-6

- **A-2** 고아 키 청소 — **별건 후속**(D-4 와 함께 §13 에 기록됨)
- **A-5** 문서 정정 — D-1~D-3 완료(개발) · L-1·L-2 는 본 절에 기록 · **D-4 에 번호·주인 부여**: N-1·N-2·P2-5·1062미측정·A-2 고아키 ⇒ **다음 세션 PM 이 별건 작업지시서로 묶는다**
- **A-6** 게시 후 측정 **P-1~P-7 확정** — §13 의 P-1~P-6 + **P-7 `되돌리기에 실패했다` 로그 감시**(누수의 유일한 탐지 · CTO 조건)

### 🚫 실측 전 "완료" 금지

게시 후 P-1~P-4 가 확인되기 전까지 이 건은 **미완**이다. 🚫 어떤 문서·보고에도 *"실물까지 끝났다"* 고 쓰지 않는다.

**처리**: PM 브라운킴 · 2026-09-24 · **A-1 🟢 · A-3 🟢 기입 · A-4 🟢 상신** ⇒ 사장님 결재 상신 가능
