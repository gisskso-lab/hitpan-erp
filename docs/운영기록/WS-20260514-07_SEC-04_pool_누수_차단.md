# WS-20260514-07 — SEC-04 SET SESSION connection pool 누수 차단

**발행:** 2026-05-14 (수) / PM
**결재:** 사장님 (임원 합의)
**담당:** 백엔드 매니저 + 백엔드개발자 1
**헌법:** #15 (silent swallow 금지), #20 (워크플로우 끊김 0)
**마감:** 5/14 11:00

---

## 배경

- **현상**: `MdbMigrationService.cs:520-557` — `RestoreMigrationSessionTuningAsync`가 catch에서 `_logger.LogWarning` 후 **swallow**. 원복 실패 시 connection이 `foreign_key_checks=0` / `unique_checks=0` / `innodb_flush=2` 상태로 pool에 반환됨
- **위험**: 다음 비-마이그 컨트롤러 (PartnerController·CollectionController 등)가 같은 connection 재사용 시 FK 우회 INSERT 통과
- **헌법 위반**: #15 (silent swallow), #20 (워크플로우 끊김 — 원장 무결성 무방비 깨짐)
- **CTO·설계팀장·보안팀장·본부장 만장일치 위험 평가**

## 봉합 범위

1. `RestoreMigrationSessionTuningAsync` catch swallow 제거
2. `try/finally` 블록 구조로 강제 원복
3. 원복 실패 시 **connection Dispose 강제** (pool 반환 차단) — 설계팀장 보강안 반영
4. 결과: 원복 실패 시 connection은 풀에 안 돌아감 = 다음 컨트롤러 오염 0

## 구현 위치

- 수정: `src/HitPan.Application/Services/MdbMigrationService.cs` L:520-557
- `MigrateCoreAsync` finally 블록 (L:310-314) 보강

## 검증 게이트

- [ ] errors 0 + warnings 0
- [ ] CodeQL SAST 통과
- [ ] 단위 테스트: 의도적 SET SESSION 실패 시뮬레이션 → Dispose 호출 확인
- [ ] 통합 테스트: 마이그 후 다른 컨트롤러 INSERT 시 fk_checks=1 확인 SHOW

## 산출물

- 봉합 커밋 1건
- 단위 테스트 2건
