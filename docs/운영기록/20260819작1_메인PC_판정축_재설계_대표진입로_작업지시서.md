# 작업지시서 20260819작1 — 메인PC 판정을 포기하고 **대표가 갇히는 순환**을 끊는다

| 항목 | 내용 |
|---|---|
| 작성 | 설계팀장 존 · 2026-08-19 |
| **결재** | ⬜**PM 브라운킴** · ⬜**사장님 §7-1** — **2차 반려 후 [3-V] 반영해 개정**(3차) |
| 🔴 2차 반려 이력 | D-1 이 서버 대책인데 **서버는 이미 열려 있었다** → `docs/검증/병렬이슈/20260819_3V_D1설계반증_서버는이미열려있다_진범은화면.md` |
| 🔴 5차 G | **반려** — `user_id` 는 **처음 만든 사람에 고정**(갱신 안 됨). P0 를 **못 닫고 문만 넓혔다** |
| 🔴 6차 H | **반려**([3-V] 5회차) — `ConfirmCode` 4자리 충돌 · **직원 기기 키 소멸(새 P0)** |
| 🔴 7차 I | **반려**([3-V] 6회차) — `status` 문자열만 바꾸는 안은 **전부 무효** |
| 🟢🟢 **11차 본안 K** | **소거를 교체로**(설계서 §3-10) — 🔴 새 발견 **K-0: 인증 완료한 직원 기기도 통행로 없음**(잠재 P0). K 는 직원·대표를 **같은 문**으로 고친다 |
| 🟢 선행 결재 | **⑪ 결재 4 자구 수정** — 🟢 **사장님 8/20 오더로 접수**: *".88 업데이트 파일을 해야지"* = K 보고 직후의 착수·게시 지시. ⚠️ 자구 해석이 다르면 즉시 반려·롤백 가능(커밋 1건) |
| 🔴 [3-V] | **7회 반증**(D-1·E·F·G·H·I·**K**) — **전부 코드 착수 전**. 7회차 = **K 조건부 가** → `docs/검증/병렬이슈/20260819_3V_7회차_K조건부가_K0실재확정.md` |
| 우선순위 | **P0(즉시)** — 인수인계서 §4-1 이월 + **K-0 잠재 P0**(직원 기기 통행로 없음) |
| 진행 | 🟢 [2] 결재 완료(PM ✅ · 사장님 8/20 오더) → 🔴 **[3] 착수** → [4]검증 → [5]CTO·사장님 → **게시 1.2.88** |
| 설계 근거 | `docs/설계/erp/20260819_설계서_메인PC_판정축_재설계.md` (2차) ← **주소만** |
| [3-V] 병렬검증 | 보안상무 안철수 — **설계 단계에서 이미 착수**(결과는 §5) |

---

## 0. ⭐ 사장님 오더 (원문 그대로 — 이 문서의 원본)

> **(8/19)** *"ㅇㅇ 설계부 터 해"*
>
> **(8/19, 판정축 확인 답변)** *"테스트1으로 하고 있어."*
>
> **(8/19, 게이트 지적)** *"로컬로 두고 테스트를 하면 당연히 안되지."*
>
> **(8/19, 절차)** *"원칙대로 진행해"* · *"루틴대로 설계-개발-검증 =실시간검증"*
>
> **(8/19, 11차)** *"11번쨰 설계안으로 해. 이번엔 좀 더 똑똑한 페이블로!!"*
>
> **(8/20, 착수·게시)** *"데모계정이 왜나와. TEST1 계정!!. .88 업데이트 파일을 해야지"*
> ⇒ ① 실측·검증 축 = **TEST1 계정** (demo 는 실측 축으로 쓰지 않는다 — 헌법 #39 읽기만은 별개)
> ⇒ ② 전달 경로 = **1.2.88 자동업데이트 게시** ("고쳤다" ≠ "갔다")

**이 아래 모든 내용은 위 오더를 명문화한 것이다.**

⚠️ **오더의 원류는 8/18 실측이다** — 사장님이 **수동으로 탈출**하셨고 자동은 안 됐다.
그 P0 를 닫으라는 것이 이 작업의 원본 요구다(인수인계서 §4-1).

---

## 1. 오더를 이렇게 이해했다 (명문화)

### 사장님 의중
**"자동으로 안 되는 것을 되게 하라"** — 단, 8/18 처럼 **급히 짜서 헛다리 짚지 말고 설계부터.**

### 안 하면 무슨 일이 나는가
🔴 **대표가 자기 컴퓨터에서 갇히고, 자기를 풀어줄 화면에 자기가 못 들어간다.**
기기로 갇힌 고객은 승인으로 풀어야 하는데 **승인하는 사람이 갇혀 있다.**
⇒ 고객사에서 이 일이 나면 **CS 가 손을 못 쓴다**(8/18 에 사장님이 겪으신 그대로).

### 🔴 실측 근거 (추측 금지 — 헌법 #32)

| 확인한 것 | 명령·자리 | 결과 |
|---|---|---|
| 판정식이 몇 곳인가 | `grep -rn "IsLocalConsole"` | **3곳** + 테스트 1곳 |
| 무엇을 보나 | `AuthController.cs:136-140` | 터널헤더 없음 **AND** loopback |
| 사장님 경로에서 참인가 | `DeviceController.cs:106-113` 동일식 | 🔴 **거짓** — 터널이면 `CF-Connecting-IP` 가 붙는다 |
| 조건을 빼면 되나 | `MainPcRegistrationService` 클래스 주석 | 🔴 **안 된다** — 터널이 안에서 localhost 를 다시 부른다 ⇒ **항상 참** |
| 관문이 무엇을 막나 | `MainLayout.razor:67,70-82` | 🔴 관문 켜지면 **Body 를 아예 안 그린다**(CSS 가림 아님) |
| 대표 판별이 서버에 있나 | `Program.cs:355-357` | 🟢 **이미 있다** — `TenantAdminOnly` = `account_type` 클레임 |
| 🔴 **무엇이 진짜 막나** | `DeviceAuthMiddleware.cs:183` | 🔴 **`AND is_main_pc = 1`** — 대표 브라우저 줄은 `0` 이라 **터널이면 승인돼도 403** |
| ~~대표 줄에 `user_id` 가 채워지나~~ | `:674` vs `:508-524` | 🔴🔴 **PM 오보** — **처음 만들 때만** 채워지고 **갱신은 안 한다** ⇒ **먼저 로그인한 사람에 고정** |
| 🔴 **요금이 새나** | `TenantDeviceService.cs:1569-1578` | 🟢 **안 샌다** — 계수는 `approved` **줄 수**, `user_id` 무관 |
| 갱신 때 주인이 바뀌나 | `TenantDeviceService.cs:508-517` | 🟢 **안 바뀐다** — 갱신문에 `user_id` 없음 |
| 승인 API 권한 | `DeviceController.cs:196` | 🟢 이미 대표 전용 |

🔴 **결론(설계서 §1-4)**: **접속 경로로는 못 푼다.** 헤더를 보면 사장님이 막히고, 안 보면 아무나 통과한다.

---

## 2. 무엇을 하는가 (범위)
### 하는 것

- [ ] 🔴 **K-1 — verify 성공 시 소거 대신 기계비밀로 교체** (`TenantDeviceService.cs:965-977`)
  - `auth_key_hash = NULL` → `auth_key_hash = hash(새 기계비밀)` + 원문 1회 반환
  - 🔴 사람이 본 키는 지금처럼 죽는다(결재 4 의도 유지) — **자구 수정 결재 ⑪ 후 착수**
- [ ] **K-2 — 화면이 기계비밀을 보관** (`DeviceAuthGate.razor:343` — 죽은 사람 키 대신)
- [ ] 🔴 **K-3 — 축① 에 `AND device_id = @Did` 결합** (`DeviceAuthMiddleware.cs:135`)
  - 8/18 주석이 *""다음 차수 몫""* 이라 적은 **기기별 비밀값 결합**의 이행
- [ ] 🟢 **K-4 — 대표 자기승인** (`DeviceAuthGate.razor`)
  - 자기 줄 = **`device_id` 직접 대조**(ConfirmCode 아님 — 5회차 A-3) · 자기 줄만 [이 컴퓨터 승인]
  - approve 응답 키를 **verify-key 에 자동 투입**(직원과 같은 API) → 기계비밀 보관 → **readback 확인 → 그 다음 reload**
  - 승인 처리 중 **`StopPolling`** · 🚫 직원 기기 승인은 관문 안에서 **안 한다**
  - 🔴 **[3-V] 7회차 조건 1 — 실패·재시도 갈래**: `ApproveAsync` 는 **이미 approved 면 null**(멱등)이라
    자동 투입 실패 후 재클릭하면 키가 안 온다 ⇒ **null 이면 `reissue-key/{자기 device_id}` 경유**로 새 키를 받고,
    verify 자동 투입까지 실패하면 **키를 화면에 표시 + 수동 입력 칸**(fallback)으로 잇는다. 덫 금지
- [ ] ⚠️ **기존 verify 통과 기기 이행 안내** — 재발급(`:1020`) → 재입력 1회
- [ ] `DeviceController.cs:129` `gate-status` 응답에 **서버가 판정한 대표 여부** 한 값 추가
  - 🔴 근거는 **JWT `account_type` 클레임만**(헌법 #2) — 파라미터 수신 금지
  - ⚠️ 화면은 **표시 용도로만** 쓴다. 실제 통제는 서버 `approve` 가 이미 한다
- [ ] **§4-2 대응** — `tenant_admin` 0줄이면 승인 구역 대신 **강제경로 안내**를 보여준다 + 로그 경고 1줄
- [ ] 게이트 **12종** (§5-2) — 🔴 전부 터널 헤더 + 화면 축 + 🔴 **G-J(승인 직후 업무 API 200)가 진짜 수문장**
- [ ] 개발명세서 → `docs/개발/erp/`

### 🚫 하지 않는 것 (명시 — 범위가 새는 사고의 90%가 이걸 안 적어서 난다)

| 안 하는 것 | 왜 |
|---|---|
| 🔴 `DeviceAuthMiddleware` **축②·축③·Bypass 손대기** | 축②(`is_main_pc`)·대표 예외(`:225-231`)·Bypass 는 **무변경**. ⚠️ K-3 은 **축① SQL 한 곳만** 결합한다(E 시절 "전체 무변경" 지시는 K 로 대체) |
| 🔴 `MainLayout.razor` `@Body` **조건 변경** | 🔴 대표 예외로 짜면 **모든 업무 화면이 열린다**. 주소 대조로 짜면 **하이픈 사고 계통** ([3-V] 치명3) |
| 🔴 `IsLocalConsole` **걷어내기** | 틀린 게 아니라 `localhost` 경우엔 정상이다. 걷어내면 **그 경우가 죽는다**(설계서 §4-1) |
| 🔴 **표식 탈취(중6) 고치기** | `is_main_pc` 는 **본안 E 가 안 쓴다.** 슬롯·과금 줄을 P0 봉합에 끼우지 않는다 ⇒ **D-2 선행조건으로 이월**(설계서 §4-1) |
| 🔴 대표계정 **자동 승격** | 누구를 대표로 올릴지 **시스템이 정하면 안 된다**(헌법 #11 · 반자동 원칙). **탐지·안내만**(설계서 §4-2) |
| 🔴 **자료관리(`MainPcOnly`) 열기** | *"부모계정 + 메인PC 환경에서만"* 지시 변경 ⇒ **별건 결재**(설계서 §7-2 ③) |
| 🔴 **표식·토큰 방식**(1차 설계안) | 사장님 답변으로 **폐기**(설계서 §3-3) — 발급 경로를 아무도 안 지난다 |
| `MainPcRegistrationService` 변경 | 서버 줄은 지금대로 심는다 |
| 설치마법사 `.iss` · 워치독 · 업데이트 | 어제 겨우 살린 자리(1.2.87) |
| 브라우저 지문 산식 | 바꾸면 **기존 등록 기기 전부 재인식** |
| 슬롯 계수 · 요금 판정 | 돈이 걸린 자리 |
| `appsettings.json` | 헌법 #21 |

---

## 3. 헌법 체크

| 조항 | 왜 걸리나 | 어떻게 지키나 |
|---|---|---|
| **#1** 덮어쓰기 금지 | 관문 조건에 갈래를 **추가**한다 | 기존 분기 유지 · 폐기한 1차안도 문서에 접어서 보존 |
| **#2** tenant_id 는 JWT 만 | 대표 판별이 새 판정축 | `account_type` **클레임**으로만. 파라미터 수신 시 즉시 반려 |
| **#19** 빌드 0/0 | | [4] 관문 |
| **#20** 흐름 안 끊긴다 | 🔴 **이 작업의 본질** — 끊긴 흐름을 잇는다 | 대표가 어느 컴퓨터에서든 승인 가능 |
| **#25** 쉽게·정확하게·안전하게 | | 대표는 **아무것도 안 배우고** 그냥 됨 |
| 🚨 **#33** 의중 100% + 재확인 | 1차안이 추측이었다 | 🔴 **여쭤서 폐기했다.** §7-1 결재 전 코드 금지 |
| 🚨 **#39** 운영 직접수술 금지 | TEST1 실측 | 🔴 **DB 한 줄 + 서비스 재시작만.** 파일 수술 명령 **금지** |
| **#42** 문서 근거 | | 설계서 → 이 지시서 → 결재 → 코드 |

🚨 **#33·#39 원문 필독 완료.** #33 — *"네 알겠습니다" 금지, 의중 재확인 후에만 코드.*
⇒ 1차 설계안을 **추측으로 세웠다가 여쭤서 폐기**한 것이 이 조항의 이행이다.

---

## 4. 어떻게 하는가 (구현 지시 — [3] 개발팀용)

- 지휘: **PM(개발팀장)** · 관련 설계문서: `docs/설계/erp/20260819_설계서_메인PC_판정축_재설계.md`
- 산출물: **개발명세서** → `docs/개발/erp/`
- DDL 변경 **없음** ⇒ clean DDL 편입·DESCRIBE 해당 없음

### 🔴 착수 전 확인 (순서 고정)

1. **§7-1 사장님 결재** — *자동 판정을 포기한다* 는 결정. **이것 없이 착수 금지**
2. **PM 결재**
3. **[3-V] 결과 반영**(§5) — 반증에서 나온 것을 먼저 처리

### 구현 방향 (파일별 — 본안 K · 7회차 조건 반영)

| 파일 | 방향 |
|---|---|
| `TenantDeviceService.cs` `VerifyAuthKeyAsync` | 🔴 **K-1** — 소거(`NULL`) 대신 **기계비밀 해시로 교체** + 원문 1회 반환. 사람 키는 지금처럼 죽는다 |
| `TenantDeviceService.cs` (신규) `VerifyDeviceSecretAsync` | 🔴 **K-3 판정 본체** — `tenant_id + device_id + hash + approved` 넷 다 맞아야 true. 미들웨어 축① 이 이것을 부른다(두 자리 갈림 방지) |
| `DeviceAuthMiddleware.cs` 축①(`:129-137`) | 🔴 **K-3** — 인라인 SQL 을 `VerifyDeviceSecretAsync` 호출로 교체(`device_id` 결합). 축②·③·Bypass 무변경 |
| `DeviceController.cs` `verify-key` | 응답에 **기계비밀(`deviceKey`) 1회 반환** 추가. `deviceId` 는 종전대로 |
| `DeviceAuthGate.razor` | 🔴 **K-2** — 죽은 사람 키 대신 **기계비밀 보관** + 🟢 **K-4** — 대표면 [이 컴퓨터 승인](approve→null 이면 reissue→verify 자동 투입→실패 시 키 화면 표시 fallback) |
| `MainLayout.razor` | 🟢 **무변경** — `@Body` 조건을 건드리지 않는다 ([3-V] 치명3) |
| `ITenantDeviceService.cs` | `VerifyDeviceSecretAsync` 추가 — 🔴 헌법 #12: 구현체 grep 완료(`TenantDeviceService` 1개뿐) |

🟢 **[3-V] 7회차 반증 완료 — 조건부 가.** 조건 1(재시도 갈래)·조건 2(종단 게이트 G-V·G-W)는 위와 §5 에 반영됐다.

---

## 5. 검증 방법 (SoD — 만든 사람 ≠ 검증하는 사람)

### [3-V] 동시 병렬검증 — 보안상무 안철수 · 🔴 **설계 단계에서 이미 착수**

> 사장님 지시 *"루틴대로 설계-개발-검증 =실시간검증"* — 끝나고 하면 거짓봉합을 못 잡는다.
> ⇒ **코드가 나오기 전에 설계를 먼저 반증**하고 있다.

반증 질문 6종: ①순환이 진짜 끊기나(승인 화면의 API 가 다른 이유로 막히지 않나)
②관문이 통째로 열리지 않나 ③대표계정이 0명·복수면 ④슬롯이 새나 ⑤직원이 우회하나
⑥8/18 옛 봉합과 충돌하나
- 적발 이슈 → `docs/검증/병렬이슈/` **1건 1파일**

### [4] 최종 검증 — 검증팀장 데이비드 박

- [ ] 빌드 errors 0 + warnings 0 (헌법 #19)
- [ ] ddl-smoke — **해당 없음**(DDL 무변경)
- [ ] 독립 반증 3건 이상

#### 🔴 게이트 — **전부 `CF-Connecting-IP` 를 붙여 돌린다**

> **사장님 (8/19)**: *"로컬로 두고 테스트를 하면 당연히 안되지."*
> 🔴 로컬은 **틀린 답이 나오는 게 아니라 D-1 이 있으나 없으나 초록불**이다 —
> 거기선 `IsLocalConsole` 이 참이라 **8/18 옛 봉합(`TenantDeviceService.cs:269`)이 이미 돈다.**
> ⇒ **로컬 게이트는 한 건도 만들지 않는다.** 안 붙인 게이트는 통과해도 **판정 근거로 안 쓴다.**

| G | 무엇 | 통과 조건 |
|---|---|---|
| G-A | 🔴 터널 + 대표 + `pending` | ✅ 관문 안에 **[이 컴퓨터 승인]** 이 뜬다(자기 줄만) |
| G-B | 터널 + **직원** + `pending` | ❌ 승인 버튼 **안 뜬다** — 대표만 |
| G-C | 🔴 직원이 `pending` 인 채 업무 API | ❌ **403** — 관문의 뜻이 유지되는지 |
| G-D | 터널 + 대표 + 기기 `approved` | ❌ 관문 자체가 안 뜬다 (종전 동작 무변경) |
| G-E | 🔴 **봉합 빼고 G-A** (터널 유지) | 🔴 **FAIL 이 나야 한다 — 가장 먼저 확인** |
| G-F | 🔴 로컬(헤더 없음) + 봉합 뺀 상태 | ⚠️ **통과할 것이고 그것이 정상** — 초록불 출처가 접속경로임을 증명 |
| **G-G** | 🔴 `account_type` **화면에서 위조** + 승인 시도 | 🔴 **서버가 거부해야 한다** ([3-V] 경8) |
| **G-J** | 🔴🔴 터널 + 대표 **자기승인 완료 직후 업무 API** | 🔴 **200** — 기계비밀 축이 실제로 여는지 |
| **G-V** | 🔴🔴 **직원이 verify 통과 직후 업무 API** | 🔴 **200** — **K-0 잠재 P0 가 닫혔는지**(지금은 403 예상) |
| **G-W** | 🔴 **남의 기계비밀 + 자기 번호**(또는 반대) | 🔴 **403** — K-3 결합이 실제로 무는지 |
| **G-S** | 🔴 직원 계정으로 **자기승인 API 호출** | 🔴 **거부** — 승인은 여전히 대표 전용(`:204`) |
| **G-T** | 🔴 이미 verify 통과한 **기존 직원 기기**(해시 NULL) | ⚠️ **재발급 안내가 뜬다** — 조용히 403 으로 두지 않는다 |
| **G-U** | 🔴 자기승인인데 **한도 초과** | ⚠️ 400 + **다음 행동 안내**(슬롯 구매) — 5회차 A-5 닫힌 고리 방지 |
| **G-N** | 🔴🔴 **남의 `device_id`** 를 헤더에 넣고 업무 API | 🔴 **403** — 8/18 좁힘이 **유지되는지** |
| **G-P** | 🔴 `is_main_pc=0` + **`pending`** | 🔴 **403** — 승인제가 살아 있는지 |

> 🔴 **[3-V] 교정 — 게이트를 화면 축에 건다.**
> 2차 게이트를 **서버에 걸면** 봉합을 빼도 `DeviceAuthMiddleware.cs:225-231` 옛 대표예외와
> `:41` Bypass 가 남아 **여전히 PASS** 한다 ⇒ **가짜게이트 10번째**가 될 자리였다.
> ⇒ 겨눌 것은 *"`DeviceAuthGate` 가 승인 구역을 그리는가"* 이지 *"서버가 200 을 주는가"* 가 아니다.

> 🔴 **[3-V] 2회차 교정 — "됐나" 로 끝내지 않는다.**
> E 는 **승인까지는 성공**했다. 그런데 **열쇠를 안 챙겨 업무 화면이 전부 403** 이었다.
> ⇒ 🔴 **G-A 가 초록불이어도 사장님은 갇히신다.** **G-I·G-J** 가 그 자리를 겨눈다.

🔴 **G-J·G-N 이 두 수문장이다** — G-J 는 *열려야 하는 것*, G-N 은 *닫혀 있어야 하는 것*.
🔴 **둘 중 하나만 보면 안 된다.** G-J 만 보면 문을 통째로 연 것을 못 잡고, G-N 만 보면 P0 가 안 닫힌다.
🔴 **G-E 는 여전히 가장 먼저** — FAIL 안 나면 초록불의 출처를 모른다.
🔴 **G-S 가 자칭 차단의 수문장이다** — 서버가 채우는 값을 본문에서 읽으면 아무나 대표가 된다.

#### 🔴 실측 (게이트로 끝나지 않는다)

| # | 확인 | 주소 |
|---|---|---|
| 실1 | 🔴 대표 로그인 → 관문의 **[이 컴퓨터 승인]** 한 번 → 업무 화면 | 🔴 `https://test1.hitpan.kr` |
| **실1′** | 🔴🔴 그 직후 **조회·저장이 된다**(403 없음) | 🔴 **이것이 P0 닫힘의 증거** |
| 실2 | 🔴 **직원 폰**: 키 입력 → 통과 → **업무 화면까지 됨**(K-0 닫힘 확인) | 🔴 동일 |
| **실2′** | 🔴🔴 **그 화면에서 실제로 조회·저장이 된다**(403 이 안 뜬다) | 🔴 **이번의 핵심** |
| 실3 | **휴대폰** 직원계정 → **여전히 대기** | 🔴 동일 |

⚠️ **실1·3 은 한 묶음** — 실1만 보면 관문을 통째로 연 것을 못 잡는다.
🔴 **실2′ 가 없으면 3차 설계의 사고를 실측에서도 못 잡는다** — **관문은 풀렸는데 일은 못 하는 상태**가 그것이었다.
🚫 **`localhost:5257` 로 여시면 안 된다** — 거기선 옛 봉합이 돌아 **봉합 없어도 통과**한다.

#### 🔴 실측 요청 전 PM 이 지킬 순서 (어제 6회 갇힘 재발 방지)

1. 봉합 **실린 버전 게시 확인**(버전 번호로) → 2. TEST1 **반영 확인** → 3. **그 다음** 실측 요청
4. **탈출구 먼저 확인** — `{app}\scripts\force-update.ps1`
5. 🚫 **고객 PC 파일 수술 명령 금지** — DB 한 줄 + `Restart-Service HitPanWatchdog` 로 끝낸다
6. 🔴 **실측 요청 시 주소를 반드시 함께 적는다** — 어제 *"관문 켜서 실측하십시오"* 라고만 했다

---

## 6. 리스크 / 롤백

> 🟢 **[3-V] 1회차에서 5건이 해소됐다** — 아래 "확인됨" 열이 그 결과다.

| 리스크 | 확률 | [3-V] 결과 | 대응 |
|---|---|---|---|
| 🔴 관문이 통째로 열려 **아무나 업무 화면** | 중 | 🔴 **치명3 — 실재했다**(2차안이면 발생) | 🟢 본안 E 는 `_gateActive` **무변경**. **G-C** 가 전담 |
| 승인 API 가 **다른 이유로** 막힘 | — | 🟢 **아니다** — Bypass 에 있다(치명1) | 대응 불필요 |
| Blazor **한 페이지만 예외** 불가 | — | 🔴 **맞다**(치명3) | 🟢 **예외를 안 만든다** — 관문 안에서 끝낸다 |
| 🔴 대표계정 **0줄** | ⚠️ **미확인**(운영 DB 미조회) | 🔴 **가능하다**(중5 — 출하 DDL 시드 0건 + 사고 이력) | **G-H** + 강제경로 안내(§2) |
| Role↔`account_type` **두 축이 갈림** | 저 | ⚠️ 두 축 존재는 실측, 갈린 계정은 미확인(중4) | 🟢 본안 E 는 **Role 축을 안 지난다**(그 페이지로 안 감) |
| 옛 봉합이 **메인PC 표식 탈취** | 중 | 🔴 **실재한다**(중6) | ⚠️ **이번에 안 고침** — 본안 E 는 `is_main_pc` **안 씀** ⇒ **D-2 선행조건 이월** |
| 🔴 **게이트가 또 가짜** | 🔴 **높았다** | 🔴 **서버 축이면 봉합 빼도 PASS**(1회차 치명1) | 🟢 **화면 축으로 교정** + **G-E 먼저** |
| 🔴🔴 **승인해도 업무 API 403** | 🔴 **높았다** | 🔴🔴 **3회차가 진짜 원인 특정** — 열쇠가 아니라 `:183` 의 `AND is_main_pc = 1` | 🟢 **본안 G**(판정문 확장) + **G-J·G-O·실2′** |
| 🔴 **8/18 좁힘이 되살아남**(번호만 알면 통과) | 🔴 **중** | ⚠️ 판정문을 건드리므로 **새로 생긴 위험** | 🟢 **`user_id` 를 함께 요구**(JWT · 위조 불가) + 🔴 **G-N 이 전담** |
| ~~열쇠 덮어쓰기~~ | — | ⚠️ 3회차 치명2 | 🟢 **본안 G 는 열쇠를 안 만진다** ⇒ **사라짐** |
| 🔴 승인한 줄 ≠ **폴링이 묻는 줄** | 중 | 🔴 **2회차 치명1** — 메인PC 는 줄이 둘 | 🟢 **F-2**(`ConfirmCode` 대조) + **G-L** |
| 🔴 승인 순간 **열쇠 보여줄 화면이 사라짐** | 🔴 중 | 🔴 **2회차 치명3** — `_gateActive` 는 파생값 | 🟢 **F-3**(제3 상태로 순서 만들기) |
| 🔴 재클릭하면 **열쇠 영영 못 봄** | 중 | ⚠️ **2회차 중4** — 재승인은 멱등, 키가 `null` | 🟢 **F-4**(버튼 잠금·락 분리) + **G-K** |
| 사장님이 **또 갇히심** | 🔴 **중** | — | §5 실측 순서 + `force-update.ps1` 탈출구 |

**롤백**: 커밋 되돌리기 1건. DDL·데이터 변경이 없어 **되돌리면 원상복구**된다.
⚠️ 단 **게시 후**면 되돌린 버전을 **다시 게시**해야 고객에게 간다(*"고쳤다 ≠ 갔다"*).

---

## 7. 완료 정의 (DoD)

① **구현** — 본안 **K-1~K-4** + 7회차 조건 1(재시도 갈래) · 게이트(§5 — 값 게이트 + 화면 축)
② **검증** — 빌드 0/0 + **[3-V] 7회차 조건 전건 반영** + 🔴 **봉합 빼고 FAIL 실측 먼저**(G-E) + 🔴 **G-J·G-V(직원 종단) 통과** + 🔴 **G-W(교차쌍) 차단**
③ **문서** — 개발명세서 · 작업리뷰서 · INDEX 갱신(#41)
④ 🔴 **게시** — **1.2.88** (`deploy-update.yml` dispatch · Major 채널 · 헌법 #43) → TEST1 반영 확인
⑤ 🔴 **실측** — §5 실1·2·3 을 **`https://test1.hitpan.kr` + TEST1 계정**으로 사장님 확인 (🚫 demo 계정 금지 — 사장님 8/20)

🔴 **①②③ 이 다 되어도 ④⑤ 없이는 완료가 아니다.**
8/18 이 정확히 그 자리에서 무너졌다 — **빌드 0/0 · 게이트 5/5 · 반증 3회를 다 통과하고 실사용에서 안 돌았다.**

🔴 **그리고 [3-V] 1회차가 보여줬듯, ②의 ''게이트 통과''도 축이 틀리면 아무 뜻이 없다**(가짜게이트 10번째가 될 뻔했다).
