# [★] 20260922 인계3 — 기기슬롯 재설계 · 메인PC 물리식별

> **이 문서가 이 트랙의 진실원이다.** 9/22 인계1·인계2 는 자료이관 트랙이고, 이 문서와 **다른 일**이다.
> **작성**: PM 브라운킴 · **날짜**: 2026-09-22
> **상태**: 🟢 코드 절A~절F 완료(빌드 0/0) · 🟢 **게이트 시험 붙음 — 순수 13건 통과 · G-3 대조 10/14**(9/22 작3) · 🔴 **실물 실측 미실시** · 🔴 **DB 게이트 5건 로컬 계측 불가**(M-10) · [4] 검증 미착수

---

## 0. 한 줄

> **메인PC 를 IP 로 가리던 것을, 브라우저가 자기 PC 안의 히트판을 직접 두드려 증명하는 방식으로 바꿨다.**
> 코드는 다 섰고 빌드는 0/0 이다. **아직 실물에서 한 번도 안 돌려 봤다.**

---

## 1. 오늘 트랙이 바뀌었다

사장님 오더: *"마이그 테스트 전 잠깐!!! 디바이스 슬롯 작업으로 다시 회귀한다."*

⇒ 자료이관(9/22 인계1·인계2) 은 **착수 전에 멈춰 세웠고**, 기기슬롯 트랙으로 옮겼다.
⇒ 자료이관 트랙은 **아무것도 건드리지 않았다.** 그 문서들은 그대로 유효하다.

---

## 2. 사장님이 보신 증상 3가지와 그 정체

| # | 증상 | 판정 |
|---|---|---|
| ① | 같은 PC·모바일이 기기 목록에 **2~3줄**로 늘어남 | 🔴 원인 확정 — 그런데 **고칠 대상이 아니게 됐다**(§3) |
| ② | 메인PC 인식이 **불안정** | 🔴 **불안정이 아니라 도메인으로 열면 100% 실패**였다 |
| ③ | ② 때문에 **자료관리 메뉴가 안 됨** | 🔴 ②의 결과 |
| ④ | (사장님 추가 실측) **외부 PC 가 메인PC 를 자칭하면 그대로 통과** | 🔴 **보안 결함** |

### ②③ 의 원인 — 코드다, 추측이 아니다

`src/HitPan.API/Security/MainPcOnlyAttribute.cs` 가 `터널 헤더 없음 AND 루프백` 으로 판정했다.
**도메인 접속은 Cloudflare 가 `CF-Connecting-IP` 를 반드시 붙이므로 첫 조건에서 항상 탈락한다.**

🔴 **IP 축은 원리적으로 불가능하다** — cloudflared 가 **그 PC 안에서** 히트판을 다시 부르므로
서버가 보는 소켓 주소는 **외부든 메인PC 든 항상 `127.0.0.1`** 이다.
같은 사무실이면 공인 IP 도 같다(실측 `124.194.59.154`).

### ① 의 원인

`hardware_id`(장비넘버) 칸은 8/15 `DB-103` 으로 만들었는데 **읽고 쓰는 코드가 지금도 0곳**이다.
`AuthController.cs:122` 에 그 사실을 적은 주석이 이미 있었다.

### ④ 의 정체 — 사장님 판정이 핵심이다

`DeviceAuthGate.razor` 의 `JoinServerRowAsync` 가 **접속한 자리를 한 번도 보지 않았다.**
대표 계정이면 집·카페 어디서든 메인PC 줄에 올라탔고, 그 순간 자료관리가 열렸다.

> 사장님: *"이 반자동 원칙은 **메인PC라고 식별한 최초의 컴퓨터에 한한** 거지,
>  그렇게 치면 **모든 PC를 메인PC라고 무단접속 하는 구멍**을 주는 거."* · *"오히려 더 꼬임."*

⇒ **반자동 원칙을 구멍의 근거로 쓴 것.** 원칙의 오용이었다.
⇒ 그래서 설계 방향이 *"사람을 더 잘 묻자"* 가 아니라 **"사람을 안 묻는다"** 가 됐다.

---

## 3. 사장님 설계 결재 (2026-09-22 · 전결)

| 항목 | 결재 |
|---|---|
| 기기 슬롯 과금 | **폐기.** "PC 5 + 모바일 3" 없앤다 |
| 기기 수 | **메인PC 1대 / 클라이언트 무제한** |
| 과금 단위 | **등록 계정 수** (동시 접속 아님) |
| 가격 | 5계정 3만 / 10계정 5만 / 1계정 추가 1만. **조합은 고객이 고른다** — *"개발자가 관여할게 아님"* |
| 기기 등록 절차 | **제거.** 조회만 남긴다 |
| 기기 컬럼·코드 | **지우지 않는다** (#1·#37) — 끄고 남긴다 |
| 백오피스 | 부모계정만 → **부모 + 자식계정 수**. 개인정보는 안 올린다 (#18·#22) |
| 계정 한도 | 기본 5계정. 초과는 **본사 문의·랜딩 연결** |
| 작업 범위 | **(ㄱ) ERP 먼저 · 그 안에서 메인PC A안이 선행** |
| 메인PC 식별 | **A안 — 물리적 방법.** *"기기가 직접 인증키를 인식하고 메인PC로 알아보는게 정확함"* |
| 왕복 시점 | **로그인 직후 1회** (D-1) |
| 키 생성·보관 | **최초 로그인 후 [메인PC 등록] 팝업 1회에 만들어 그 PC 에 봉인** (D-3·D-4) |
| 온보딩 화면 | **범위 밖.** 나중에 만들 때 팝업을 그 안으로 옮긴다 (D-6) |
| 팝업 재출현 | **안 뜬다.** 등록 후에는 바로 들어간다 (D-7) |
| 되돌리기 | 🔴 **필요하다.** 새 컴퓨터로 이전 (D-9) |
| 자료 이전 | **기존 자료관리(백업/복구)로 고객사가 한다.** 새로 안 만든다 (D-10) |
| 빈 DB | 🔵 **팝업으로 복구하라고 안내** (D-11) |
| 이전 입구 | **[메인PC 연결]** → *"히트판이 설치되어 있습니까?"* → 예=자동연결 / 아니요=설치 후 연결 (D-12) |

🔴 **이 결재로 증상 ①이 증상이 아니게 된다** — 기기로 돈을 안 받으면 줄이 늘어도 고객이 손해 보지 않는다.

---

## 4. 무엇을 만들었나 (절A~절F · 전부 커밋됨)

브랜치 `feat/20260920-rc-safe-path` · origin 동기.

| 커밋 | 내용 |
|---|---|
| `1bc76f7f` | [1-V] 선행검증서 |
| `916b3924` | [2] 설계문서 |
| `d5fa3745` | [2] 작업지시서 + 결재 반영 |
| `f130fbce` | [2] 절F(되돌리기) 설계 추가 |
| `34e55dcc` | [3] 절A·절B — 서버 + CSP·JS |
| `5fa915ae` | [3] 절C·절D·절E — 팝업 · 자료관리 개방 · 자칭 차단 |
| `3c88eb1e` | [3] 절F — 메인PC 연결 · 빈 DB 안내 |

### 설계 한 줄

> **브라우저가 자기 PC 안의 히트판을 두드려 증표를 받아 온다.**
> 받아 오면 메인PC, 못 받아 오면 클라이언트. **고객은 아무것도 하지 않는다.**

```
① POST /api/devices/mainpc-challenge   (도메인)  → 1회용 표 N (60초 · 세션 묶음)
② POST http://127.0.0.1:5257/…/mainpc-proof     → 🔴 터널을 안 지난다 = 진짜 로컬
③ POST /api/devices/mainpc-verify       (도메인) → 결과 + 출입증
```

클라이언트 PC 는 ②에서 끝난다. **두드릴 히트판이 없다.**
API 가 **루프백 전용 바인딩**이라 남의 메인PC 를 두드릴 방법도 없다.

### 파일

| 파일 | 무엇 |
|---|---|
| `src/HitPan.API/Migrations/SQL/DB-126_*.sql` | `mainpc_sealed_key` · `mainpc_key_issued_at` (+ clean DDL 편입 #36) |
| `src/HitPan.Application/Interfaces/IMainPcProofService.cs` | 계약 |
| `src/HitPan.Infrastructure/Services/MainPcProofService.cs` | 표·출입증·봉인·등록 |
| `src/HitPan.API/Controllers/DeviceController.cs` | API 4개 |
| `src/HitPan.API/Security/MainPcOnlyAttribute.cs` | 🔴 판정 **두 갈래로 분리**(§6-1) |
| `src/HitPan.API/Program.cs` | DI 1줄 + CSP 1줄 |
| `src/HitPan.Web/wwwroot/js/hitpan-mainpc-proof.js` | 브라우저의 한 걸음 |
| `src/HitPan.Web/Components/Common/MainPcGate.razor` | 등록·변경·복구안내 팝업 |
| `src/HitPan.Web/Components/Common/DeviceAuthGate.razor` | 🔴 자칭 경로 **차단**(끄되 안 지움) |
| `src/HitPan.Web/Pages/Settings/DeviceManagePage.razor` | [메인PC 연결] |
| `src/HitPan.Web/Services/HitPanApiAuthHandler.cs` | 출입증 헤더 |
| `src/HitPan.Web/Layout/MainLayout.razor` | `<MainPcGate />` |

---

## 5. 🟢 다음 행동 — 사장님이 고르셨다 (2026-09-22 전결)

> **채택: 1번 「게이트 시험 붙이고 → 실측」**

🟢 **게이트 시험은 붙였다** — [`작3 작업지시서`](20260922작3_메인PC물리식별_게이트시험_작업지시서.md)(PM 결재 §11) ·
[`개발명세서`](../개발/erp/20260922작3_메인PC게이트시험_개발명세서.md).
순수 **13건 실행 통과**(2회 연속) · **G-3 대조군 10/14 FAIL 확인** · 빌드 0/0.

🔴 **남은 것은 여전히 실측이다.** 그리고 게이트를 붙이다가 **결재 3건이 새로 생겼다** —
개발명세서 §7 (M-10 DB 게이트 계측 경로 · M-11 DB-126 시드 한 줄 · M-12 TPM 키 이름 공유).

### 그 다음 선택지 (아직 안 고르셨다)

1. **실물 실측** — 🔴 **관리자 권한이 필요하고, UAC 는 사장님이 직접 누르셔야 한다**(§7-6)
2. **[4] 검증에 넘김** — 코드+게이트까지 보고 판정받기
3. **아무것도 안 한다**

---

## 6. 🚨 함정 — 다음 사람이 반드시 알아야 할 것

### 6-1. 🔴 `IsLocalConsole` 과 `IsMainPc` 를 절대 합치지 마라

```
IsLocalConsole   소켓만 본다            ← 왕복 확인(②)이 쓴다
IsMainPc         IsLocalConsole OR 출입증  ← 자료관리가 쓴다
```

합치면 **출입증을 가진 브라우저가 스스로 출입증을 갱신하는 고리**가 생긴다.
한 번 새어 나간 출입증이 **영원히 산다.** 두 파일 모두 주석으로 경고해 뒀다.

### 6-2. 🔴 헤드리스 브라우저로 재면 결과가 **정반대**로 나온다

```
headless: true   → 🔴 "Permission was denied … loopback"   ← A안 불가로 오판한다
headless: false  → 🟢 HTTP 200, 차단 메시지 0
```

**실제로 이 함정에 빠질 뻔했다.** 헤드리스 결과만 보고 *"브라우저가 막는다 = A안 불가"* 로
보고할 참이었다. **반드시 실제 브라우저 창으로 잰다.**
(재현 스크립트 모양은 [1-V] 선행검증서 §4 에 있다)

### 6-3. 🔴 팝업이 두 번 뜨는 것은 **정상**이다 — 없애지 마라

```
새 PC 설치(빈 DB) → [등록] 팝업 1회차 → 자료복구
→ 복원이 옛 DB 를 덮어써 **옛 메인PC 등록이 되살아난다**
→ 봉인 불일치 → [변경] 팝업 2회차 → 재등록
```

④는 버그가 아니라 **복원의 정의**다. *"왜 두 번 뜨지?"* 하고 ⑤를 없애면 **자동 감지가 통째로 죽는다.**

### 6-4. 🔴 봉인키를 파일로 옮기지 마라

`hitpan-keys.conf` 가 아니라 **DB(`tenant_devices.mainpc_sealed_key`)** 에 둔 이유가 있다 —
**자료와 함께 이사해야** *"컴퓨터가 바뀌었다"* 를 알 수 있다.
파일에 두면 복원해도 안 따라오므로 **그 신호가 영영 안 뜨고, 메인PC 를 영영 못 옮긴다.**

### 6-5. 이번에 실제로 밟은 지뢰 3개

- **헤더 이름** — `RegisterMainPc` 가 `X-Device-Id` 를 읽고 있었는데 핸들러는 `X-HitPan-Device-Id` 를 보낸다.
  그대로 뒀으면 등록이 늘 *"기기 정보를 확인할 수 없습니다"* 로만 끝났을 것이다
- **Dapper 튜플** — `ValueTuple` 은 **이름이 아니라 순서**로 채워진다. 컬럼 순서를 바꾸면
  값이 조용히 뒤바뀌고 빌드는 멀쩡하다 ⇒ 명시 클래스로 받게 바꿨다
- **네임스페이스** — `src/HitPan.Infrastructure/Services/` 폴더인데 네임스페이스는 `HitPan.Application.Services` 다.
  같은 폴더의 `TenantDeviceService` 가 그렇다. 혼자 다르게 쓰면 `Program.cs` 가 못 찾는다

### 6-6. `@if (false)` 는 경고를 만든다

Razor 가 미도달 코드를 생성해 `CS0162` 가 난다(#19 경고 0 위반).
`static readonly bool` 필드로 막아야 한다. `const` 도 안 된다 — 컴파일러가 단정해 버린다.

---

## 7. 🚫 건드리면 안 되는 것

| 대상 | 이유 |
|---|---|
| 워치독·업데이트·Major 팝업 흐름 | #43 — **불변.** 이번 작업은 **무접촉**이다 |
| `fingerprint` · `uq_tenant_fp` | #37 — 옛 기기 전부가 이 값으로만 찾힌다 |
| `hardware_id` | 이번 작업은 이 칸을 **안 쓴다**. 기기 식별(증상 ①)은 별건 |
| `is_main_pc` · `main_pc_key` · `uq_tenant_main_pc` | DB-120 의 **1대 보장 장치.** 🟢 덕분에 G-15 가 DB 로 이미 보장된다 |
| `appsettings.json` · `appsettings.Development.json` | #21 |
| 운영 DB 직접 수술 | #39 — 이번 세션은 **읽기·측정만** 했다 |
| `DeviceAuthGate.JoinServerRowAsync` 본문 | 껐지만 **지우지 않았다**(#1·#37). 되살리려면 사장님 결재 |

### 7-6. 🔴 이 PC 의 API 를 손으로 갈아끼우기 전에 반드시 읽을 것

- API 는 **서비스가 아니라 프로세스**로 돈다 (`HitPan.API.exe` PID 는 그때그때 다르다)
- `C:\Program Files\HitPan\api` 는 **읽기조차 거부**된다 — 관리자 권한이 필요하다
- 워치독 설정이 **`RestartTask: ""`** 다 ⇒ **API 가 죽어도 워치독이 되살리지 않는다.** 감지만 한다
- 정식 교체 경로는 **자동업데이트**다 (오늘 19:40 에 실제로 돌았다 — `api`·`web`·`watchdog` 가 그 시각에 바뀌었다)
- 수동 기동 스크립트는 `C:\Program Files\HitPan\hitpan-start.bat`

⇒ 손으로 바꿔 끼우려면 **백업(`api.bak_*`) → 교체 → `hitpan-start.bat` 재기동** 이고,
  **UAC 창은 사장님이 직접 누르셔야 한다.**

---

## 8. 미결 · 미확인

| # | 무엇 | 상태 |
|---|---|---|
| **M-13** | 🟢 **닫힘 — 봉합 + 게이트**(사장님 결재 9/22 「지금 봉합 + 게이트」). 화이트리스트에 `mainpc-proof` **한 줄만** · **G-1c 신설**(음성 대조군 포함 — verify·register·목록·없는 길은 여전히 401) · **C-16 대조군 FAIL 확인** · 전체 회귀 **1551/1551 · 2회 연속** · 빌드 0/0. ⚠️ **봉합이 실측을 대신하지 않는다** — 브라우저가 실제로 loopback 을 두드리는지는 G-1b 의 몫 | 〔발견 당시〕 🚨 **P0 — ②가 `TenantMiddleware` 에서 401 로 잘려 왕복 증명이 실물에서 완성되지 않았다.** 🔴 **실측 확정**(미들웨어를 직접 불러 쟀다): `/api/devices/mainpc-proof` → **401 · 컨트롤러 도달 False** / 대조군 `update-status-local` → 200 · 도달 True. ②는 설계상 **토큰을 싣지 않는데**(`credentials:'omit'`) 화이트리스트에 그 주소가 **없다**. `[AllowAnonymous]` 는 이 미들웨어보다 뒤라 소용없다. ⇒ **모든 PC 가 클라이언트로 판정 · 증상 ②③ 미봉합 · 절A~F 가 길이 막혀 안 돈다.** 🔴 **세 번째 재발**(같은 파일 주석에 작10·작12 로 두 번 적혀 있다). 🔴 **오늘 붙인 게이트 18건이 전부 못 잡았다** — 게이트는 미들웨어를 지나가 보지 않는다. 고침은 화이트리스트에 **그 주소 하나만**(보안 상무 검토 대상) — 개발명세서 §6 별건 ③ |
| M-1 | **실물 실측 (G-1~G-21)** | 🔴 **한 번도 안 했다.** 가장 큰 미결 — 🔴 **M-13 을 먼저 닫지 않으면 실측은 100% ② 에서 실패한다** |
| M-2 | 게이트 시험 코드 | 🟢 **붙였다 (9/22 작3)** — 순수 **13건 실행 통과** · G-3 대조군 **10/14 FAIL 확인** · 🔴 DB 게이트 5건은 **이 PC 에서 계측 불가**(M-10) |
| M-10 | 🟢 **닫힘 — CI 에서 5건 전부 PASS**(사장님 결재 「CI 에 맡긴다」 · run `35740148757` · SHA `2cf53752` · 각 700~790ms · SKIP 0건 · 필터에 물린 것까지 확인). 🔴 **단 C-10~C-13 대조실험은 미실행** | 〔막힌 이유〕 개발 PC 의 `hitpan` 계정에 **CREATE DATABASE 권한이 없다**(`GRANT ALL` 이 `hitpan_erp`·`hitpan_backoffice`·`hitpan_e2e`·`hitpan_trgtest` 4개 이름에만). root 비번은 **사장님만 아신다.** ⇒ 로컬은 `[SKIP]`, **`db-gate` 잡이 유일한 계측 경로**이고 그 잡은 **PR 에서만 깨어난다.** 사장님 결재 대기 — 개발명세서 §7-1 |
| M-11 | 🟢 **닫힘 — DB-126 을 출하 DDL 시드에 넣었다** (사장님 결재 9/22 「지금 고친다」 · 전체 회귀 **1550/1550**) | 〔발견 당시〕 전체 회귀에서 `MigrationLocationGuardTests` 1건 **빨간불.** 칸은 출하 DDL 에 있는데 `schema_migrations` 시드가 **`DB-125` 에서 끝난다** ⇒ 신규 설치 고객이 DB-126 을 **다시 돌린다**(멱등이라 피해는 없으나 게이트는 빨간불). 고침은 시드 꼬리 **한 줄**. 🔴 출하 DDL = 신규설치 단일 진실원(#36)이라 **PM 이 임의로 안 고쳤다** — 개발명세서 §7-2 |
| M-12 | ⚠️ **세금계산서 마스터키와 메인PC 봉인키가 같은 TPM 키 이름을 쓴다** | `TpmKeyService.SealKey` = `CngKey.Create(..., "HitPan.TaxInvoice.MasterKey", OverwriteExistingKey)`. **메인PC 등록이 세금계산서 마스터키를 무효화할 수 있다.** ⚠️ **가설 — 실측 안 함**(실측하려면 이 PC 의 TPM 키를 덮어써야 한다). 별도 선행검증 대상 |
| M-3 | 랜딩 **다운로드 전용 주소** | ⚠️ 못 찾아서 홈으로 보낸다. 확정되면 `DeviceManagePage.GoDownloadPage` 한 줄 |
| M-4 | 팝업을 **부모계정에게만** 띄울 것인가 (D-8) | 🟡 기본값 "부모계정만" 으로 넣었다. 사장님 한 줄이면 바뀐다 |
| M-5 | 팝업 **문구** | 🟡 초안 상태. **사장님 확인 필요.** 개발 용어는 안 썼다 |
| M-6 | 화면·서비스가 **직원과 계정을 한 몸으로** 다루는가 (U-1) | ⚠️ 미확인. DB 는 분리돼 있다(`employees.user_id` NULL 허용) |
| M-7 | CSP 를 **얼마나** 열지 보안 검토 (U-2) | 🟡 주소 2개만 열었다. 보안 상무 검토 대상 |
| M-8 | `hitpan-keys.conf` 접근 권한 (U-3) | ⚠️ 이번 범위 밖으로 뒀다 |
| M-9 | 기기관리 화면 부제가 *"디바이스 개수 과금 — 계정 수는 무제한"* | 🔴 **결재로 뒤집힌 문구다.** 계정 과금 작업에서 같이 고쳐야 한다 |

### 별도 작업지시서로 나갈 것 (이번 범위 밖)

- **계정 과금 전환** (5계정 3만 / 10계정 5만 / 추가 1만 · 등록 계정 수)
- **계정관리 기능 추가** — 직원 ≠ 계정
- **기기 등록 절차 제거 · 조회만 남기기**
- **백오피스 자식계정 카운트** — 개수만 (#18·#22)
- **`hardware_id` 배선** (증상 ① 정리)
- **온보딩 화면**(환영·사용방법) — 만들 때 [메인PC 등록] 팝업을 그 안으로 옮긴다

---

## 9. 문서 (근거 사슬)

```
[1] 사장 오더 (대화)
 ↓
[1-V] docs/검증/선행/20260922_선행검증서_메인PC물리식별_기기슬롯재설계.md   🟢 진실원
 ↓
[2]  docs/설계/erp/20260922_설계_메인PC물리식별_왕복증명.md                 🟢 결재 D-1~D-12
     docs/운영기록/20260922작2_메인PC물리식별_작업지시서.md                  🟢 절A~절F
 ↓
[3]  코드 (커밋 34e55dcc · 5fa915ae · 3c88eb1e)                            🟢 빌드 0/0
 ↓
[4]  🔴 미착수
```

---

## 10. 세션 마감 상태

- 추적 파일 변경 **0건** · 브랜치 `feat/20260920-rc-safe-path` · **origin 동기**
- 빌드 **API 0/0 · Web 0/0**
- CSP 게이트 **5/5 통과**(G-10)
- 🔴 **실물 실측 0회** — 이것이 이 트랙의 현재 위치다
