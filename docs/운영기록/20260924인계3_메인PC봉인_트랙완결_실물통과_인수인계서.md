# [★] 20260924 인계3 — 🟢🟢 **메인PC 봉인 트랙 완결** · 1.3.44 게시 · 실물 통과

> ⬛ 인계1·인계2 는 **흡수**됐다(지우지 않았다 · #41). **이 문서가 트랙 진실원이다.**
> **트랙 = 디바이스슬롯 / 메인PC 등록·감지.** 🟢 **완결.** 남은 것은 전부 **별건**이다.

---

## §0 🔴 다음 세션 첫 행동

### ① 미푸시 커밋 정리 (이것부터)

```
로컬 main 에 39af81e1 이 있다 — 🔴 main 직접 push 는 레포 규칙이 막는다(PR 필수)
브랜치 docs/20260924-mainpc-handoff 로 PR 을 올려 머지한다
```

### ② 본 트랙은 **끝났다.** 새 오더를 받는다

🚫 **아래 별건(§5)을 PM 판단으로 먼저 꺼내지 마라.** 사장님이 고르신다.

---

## §1 🟢 결과 — 한 줄

사장님 증상 *"팝업 메인pc 등록 해도 메인pc로 인식이 안되서 안됨"* 이 **실물에서 해소됐다.**

| P | 무엇 | 결과 |
|---|---|---|
| P-1 | 로그인 시 팝업이 **[다시 인증]** 으로 | 🟢 통과 |
| P-2 | 눌렀을 때 등록 | 🟢 통과 |
| P-3 | **자료관리 화면 열림** ← 증상 그 자체 | 🟢 통과 |
| P-5 | CI 에서 게이트가 실제로 돌았나 | 🟢 통과 |

게시: **1.3.44 · Major** · run `36019755555` 두 잡 success · 피드 `manifest.json` = `1.3.44` · 패키지 HTTP 200 (105,699,315 B) · `releasedAt 2026-09-24T15:26:51Z`

---

## §2 원인 — 다시 안 재도 된다 (확정)

**봉인은 TPM 금고(`MicrosoftPlatformCryptoProvider`), 해제는 소프트웨어 금고(1인자 `CngKey.Exists/Open`).**
같은 PC 인데 키를 못 찾는다 ⇒ **출입증이 발급된 적이 없다** ⇒ 자료관리 403 `main_pc_only`.

PM 동작 실측(Software KSP 에만 키 생성): `Exists(name)`=True · `(name,Software)`=True · `(name,Platform)`=**False**

🔴 **등록은 실패한 적이 없다.** `mainpc-register` 는 200 이었다.

---

## §3 고친 것 (main `a9e4e580` · PR #411)

- `MainPcSealService` 신설 — 키 `HitPan.MainPc.SealKey.{T16}.{GG}` (회사별 + 세대) · 🚨 **`OverwriteExistingKey` 0회**
- 봉투 `HPM2` + 평문 이름표 `tenantId‖deviceId‖난수32` 대조
- 봉인은 `P`(TPM) 또는 `L`(DPAPI LocalMachine)만 · `U` 봉인 없음(해제만 옛 값 호환)
- `rows==0` 이면 **보상 UPDATE** 로 강등을 되돌린다 (누수 2개는 §4)
- **`LegacyTpmSealAdapter` 3인자 ctor 를 `[Obsolete(error:true)]` 로 잠금** — 그 길로 가면 **빌드가 깨진다**(`#pragma` 로도 안 열린다 · 검증자 실측)
- `DeviceController:621-625` 화이트리스트에 `ReRegisterRequired`
- 서버가 `account_type` 을 직접 본다 · 출입증을 **회사 1축**에 결속
- 403 시 1회 자동 재왕복 + 직렬화 + 쿨다운 · `MainPcOnly` 가 **통과 후에만** 자식 조회
- 고객 문구 정정 — 자리가 다 찼을 때 *"잠시 후 다시"* 는 거짓이었다

**게이트 16건 / 시험 21개** · 무력화(#32) **누적 22/22 FAIL-CONFIRMED · STILL-GREEN 0**
회귀 **1553/1553**(건너뜀 0) · 빌드 **0/0**

---

## §4 🔴 남은 누수 (정직하게 · 등급 P2)

C-1 은 **트랜잭션이 아니라 보상 UPDATE** 다.
① 보상 자체가 실패하면 `LogError` 한 줄뿐 ② 강등~보상 사이 **프로세스 사망은 탐지 불가**
🟢 **막다른 방은 아니다** — 줄 0개 → `NotRegisteredYet` → 등록 1회로 자가복구(검증자 경로 추적)
🔴 **탐지는 `되돌리기에 실패했다` 로그뿐**인데 **PM 은 API 로그를 못 본다**(M-23)
🔴 명세서 §8 에 *"트랜잭션이 가능하다"* + **다음 판 권고**가 적혀 있다(D-1 정정 완료)

---

## §5 ⏸ 별건 — 🚫 **PM 이 먼저 꺼내지 마라**

| # | 내용 |
|---|---|
| **A-2** | 고아 키 청소 (TPM 자리 3칸이 차면 그 PC 는 등록 영구 불가) |
| **D-4** | N-1(G-33 판정경로) · N-2(G-34 컴파일) · P2-5(G-27) · 1062 미측정 |
| **O-2** | 출입증 기기·사용자 결속 (지금은 회사 1축만) |
| **O-5** | `L` 갈래 사본 복제 (DB+MachineKeys 동시 복제) |
| **ZAP** | `ZAP Baseline Scan` 빨간불 — 🔴 **9/14 부터 계속** · 이번 변경 무관(1.3.43 도 같은 상태로 게시) |
| ⬛ | 세금계산서 — `docs/운영기록/20260924_상신_...` 🔴 **사장님이 세 번 정리하셨다. 꺼내지 마라.** |

---

## §6 🔴 이번에 PM 이 틀린 것 (다음 사람이 반복하지 말 것)

1. **진단이 절반이었다** — *"전달 경로가 끊겼다"* 로 단정했으나 [1-V] 가 반려했다. 진짜는 그 아래였다.
2. **축을 안 세고 결재했다** — 설계가 *"새 키만 지운다"* 라 답하자 **키 축만 보고 닫았다.** DB 축이 죽는 것을 [4] 가 잡았다. ⇒ *"X 가 산다"* 는 답을 받으면 **X 가 몇 축인지부터 센다.**
3. **순서를 거꾸로 잡았다** — *"실물 전엔 [5] 금지"* 는 게시를 기다리는 고리였다. 사장님이 끊으셨다.
4. 🔴 **트랙을 세 번 벗어났다** — 세금계산서. 사장님 지적 3회. ⇒ **발견이 크다고 그게 1번이 되는 게 아니다.**

---

## §7 함정 (그대로 유효)

- 🚨 **되돌리면 M-12 가 되살아난다** — 옛 판이 새 봉투를 못 읽어 `DifferentPc` → [등록] → 옛 `SealKey`. **전진 수정만.**
- 🚨 **`TpmKeyService.SealKey` 를 부르지 마라** — 이 PC TPM 키를 덮는다. 시험도 부르면 안 된다.
- 🔴 `/api/devices/mainpc-*` 의 **200 은 기기승인 통과의 증거가 아니다**(`BypassPrefixes`).
- 🔴 **403 은 한 종류가 아니다** — `forbidden_device_auth`(기기승인) ≠ `main_pc_only`(메인PC).
- 🔴 **main 직접 push 금지** — 레포 규칙이 막는다. **PR 로만.**
- 🔴 `feat/20260920-rc-safe-path` 는 **자료이관 RC 트랙(미머지)** — 섞지 마라.
- 🔴 Bash `grep -c "\[Fact\]"` 가 0 을 준다 — 이스케이프 문제. **Grep 도구로 세라.**
- 🔴 **CI 러너에는 TPM 이 없다** — `CngKey.Exists(name, Platform)` 이 **예외를 던진다**(false 아님). 초록불 옆 `##[warning] 안 잰 축` 을 반드시 읽어라.

---

**기록**: PM 브라운킴 · 2026-09-24 · main `a9e4e580` · 게시 **1.3.44** · 🟢 **트랙 완결**
