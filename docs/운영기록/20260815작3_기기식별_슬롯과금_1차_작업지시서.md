# 작업지시서 20260815작3 — 기기 식별·슬롯 과금 재설계 [1차: P1·P2·P2.5]

| 항목 | 내용 |
|---|---|
| 작성 | 설계팀장 존 · 2026-08-15 |
| **결재** | ⬜PM 브라운킴 (결재 없이 코딩 금지) |
| 우선순위 | **P0(즉시)** — 과금 사고가 지금 나고 있다 |
| 진행 | ⬜[3]개발 → ⬜[4]검증 → ⬜[5]CTO·사장님 |
| 차수 | **1차만** (P1 계수 · P2 기준값 · P2.5 DDL). 2차(P3·P4·P5)는 별도 작지서 |
| 대체 | `20260815작2` **폐기**(PI-12 차수 재편). P0 는 작2 에서 **완료** |

## 0. ⭐ 사장님 오더

> 원문 전체 → `docs/운영기록/20260815작2_...작업지시서.md` §0 (보존됨 · 참조식)

**이 차수에 직접 걸리는 오더**:
> *"추가슬롯 1+1(PC, 모바일)당 1만원이야"*
> *"프리미엄은 (…) 이건 일단 빼고, 베이직, 프로만 염두해서 개발하면 될듯"*
> *"**특히나 강조하는것은, 과금에 연결된거니 더욱 촘촘하게 신경쓰도록**"*
> *"게이트를 양방향으로 해야지"*

**2차로 넘어간 오더** (이 차수에서 하지 않는다):
수동 3단 승인 · 중간 화면 · 401 제거 · 알림 · 승인 영구 → **작4**(2차)

## 1. 오더를 이렇게 이해했다

**의중** — 2차에서 열쇠를 바꾸기 전에 **바닥을 고른다.**
계수가 5곳으로 갈리고 요금 기준값이 코드에 박히고 DDL 에 장비넘버 자리가 없는 채로 열쇠를 바꾸면,
숫자가 틀렸을 때 **어디가 틀렸는지 못 찾는다.** 과금이라 이 순서가 결정적이다.

**안 하면** — 2차가 물리적으로 성립하지 않는다(PI-09). 그리고 지금도 최상위 고객이 basic 한도를 받고 있다(PI-14).

**실측 근거** (헌법 #32)
> 전수 실측 → 명세서 §2 / P0 스냅샷 → `docs/개발/erp/20260815_P0_현행봉인_계수스냅샷_기기슬롯.md`
> [3-V] 적발 7건 → `docs/검증/병렬이슈/`

## 2. 무엇을 하는가

> 상세 PRD → `docs/설계/erp/20260815_개발스케줄_PRD_기기식별_슬롯과금.md` §2 의 P1·P2·P2.5 (참조식)

### P1 — 계수 단일화
- [ ] 슬롯 계수 **5곳**(4곳 아님 — 명세서 §17-3)을 **단일 메서드**로
  `TenantDeviceService.cs:97 / :245 / :410 / :597` + 백오피스 `DeviceRegistrationController.cs:98-101`
- [ ] `?? "pc"` 폴백 제거 — 🔴 **두 곳**(`AuthController.cs:93` **와** `TenantDeviceService.cs:260`, §17-5)
- [ ] `TenantDeviceService.cs:165` **주석 정정** — 메인PC 도 슬롯을 센다
- [ ] ERP 쪽 `tablet` 누락 차단 (G-13 중 ERP 분)
- [ ] 게이트 **G-7 · G-13(ERP) · I-6 · I-7**

### P2 — 기준값 설정화
- [ ] `GetLimitsForTier`(`:662-669`) → **설정에서 읽기**
- [ ] 추가슬롯 산식 `extra * 2` → 🔴 **1+1** 로 정정 + 설정화
- [ ] 추가슬롯 **가격(1만원)** 설정 편입
- [ ] 🔴 **`enterprise` 정정**(PI-14) — 코드만 `premium`, 나머지 전부 `enterprise`.
      최상위 고객이 `_ => (5,3)` 으로 떨어져 **basic 한도**를 받고 있다
- [ ] ERP ↔ 백오피스 **이중 진실원 해소**
- [ ] 게이트 **G-8**(🔴 재작성 — "설정값을 바꾸면 실제 한도가 바뀌는가")

### P2.5 — DDL 칸 분리 (PI-09)
- [ ] 하드웨어 식별값 컬럼 추가 (메인PC 용)
- [ ] `fingerprint` 를 **옛 기기 호환 전용**으로 격하
- [ ] UNIQUE 제약 재설계 — 한 줄이 장비넘버와 지문을 **동시에** 가질 수 있게
- [ ] clean DDL 편입(#36) + `ddl-smoke`

### 🔴 하지 않는 것
- ❌ **백오피스 "싼 칸" 정렬** — PI-12. 모바일 한도(3) < PC 한도(5)라 **차단이 빨라진다.** **2차로**
- ❌ **P3 열쇠 교체 · P4 메인PC 합류 · P5 중간 화면** — 2차(작4)
- ❌ **401 제거 · 승인 알림 · 수동 3단** — 2차
- ❌ 기본 제공 슬롯 **숫자 확정** — 구조만. 숫자는 사장님이 나중에
- ❌ 지문 산식 개선 — 지문은 열쇠에서 내린다
- ❌ 워치독·업데이트·`appsettings.json`·운영 demo

## 3. 헌법 체크

| 조항 | 왜 걸리나 | 어떻게 지키나 |
|---|---|---|
| **#1** 덮어쓰기 금지 | 기존 서비스·DDL 수정 | 추가만. Write 전 존재 확인 |
| **#11** 기준값 어드민 설정 | P2 | 코드 상수 금지 → 설정 |
| **#13** 새 SQL 전 DESCRIBE | P1·P2.5 | `DESCRIBE tenant_devices` 선행 |
| **#17** ENGINE=InnoDB | P2.5 | 신규 테이블 시 명시 |
| **#19** 빌드 0/0 | 전 단계 | errors 0 + warnings 0 |
| **#21** appsettings 금지 | P2 설정 위치 | `appsettings*.json` **무접촉** |
| **#36** 출하 DDL 진실원 | P2.5 | clean DDL 편입 |
| **#37** 컬럼 제거 금지 | P2.5 | *"안 읽힌다 ≠ 잔재"* — 기존 컬럼 보존 |
| 🚨**#39** 운영 수술 금지 | 실측 | **샌드박스만.** demo·NCP 무접촉 |
| **#42** 문서 근거 | 전 단계 | 개발명세서 필수 |

## 4. 어떻게 하는가 ([3] 개발팀용)

- 지휘: **PM(개발팀장)**
- 설계문서 (**주소만**):
  - 구조 진실원 → `docs/설계/erp/20260815_아키텍처명세서_기기식별_슬롯과금_재설계.md` (**2차 개정본**)
  - 단계 PRD → `docs/설계/erp/20260815_개발스케줄_PRD_기기식별_슬롯과금.md`
  - P0 스냅샷 → `docs/개발/erp/20260815_P0_현행봉인_계수스냅샷_기기슬롯.md`
- 🔴 마이그레이션은 **`src/HitPan.API/Migrations/SQL/DB-NN_*.sql` 에만**
- **산출물: 개발명세서** → `docs/개발/erp/`

### 🔴 구현자 함정 (P0 실측 D-3·D-4·D-9)
| # | 함정 |
|---|---|
| **D-3** | `:165` 주석을 보고 SQL 에 `is_main_pc = 0` 을 넣으면 **요금이 샌다.** 코드가 맞고 **주석을 고친다** |
| **D-4** | `ApproveAsync` 는 "자기 자신이 아직 pending" 전제 위에 있다. 계수 대상을 넓히면 **승인이 영원히 막힌다** |
| **D-9** | `?? "pc"` 가 **두 곳**. 한 곳만 고치면 아무것도 안 바뀐다 |

## 5. 검증 방법 (SoD)

**[3-V] 동시 병렬검증** — 보안상무 안철수. 작업 **중에** 실시 → `docs/검증/병렬이슈/`
**[4] 최종 검증** — 검증팀장 데이비드 박 → `docs/검증/erp/`
- [ ] 빌드 **errors 0 + warnings 0**
- [ ] 시험 전건 통과 (482건 + 신설분)
- [ ] `ddl-smoke` (P2.5)
- [ ] **독립 반증 3건 이상**
- [ ] P0 스냅샷과 **입력→출력 대조** (계수 통합으로 동작이 안 바뀌었는가)

### 통과 판정 — 🔴 반증까지
| 게이트 | 통과 조건 | 반증 |
|---|---|---|
| **G-7** | 계수가 단일 메서드 통과 | 복제 되살리면 **FAIL** |
| **G-8** | 설정값 바꾸면 **실제 한도가 바뀐다** | 코드 상수로 되돌리면 **FAIL** |
| **G-13** | `tablet` 이 어느 칸에도 안 빠진다 | 등호 비교로 되돌리면 **FAIL** |
| **I-6** | 모르는 종류 → `mobile` | `pc` 로 바꾸면 **FAIL** |

> 🔴 **G-8 은 1차 초안이 가짜였다** — *"코드에 없는지"* 는 상수를 옮기면 통과한다.
> 8/15 메신저 교훈(`ChatWindowGuardTests` 가 글자만 검사해 통과)을 인용해 놓고 **같은 게이트를 만들었다.**

## 6. 리스크 / 롤백

| 리스크 | 확률 | 대응 |
|---|---|---|
| 계수 통합 중 동작이 미묘하게 바뀜 | **중** | P0 스냅샷과 입력→출력 대조. 다르면 즉시 중단 |
| **UNIQUE 제약 변경이 되돌리기 어려움** | **중** | 🔴 변경 전 **백업 필수**. 실사용 고객 0 이나 demo·테스트 데이터 보존 |
| 설정화하며 기본값이 달라져 한도가 바뀜 | 중 | 현행 값을 **초기값 그대로**. 숫자 변경은 범위 밖 |
| `enterprise` 정정이 다른 곳을 깨뜨림 | 중 | 호출처 전수 grep(#12) 후 착수 |

**롤백**: 커밋 전 `git checkout` / DDL 은 백업 복원.

## 7. 완료 정의 (DoD)

① **구현** — P1 계수 단일화 + P2 설정화(enterprise 포함) + P2.5 DDL
② **검증** — 빌드 0/0 · 시험 전건 · 게이트 4개 **반증까지** · 독립반증 3건 · P0 대조
③ **문서** — 개발명세서 + 작업리뷰서 + 병렬이슈

> 🔴 **커밋·게시는 [5] 사장님 결재 후.**
> 🔴 **1차만으로는 사장님 3증상이 안 닫힌다**(PI-12). **2차(작4)까지 가야 닫힌다.**
> 사장님: *"과금에 연결된거니 더욱 촘촘하게 신경쓰도록"*
