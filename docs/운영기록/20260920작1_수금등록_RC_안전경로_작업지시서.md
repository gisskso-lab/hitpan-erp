# [2] 작업지시서 20260920작1 — 수금·지급 등록 READ COMMITTED 안전 경로 (게시 전 필수)

| 항목 | 내용 |
|---|---|
| 상태 | ⬜ [1-V] 선행검증 진행 중 — 설계·코딩 착수 금지(#33·#42) |
| 근거 | 인수인계서 `20260918인계1_…` §4-1 · [5] CTO 결재 `docs/검증/erp/20260915_작업리뷰서_자료이관_장부반영분류.md` §12 「가장 위험한 한 가지」 |
| 브랜치 | `feat/20260920-rc-safe-path` (base `origin/main` `43e1fe4d`) |
| 규모(가) | M — 한 영역(수금·지급 등록 트랜잭션) · DB 설정 의존 · 게이트 신설 |
| 진실원 | 이 문서 |

## 0. ⭐ 사장 오더 (원문)

> 2026-09-18 *"응 전결!!!! 모두 루틴대로 진행해!!."* (3판 트랙) → 3판 main 머지 완료
> 2026-09-20 *"인수인계서부터 진행해"* → 9/18 인계1 §9 의 다음 첫 행동 = §4-1 RC 안전 경로 착수.

## 1. 오더를 이렇게 이해했다

- 3판에서 수금·지급 **등록 트랜잭션 전체**를 READ COMMITTED 로 열었다(`LegacyBalanceMatching.MatchIsolation` · `CollectionService.CreateCollectionAsync`·`CreatePaymentAsync`). 이월잔액 매칭의 동시성(병렬이슈44 교착)을 피하려고 넣은 규칙인데 **영향 범위가 모든 수금·지급 등록**이다.
- MariaDB 는 `log_bin=ON` + `binlog_format=STATEMENT` 이면 RC 트랜잭션의 쓰기를 거절한다고 알려져 있다 → 그런 고객 PC 에서는 **수금·지급 등록이 전부 막힌다**(#20 흐름 끊김 · P0).
- 설치본은 MariaDB 가 이미 있으면 그 설정을 그대로 쓴다(`installer/HitPan-Universal.iss:115` `NeedsMariaDB`) → 고객 설정에 우리 기능이 달려 있다.
- **게시 전에 고친다.** main 머지만으로는 고객에게 안 나가지만(ERP 게시는 수동), 다른 트랙이 먼저 게시하면 같이 나간다.

## 2. 범위 (⬜ [1-V] 결과로 확정)

### 하는 것 (가안)
- 서버 설정을 보고 **안전 경로로 가르는 것** — 거절되는 조합이면 등록이 막히지 않게 하고, 그때도 이월잔액 매칭이 두 번 빠지지 않게 한다.
- 그 조합에서 도는 **동작 게이트**(설정을 실제로 STATEMENT 로 놓고 등록이 되는지).

### 하지 않는 것
- 고객 PC·사장님 PC 설정 변경(#29·#39) · 설치본이 남의 MariaDB 설정을 바꾸는 것 · appsettings 수정(#21) · 3판 기능 되돌리기.

## 3. 헌법 체크
- #20 흐름 끊김 금지(수금·지급 등록은 매일 쓰는 길) · #25 쉽게·정확하게·안전하게 · #29 인프라 조작 사전 승인 · #33 의중 재확인 후 코딩 · #39 운영 직접 수술 금지 · #42 문서 먼저.

## 4. 구현 범위 (설계팀장 존 · [1-V] 완료 반영)

> 설계 정본 = `docs/설계/erp/20260920_설계_수금등록_RC_안전경로.md`. 아래는 그 설계의 **지시 형태**다. 충돌하면 설계 정본이 우선.

### 4-1. 결정 한 줄
**서버 설정을 보고 두 경로로 가른다. 두 경로가 같은 정확성 계약을 지킨다. 등록이 막히는 경로는 만들지 않는다(#20).**

| 판정 | 경로 | 내용 |
|---|---|---|
| `log_bin=OFF` 또는 `binlog_format∈{MIXED,ROW}` | **RC** | 지금 3판 그대로 (무변경) |
| `log_bin=ON` **AND** `STATEMENT` | **RR** | RR + 거래처 행 잠금 + **R 재계산·전표 남은금액을 잠금 읽기** + 교착 짧은 재시도 |
| 조회 실패·권한거절·타임아웃 | **RR** | RR 은 두 서버 모두에서 돈다 → 모르면 도는 쪽 |

### 4-2. 하는 것
1. **판정기 신설** `BinlogSafetyProbe` — `SELECT @@log_bin,@@binlog_format` · 프로세스 값 1개 · TTL 5분 · 1665 를 만나면 즉시 무효화 · 실패 시 `LogWarning` 한 줄(#15) 후 RR.
2. **모드 계약** `LegacyMatchMode { ReadCommittedFresh, RepeatableReadLocking }` — 호출자가 **선언**하고, 가드는 (모드 ↔ 실제 격리수준) 쌍을 본다. 모드가 SQL 의 잠금절도 고른다.
3. **RR 잠금 읽기** — `GetForUpdateAsync` 의 R 재계산 문장 꼬리에 `LOCK IN SHARE MODE`(RR 모드일 때만). **공용 식 본문은 한 글자도 안 건드린다**(11곳 소비자 계약).
4. **D1 봉합** — `EnsureCollectionTargetAllowedAsync`·`EnsurePaymentTargetAllowedAsync` 의 남은금액 SUM 도 RR 모드에서 잠금 읽기. (설계가 새로 찾은 구멍 — 설계 §4-4)
5. **재시도 껍질** — 등록 **트랜잭션 통째** 재시도. 1213·1205·1665 만. 총 3회 · 50ms→150ms + 지터. 소진 시 고객 문구.
6. **게이트 신설** `RcSafePathGate` + CI 등록.

### 4-3. 하지 않는 것 (§2 에 더한다)
- BINLOG ADMIN 권한 확대 · 설치본의 남의 my.ini/SET GLOBAL 수술(#29) — **둘 다 기본안에서 뺐다**(§9 결재 안건).
- 공용 식(`ReceivableRemainingSql`·`PayableRemainingSql`) 본문 수정 · 3판 기능 되돌리기 · 로거 배선 확대 · RC 경로 동작 변경.
- 기존 G8 사례 삭제(§5-3 의 한 줄 기대값 변경만).

## 5. 게이트 · 기대값

### 5-1. 인스턴스
- CI: db-gate 잡에 스텝 추가 — `docker run -d -p 3307:3306 -e MARIADB_ROOT_PASSWORD=… mariadb:11.4 --log-bin=stmt --binlog-format=STATEMENT --server-id=9920`.
- 게이트는 **`HITPAN_DB_STMT_PORT`** 로만 찾는다. **CI 에서 없거나 못 붙으면 FAIL**(SKIP 을 통과로 세지 않는다).
- 로컬은 새 datadir · 포그라운드 `mariadbd` · **서비스 등록 금지** · 시험 후 SHUTDOWN(선행검증 §7 방식). 운영 3306·격리본 33306 무접촉(#39).

### 5-2. 사례 · 봉합 빼고 FAIL

| 사례 | 잰다 | 무력화(빨간불이어야 한다) |
|---|---|---|
| G-RC1 | 위험 서버에서 **수금 등록 3경로**(이월·명세서·ref 없음) 성공 — 서비스 진입점 경유 | 경로 분기 제거 → ERROR 1665 |
| G-RC2 | 위험 서버에서 **지급 등록 3경로** 성공 | 같음 |
| G-RC3 | G8-u 동등 — 앞선 일반 읽기 뒤에도 최신 R · 최종 R ≥ 0 · 초과 거절 문구 | R 재계산 잠금절 제거 → 최종 R 음수 |
| G-RC4 | G8-x 동등 — 같은 거래처 동시(각 R 이하·합 초과) 뒤가 대기 후 거절 | 같은 무력화 → 거절 안 남(Actual: null) |
| G-RC5 | G8-w 동등 — 다른 거래처 동시, **서비스 진입점 두 연결** 둘 다 성공 · 시도 횟수 기록 | 재시도 껍질 제거 → 1213 노출 |
| G-RC6 | **D1** — 같은 명세서 동시 수금 합 초과 거절(매입전표 대칭) · 최종 합 ≤ 전표금액 | 전표 SUM 잠금절 제거 → 초과 통과 |
| G-RC7 | 판정표 4조합 + 조회 실패 → RR (순수 · DB 불필요) | 기본값을 RC 로 → 빨간불 |
| G-RC8 | **대조군** — 안전 서버(3306)에서 모드 = RC | 항상 RR 이면 빨간불 |
| G-RC9 | 캐시 — 2번째 등록은 조회 왕복 0 · TTL 뒤 1회 (세는 구현체 주입) | 매번 조회면 빨간불 |

### 5-3. 기존 회귀
- **G8 24사례는 안전 서버에서 그대로 초록**(RC 무변경 증거) · G9·G10·LabelParity 무변경.
- **바뀌는 한 줄**: `LegacyBalanceMatchGateTests.cs:722-723` G8-u 끝 계약 가드 — `RepeatableRead` 는 이제 정식 경로이므로 **`Serializable`** 로 바꿔 「아무 격리나 통과 아님」을 계속 잰다. 사례 수 24 유지.
- CI: `GATES="$GATES|RcSafePathGate"` · 등록 근거 주석은 **무력화 시험 실측표**(관례 · "초록불이라서" 금지).

## 6. 리스크 · 롤백

| 리스크 | 대비 |
|---|---|
| RR 경로 교착 잦음 | 상수 한 곳 · `LogWarning` 에 횟수가 남는다 · 값만 조정 |
| 판정이 틀려 안전 서버가 RR 로 감 | 두 경로의 정확성 계약이 같다 → **돈이 안 틀어진다**. 대가는 지연뿐 |
| 파생표 잠금이 회사 범위를 훑음 | 좁히면 공용 식이 11곳과 갈라진다 → 이번엔 안 한다(재시도로 흡수) |
| **롤백** | 판정기가 항상 RC 를 돌려주게 하는 **상수 1줄** → 3판 동작 복귀. 게시 전 즉시 |

## 7. 완료 정의
1. 빌드 **errors 0 · warnings 0**(#19).
2. 안전 서버: 기존 G8 24 + G9 + G10 + LabelParity **초록**.
3. 위험 서버(STATEMENT): 신규 9사례 **초록**.
4. **무력화 시험 표 전부 빨간불**을 실측으로 기록(개발명세서 §4) — 표만 쓰고 안 돌리면 미완.
5. 갈래별 개발명세서 2건 · 설계 정본 링크 · 폴더 INDEX 한 줄(#41).
6. [4] 검증팀장 검증 통과 → [5] 결재 → [6] 커밋. **PM 결재 전 코딩 금지**(#33·#42).

## 8. 갈래별 지시 (최대 2갈래 · 각 파일 5개 이하)

### 갈래 S1 — 판정·경로 (파일 4)
`src/HitPan.Application/Services/BinlogSafetyProbe.cs` 🆕 · `…/LegacyBalanceMatching.cs` · `…/CollectionService.cs` · `src/HitPan.API/Program.cs`(DI 등록 · appsettings 무접촉 #21)

**0단계 — 코딩 전 raw 확인 (⚠️ 미실측 해소)**
STATEMENT 인스턴스에서 `SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ; BEGIN; SELECT … FOR UPDATE;` 가 통과하는지 먼저 잰다.
- 통과 → 설계대로.
- **1665 가 나면** → RR 모드의 거래처 행 잠금도 `LOCK IN SHARE MODE` 로 내리고, 같은 거래처 동시는 교착으로만 갈린다(재시도가 메운다). 설계 §5-3 분기 · **PM 에 보고 후 진행**.

**계약(갈래 간 고정 · S2 가 이 시그니처를 소비한다)**
```
public enum LegacyMatchMode { ReadCommittedFresh, RepeatableReadLocking }
public static IsolationLevel IsolationFor(LegacyMatchMode mode);

public interface IBinlogSafetyProbe {
    Task<LegacyMatchMode> GetModeAsync(IDbConnection db, ILogger? logger, CancellationToken ct);
    void Invalidate();
}
public sealed class BinlogSafetyProbe : IBinlogSafetyProbe {
    public BinlogSafetyProbe(TimeSpan? ttl = null, TimeProvider? clock = null);
}

Task<Remaining?> LegacyBalanceMatching.GetForUpdateAsync(IDbConnection db, IDbTransaction tx,
    LegacyMatchMode mode, string tenantId, string partnerId, bool receivable, CancellationToken ct);
Task LegacyBalanceMatching.EnsureMatchAllowedAsync(IDbConnection db, IDbTransaction tx,
    LegacyMatchMode mode, string tenantId, string partnerId, string? refId, decimal amount,
    DateTime date, bool receivable, ILogger? logger, CancellationToken ct);

private Task<T> CollectionService.RunMatchTxAsync<T>(LegacyMatchMode mode,
    Func<IDbTransaction, CancellationToken, Task<T>> body, string what, CancellationToken ct);
```
- `mode` 는 `tenantId` **앞**에 둔다(잊고 못 지나간다).
- `CollectionService` 생성자에 `IBinlogSafetyProbe? probe = null` **선택 인자** → 기존 호출자·테스트 무변경.
- 가드 실패 = 지금과 같은 `NotSupportedException`. `Serializable`·`ReadUncommitted`·`Unspecified` 는 어느 모드로도 통과 못 한다.
- 재시도는 `InvalidOperationException`(고객 문구 거절)에 **절대 걸지 않는다**. 감사로그는 커밋 뒤 = 껍질 밖.
- 소진 문구(상수 한 곳 · 개발용어 금지 #23): *"지금 다른 사용자가 같은 거래처의 수금을 처리하고 있습니다. 잠시 후 다시 저장해 주세요."* (지급은 "수금"→"지급")

**상태 커밋 순서 (60턴 대비)**
① 판정기 + 순수 판정 사례 → 빌드 0/0 · **커밋** → ② 모드·가드·R 잠금절 → **커밋** → ③ 서비스 분기·재시도·D1 → **커밋**.
개발명세서는 **코딩 전에 뼈대**(§0 카드 · §1 계약 · §2 변경 파일 · §4 무력화 표 빈칸)를 먼저 커밋한다.

### 갈래 S2 — 게이트·CI (파일 3)
`src/HitPan.Tests/Integrity/RcSafePathGateTests.cs` 🆕 · `…/LegacyBalanceMatchGateTests.cs`(§5-3 한 줄) · `.github/workflows/build-and-test.yml`

- **S1 ①이 커밋된 뒤 착수**(계약 확정 후). 그 전에는 §5-2 표만 보고 게이트 뼈대(사례 이름·DisplayName·fixture)를 먼저 커밋.
- fixture 는 `LegacyBalanceMatchDbFixture` 모양을 따르되 포트만 `HITPAN_DB_STMT_PORT`. 출하 DDL(`installer/hitpan_db_clean.sql`) 적재 · `--default-character-set=utf8mb4` 필수.
- G-RC5·G-RC1·2 는 **서비스 진입점**(`CreateCollectionAsync`/`CreatePaymentAsync`)으로 부른다 — raw SQL 로 부르면 재시도 껍질 밖이라 다른 것을 재게 된다.
- 무력화 시험은 **봉합을 빼고 실제로 돌려** 빨간불을 확인하고, 결과를 개발명세서 §4 표에 옮긴다.

## 9. 새 결재 안건

| # | 안건 | 누구 | 설계 입장 |
|---|---|---|---|
| A-1 | 고객 DB 계정 **BINLOG ADMIN** 부여 후 세션 `binlog_format='ROW'` | 사장님 | ❌ 기본안 아님 — 권한 확대 = 공격면 확대. 이 설계는 권한 없이 해결한다 |
| A-2 | 설치본이 **기존 MariaDB 의 my.ini/SET GLOBAL** 을 고치는 안 | 사장님 | ❌ 기본안 금지 — #29 사전 승인 대상 |
| A-3 | 2경로 유지 vs **항상 RR 한 경로** 통일 | 사장님 | 기본안 = 2경로. 한 경로는 단순하나 전 고객이 재시도를 안는다 |
| A-4 | **이미 STATEMENT 인 고객 PC 가 있으면 지금도 수금·지급이 막혀 있다** — 현장 확인 주체·방법 | 사장님 | 설계 밖. 본사가 설정을 긁어오는 방식은 #18·#22 와 걸린다 |
| A-5 | 재시도 3회·50/150ms · TTL 5분 · 소진 문구 | PM | 상수 한 곳 · 게이트가 실측 |

## 10. ⚠️ 미실측 (추측을 사실로 쓰지 않는다)
1. **RR + STATEMENT 에서 `FOR UPDATE` 허용 여부** — 안 쟀다. §8 갈래 S1 0단계에서 먼저 확인.
2. **파생표 `LOCK IN SHARE MODE` 가 STATEMENT 서버에서 최신 커밋을 읽는가** — 3판 1차안 기록이 근거일 뿐. G-RC3·G-RC4 가 판정한다.
3. **교착 빈도·재시도 성공률** — 미실측. 3회는 체감 기준이지 실측 근거가 아니다.
4. **고객 PC·사장님 PC 의 `log_bin`/`binlog_format` 분포** — 안 봤다. 「모른다」가 사실. 빈도를 숫자로 말하면 받아쓰기(#32).
5. **D1 이 현장에서 실제로 났는지** — 모른다. RR 경로를 새로 여는 이번 작업이 **만들지 않게** 막는 것이다.

---
**작성**: PM 브라운킴 · 2026-09-20 / **§4~§10 작성**: 설계팀장 존 · 2026-09-20 / **상태**: ⬜ PM 결재 대기 (작성자 ≠ 결재자)

## 11. 🟢 PM 결재 (브라운킴 · 2026-09-21)

**결재: 승인** — 설계 정본 `docs/설계/erp/20260920_설계_수금등록_RC_안전경로.md` · 작지 §4~§10 그대로 [3] 착수.

| 안건 | PM 판정 |
|---|---|
| A-1 BINLOG ADMIN 확대 | ❌ 기본안 제외 승인 — 권한 없이 푸는 설계가 있는데 공격면을 넓힐 이유 없다. 사장님께 올리지 않는다(필요해지면 그때 안건). |
| A-2 설치본이 남의 my.ini 수술 | ❌ 제외 승인 — #29 사전 승인 대상 · 남의 PC 설정을 우리가 바꾸는 순간 책임 경계가 무너진다. |
| A-3 2경로 vs 항상 RR | **2경로 유지(설계 기본안)** — 지금 안전한 고객(신규 설치 = 기본값 안전)에게 새 재시도·지연을 지우지 않는다. 두 경로의 정확성 계약이 같고 대조군 게이트(G-RC8)가 갈림을 잰다. 한 경로 통일은 교착 빈도 실측이 쌓인 뒤 다시 본다(백로그). |
| A-4 이미 STATEMENT 인 고객 현장 확인 | **지금은 조치 불필요** — 3판은 **미게시**라 고객에 RC 코드가 없다(오늘 막혀 있는 고객은 없다). 이번 봉합이 게시 전에 들어가면 그 조합에서도 등록이 된다. 본사가 고객 DB 설정을 긁어오는 방식은 #18·#22 위반이라 안 한다. 사장님 결재 불필요 — 게시 결재 때 한 줄 보고만. |
| A-5 재시도 3회·50/150ms·TTL 5분·소진 문구 | 승인(PM) · 상수 한 곳 · G-RC5·G-RC9 가 실측 |
| D1 (설계 발견) | 이번 범위 포함 승인 — RR 경로가 새로 여는 구멍은 그 경로를 여는 작업에서 막는다. |

- 사실 확인(PM): CI db-gate 잡은 `ubuntu-latest` → `docker run` 가능(`.github/workflows/build-and-test.yml:93`). S2 의 STATEMENT 인스턴스 기동 방식 성립.
- 🔴 착수 조건: S1 **0단계**(RR+STATEMENT 에서 `FOR UPDATE` 허용 여부 raw 확인) 결과를 PM 에 보고한 뒤 본체 진행. 1665 면 §5-3 분기.
- 투입: S1(hp-implementer · worktree) → ① 커밋 뒤 S2 착수 · [3-V]·[4] 는 S1 ③ 커밋 시점에 동시 착수.

## 12. 개정 2판 (설계팀장 존 · 2026-09-21) — 갈래 S1b

> 근거: 설계 정본 **§11**(2판) · 갈래 S1 개발명세서 §3-2 실측. **§1~§11 은 그대로 살아 있다.** 바뀌는 점 = §4-3 의 「R 재계산 = 파생표 + 꼬리 잠금절」 **한 점**. 결재는 PM.
> 커밋은 S1 의 ①②③④ 위에 쌓는다(되돌리지 않는다 · #1).

### 12-1. 갈래 S1b 지시 — 최소 변경 (파일 2)

| # | 파일·자리 | 무엇 | 왜 |
|---|---|---|---|
| ㉮ | `LegacyBalanceMatching.GetForUpdateAsync` 첫 문장 | `SELECT balance_id` → `SELECT balance_id, base_date, balance_amount … FOR UPDATE` (값도 같이 받는다) | 잠금 읽기 = 최신 L0 · 잠금이 늘지 않는다 (설계 §11-4) |
| ㉯ | 같은 메서드, 두 번째 문장 | `mode == RepeatableReadLocking` 이면 **파생표를 쓰지 않는다** → 신설 `RecomputeRemainingLockedAsync` 로. RC 분기는 **지금 코드 그대로** | S1 §3-2: 파생표 바깥 꼬리절은 옛값 |
| ㉰ | `LegacyBalanceMatching` 신설 상수 | 미수 2 · 미지급 3 — 실테이블 직접 · `partner_id` 1건 필터 · 꼬리 `LOCK IN SHARE MODE`. 술어·별칭(`lc·ec·lp·ep·rt/rti/pr`)은 공용 식에서 **그대로** 옮긴다 | 동등성(§11-3) |
| ㉱ | `LockTailFor` | **남긴다** — D1 이 쓴다. 파생표 꼬리에는 더 이상 안 붙인다 | RC 경로 무변경 |
| ㉲ | `CollectionService` 지급 D1(`:851·855`) | 스칼라 하위질의 2개 → **직접 문장 2개**로 분리(수금 쪽과 같은 모양) | 같은 함정 의심 · 실측된 모양만 쓴다 (설계 §11-4) |
| — | `BinlogSafetyProbe.cs` · `Program.cs` · 재시도 껍질 · 모드 가드 · 수금 D1 | **무변경** | 이미 실측 통과 |

- **0단계(코딩 전 · 필수)**: 33406 STATEMENT 인스턴스에서 **두 연결**로 ㉰ 문장들이 최신 커밋을 돌려주는지 raw 로 먼저 잰다(조인 있는 `em`·`rm` 포함). ㉲ 의 분리 전/후도 같이 잰다. **결과를 PM 에 보고한 뒤 본체.** 2판이 태어난 이유가 「안 재고 처방했다」이다.
- **금지**: 공용 식(`ReceivableRemainingSql`·`PayableRemainingSql`) 본문 수정 · 파생표/`UNION ALL`/스칼라 하위질의로 재계산 · `Task.WhenAll`(#16) · `MySqlConnection` 직접 생성.
- **계약**: `R = GREATEST(L0,0) - ΣM` 을 **C# decimal**(#4) · 행 없으면 `null`(E5) · 음수 R 은 자르지 않는다(E4·E7).
- **상태 커밋**: ⑤ 0단계 실측 기록 → ⑥ ㉮㉯㉰(빌드 0/0) → ⑦ ㉲ + 안전 서버 회귀 40/40·Skipped 0 → ⑧ 개발명세서 2판. 각 단계 빌드 0/0(#19).

### 12-2. §5-2 게이트 표에 더한다

| 사례 | 잰다 | 무력화(빨간불이어야 한다) |
|---|---|---|
| **G-RC3**(기대값만 정정) | RR 경로에서 앞선 일반 읽기 **뒤에도** 최신 R — 파생표가 아니라 직접읽기로 | 재계산을 파생표로 되돌리면 옛 R(90,000) → 빨간불 |
| **G-RC10** 🆕 동등성 | 같은 상태에서 `공용 식` 과 `RR 재계산` 의 **L0·M·R 이 모두 같다** — 사례 E1 부분매칭 · E2 취소분 · E3 이관분 · E4 음수 이월 · E5 행 없음 · E6 매입 반품(확정/미확정) · E7 초과(음수 R) | RR 식에서 필터 한 줄 빼면(예: `is_active=1`) 두 값이 갈라져 빨간불 |
| **G-RC11** 🆕 앵커 | 공용 식 2개의 **SHA-256 이 기록값과 같다** | 공용 식을 한 글자 고치면 빨간불 → RR 식을 같이 고치게 만든다 |
| **G-RC6**(사례 추가) | D1 **매입 축** — 같은 매입전표 동시 지급 합 초과 거절(반품 포함) | 지급 합산 잠금절 제거 → 초과 통과 |

- **무력화 방법**(게이트가 진짜 잰다는 증거 · 관례): ①㉯ 를 파생표로 되돌린다 → G-RC3 빨간불 ②RR 식의 `COALESCE(source_type,'') <> 'migration'` 한 줄 제거 → G-RC10 E3 빨간불 ③공용 식에 공백 1개 추가 → G-RC11 빨간불 ④㉲ 잠금절 제거 → G-RC6 매입 축 빨간불. **네 가지를 실제로 빼고 FAIL 을 확인한 표**를 개발명세서에 싣는다(초록불만으로 CI 등록 금지).
- 게이트 신설·CI 등록은 종전대로 **갈래 S2**. S1b 는 ㉮~㉲ 와 0단계 실측만 한다.

### 12-3. 새 결재 안건 (설계 §11-7)

| # | 안건 | 누구 | PM 판정 |
|---|---|---|---|
| B-1 | 공용 식을 조각 상수로 분해해 RR 식과 텍스트 공유 | PM | ⬜ (설계 입장: 기본안 제외 · 앵커 게이트로 대체) |
| B-2 | RR 재계산이 문장 2~3개로 나뉜다(왕복 증가) | PM | ⬜ |
| B-3 | 지급 D1 문장 모양 변경(스칼라 하위질의 → 직접 문장 2개) | PM | ⬜ |

### 12-4. ⚠️ 2판이 안 잰 것
1. **(다) 「트랜잭션 첫 문장이 `FOR UPDATE` 면 일반 읽기도 최신인가」 — 안 쟀다.** 기각 사유는 실측이 아니라 **코드로 강제할 수 없다**는 것(설계 §11-2 · 병렬이슈44 함정).
2. **신설 RR 문장의 최신성** — 조인 있는 `em`·`rm` 은 미실측 → S1b 0단계가 잰다.
3. **좁아진 잠금의 교착 빈도** — 여전히 미실측. §4-3 재시도가 받는다.

---
**개정 2판 작성**: 설계팀장 존 · 2026-09-21 / **상태**: ⬜ PM 결재 대기 (작성자 ≠ 결재자)

## 13. 🟢 PM 결재 — 개정 2판 (브라운킴 · 2026-09-21)

**결재: 승인** — 설계 §11 처방 (나) RR 전용 재계산 SQL · 작지 §12 갈래 S1b 그대로 착수.

| 안건 | PM 판정 |
|---|---|
| B-1 공용 식 조각 분해 제외 | **승인** — 11곳 텍스트를 건드리는 순간 이번 작업이 3판 전체 회귀가 된다. 대신 갈라짐은 G-RC10(값 동등성 7사례) + G-RC11(공용 식 SHA 앵커)로 잰다. |
| B-2 재계산 문장 2~3개 분할 | **승인** — 조인 있는 한 문장의 최신성이 ⚠️미실측이므로, **S1b 0단계에서 두 연결 raw 로 먼저 재고** 결과대로 간다(재지 않고 처방한 것이 2판이 태어난 이유다). |
| B-3 지급 D1 을 직접 문장 2개로 | **승인** — 수금 쪽만 실측됐고 지급은 같은 함정 의심. 모양을 맞춰 둘 다 실측으로 덮는다. |
| (다) 「첫 문장 = FOR UPDATE」 계약안 기각 | **승인** — 코드로 강제 못 하는 규칙은 규칙이 아니다(병렬이슈44 와 같은 함정). |
| 처방 (나)의 부수 이득 | 잠금 범위가 회사 전체 → 거래처 1건으로 좁아진다 = 3판 1차안 교착 원인 제거. [4] 에서 G-RC5(다른 거래처 동시)로 확인. |

- 🔴 착수 조건: **S1b 0단계**(RR+STATEMENT 두 연결 raw 로 `em`·`rm` 문장 최신성) 결과를 PM 보고 → 최신이 아니면 문장 분할 · 그래도 아니면 멈추고 보고.
- RC(안전 서버) 경로 무변경 — 증거는 기존 게이트 40/40 skip 0 유지.

## 14. [3] S1·S1b 머지 · PM 판정 (2026-09-21)

- **머지 `3cdadfd3`** (S1 ⓪`3203e54e`~④`13f7ab4a`·INDEX`6e1d0670` + S1b ⑤`8384f0eb`~⑧`dff78011`). 머지 후 API 빌드 0/0 · 안전 서버 회귀 52/52 skip 0.
- **0단계 실측(2판 처방 검증)**: STATEMENT 33406 두 연결 — RR 전용 재계산은 조인(`em` 30,000 · `rm` 3,300)까지 **전부 최신**, 같은 순간 파생표는 옛값(M 15,000/R 85,000 ↔ 기대 41,000/59,000). → 처방 (나) 성립 · 문장 분할(B-2) 불필요.
- **무력화 SQL 층 5건 전부 빨간불**(M6 파생표 복귀 R 85,000 ↔ 59,000 · M7-①② 지급·반품 · M8-①②).
- 🔴 **PM 판정 (중요)**: **봉합을 빼도 기존 40 게이트는 초록** — 33306 은 RC 경로라 RR 분기를 안 탄다. 즉 **S2 의 STATEMENT 게이트가 이 봉합의 유일한 감시자**다. S2 없이 [4] 로 넘기지 않는다(완료 정의 §7-3).
- PM 판정 ①: worktree 는 하네스가 기존 worktree 재사용을 막아 S1 팁을 ff 로 이어받은 새 worktree 에서 진행 — S1 커밋 전부 조상 확인(`merge-base --is-ancestor` 통과), 계보 손실 0. 승인.
- PM 판정 ②: ㉲ 지급 D1 문장 분리는 **원복하지 않는다** — 실측상 현행 스칼라 하위질의도 최신이었지만(함정 아님), 수금과 모양을 맞춰 두면 다음 사람이 두 곳을 다르게 읽지 않는다. 명세서에 「함정 아님 · 대칭 목적」 근거 기록.
- 남은 것: **갈래 S2**(게이트 신설 `RcSafePathGate` 9+G-RC10·11 · CI STATEMENT 인스턴스 · 기존 G8-u 계약 한 줄) → [3-V]·[4] 동시.

## 15. [3] S2 머지 · PM 실측 (2026-09-21)

- **머지 `abd951d4`** — `RcSafePathGate` 11사례 · `LegacyBalanceMatchGateTests` 계약 한 줄 · CI db-gate 잡에 STATEMENT 인스턴스 스텝(`docker run … --log-bin=stmt --binlog-format=STATEMENT` · `HITPAN_DB_STMT_PORT=3307` · 값 확인 + 실패 시 exit 1) · GATES 등록. 제품 코드 무접촉.
- **무력화 8건 전부 빨간불**(S2 실측): M1 판정기 호출 제거 → G-RC1·2·5 · M2/M2b RR 분기·잠금절 → G-RC3·4 · **M3 재시도 비움 → G-RC5 가 교착 1213 으로 터짐** · M4 `LockTailFor` 빈값 → G-RC6 · M5 기본값 RC → G-RC7 · M6 RR 식 이관 제외 제거 → G-RC10 E3 · M7 공용 식 공백 1개 → G-RC11. 대조군 G-RC8 은 전 건 초록 유지.
- **PM 실측(머지 뒤 · 메인 레포)**: Tests 빌드 0/0 · STATEMENT 33406 기동(`log_bin=ON` `fmt=STATEMENT`) → `RcSafePathGate|LegacyBalanceMatchGate|LegacyBalanceFormulaGate|MoneyFlowJournal|Collection` **51/51 skip 0**. `HITPAN_DB_STMT_PORT` 없이 `HITPAN_REQUIRE_DB=1` 이면 11사례가 **FAIL**(설계대로 SKIP=FAIL) · Windows build 잡은 `HITPAN_REQUIRE_DB` 미설정이라 영향 없음(확인).
- 정정: §14 의 「52/52」는 필터 조합 차이 · 안전 서버 회귀 실측치는 **40**(+STATEMENT 11 = 51).
- **PM 판정**: ① G-RC3·4·6 이 「앞선 일반 읽기」를 먼저 치는 최악 조건으로 재는 방식 **채택**(RR 읽기 뷰는 첫 일반 읽기에서 열린다 — 실측). 「첫 문장은 잠금 읽기」 계약 게이트는 **안 만든다**(코드로 강제 못 하는 규칙 금지 · 처방 (나)가 순서와 무관하게 성립하므로 불필요). ② G-RC6 이 private 메서드를 못 불러 같은 모양 문장을 게이트에서 만든 것 → **[4] 확인 대상**(문장이 제품과 갈라지지 않는지).
- 남은 것: [3-V]·[4] 동시 → [5] → [6] PR·머지. CI docker 스텝 실동작은 **PR 첫 실행이 최종 확인**.

## 16. 개정 3판 (설계팀장 존 · 2026-09-21)

> 근거: 설계 정본 **§12**(3판) · [3-V] 병렬이슈53(P1) · 병렬이슈54(P2). **§1~§15 는 그대로 살아 있다.**
> 바뀌는 점 두 점 — ① 잠금 범위(SQL 무수정 · 인덱스로) ② 게이트가 제품 문장·문구를 그대로 쓴다. 결재는 PM(사장님 2건 별도).
> 커밋은 S2 머지 `abd951d4` 위에 쌓는다(되돌리지 않는다 · #1).

### 16-0. 2판이 틀린 자리 (자백)

§12-1 표 ㉰ 칸과 설계 §11-1 (나) 칸의 「잠금이 **거래처 1건으로 좁아진다**」는 **실측되지 않은 기대**였다.
실측은 반대다 — `ec ref idx_tenant_partner rows=4`(회사 전 행). `LegacyBalanceMatching.cs:368` 이 이미 폐기했던 그 모양이다.
PM 결재 §13 의 「처방 (나)의 부수 이득」 줄도 같은 기대를 이어받았다. **[4] 가 확인하기 전에 [3-V] 가 먼저 잡았다.**

### 16-1. 갈래 S3 지시 — 제품·DDL (파일 4)

| # | 파일·자리 | 무엇 | 왜 |
|---|---|---|---|
| ㉳ | `src/HitPan.API/Migrations/SQL/DB-124_lock_scope_indexes.sql` 🆕 | 인덱스 4개 — `collections(tenant_id,ref_doc_type,ref_doc_id)` · `payments(tenant_id,payment_type,ref_order_id)` · `payments(tenant_id,partner_id)` · `purchase_returns(tenant_id,receipt_id)`. `information_schema` 멱등(DB-118 관용구) · `tenants` 조인 금지 · 데이터 변경 0 · 인덱스 삭제 0 | 설계 §12-5 — SQL 을 안 고치고 잠금을 좁히는 유일한 길 |
| ㉴ | `installer/hitpan_db_clean.sql` | 네 표 정의에 같은 `KEY` 줄 **추가만** | #36 출하 DDL = 신규설치 단일 진실원 |
| ㉵ | `CollectionService.cs:805·850·856` | D1 세 문장을 **같은 파일 안** `internal static string DeliveryUsedSql(LegacyMatchMode)` · `ReceiptPaidSql(…)` · `ReceiptReturnedSql(…)` 로 추출. 제품 메서드는 그 메서드를 부른다 — **문자열은 옮기기만 한다** | 게이트가 복제 대신 제품 문장을 쓰게(§12-7 · 병렬이슈54) |
| ㉶ | `CollectionService.cs:702·703` | `MsgMatchBusyCollection`·`MsgMatchBusyPayment` 에서 **「같은 거래처의」 삭제** → 「지금 다른 사용자가 수금을 저장하고 있습니다. 잠시 후 다시 저장해 주세요.」 | 사실과 다른 문구(#25) · 사장님 결재 C-2 |
| ㉷ | `src/HitPan.Application/HitPan.Application.csproj` | `<InternalsVisibleTo Include="HitPan.Tests" />` **한 줄** (선례 `HitPan.Infrastructure.csproj:13`) | 게이트가 제품 상수·문구를 보게 |
| — | `LegacyBalanceMatching.cs` · `BinlogSafetyProbe.cs` · `Program.cs` · 재시도 상수·껍질 · 공용 식 | **무접촉** | L1~L5 는 한 글자도 안 바꾼다. 공용 식 본문 무수정은 3판 계약 |

- **0단계(코딩 전 · 필수)**: 33406 STATEMENT 인스턴스 전용 DB 에서 **인덱스 넣기 전/후 EXPLAIN 을 둘 다** 찍는다 — 대조표(문장 8개 × before/after × `key`·`key_len`·`rows`). 무관 거래처 행 30건을 심은 상태로. **「after 에서도 옵티마이저가 안 고른다」가 나오면 멈추고 PM 보고**(→ 설계 C-3 재상정). 운영 3306·5257·5234·서비스·appsettings 무접촉(#39).
- **금지**: `LegacyBalanceMatching` 의 L1~L5 SQL 수정 · 공용 식 수정 · 인덱스 **힌트**(`USE INDEX`)를 제품 코드에 박기 · `public` 으로 SQL 공개 · 재시도 상수 변경 · 기존 인덱스 삭제(#37).
- **계약(갈래 간 고정)**:
```csharp
// CollectionService — 게이트와 제품이 같은 문장을 쓴다(문자열 복제 금지)
internal static string DeliveryUsedSql(LegacyMatchMode mode);   // collections 합 (D1-a)
internal static string ReceiptPaidSql(LegacyMatchMode mode);    // payments 합 (D1-b)
internal static string ReceiptReturnedSql(LegacyMatchMode mode);// purchase_returns × items 합 (D1-c)
// 파라미터 이름 고정: @TenantId · @RefId  (게이트가 같은 이름으로 넘긴다)
```
- **상태 커밋**: ⑨ 0단계 EXPLAIN 대조표 → ⑩ ㉳㉴ DDL(빌드 0/0) → ⑪ ㉵㉶㉷ + 안전 서버 회귀 40/40·Skipped 0 → ⑫ 개발명세서 3판. 각 단계 빌드 0/0(#19).

### 16-2. 갈래 S4 지시 — 게이트·CI (파일 2)

`src/HitPan.Tests/Integrity/RcSafePathGateTests.cs` · `.github/workflows/build-and-test.yml`. **S3 ⑪(㉷ 커밋) 이후 착수.**

| 사례 | 잰다 | 무력화(빨간불이어야 한다) |
|---|---|---|
| **G-RC6**(고침) | 매입 축을 `paid + returned` **두 문장**으로 — `CollectionService.ReceiptPaidSql`·`ReceiptReturnedSql` 을 **그대로 호출**(복제 금지). **확정 반품 20,000 이 붙은 매입전표** 사례 1건 추가. 전표 조회 조건도 제품과 같게(`pr.status='confirmed'`) | **㉲ 반품 문장의 꼬리절만** 제거 → 빨간불 (지금은 안 걸린다 — 병렬이슈54) |
| **G-RC6**(문구) | 기대 문구를 `string.Format(ko-KR, CollectionService.MsgReceiptOver, …)`·`MsgDeliveryOver` 로 만든다 | 제품 문구 상수를 한 글자 고치면 빨간불 |
| **G-RC5**(문구·사례) | 문구 단언을 `CollectionService.MsgMatchBusyCollection` 로 · **다른 거래처 P1·P2 동시 매칭에서 1213 0건 · 로그에 `attempt=` 없음** | 인덱스 4개를 지우면 1213 → 빨간불 |
| **G-RC12** 🆕 대조군 | A 가 P1 RR 매칭 잠금을 쥔 상태에서 B(`innodb_lock_wait_timeout=3`) 가 ① P3 `collections` UPDATE ② P9 `collections` INSERT ③ P3 `payments` UPDATE·P9 INSERT ④ P3 `purchase_returns` UPDATE·P9 INSERT — **전부 성공** | 인덱스 없으면 1205 → 빨간불 (병렬이슈53 ②③ 재현) |
| **G-RC13** 🆕 EXPLAIN | 무관 거래처 행 30건을 심고 제품 문장 8개를 `EXPLAIN` → 각 행 `key` = 기대 인덱스명 · `rows` ≤ 상한. **대조군**: 무관 행 30→300 으로 늘려도 `rows` 가 안 커진다 | 인덱스 없으면 `key=idx_tenant_partner`·`rows` 가 심은 만큼 늘어 빨간불 |

- **EXPLAIN 구현**: `"EXPLAIN " + <제품 SQL 상수>` 를 Dapper `Query<ExplainRow>` 로. `sealed class ExplainRow { public string? Table; public string? Key; public string? KeyLen; public long Rows; }` — Dapper 대소문자 무시 매핑. `rows` 는 **추정치**이므로 「상한 이하」 + 「10배 늘려도 안 커진다」 **두 단언**으로 건다(하나만 걸면 통계 갱신에 흔들린다).
- **무력화 방법**(관례 · 초록불만으로 CI 등록 금지): ①`ReceiptReturnedSql` 의 꼬리절만 제거 → G-RC6 빨간불 ②`MsgReceiptOver` 한 글자 변경 → G-RC6 빨간불 ③DB-124 인덱스 4개 DROP → G-RC12·13·5 빨간불 ④`DeliveryUsedSql` 에 `USE INDEX(idx_tenant_partner)` 강제 → G-RC13 수금 축 빨간불. **네 건을 실제로 넣고 FAIL 을 확인한 표**를 개발명세서에 싣는다.
- **CI**: STATEMENT 인스턴스 스텝은 종전 그대로(`HITPAN_DB_STMT_PORT`). **DDL 적용 스텝이 DB-124 를 포함하는지 확인**하고, 주석의 무력화 목록에 위 4건을 더한다. 「재시도는 RR 경로에서 정상 동시 등록의 성립 조건」이라 적은 기존 주석은 **정정**한다 — 다른 거래처 동시는 이제 교착이 **안 나는 것이 기대값**이다.

### 16-3. 기존 게이트 회귀 목록 (전부 **무변경 초록**이어야 한다)

| 게이트 | 왜 초록이어야 하나 |
|---|---|
| `LegacyBalanceFormulaGate` | 공용 식 무접촉 |
| **G-RC10**(동등성 7사례) · **G-RC11**(SHA 앵커) | L1~L5·공용 식 **한 글자도 안 바꿨다**는 증거. 빨간불이면 ㉳~㉷ 가 범위를 넘은 것 |
| G-RC1·2·3·4·7·8·9 | 판정·경로·캐시 무접촉 |
| `LegacyBalanceMatchGate` · `MoneyFlowJournal` · `Collection` | 안전 서버 **40/40 skip 0** + STATEMENT **11 → 13**(G-RC12·13 추가) = **53** |

### 16-4. 새 결재 안건 (설계 §12-10)

| # | 안건 | 누구 | PM/사장님 판정 |
|---|---|---|---|
| **C-1** 🔴 | `DB-124` 인덱스 4개 = **고객 DB 변경** · 출하 DDL 동기 | **사장님** | ⬜ |
| **C-2** 🔴 | 재시도 문구에서 「같은 거래처」 삭제 (고객 노출 문구) | **사장님** | ⬜ |
| C-3 | 인덱스로 안 좁혀지면 (가) `partner_id` 직접 술어 재상정 | PM | ⬜ (설계 입장: 기본안 제외 — 「좁히기」가 「덜 세기」가 되면 돈이 틀린다) |
| C-4 | `InternalsVisibleTo(HitPan.Tests)` + D1 SQL `internal` 추출 | PM | ⬜ |
| C-5 | 갈래 2개(S3·S4) 추가 | PM | ⬜ |

### 16-5. ⚠️ 3판이 안 잰 것 (설계 §12-9 요약)

1. **옵티마이저가 DB-124 를 실제로 고르는가** — 안 쟀다. S3 0단계·G-RC13 이 판정. 안 고르면 **멈추고 PM 보고**(C-3).
2. **갭 잠금 잔여 차단** — 안 쟀다. G-RC12 ②가 재는 자리. 초록이어도 「이 사례에서는」까지가 사실이다.
3. **인덱스 4개 쓰기 비용 · 기존 고객 DB `ALTER TABLE` 소요시간** — 실데이터 규모 미측정. 숫자로 말하지 않는다(#32).
4. **`payments`·`purchase_returns` 축 실차단** — [3-V] 도 `collections` 축만 쟀다. G-RC12 ③④ 가 메운다.
5. **L3(`payments lp`)이 지금 고르는 인덱스** — 안 쟀다. 「복합이 없어 갈린다」까지가 사실이다.

---
**개정 3판 작성**: 설계팀장 존 · 2026-09-21 / **상태**: ⬜ PM 결재 대기 (작성자 ≠ 결재자) · 🔴 사장님 결재 2건(C-1·C-2) 선행

## 17. 🟢 PM 결재 — 개정 3판 (브라운킴 · 2026-09-21)

**결재: 조건부 승인** — 설계 §12 처방 (다) 인덱스 신설 + 게이트 동일성. 조건 아래.

| 안건 | PM 판정 |
|---|---|
| C-1 `DB-124` 인덱스 4개(고객 DB 변경) | **PM 승인 · 사장님 결재 불필요** — 인덱스 추가는 **데이터·의미 무변경**(#9 「DB 는 미리 다」 · 마이그 신설은 DB-123 때도 PM 결정 §14-2). 단 🔴 **조건 C-1a**: 공영정보 실물 규모 DB(격리 `hitpan_v4_a` 사본)에서 `ALTER TABLE` 4개 **소요시간·잠금 여부를 실측**해 명세서에 기록(고객 이관 뒤 첫 업데이트에서 도는 DDL이다). 10초를 넘으면 멈추고 PM 보고. |
| C-2 고객 문구에서 「같은 거래처의」 삭제 | **승인** — 사실이 아닌 문구를 그대로 두는 것이 더 나쁘다(#25). 문구 전문은 명세서에. |
| 처방 (가) `partner_id` 직접 술어 기각 | **승인** — 잠금(성능) 문제를 고치려다 **금액(M 과소 계상)** 을 걸 수는 없다. 설계 판단이 맞다. |
| 처방 (라) 잠금 없는 재계산 재기각 | **승인** — 2판 실측(R 90,000 ↔ 기대 70,000)이 근거. |
| C-3 옵티마이저가 DB-124 를 안 고르면 | **멈추고 PM 보고**(설계안 그대로) — 인덱스를 「넣었다」로 끝내지 않는다. G-RC13 이 `key`·`rows` 로 잰다. |
| C-4·C-5 (PM 몫) | 승인 — `InternalsVisibleTo` 한 줄(선례 있음) · D1 문장을 같은 파일 안 `internal static` 으로 **옮기기만**(로직 변경 0). |

- 🔴 남는 위험(설계 §12 ⚠️): **갭 잠금**으로 인한 잔여 차단은 미실측 — S3 0단계 EXPLAIN 전/후 + G-RC12(무관 거래처 4건)가 실측으로 답한다. 안 풀리면 재시도 껍질이 마지막 방어선이고, 그때는 **재시도 상한·간격을 다시 본다**(상수 한 곳).
- 투입: **S3**(제품·DDL 4파일) → 머지 → **S4**(게이트·CI 2파일) → [4] 재검증(병렬이슈53·54 봉합 확인 포함).

## 18. [3] S3 머지 · [4] 1차 결과 · PM 판정 (2026-09-21)

- **S3 머지 `366d7019`** (DB-124 인덱스 4 · 출하 DDL 동기 · 문구 정정 · D1 문장 `internal` 이동 · `InternalsVisibleTo`). 🔴 PM 봉합 1건: **출하 DDL `schema_migrations` 시드에 `DB-124` 누락** → 기존 게이트 `MigrationLocationGuardTests` 가 잡았다(신규 설치 고객이 이미 반영된 마이그를 다시 돌게 된다). 한 줄 추가 후 **274/274 skip 0**(작업·회귀·마이그·DDL). 빌드 0/0.
- **S3 실측**: EXPLAIN 전 **4문장이 `ALL` 246행**(설계 §12-2 예측보다 나빴다) · `purchase_return_items` 가 선두 표로 `ALL` 164행 → 후 **8문장 전부 신설 인덱스 · rows ≤ 4**. 무관 거래처 차단 재현 **인덱스 없으면 5/6 ERROR 1205 → DB-124 적용 후 6/6 통과(0.1~0.2초)**. C-1a `ALTER` 4건 **합 4.76초**(109,822행 · `ALGORITHM=INPLACE, LOCK=NONE` 4/4 수용 = 도중 읽기·쓰기 허용) → 10초 게이트 통과.
- **PM 판정 ①** 인덱스 ③ `payments(tenant_id,partner_id)` 는 8문장이 안 고르지만 **유지** — 근거: `payments` 에는 `(tenant_id,partner_id)` 복합이 없어 옵티마이저가 **테넌트를 넘어가는 `idx_pay_partner`** 를 고를 여지가 있다(테넌트 격리·잠금 범위 위험). S4 의 G-RC13 에 「테넌트를 넘는 인덱스를 고르지 않는다」 단언 추가.
- **PM 판정 ②** 설계 §12-2 표(전 상태 예측)와 **③ 병렬이슈53 범위**(`collections` 축만 쟀다 → `payments`·`purchase_returns` 도 실제로 막혔다)는 **S3 실측으로 정정**. 문서 정정은 S4 착수 뒤 PM 이 한 줄씩(이 절이 정본).
- **[4] 1차 결과**(리뷰서 `docs/검증/erp/20260920_작업리뷰서_수금등록_RC_안전경로.md` · `be7d2d54`): 🟡 조건부 통과 — 51/51 · 3판 게이트 46/46 · 전체 1520/1524(실패 4 = 환경) · 무력화 4건 재현. 🔴 **병렬이슈60**: G-RC5 가 재시도로 교착을 덮어 「교착 0」을 아무도 안 잰다 · **병렬이슈54 확정**: 반품 잠금절만 빼면 **전체 1524 중 한 건도 안 깨진다**(감시자 0 · 전수 실측).
- **S4 필수 요구(작지 §16 + [4] 반영)**: ① G-RC5 에 **「시도 횟수 = 1」(재시도 0회)** 단언 ② G-RC6·G-RC10 이 **제품 SQL 상수·제품 문구 상수**를 쓴다(매입 축은 `paid + returned` 두 문장 · 확정반품 사례 포함) ③ 🆕 G-RC12 무관 거래처 UPDATE/INSERT 4건 비차단 ④ 🆕 G-RC13 EXPLAIN 단언(`key` 일치 · rows 상한 · 무관 행 10배에도 rows 불변 · **테넌트 넘는 인덱스 금지**) ⑤ 🆕 지급 축 「다른 거래처 동시」([4] 미실측 ④) ⑥ CI GATES 유지.

## 19. 🟢 PM 판정 — 갈래 S4 중단분 (브라운킴 · 2026-09-21)

> 근거: S4 개발명세서 §3·§6·§7 · **PM 재실측 2026-09-21**(위험 33406 14사례 · 실물규모 사본 `hitpan_v4_a` · 스키마).
> 이 절이 S4 재개의 근거다. 여기 없는 것은 하지 않는다(#42 · #33).

### 19-0. PM 재실측 (판정 전에 내가 직접 잰 것)

| 잰 것 | 결과 |
|---|---|
| wip `2110dfe7` 빌드 | 🟢 **errors 0 · warnings 0** — 인계서의 「빌드 미확인」 해소 |
| 위험 33406 `RcSafePathGate` 14사례 | **통과 13 · 실패 1 · skip 0** — G-RC12(B4)·G-RC14 는 **이미 초록**(명세서 §6-1·§6-3 은 그 뒤 커밋으로 해소됨). 남은 빨간불은 **G-RC13 하나** |
| G-RC13 실패 내용 | `L3 PayableMatchedLegacyLockedSql` · 표 `lp` 가 **`idx_pay_partner`**(첫 칸 `partner_id`)를 고른다 |
| `hitpan_v4_a`(실물규모 사본) | partners 13,543행 · payments 2,228행 · **회사 1개** → 「회사를 넘는 거래처 0건」은 ⚠️ **증거가 못 된다**(회사가 하나뿐이라 넘을 수가 없다) |
| 스키마 (결정적) | `partners.partner_id` 는 **단일칸 PRIMARY KEY** → 한 값은 표 전체에 **한 행**뿐이고, 그 행의 회사는 하나다. `return_id`·`receipt_id`·`item_id`·`collection_id`·`payment_id` 도 같다. 반면 `payment_type`·`payment_date`·`ref_order_id` 는 **아니다** |

### 19-1. 🔴 본안 판정 — 「L3 가 테넌트 넘는 인덱스를 고른다」 (인계서 §4-2)

**판정: 설계 4판 안 연다. 제품 SQL·DDL 무변경. 고칠 것은 게이트의 단언 축이다.**

근거 셋 —
1. **「테넌트 접두」는 목적이 아니라 대리물이다.** 우리가 지켜야 하는 것은 *「잠그는 범위에 남의 회사 행이 못 들어온다」*이지 *「인덱스 첫 칸이 `tenant_id` 다」*가 아니다. `partner_id` 는 단일칸 PK 라 **한 값 = 한 회사**다. 대리물이 목적을 잘못 잡은 자리다.
2. **같은 판단을 구현이 이미 했다.** wip 은 `fk_pr_partner`(= `purchase_receipts.partner_id`)·`idx_return`·`PRIMARY` 를 「전역 유일 id 라 남의 회사가 못 들어온다」는 이유로 면제했다. `idx_pay_partner` 는 **글자 그대로 같은 모양**이다. 같은 이유를 하나에만 적용하지 않는다.
3. **잠금 범위가 넓어지지도 않는다.** `idx_pay_partner`(partner=Y) 가 잠그는 행 집합 = `idx_pay_tenant_partner`(tenant=X, partner=Y) 가 잠그는 집합과 **같다**(Y 의 회사가 X 하나이므로). 바꿔서 얻을 것이 없다.

🔴 **다만 면제를 이름 목록으로 두지 않는다.** wip 의 `TenantPrefixExempt = {idx_return, fk_pr_partner, PRIMARY}` 는 **손으로 적은 예외**라, 나중에 진짜 위험한 인덱스가 끼어들어도 이름만 더하면 조용히 초록이 된다(함정 #2 「감시자 0」의 재발 경로다). 대신 **스키마에 물어서** 판정한다:

> **면제 규칙(S4-3 확정)** — 고른 인덱스의 첫 칸이
> ① `tenant_id` 이거나,
> ② **어느 표의 단일칸 PRIMARY KEY** 인 칸(= 값 하나가 한 회사에만 속한다) 이고,
> ③ **그 표에서 실제로 그 칸이 두 회사에 걸치지 않음을 시험 DB(회사 2개 시드)에서 세어 확인**했을 때.
> 셋 다 `information_schema` 와 실제 COUNT 로 **잰다**. 손으로 적은 이름 목록은 **삭제**한다.

### 19-2. 🔴 내가 §18 에 잘못 적은 것 (자백 · 정정)

§18 **PM 판정 ①** 은 인덱스 ③ `idx_pay_tenant_partner` 를 「**테넌트를 넘어가는 `idx_pay_partner` 를 고를 여지를 막는다**」는 이유로 유지시켰다. **틀렸다.** 실측하니 인덱스 ③ 이 있어도 옵티마이저는 **그대로 `idx_pay_partner` 를 고른다.** 막아주지 못한다. 「기대는 실측 전까지 사실이 아니다」(인계서 §5-1)를 **내가 다시 했다.**

- 인덱스 ③ 은 **유지한다** — 지우는 것도 DDL 변경이고(#1), 통계가 바뀌면 `idx_pay_tenant` 대신 고를 여지가 있어 보험으로 값이 있다. 다만 **「지금 8문장 중 아무도 안 고른다」**를 DDL 주석과 백로그에 **사실대로** 적는다. 「넣었으니 됐다」로 끝내지 않는다(§17 C-3).
- 부수 판정: §17 **C-3**(인덱스가 안 통하면 처방 (가) 재상정)은 **발동하지 않는다** — 안 통한 것이 아니라 **다른 인덱스가 같은 범위를 잠그고 있었다.**

### 19-3. 나머지 S4 안건 판정

| # | 안건 | PM 판정 |
|---|---|---|
| **S4-1** | 「다른 거래처 동시 = 교착 0」을 완료 조건에서 내릴 것인가 | **내린다 — 이미 §3(인계서)에서 확정.** 대신 ① 교착 상대가 매칭 잠금 인덱스가 **아닐 것** ② 재시도가 받아 사용자에겐 성공 ③ DB-124 DROP 시 빨간불. wip 구현이 이 축이다 — **승인** |
| **S4-2** | 지급 축 B4 잔여 차단 — 추가 실측을 붙일지 / 갈래 S5 로 뺄지 | **S5 로 빼지 않는다.** wip 이 붙인 「막힌 상대를 `INNODB STATUS` 로 찍고, 매칭 잠금이면 빨간불 · 아니면 건수를 고정값(1)으로 못 박는다」가 정답이다 — **승인**. 원인(유니크 간극)은 §19-4 별건 |
| **S4-3** | G-RC13 단언 축 변경 | **승인 · 단 §19-1 의 면제 규칙으로.** 이름 목록 방식은 반려 |
| **S4-4** | 병렬이슈53 원인 범위 정정(하나 → 둘) | **승인** — 「매칭 잠금 범위 + 수기 등록 유니크 간극」 둘이다. 병렬이슈 53 본문에 한 줄 |
| **S4-5** | 수기 등록 `source_id` 는 제품 변경이라 설계로 돌려보내야 한다 | **승인** — 이번 범위 밖. §19-4 |

### 19-4. 범위 밖으로 확정 (별건)

**수기 등록 `source_id` NULL 유니크 간극**(교착 원인 ②) — `uq_payments_source`·`uq_collections_source` 의 NULL 자리에서 RR 간극 잠금이 부딪힌다. 금액·멱등 키에 닿는 변경이라 **별건 설계**로 연다(인계서 §4-4). S4 는 **덮지 않고 고정만** 한다(고정값 1 · 늘면 빨간불).

### 19-5. S4 재개 시 남은 일 (이 목록이 완료 정의다)

| # | 할 일 | 상태 |
|---|---|---|
| ㉠ | G-RC13 면제 규칙을 §19-1 대로 구현 — 이름 목록 삭제 · `information_schema` + 실제 COUNT 로 판정 | ⬜ |
| ㉡ | 위험 33406 **14사례 전부 초록 · skip 0** | ⬜ |
| ㉢ | **무력화 4건 실측 표**(①반품 꼬리절 ②문구 한 글자 ③DB-124 4개 DROP ④`USE INDEX` 강제) — 실제로 넣고 FAIL 을 본 기록 | ⬜ |
| ㉣ | CI `GATES` 등록 · 주석 정정(「다른 거래처 동시는 교착이 안 난다」는 **기대값이 아니다** — §19-3 S4-1) · DDL 적용 스텝이 DB-124 를 포함하는지 확인 | ⬜ |
| ㉤ | 안전 33306 회귀 40 + 마이그·DDL 274 + 빌드 0/0 | ⬜ |
| ㉥ | 개발명세서 갱신(§3-2·§3-3·§6 은 **해소됨**으로 · §7 은 이 절로 답함) · DDL 주석에 §19-2 사실 | ⬜ |

**판정**: PM 브라운킴 · 2026-09-21 / **작성자 ≠ 결재자**: 구현은 개발팀, 판정은 PM, 다음은 [4] 검증팀장 재검증.

### 19-6. 🔴 무력화 실측 중 나온 판정 (PM · 2026-09-21 · ㉢ 수행 중)

무력화 5건을 **실제로 넣고 돌렸다**. 그 과정에서 작지 §16-2 의 기대와 **다른 것이 둘** 나왔다. 덮지 않고 여기 적는다.

| # | 무력화 | §16-2 기대 | **실측** | 판정 |
|---|---|---|---|---|
| ① | `ReceiptReturnedSql` 잠금 꼬리절 제거 | G-RC6 빨간불 | 🟢 **기대대로** G-RC6 FAIL | — |
| ② | `MsgReceiptOver` 한 글자 변경 | G-RC6 빨간불 | 🔴 **14/14 초록 그대로** | **§19-6-1** |
| ③ | DB-124 인덱스 4개 제거 | G-RC12·13·**5** 빨간불 | 🟡 G-RC12·13 FAIL · **G-RC5 는 초록** | **§19-6-2** |
| ④ | `DeliveryUsedSql` 에 `USE INDEX` 강제 | G-RC13 빨간불 | 🟢 **기대대로** FAIL | — |
| ⑤🆕 | L3 에 `FORCE INDEX(idx_pay_type)` 강제 | (PM 추가) | 🟢 **새 안전판정이 그 이유로** FAIL | 규칙에 이빨 있음 확인 |

#### 19-6-1 🔴 무력화 ② 는 **원리상 성립할 수 없는 항목이었다**

§18 은 「게이트가 **제품 문구 상수를 쓴다**」(복제 금지)를 요구했고, §16-2 는 동시에 「**문구 상수를 한 글자 고치면 빨간불**」을 요구했다. **둘은 같이 성립할 수 없다** — 기대값을 제품 상수에서 만들면 상수를 고칠 때 양쪽이 같이 바뀐다. 실측이 그것을 드러냈다(초록 유지).

**판정: 무력화 ② 는 폐기하지 않고 축을 바꾼다.** 고객이 보는 문구는 조용히 바뀌면 안 된다(#23 · #25 · C-2 가 사장님 판단을 받은 이유). 공용 식에 이미 쓰는 **SHA 앵커 방식**(G-RC11)을 문구에도 적용한다 —

> **G-RC11 에 문구 앵커를 더한다**: 고객 노출 문구 4개(`MsgDeliveryOver`·`MsgReceiptOver`·`MsgMatchBusyCollection`·`MsgMatchBusyPayment`)의 **본문을 기록값으로 박아 둔다**. 고치면 빨간불이 나고, 앵커를 같이 고쳐야 통과한다 = **고객 문구 변경이 의도적인 행위가 된다.**

이것은 갈래 S4 의 파일(게이트 1개) 안에서 끝난다 — **제품 무변경**이라 설계로 올리지 않는다.

#### 19-6-2 🟡 무력화 ③ 에서 G-RC5 는 안 걸린다 — 사실대로 적는다

DB-124 를 전부 빼도 **G-RC5 는 초록**이었다. 이유: G-RC5 의 축은 PM 판정 S4-1 로 「**교착 상대가 매칭 잠금이 아닐 것**」이 되었는데, 인덱스가 없는 상태에서도 **먼저 부딪히는 상대가 `uq_*_source` 유니크 간극**(교착 원인 ②)이라 그 단언이 그대로 통과한다.

**판정: G-RC5 를 고치지 않는다.** 이 무력화의 감시자는 **G-RC12·G-RC13** 이고 둘 다 빨간불이 났다 — 감시자는 0 이 아니다. G-RC5 에 「DB-124 가 없으면 빨간불」을 억지로 붙이면 §19-4 별건(유니크 간극)과 축이 뒤섞인다. 대신 **작지 §16-2 의 「G-RC5」 기대는 여기서 정정**한다.

🔴 남는 사실: **「DB-124 제거 → G-RC5 빨간불」은 성립하지 않는다.** 다음 사람이 그 기대로 읽지 않도록 개발명세서 무력화 표에도 같은 줄을 적는다.

## 20. 🟢 PM 판정 — [4] 재검증 결과 수용 (브라운킴 · 2026-09-21)

> [4] 작업리뷰서 `docs/검증/erp/20260921_작업리뷰서_20260920작1_S4재개분.md` · 판정 🟡 조건부.
> **구현자와 결재자가 같았던 건이다**(PM 직접 수행). 검증이 내 근거를 반증했고, **전부 받는다.**

### 20-1. 🔴 두 번째 자백 — §19-1 근거 3 이 틀렸다

내가 §19-1 에 적은 세 번째 근거는 이것이었다:

> 「`idx_pay_partner`(partner=Y) 가 잠그는 행 집합 = `idx_pay_tenant_partner`(tenant=X, partner=Y) 가 잠그는 집합과 **같다**」

**틀렸다.** *일치하는 행*의 집합은 같지만, RR 의 잠금은 **넥스트키 = 일치 행 + 그 옆 간극**이다.
`idx_pay_partner` 는 회사 구분 없이 `partner_id` 하나로 전역 정렬되므로 **간극의 이웃이 남의 회사 행**이다.
`idx_pay_tenant_partner` 는 회사 안에서만 이웃이 생긴다. **두 인덱스의 잠금 구간은 같지 않다.**

🔴 더 뼈아픈 것: **우리 문서가 이미 그 사실을 적고 있었다.** S4 명세서 §3-4 와 게이트의 `SeedGapSeparatorAsync` 주석은
「인덱스 순서상 바로 옆에 끼어드는 INSERT 는 거래처가 달라도 막힌다」를 **실측 결과로** 적어 두었다.
나는 같은 문서의 한쪽을 사실로 쓰면서 다른 쪽에서 그와 모순되는 문장을 안전 근거로 삼았다.
**인계서 §5-1(「기대는 실측 전까지 사실이 아니다」)을 이 트랙에서 세 번째로 반복했다.**

⚠️ 다만 **데이터가 새는 문제는 아니다** — L3 의 `WHERE` 에 `tenant_id` 가 있어 읽히는 값은 그대로 그 회사 것뿐이다.
넘어가는 것은 **잠금 구간**이다(= 남의 회사 등록이 막힐 수 있다 · 가용성 문제).

### 20-2. 그래서 결론이 바뀌는가 — **안 바꾼다. 대신 잰다.**

「집합이 같으니 안전하다」가 무너졌으므로, **안전하다는 결론을 그대로 두면 그게 받아쓰기다.**
그렇다고 제품 SQL 에 인덱스를 강제하는 것(설계 4판)을 **추측으로** 열지도 않는다. 사이에 있는 것은 하나뿐이다 — **실측.**

> **판정: G-RC12 에 「다른 회사」 축을 더한다.** A 가 회사 T 에서 RR 매칭 잠금을 쥔 동안,
> **다른 회사**의 지급·수금 INSERT 가 막히는지 잰다.
> - **안 막히면** → 결론 유지(제품 무변경) · 근거는 「집합이 같다」가 아니라 **이 실측**이 된다.
> - **막히면** → 🔴 **멈추고 설계 4판**(L3 의 인덱스 축 고정)으로 올린다. 그때는 §17 C-3 이 진짜로 발동하는 자리다.

### 20-3. 나머지 지적 — 전부 수용

| # | 지적 | 판정 |
|---|---|---|
| **F-2** 🔴 | 조건 ③(두 회사에 걸친 값 0건)은 시드가 `noise-{회사8자리}-NNNN` 라 **구조상 항상 0** = 감시자 0 | **수용** — 음성 대조군을 넣는다: B 회사에 A 회사의 `partner_id` 로 1행을 심고 ③ 이 **빨간불을 내는지** 잰다. 못 내면 그 조건은 장식이다 |
| **F-3** 🟡 | 단일칸 PK 질의에 `TABLE_NAME IN @T` 가 빠져 DB 전체 PK 이름 ~110개(`id` 포함)가 면제 후보 | **수용** — 손으로 적은 목록 3개를 자동 생성 목록 110개로 바꾼 꼴이다. 관련 표로 좁힌다 |
| **F-4** 🟡 | `DB-124_lock_scope_indexes.sql`·출하 DDL 주석이 여전히 「`idx_pay_partner`(테넌트를 넘어간다)」 — §19-5 ㉥ 미완인데 자백도 없음 | **수용** — 주석을 §19-2·§20-1 사실로 고친다(인덱스는 그대로 · 주석만) |
| **F-5** 🟡 | CI 주석의 근거가 「개발명세서 §4」인데 그 절은 「무력화 못 했다」 | **수용** — §8-4 로 정정 |
| **F-6** 🟡 | G-RC13 이름표는 「DB-124 인덱스를 타고」인데 그 단언은 삭제됨 · `RequiredKey` 는 죽은 칸 | **수용** — 이름표를 실제로 재는 것으로 바꾸고 죽은 칸을 지운다 |
| **F-7** 🟡 | 완료정의 ㉤(안전 33306 회귀)이 명세서 §8-6 에 없고 자백도 없음 | **수용** — PM 이 실측한 값을 §8-6 에 적는다(전체 1,528 중 1,524 통과 · 실패 4 는 `BackupCredentialGate` · 중단 시점과 동일 · `RcSafePathGate` 실패 0) |
| **F-8** 🟢 | `IsSafe` 의 ③ 은 측정값이 없으면 통과(fail-open) | **수용** — 모르면 **막는다**(fail-closed)로 바꾼다 |
| — | §19-6-2(G-RC5 축 분리)는 정당하나 §16-2 표에 정정 표시 필요 | **수용** — §16-2 표의 G-RC5 칸은 §19-6-2 로 정정됐음을 여기 명시 |

### 20-4. ⚠️ [4] 가 못 잰 것 (한계 명시)

이번 [4] 는 **시험·빌드 금지 조건**에서 돌았다(PM 이 같은 DB 에서 전체 회귀 중이라 충돌 방지). 따라서
**§8 의 모든 숫자(15사례 초록 · 무력화 5건 FAIL · 빌드 0/0)는 독립 재현이 안 됐다.** F-1·F-2 도 코드·정의 기반 논증이지 실측이 아니다.
⇒ **봉합 후 [4] 를 다시 돌린다 — 이번에는 시험을 직접 돌리게 한다.** 그 전에는 [5] 로 올리지 않는다.

**판정**: PM 브라운킴 · 2026-09-21

## 21. 🔴 PM 판정 — **S4 중단 · 설계 4판 필요** (브라운킴 · 2026-09-21)

> §17 **C-3** 이 드디어 진짜로 발동했다: *「옵티마이저가 DB-124 를 안 고르면 **멈추고 PM 보고**」*.
> 아래는 전부 **실측**이다. 추측 문장은 이 절에 없다.

### 21-1. 실측 ① — 다른 회사 차단의 원인은 **인덱스 축이 아니다** (F-1 가설 기각)

§20-2 대로 G-RC12 에 「다른 회사」 축을 넣고, **대조군을 같이** 넣었다 —
이웃(거래처 id 앞 35글자가 같음) 2건 + **멀리 떨어진** 대조군 2건.

| 사례 | 결과 | 찍힌 대기 상대 |
|---|---|---|
| B7 다른 회사 `payments` INSERT (이웃) | 🔴 1205 (3.0초) | `uq_payments_source` 간극 |
| B8 다른 회사 `collections` INSERT (이웃) | 🔴 1205 (3.0초) | (못 찍음) |
| **B9 다른 회사 `payments` INSERT (대조군·멂)** | 🔴 1205 | `uq_payments_source` 간극 |
| **B10 다른 회사 `collections` INSERT (대조군·멂)** | 🔴 1205 | (못 찍음) |

**넷 다 막혔다.** 이웃만 막혔어야 「인덱스 축(간극 이웃)」이 원인인데, **먼 것도 똑같이 막혔다.**
⇒ [4] F-1 이 제기한 「`idx_pay_partner` 의 이웃 간극이 회사 경계를 넘는다」는 **이 시나리오에서는 원인이 아니다.**
잡힌 상대는 `uq_*_source` 의 NULL 간극 — **이미 별건으로 뺀 교착 원인 ②**(§19-4)다.

🔴 **대조군이 없었다면 나는 인덱스를 고쳤을 것이다.** 그리고 그건 엉뚱한 곳을 고치는 것이었다.
다만 **「다른 회사가 막힌다」는 사실 자체는 남는다** — 별건 ②의 심각도가 올라간다(같은 회사만이 아니라 **남의 회사까지** 막는다).

### 21-2. 🔴 실측 ② — **DB-124 인덱스를 옵티마이저가 안 고른다** (실물 규모에서 확인)

게이트가 L4 에서 빨간불을 냈다(`idx_pay_type` 선택 · **3회 연속 재현**). 시험 시드가 작아서일 수 있어
**실물 규모 사본**으로 다시 쟀다 — `rc_l4_probe`(33306 · 출하 DDL = DB-124 포함 · `hitpan_v4_a` 의 실제 행 복사:
payments 2,228 · purchase_receipts 1,962 · partners 13,543 · `ANALYZE TABLE` 후).

| 문장 | 고른 인덱스 | 첫 칸 | rows | 판정 |
|---|---|---|---|---|
| L1 미수 이월잔액 | `uq_collections_source` | `tenant_id` | 1 | 🟢 회사 경계 안 (DB-124 는 아니지만 안전) |
| **L3 미지급 이월잔액** | **`idx_pay_type`** | **`payment_type`** | 1 | 🔴 **회사 경계를 넘는다** |
| **L4 이관 매입 지급** | **없음 — `type=ALL` 2,196행** (BNL 조인) | — | 2196 | 🔴 **payments 를 통째로 훑으며 잠근다** |

🔴 **S3 0단계 실측(「후 8문장 전부 신설 인덱스 · rows ≤ 4」)은 시험 시드 규모에서만 성립했다.**
실물 분포에서는 조인 순서가 뒤집혀(`pr` 를 `fk_pr_partner` 로 먼저 몰고) `payments` 를 전체 스캔한다.
= 우리가 병렬이슈53 을 고치려고 넣은 인덱스가 **실제 고객 데이터에서는 안 쓰인다.**

### 21-3. 판정

| # | 판정 |
|---|---|
| **21-3-1** 🔴 | **갈래 S4 를 여기서 멈춘다.** 남은 항목(㉣ CI · ㉥ 문서)은 했으나, **㉡(전 사례 초록)은 달성 불가** — G-RC13 이 **정당하게** 빨간불이다. 초록으로 만들려면 게이트를 무르게 해야 하는데 그건 감시자를 없애는 일이다(인계서 §5-2) |
| **21-3-2** 🔴 | **설계 4판을 연다.** 안건: **L3·L4 의 잠금 축을 실제 고객 데이터에서 보장하는 방법.** 후보 — ⓐ `payments(tenant_id, ref_order_id)` 등 조인 방향에 맞는 인덱스 신설(DB-125) ⓑ 문장 재작성(⚠️ §17 에서 이미 기각된 「덜 세기」 위험 재검토 필요) ⓒ 조인 순서 고정. **PM 이 고르지 않는다 — 설계팀장 몫이다** |
| **21-3-3** | **머지하지 않는다 · PR 올리지 않는다.** 4판 결과가 나오면 S5 로 이어 붙인다 |
| **21-3-4** | 지금까지의 S4 작업(게이트 개편 · 문구 앵커 · 무력화 5건 · 대조군)은 **버리지 않는다** — 이번 사실을 **잡아낸 것이 바로 그 게이트**다. 브랜치에 커밋해 둔다 |
| **21-3-5** | 별건(교착 원인 ②)의 심각도 상향: 「같은 회사 수기 등록끼리」 → **「다른 회사 등록까지 막는다」**(§21-1) |

### 21-4. ⚠️ 내가 못 잰 것

1. **`collections` 축 차단의 상대를 못 찍었다**(B8·B10) — 관측 장치가 `INNODB STATUS` 의 해당 블록을 못 찾았다. 「매칭 잠금이 아니다」는 payments 축에서만 증명됐다.
2. **실물 규모에서의 잠금·교착 실측은 안 했다** — 잰 것은 `EXPLAIN` 계획뿐이다. 계획이 나쁘면 잠금도 넓다는 것은 합리적 추론이지만 **실측은 아니다.**
3. `rc_l4_probe` 는 **내가 만든 측정용 DB** 다(33306). 4판에서 쓰라고 지우지 않고 뒀다.
4. [4] 재검증은 **시험 실행 금지 조건**에서 돌았다(§20-4) — 봉합 후 재검증이 아직 안 됐다.

**판정**: PM 브라운킴 · 2026-09-21 · **다음: [2] 설계팀장 존 — 4판** · 그 전에 **사장님 보고**
