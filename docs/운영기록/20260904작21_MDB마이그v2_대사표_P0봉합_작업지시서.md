# 작업지시서 20260904작21 — MDB 마이그 v2 · W1 대사표 + W2 P0/P1 봉합

| 항목 | 내용 |
|---|---|
| 작성 | 설계팀장 존 · 2026-09-04 |
| **결재** | ✅ PM 브라운킴 (2026-09-04 — 사장님 전결 위임 [전결1](20260904전결1_MDB마이그v2_Q1-Q12_PM전결서.md)) |
| 우선순위 | P0 |
| 진행 | ✅[3]개발(9/7 · 세 갈래 병렬) → ✅[3-V]동시검증(병렬이슈 9~13 · 작지서 오기 2 + 5월 결함 3 봉합) → ✅[4]검증 조건부→통과(리뷰서 `docs/검증/erp/20260907_작업리뷰서_MDB마이그v2_대사표_P0봉합_조건부.md` · 문서 정정 3건 반영) → ✅[5]CTO 반증·PM 전결(`docs/운영기록/20260907결1_…`) → 🟡[6]커밋 `45e94d92`(브랜치 push 완료 · PR 생성은 권한 차단 → 사장님 수동) |

## 0. ⭐ 사장님 오더 (원문 그대로)
> 1. 히트판erp에서 마이그레이션 후 레거시 히트판에서 뽑는 값이랑 같은 값이 나와야 하고 2. 마이그레이션 속도도 굉장히 중요함. 3. 안정적으로 데이터 정합성, 무결성이 맞아야 함. 4. 마스터자료, 판매자료, 매입자료, 계산서, 회계, 재고, 등 모든 자료가 맞아야 함. 5. 히트판ERP에는 있고, 레거시에 없는 기능들은 NULL값으로 남겨둬야 하고, 6. 마이그레이션 옵션이 두개가 되어야 함. 1덮어쓰기, 2병합 7. 편리해야함. 폴더경로의 주소를 붙여넣기도 할수있게, 파일경로를 직접 찾을 수 있게. 8. 히트판ERP의 BOM은 레거시 히트판의 셋트조립과 동일한 기능을 함.
> *(9/4 추가)* 1~12 모두 전결. 1. 설계팀 설계 2. 개발팀 코딩 3. 검증팀 검증 1-4. 임원들 실시간 동시검증. 루틴대로!! 문서로 주고받고 일하고, 문서는 폴더에 보관,관리 인덱스 갱신!!

## 1. 오더를 이렇게 이해했다
- 이번 차수는 오더 **1·3·4·5·8** 을 만든다(같은 값 · 정합 · 모든 자료 · NULL · BOM). **6·7 은 작22**, 2(속도)는 이번에 **베이스라인을 재고** 목표를 작22 에서 확정한다.
- 안 하면: 지금 이관은 **매출과 매입이 반대**로 들어가고 **재고 수량이 전부 0** 이다 — 고객이 이관 직후 재고현황·매출현황을 보면 첫 화면에서 끝난다.
- 실측 근거: [선행검증서 20260904검2](../검증/선행/20260904_선행검증서_MDB마이그v2_8개전제_실측.md) (MDB 실물 읽기 전용 + 코드 줄 번호) · 결정 근거: [전결1](20260904전결1_MDB마이그v2_Q1-Q12_PM전결서.md)

## 2. 무엇을 하는가
### 하는 것 — 세 갈래 (병렬 · 파일 겹침 0)

**갈래 A — `MdbMigrationService.cs` 봉합 + `LegacyMdbMapping` 순수함수 (백엔드 개발자 1)**
- [x] A0 신설 `src/HitPan.Application/Services/LegacyMdbMapping.cs` — **판정을 전부 순수 static 함수로** 빼서 서비스가 이것만 부른다(게이트가 실제로 부를 수 있게). 시그니처 고정:
  - `static string DeliveryKind(int ijIo)` → `"purchase"`(1) / `"sales"`(2) / 그 외 `throw`
  - `static (string MoveType, decimal QtyIn, decimal QtyOut) LedgerMove(string ijIo, decimal qty)` → 1: in / 2: out · **음수는 반대 칸에 절대값**
  - `static string LedgerSourceId(string dt, string io, int seq, int buy, int sun)` → `$"mb-{dt}-{io}-{seq}-{buy}-{sun}"` (≤ 36자 보장 — 초과 시 throw)
  - `static string TaxDirection(string txIo, string txGu)` → `"1"→"B"`, `"2"→"S"`, `"S"/"B"` 통과, 빈값이면 `txGu` 같은 규칙, 그래도 없으면 `"S"`
  - `static (decimal Debit, decimal Credit) JournalSides(decimal scCr, decimal scDr)` → `SC_CR`=차변·`SC_DR`=대변(현행 swap 유지) · **음수는 반대편 양수** · 양쪽 0 → `(0,0)`
  - `static (string Kind, string Method) PartnerLedgerKind(string sGu)` → Kind ∈ `"collection"`(1·2·3·4·5) / `"payment"`(B·C·D·E·F) / `"skip"`(0·A·그 외) · Method: 1 cash · 2 bank_transfer · 3 bank_transfer · 4 check · 5 card · B cash · C bank_transfer · D check · E card · F cash
  - `static string CashbookDirection(string acJen)` → `"skip"`(0) / `"income"`(1·3) / `"expense"`(2·4) / 그 외 `"expense"`
- [x] A1 P0-A 방향 — `:4275-4300` `io == 1` 분기 → `DeliveryKind` 로 · 계산서 `:3148-3160` direction → `TaxDirection` (D2)
- [x] A2 P0-B 수량 — `:1820-1836` → `LedgerMove` · `unit_cost = |amt|/|qty|`
- [x] A3 P0-C 누락 3갈래 — `:1825` → `LedgerSourceId`(D3) · **품목**: `MigrateCoreAsync` 1단계(`pyojun_master` 스텝) 안에 `RegisterUnlistedDocfbItemsAsync(pandataPath, …)` 신설 → DOCFB `DISTINCT IJ_PUM, IJ_KU` 중 itemMap 에 없는 키를 `EnsureMigAutoItemAsync` 로 등록하고 itemMap 에 넣는다(D1 · 병렬 단계 전에 끝내 공유 딕셔너리 무변경) · 원장 `:1810` `continue` 제거(이제 전부 매핑됨) · 명세서 `:4256` `skipPartner → continue` 를 `EnsureLegacyFallbackPartnerAsync` 호출로(주석이 이미 그렇게 돼 있다)
- [x] A4 P0-D 회계 — `:4960-5000` → `JournalSides` · `(0,0)` 은 카운트 후 제외 · `drCr` 라벨은 전환 후 값으로
- [x] A5 P0-E 수금/지급 — `MigrateCollectionsAsync :2118-2200` → `PartnerLedgerKind` 로 3분기: collection → 현행 경로(금액 = `S_SUK`) · payment → **신설 `MigratePaymentsAsync`**(`payments` INSERT IGNORE, `payment_type='payment'`, `amount=S_BAL`, `memo="[레거시 S_GU={gu}] {S_REM}"`, `source_type='migration'`, `source_id=$"mig-{buy}-{ymd}-{sun:D5}-{gu}"`) · skip → 카운트만 로그. `:2153` `S_BAL` 대체 로직 **삭제**
- [x] A5-DDL **DB-118** `src/HitPan.API/Migrations/SQL/DB-118_payments_source.sql` — `payments` 에 `source_type varchar(30) NULL`, `source_id varchar(80) NULL`, `migrated_source_hash char(64) NULL`, `UNIQUE KEY uq_payments_source (tenant_id, source_type, source_id)` — 멱등 관용구(다른 DB-1xx 와 같은 `SET @col_exists` 패턴) + `installer/hitpan_db_clean.sql` 편입(#36)
- [x] A6 P1 창고 — `EnsureMigrationWarehouseAsync :824` → `WarehouseLookup.ResolveTenantDefaultWarehouseAsync` 로 기본창고를 찾고 없을 때만 `wh_code='MAIN'` 생성 · 반환값을 `defaultWarehouseId` 로(상수 `"wh-migration"` 폐기)
- [x] A7 P1-F 현금출납 — `:2365-2400` → `CashbookDirection`(월계 skip · 입/출 분기) · description 은 `AC_JEK`, memo 는 `AC_cheri`
- [x] A8 Q12 — items `S_SET` ∈ {1,2,3} → `memo` 앞에 `[셋트:{S_SET}] ` · `MigrateBomAsync :1162` `bom_items.memo` = `RT_GU` + (`RT_SON`/`RT_KUM` 0 아니면 ` | 원가 {SON} 금액 {KUM}`)
- [x] A9 결과 클래스 `MdbMigrationResult` 에 `Payments` 추가 + `Total` 반영 · UI 카드 키 `"payments"` 는 갈래 C 가 받는다
- [x] A10 **게이트** `src/HitPan.Tests/Integrity/MdbMigrationV2MappingGateTests.cs` — `LegacyMdbMapping` 7개 함수를 **실제로 호출**해 판정. 각 케이스는 선행검증서 숫자에서 뽑는다(예: `LedgerMove("2", -16)` → `("out", 16, 0)`… 아니다 — 매출 음수는 **입고** ⇒ `("in", 16, 0)`). 🔴 반드시 **무력화 시험**을 주석으로 남긴다: 어느 줄을 되돌리면 어느 테스트가 빨간불인지

**갈래 B — 대사표 서비스 + API (백엔드 개발자 2)** — `MdbMigrationService.cs` **손대지 않는다**
- [x] B1 신설 `src/HitPan.Application/Services/MdbReconciliationService.cs` (+ 같은 파일에 DTO) — `Task<MdbReconciliationReport> BuildAsync(string folderPath, string? mdbPassword, string tenantId, CancellationToken ct)`
  - DTO: `MdbReconciliationReport { DateTime GeneratedAt; List<ReconItem> Items; List<ReconDiffRow> PartnerDiffs; List<ReconDiffRow> ItemDiffs; int OkCount; int DiffCount; }` · `ReconItem { string Key; string Category; string Label; decimal? Legacy; decimal? Erp; decimal? Diff; string Status /* OK | DIFF | NA */; string? Detail; }` · `ReconDiffRow { string Name; decimal Legacy; decimal Erp; decimal Diff; }`
  - **레거시 쪽은 MDB 를 OLEDB `Mode=Read` 로 직접** 읽는다(비번은 파라미터). 경로 해석은 `MdbMigrationService.ResolveMdbPaths` 와 같은 규칙(절대경로·`..` 차단·대소문자 무시)을 **자체 구현**(서비스 파일 무접촉)
  - 항목(설계 §3 그대로 · 방향은 전결 Q1 · 계열은 Q2): ①판매 DOCFE `IJA_IO=2` 건수·ΣAMT1·ΣAMT2 ↔ `sales_deliveries`(`source_type='migration'`) 건수·Σtotal·Σvat(+DOCFB 헤더그룹 수를 Detail 에) ②매입 `IJA_IO=1` ↔ `purchase_receipts` ③계산서 TX_IO 별 건수·ΣKUM·ΣVAT ↔ `tax_invoices` direction S/B ④원장 DOCFB 행수·순수량(ΣIO=1 qty − ΣIO=2 qty)·ΣIJ_AMT ↔ `stock_ledger` 행수·(Σqty_in−Σqty_out)·Σsupply_amount ⑤재고 DOCFC 최신 `IM_YM` ΣIM_CQTY·ΣIM_CAMT ↔ `item_stock` Σcurrent_qty (+ 품목별 상위 20 차이 = `ItemDiffs`, 키 = `품명|규격` ↔ `items.item_name|spec`) ⑥회계 DOCF7 유효행수·Σ차변·Σ대변(`JournalSides` 규칙과 동일하게 음수 전환) ↔ `journal_lines` migration 행수·Σdebit·Σcredit ⑦수금 DOCF5 S_GU 1~5 ΣS_SUK ↔ `collections` Σamount ⑧미수 거래처별 ΣS_BAL−ΣS_SUK>0 합 ↔ `FinanceService` 미수 식과 동일 SQL(`:770-772`) (+ 거래처별 상위 20 차이 = `PartnerDiffs`) ⑨미지급 거래처별 음수 합 ↔ 미지급 식(`:778-781`) ⑩마스터 4종 건수
  - 🔴 **새 SQL 전부 DESCRIBE 선행**(#13) — 컬럼명을 추측하지 않는다. `hitpan_e2e` 로 확인
  - 상태: `Diff == 0` → OK · 레거시/ERP 한쪽 없음 → NA · 그 외 DIFF
- [x] B2 `MigrationController` 에 `GET legacy-mdb/reconcile?folderPath&mdbPassword` (TenantAdminOnly · tenant 는 `HttpContext.Items["TenantId"]`) · `Program.cs` DI `AddScoped<MdbReconciliationService>()` · Windows 전용 pragma 는 `MdbMigrationService` 등록 줄과 같은 방식

**갈래 C — 화면 (프론트 개발자 1)** — `src/HitPan.Web/Pages/Settings/MdbMigration.razor` 만
- [x] C1 이관 완료 시(`_migrationResult` 세팅 직후) `GET api/migration/legacy-mdb/reconcile` 호출 → 결과 카드 아래 **「레거시 대사표」** 표: 항목 | 레거시 | 히트판 | 차이 | 판정(✅/❌/—) · 상단에 `OkCount/총` + 차이가 있으면 「차이 N건 — 아래 목록을 확인하세요」 · 거래처·품목 차이 상위 20 을 접이식으로
- [x] C2 미리보기 성공 시 같은 API 를 불러 **레거시 열만** 채운 「가져올 자료 요약」 표(히트판 열은 "이관 전")
- [x] C3 진행 카드에 `"payments"`(지급) 카드 1개 추가(16→17) · `_totalTables` 갱신
- [x] C4 문구는 개발용어 0 — "대사" 는 화면에 **「레거시와 숫자 맞춰보기」** 로

### 하지 않는 것 (명시)
- 두 모드(덮어쓰기/병합) · 찾아보기 · 업로드 · 1/2단계 분리 · 체크포인트 재개 → **작22**
- 수주·발주 소급 생성 · 양식 30종 · 화면 리디자인 · DOCME 등 버림 표 이관
- `expenses ← DOCF7` 이중 표시 여부 정리(관찰만 병렬이슈에 남긴다)
- `TX_PDT` 이상값 42건 처리(관찰만)

## 3. 헌법 체크
| 조항 | 왜 걸리나 | 어떻게 지키나 |
|---|---|---|
| #1 | 6,099줄 수정 | 갈래 A 는 **자리 지정 수정 + 순수함수 추출**만. 새로 짜지 않는다 |
| #2 | tenant | JWT 클레임 경로 유지 |
| #3 | 원장 | INSERT 만. 음수는 반대 칸 절대값(Q5) |
| #5 | PII | 대사 서비스는 금액·건수만 읽는다. 주민번호 컬럼 SELECT 금지 |
| #13 | 새 SQL | 갈래 B 전 SQL DESCRIBE 선행 |
| #15 | 빈 catch | 금지 |
| #16 | Task.WhenAll + MySqlConnection | 갈래 A 의 품목 전수 등록은 **병렬 단계 전**(1단계)에서 끝낸다 |
| #17·#36 | DB-118 | `ENGINE` 은 ALTER 라 해당 없음 · clean DDL 편입 |
| #19 | 빌드 | errors 0 · warnings 0 |
| #23 | 5중 검증 | 외부 노출 문구에 AI·Claude 단어 0 |
| #37 | 컬럼 제거 | `WH-MIG` 상수는 지우되 DDL 컬럼 제거 0 |
| #39 | 운영 | 실측은 `hitpan_e2e` 만 |
| #42 | 문서 | 이 문서 + 전결1 + 설계 + 개발명세서 + 리뷰서 |

## 4. 어떻게 하는가
- 지휘: PM · 갈래 A/B/C 서브에이전트 **동시** · 파일 겹침 0 (A: 서비스·매핑·테스트·DB-118·clean DDL / B: 대사 서비스·컨트롤러·Program.cs / C: razor)
- 관련 설계: [설계 20260904](../설계/erp/20260904_설계_MDB마이그v2_대사표_덮어쓰기병합.md) §2~§9
- 산출: 개발명세서 → `docs/개발/erp/20260904_개발명세서_MDB마이그v2_대사표_P0봉합.md`
- **베이스라인**: PM 이 `hitpan_e2e` 에 현행(봉합 전) 코드로 1회 이관해 **표별 소요·재고 수량 0·방향 반대**를 숫자로 남기고, 봉합 후 같은 MDB 로 재실측

## 5. 검증 방법 (SoD)
**[3-V] 동시 병렬검증** — 보안상무 안철수: 갈래 산출이 나오는 대로 diff 반증 · 이슈 → `docs/검증/병렬이슈/`
**[4] 최종 검증** — 검증팀장 데이비드 박 → 작업리뷰서 `docs/검증/erp/`
- [x] 빌드 errors 0 · warnings 0 — 9/7 본 트리 0/0
- [x] `dotnet test --filter MdbMigrationV2MappingGate` 통과 **+ 무력화** — 64/64 · 무력화 3자리(DeliveryKind 스왑 G1 FAIL · LedgerMove 스왑 G2 5 FAIL · purchase→payment W5 FAIL): `DeliveryKind` 의 1/2 를 바꾸면 FAIL · `LedgerMove` 를 종전 `"I"/"O"` 비교로 되돌리면 FAIL · `JournalSides` 음수 전환을 빼면 FAIL · `PartnerLedgerKind("0")` 을 collection 으로 바꾸면 FAIL
- [x] `hitpan_e2e` 실측(이 MDB): `SELECT SUM(qty_in),SUM(qty_out) FROM stock_ledger WHERE source_type='migration'` **둘 다 0 아님** · `sales_deliveries` 헤더 ≈ 115,150(IO=2) · `journal_lines` = 27,661 · `stock_ledger` 행수 ≥ 124,000 · 대사표 ①②③④⑥⑦ OK · ⑤⑧⑨ 는 차이를 **숫자로** 기록 — **9/7 2차 실측**(20260907검2): Σqty_in 128,926,315.05 · Σqty_out 100,542,678 · sales 115,503 · purchase 2,171 · journal_lines 27,661 · stock_ledger 124,152 · 대사표 OK 10/DIFF 16(①②③④⑥ 은 금액 0 차 다수·⑤⑧⑨ 는 정의 차이를 숫자로 기록) · 6.5분
- [x] DB-118 멱등: 2회 적용 = 신규 0 — hitpan_e2e 실측 skipped 4·4
- [x] 독립 반증 3건 이상 — 검증팀장 10건: 대사표 −1 감지 · 병합 재이관 멱등(21표 동일 · 255.8s) · 사원 +1·판매 +353 규명 · `note` 필터 0 · DESCRIBE 18표 · #2·#5·#15 · 삭제 −108줄 전수 · **P1 계산서 21쌍 승자 무작위(5월 잠복)**

## 6. 리스크 / 롤백
| 리스크 | 확률 | 대응 |
|---|---|---|
| 방향을 뒤집었는데 다른 곳(리포트·정합성 검사)이 옛 방향을 전제 | 중 | 대사표가 잡는다 · 이관은 `source_type='migration'` 격리라 운영 경로 무영향 |
| 품목 760개 자동 등록으로 상품 목록이 지저분 | 중 | `MIG-AUTO-ITEM` 명명 유지 · 사장님 정리 가능(30차와 동일) |
| `payments` DB-118 이 고객 PC 에서 미적용 | 저 | 러너 멱등 관용구 그대로 · 게시 시 `requiresMigration=true` |
| 대사 SQL 이 60만행에서 느림 | 중 | 인덱스 확인 · 미리보기는 레거시 쪽만 |
- 롤백: 커밋 되돌리기 · DB-118 은 컬럼 추가만이라 무해

## 7. 완료 정의 (DoD)
① 갈래 A·B·C 구현 + 빌드 0/0 ② 게이트 통과 + 무력화 확인 ③ `hitpan_e2e` 실측 숫자가 리뷰서에 ④ 개발명세서·리뷰서·INDEX 갱신 ⑤ 커밋·PR

---
**결재**: ✅ PM 브라운킴 2026-09-04 (사장님 전결 위임) — [3] 착수 가능
