# 일일작업보고서 — 2026-07-31 (금)

- **작성**: PM 브라운킴 (루틴 [7])
- **참조 세션**: 오전 작10 절차복구 → 오후 보안·main보호 → 저녁 7/30 회귀 봉합
- **다음날 이 파일부터 읽는다** (사장님 정의: 다음날=일일보고서 / 같은날 새 창=인수인계서)

---

## 0. 한 줄

**"봉합했다"고 적힌 것 3건이 전부 거짓이었고, 오늘 전부 실물로 확인해 봉합했다.**
main 보호(일주일 무방비) · 시크릿 스캐너(11연속 사망) · taskkill 가드(발동 불가).
**공통 원인: 기록과 실물을 대조하지 않았다.**

---

## 1. 오늘 커밋 (main 기준)

| 시각 | 커밋 | 내용 |
|---|---|---|
| 15:51 | `24e278c` | 작10 [3] 개발명세서 — **절차 위반 사실을 명기** |
| 16:19 | `2a45143` | PR #25 머지 |
| 16:33 | `21f2dbc` | 사고기록 — main 보호 일주일 무방비 |
| 16:42 | `e8e7510` | RV-3~RV-7 판정기준서 (**측정 전** 기준 확정) |
| 16:43 | `5e58917` | 검증 INDEX 갱신 (#41) |
| 16:53 | `bc51f1d` | PR #27 머지 — **approval 0 머지 실증** |
| 18:07 | `6aeb72b` | 🔴 시크릿 스캔 정상화 — 11건 전건 실패 봉합 |
| 19:55 | `e51730f` | 🔴 7/30 회귀 진범 봉합 (PR #31) |

**미커밋 0** (헌법: 세션 종료 시 워킹트리 잔여 금지).

---

## 2. 🔴 오늘 잡은 3대 거짓봉합

### 2-1. main 보호가 7/24~7/31 **일주일간 꺼져 있었다**

- 인수인계서엔 *"Active 로 재잠금"* 이라 적혀 있었다. **실물은 `enforcement: disabled`.**
- **근본 원인**: 못 채우는 규칙은 꺼진다.
  `approval 1 + CodeOwner` 인데 계정이 1개 → 자기 PR 자기 승인 불가 → 급할 때 끄고 잊는다.
- **봉합**: `approval 1→0`, `CodeOwner true→false`, `enforcement disabled→active`.
  삭제금지·force-push금지·PR필수는 **전부 유지**.
- **실증**: PR #27 이 approval 0 으로 실제 머지되어 규칙이 살아있음을 증명.
- 📄 `20260731_사고기록_main보호_일주일_무방비_재잠금.md`
- ⭐ **규칙**: Ruleset 변경 후 **2경로 재조회 의무**. 기록을 믿지 말고 실물을 본다.

### 2-2. 시크릿 스캐너가 **11번 연속 실패**로 죽어 있었다 (7/21~7/31)

- `base`/`head` 를 같은 값으로 넘겨 → 액션이 **스캔 전에 exit 1**. 10일간 무방비.
- **봉합**(`6aeb72b`): base/head 제거 + 제외경로 도입 + **전체 히스토리 트리거 추가**
  (`schedule` 주1회 + `workflow_dispatch`) — diff 스캔은 **안 바뀐 파일을 영원히 안 읽는다**.
- **성과**: 살리자마자 진탐 검출. 그중 하나가 `.iss:1545` (6주 전 `eaa8f15` 주석).
- ⛔ **제외목록에 진탐을 넣지 않았다.** 덮으면 스캐너가 서서히 꺼진다.
- 📄 `docs/설계/보안/20260731_시크릿스캔_정상화_설계서.md`

### 2-3. 제가 만든 taskkill 가드가 **발동 불가**였다 (봉합한 척)

- v1: `if ScStopCode = 1060` → **절대 참이 안 됨**.
  실측: `sc stop` 부재 = **종료코드 36**. 1060 은 **출력 본문의 메시지 텍스트**였다.
  ⇒ taskkill 이 봉합 전과 **100% 동일하게 실행**됐다.
- 부수: *"로그 침묵 = hang"* 근거도 무효. `git show e55a1e9 | grep -c ExecLogged` = **0**
  (그 버전엔 로깅 코드가 없었다. 침묵은 hang 의 증거가 아니었다).
- v2(`e51730f`): `sc query` 로 탐지, **`= 36` 이 아니라 `<> 0`** 판정.
  미지의 실패코드에서도 가드가 새지 않게. **코드값을 추측하지 않는다.**
- 📄 `docs/검증/20260731_검증리뷰서_6-1_taskkill봉합_시크릿진탐.md` (🟡 조건부 통과)

---

## 3. 사장님 결재 사항

### 3-1. DB 공통 비밀번호 = **`HITPAN2026!`** ✅

- 근거(사장님): ***"DB가 다르면 CS를 해줄 수가 없어."***
- 성격 규정: **비밀번호가 아니라 제품 설정값**이다. 설치 파일에 평문으로 나간다.
  안전한 이유는 "비밀이라서"가 아니라 **localhost 바인딩**이라서다.
- 기존 설치본 0건 → 지금 적용이 가장 싸다.
- 📄 `docs/CS/20260731_DB비밀번호_CS안내기준.md` + `20260731작1_...작업지시서.md`

### 3-2. 시크릿 진탐 A안(이대로 머지) ✅

- B안(rebase 히스토리 세탁)은 **오늘 재잠금한 main 보호를 같은 날 다시 여는 자충수**라 기각.

---

## 4. 🔴🔴 아차사고 — 마스터계정 비번이 전 고객에게 배포될 뻔했다

PM 이 *"공통 DB 비밀번호 값을 확정해주십시오"* 라고 **값의 성격을 설명하지 않고** 물었다.
사장님이 `Thtnsrms1!` 을 주시며 ***"히트판 마스터계정 패스워드야"*** 라고 하셨다.

- 그 값이 들어갈 `install.bat` 는 **전 고객에게 배포되는 평문 배치 파일**이다.
- 즉시 중단하고 값을 `HITPAN2026!` 으로 확정.

### ⚠️ 미완료 — 사장님 조치 필요

> **🔴 히트판 마스터계정 비밀번호를 오늘 안에 교체해 주십시오.**
> 이 대화에 노출됐습니다. 배포는 막았지만 노출 자체는 되돌릴 수 없습니다.

**근본 원인 = PM 의 질문 설계 결함.** 상시 규칙(작지서 §7-5):
비밀값을 요청하기 전에 ①어디에 들어가는지 ②누가 볼 수 있는지 ③재사용 금지 경고
**3가지를 먼저 밝힌다.**

---

## 5. 🔴 헌법 #39 위반 — 조사 에이전트가 운영에 썼다

PM 이 지시문에서 읽기전용 제약을 빠뜨려, 설계팀 에이전트가
`back.hitpan.kr/api/landing/signup` 에 **실제 POST** 를 보내 운영 레코드를 만들었다.

- 삭제하지 않았다 — **지우는 것도 또 하나의 운영 쓰기**다.
- ⚠️ **사장님 조치**: 백오피스에서 그 유령 가입 건 **반려** 부탁드립니다.
- 이후 모든 에이전트 지시문에 명시: *GET/HEAD 만 · POST/PUT/DELETE 금지 · SSH 금지*.

---

## 6. 실측 사전점검 결과 (전부 GET — #39 준수)

| 항목 | 결과 |
|---|---|
| `landing.hitpan.kr/signup`·`/download`·`/pricing` | ✅ 200 |
| 랜딩이 가리키는 설치본 | ✅ **1.2.46** · 실제 200 · **270,878,717 bytes** |
| 샌드박스 폴더 파일 | ✅ **바이트 단위 일치** |
| `updates.hitpan.kr/manifest.json` | ✅ 200 (현재 게시본 1.2.46) |
| `back.hitpan.kr` | ✅ 200 |
| **백오피스 로그인 경로** | 🔴 `/admin`·`/login` = **404** / **`/backoffice/login` = 200** |

⇒ 절차서·안내문에 **정확한 주소를 명시**했다. 딱 한 번뿐인 실측을 주소 오타로 날릴 수 없다.

### ⑥ 자동업데이트 진입조건 2건 — 둘 다 사망 아님으로 확인

1. `payload/HitPan.Watchdog/appsettings.json` 에 **업데이트 피드 키가 없다**
   → `UpdateClient.cs:63` 이 `?? "https://updates.hitpan.kr"` **하드코딩 폴백**. 죽지 않는다.
2. `HealthCheckUrl` 이 `demo.hitpan.kr` (남의 주소)
   → `DbConfReader` 가 설치 시 `db.conf` 의 `PRIMARY_DOMAIN` 으로 **런타임 덮어씀**
     (2026-06-23 6차 D-P0-01 기봉합). 파일값은 무시되는 잔재.

---

## 7. 🟡 내일로 넘기는 것 — 실측이 아직 안 끝났다

### 7-1. 종단 6단계 실측 (오늘 착수 못 함)

준비는 **전부 끝났다**: `C:\HitPanTest125\` 1.2.46 + 안내문 + 절차서 + P0 7건 사전제거.

⚠️ **게시 순서 함정** — 지금 1.2.47 을 올리면 ⑥번이 측정 불가가 된다.
```
①~⑤ 1.2.46 설치·로그인  →  "5단계까지 됐다"  →  그때 1.2.47 게시  →  46→47 감지
```
NCP 디스크 여유가 **약 1회분**이라 헛게시하면 ⑥번을 못 한다.

### 7-2. 봉합분이 EXE 에 아직 안 들어갔다 🔴

오늘 봉합(`e51730f`)은 **소스에만** 있다. `C:\HitPanTest125\` 의 1.2.46 은 **봉합 전 빌드**다.
⇒ **내일 첫 순서 = main 기준 EXE 재빌드** (`Build Installer EXE` dispatch).
   그래야 taskkill 봉합의 진위를 실측이 판정한다.

### 7-3. 진범 확정 미완

**Sandbox 에서 taskkill hang 을 재현하지 못했다** (이 PC 는 128 즉시 반환).
7/30 로그는 Sandbox 휘발로 소실. 7/21 작1 도 *"실 PC 불확실"* 유보였다.
⇒ 현재 봉합은 **"진범이면 막는다"는 조건부 안전장치**. 진범 확정은 실측 로그가 한다.

**실측 후 이 두 줄이 없으면 v2 도 재반려:**
```
[작10 P0-5][6-1-svcprobe]        ← 36 인가 0 인가
[작10 P0-5][6-1-taskkill] 건너뜀  ← 가드가 실제로 발동했는가
```

### 7-4. [2] 결재 대기 작업지시서

| 문서 | 남은 진짜 P0 |
|---|---|
| `20260731작1` | **`GRANT ALL ON *.*`** — 같은 MariaDB 의 **남의 프로그램 DB 전권** 부여 |
| | **기존 MariaDB root 비번 침묵 실패** — `>nul 2>&1` 로 오류를 삼킴. 설치 "성공", 로그인 사망 |
| `20260730작2` | 워치독 MariaDB 경로 비대칭 — 10.11 고객 자동업데이트 3중 사망(잠복) |

### 7-5. 별건 조사 (실측 결과에 영향 가능)

- `.iss:1845 ConvertFrom-Json -AsHashtable` — **PS 6.0+ 전용**, Windows 기본 5.1 → 침묵 실패 가능
- NCP 환경변수 미확인 (**SSH 필요 = 헌법 #29 결재 대상**)
  `CLOUDFLARE_API_TOKEN`/`ZONE_ID`/`ACCOUNT_ID` 부재 → DNS 침묵 스킵 → 설치 "성공" + **1033**
  `License:Pepper`/`Backoffice:BizNoPepper` 부재 → 승인 **500**
- 랜딩 HTML 4개에 `api-demo` 하드코딩 잔존
- `HealthProbe` 가 STOPPED cloudflared 를 정상으로 보고
- 자동복구가 db.conf 만 갱신, appsettings 미갱신
- OWASP ZAP 아티팩트 업로드 8연속 실패
- 레포 드리프트: NCP `publish-update.sh` 에 패키지 로테이션 有 / 레포본 無

---

## 8. 오늘의 교훈 — 한 줄로

> **"봉합했다"는 기록은 증거가 아니다. 실물을 조회해야 증거다.**

오늘 3건 전부 같은 구조였다.
main 보호도, 스캐너도, taskkill 가드도 **문서에는 "완료"라고 적혀 있었다.**

그리고 **SoD 가 실제로 작동했다** — [3-V] 가 PM 의 봉합을 P0 5건으로 반려했다.
PM 이 자기 설계서 §2 에 써둔 경고를 스스로 어긴 것까지 잡아냈다.
**자기검증이었다면 오늘도 "완료"로 넘어갔다.**

---

## 9. 🔴 사장님 조치 2건 (미완료)

1. **히트판 마스터계정 비밀번호 교체** — 오늘 노출됨 (§4)
2. **백오피스 유령 가입 건 반려** — PM 에이전트가 만든 것 (§5)
