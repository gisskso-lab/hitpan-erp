# 작업지시서 20260513작12 — 4개 신규 테이블 CREATE

> **발행:** 2026-05-12 야간 (W2 D2 사전 발행)
> **담당:** DB개발자 + 보안개발자 (raw_data·raw_response AES)
> **헌법:** #1, #5, #15, #17, #18, #20, #22
> **선행 결재:** 사장님 결재 #2 (etax) + W1 게이트 통과 2026-05-12
> **참조:** [INFRA_DDL_SPEC.md](../migration/INFRA_DDL_SPEC.md), [ETAX_SEND_HISTORY_DDL.md](../migration/ETAX_SEND_HISTORY_DDL.md)

---

## 1. 작업 목적

마이그 인프라 3개 + 전자세금계산서 이력 1개 = **4개 신규 테이블** 생성.

| # | 테이블 | 용도 |
|---|---|---|
| 1 | `migration_jobs` | 마이그 작업 추적 마스터 |
| 2 | `migration_checkpoints` | 테이블별 청크 진행 추적 |
| 3 | `migration_errors` | 실패 레코드 보관 |
| 4 | `etax_send_history` | 전자세금계산서 발행 이력 통합 |

---

## 2. 사전 확인

```sql
-- 기존 테이블 없음 확인
SELECT TABLE_NAME FROM information_schema.TABLES
WHERE TABLE_SCHEMA='hitpan_erp'
  AND TABLE_NAME IN ('migration_jobs','migration_checkpoints','migration_errors','etax_send_history');
-- 기대: 0건

-- tax_invoices 존재 확인 (etax FK)
SELECT TABLE_NAME, ENGINE FROM information_schema.TABLES
WHERE TABLE_SCHEMA='hitpan_erp' AND TABLE_NAME='tax_invoices';
-- 기대: tax_invoices, InnoDB ✅
```

---

## 3. CREATE 실행 (순서 의존성 주의)

### 3.1 migration_jobs (최상위)

```sql
CREATE TABLE IF NOT EXISTS migration_jobs (
    job_id            CHAR(36) PRIMARY KEY,
    tenant_id         CHAR(36) NOT NULL,
    initiated_by      CHAR(36) NOT NULL,
    source_folder     VARCHAR(500) NOT NULL,
    status            ENUM('pending','preview','running','paused','completed','failed','canceled')
                      NOT NULL DEFAULT 'pending',
    total_tables      SMALLINT UNSIGNED DEFAULT 0,
    completed_tables  SMALLINT UNSIGNED DEFAULT 0,
    total_rows        INT UNSIGNED DEFAULT 0,
    processed_rows    INT UNSIGNED DEFAULT 0,
    skipped_rows      INT UNSIGNED DEFAULT 0,
    error_rows        INT UNSIGNED DEFAULT 0,
    preview_at        DATETIME NULL,
    started_at        DATETIME NULL,
    paused_at         DATETIME NULL,
    completed_at      DATETIME NULL,
    error_summary     TEXT NULL,
    checkpoint_data   JSON NULL,
    client_ip         VARCHAR(45) NULL,
    user_agent        VARCHAR(255) NULL,
    created_at        DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at        DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_tenant_status (tenant_id, status),
    INDEX idx_tenant_created (tenant_id, created_at DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3.2 migration_checkpoints (FK → migration_jobs)

```sql
CREATE TABLE IF NOT EXISTS migration_checkpoints (
    checkpoint_id     CHAR(36) PRIMARY KEY,
    job_id            CHAR(36) NOT NULL,
    tenant_id         CHAR(36) NOT NULL,
    mdb_file          VARCHAR(50) NOT NULL,
    table_name        VARCHAR(50) NOT NULL,
    table_order       SMALLINT UNSIGNED NOT NULL,
    status            ENUM('pending','running','done','failed','skipped')
                      NOT NULL DEFAULT 'pending',
    total_rows        INT UNSIGNED DEFAULT 0,
    processed_count   INT UNSIGNED DEFAULT 0,
    last_pk_value     JSON NULL,
    chunk_size        SMALLINT UNSIGNED DEFAULT 1000,
    started_at        DATETIME NULL,
    completed_at      DATETIME NULL,
    avg_commit_ms     INT UNSIGNED DEFAULT 0,
    last_error        TEXT NULL,
    retry_count       TINYINT UNSIGNED DEFAULT 0,
    created_at        DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at        DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_job_table (job_id, table_name),
    INDEX idx_tenant (tenant_id),
    INDEX idx_chkpt_pending (job_id, status, table_order),   -- W1 게이트 수정 (일반 인덱스)
    CONSTRAINT fk_checkpoint_job FOREIGN KEY (job_id)
        REFERENCES migration_jobs(job_id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3.3 migration_errors (FK → migration_jobs)

```sql
CREATE TABLE IF NOT EXISTS migration_errors (
    error_id          CHAR(36) PRIMARY KEY,
    job_id            CHAR(36) NOT NULL,
    tenant_id         CHAR(36) NOT NULL,
    checkpoint_id     CHAR(36) NULL,
    mdb_file          VARCHAR(50) NOT NULL,
    table_name        VARCHAR(50) NOT NULL,
    row_pk_value      JSON NULL,
    row_offset        INT UNSIGNED NULL,
    error_type        ENUM('encoding','fk_missing','duplicate','schema','constraint','timeout','other') NOT NULL,
    error_severity    ENUM('warning','error','critical') NOT NULL DEFAULT 'error',
    error_code        VARCHAR(20) NULL,
    error_message     TEXT NOT NULL,
    error_detail      TEXT NULL,
    raw_data          JSON NULL,                              -- ⚠️ Value Converter AES-256
    is_resolved       TINYINT UNSIGNED DEFAULT 0,
    resolved_at       DATETIME NULL,
    resolved_by       CHAR(36) NULL,
    resolution_note   TEXT NULL,
    occurred_at       DATETIME NOT NULL,
    created_at        DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_job (job_id, error_severity, occurred_at),
    INDEX idx_tenant (tenant_id),
    INDEX idx_resolved (is_resolved, occurred_at),
    INDEX idx_errors_severity (job_id, error_severity, occurred_at DESC),
    CONSTRAINT fk_error_job FOREIGN KEY (job_id)
        REFERENCES migration_jobs(job_id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3.4 etax_send_history (FK → tax_invoices)

```sql
CREATE TABLE IF NOT EXISTS etax_send_history (
    history_id           CHAR(36) PRIMARY KEY,
    tenant_id            CHAR(36) NOT NULL,
    tax_invoice_id       CHAR(36) NOT NULL,
    issue_date           DATE NULL,
    sent_at              DATETIME NULL,
    nts_read_date        DATE NULL,
    nts_report_date      DATE NULL,
    nts_approval_no      VARCHAR(50) NULL,
    nts_response_code    VARCHAR(20) NULL,
    nts_response_message VARCHAR(500) NULL,
    asp_provider         VARCHAR(20) NULL,
    asp_transaction_id   VARCHAR(100) NULL,
    status               ENUM('legacy','pending','sent','approved','rejected','failed','canceled')
                         NOT NULL DEFAULT 'pending',
    attempt_no           TINYINT UNSIGNED NOT NULL DEFAULT 1,
    is_retry             TINYINT(1) NOT NULL DEFAULT 0,
    raw_request          JSON NULL,
    raw_response_encrypted VARBINARY(4096) NULL,              -- ⚠️ AES-256
    created_by           CHAR(36) NULL,
    created_at           DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at           DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_tenant_invoice (tenant_id, tax_invoice_id),
    INDEX idx_status (tenant_id, status, created_at DESC),
    INDEX idx_sent_date (tenant_id, sent_at DESC),
    INDEX idx_asp (asp_provider, asp_transaction_id),
    CONSTRAINT fk_etax_invoice FOREIGN KEY (tax_invoice_id)
        REFERENCES tax_invoices(invoice_id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 4. 검증

```sql
-- 1. 4개 테이블 생성 확인
SELECT TABLE_NAME, ENGINE, TABLE_COLLATION
FROM information_schema.TABLES
WHERE TABLE_SCHEMA='hitpan_erp'
  AND TABLE_NAME IN ('migration_jobs','migration_checkpoints','migration_errors','etax_send_history')
ORDER BY TABLE_NAME;
-- 기대: 4건 모두 InnoDB + utf8mb4_unicode_ci

-- 2. FK 확인
SELECT CONSTRAINT_NAME, TABLE_NAME, REFERENCED_TABLE_NAME, DELETE_RULE
FROM information_schema.REFERENTIAL_CONSTRAINTS
WHERE CONSTRAINT_SCHEMA='hitpan_erp'
  AND CONSTRAINT_NAME IN ('fk_checkpoint_job','fk_error_job','fk_etax_invoice');
-- 기대: 3건 모두 CASCADE

-- 3. VARBINARY 확인
SELECT TABLE_NAME, COLUMN_NAME, DATA_TYPE, CHARACTER_OCTET_LENGTH
FROM information_schema.COLUMNS
WHERE TABLE_SCHEMA='hitpan_erp'
  AND DATA_TYPE='varbinary'
  AND TABLE_NAME IN ('etax_send_history');
-- 기대: raw_response_encrypted (4096)

-- 4. JSON 컬럼 확인
SELECT TABLE_NAME, COLUMN_NAME
FROM information_schema.COLUMNS
WHERE TABLE_SCHEMA='hitpan_erp'
  AND DATA_TYPE='json'
  AND TABLE_NAME IN ('migration_jobs','migration_checkpoints','migration_errors','etax_send_history');
-- 기대: checkpoint_data, last_pk_value, row_pk_value, raw_data, raw_request
```

---

## 5. 보관 정책 (보안매니저 + 법무팀장)

```
migration_jobs        영구 (감사)
migration_checkpoints 1년 (재시작 가능성)
migration_errors      1년 (사고 추적)
etax_send_history     영구 (세무 5년 의무 + 감사)
```

배치 작업은 W3 별도 작업지시서.

---

## 6. 후속 작업

- 작12-1: Value Converter 매핑 — `migration_errors.raw_data`, `etax_send_history.raw_response_encrypted` (보안개발자, `VALUE_CONVERTER_SPEC.md`)
- 작12-2: MigrationService 클래스 5종 구현 (백엔드개발자, `CLASS_SEPARATION_SPEC.md`)
- 작12-3: API 4종 컨트롤러 구현 (백엔드개발자, `INFRA_API_SPEC.md`)

---

## 7. 헌법 부합

| 헌법 | 적용 |
|---|---|
| #1 신규 테이블 (수정 OK) | ✅ |
| #2 모든 테이블 tenant_id | ✅ |
| #5 raw_data·raw_response Value Converter | ✅ |
| #15 빈 catch 금지 (migration_errors 의무 기록) | ✅ |
| #17 InnoDB | ✅ 4개 모두 |
| #18 본사 송신 0 | ✅ 로컬 |
| #19 errors 0 + warnings 0 | ✅ DEFAULT·FK 명시 |
| #20 워크플로우 끊김 0 | ✅ status·attempt 전수 |
| #22 데이터 최소주의 | ✅ raw_* 별도 권한 |
| #23 5중 검증 | ⚠️ PR 시 적용 |

---

## 8. 5중 검증 체크리스트

- [ ] ① 작업지시서 보안 요구사항 명시 ✅
- [ ] ② 매니저 리뷰
- [ ] ③ SAST (W2 D3 코드 추출 시)
- [ ] ④ DAST (베타 전 1회)
- [ ] ⑤ 데이터 최소주의 검증 ✅

---

## 9. EVF 6대 영역 점검

| 영역 | 시나리오 | 대응 |
|---|---|---|
| ① 부하 | 100만 행 마이그 | 청크 1,000 + 인덱스 idx_chkpt_pending |
| ② 장애 | 마이그 중 정전 | last_pk_value JSON + 재시작 |
| ③ 악의 | 다른 tenant 마이그 침투 | tenant_id 검증 |
| ④ 혼돈 | 재마이그 100회 | UK uk_job_table + 멱등 |
| ⑤ 무지 | 사장님이 raw_data 평문 노출 | raw_data 별도 권한 |
| ⑥ 노후 | 1년 후 마이그 이력 조회 | 보관 정책 + 인덱스 |

---

**발행:** PM 닥터스트레인지
**검토:** DB매니저, 보안매니저, 백엔드매니저, 설계팀장
**실행자:** DB개발자 + 보안개발자 (W2 D2)
**예상 소요:** 20분
**결재:** 사장님 2026-05-12
