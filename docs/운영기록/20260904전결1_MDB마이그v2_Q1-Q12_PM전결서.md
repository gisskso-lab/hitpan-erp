# 전결서 20260904전결1 — MDB 마이그 v2 · Q1~Q12 PM 전결

| 항목 | 내용 |
|---|---|
| 근거 | 사장님 2026-09-04: *"1~12 모두 전결. 1. 설계팀 설계 2. 개발팀 코딩 3. 검증팀 검증 1-4. 임원들 실시간 동시검증. 루틴대로!! 문서로 주고받고 일하고, 문서는 폴더에 보관,관리 인덱스 갱신!!"* |
| 전결자 | PM 브라운킴 (CTO 래리 공람) |
| 질문 원본 | [선행검증서 20260904검2 §4](../검증/선행/20260904_선행검증서_MDB마이그v2_8개전제_실측.md) |
| 전결 범위 확인 | 요금·가격에 닿는 항목 **0건** (8/21 전결 잣대) — 전결 가능 |
| 원칙 | 전결이라도 **근거 없이 정하지 않는다**(#32). 아래 각 항목은 오늘 MDB 실물(읽기 전용)로 잰 근거를 붙였다. 근거가 약한 항목은 **"약함"** 이라 적고 대사표가 판정하게 했다 |

---

## 전결 판정표

| # | 질문 | 판정 | 근거 (실측) |
|---|---|---|---|
| **Q1** | 방향 | **`IO=1 = 매입` · `IO=2 = 매출` · `TX_IO` 동일** | IO=1 거래처 아리엘상사·가비아·소모품 / 품목 거래명세서 용지 · IO=2 거래처 피시아트·바로소프트 / 품목 HTP21C·전자결제 충전. 추가로 **`S_GU=A` 합 = DOCFB IO=1 합 1원 일치**(아리엘상사 241,534,750 · 피시아트 22,307,700) — IO=1 이 매입이라야 성립 |
| **Q2** | `S_GU` 코드표 | **수금 = 1·2·3·4·5(S_SUK, 매출처 98%+)** → `collections` · **A = 매입 발생(S_SUK, 매입처 99.7%)** → 이관 안 함(purchase_receipts 사본) · **0 = 발생(S_BAL, 양부호)** → 이관 안 함(ERP 는 명세서에서 계산) · **B·C·D·E·F = 지급/조정(S_BAL, 매입처·양쪽 다수 · C 적요 = 자사 계좌 · F = "잔액정리·조정금액")** → `payments` · 레거시 거래처 잔액식 = **ΣS_BAL − ΣS_SUK(전 코드)** | 거래처별 잔액식 H1 이 **11,813 거래처 중 8,858(75%) 정확히 0** (H2 는 8,692) · 아리엘상사: B+C+F 245.2M ≈ 매입 A 241.5M + 수금 3.8M. **약함**: 0 의 뜻(피시아트 0=32.6M vs 명세서 219M — 발생 ≠ 명세서) ⇒ **대사표 8번이 거래처별 차이로 드러낸다** · D·E 27행은 근거 없음(memo 에 코드 보존) |
| **Q3** | `AC_JEN` | **0 = 월계(이월) → 이관 안 함** · **1·3 = 입금** · **2·4 = 출금** | 0: `AC_JWASU=0`·`AC_YMD=YYYYMM00`·거래처 0 = 집계행(311) · 1: cheri "수금" 2,765 · 2: "1경비" 12,246·"경비" 2,245·"지급" 492 · 3/4: "은행"(SGU 1/2 = 입/출). 현행 코드는 전부 지출로 넣는다(`isExpense=true` 고정, :2377) |
| **Q4** | 회계 음수 3,667 | **(A) 반대편 양수로 전환** — `SC_DR<0` → 차변 +\|v\| · `SC_CR<0` → 대변 +\|v\| · 양쪽 0 (3행) → 기록 후 제외 | CHECK `chk_jl_debit_or_credit` 유지 = #3 복식부기 정신. 음수 3,645 가 `SC_DR`, 22 가 `SC_CR`, 양쪽 음수 0 |
| **Q5** | 반품 | **(A) 명세서 라인은 음수 그대로** · **원장은 반대 칸에 절대값**(매출 음수 → `qty_in`, 매입 음수 → `qty_out`) | 레거시 그대로(5/14 예외). 원장 `qty_in/qty_out` 두 칸 구조라 음수를 넣으면 화면·검사식이 깨진다(8/31 판정과 동일 근거). 순수량은 불변 |
| **Q6** | 미이관 10표 | **대사만**: DOCFE·DOCFC · **버림**: SETUP(인증서 경로·비번 문자열)·DOCME·DOCSC·LOCK1·DOCLT·DELIVERY·DOCF1/F2(5/20 폐기 유지 — DOCFB 로 통합됨)·REMARK1·DOCAS1·DOCCD1(관계 없음 확정) | 값이 대사 정답지이거나, 업무 자료가 아니거나, 이미 폐기 결정. ⚠️ DOCME 242K 는 CS 이력일 수 있어 **사장님 재확인 1줄** 남긴다(버려도 MDB 원본은 남는다) |
| **Q7** | 덮어쓰기 경계 | **DataReset 화이트리스트(6/23) 그대로** | 이미 사장님 결재된 분류 |
| **Q8** | 병합 충돌 | **(C) 표에서 사람이 확정 · 기본값 (A) ERP 값 보존** — W3(작22) | 반자동 원칙 |
| **Q9** | 창고 | **이관 원장 = 테넌트 기본창고(MAIN)** · `WH-MIG` 생성 중단 | 9/4 사장님 정의 *"미지정=MAIN"* · `WarehouseLookup.ResolveTenantDefaultWarehouseAsync` 실재 |
| **Q10** | 속도 | **1단계 ≤ 1분 · 전체 ≤ 10분** (이 MDB 60만행) — 5/16 결재 유지 · 단 **베이스라인 실측 후 목표 확정** | 기록 0건 |
| **Q11** | 대사 합격선 | **9항목 금액 차 0** · 차이 있으면 「완료」 대신 **「차이 N건 + 목록」** | 사장님 *"레거시와 숫자가 맞는다"* |
| **Q12** | `S_SET`·`RT_SON/KUM` | `S_SET` 1·2 = 셋트 완제품 · 3 = 셋트 자재 → **item_type 은 건드리지 않고 `memo` 에 `[셋트:{S_SET}]`** · `RT_SON/RT_KUM` → **`bom_items.memo` 에 `원가/금액` 덧붙임** | DOCRT 완제품(거래명세서123·볼펜·태나상품·AL피뢰도선)=1, 제품BOX=2, 자재(T-렌찌·기장료·AL가공)=3. ERP `item_type` 어휘가 코드에 흩어져 있어(product·finished·assembly·promo) 값을 바꾸면 화면 필터가 깨질 수 있다 — 손실 0 으로 보존만 |

---

## 전결에서 파생된 추가 결정 (선행검증 밖에서 발견)

| # | 결정 | 근거 |
|---|---|---|
| D1 | **품목 미등록 라인은 버리지 않고 `MIG-AUTO-ITEM` 으로 품목별 등록** — 1단계(마스터)에서 DOCFB `DISTINCT(IJ_PUM, IJ_KU)` 를 전수 등록해 2단계 병렬 잡이 공유 딕셔너리를 안 건드리게 | 7,732행·18.4억이 `LEGACY_UNKNOWN_ITEM` 하나에 뭉치면 품목별 재고·판매현황이 안 맞는다. BOM(#78 H-1)이 이미 같은 방식 |
| D2 | **`tax_invoices.direction` = `S`(매출)/`B`(매입)** 로 통일 (DDL 주석 `'S=매출, B=매입 (TX_IO)'`) | 현행은 TX_IO 원값 `1/2` 를 그대로 넣어 DDL 어휘와 불일치 |
| D3 | **원장 `source_id` = `mb-{DT}-{IO}-{SEQ}-{BUY}-{SUN}`** (5키 · 최대 34자 ≤ varchar(36)) | 5키 DISTINCT = 전체 124,152 (실측). `mig-docfb-…` 형식은 36자를 넘는다 |
| D4 | **`payments` 에 `source_type/source_id/migrated_source_hash` + UNIQUE 추가 (DB-118)** | 지급 이관의 멱등 키가 없다. 다른 이관 표는 전부 있다(#36 clean DDL 편입) |
| D5 | 이번 차수(작21) 범위 = **W1 대사표 + W2 P0/P1 봉합**. W3(두 모드·찾아보기·업로드·1/2단계 분리) = **작22** · W4/W5 = 작23~ | 한 화면씩(#10). 대사표가 먼저 있어야 나머지를 숫자로 검증한다 |

---

## 사장님 재확인 1줄 (전결 밖으로 남기는 것)
- DOCME(상담·마감 메모 242,106행) — CS 이력이면 이관 가치가 있습니다. 지금은 **버림**으로 두되 MDB 원본은 그대로 남으니 나중에 켤 수 있습니다.

---
**전결**: PM 브라운킴 · 2026-09-04 / **공람**: CTO 래리 · 보안상무 안철수 · 검증팀장 데이비드 박 / **다음**: [작업지시서 20260904작21](20260904작21_MDB마이그v2_대사표_P0봉합_작업지시서.md)
