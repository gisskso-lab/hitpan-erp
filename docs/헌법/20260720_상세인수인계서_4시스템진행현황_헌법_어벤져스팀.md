# 히트판 상세 인수인계서 — 4영역 진행현황 · 헌법 · 어벤져스팀

> 작성: 2026-07-20 / 작성자: PM 브라운킴 / 기준 브랜치: `develop` / 최신 커밋 `da41403`
> 목적: 다음 세션이 끊김 없이 이어받도록 ① 4개 업무영역 진행현황 ② 헌법·중요 .md ③ 어벤져스 개발팀 정보를 한 문서에 집약.
> 원칙: 전부 실측(git·코드·파일). 추정은 "추정"으로 명시.

---

## 0. 지금 어디까지 왔나 — 한눈 요약

| 영역 | 완성도(러프) | 상태 한 줄 |
|---|---|---|
| **히트판 ERP** | 본체 높음 | API 52컨트롤러 + Web 119페이지. 6단계 워크플로우 채워짐. 종단 로그인·온보딩 통과(7/14 Sandbox) |
| **백오피스** | 뼈대+ | API 27컨트롤러 + 80페이지. 가입·재발급·프로비저닝 존재. 재발급 UI 확장(W-5) 남음 |
| **랜딩** | 뼈대+ | 25페이지. 가입·다운로드 흐름 존재. 다운로드 링크 1.2.31까지 갱신됨 |
| **자동 업데이트(고리4)** | 절반+ | 감지·다운로드·백업·서명·정지까지 완료. **실제 파일교체·재시작·롤백(W4-2~6) 미구현** |

**오늘(7/20) 한 일**: 고리4 서명 공개키(`upd-v1`) NCP 생성 → EXE 내장 → openssl 왕복 실증 → 커밋 `da41403`. 고리4의 유일한 blocker 해소.

**바로 다음 할 일**: 고리4 W4-2(파일교체)~W4-6(상태) 구현. `UpdateOrchestrator.cs:132`가 자리표시.

---

# 1. 업무내용 — 4영역 진행사항 & 해야 할 일

## 1-1. 히트판 ERP (고객사 PC 로컬 + cloudflared 터널)

> `src/HitPan.API`(백엔드 ASP.NET Core 8) + `src/HitPan.Web`(Blazor WASM + MudBlazor)
> 도메인: `{고객사ID}.hitpan.kr` / DB: 고객 PC 로컬 MariaDB / 출하 DDL 테이블 **124개**(`installer/hitpan_db_clean.sql`)

### 진행현황
| 구분 | 내용 | 상태 |
|---|---|---|
| 6단계 워크플로우 | 설정→마스터→매입→판매→현황→재무 | 채워짐(Web 119페이지) |
| 부모계정 온보딩 | 공개키 서명 시리얼 → 설치마법사 로컬 계정생성 (헌법 #40) | ✅ 종단 통과(7/14 Sandbox) |
| 로그인 종단 | users.role enum 컨버터 자가치유 등 W1~W3 15건 | ✅ 봉합(`03c6893`) |
| 회사정보 | `/api/tenants/me` local_company 우선 조회 | ✅ 봉합(`7ce390e`) |
| 대시보드 | API 실패 시 가짜 견본매출 폴백 제거 | ✅ 봉합(`770b3a2`, EXE 1.2.30+) |
| 설치마법사 무단사용방지 | 처음/다시 설치 옵션 + 동일 사업자 신규설치 차단 | ✅ W-2/3/4(`1cd930b`) |
| AI직원(FSD) | Tool Use 엔진 Lv1~3 뼈대 | 베타후 확대(백로그) |

### 국소 미완성 흔적 (실측 grep — 진짜 TODO, 대부분 razor는 placeholder라 무관)
| 파일:라인 | 내용 |
|---|---|
| `Application/Services/TaxInvoice/DirectHometaxProvider.cs:93,120` | 홈택스 직접연동 Hangfire 큐/폴링 잡 미완(W2 가도) |
| `Application/Services/CloudflareProvisioningService.cs:113` | tenants.provisioning_status 저장 TODO |
| `API/Controllers/InstallController.cs:38` | 라이선스 키 HMAC 검증 TODO |
| `Infrastructure/Services/ChatbotService.cs:117` | 챗봇 키 없을 때 KB-only 폴백 |

### 해야 할 일
| 우선 | 작업 | 비고 |
|---|---|---|
| 중 | 설치마법사 W-5·W-6 (재발급 UI는 백오피스, 폴백 회수) | 아래 1-2·1-4 참조 |
| 낮 | 자식계정(직원) 이메일형식 강제 → 부모=아이디/자식=이메일 비대칭 정책 통일 | 사장님 결정 대기(헌법 #40 백로그) |
| 낮 | 견적서 PDF 모바일 미작동 | 별도 디버깅 미결(오래된 백로그) |

---

## 1-2. 백오피스 (본사 클라우드 NCP)

> `src/HitPan.Backoffice.API`(27컨트롤러) + `src/HitPan.Backoffice`(80페이지)
> 도메인: `back.hitpan.kr` / DB: 클라우드(ERP와 물리 분리, 헌법 #35)
> 역할: 운영의 워크스페이스 — 계정·CS·구독·추천인(대리점) 관리. **업무 데이터 0건 보유**(헌법 #22)

### 진행현황 (API 27컨트롤러 / 페이지 59)
| 구분 | 내용 | 상태 |
|---|---|---|
| 가입 수신 | 랜딩 가입 → 백오피스 저장(비밀번호 안 받음, 헌법 #40) | 존재 |
| 시리얼 서명 발급 | `SerialSignatureService`(ECDSA P-256, 공개키 EXE 내장) | ✅ 7/7 |
| 온라인 라이선스 claim | `LandingPublicController`(pricing·beta-signup·OTP·license/claim·public-key) | ✅ 구현 |
| 재발급 | `SignupsAdminController.ResendLicense`(232~312행: 신규 시리얼 발급→기존 무효화→이메일, 평문 미저장, 실패 시 화면 전달 폴백) | ✅ 완전 구현(문구 확장만 남음) |
| 고객사·협력업체 관리 | AdminTenantsV2·AdminResellersV2 | ✅ 실동작 |
| 프로비저닝 | 고객사 자동 발급(터널·DNS 등) | 존재 |

### ⚠️ 백오피스 미완성 흔적 (실측)
| 항목 | 상태 |
|---|---|
| `Platform/AdminDashboard.razor` | **"본사 대시보드 준비 중" 봉합** — 옛 ERP api/admin/dashboard 삭제 후 W9 차수에 백오피스 API로 신설 예정 |
| `Services/BackofficeService.cs` | 구 ERP 어드민 흐름 좀비 stub 잔재(인증 2개만 작동, 14개 NotSupportedException, 화면 7개 "준비 중"). 신규 27컨트롤러로 대체 진행 중 |
| 키워드 | "W9 차수 / 헌법 #35 완전분리"가 남은 정리 작업 |

### 해야 할 일 (설치마법사 무단사용방지 후속)
| 우선 | 작업(W-5) | 비고 |
|---|---|---|
| 중 | `ResendLicense` 문구 확장(재설치/분실 시 재발급 안내) | 백오피스 |
| 중 | 재발급 이력 테이블(`tenant_license_reissue_history`) | **백오피스 DB — ERP clean DDL 혼입 금지. 신설 여부 사장님 결재 필요** |
| 중 | 조회키 company_name → tenant_code/signupId 상향 | 백오피스 |

---

## 1-3. 랜딩 (본사 클라우드 NCP)

> `src/HitPan.Landing`(순수 Blazor, 페이지 9개 + 공용 컴포넌트 다수)
> 도메인: `landing.hitpan.kr` / 역할: 운영의 가입 입구(헌법 #35)

### 진행현황 (전부 존재·실동작)
| 페이지 | 역할 | 규모 |
|---|---|---|
| `SignupPage.razor` | **가입**(시리얼 발급·OTP·사업자번호, 비밀번호칸 없음=의도된 설계 #40·#22) | 579줄, 최대 |
| `DownloadPage.razor` | **다운로드**(시리얼 1회 표시·EXE 다운로드·설치 5단계 안내) | 실동작 |
| `PricingPage`·`PaymentPage`·`VerifyPage`·`WelcomePage`·`RecoveryPage`·`ResellerSignupPage` | 요금제·결제(토스)·OTP·환영·복구·협력업체 가입 | 존재 |
| 공용 컴포넌트 | HeroSection·PricingSection·BizLicenseUploader(사업자등록증 업로드) 등 | 존재 |

### ⚠️ 랜딩 미완성 흔적
- `DownloadPage.razor:44` — 릴리스 정보 못 읽으면 "설치 파일 준비 중" 폴백(정상 예외처리). **실제 파일 유무는 배포 config(ReleaseConfigured/DownloadUrl)에 의존 → 다음 세션에서 릴리스 URL·버전 config 채워졌는지 확인 필요.**

### 해야 할 일
| 우선 | 작업 | 비고 |
|---|---|---|
| 낮 | 랜딩 다운로드 링크·릴리스 config를 최신 EXE(추후 1.2.34)로 갱신 | EXE 재빌드 시마다 |
| 낮 | 고객노출 영역 개발용어·영어 정정(최종 직전 전수 1회) | 헌법: 지금은 일정 우선 |

---

## 1-4. 자동 업데이트 (고리4) — ★ 현재 핵심 작업

> `src/HitPan.Watchdog`(워치독 = 고객 PC Windows 서비스) + `installer/updates/`
> 목표: **1.2.xx 깔린 PC가 고객 손 0번으로 다음 버전에 스스로 넘어가게**(헌법 #34, [[feedback_no_overwrite_reinstall]])
> "덮어 재설치"는 존재하지 않는 경로 — 최초 1회 설치 후엔 워치독 자동업데이트만(사장님 못박음 7/15)

### 자동 업데이트 4단계 진행현황
| 단계 | 내용 | 상태 | 근거 |
|---|---|---|---|
| ① 새 버전 감지 | manifest 조회, 채널 3분기(Emergency/Normal/Major) | ✅ 완료 | `UpdateClient.cs`·`UpdateOrchestrator.cs` |
| ② 다운로드+sha256 | 실패 시 적용 진입 금지 | ✅ 완료 | `UpdateOrchestrator.cs:101` |
| ③ 적용 전 백업 | 백업 실패 = 업데이트 물리 차단 | ✅ 완료 | `:99~110` |
| **W4-0 버전 스탬핑** | Directory.Build.props 단일출처 `1.2.34` (19 csproj 자동상속) | ✅ 완료 | 루트 `Directory.Build.props` |
| **버전 하드코딩 제거** | HealthProbe·HealthController "1.0.0"→어셈블리 조회(`VersionInfo.Current`) | ✅ 완료 | `HealthProbe.cs:39`·`HealthController.cs:61` |
| **SemVer 비교** | 문자열 불일치→3자리 SemVer(다운그레이드 차단·1.2.34≡1.2.34.0) | ✅ 완료 | `UpdateClient.cs:123~149 IsNewerVersion` |
| **서명 검증** | manifest 위조 차단, 공개키 `upd-v1` 내장, UpdateClient에서 호출 | ✅ **오늘 완료** | `UpdateSignatureVerifier.cs:60`·`UpdateClient.cs:78`, 커밋 `da41403` |
| **W4-1 정지·복원** | keepalive DISABLE 3중보장(finally+부팅안전망+자가치유) | ✅ 완료 | `UpdateProcessGate.cs` 전체, `Worker.cs:95` 배선 |
| **④ W4-2 파일교체** | 실제 EXE 교체(web→api 순) | ❌ **미구현** | `UpdateOrchestrator.cs:132` 자리표시 |
| **④ W4-4 재시작** | API→Web 기동 + keepalive 복원 | ❌ 미구현 | 재사용 자산: `WS28I_FourProcess.cs:92` |
| **④ W4-5 검증·롤백** | 헬스폴링 2중판정 + 실패 시 롤백 | ❌ 미구현 | 재사용: `WS28I_FourProcess.cs:25` |
| **④ W4-6 상태·멱등** | applied_version 기록, 버전 멱등키 | ❌ 미구현 | `local_update_status`(DB-83) |

### ⚠️ 다음 세션 필수 인지 — 현재 "거짓 성공" 함정
파일 교체를 안 했는데도 `ApplyUpdateAsync`가 `true`를 반환한다(`UpdateOrchestrator.cs:136` 경고 로그 남기고 `:138` return true). 즉 지금 업데이트를 실행하면 **백업·정지까지만 하고 파일은 그대로인데 "성공"으로 보고**된다. W4-2 구현 시 이 반환값을 실제 교체 결과로 바꿔야 함. (그래서 W4-7 로그인 버튼도 제외 — 지금 누르면 거짓 표시)

### 착수 조건 (전부 충족됨 — 바로 시작 가능)
- ✅ 서명 키(사장님 NCP 실행, 오늘 완료) — 유일했던 blocker
- ✅ W4-0·W4-1 완료
- 작업지시서 v4 CTO·사장님 결재 완료: `docs/개발/업데이트배포/20260715작1_고리4_자동업데이트_실적용_작업지시서.md`

### 다음 세션 착수 순서 (작업지시서 v4 기준)
1. **W4-2 파일교체** — `UpdateOrchestrator.cs:132`에 best-effort 스왑 삽입. **web→api 순**(CTO B-5: 롤백 시 거래 노출창 줄이려 스왑은 web먼저, 기동은 api먼저 = 비대칭 의도). `{app}\api → api.old → extract\api → {app}\api`, 실패 시 즉시 역리네임. 설정파일(db.conf·keys·bootstrap)은 `{app}` 루트라 스왑 대상 밖(구조적 안전).
2. **W4-4 재시작** — `WS28I_FourProcess.TryRestartTask` 그대로 재사용. API 기동→헬스→Web 기동→keepalive `/ENABLE`(헬스 통과 전에 복원 = 롤백 시 keepalive가 구버전 지킴).
3. **W4-5 검증·롤백** — API `/health` 2초×최대60초, **200+버전이 신버전**이어야 통과(200만으론 불통과, 구버전도 200). **Web은 200으로 판정 금지**(없는 경로도 200+index.html). 롤백 = W4-1 재실행→파일복원→keepalive ENABLE→구버전 헬스폴링. **DB 자동복원 안 함**(1차는 마이그 없는 릴리스만, 정상거래 소실 방지).
4. **W4-6 상태·멱등** — `local_update_status`에 기록, 멱등키=버전. `_pendingConsentUpdate` 인메모리 소실버그 동시 해결. mysqldump PATH 탐색 봉합.

### 검증 (헌법 #39 — Windows Sandbox 휘발성에서만, demo 불가침)
- G-최종: **1.2.33 PC → manifest 1.2.34 → 고객 손 0번 자동 갱신 → 로그인 200 + 버전 1.2.34**
- 주입 테스트 필수: 깨진 zip 주입 → 구버전 무손상 복귀 / 변조 manifest → 다운로드 진입 0
- 3관문: 빌드 0/0 + ddl-smoke + 검증팀 SoD 독립반증

### 제외(사장님 결재로 후속) — 착수하지 말 것
- W4-3 워치독 자기교체(감시자 2중 무력화 위험)
- W4-7 로그인 전 "업데이트하기" 버튼(고리4 없으면 거짓표시, 고리4 서면 버튼 없이도 목적 달성)
- 롤백 시 DB 자동복원(고리5에서 마이그와 함께)

### 배포 버전 현황 (실측)
- 최신 배포 EXE: **1.2.33**(`592fe55`)까지 빌드 이력. 랜딩 링크는 1.2.31.
- 대시보드·tenants/me·워치독 봉합 포함 EXE는 1.2.30까지 확인. **버전 정합 재확인 필요**(다음 세션 첫 점검 항목).
- ⚠️ EXE 버전은 배포 시마다 어긋나기 쉬움 — Directory.Build.props(1.2.34) ↔ .iss ↔ 랜딩 링크 3자 정합 확인.

---

# 2. 헌법 · 중요 .md

## 2-1. 절대 잊으면 안 되는 최상위 3가지

1. **3시스템 운영 설계** (사장님 100번+ 박음): `{고객사ID}.hitpan.kr`=ERP 로컬+터널 / `landing.hitpan.kr`=클라우드 / `back.hitpan.kr`=클라우드. **`www`·`app`·`api` 관례 갖다붙이기 금지.**
2. **본사 데이터 최소주의** (#18·#22): 본사는 SaaS 운영 메타만. 매출·매입·원장·거래처·직원·세금계산서 등 업무 데이터 **본사 전송 코드 절대 금지**. 부모계정 비밀번호도 본사 0건.
3. **사장 의중 100% 이해 후 코드** (#33) + **개발 SOP**: 전수조사→작업지시서→CTO결재→사장님승인→매니저작업+검증팀동시검증→보고→인수인계. **봉합 즉석 금지.**

## 2-2. 헌법 39개조 — 자동 업데이트·설치 작업에 직결되는 것 (전문은 CLAUDE.md)

| # | 요지 | 왜 지금 중요한가 |
|---|---|---|
| #1 | 수정 OK, 덮어쓰기 금지 (추가만) | Directory.Build.props가 이 정신(19 csproj 무수정) |
| #13 | 새 SQL 전 DESCRIBE 의무 | 런타임 500 방지 |
| #14 | Razor에 C# raw string 금지 | `$"..."` 사용 |
| #15 | 빈 catch 금지(침묵 금지) | W4-2 열린핸들 진단에 직결 |
| #19 | 빌드 errors 0 + warnings 0 = 무결성 | 모든 커밋 게이트 |
| #20 | 워크플로우 절대 안 끊김 | 반쯤 적용된 업데이트 상태 금지 |
| #22 | 본사 데이터 최소주의·개인키 격리 | 서명 개인키 NCP만, 채팅 미노출 |
| #23 | AI 협업 코드 5중 검증 / 외부에 "AI·Claude" 단어 금지 | 문서·마케팅 |
| #28 | Windows Update 후 cloudflared 자동복구 | 워치독 5단계 |
| #29 | 인프라 조작 사전 승인제 (PM은 SSH 안 함) | 서명 키는 사장님이 NCP에서 실행 |
| #30 | 고객 PC 자가회복, 본사 의존 0 | 오프라인 완결·본사는 통지만 |
| #33 | 사장 의중 100% 이해 + 재확인 결재 후 코드 | 4단계 절차 |
| #34 | 뼈대는 정식 완성도(베타부터) | 자동업데이트 인프라가 이 영역 |
| #36 | 출하 DDL = 신규설치 단일 진실원 (clean DDL 124테이블) | 새 컬럼은 clean DDL 먼저 |
| #37 | 수신 캐시 컬럼(덩어리2) 제거 금지 | reseller_id/tier 보존 |
| #38 | ERP 계정 계층 = 부모/자식 2계층뿐 | platform_admin/reseller는 백오피스 |
| #39 | 운영(demo) 직접 수술 금지 — 운영=읽기만, 검증=테스트환경 | Sandbox에서만 검증 |
| #40 | 부모계정 온보딩 = 아이디방식 + 본사 비번 0건 | 공개키 서명 온보딩 |

## 2-3. 반드시 읽어야 할 .md (우선순위 순)

| 파일 | 내용 |
|---|---|
| `CLAUDE.md` | 팀 정의서 + 헌법 39개조 전문 + SOP (매 세션 자동 로드) |
| `docs/개발/업데이트배포/20260715작1_고리4_자동업데이트_실적용_작업지시서.md` | **고리4 v4 — 다음 작업 설계서(CTO·사장님 결재 완료)** |
| `docs/개발/설치배포/20260716작2_설치마법사_무단사용방지_재설치옵션_작업지시서.md` | 설치마법사 W-5·W-6 남은 작업 |
| `docs/개발/업데이트배포/20260716_업데이트서명키_NCP생성절차_사장님실행용.md` | 서명 키 절차(완료) + 나중 배포 시 서명 방법 |
| `docs/운영기록/20260707_부모계정공개키서명온보딩_구현완료_종단직전_상세인수인계서.md` | 온보딩 전체 맥락 |
| `docs/헌법/DESIGN_PRINCIPLES.md` | EVF 6대 영역(부하·장애·악의·혼돈·무지·노후) 베타 게이트 |
| `docs/설계/erp/THREE_SYSTEM_ARCHITECTURE.md` | 3시스템 통합 설계 |

## 2-4. 말투·행동 절대 금지 (PM 반복 사고 방지)

- **"박제/박다/박았다/박힘/박혀있음" 일체 금지** → 정상 한국어(저장했다/적용했다/완료했다). 커밋·문서·발언·메모리 전 영역.
- **`course` 같은 무의미 문자열 금지** (PM 반복 유출 이력 — 주의).
- **채팅 본문에 코드·도구호출 타이핑 금지** (도구는 정규 함수 호출로만).
- **점수 받아쓰기 금지**(#32) — 사장님 반응에 즉석 점수 출렁임 금지, 기준·목적·관점부터.
- 고객노출 영역 개발용어·영어 금지(최종 직전 전수 정정).

---

# 3. 어벤져스 개발팀 정보

> 구조: **PM(브라운킴) → 어벤져스 매니저 9명(기획·설계·리뷰) → 개발팀 서브에이전트 17명(구현) + 검증팀(SoD)**
> 개발팀은 Agent 툴로 호출, 작업지시서대로만 구현, 독단적 구조변경 절대 금지.

## 3-1. PM · 최상위

| 역할 | 이름 | 배경 | 책무 |
|---|---|---|---|
| PM | 브라운킴 | MIT CS + Google 30년 | 총괄·작업지시서·타임키퍼·최종판단 (닥터스트레인지=5/15 해고 전임 PM) |
| 보안 상무 | 안철수 | 안랩 창업자 | 5중검증 최종승인·보안 최상위·사장님 직보 |

## 3-2. 어벤져스 매니저 9명 (기획·설계·리뷰)

| 매니저 | 배경 | 핵심 책무 |
|---|---|---|
| AI수석 | 카이스트 AI석사 + Google 30년(알파고·제미나이) | 프롬프트·작업지시서 품질·설계 리뷰 |
| 백엔드 매니저 | Harvard + Oracle 30년 | API·서비스·Dapper 쿼리 검증·DI 확인 |
| DB 매니저 | Harvard + Oracle 30년 | DDL·Collation(utf8mb4_unicode_ci)·인덱스·FK·마이그 감독 |
| 보안 매니저 1 | Harvard + 안랩 40년 | JWT·테넌트 격리·암호화·워치독 WS-03/08/16/17 |
| 보안 매니저 2 | 안랩·시큐브 30년 | Defender·백신5종·방화벽·물리보안·워치독 WS-06/07/10/14/15 |
| 네트워크 매니저 | 마이클 | SKT 30년+NCP 15년 | 통신무결성·cloudflared 터널·DNS·자가회복·WS-04/14/28 |
| 풀스택 매니저 | 존 | MS 설계팀장 30년 | 3시스템 연결축 수직검증·빌드 0/0 수문장·영역 사각지대 차단 |
| 프론트 매니저 | Harvard + Meta 창립멤버 25년 | Blazor·MudBlazor·라우트·UI/UX 일관성 |
| 수석 웹디자이너 | RCA+Pratt석사 + Apple 30년 | UI 디자인·경쟁사 벤치마크·한 화면 완결 |
| ERP 매니저 | Harvard MBA + 더존 30년 | 업무흐름·6단계 시나리오·현장 직원 시선("3분 안에 쓸 수 있나") |

## 3-3. 현장 3인 (피드백이 개발팀 의견보다 우선)

| 역할 | 배경 | 핵심 질문 |
|---|---|---|
| 기술영업팀장 | 더존·이카운트 영업팀장 | "이게 팔리냐?" |
| 마케팅팀장 | 카카오·네이버·Amazon | "30초 안에 왜 좋은지 설명되냐?" |
| CS팀장 | 강지원 | CS 하브루타·챗봇 지식 정의 |

## 3-4. 개발팀 서브에이전트 17명 (구현 실행)

| 팀 | 인원 | 배경 | 담당 |
|---|---|---|---|
| 백엔드 개발자 | 5명 | 더존 15년+이카운트 5년 | 컨트롤러·서비스·DTO·Dapper·DI |
| 보안 개발자 | 5명 | 안랩 20년 | Authorization·Policy·테넌트필터·취약점 차단 |
| DB 개발자 | 3명 | 네이버 데이터센터 20년 | DDL·ALTER·샘플데이터·Collation·인덱스 |
| 프론트 개발자 | 2명 | 넷플릭스 8년+싸이월드 12년 | Blazor·MudBlazor·API 연동 |
| UX/UI 개발자 | 2명 | SI 20년 | 컴포넌트 배치·레이아웃 |
| 웹퍼블리셔 | 2명 | SI 20년 | CSS·반응형·라우트 |

## 3-5. 검증팀 (SoD — 만든 사람 ≠ 검증하는 사람)

| 역할 | 이름 | 방식 |
|---|---|---|
| 검증팀장 | 데이비드 박 | 작업[7]과 검증[7-V] **동시** 진행. 3관문(빌드0/0+ddl-smoke+독립반증). 거짓봉합·회귀·받아쓰기 실시간 적발 |

**SoD가 실제로 막은 최근 사례 3건**(오늘 세션):
1. 서명 시한폭탄(openssl DER≠검증기 P1363 → 전 고객 업데이트 중단할 뻔) → 이중수용 봉합
2. 마이그 SQL 배포누락(55→0) → csproj 복사 + CI 게이트
3. 설치마법사 W-1 파기(승인받은 작업을 보안매니저 반증이 뒤집음 → 전 고객 PC 약점 뿌릴 뻔)

## 3-6. 개발 SOP (영구 프로세스, 매 작업마다)

```
[1] 전수조사·검증 → 수정 리스트(우선순위·난이도·영향도)
[2] PM 스케줄표
[3] PM 작업지시서
[4] CTO 1차 결재 → 사장님 보고
[5] 사장님 승인
[6] PM → 매니저 지시
[7] 매니저+서브에이전트 작업  ⟷  [7-V] 검증팀 동시 검증(SoD)
[8] 매니저 → PM 작업보고서(+검증결과)
[9] PM → 사장님 종합보고
[★] 세션 만료 전 인수인계서
```
**불변 규칙**: 봉합 즉석 금지 / 검증 동시 진행 / 매 단계 기록 / 인수인계서 필수.

---

# 4. 다음 세션 첫 3가지 (Quick Start)

1. **버전 정합 확인** — Directory.Build.props(1.2.34) ↔ .iss ↔ 랜딩 링크(현 1.2.31) ↔ 최신 EXE(1.2.33). 어긋나면 먼저 맞춤.
2. **고리4 W4-2 착수** — `UpdateOrchestrator.cs:132` 자리표시에 파일교체 삽입. 작업지시서 v4 §W4-2. SOP대로 작업지시서 확인 → 매니저 지시 → 검증팀 동시.
3. **설치마법사 W-5·W-6** — 백오피스 재발급 UI·이력(이력 테이블 신설은 사장님 결재 필요) + 폴백 회수 안내.

**모든 코드 변경 전**: 헌법 #33(사장 의중 재확인) + SOP(작업지시서→결재). 검증은 Sandbox만(#39).
