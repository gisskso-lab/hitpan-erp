# 선행검증서 — 슬롯관리 전수조사 A · 메인PC 기기 줄의 서버·DB 경로 (생성·표식·해제·삭제)

| 항목 | 내용 |
|---|---|
| 문서 | 20260911작4 [1-V]-A |
| 작성 | 검증팀장 데이비드 박 (보안상무 안철수 반증 관점 병행) |
| 단계 | [1-V] 선행검증 — **설계하지 않는다.** 경로 전수 · 가설 · 조회 결과 전에 적는 판정 기준까지 |
| 코드 기준 | 작업본 `feat/20260910-mainpc-seat` HEAD `0f6fb946` — 인용 10개 파일이 1차 기준 `976fa90d` 와 **diff 0** (🟢 `git diff --stat 976fa90d HEAD -- …` 출력 없음). 1차 S 갈래 = worktree `agent-ad6e0f271e6923e6d` 커밋 `690cf57a`·`f4c20ff1`·`ba2c89cc` |
| 입력 | 회의록 `docs/운영기록/20260910_하브루타_메인PC_식별등록_회의록.md` §7-2·§7-6 · 검증계획서 `docs/검증/erp/20260910_검증계획서_메인PC_식별등록_1차.md` §2-1 · 조회 `scripts/readonly/20260910_mainpc_query.ps1` Q1~Q8 |
| 틀 | `docs/_틀/` 에 선행검증서 틀 없음 → 선행검증서 20260910검1 형식을 따름 |
| 상태 | 🟡 [1-V] — 사장님 조회 결과 **도착 전** 작성 (#32 받아쓰기 방지) |

> 표기: 🟢 코드·명령으로 확인(`파일:줄`) / 🟡 코드로 도출했으나 재현 안 함 / ⬜ 미측정.
> 🔴 운영 DB(3306 `hitpan_erp_t004`)·`127.0.0.1:5257`·`localhost:5234` 요청 **0건**(#39 · 작업지시서 §8 C-9). 아래 원인은 전부 **가설**이다.

---

## §0. 사장님 오더 원문 (2026-09-11)

> "자 마이그레이션 작업하고 실측을 못하고 있어. 1. 디바이스 슬롯관리에서 메인PC를 인식하지 못해, 마이그레이션 실측을 못했음. 2. 데이터가 있는 메인PC를 정확하게 식별하고, 메인PC의 관리메뉴를 동작할 수 있도록 슬롯관리 전수조사, 필요한 부분 설계 착수하자. 하부르타 토론 했던 것들 가지고 설계팀은 원인, 대책 강구해서 설계, 개발, 검증 : 동시검증의 3단계 작업 착수하도록 해"

- 결재: 「1차(20260910작2) 이어가며 전수조사 병행」
- 사장님 화면 증상: **①** 기기관리 목록에 「회사 서버」 표시가 아예 없음 **②** 메인PC 가 2~3줄로 보임 **③** 자료관리 들어가면 막힘

### 결론 한 줄

🔴 **①②③ 을 한꺼번에 설명하는 서버·DB 경로는 「JWT 회사 안에 `is_main_pc=1` 줄이 0개」 계열(A1·A2·A4·A5)뿐이다.** 표식 2줄(B1)·8/18작4 잔재(J3)는 「회사 서버」 칩이 **보이는** 모양이라 증상 ① 과 맞지 않는다.
⇒ 1차 S 갈래는 **메인 줄이 정확히 1개일 때만** ③ 을 127 창에서 연다. **0줄 계열은 S 갈래 뒤에도 전부 남는다**(J = `no_main_row`, K2 로 127 창 새 줄이 `pending` 이 됨).
⇒ 판정은 Q1 에 **`tenant_id` 칸이 없어** A2(회사 번호 불일치)를 못 가른다 — 보충 조회 §4-3 필요.

---

## §1. 증상별 서버·DB 경로 전부 (질문 1)

### 1-1. 증상 ① 「회사 서버」 표시가 아예 없음

화면 칩은 `DeviceListDto.IsMainPc` 하나로만 그려진다(`DeviceManagePage.razor:163-171`). 목록은 **JWT 회사의 모든 상태 줄**을 표식 먼저 정렬해 준다(`TenantDeviceService.cs:97-117` — 상태 필터 없음). 따라서 ① = **「이 목록 응답에 `is_main_pc=1` 줄이 0개」** 다.

| # | 경로 | 파일:줄 | 발동 조건 | 판정 |
|---|---|---|---|---|
| **A1** | 기동 등록이 한 번도 성공 안 함 | `MainPcRegistrationService.cs:133-143` · `:83-88` · `:115-123` | ⓐ API 기동 시 `local_company` 0행이면 **재시도 없이** 끝나고 「다음 기동」까지 미룬다(:141) — 설치 후 회사 부트스트랩이 기동보다 늦으면 재기동 전까지 줄 0 ⓑ db.conf 누락(:84-88) ⓒ 3회 모두 예외(:115-123) | 🟡 |
| **A2** | 기동 등록은 됐으나 **다른 회사 번호**로 들어감 | 등록 `MainPcRegistrationService.cs:133-135`(`local_company ORDER BY bootstrap_at LIMIT 1`) ↔ 목록 `DeviceController.cs:30-34`(`Items["TenantId"]` = JWT `tenant_id` · `TenantMiddleware.cs:81-86`) | `local_company` 첫 행의 `tenant_id` ≠ 사장님 계정 JWT 의 `tenant_id` (재부트스트랩·옛 행 잔존 등) ⇒ 서버 줄은 있는데 JWT 회사 목록에 안 잡힘 | 🟡 · ⬜ Q1 에 tenant_id 칸 없음 |
| **A3** | 대표가 아닌 계정으로 목록을 봄 | `DeviceController.cs:38-42` | JWT `account_type ≠ tenant_admin` 이면 **자기 `user_id` 줄만** 남긴다. 서버 줄은 `user_id=NULL`(`MainPcRegistrationService.cs:205`) ⇒ 항상 빠짐 | 🟢 코드 · ⬜ 사장님 로그인 계정 |
| **A4** | 백업 복원이 기기표를 **백업 시점으로 통째 교체** | `BackupService.cs:302` → `:339-344` (`mysql.exe < 파일`) · 덤프 `:331-336`(DB 전체 · `--triggers`) | 복원한 백업이 ⓐ 서버 줄 생성 전 ⓑ DB-86(표식 컬럼) 전 ⓒ 다른 PC·다른 회사 파일(`ExternalFilePath` :259-262)일 때. 기동 등록은 **다음 API 재기동**에야 돈다 ⇒ 그 사이 표식 0 | 🟡 (mysqldump 기본 `--add-drop-table` 전제 · 미재현) |
| **A5** | 재설치 스키마 import 가 표를 DROP | `HitPan-Universal.iss:2192-2204` → `hitpan_db_clean.sql:3873` `DROP TABLE IF EXISTS tenant_devices` | 재설치 시 `items`+`partners` 합이 **0** 이면 import 실행(:2204) · 테이블 1~123개면 `DROP DATABASE`(:2199). **마이그 실측 전(=items·partners 0)인 사장님 DB 는 이 조건에 든다** | 🟡 · 발동 여부 ⬜ |
| A6 | 표식 컬럼 자체가 없음(DB-86 미적용) | `MigrationRunner.cs:143-161` 실패 시 **이후 마이그 전부 중단** | 목록 SQL `d.is_main_pc` 에서 500 ⇒ 「칩 없음」이 아니라 「목록 실패」 모양 | 🟡 · 증상 모양 불일치 가능 |
| A8 | 8/18작4 표식 이동이 **반쯤** 끝남 | `TenantDeviceService.cs:274-283`(①내림) → `:286-296`(②세움) — **트랜잭션 없음** | ① 성공 뒤 ② 전에 연결 끊김·예외 ⇒ 표식 0 · 서버 줄 `revoked` | 🟡 가능성 낮음 |
| (범위 밖) | 화면 쪽 — WASM 캐시·DTO 매핑 | `DeviceManagePage.razor:360` | 서버 응답엔 있는데 칩이 안 그려짐 | ⬜ 본 문서 범위 밖(갈래 B 몫) |

- 🟢 **닫힌 가설 — 요청 DB ≠ 기동 등록 DB**: 둘 다 `TenantConfigReader` 의 `DB_NAME`(db.conf)을 쓴다(`InfrastructureExtensions.cs:20-24` · `MainPcRegistrationService.cs:241-247`). 코드상 같은 DB.
- 🟢 **닫힌 가설 — 8/18작4 이동이 ① 을 만든다**: 이동은 표식을 **옮길 뿐** 0 으로 만들지 않는다(A8 제외). 이동 뒤엔 칩이 브라우저 줄에 붙어 **보인다.**

### 1-2. 증상 ② 메인PC 가 2~3줄

두 모양으로 갈라 적는다 — Q1 이 가른다.

**②-가 「회사 서버」 칩 2줄 이상 (`is_main_pc=1` ≥ 2)**

| # | 경로 | 파일:줄 | 발동 조건 | 판정 |
|---|---|---|---|---|
| **B1** | 8/18작4 표식 이동 → **API 재기동 시 서버 줄 재표식** | 이동 `TenantDeviceService.cs:269-309` → 재표식 `MainPcRegistrationService.cs:156-164`(`SET is_main_pc=1 … WHERE is_main_pc=0` — **status 는 안 건드림**) | 로컬 콘솔 판정 참(`AuthController.cs:136-140`)으로 브라우저 줄 로그인 → 서버 줄 `is_main_pc=0·revoked` → 재기동 → 서버 줄 `is_main_pc=1·revoked` ⇒ **표식 2줄(브라우저 approved + 서버 revoked)**. 다른 브라우저로 다시 로컬 로그인하면 둘 다 내리고 1줄 → 재기동에 2줄 — **1↔2 진동** | 🟡 · 전제: DB-89 방어선 없음 |
| B1 전제 | 출하 DDL 에 DB-89 방어선 없음 | `hitpan_db_clean.sql:3876-3905`(`main_pc_key`·`uq_tenant_main_pc`·트리거 0 — 🟢 grep 0건) + `:3502` `('DB-89','clean-ddl',1)` ⇒ `MigrationRunner.cs:91-107` 가 **성공으로 보고 건너뜀** | 신규 설치(출하 DDL) 고객은 DB-89 가 **영원히 안 돈다** | 🟢 코드 |
| B1′ | 업그레이드 고객에서 DB-89 가 표식 2줄 위에 돌면 | `DB-89_main_pc_db_guard.sql:62-65` ADD UNIQUE → 1062 → `MigrationRunner.cs:143-161` `success=0` + **이후 마이그 중단** | 표식 2줄이 먼저 생긴 DB 에 DB-89 가 처음 적용될 때 | 🟡 · 별개 P0 후보(Q6) |
| B4 | 옛 백업 복원이 표식 2줄 상태를 되살림 | A4 와 같은 줄 | 백업 시점이 B1 상태였을 때 (회의록 §6-1 F-5) | 🟡 |

**②-나 칩 1줄 + 같은 PC 의 다른 줄 1~2개 (`is_main_pc=0`)**

| # | 경로 | 파일:줄 | 발동 조건 | 판정 |
|---|---|---|---|---|
| **B2** | 서버 줄(`MAINPC-`)과 브라우저 줄(`HFPv2-`)은 **산식이 달라 반드시 따로** 생긴다 | 주석 `TenantDeviceService.cs:196-197` · 서버 지문 `MainPcRegistrationService.cs:230-239` · 새 줄 `TenantDeviceService.cs:713-741` | 그 PC 에서 브라우저로 로그인만 해도 +1줄. 지문에 UserAgent 가 들어가 **엣지·크롬마다 +1줄**. 같은 브라우저면 주소(도메인·127·localhost)가 달라도 지문이 같아 `:226-233` 로 같은 줄(선행검증 CTO-4 실측) | 🟢 코드 |
| B2′ | 8/18작4 로 내려진 옛 서버 줄이 `revoked` 로 남음 | `TenantDeviceService.cs:274-283` | 목록은 상태 무관 전부 보여줌(`:97-117`) ⇒ 칩 없는 서버 줄이 따로 1줄 | 🟢 코드 |
| B3 | 컴퓨터 이름·`TENANT_CODE`·`SLOT_INDEX` 변경 → 새 서버 지문 | `MainPcRegistrationService.cs:173-189` · `:230-235` | **표식 줄이 0개일 때만** 새 `MAINPC-` 줄 INSERT(:198-210) ⇒ 옛 `MAINPC-` 줄 + 새 줄 = 서버 줄 2개 | 🟡 |

- 🟢 줄이 **안** 생기는 경로(참고): 1.2.89 [회사서버 컴퓨터] 합류는 서버 줄 번호를 브라우저가 가져갈 뿐 INSERT 0 — 그 뒤 로그인은 `:212-222`(번호 1순위)로 서버 줄을 찾고 `:560-579` 가 이름·종류를 보존한다. QR 은 `mobile/tablet` 칸이라 「메인PC 줄」 모양이 아니다.

### 1-3. 증상 ③ 자료관리 들어가면 막힘

자료관리 입구는 **두 문**이다(선행검증서 20260910검1 결론). 둘 다 서버다.

| # | 문 | 파일:줄 | 막는 조건 | 응답 모양 | 판정 |
|---|---|---|---|---|---|
| **C1** | 문 B — 자리(헤더·소켓) | `[MainPcOnly]` `BackupController.cs:23` · `DataResetController.cs:19` · `MigrationController.cs:438`(찾아보기) · 인라인 `:684`(덮어쓰기) | 터널 헤더가 있으면 거짓 ⇒ **도메인 창은 항상 막힘**. `tenant_devices` 를 **읽지 않는다** | 403 `main_pc_only` | 🟢 코드 |
| **C2** | 문 A — 기기(`DeviceAuthMiddleware`) | `DeviceAuthMiddleware.cs:128-146`(축① 기계비밀+번호) · `:186-194`(축② `is_main_pc=1 ∧ approved` 번호) · `:233-239`(대표 탈출은 `/api/devices`·`/api/auth` 만) | 스위치 **켜짐**(`appsettings.json:20` `Enabled: true`). `/api/backup`·`/api/data-reset`·`/api/migration` 은 통과목록(`:36-64`)에 없음 ⇒ 세션 기기 줄이 ⓘ `pending` ⓘⓘ **표식 줄인데 `revoked`**(B1 의 서버 줄 — 1.2.89 합류로 그 번호를 쓰는 브라우저) ⓘⓘⓘ `approved` 지만 `is_main_pc=0` 이고 열쇠 없음(8/18작4 로컬 자동승인 `:711` 로 들어온 줄 = 20260910검1 H3′) 이면 **대표도 막힘** | 403 `forbidden_device_auth` 「인증된 기기가 아닙니다」 | 🟢 코드 |
| C2 보조 | 표식+revoked 줄의 로그인 | `TenantDeviceService.cs:366`(`&& !isMainPc` 로 회귀 갈래 건너뜀) → `:588` | `allowed=false` 「기기 승인 대기 중입니다」 — 되살리는 갈래 없음(DB-89 트리거가 있으면 UPDATE 때 approved 로 돌리지만 B1 전제상 없음) | 관문 머묾 | 🟡 |
| C3 | 화면 관문 | `DeviceController.cs:129-171` `gate-status` → `IsDeviceApprovedAsync`(`TenantDeviceService.cs:1631-1639`) | 세션 줄이 `approved` 아님 | 메뉴 잠금 | 🟢 코드 |
| (참고) | 검사 고장 시 | `DeviceAuthMiddleware.cs:252-258` | 예외면 **통과** — 막는 경로 아님 | — | 🟢 |

---

## §2. `tenant_devices` 에 쓰는 곳 전수 (질문 2)

> 🟢 `src` 전체 grep `tenant_devices`(시험 제외) + 동적 표 이름(초기화 `DELETE FROM {table}`) + 외부 프로세스(복원·설치) 추적. ERP 로컬 DB 기준.

### 2-1. INSERT

| # | 자리 | 파일:줄 | 넣는 값 | S 갈래 뒤 |
|---|---|---|---|---|
| W7 | 로그인 새 기기 | `TenantDeviceService.cs:713-741` | `is_main_pc` 기본 0 · 상태 = `(_approvalEnabled && !IsLocalConsole) ? pending : approved`(:711) | **바뀜** — `ba2c89cc` K2 가 `!IsLocalConsole` 제거 ⇒ 127 창 새 줄도 `pending` |
| W13b | QR 휴대기기 | `TenantDeviceService.cs:1517-1549` | `pending` · mobile/tablet | 무변경 |
| W14b | 기동 등록 | `MainPcRegistrationService.cs:198-210` | `approved` · `is_main_pc=1` · `user_id NULL` · 한도 무시 | 무변경 |

### 2-2. `is_main_pc` / `status` UPDATE

| # | 자리 | 파일:줄 | 무엇을 | 조건 | S 갈래 뒤 |
|---|---|---|---|---|---|
| W1 | 8/18작4 옛 표식 내림 | `TenantDeviceService.cs:274-283` | 다른 표식 줄 `is_main_pc=0·revoked` | `IsLocalConsole ∧ !isMainPc` | **삭제**(K2) |
| W2 | 8/18작4 표식 세움 | `:286-296` | 현재 줄 `is_main_pc=1·approved` | 같음 | **삭제**(K2) |
| W3 | 폐기 → 대기 회귀 | `:368-378` | `pending` · `revoked_reason=NULL` | `revoked ∧ !isMainPc` | 무변경 · ⚠️ W1 이 쓴 사유를 지움 |
| W4 | 메인 반려 자가회복 | `:410-420` | `approved` | `rejected ∧ isMainPc` | 무변경 |
| W5 | 반려 → 재신청 | `:448-458` | `pending` | `rejected` | 무변경 |
| W6 | 갱신 | `:562-579` | last_seen·ip·ua·이름·종류(표식 줄은 last_seen 만) | 기존 줄 | 무변경 · WHERE 에 tenant_id 없음(번호는 회사 범위 조회에서 옴 — 보안 관찰 낮음) |
| W8 | 폐기 | `:804-814` | `revoked` · 열쇠 NULL | 표식 줄은 거부(:779-783) | 무변경 |
| W9 | 승인 | `:906-927` | `approved` · 열쇠 해시 | — | 무변경 |
| W10 | 인증키 대조 | `:1034-1044` | `approved` · 기계비밀 해시 | 해시 일치 | 무변경 |
| W11 | 재발급 | `:1117-1125` | 열쇠 해시만 | `revoked` 아님 | 무변경 |
| W12 | 거부 | `:1189-1197` | `rejected` | `pending` · 표식 줄 거부(:1179-1187) | 무변경 |
| W13a | QR 재등록 | `:1428-1438` | 지문·ip·ua | 기존 폰 | 무변경 |
| W14a | **기동 재표식** | `MainPcRegistrationService.cs:156-164` | `is_main_pc=1` (status 그대로) | 서버 지문 줄이 `is_main_pc=0` | **무변경 — 남음**(회의록 §7-6 #4) |
| W15 | J 127 창 합류 | `ba2c89cc` `TenantDeviceService.cs` `TryJoinMainPcRowAsync`(diff `+169~+318`) | `last_seen_at` 만 | 대표 ∧ 127 직결 ∧ 표식 줄 status 무관 정확히 1 ∧ approved | **신설** |
| W16 | DB-89 1회 보정·트리거 | `DB-89_main_pc_db_guard.sql:96-99` · 트리거 `:73-90` | 표식+revoked → approved | **DB-89 가 실제로 돈 DB 만**(B1 전제 — 출하 DDL 은 안 돎) | 무변경 |
| W17 | DB-86·88·103 | `DB-86…:50` · `DB-88…:52-82` · `DB-103…:84-96` | ALTER 만 (데이터 UPDATE 0 — 🟢 grep) | — | 무변경 |

### 2-3. 지우기·되돌리기 (DELETE / DROP / 통째 교체)

| # | 경로 | 파일:줄 | 기기표에 무슨 일 | S 갈래 뒤 |
|---|---|---|---|---|
| D1 | **초기화** | `DataResetService.cs:47-70`(보존 목록 :54 에 `tenant_devices`) → `:118-120` 대상 제외 | 🟢 **안 지운다** | 무변경(대조문만 공용화 `f4c20ff1`) |
| D2 | **MDB 「모두 지우고 새로 가져오기」 + 뼈대 재시드** | `MigrationController.cs:705-711`(= D1 호출) · `:722` `ReseedCompanySkeletonAsync`(`CompanyBootstrapProvisioner.cs:593~` — `tenant_devices`·`local_company` 쓰기 0, 🟢 grep) | 🟢 **안 지운다 · 안 되돌린다** | 무변경 |
| D3 | **백업 복원** | `BackupService.cs:302` → `:339-344` | 🟡 **백업 시점으로 통째 교체**(DROP+CREATE+INSERT · 트리거·`schema_migrations` 도 함께) — 폐기 기기·열쇠·표식 부활 = 회의록 §6-1 F-5 | **남음** — P(`f4c20ff1`)는 비밀번호만 추가. 「복원 시 기기표 처리」는 1차에서 뺌(§7-2) |
| D4 | **재설치 스키마 import** | `HitPan-Universal.iss:2192-2204` · `hitpan_db_clean.sql:3873` | 🟡 `items`+`partners`=0 이면 `DROP TABLE tenant_devices` 포함 전체 재생성 · 테이블 1~123 이면 `DROP DATABASE` | **남음**(설치기 무변경) |
| D5 | 백업 생성·사전 백업 | `BackupService.cs:158` · `:299` | 읽기만 | — |
| D6 | 퇴사 | `EmployeeService.cs:684-696` 소프트 삭제 ⇒ `fk_device_user ON DELETE SET NULL`(`hitpan_db_clean.sql:3904`) 미발동 | 🟢 안 건드림 | — |
| (범위 밖) | 본사 백오피스 DB | `Backoffice.API/…/TenantsAdminController.cs:229` DELETE · `DeviceRegistrationController.cs:163·234·368` | 본사 서버의 **같은 이름 다른 DB** — 고객 PC 로컬 표가 아님 | ⬜ 본 문서 범위 밖 |
| (범위 밖) | 시험 | `MdbOverwriteReseedGateTests.cs:495` | 시험 DB 만 | — |

- 🟢 **MDB 이관 자체**(잡 실행)는 `tenant_devices` 를 쓰지 않는다 — Application·Infrastructure 이관 서비스에 grep 0.
- 🟢 `hardware_id` 를 쓰는 C# 코드 0 (컬럼만 있음).
- 🟢 동기화 `SyncService.cs:57` 은 SELECT(본사로 기기 목록 Pull — 헌법 요약 「직원·기기 = Pull」 허용 범위, 업무 데이터 아님 #18).

---

## §3. 1차 S 갈래 뒤 — 닫힘 / 남음 / 모름 (질문 3)

> S 갈래가 바꾼 파일: AuthController · BackupController · DeviceController · MigrationController · MainPcOnlyAttribute · MainPcSeat(신설) · DTO 3 · IAuditService · AuditService · BackupService · DataResetService · TenantAdminPasswordCheck(신설) · TenantDeviceService (🟢 `git diff --stat 976fa90d ba2c89cc`).
> **안 바꾼 파일**: `MainPcRegistrationService` · `DeviceAuthMiddleware` · `installer/*` · `Migrations/*` (🟢 같은 명령 출력 0).

| 경로 | S 갈래 뒤 | 근거 한 줄 |
|---|---|---|
| A1 기동 등록 미성공 | 🔴 **남음** | 등록 서비스 무변경. J 는 `no_main_row` 로 합류 0 · K2 로 127 창 새 줄이 `pending` ⇒ 종전(approved)보다 **한 단계 더 막힌 모양** |
| A2 회사 번호 불일치 | 🔴 **남음** · ⬜ 발동 여부 | J 는 JWT 회사로만 센다 ⇒ `no_main_row` |
| A3 대표 아닌 계정 | ⬜ **모름** — 설계 판단 | 필터 무변경 · 사장님 계정이 대표면 무관 |
| A4 복원 표 교체 | 🔴 **남음** | P 는 비밀번호만(§7-6 #3) |
| A5 재설치 DROP | 🔴 **남음** | 설치기 무변경 |
| A8 이동 반쪽 실패 | 🟢 **닫힘**(새 발생) · 옛 잔재는 남음 | K2 가 W1·W2 삭제 |
| B1 이동 + 재표식 2줄 | 🟡 **절반** — 새 이동은 닫힘 · **기존 2줄·재표식(W14a)·DB-89 부재는 남음** | J 는 `multiple_main_rows` 로 합류 0 ⇒ 검증계획서 §2-1 「2줄 이상 = 게시 차단」 그대로 |
| B1′ DB-89 실패 중단 | 🔴 **남음** · ⬜ 발동 여부 | 러너·마이그 무변경 |
| B2 서버 줄 + 브라우저 줄 | 🔴 **남음** (의도) | 회의록 §7-6 #1 「2중」 → 2차 2-C. K2 뒤 새 브라우저 줄은 `pending`(슬롯 0) 으로 들어온다 |
| B3 이름 변경 서버 줄 2개 | 🔴 **남음** | 등록 서비스 무변경 |
| C1 문 B 도메인 | 🔴 **남음 (결재 1 (가) — 의도)** · 127 창은 🟡 **조건부 닫힘** | `MainPcSeat` 가 `127.0.0.1` 리터럴·포트·`Sec-Fetch-Site` 요구 — 헤더 생존 실측 전(검증계획서 §2-2) |
| C2 문 A | 🟡 **조건부 닫힘** — 127 창 ∧ J `joined` 일 때만 | J 가 표식·approved 줄 번호를 내려주면 축②(`:186-194`) 통과. 조건: **Q1b 가 approved 표식 정확히 1줄.** 도메인 탭은 남음 |
| C2 ⓘⓘ 표식+revoked | 🔴 **남음** | J 는 `main_row_not_approved` 로 합류 0 · 되살리는 갈래 없음(`:366`) |
| C3 관문 | C2 와 같음 | — |

### 3-1. 🔴 S 갈래에서 새로 보이는 위험 (적발 · 코드 수정 안 함)

1. **K2 가 0줄 계열을 더 막는다** — 표식 0줄(A1·A2·A4·A5)이면 J 불충족 → 종전 흐름 → `newStatus = _approvalEnabled ? pending : approved`(S `+429` 부근). 스위치가 켜져 있어(`appsettings.json:20`) 127 창 새 줄이 **pending**. 대표 자기승인 → 열쇠 대조(W9→W10) 로는 축① 이 열리지만 「그 줄은 메인PC 가 아님」이 된다. ⬜ 127 창 관문에서 대표 자기승인 버튼이 뜨는지 — 화면 갈래 확인 필요.
2. **게시 = 서비스 재기동이 A1 을 저절로 고칠 수 있다** — 원인이 A1ⓐ(기동 때 `local_company` 없음)이면 업데이트 재기동에서 서버 줄이 새로 생긴다(`MainPcRegistrationService.cs:198-210`). ⇒ 게시 뒤 「①이 풀렸다」를 **S 갈래 효과로 오인할 위험**(#32). 대조군: **게시 전 재조회**(재기동만 한 상태)로 먼저 가른다 — 단 재기동은 사장님 결재 사항(#29·#39).
3. **J 가 8/18작4 잔재에서 브라우저 줄에 합류** — 검증계획서 §2-1 3행과 같음(재확인). 추가 없음.
4. **AuthController 는 지문이 있을 때만 기기 등록을 부른다**(S diff `:68-69` 문맥 `!string.IsNullOrEmpty(request.DeviceFingerprint)`) ⇒ 127 창에서 지문 생성이 실패하면 J 도 안 돈다(`not_applicable`). ⬜ 발동 조건 미측정.

---

## §4. 판정표 — 조회 결과가 오기 **전에** 적는 기준 (질문 4 · #32)

> 전제: 조회 결과 파일 머리의 `DB 이름` 이 registry.json 과 같을 것(스크립트 :134-136). 다르면 **아래 전부 보류** — 다른 DB 를 본 것이다.

### 4-1. Q1 · Q1b — 줄 모양으로 경로를 가른다

| 판정 | Q1b (`is_main_pc=1` 줄 수 · 상태) | Q1 에서 함께 볼 값 | 가리키는 경로 | 증상 ① 과 맞나 | 다음 |
|---|---|---|---|---|---|
| **J0** | **0** | `fp` 가 `MAINPC-` 인 줄 **없음** | A1 · A4 · A5 | ✅ 맞음 | 가장 이른 `registered_at` 과 설치·복원 날짜 대조(§4-3 Q11) · API 로그 `[MainPc]` 줄(⬜ 위치 미확인) |
| **J0′** | **0** | `MAINPC-` 줄 **있음** · `is_main_pc=0` · `revoked` · 사유 `…(20260818작4)` 또는 NULL | A8(이동 반쪽) · A4(옛 백업) | ✅ 맞음 | 다른 줄에 표식이 없음을 확인 · Q11 복원 이력 |
| **J1** | **1 · approved** · 그 줄 `MAINPC-` | 브라우저 줄 1~2개 `is_main_pc=0` | **서버·DB 는 정상** — ② 는 B2, ③ 은 C1·C2 | ❌ **안 맞음** ⇒ ① 은 **A2·A3·화면** 축 | §4-3 Q9·Q10 로 A2 · 사장님 로그인 계정 유형으로 A3 · 둘 다 아니면 화면 갈래로 넘김 |
| **J2** | **1 · approved** · 그 줄 `HFPv2-` | `MAINPC-` 줄 `is_main_pc=0·revoked`(사유 작4 또는 W3 로 NULL·pending) | 8/18작4 잔재 (검증계획서 §2-1 3행) | ❌ 안 맞음(칩이 브라우저 줄에 보임) | 같음 + 게시 전 PM 판정 (i)/(ii) |
| **J3** | **2 이상** · `HFPv2-` approved + `MAINPC-` **revoked** | Q7·Q8 **비어 있음** | **B1** (이동 + 재기동 재표식) | ❌ 안 맞음(칩 2개) — ② 와 맞음 | 게시 차단(§2-1) · Q6 DB-89 가 `clean-ddl` 인지 |
| **J3′** | **2 이상** · `MAINPC-` 두 줄 | 이름 다름·`registered_at` 다름 | B3(이름 변경) · B4(복원) | ❌ | Q11 · 컴퓨터 이름 이력(⬜) |
| **J4** | 2 이상 인데 **Q7·Q8 있음** | — | **모순** — DB 방어선이 있으면 2줄 불가 | — | 조회 DB 가 다르거나 복원이 표 구조를 바꿈 → 결과 전체 재확인 |
| **J5** | 표식 줄 `revoked` (몇 줄이든) | 그 줄 번호를 쓰는 브라우저(1.2.89 합류) | **C2 ⓘⓘ** — 그 창 업무 API 403 | — | 사장님이 막힌 창이 어느 줄 번호인지(⬜ 화면 갈래) |
| **J6** | (무관) | `HFPv2-` 줄이 `approved ∧ is_main_pc=0 ∧ has_key=0` | **C2 ⓘⓘⓘ** H3′ — 대표도 자료관리 403 | — | 그 줄 `approved_at` ≈ 첫 로컬 로그인이면 `:711` 자동승인 흔적 |
| **J7** | (무관) | `HFPv2-` 줄이 `pending` | C2 ⓘ · C3 — 관문 머묾 | — | K2 게시 뒤 늘어날 모양(§3-1 #1) |
| **J8** | (무관) | `HFPv2-` 줄 수 ≥ 2 | B2 — 브라우저 엔진 수(엣지·크롬) | — | `user_agent` 는 Q1 에 없음 → 보충 Q9 |

### 4-2. Q2~Q8 — 곁가지 판정

| 조회 | 값 | 뜻 | 가르는 경로 |
|---|---|---|---|
| **Q2** | `pc·approved` 수 vs 요금 한도 | 슬롯 계수는 `approved` 만(`TenantDeviceService.cs:1697-1704`) | 한도 초과 로그인 401 (20260910검1 H2) 여부만 — ①②③ 직접 원인 아님 |
| **Q3** | 0행 | **근거 아님**(로그인 안 감사 실물 0행 · 병렬이슈20) | 판정에 쓰지 않는다 |
| **Q4** | `jobs=0 ∧ mig_invoices=0` | 이관 전 DB | **D4 조건(`items+partners=0`)에 들 수 있음** ⇒ A5 가능성 열림 · 결재6 숫자 실측 가능 |
| **Q4** | `jobs>0` | 덮어쓰기·보태기 있었음 | D2 는 기기표를 안 건드림 ⇒ 기기표 이상과 **무관**. 표 이상 시점이 잡 시점과 겹치면 D2 가 아니라 **D3·D4** 를 의심 |
| **Q5** | `tenant_admin` 행 0 또는 사장님 계정이 아님 | A3 | ① 이 계정 축 |
| **Q6 DB-89** | `app_version='clean-ddl' ∧ checksum NULL ∧ success=1` | 출하 DDL 설치 — **방어선 없음** | B1 가능 · Q7·Q8 은 비어야 정상 |
| **Q6 DB-89** | `checksum 있음 ∧ success=1` | 마이그로 실제 적용 | Q7·Q8 **둘 다 있어야** 함 — 없으면 D3(복원이 표 구조를 옛것으로) |
| **Q6 DB-89** | `success=0` | **러너가 DB-89 에서 멈춤**(`MigrationRunner.cs:143-161`) | B1′ — DB-103·104·119 행도 없으면 이후 마이그 전부 미적용 = **별개 P0** |
| **Q6 DB-86** | 없음·`success=0` | 표식 컬럼 없음 | A6 — Q1 자체가 `Unknown column` 으로 실패할 것 |
| **Q7·Q8** | 둘 다 있음 | 표식 2줄 DB 가 막음 | J3 불가 · W14a 재표식은 1062 로그만 |
| **Q7·Q8** | 하나만 있음 | 반쯤 적용 | DB-89 중간 실패 흔적 → Q6 과 대조 |

### 4-3. 🔴 보충 조회 제안 — Q1 로는 못 가르는 것 (SELECT 만 · 사장님 결재 후 · 스크립트는 고치지 않음)

| 번호 | 무엇을 가르나 | SQL (읽기 전용) |
|---|---|---|
| Q9 | **A2** 회사 번호 불일치 · B2 브라우저 수 | `SELECT tenant_id, LEFT(fingerprint,7) AS p, is_main_pc, status, COUNT(*) n, GROUP_CONCAT(LEFT(user_agent,40)) ua FROM tenant_devices GROUP BY tenant_id, p, is_main_pc, status;` |
| Q10 | A2 — 기동 등록이 쓰는 회사 vs 계정 회사 | `SELECT tenant_id, bootstrap_at FROM local_company ORDER BY bootstrap_at;` · `SELECT tenant_id, account_type, COUNT(*) n FROM users WHERE is_deleted=0 GROUP BY tenant_id, account_type;` |
| Q11 | A4·D3 복원 · A5 재설치 시점 | `SELECT started_at, status, triggered_by_user IS NOT NULL AS by_user FROM restore_history ORDER BY started_at;` · `SELECT MIN(registered_at), MAX(registered_at) FROM tenant_devices;` |
| Q12 | B1′ 러너 중단 | `SELECT migration_id, app_version, success, checksum IS NULL AS no_ck FROM schema_migrations WHERE success=0 OR migration_id >= 'DB-86' ORDER BY id;` |

- ⚠️ 비밀·업무 데이터 없음(tenant_id·개수·시각만) — 결과는 고객 PC 안에서만(#18·#22).
- ⚠️ 판정 순서: **머리 DB 이름 → Q6·Q7·Q8(방어선) → Q1b(표식 수) → Q1(지문 접두·상태) → Q9·Q10(회사 번호) → Q11(시점)**.

---

## §5. 가설 순위 (결과 전 · 단정 아님)

| 순위 | 가설 | 세 증상과의 맞음 | 뒤집는 결과 |
|---|---|---|---|
| 1 | **A1 / A5 / A4 — 표식 0줄** + B2(브라우저 줄 2~3) + C1·C2 | ①②③ 모두 | Q1b ≥ 1 (JWT 회사 기준) |
| 2 | **A2 — 회사 번호 불일치** + B2 + C2 | ①②③ 모두 · Q1b 는 1 로 보일 수 있음(Q1 은 회사 무관) | Q9 에서 표식 줄 tenant_id = 계정 tenant_id |
| 3 | J1 + 화면 축(A3·캐시) | ② ③ 은 서버, ① 은 화면 | Q5·로그인 계정이 대표 ∧ Q9 일치 |
| 4 | B1(표식 2줄) · J2(작4 잔재) | ② ③ 은 맞음 · **① 과 모순** | 사장님 화면 재확인: 칩이 정말 0개인가 |

---

## §6. 무엇을 쟀고 무엇을 못 쟀나

- 🟢 코드 판독 16개 파일 · grep · `git diff --stat` (기준 동일성·S 갈래 범위).
- ⬜ 실물 DB·실경로 요청 **0건**(금지) · 테스트환경 재현 **0건** · mysqldump 기본 옵션(`--add-drop-table`)은 도구 기본값 전제로만 적음 · API 로그 위치 · 화면(WASM) 쪽 칩 렌더링 · 127 창 대표 자기승인 버튼.
- ⬜ 본사 백오피스 DB 의 `tenant_devices` 쓰기 3곳은 범위 밖으로 표시만.

## §7. 설계팀에 넘기는 사실 (설계 아님)

1. 기기표를 **지우는·되돌리는** 서버 경로는 **복원(D3)·재설치 import(D4) 둘뿐**이다. 초기화·덮어쓰기+재시드는 기기표를 보존한다(🟢).
2. 표식을 **세우는** 자리는 3곳 — W14b(기동 INSERT)·W14a(기동 재표식)·W2(S 갈래에서 삭제). 표식을 **내리는** 자리는 W1(S 갈래에서 삭제) 하나뿐. ⇒ S 갈래 뒤 표식을 세우는 자리는 **기동 서비스 하나**만 남는다.
3. 출하 DDL 신규 설치는 DB-89 가 `clean-ddl` 로 성공 기록돼 **영원히 안 돈다**(`hitpan_db_clean.sql:3502` + `MigrationRunner.cs:91-107`).
4. 기동 등록은 `local_company` 가 없으면 **다음 기동까지** 미룬다 — 재시도 없음(`MainPcRegistrationService.cs:137-143`).
5. 조회 스크립트 Q1 에는 `tenant_id`·`user_agent` 가 없다 → A2·B2 판정 불가 → 보충 Q9~Q12.

---

## §8. 조회 결과 대조 — **J3 확정** (2026-09-13 · 실물 `hitpan_erp_t004` · `DESKTOP-58D4SDM`)

> 결과 파일 `hitpan_mainpc_query_20260913_154821.txt` (사장님 실행 · PM 읽기만) · 머리 DB 이름 = registry.json (불일치 경고 없음) ⇒ §4 전제 충족.

### 8-1. 판정 순서대로 (§4-2 마지막 줄 순서)

| 조회 | 실측 | §4 판정 |
|---|---|---|
| Q7·Q8 | **둘 다 빈 결과** | `main_pc_key`·`uq_tenant_main_pc` 없음 ⇒ **DB 방어선 부재** |
| Q6 | DB-86·89·103·104 = `clean-ddl`·checksum NULL·success=1 / DB-119 만 1.3.39.0 | §7-3 예측 적중 — **DB-89 는 영원히 안 돈다** · Q7·Q8 이 빈 것과 정합 |
| Q1b | `is_main_pc=1` 이 **2줄** (approved 1 · revoked 1) | 🔴 표식 2줄 |
| Q1 | `MAINPC-9c1163c` 「회사 서버 (자료 보관 컴퓨터)」 `is_main_pc=1 · revoked` · 사유 「…(20260818작4)」 · revoked_at **9/11 18:39:31** · **last_seen_at 9/11 23:05** ／ `HFPv2-a341d087` 「Windows · Edge」 `is_main_pc=1 · approved` · 9/10 04:18 등록 · last_seen 9/13 15:18 ／ 나머지 HFPv2 4줄 `is_main_pc=0 · approved` | **J3 = B1 (이동 + 재기동 재표식)** · Q7·Q8 비어 있어 J4(모순) 아님 |
| Q4 | jobs·mig_invoices·hr 전부 0 | **§7-1 예측 적중** — 9/13 초기화가 기기표를 안 건드렸다 |
| Q5 | `tenant_admin · is_parent=1` 1명 | H1-e·A3(계정 필터로 줄이 숨음) **배제** |
| Q2 | pc approved 4 · mobile approved 1 · pc revoked 1 | revoked 는 계수에서 빠짐 ⇒ **요금 영향 0** |
| Q3 | 6행 (9/11 23:02 `0f62dc65` key_reissue·verified) | 표식 이동은 감사에 안 남음(병렬이슈20) — 판정은 Q1 로 한 대로 |

### 8-2. 🔴 원인 — 코드로 닫혔다 (가설 아님)

**진범은 「내림 실패(H1-d)」 가 아니다. 내려간 표식을 재기동이 되올린다.**

1. `Infrastructure/Services/TenantDeviceService.cs:274-283` ① 이 돌았다 — 사유 문자열 `'메인PC 표식을 실제 사용 화면으로 옮김 (20260818작4)'` 은 **이 한 곳에서만** 나온다(전수 grep). 같은 UPDATE 가 `is_main_pc=0`·`status`·`revoked_at`·사유를 **한 문장**으로 쓰므로, 사유만 남고 표식이 1인 상태는 **이 문장이 반쪽 실행된 결과가 될 수 없다** ⇒ H1-d(두 UPDATE 사이 트랜잭션 없음) 는 이 건의 원인이 **아니다**.
2. `API/HostedServices/MainPcRegistrationService.cs:156-164` 가 되올린다 — `SET is_main_pc=1, last_seen_at=NOW() WHERE device_id=@Id AND **is_main_pc=0**`. **`status` 를 보지 않는다.** `revoked` 줄도 되올린다. `alreadyHasMain` 검사(`:179-189`)는 **아래 ② 신규 INSERT 경로에만** 있어, 이 되올림 경로는 「이미 표식 1줄 있음」 을 검사하지 않는다. `BackgroundService` 라 **API 기동마다 1회** 돈다.
3. 시각이 맞물린다 — 9/11 **18:39:31** 표식 이동(①·②) → 9/11 **23:02~23:05** 1.3.40 업데이트로 API 재기동 → 되올림. 폐기 줄의 `last_seen_at = 9/11 23:05` 이 그 UPDATE 의 `NOW()` 흔적이다(그 줄이 접속한 것이 아니다).
4. DB-89 는 이 조합을 **정확히 예견하고 세 겹으로 막으려 했다** — `main_pc_key` 생성컬럼 + `uq_tenant_main_pc` UNIQUE(2줄 물리적 불가) · 트리거 2개 `IF NEW.is_main_pc=1 AND NEW.status='revoked' THEN status='approved'`(`DB-89_main_pc_db_guard.sql:73-90`) · 기존 revoked 메인PC 되살리기(`:96-99`). **실물엔 셋 다 없다**(Q7·Q8) — 출하 DDL 설치라 그 마이그가 안 돌았다.

⇒ **증상 ②(메인PC 2~3줄) = 표시 중복(V)이 아니라 실제 DB 2줄.** 사장님 9/13 「수정안됨. 반려」 와 정합.

### 8-3. 아직 닫히지 않은 것 (단정 금지)

- **증상 ③ 자료관리 막힘** — 사장님이 쓰는 줄은 `HFPv2-a341d087`(approved ∧ is_main_pc=1 ∧ has_key=1) 이라 `DeviceAuthMiddleware.cs:186-194`(`approved ∧ is_main_pc=1`) 를 **통과해야 한다.** 위 원인으로 **설명되지 않는다** ⇒ 막힌 화면 문구 캡처(B §7 K5) 전에는 원인을 적지 않는다. 「재현했다고 원인이 아니다」 재발 방지.
- **증상 ①(「회사 서버」 칩 안 보임)** — 목록 SQL(`GetAllAsync:100-117`)은 revoked 를 걸러내지 않고 칩은 `IsMainPc` 만 본다(`DeviceManagePage.razor:163-169`) ⇒ 지금 DB 면 칩이 **2개** 보여야 한다. 사장님 화면이 그렇지 않다면 **이 판정이 틀린 것**이므로 K1·K2 가 반증 시험이다.
- 부수 적발: `DeviceManagePage.razor:198-204` 는 `IsMainPc` 면 해제 버튼을 잠근다 ⇒ **폐기된 서버줄을 화면에서 치울 수 없다**(목록에 계속 남는다). 설계 범위 밖 · 적발만.

### 8-4. 설계팀에 넘기는 사실 추가 (설계 아님)

6. 표식을 되올리는 자리는 `MainPcRegistrationService:156-164` 하나이고, 그 자리에 **status 검사도 「이미 1줄 있나」 검사도 없다**.
7. 출하 DDL 설치 고객은 DB-89 방어선이 통째로 없다 ⇒ **코드에서 1줄을 보장하지 못하면 DB 가 받아 준다**.
8. 보충 조회 Q9~Q12 는 이 판정에 **불필요해졌다** — J3 가 Q1·Q1b·Q6·Q7·Q8 만으로 갈렸고, 회사번호 불일치(A2)·브라우저 수(B2) 가설은 J3 확정으로 순위에서 내려갔다. 필요해지면 그때 결재받는다.
