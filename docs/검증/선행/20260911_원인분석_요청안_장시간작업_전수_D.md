# 원인분석 D — 요청 하나 안에서 오래 걸리는 일 전수 · 시간 제한에 잘릴 때 반쯤 남는 것 (20260911작4)

| 항목 | 내용 |
|---|---|
| 역할 | [1-V] 원인분석 · 검증팀장 데이비드 박 (hp-verifier) — 설계·수정 안 함 |
| 대상 | 브랜치 `feat/20260910-mainpc-seat` · HEAD `0f6fb946` 코드 읽기만 |
| 출발 문서 | [병렬이슈24](../병렬이슈/20260911_병렬이슈24_모두지우고_가져오기는_요청안에서_백업하다_100초에_잘린다.md) |
| 판정 | **반려(전제)** — 같은 사고 모양이 **복원(P0)** 에도 있고, 병렬이슈24 의 "안 지워졌다" 추론에 **빠진 창**이 있다. 이 전수를 지키는 시험 0 |
| 표기 | (실측) 코드 `파일:줄` 확인 · (추정) 코드상 추론 · (미측정) 확인 못 함 |
| 금지 준수 | DB·5257·5234·도메인 요청 0 · 코드·기존 문서 수정 0 |

---

## 0. 사장님 오더 원문

> "너희가 전수조사 하고 분석해서 원인을 찾아봐."
>
> (앞선 실측: localhost:5234 에서 「모두 지우고 새로 가져오기」·「모든데이터 초기화」 둘 다 502 프록시 시간초과, 콘솔 `[DataResetService.ResetAllAsync] JsonException`)

---

## 1. 접속 경로별 시간 제한

| 경로 | 제한 | 근거 | 잘리면 서버 `ct` |
|---|---|---|---|
| **localhost:5234** (PowerShell 프록시) | 응답 첫 바이트 **100초** (`HttpWebRequest.Timeout` 미설정 = .NET 기본값) · 응답 본문 복사는 `ReadWriteTimeout` 기본 300초 (문서 근거, 미측정) | `installer/web-server.ps1:92-113` (실측) | 울린다 — 병렬이슈24 실측 2회 |
| 〃 추가 결함 | 🔴 **한 번에 한 요청만 처리한다.** `while` + `GetContext()` 로 받은 요청을 끝까지 처리한 뒤에야 다음 요청을 받는다 → 긴 요청 하나가 도는 100초 동안 **같은 5234 의 정적 파일·다른 API·진행 폴링이 전부 멈춘다** | `web-server.ps1:63-64·89-137` (실측) | — |
| **도메인** (`{id}.hitpan.kr` → cloudflared → `http://localhost:5257`) | Cloudflare 엣지 응답 대기 → 524. 코드 안 근거는 **주석 한 줄뿐**: "Cloudflare 524 회피용" `MigrationController.cs:293`. 터널 ingress 는 `hostname + service` 만 넣고 `originRequest` 설정 0 (`CloudflareDomainService.cs:265-273`). 값(요금제별 100초 여부) 미측정 | `InstallerBootstrapController.cs:129` 주석 (ingress → 5257) | **미측정** — 끊겨도 오리진 연결을 닫는지 모른다 |
| **127.0.0.1:5257** 직결 | 프록시 없음. API 에 요청 제한 미들웨어 0 (`AddRequestTimeouts`·`RequestTimeout` grep 0). 남는 제한 = 화면 HttpClient **10분** | `src/HitPan.Web/Program.cs:127` (실측) | 10분 뒤 화면이 취소하면 울린다 (추정) |
| 서버 안쪽 | 대사 쿼리 1개 **180초** 허용 — 100초 경로보다 길다 · 회사 뼈대 연결 `DefaultCommandTimeout=90` (재시드가 이 연결을 쓰는지 미측정) | `MdbReconciliationService.cs:70` · `CompanyBootstrapProvisioner.cs:43` | — |

---

## 2. 전수표 — 요청 안에서 무거운 일을 하는 액션 (질문 1·2)

> 범위: `src/HitPan.API/Controllers` 전체에서 백업·복원·초기화·MDB·일괄확정·일괄등록·대량발송·내보내기·외부호출 키워드와 `[HttpPost]` 목록으로 추렸다.
> **요청 밖(백그라운드)으로 넘긴 것은 딱 하나**: MDB 가져오기 본체 `RunMigrationInBackground` (`MigrationController.cs:759-848`, `Task.Run` + `CancellationToken.None` `:803`). 나머지는 전부 요청 안에서 끝낸다.

| # | 액션 | 무거운 이유 · 소요 근거 | `ct` 가 닿는 곳 | 잘린 시점별 반쯤 남는 것 | 등급 |
|---|---|---|---|---|---|
| **D1** | `POST migration/legacy-mdb/start` **덮어쓰기** `MigrationController.cs:300-328·675-740` | 전체 DB mysqldump → 삭제 트랜잭션 → 뼈대 재시드를 **응답 전에** (실측) | 백업 프로세스 복사·대기 · 삭제 tx · **커밋** · 커밋 뒤 감사 · 재시드 | 병렬이슈24 §2 그대로 + 🔴**빠진 창(아래 D1-a)** | **P0** |
| D1-a | 〃 커밋 직후 | — | `DataResetService.cs:169-175` 감사 기록 `_audit.LogAsync("data_reset", …, ct)` | 커밋 **뒤** 감사 기록이 취소되면 예외로 빠진다 → **지워짐 + 뼈대 없음 + `data_reset` 감사 줄도 없음**. ⇒ 병렬이슈24 §3-1 "`data_reset` 줄 없음 = 백업 중 잘림" 은 이 창을 **배제하지 못한다** (03:43 정합성 감시가 표를 읽은 사실이 따로 받쳐줄 뿐) (추정) | P0 |
| D1-b | 〃 삭제 도중 | — | `DataResetService.cs:161` `RollbackAsync(ct)` — **이미 취소된 ct 로 롤백 호출** | 롤백 호출 자체가 취소 예외 → `data_reset_failed` 감사(`:163`) 도 안 남음. 자료는 연결 폐기 시 롤백 (추정) | P1 |
| **D2** | `POST data-reset` `DataResetController.cs:45-60` | D1 과 같은 메서드 (`ResetAllAsync` + `ReseedCompanySkeletonAsync`) | 같음 | D1·D1-a·D1-b 와 같음 | **P0** |
| **D3** | `POST backup/run` `BackupController.cs:57-63` → `BackupService.cs:132-230` | 전체 DB mysqldump. 화면 문구는 "몇 초~수십 초" (`BackupPage.razor:190`) — 큰 DB 근거 없음 | `INSERT backup_history 'running'`(:144) · 덤프 · **실패 기록 UPDATE 도 같은 ct** (:222·:230) | 잘리면 실패 기록 UPDATE 도 취소 → `backup_history` **'running' 으로 영구** · **반쪽 .sql 파일**이 백업 폴더에 남음 · mysqldump 고아 (병렬이슈24 §3-2 실측 2개). `using var proc`(:359) 는 프로세스를 죽이지 않는다 | P1 |
| **D4** | `POST backup/restore` `BackupController.cs:65-71` → `BackupService.cs:237-328` | **사전 백업(mysqldump) + mysql.exe 가져넣기** — 전체 DB 두 번 분량 | 사전 덤프 · `fs.CopyToAsync(stdin, ct)`(:389) · 대기(:393) · 실패 기록(:323) | (a) 사전 백업 중: 자료 그대로 · `restore_history 'running'` 영구 · 반쪽 pre_restore 파일<br>(b) 🔴 **가져넣기 중**: 트랜잭션 없음. 덤프 인자에 기본 동작을 끄는 옵션 0(:335) → 표마다 `DROP TABLE → CREATE → INSERT` 순 → **앞 표는 백업 시점 · 뒤 표는 현재 · 잘린 표는 비었거나 반쪽 = 표끼리 시점이 다른 DB** (추정)<br>(c) 예외 경로에서는 `StandardInput.Close()`(:391) 가 안 불린다 → mysql.exe 가 입력 대기로 남을 수 있음. 덤프 출력의 `LOCK TABLES … WRITE` 구간에서 멈추면 **그 표 쓰기 잠금을 쥔 채** 남는다 (추정 · 미측정) | **P0** |
| D5 | `POST migration/legacy-mdb/preview` `:68-134` → `MdbMigrationService.cs:178-230` | MDB 23개 표 COUNT (OLEDB 동기). MDB 크기 비례 · 미측정 | **ct 를 받고 안 쓴다** (`PreviewCoreAsync :186-230` 사용 0) | 읽기 전용 → 반쪽 없음. 잘려도 서버는 끝까지 돈다 | P3 |
| D6 | `POST migration/legacy-mdb/reconcile` `:150-219` → `MdbReconciliationService` | MDB 집계 + ERP 집계. 쿼리당 180초 허용(:70) | ERP 쿼리 `CommandDefinition(ct)` · OLEDB 쪽 미측정 | 읽기 전용 → 반쪽 없음. **설계상 100초 경로에서 끝까지 못 갈 수 있다** | P2 |
| D7 | `POST migration/legacy-mdb` (**동기 전체 이관**) `:225-290` | 이관 전체를 요청 안에서. 화면 호출 0 (Web grep 0) · 5/13 실호출 흔적(`db-backup-20260608-212123/hitpan_erp.sql` 감사 행 다수 500) | `MigrateAsync(ct)` · 확인 지점 `ThrowIfCancellationRequested` `MdbMigrationService.cs:2223·2377·2963` | 병합 모드로 **표 일부만 들어간 채** 멈춤. 표 단위 커밋 여부 미측정. `TenantAdminOnly` 로 열려 있어 주소만 알면 부를 수 있다 | P2 |
| **D8** | `POST sales/deliveries/bulk-confirm` `SalesController.cs:168-192` | 건마다 `ConfirmDeliveryAsync` (재고·원장·분개). 건수 비례 | 건마다 ct | 앞 건 **확정** · 뒤 건 미확정. `catch (Exception)`(:185) 가 취소 예외도 삼켜 남은 건을 "실패"로 담지만 **응답은 아무도 못 받는다**. 한 건 도중 취소 시 그 건 내부 원자성 미측정 | P1 |
| D9 | `POST users/bulk` `UserController.cs:174-222` → `UserService.cs:341-362` | 행마다 `CreateAsync` (행 독립) | 행마다 ct | 앞 행 생성 · 뒤 행 없음 · 재업로드 시 앞 행이 "이미 있음" 실패 | P2 |
| **D10** | `POST payroll/slips/send-mail` `PayrollController.cs:449-465` → `PayslipMailService.cs:98-130` | 사람마다 PDF 생성 + SMTP. 인원 비례 | 사람마다 ct · 이력 INSERT(:426) | 앞 사람 발송 · 뒤 사람 미발송. 한 사람 SMTP 전송 뒤 이력 전 취소 시 **보냈는데 이력 없음** → 재발송 = **급여명세서 중복 메일** (추정) | P1 |
| D11 | `POST email/send` `EmailController.cs:52-58` → `EmailService.cs:195-230` | PDF + SMTP 1통 | 연결·인증·전송·**끊기**(:203)·이력 INSERT(:214-225) | 전송 성공 뒤 `DisconnectAsync(ct)` 취소 → catch 가 **status=failed** 로 기록하거나, 이력 INSERT 자체가 취소 → **보냈는데 실패/무기록** (추정) | P2 |
| D12 | `POST fax/send` `FaxController.cs:54-60` → `FaxService.cs:206-245` | 외부 팩스 공급자 (과금) | 송출 · 이력 INSERT(:229-230) | D11 과 같은 모양 — **송출됐는데 무기록** (추정) | P2 |
| D13 | `POST delivery/batch` `DeliveryBatchController.cs:20-27` → `DeliveryBatchService.cs:31-146` | 한 트랜잭션 일괄 저장 | 전 구간 | 커밋(:141) 전 잘림 = 롤백 → 반쪽 없음. 화면 호출자 Web grep 0 | P3 |
| D14 | `POST monthly-closing/close` `MonthlyClosingService.cs:93-149` | 쿼리 3~4 + UPSERT + 감사. 무거울 근거 없음 | 전 구간 | UPSERT 뒤 감사 전 잘림 = 마감됨 · 감사 없음 | P3 |
| — | 읽기·가벼움 (반쪽 없음) | `export/excel` 10만 행 상한·ct 없음 `GridExportController.cs:48·59` · `documents/{type}/{id}/pdf·excel` `DocumentController.cs:87·134` · `finance/integrity-check` `FinanceController.cs:115` · `reports/*` `ReportController.cs:22-327` | — | 잘려도 오류 표시만 | P3 |
| ⬜ | 외부 호출 (판정 미실시) | `install/provision-tunnel` `InstallController.cs:31` (Cloudflare · AllowAnonymous) · `esign/sign` `ESignController.cs:52` · `labor-contracts/{id}/send` `LaborContractController.cs:73` · `chatbot/ask` `ChatbotController.cs:28` (기본 HttpClient `Program.cs:212`) · `setup/bootstrap` `CompanyBootstrapController.cs:43` | — | 미측정 | ⬜ |

---

## 3. 잘렸을 때 화면이 사장님께 보여주는 것 (질문 3)

| 액션 | 화면 코드 | 502(5234) · 524(도메인) · 비 JSON 본문일 때 | 문제 |
|---|---|---|---|
| D1 덮어쓰기 시작 | `MdbMigration.razor:824-829` | `시작 실패 (502): API proxy error: … 작업 시간이 초과되었습니다.` — **프록시 원문 그대로**. 524 면 Cloudflare HTML 본문이 통째로 스낵바에 (추정) | 고객 화면에 개발 문구. "지워졌는지·다시 누르지 말 것" 안내 0. 올바른 안내(`:841` "다시 누르지 마세요")는 **10분 예외 경로에서만** 나온다 |
| D2 초기화 | `DataResetPage.razor:95-117` ← `Web/Services/DataResetService.cs:36-43` | 상태코드 확인 없이 JSON 파싱 → `JsonException` → **「초기화 요청 중 통신 오류가 발생했습니다.」** | 백업·삭제가 어디까지 갔는지 안내 0 · 재시도 억제 0 |
| D3 백업 | `BackupPage.razor:193-202` ← `Web/Services/BackupService.cs:55-56` | **「백업 실패: HTTP 502」** | 폴더에 반쪽 .sql 이 남는다는 안내 0 |
| D4 복원 | `BackupPage.razor:227-234` ← `Web/Services/BackupService.cs:72-73` | **「복원 실패: HTTP 502」** | 🔴 반쪽 복원일 수 있는데 **"실패"로 단정** → 재시도를 부른다 |
| D5·D6 미리보기·대사 | `MdbMigration.razor:738-742` · `:1226-1231` | 미리보기 = 본문 원문 · 대사 = 상태코드만 | 개발 문구 |
| D8 일괄확정 | `Web/Services/DeliveryService.cs:225-237` | **전 건 실패** + 사유 = 프록시 원문 | 🔴 실제로 확정된 건이 있는데 전부 실패로 표시 |
| D9 사용자 일괄 | `Web/Services/UserService.cs:138-141` | 오류 1줄 = 프록시 원문 | 앞 행은 생성됨 |
| D10 급여명세 발송 | `Web/Services/PayrollService.cs:251-262` | `null` 반환 (페이지 문구 미확인) | 누구에게 나갔는지 모름 |
| D11 메일 | `Web/Services/EmailClientService.cs:36` | `HTTP 502` | — |
| 공통 | `HitPanApiAuthHandler.cs:40-93` | 401·403 만 처리 · 5xx·비 JSON 공통 처리 0 | 화면마다 제각각 |

---

## 4. 경로별로 어느 액션이 잘릴 수 있나 (질문 4)

> 기준: 100초 = 5234 (실측) · 도메인 = Cloudflare 제한(값 미측정, 코드 주석상 524 존재) · 127 = 10분

| 액션 | 5234 | 도메인 | 127 직결 | 비고 |
|---|---|---|---|---|
| D1 덮어쓰기 · D2 초기화 | **잘림 (실측 2회)** — 백업 > 100초면 | 잘림 (추정) · 서버 취소 여부 미측정 | 백업+삭제+재시드 > 10분이면 | D1 은 `MainPcOnly` 판정(`:684`) — 메인PC 가 도메인으로 접속하면 이 경로 |
| D3 백업 | 잘림 | 잘림 (추정) | > 10분 | 컨트롤러 전체 `MainPcOnly` `BackupController.cs:23` |
| D4 복원 | **잘림 — 덤프+가져넣기 = 백업의 약 2배 시간** | 잘림 (추정) | > 10분 | 가장 먼저 잘리는 액션 |
| D5 미리보기 · D6 대사 | MDB 크기 따라 | 〃 | 대체로 안 잘림 | D6 은 쿼리 하나에 180초 허용 |
| D7 동기 이관 | 거의 확실히 잘림 | 〃 | 1년치 100만+건이면 10분도 넘음 (`Program.cs:126` 주석) | 화면 없음 |
| D8·D9·D10 | 건수·인원 따라 | 〃 | 대체로 안 잘림 | — |
| 모든 액션 공통 (5234) | 🔴 **긴 요청 하나가 5234 전체를 최대 100초 멈춤** (§1) | 해당 없음 (5257 직결) | 해당 없음 | 사장님 "화면이 멈췄다" 체감의 다른 원인 후보 |

---

## 5. 이 전수를 지키는 게이트·시험 (질문 5)

| 확인 | 결과 |
|---|---|
| `src/HitPan.Tests` · `src/HitPan.IntegrationTests` 에서 `RequestAborted`·`CancelAfter`·`.Cancel()`·`TaskCanceledException`·`OperationCanceledException`·`HttpWebRequest`·`web-server` 검색 | **0건** (실측) |
| `HitPan.Tests/Integrity/MdbOverwriteReseedGateTests.cs` (Fact/Theory 10개) | `Cancel`·`Timeout` 0건 — 순서·재시드 호출만 본다. 백업이 느리거나 요청이 끊기는 경우를 **안 본다** (실측) |
| 복원·백업·일괄확정·급여메일의 취소 시험 | 테스트 프로젝트에서 `RestoreAsync`·`RunBackupAsync`·`BulkConfirm` 검색 0건 (실측) |
| CI 의 `web-server.ps1` | `deploy-update.yml:514-523` — **EXE 전용 동봉물**로 시크릿 스캔만 한다. 동작 시험 0 · 업데이트 zip 미포함(병렬이슈24 §4 와 일치) (실측) |
| TestServer 계열 | 프록시·터널이 없어 100초 차단을 **원리상 못 본다** |

⇒ **이 전수를 지키는 게이트 0.** 병렬이슈24 §4 의 「느린 백업 + 끊긴 요청」 대조 시험은 D1 뿐 아니라 **D2·D3·D4** 를 같이 덮어야 한다.

---

## 6. 독립 반증 — "안 되는 경우"를 찾았나

| 시도한 반증 | 결과 |
|---|---|
| "`data_reset` 감사가 없으니 안 지워졌다" (병렬이슈24 §3-1) | **반증됨** — 커밋 뒤 감사 기록도 같은 ct 라 커밋 직후 잘리면 감사 없이 지워진다 (D1-a). 03:43 감시 판독만이 근거로 남는다 |
| "사고는 덮어쓰기·초기화 두 화면뿐" | **반증됨** — 복원(D4)은 같은 구조에 트랜잭션도 없어 **더 나쁘다**. 백업(D3)도 반쪽 파일·'running' 영구 |
| "127 직결은 안 잘린다" | 부분 반증 — 10분 화면 제한이 남는다. 1년치 이관 동기 경로(D7) 는 10분도 넘는다 |
| "가져오기는 백그라운드라 안전" | 부분 반증 — 병합 `/start`·`continue` 만 요청 밖이다. 덮어쓰기 앞 절반·미리보기·대사·동기 이관은 요청 안이다 |
| "실패 기록은 catch 가 남긴다" | **반증됨** — D3 `:222·230` · D4 `:323-325` 의 실패 기록도 취소된 ct 를 받아 **실패 기록 자체가 안 남는다** |
| "5234 에서 한 요청이 늦어도 다른 화면은 된다" | **반증됨** — 프록시가 한 번에 한 요청만 처리한다 (§1) |

---

## 7. 적발 요약

| # | 등급 | 내용 | 근거 |
|---|---|---|---|
| 1 | P0 | 복원은 요청 안에서 덤프+가져넣기를 하고 트랜잭션이 없다 → 잘리면 **표끼리 시점이 다른 DB** · 화면은 "실패"로 단정 | `BackupService.cs:292-328·373-397` · `BackupPage.razor:234` |
| 2 | P0 | 덮어쓰기·초기화는 커밋 뒤 감사 기록까지 ct 를 따른다 → **지워짐·뼈대 없음·기록 없음** 조합이 가능 | `DataResetService.cs:157·169-175` |
| 3 | P1 | 실패 기록 UPDATE·롤백이 이미 취소된 ct 를 받는다 → 백업/복원 이력 'running' 영구 · `data_reset_failed` 누락 | `BackupService.cs:222·230·323` · `DataResetService.cs:161·163` |
| 4 | P1 | 5234 프록시는 한 번에 한 요청 → 긴 요청이 화면 전체를 멈춘다 | `web-server.ps1:63-64` |
| 5 | P1 | 일괄확정: 일부 확정됐는데 화면은 전 건 실패 | `SalesController.cs:178-191` · `DeliveryService.cs:225-237` |
| 6 | P1 | 급여명세 메일: 보냈는데 이력 없음 → 중복 발송 가능 | `PayslipMailService.cs:98-130·426` |
| 7 | P2 | 메일·팩스: 보냈는데 실패/무기록 | `EmailService.cs:199-225` · `FaxService.cs:206-230` |
| 8 | P2 | 대사 쿼리 180초 허용 > 100초 경로 · 동기 전체 이관 엔드포인트가 열려 있다 | `MdbReconciliationService.cs:70` · `MigrationController.cs:225` |
| 9 | P2 | 비 JSON·5xx 공통 처리 0 → 프록시 원문·"통신 오류" 가 고객 화면에 | `HitPanApiAuthHandler.cs:40-93` · §3 |
| 10 | P2 | 미리보기는 ct 를 받고 안 써 잘려도 서버가 끝까지 돈다 | `MdbMigrationService.cs:186-230` |
| 11 | — | 게이트 0 | §5 |

---

## 8. 미측정 (다음 확인 대상)

1. Cloudflare 터널 경로의 응답 제한 값과, 524 뒤 오리진 연결을 닫아 `ct` 가 울리는지.
2. `Process.Dispose` 가 동기 모드로 꺼낸 표준출력·입력 파이프를 닫는지 (.NET 런타임 소스 대조) — 고아 mysqldump·mysql 이 남는 원리 확정.
3. 복원 가져넣기 중 잘렸을 때 mysql.exe 가 잠금을 쥐고 남는지 — **테스트 환경에서만** 재현 (#39).
4. `ConfirmDeliveryAsync` 한 건 도중 취소 시 원자성 · 재확정 중복 가드.
5. `MigrateAsync`(동기 이관) 표 단위 커밋 여부.
6. `PayrollPage` 가 `null` 을 받았을 때의 문구.
7. §2 ⬜ 외부 호출 5종의 반쪽 상태.

---
**기록**: 검증팀장 데이비드 박 (hp-verifier) · 2026-09-11 / **상태**: 판정 반려(전제) — 설계 보완 입력 · 폴더 INDEX 미기재(투입 카드 지시)
