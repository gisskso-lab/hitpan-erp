# 검증리뷰서 — ② 결재관리 필터 (로컬 실측 · [3-V] 동시검증)

| 항목 | 값 |
|---|---|
| 작성 | 검증팀장 데이비드 박 · 2026-08-24 |
| 대상 | [20260824작1](../../운영기록/20260824작1_결재관리_필터일원화_작업지시서.md) |
| 실측 환경 | 🔴 **로컬 `hitpan_e2e`** (터널 실측은 **미완** — 아래 §4) |
| 판정 | 🟡 **조건부** — 로직 PASS · **배포 실측 대기** |

---

## 0. 🔴 먼저 밝힌다 — 무엇을 못 쟀나

| 잰 것 | 못 잰 것 |
|---|---|
| ✅ SQL 로직 (반려 분리·격리·대표) | 🔴 **터널 종단** — 배포가 사장님 결재 사항(#29) |
| ✅ 빌드 0/0 | 🔴 **화면** — 콤보박스 항목 수·글자 |
| ✅ 컬럼 실재 (DESCRIBE) | 🔴 **계정 2개 격리** — 대표/일반 각각 로그인 |

🔴 **개발PC 정상은 검증이 아니다.** 이 문서는 **로직이 옳다**까지만 말한다.
**"갔나"** 는 배포 후 `measure-e2-20260824.js` 로 잰다.

---

## 1. 🔴 게이트를 먼저 실패시켰다

**초록불이 어디서 오는지 밝히는 것**이 규율이다. 봉합 전 상태를 먼저 재현했다.

### 옛 SQL (봉합 전) — 부장이 본 「완료된 결재」

```
A-APPROVED   휴가 신청(승인됨)      approved    ← 진짜 완료
A-INFLIGHT   일일보고서(진행중)     pending     🔴 아직 도는 중인데 완료
A-REJ-1ST    초과근무(1차 반려)     rejected    🔴 반려인데 완료
A-REJECTED   경비 신청(반려됨)      rejected    🔴 반려인데 완료
```

⇒ **4건 중 3건이 거짓말이었다.** 결함을 **눈으로 보고** 봉합했다.

### 새 SQL (봉합 후)

```
[completed]  A-APPROVED                          ← 1건. approved 만
[rejected]   A-REJ-1ST · A-REJECTED              ← 2건. 제 자리로 갔다
```

🔴 **초록불의 출처 = `ad.status` 조건.** 화면 글자가 아니라 SQL 조건이 걸렀다.

---

## 2. 실측 결과

| # | 재는 것 | 결과 | 근거 |
|---|---|---|---|
| V-2 | ERP 7종 제외 | 🟢 **PASS** | `A-ERP`(delivery)가 양쪽 목록에서 빠짐 |
| V-3 | 🔴 반려 분리 | 🟢 **PASS** | 완료 4건 → 1건. §1 대조 |
| V-4 | 🔴 1차 반려를 신청자가 봄 | 🟢 **PASS** | 이력 없는 `A-REJ-1ST` 가 신청자에게 뜸 |
| V-5 | 필터2 교차 | 🟢 **PASS** | 5건 → `leave` 1건 |
| V-8 | 🔴 일반 직원 격리 | 🟢 **PASS** | 사원 목록에 `A-OTHERS` **없음** |
| V-9 | 🔴 대표는 전체를 봄 | 🟢 **PASS** | 대표 목록에만 `A-OTHERS` 있음 |
| V-1·V-6·V-7 | 화면·옛주소·배지 | ⚪ **미측정** | 배포 후 |

### 🔴 대조실험 — 조건이 실제로 거르나

| 실험 | 결과 |
|---|---|
| 결재선 **0개**일 때 `pending` | **0건** |
| 결재선 **2개** 넣은 뒤 `pending` | **정확히 그 2건** |

⇒ 조건을 무시했다면 `pending` 문서 3건이 다 나왔어야 한다.
**초록불이 `approval_doc_lines` 에서 왔음이 확인됐다.**

| 실험 | 결과 |
|---|---|
| 사원 `all` | 4건 — 남의 휴직 **없음** |
| 부장 `all` | 5건 — 남의 휴직 **있음**(결재자니까) |
| 대표 `all` | 5건 — 전부 |

⇒ 세 갈래가 **서로 다른 결과**를 냈다. 조건이 살아 있다는 증거다.

---

## 3. 🔴 [3-V] 가 적발한 것

**P0 1건** — [병렬이슈 8](../병렬이슈/20260824_병렬이슈8_대표판정이_없는컬럼을_봤다.md)

대표 판정 쿼리가 `e.emp_id` 를 봤다. 실제 컬럼은 `employee_id` 다.
**빌드 0/0 을 통과했는데 실행하면 500** 이었다 — SQL 은 문자열이라 컴파일러가 안 본다.

⚠️ 「모든 결재」를 고를 때만 타는 코드라 **다섯 중 하나만 죽는** 형태였다.
✅ 같은 세션에서 봉합. 재빌드 0/0.

---

## 4. 🔴 남은 관문 — 이게 끝나야 완료다

```
⬜ 배포 (사장님 결재 — 헌법 #29)
⬜ measure-e2-20260824.js 실측  ← V-1·V-5b·V-6·V-7·V-1f
⬜ 화면 눈으로 확인 (콤보 5개 · 11줄)
⬜ 계정 2개로 격리 실측 (대표 / 일반)
```

🔴 **위 4개 전 [5] 결재 상신 금지.** G1 때 실측 없이 올려 사장님께 물리셨다.
