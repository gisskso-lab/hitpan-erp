# 작업리뷰서 2026-09-11 — 백업 비밀번호 결함 핫픽스 (1.3.40) · **완성 ([4]+[3-V])**

| 항목 | 내용 |
|---|---|
| 작업지시서(PRD) | `docs/운영기록/20260911작5_백업비번결함_핫픽스_작업지시서.md` (§3·§6·§7·§9·§11-1·§11-2) |
| 원인 | `docs/검증/선행/20260911_원인분석_백업100초초과_C.md` · 선행검증서 `docs/검증/선행/20260911_선행검증서_백업비번결함_격리재현.md` |
| 개발명세서 | worktree `docs/개발/erp/20260911_개발명세서_백업비번결함_핫픽스.md` (커밋 `ccd9b4ec`) |
| 구현자 | `hp-implementer` 갈래 1 · worktree `.claude/worktrees/agent-a56c5bc94e5b218b7` · 브랜치 `fix/20260911-backup-credential` · 게이트 `f747cda3` → 코드 `ccd9b4ec` · 기준 `origin/main 2bee68f0` |
| **검증자** | `hp-verifier` — [4] 검증팀장 데이비드 박 + [3-V] 보안상무 안철수 · 구현 문맥과 분리(SoD) |
| **판정** | ⚠️ **조건부 통과** — 핵심 결함(빈 비번 `-p` 멈춤)은 봉합 확인. 게시 전 조건 4건(§8) |
| **재확인 판정** (§9 · 커밋 `9d389cc7` · 2026-09-11) | ⚠️ **조건부 통과** — C-12·C-12 정정·C-16 봉합 확인 · 검증자 손 무력화 2건 빨간불 재현 · P0·P1 0 · 코드 반려 사유 0. **게시 전 남는 것 2건**: CI 로그 원문(C-14) · CI 등록 근거 주석 보강(푸시 전) · 새 적발 병렬이슈31(P2 · 게시 차단 아님) |

> **첫 판(초안) 시점 상태**: 구현 커밋 0 · 반증할 대상 없음 → 준비만 했다. 아래는 커밋 `ccd9b4ec` 대상 검증 결과.
> 표기: 🟢 실측(이 검증자가 직접) · 🟡 코드 읽기 판정 · ⬜ 미측정 · 📄 개발 기록(받아쓰기 아님 — 참고로만)

---

## 1. PRD 대비 검증 (지시대로 만들었나)

| PRD 요구 (§3) | 확인 | 판정 |
|---|---|---|
| ① 자격증명을 `Application.Common.TenantConfigReader` 에서 (C-5) | `ResolveDbCredentialsFromConfig` `:584` — `TenantConfigReader.Get("DB_…")` 5키 · 덤프 경로에 `_db.ConnectionString` 읽기 0 · `using HitPan.Infrastructure` 0. 옛 `ResolveDbCredentials`(`:489`) 는 복원 가져넣기 전용으로 남음(C-11 로 도달 불가) | 🟢 코드 · 🟢 G-BK1 |
| ① 빈 비번·사용자·DB 이름 → 프로세스 시작 **전** 실패 · 문구에 값 없음 | `:587-594` 검사 뒤 `:406` 실행파일·`:409` 실행 · 문구는 고정 문자열 | 🟢 G-BK2 (79ms) |
| ① 설정 DB 이름 ≠ 연결 DB 이름 → 즉시 실패 | `:602-604` · 대소문자 무시 비교(Windows 는 맞음 · 리눅스 대소문자 구분 DB 에선 오판 여지 — P3 기록) | 🟡 코드 · ⓑ 실측 ⬜ |
| ② `-p` 폐지 · `MYSQL_PWD` 는 자식 환경에만 | 덤프 인자 `:408` 에 `-p` 0 · `psi.Environment["MYSQL_PWD"]` `:435` 한 곳 · `SetEnvironmentVariable` 0 · 옛 `"-p{pass}"` 는 `:416` 복원 전용(도달 불가) | 🟢 코드 |
| ② 표준입력 연결 후 즉시 닫기 | `:428` · `:440` | 🟢 코드 · 🟢 G-BK1 |
| ② 취소·예외 시 자식까지 종료 · 짧은 대기 | `finally → StopProcessTree` `:459` · `Kill(entireProcessTree:true)` + `WaitForExit(5000)` · 예외 3종 LogWarning | 🟢 G-BK3 · conhost ⬜ |
| ③ 기본 폴더: 빈 값·상대·Windows 하위 → `%ProgramData%\HitPan\Backup` | `ResolveBackupFolderCore` `:673` | 🟢 G-BK4 |
| ③ 저장된 `primary_path` 교정 + 설정 행 저장 (K3) · 로그 1줄 | `GetSettingsAsync` `:73-83` — `RunBackupAsync`·`RestoreAsync` 둘 다 이 함수를 거친다 · 미러 경로는 무접촉(§6 미완) | 🟡 코드 · DB 교정 UPDATE 실측 ⬜ |
| ③ 실행파일: MariaDB 11.4 폴더 → PATH `mariadb-dump` 이름만 · `where` 제거 · 경로 로그 | `ResolveDumpBinary` `:614` · `FindOnPath` `:647` · 로그 `:407` | 🟢 G-BK5 · 🟢 ⓖ |
| ④ 실패 UPDATE `None` · 재오픈 · 기록 실패도 원래 결과 · 취소 이유 구분 | `:251-289` · `EnsureOpenForRecordAsync` `:832` | 🟢 G-BK3 |
| 소유표 밖 변경 0 | `git diff --stat 2bee68f0..ccd9b4ec` = **4파일**(BackupService·게이트·CI·개발명세서) · `BackupController`·`DataResetService`·`IBackupService`·Web diff **0줄** | 🟢 |

## 2. 3관문

| 관문 | 결과 | 근거 (실제 출력) |
|---|---|---|
| 빌드 errors 0 + warnings 0 (#19) — worktree `src/HitPan.sln` | ✅ | `BUILD exit=0 errors=0 warnings=0 project=…\src\HitPan.sln` (build-brief) |
| ddl-smoke — 해당 없음 | ✅ | diff 에 `.sql` 0 |
| 게이트 `BackupCredentialGate` — **이 검증자의 격리 인스턴스** | ✅ | `Total tests: 7 · Passed: 7` · Skipped/`[SKIP]` **0** · `HITPAN_REQUIRE_DB=true` (G-BK1 3s · G-BK2 79ms · G-BK3 4s · 대조군 2s · G-BK4 41ms · G-BK5 12ms · G-BK6 69ms) |
| CI DB 게이트 잡 | ⬜ | **푸시 전이라 CI 로그 없음** → 게시 전 조건 ① |
| 독립 반증 | ⚠️ | §3 — 동작 4행 🟢 · ⓐⓑⓒ 격리 실측 ⬜(코드 판정만) |

### 2-1. 격리 환경 (C-7 · 선행검증서 §1 절차)

| 항목 | 값 |
|---|---|
| 서버 | 설치된 `mariadbd.exe` 11.4.10 일반 프로세스(PID 28732) · `--no-defaults` · 서비스 등록 0 |
| datadir · 포트 | `C:\HitPanBuild\iso_20260911_5v\data`(`mariadb-install-db` 새로) · **33951** · bind 127.0.0.1 |
| 스키마 | worktree 출하 DDL → `hitpan_bkgate` **152표** |
| 계정 | `bkgate`@127.0.0.1 · 이 검증자가 새로 만든 무작위 22자 비번(`!` 포함) · 파일로만 전달 · 출력 0 |
| 게이트 PATH | 이 PC 사용자 PATH 그대로 — `mysqldump` = `MySQL Server 8.4` 가 먼저 잡힘 |
| 정리 | `mariadb-admin shutdown` → PID 28732 없음 · 포트 33951 LISTEN 0 · 격리 폴더(datadir·비번 파일·로그) 삭제 · 게이트 임시 폴더 0 · 대조 실험이 남긴 입력 대기 덤프 1개(PID 12040 — 이름·생성시각·포트 33951 인자 3중 확인)만 종료 · 운영 고아 21540·36284 무접촉 |

## 3. 독립 반증

### 3-1. 원인·무력화 대조 — 실제 클라이언트로 직접 (코드 되돌림 대신 동작 대조 · 코드 수정 금지)

| # | 시도 (격리 33951 · 표준입력 연결 후 즉시 닫음 · 제한시간 있음) | 결과 | 뜻 |
|---|---|---|---|
| R1 | `mariadb-dump` + `MYSQL_PWD` 맞음 (수정 후 방식) | exit 0 · 1.9s · 256,311자 · `CREATE TABLE` | 봉합 방식이 실제로 끝난다 🟢 |
| R2 | `mariadb-dump` + `MYSQL_PWD` 빈 값 · `-p` 없음 | exit 2 · **0.1s** · 1045 | 빈 비번이 와도 **멈추지 않고** 즉시 실패 🟢 |
| R3 | `mariadb-dump` + 옛 `-p` 빈 값 (수정 전 인자 모양 = 「빈 비번 차단 제거 + `-p` 복귀」 무력화) | **10s 멈춤 → 강제 종료** | 원래 결함을 이 검증자가 따로 재현 🟢 — 봉합을 빼면 멈춘다 |
| R4 | PATH 의 Oracle `mysqldump` 8.4 + `MYSQL_PWD` 맞음 (실행파일 순서 되돌림 무력화) | exit 2 · 0.3s · `COLUMN_STATISTICS (1109)` | PATH `mysqldump` 를 고르면 덤프 실패 🟢 — G-BK5 의 존재 이유 |

- R1·R2 stderr 첫 줄은 둘 다 `ssl-verify-server-cert is disabled … passwordless login` — **성공에도 실패에도 같은 경고** → 판정을 exit 코드로만 해야 한다(C28-1)는 근거 🟢.
- **남는 것**: 게이트 코드 되돌림 무력화 5행(개발명세서 §4-5 A~E)은 코드 수정 금지라 **이 검증자가 다시 돌리지 않았다** 📄. 위 R3·R4 는 무력화 대상 동작이 실제로 게이트가 잡는 모양(멈춤·실패)으로 나타남을 확인한 것이지, 게이트가 빨간불을 켜는지를 다시 잰 것은 아니다.

### 3-2. ⓐ~ⓘ

| # | 시도 | 결과 | 판정 |
|---|---|---|---|
| ⓐ | db.conf 에만 비번 · 환경변수 없음 | 코드: 덤프·API 연결 모두 같은 `TenantConfigReader`(db.conf 캐시 → 환경변수 폴백) · 두 사본 줄 단위 동일(PM §11-1) · 게이트는 환경변수 경로만 잰다 | 🟡 코드 · 별도 프로세스 실측 ⬜ |
| ⓑ | 설정 DB 이름 ≠ 연결 DB 이름 | `:602-604` 가 `RunMysqldumpAsync` 맨 앞(실행파일·프로세스 전) · 이력은 `RunBackupAsync` catch 가 연결 DB 에 `failed` · 대소문자만 다르면 통과(Windows 정상) | 🟡 코드 · 실측 ⬜ |
| ⓒ | 덤프 정상 종료 직후 취소 20회 | C-9 `AND status='running'` `:266` + 보관정책을 성공 기록 뒤로(`:220`) → 병렬이슈27 W1(success→failed 덮기)·W2(먼저 삭제) 코드상 닫힘. 남는 모양: 덤프 종료~성공 UPDATE 사이 취소면 「파일 완전 + `failed`(끊겨)」 — 요청이 끊긴 쪽 기록이라 수용, `last_*` UPDATE 만 실패하면 반환 false·이력 success(개발명세서 §5) | 🟡 코드 · 20회 실측 ⬜ |
| ⓓ | 옵션 파일 `password` 가 `MYSQL_PWD` 를 이기나 | 이 PC 클라이언트 전역 옵션 파일(`MariaDB 11.4\my.ini·my.cnf` · `C:\Windows\my.ini` · `C:\my.ini·my.cnf`) **0개** 🟢 → R1·게이트 성공과 정합 · 고객 PC MSI | 🟢 이 PC · 고객 PC ⬜ |
| ⓔ | 비번이 출력·기록으로 새나 | 게이트 `CheckNoSecretLeak`(로그·반환 이유·이력) G-BK1~3 PASS 🟢 · 문구는 고정 문자열·stderr(1045 는 사용자명·호스트만) · 로그 인자에 `psi`·`Arguments`·`Environment` 0 🟢 코드 · `last_error`·덤프 파일 표식 계수 ⬜ | 🟢 부분 |
| ⓕ | 부모 환경 오염 | `SetEnvironmentVariable` 0 · `psi.Environment` 만 | 🟢 코드 |
| ⓖ | 실제 PATH 모양 (MySQL 8.4 먼저) | 게이트 G-BK1 이 **이 PC 실제 PATH** 로 실행파일 로그가 MariaDB 쪽·`MySQL Server` 아님을 단언하고 PASS · R4 로 8.4 를 고르면 실패함 확인 | 🟢 |
| ⓗ | K3 경계값 | G-BK4 주입 PASS 🟢 · `D:\백업`·`\\nas\backup`(절대·Windows 밖 → 무변경) · `C:\Windows\..\Users\x`(`GetFullPath` 정규화 → 무변경) · 대소문자 System32(`OrdinalIgnoreCase` → 교정) 🟡 · `\\?\C:\Windows\…` 는 정규화 안 돼 Windows 하위로 안 잡힘(P3 · 화면에서 부모계정이 직접 입력해야만) | 🟢/🟡 |
| ⓘ | 자식까지 종료 · 무관 프로세스 무접촉 | 게이트 `KillNewDumps` = 자손 ∩ 시험 뒤 생성 ∩ 이름(`:704-718`) — 이름 전체 종료 0 🟢 · G-BK3 덤프 종료 PASS 🟢 · conhost 소멸 직접 확인 ⬜ | 🟢/⬜ |

### 3-3. §11-2 조건 동작

| 조건 | 결과 | 판정 |
|---|---|---|
| **C-8** 기본 폴더 제한 생성·선점 거부 | G-BK4 C-8 ⓐ(상속 끊김·셋만 모든 권한)·ⓑ(Users 쓰기 폴더 거부)·ⓒ(대조 통과) PASS 🟢. **그러나 선점 검사는 디렉터리 교차점(junction)으로 우회된다** — 존재·소유자·ACL 을 경로로 읽으면 연결 대상의 것이 읽힌다(`:760·778`, 재분석 지점 검사 0). `%ProgramData%\HitPan` 은 Users 하위 폴더 생성 가능(병렬이슈25 A1) → 일반 사용자가 `Backup` 을 교차점으로 먼저 만들어 SYSTEM 소유·Users 읽기 폴더(예: `C:\ProgramData\Microsoft\Windows\Templates` — 소유자 SYSTEM · SYSTEM/Administrators 모든 권한 · Users·Everyone 읽기만 🟢 ACL 조회)를 가리키면 검사 통과 → 덤프가 그 폴더 권한을 상속해 **누구나 읽는다** | 🔴 **병렬이슈29** (🟡 코드+Windows 동작 · 교차점 생성 실측은 안 함) |
| **C-9** 실패 UPDATE 상태 조건 · 보관정책 순서 | `:266` · `:220-235`(정리 실패는 LogWarning, 성공 유지) · `marked>0` 일 때만 `last_*` | 🟢 코드 |
| **C-10** C28-1~3 | C28-1 exit 코드로만 `:452` · 성공 시 stderr 미기록 · 실패 문구에서 경고 줄 제거 `:826` 🟢 · C28-2 📄 개발 실측(명시해도 꺼짐) + R1·R2 경고 재확인 🟢 · C28-3 개발명세서 §5 한 줄 🟢 | 🟢 |
| **C-11** 복원 맨 앞 차단 | `:300` 이 파일 조회·회사명·이력 INSERT·사전 백업·가져넣기 **모두보다 먼저** 🟢 코드 · G-BK6 PASS(프로세스 0 · `restore_history` 0 · 사전 백업 파일 0 · 안내 문구) 🟢 · 문구 화면 도달: `BackupController` `Ok(result)` → Web `BackupService.RestoreAsync` → `BackupPage.razor:234` `복원 실패: {resp.Error}` (Web 무수정) 🟡 코드 | 🟢 |

## 4. 코드리뷰 발견

| # | 등급 | 위치 | 내용 | 처리 |
|---|---|---|---|---|
| 1 | P1 | C-8 `EnsureRestrictedBackupFolderWindows` `:749` | 교차점 우회 → 선점 방어 무력 · **이번 핫픽스 전에는 덤프가 0바이트라 노출 자체가 없었다 → 새로 생기는 노출** | 🔴 병렬이슈29 · 게시 전 PM 결재 |
| 2 | P2 | §8 대본 순서 4 「탐색기 속성」 | SYSTEM 이 만든 제한 폴더는 비관리자 권한 탐색기에서 접근 거부 → 「계속」 을 누르면 사용자 계정 권한 줄이 **폴더에 영구 추가**된다(Windows 동작). 그 줄이 쓰기 성격이면 다음 백업이 C-8 검사(`:788`)로 **거부**된다 ⬜ 미측정 | 대본을 「화면 이력의 크기」 로 바꾸고 탐색기 진입 금지 한 줄 — 게시 전 조건 ③ (문서만) |
| 3 | P2 UX | `BackupPage.razor:224-234` · `RestoreConfirmDialog.razor` | 복원 버튼 → 회사명 입력 확인창 → 「복원 진행 중… 화면을 닫지 마세요」 → 빨간 「복원 실패: 복원은 안전장치를…」. 막힌 기능에 확인 의식을 다 치르고 「실패」로 끝남 — 처음 보는 사람은 **고장·자료 손상으로 오해**할 수 있다. 자료는 안전(맨 앞 차단) · 수정 전에도 복원은 멈춤으로 안 됐다(후퇴 아님) | 차단 아님 → **다음 과제**(버튼 숨김·사전 안내 · Web 소유표 밖) |
| 4 | P3 | `:701-703` | 경로 해석 불가 `catch` 가 로그 없이 `false` (정적 함수 · 빈 catch 는 아님 — #15 성격 기록) | 기록 |
| 5 | P3 | `:603` | DB 이름 대소문자 무시 — 리눅스 대소문자 구분 DB 에서 다른 DB 통과 여지 (고객은 Windows) | 기록 |
| 6 | P3 | `:708-710` | `\\?\` 접두 경로는 Windows 하위 판정을 비껴간다 (부모계정이 화면에 직접 입력해야만) | 기록 |
| 7 | P2 기존 | `:565` | `catch (IOException ex) { _ = ex; }` 로그 없음 — 보관정책 넘김 범위 | 넘김 |
| 8 | 기록 | `:541` | 옛 `Console.Error` — 복원 전용 옛 함수에만 남음(새 코드에 같은 모양 0) | — |
| 9 | 기록 | #12 | `IBackupService` 구현체 재grep: `Application/Services/BackupService.cs:22` 1개 · 등록 `Program.cs:132` · 시그니처 무변경 | 🟢 |
| 10 | 기록 | 호출처 3곳 | `BackupController.cs:61`(RunBackup)·`:69`(Restore) · `DataResetService.cs:96`(실패 시 중단 흐름 무수정) · `MigrationController` → `_dataReset` 간접 — 셋 다 diff 0줄 | 🟢 |

## 5. 거짓봉합 점검

- [x] 수정 전 FAIL 사유가 증상 모양과 맞나 — 📄 개발 §4-3 은 Windows 격리(60초 멈춤·0B·`mysqldump` 생존·`running`)로 기록 · 이 검증자 R3 이 같은 모양(멈춤) 독립 재현 🟢
- [~] 무력화 5행 실제 빨간불 — 📄 개발 기록만 · 이 검증자는 동작 대조 R3·R4 (§3-1) · 게이트 되돌림 재실행 ⬜
- [x] DB 없을 때 조용히 끝나지 않나 — `ServerAvailable()` 로컬은 포트 명시 필수(3306 기본 접속 금지) · `SkipOrFail` + `HITPAN_REQUIRE_DB` · CI `GATES` 1줄 + 등록 근거 주석 🟢 코드
- [x] 운영 모양(`User=;Password=`)으로 **연 뒤** 잰다 — `OpenLikeProduction` `:607` · `AssertNoDbConfShadow` 🟢
- [x] 개발명세서 「안 한 것」 — 워치독 `-p`(#43) · 복원 경로 · 요청 밖 전환 · 리눅스 CI 한계 · C28-3 전부 있음 🟢

## 6. [3-V] 병렬이슈

| 이슈 문서 | 봉합 확인 |
|---|---|
| `docs/검증/병렬이슈/20260911_병렬이슈25_새_백업폴더는_로컬사용자_누구나_읽고_먼저_만들수있다.md` | 🟡 **부분 봉합** — B1(읽기)·B3(선점) 은 C-8 로 막힘(G-BK4 🟢) · 교차점 우회는 병렬이슈29 로 분리 |
| `docs/검증/병렬이슈/20260911_병렬이슈26_비번을_고치면_복원이_처음으로_돌고_요청이_끊기면_DB가_반쪽이된다.md` | 🟢 **봉합** — C-11 (가) 맨 앞 차단 · G-BK6 격리 재실행 PASS · 화면 안내 순서는 다음 과제 |
| `docs/검증/병렬이슈/20260911_병렬이슈27_실패기록을_취소없이_쓰면_끝난_백업이_failed로_덮인다.md` | 🟢 **봉합(코드)** — C-9 · ⓒ 20회 실측 ⬜ |
| `docs/검증/병렬이슈/20260911_병렬이슈28_MYSQL_PWD로만_넘기면_서버인증서_검증이_꺼진다_판정.md` | 🟢 **조건 충족** — C28-1 코드 · C28-2 개발 실측+R1·R2 경고 재확인 · C28-3 기록 |
| `docs/검증/병렬이슈/20260911_병렬이슈29_기본백업폴더_선점검사는_디렉터리교차점으로_우회된다.md` | 🔴 **신규 · 미봉합** — PM 결재 대기 |

## 7. 개발 미완 3건 — 게시 차단인가

| 미완 | 판정 | 이유 |
|---|---|---|
| 미러 경로(`mirror_path`) 교정·권한 | **차단 아님** | 기본 NULL · 부모계정이 화면에서 고르는 값 · 상대 경로여도 1차 백업은 성공(후퇴 아님) · 지시서 범위 1차 폴더 |
| C-8 부모 폴더 권한 잔여 | **차단 아님 — 단 같은 뿌리의 교차점 우회(병렬이슈29)는 게시 전 결재** | 부모 `HitPan` 을 먼저 가져도 새로 만든 `Backup` 은 소유자 검사로 거부됨 · 교차점만 검사를 통과한다 |
| C28-3 원격 DB | **차단 아님** | 설치 형태가 루프백뿐 · API 본 연결도 무검증(새 노출 0) |

## 8. 판정 근거

- ✅ 확인된 것: 빌드 0/0 · 소유표 4파일 · 격리 게이트 7/7 · 원인 독립 재현(R3 멈춤) · 봉합 방식 동작(R1·R2) · C-9·C-10·C-11 · 호출처·인터페이스 무변경 · P0 0
- ⚠️ **조건부 — 게시(1.3.40) 전 남는 것**
  1. **CI DB 게이트 잡 로그**: 푸시 뒤 `BackupCredentialGate` 7건 PASS · `[SKIP]` 0 원문 확인
  2. **병렬이슈29 PM 결재**: (A) 재분석 지점 거부 한 자리 추가 + G-BK4 단언 1줄 뒤 게시 — **추천** · (B) 알고 게시(릴리스 기록·대본에 명시)
  3. **§8 대본 순서 4 정정**(문서): 파일 크기는 화면 이력으로 · 탐색기로 백업 폴더 진입 금지(발견 2)
  4. **ⓐⓑⓒ 격리 실측** 3건(§9 [4] 「독립 반증 3건 이상」) — 이번에는 코드 판정만 했다 · PM 이 코드 판정으로 갈음할지 결재
- 다음 과제(차단 아님): 복원 안내 순서 UX(발견 3) · P3 3건(4·5·6) · conhost 소멸 직접 확인
- ❌ 반려 사유: 없음

---

## 9. 재확인 — 추가 커밋 `9d389cc7` (C-12 · C-12 정정 · C-16) · 2026-09-11

| 항목 | 내용 |
|---|---|
| 기준 | 작업지시서 §11-3 **C-12** · **C-12 정정** · **C-16** 행 |
| 대상 | `git diff ccd9b4ec..9d389cc7` = 3파일 (`BackupService.cs` · `BackupCredentialGateTests.cs` · 개발명세서 §8) · CI 워크플로·Web·DDL·마이그 0 |
| 검증자 문맥 | 구현 worktree 와 분리 — 검증자 전용 `C:\HitPanBuild\wt-bk-r2` (detached `9d389cc7`) · 개발명세서 §8-4 수치는 받아쓰지 않고 다시 쟀다 |

### 9-1. 3관문

| 관문 | 결과 | 근거 |
|---|---|---|
| 빌드 (#19) | ✅ | Tests · API `errors=0 warnings=0` · 무력화 복구·탐침 삭제 뒤 최종 Tests 빌드도 0/0 |
| 게이트 — 검증자 격리 인스턴스 | ✅ | `mariadbd` 11.4.10 일반 프로세스 · 포트 **33961** · 새 datadir · 출하 DDL **152표** · `HITPAN_REQUIRE_DB=true` · **8/8 PASS × 2회**(착수 · 복구 뒤 최종) · Skipped · `[SKIP]` **0** — 최종: G-BK1 4s · G-BK2 70ms · G-BK3 3s · 대조군 3s · G-BK4 346ms · G-BK5 9ms · G-BK6 70ms · G-BK7 7s |
| ddl-smoke | 해당 없음 | `.sql` 0 |

### 9-2. 무력화 — 검증자 손 (검증자 worktree 안에서만 · 커밋 0)

| 회차 | 되돌린 것 | 결과 | 원문 요지 |
|---|---|---|---|
| A | `ThrowIfReparsePointOnPathWindows` 첫 줄 `return` (재분석 지점 검사 제거) | 🟢 **G-BK4 만 빨간불** · 나머지 7 초록 | `C-12 ⓕ Backup 이 교차점인데 받아들였다` · `C-12 ⓖ 상위 HitPan 이 교차점인데 받아들였다` |
| B | `ApplyRetention` 의 `.Where(f => f.Length > 0)` 제거 (보관 0B 제외 제거) | 🟢 **G-BK7 만 빨간불** · 나머지 7 초록 | `C-16 ② 지워지면 안 되는 이전 성공 백업이 지워졌다: …000002.sql · …000003.sql` · `옛 0바이트 파일을 보관정책이 지웠다: …20000102_000001.sql` |
| 🔴 함정 적발 | A 첫 실행은 빌드 스크립트가 경로 인코딩으로 **빌드 실패**했는데 게이트는 옛 DLL 로 **8/8 초록** — DLL 이 소스보다 새것인지 검사(`False`)로 무효 판정 · 인라인 빌드로 재실행 | 유효 회차 모두 DLL > 소스 확인 | 구현자가 겪은 것과 같은 함정 — 「초록」 만 보면 무력화 실패를 통과로 적는다 |
| 복구 확인 | B 적용 시 `git diff` = 1줄 교체만(= A 되돌림 정확) → B 되돌림 뒤 `BackupService.cs` diff 0 | 🟢 | |

### 9-3. 반증 (지시 3)

| # | 질문 | 판정 | 근거 |
|---|---|---|---|
| R1 | 허용 목록 — 개별 사용자 vs 그룹 · 판정 실패 시 거부 | 🟢 | 검증자 탐침(실제 `IsIndividualUserAccountWindows` 호출): 실행 계정 · 기본 Administrator(500) · Guest(501) = 개별 / Everyone · Users · Authenticated Users · LOCAL SERVICE · NETWORK SERVICE · CREATOR OWNER · NT SERVICE\TrustedInstaller = 거부 / **삭제된 로컬 계정 SID · 풀리지 않는 도메인 사용자 SID = 거부**(판정 실패 → 거부 실측). ⚠️ P3-a: Guest **계정** 줄은 허용(개별 사용자 — 줄을 붙일 수 있는 건 관리자뿐이라 위협 모델 안) · ⚠️ P3-b: 풀리지 않는 사용자 줄의 거부 문구가 「개별 사용자가 아닌 그룹(모든 사용자·Users 등)」 이라 관리자가 원인을 오해한다 |
| R2 | 실제 위치 확인이 기본 폴더에만 적용되나 | 🟢 | `:469` `OperatingSystem.IsWindows() && IsRestrictedDefaultBackupFolder(...)` — 폴더 검사(`:781`)와 같은 조건 · 사용자가 고른 경로는 무검사(C-8 원칙). ⚠️ P3-c: 같은 기본 폴더를 **다른 표기**(8.3 짧은 이름 · `\\?\` · `\\localhost\C$`)로 저장하면 사용자 경로로 취급돼 C-8·C-12 전체가 빠진다 — 저장은 설정 화면(부모계정 선택)뿐 · 자동 교정은 「못 쓰는 경로」 만 |
| R3 | 새로 만들기만 → 같은 초 두 백업의 두 번째가 사용자에게 무엇으로 보이나 | 🟢 | 검증자 탐침(같은 이름 파일 선배치 후 실제 `RunBackupAsync`): `Success=false` · 문구 「같은 이름의 백업 파일이 이미 있어 덮어쓰지 않았습니다. 잠시 뒤 다시 실행하세요.」(경로·값·개발 용어 0) · 이력 `failed` 같은 문구 · 새 덤프 프로세스 0 · **먼저 있던 파일 내용 그대로** · 추가 파일 0 · 106ms. 코드: 만들기 실패는 C-16 정리 `try` 밖이라 첫 백업 파일을 지우지 않는다. 호출자 = 수동(`BackupController`) · 초기화 전 백업(`DataResetService`) (워치독 `WatchdogBackupRunner.cs` 는 검색에 걸림 · 호출 방식은 범위 밖 ⬜) → 겹침 = 두 번 누름·초기화와 겹침. ⚠️ P3-d: 두 번째는 화면에 실패로 보이고 `last_status` 는 늦게 끝난 쪽 |
| R4 | 도메인 컨트롤러 불통 오거부가 **기본 생성 폴더**에서도 생기나 | 🟢 **안 생긴다** | 탐침: 코드가 만든 기본 폴더 = 보호(상속 끊김) · 줄 3개 = SYSTEM · Administrators · 실행 계정 · 소유자 = 실행 계정 → 모두 허용 셋이라 `continue` 되어 계정 조회(`LookupAccountSid`) 호출 0 · 재검사 통과. 오거부는 **관리자가 줄을 더 붙인 폴더**(탐색기 「계속」 의 도메인 사용자 줄 + DC 불통 · 계정 삭제)에서만 — 풀리지 않는 사용자 줄 = 거부 실측. 실제 DC 불통 ⬜ · 탐색기 「계속」 이 붙이는 줄 종류 ⬜ (개발명세서 §8-6 요청 그대로 남음) |
| R5 | G-BK7 생성시각 정렬이 리눅스 CI 에서 성립하나 | 🟡 성립 (코드 판정) | .NET 리눅스는 태어난 시각을 못 주면 생성시각 = ctime·mtime 중 이른 값 · `File.SetCreationTime` 은 mtime 을 설정(ctime=지금) → 설정한 과거 시각이 곧 생성시각 · 새 성공 백업은 지금 → 순서 = 이전 성공 < 옛 0B < 새 백업. 태어난 시각을 주는 파일시스템이어도 생성 순서(20ms 간격)가 같은 순서 · 0B 는 어차피 제외. 덤프 도구: yml `:119`·`:282` `mariadb-client` 설치 → PATH `mariadb-dump`(비 Windows 는 PATH 만 봄 `:688`) → **G-BK1~3·7 리눅스 실행 가능** · CI 원문 ⬜ |
| R6 | 빈 catch · 비번 출력 | 🟢 | `BackupService.cs` `_ = ex` · 빈 catch 0 (grep) · 새 catch 전부 `LogWarning`(게이트는 stderr 한 줄) · 새 로그는 경로만 · 고객 문구 5개 경로·값 0 · **검증자 로그 파일 전체에서 격리 비번 문자열 0회**(파일 검색 · 값 출력 없이) |

### 9-4. CI 등록 근거 주석 — PM 질의 판정

| 항목 | 내용 |
|---|---|
| 사실 | `.github/workflows/build-and-test.yml` (`9d389cc7` 에서 무수정) `:210-225` 주석 = G-BK1~6 · 무력화 결과는 `2bee68f0`·`ccd9b4ec` 기준뿐 · **G-BK7 · G-BK4 C-12 단언과 그 무력화 결과 없음** · 「리눅스 한계」 줄은 C-8 만 적는다 |
| 규칙 | 작업지시서 §7-3 「CI 주석에 위(무력화) 결과를 적고 `GATES` 1줄」 · 20260904 작20 관례 「등록 근거 = 되돌리면 빨간불이 되는 것을 확인해서」 |
| 판정 | **코드 반려 아님 · 푸시(C-14) 전 보강 필수 = 게시 차단 조건.** 실행은 클래스 이름 필터로 자동 포함되지만, 새 단언 2묶음이 등록 근거 없이 CI 에 오르는 상태다. 근거 원문은 이미 있다(개발명세서 §8-4 · 이 절 9-2) → 주석만 보강(코드·게이트 무변경) · 보강 뒤 CI 원문으로 확인 |
| 보강 내용 제안 | ① 무력화 2행: 재분석 지점 검사 제거 → G-BK4 만(ⓕ·ⓖ) · 보관 0B 제외 제거 → G-BK7 만 ② 리눅스 한계 줄: C-12(권한 줄 · 교차점 · 연 파일 실제 위치)는 Windows 전용 · 리눅스 DB 잡이 새로 재는 것은 G-BK7(C-16) ③ G-BK4 는 DB 가 필요 없어 **Windows 빌드 잡**(`:40` `dotnet test src/HitPan.sln`)에서 C-12 단언까지 돈다 |
| ⚠️ 함께 볼 것 | Windows 빌드 잡(`windows-latest`)이 G-BK4 C-12 ⓔ 를 잰다 — 러너 실행 계정이 **기본 Administrator(RID 500) 자신이면** ⓔ 준비 단계가 위반을 남겨 **빨간불**(「대조가 되지 않는다 — 이 단언은 재지 못했다」). 러너 계정 RID ⬜ 미확인(검색으로 확정 못 함) → C-14 첫 CI 에서 Windows 잡 원문 확인. 이 PC(개발PC) 초록은 근거가 아니다 |

### 9-5. 이전 「게시 전 남는 것」(§8) 상태

| # | 조건 | 상태 |
|---|---|---|
| 1 | CI DB 게이트 잡 로그 | ⬜ **남음** — 이제 `BackupCredentialGate` **8건** PASS · `[SKIP]` 0 + Windows 빌드 잡 G-BK4 PASS 원문 |
| 2 | 병렬이슈29 결재 | 🟢 해소 — 안 A 구현 · 무력화 재현 (병렬이슈29 §5) |
| 3 | §8 대본 순서 4 정정(문서) | ⬜ 이 재확인 범위 밖 — PM 확인 |
| 4 | ⓐⓑⓒ 격리 실측 | 🟢 해소 — C-13 검증보고(ⓐ 설치 모양 · ⓑ 대소문자) · ⓒ 는 지시서 C-13 대로 코드 판정 갈음 |

### 9-6. 병렬이슈

| 이슈 문서 | 상태 |
|---|---|
| `docs/검증/병렬이슈/20260911_병렬이슈29_기본백업폴더_선점검사는_디렉터리교차점으로_우회된다.md` | 🟢 **봉합 확인** (§5 추가) |
| `docs/검증/병렬이슈/20260911_병렬이슈30_실패한_덤프의_0바이트_파일이_보관개수를_먹어_진짜백업이_지워진다.md` | 🟢 **봉합 확인** (§4 추가) |
| `docs/검증/병렬이슈/20260911_병렬이슈31_실제위치확인_뒤에도_경로를_다시써_상위폴더를_가진_사용자가_바꿔칠수있다.md` | 🔴 **신규 P2** — ② 확인 뒤 크기 확인·보관 정리·이력 경로가 경로를 다시 풂 + 상위 `HitPan` 소유자 미검사 · 설치 **전** 선점 조건 · 기밀성 노출 0 · 도달 위험(복원)은 C-11 로 막힘 → **게시 차단 아님** · 복원 재개 전 결재 |

### 9-7. 판정 근거

- ✅ 확인된 것: 빌드 0/0 · 격리 게이트 8/8 × 2 · SKIP 0 · 검증자 손 무력화 2/2 표적 게이트만 빨간불 · 허용 목록·판정 실패 거부·같은 초 충돌·기본 폴더 오거부 없음 탐침 · 빈 catch·비번 0 · P0·P1 0
- ⚠️ **조건부 — 게시(1.3.40) 전 남는 것 2건**
  1. **C-14 CI 원문**: DB 잡 `BackupCredentialGate` 8 PASS · `[SKIP]` 0 · Windows 빌드 잡 G-BK4 PASS(ⓔ RID 500 위험 확인)
  2. **푸시 전 CI 등록 근거 주석 보강**(9-4) — 코드 무변경
- 다음 과제(차단 아님): 병렬이슈31 · P3 4건(a Guest 계정 허용 · b 풀리지 않는 사용자 거부 문구 · c 기본 폴더 다른 표기 · d 같은 초 두 번째 실패 표시)
- ❌ 반려 사유: 없음

### 9-8. 금지 준수 · 정리

- 제품 브랜치·구현자 worktree 쓰기 0 · 커밋·푸시 0 · 3306·5257·5234 접속 0 · 서비스 조작 0 · 기존 덤프 PID 21540·36284 무접촉(목록 조회만) · 실물 `ProgramData` 폴더·권한 변경 0(교차점·권한 줄은 게이트·탐침 임시 폴더 안) · 비밀번호·명령줄 출력 0 · 폴더 INDEX 무수정
- 정리: 검증자 탐침 파일(worktree 임시 · 미커밋) 삭제 → worktree 변경 0 확인 뒤 `git worktree remove` · `mariadb-admin shutdown` → `mariadbd` 20404 없음 · 33961 LISTEN 0 · 격리 폴더 삭제 · 게이트·탐침 임시 폴더 0 · 남은 덤프 프로세스 = 21540·36284 만 · 구현자 worktree HEAD `9d389cc7` (status = 추적 안 되는 `.claude/` 도구 폴더 1건뿐 · 코드 변경 0 · 검증자 쓰기 0) · `git worktree list` 에 `wt-bk-r2` 0

## 10. 재확인2 (TruffleHog·C-17) — 추가 커밋 `6ca80660` · `1e048090` · 2026-09-11

| 항목 | 내용 |
|---|---|
| 기준 | 작업지시서 §11-3 **C-14 적발** · **C-14 CI 원문** · **C-17** 행 |
| 대상 | `bc500406` 위 선형 2커밋 · `fix/20260911-backup-credential` (origin = `1e048090`) · PR #392 |
| 검증자 문맥 | 구현 worktree 는 읽기만 · 검증자 전용 `C:\HitPanBuild\wt-bk-r3` (detached `1e048090`) · 격리 `mariadbd` 포트 33983 |

### 10-1. TruffleHog 봉합

| 잰 것 | 판정 | 원문·근거 |
|---|---|---|
| 추가 줄 모양 | 🟢 0 | `git diff 2bee68f0 1e048090` 추가 줄 `Server=`·`Password=`·`Pwd=` **0** (넓힌 검색에 걸린 1줄 = 로그 틀 `userId=` — 연결 문자열 아님) · `1e048090` 트리의 게이트 파일 0 |
| 제외 목록 | 🟢 변경 0 | `.github/trufflehog-exclude-paths.txt` — 범위 안 diff·log 0 |
| 빌더 연결이 실제 DB 에 붙나 | 🟢 | 격리 G-BK1 **PASS** (`:94-97` 단언 = 파일 0B 면 위반 · `CREATE TABLE` 포함) · **대조: 비번만 틀리게** → G-BK1 Failed `Access denied for user 'hitpan_iso'@'localhost' (using password: YES)` = 빌더 `Password` 속성이 실제로 쓰인다 |
| 🔴 CI pull_request 스캔 | 🔴 **여전히 실패** | push 스캔 run 34604079269 = success · **pull_request 스캔 run 34604084230 = failure** · 원문 `scanning repo … "base": "2bee68f0…", "head": "1e048090…"` → `Found unverified SQLServer result` ×2 · `… with HTML encoding` ×2 · `"unverified_secrets": 4` · `exit code 183` (봉합 전 run 34601851650 과 같은 4건) |
| 원인 | — | PR 스캔은 base..head 의 **모든 커밋**을 읽는다(`trufflehog.yml:53-55` 주석 「pull_request → BASE=PR base / HEAD=PR head」). 문자열을 넣은 첫 게이트 커밋 **`f747cda3` `BackupCredentialGateTests.cs:38`·`:600`·`:610`** 이 PR 범위에 그대로 있다 — 뒤 커밋에서 지워도 역사 속 추가 줄은 계속 잡힌다. push 스캔이 초록인 것은 `bc500406..1e048090` 만 읽었기 때문(= 초록 한쪽만 보면 봉합됐다고 적는다) |
| 판정 | 🔴 | **코드 반려 아님**(트리는 깨끗) · §11-3 사장님 결재 행 「TruffleHog 통과 전에는 머지·게시하지 않는다」 **조건 미충족**. 선택지(PM 결재): ① 브랜치 역사에서 `f747cda3` 의 문자열을 없애 다시 푸시(강제 푸시 = 결재 사항 · 스쿼시·재작성 뒤 SHA 로 체크 재확인) ② 제외 목록 추가 = 그 파일 규칙상 금지 ③ 빨간 채 스쿼시 머지 = 조건 위반. main 규칙 필수 체크 조회 = 행 0 (⚠️ 조회 한계 가능) → 기계가 막지 않으므로 사람 규칙이 유일한 문 |

### 10-2. C-17 코드 읽기 (`1e048090` `BackupCredentialGateTests.cs`)

| 질문 | 판정 | 근거 |
|---|---|---|
| 순서 500 → 501 → 503 | 🟢 | `:454` `ContrastRids = { 500, 501, 503 }` · `:466-471` 첫 사용자 판정 계정 |
| 시험 계정 자신 제외 | 🟢 | `:469` 허용 셋에 있으면 `continue` · 허용 셋에 실행 계정 `:505` |
| 못 고르면 실패(건너뛰기 아님) | 🟢 | `:579-580` `v.Add(…재지 못했다)` · 도메인 SID 없음도 `:578` 실패 · `Skipped` 호출 0 |
| 표준오류 한 줄 = RID 만 | 🟢 | `:583` 숫자 RID 만 · SID 전체값·계정 이름 0 · 실패 문구 `:586` 도 RID + 제품 고객 문구(경로·SID 0) |
| 판정 함수 분리 | 🟢 | `:461`·`:477` 시험 쪽 `LookupSidType` — 제품 판정을 끄는 무력화가 「준비 실패」로 가려지지 않는다(10-3 V1·V5 실측) |
| RID 500 분기가 실제로 타나 | 🟢 | 개발PC(실행 계정 ≠ 500) = `RID 500` · CI Windows 러너(=500) = `RID 501` · 모사로 503·실패까지 도달(10-3) |
| ⚠️ P3 관찰 | — | 도메인 가입 PC 에서는 `AccountDomainSid` = 도메인 → DC 불통이면 세 RID 모두 판정 0 → 「고르지 못했다」 빨간불(조용한 통과 아님 · CI 러너는 로컬 계정이라 해당 없음) · 병렬이슈 아님 |

### 10-3. 무력화·분기 도달 — 검증자 손 (검증자 worktree 안 임시 환경변수 스위치 2개 · 한 빌드 · 커밋 0)

| 회차 | 바꾼 것 | 결과 | 원문 |
|---|---|---|---|
| V0 | 없음 | 🟢 Passed | stderr `[BackupCredentialGate] G-BK4 ⓔ 대조 계정 RID 500` |
| V1 | 제품 `IsIndividualUserAccountWindows` 첫 줄 `return false` (개별 사용자 허용 끔) | 🔴 Failed | `C-12 ⓔ 개별 사용자(RID 500) 권한 줄이 붙은 기본 폴더를 거부했다: InvalidOperationException: 기본 백업 폴더에 개별 사용자가 아닌 그룹(모든 사용자·Users 등)의 권한이 있어 백업 파일을 쓰지 않았습니다. 폴더 권한을 확인하세요.` |
| V2 | 시험 허용 셋에 RID 500 추가 (= CI 러너 모사) | 🟢 Passed | stderr `RID 501` |
| V3 | 500·501 추가 | 🟢 Passed | stderr `RID 503` |
| V4 | 500·501·503 추가 | 🔴 Failed | `C-12 ⓔ 준비: 대조할 개별 사용자 계정을 고르지 못했다(RID 500 은 시험 계정 자신이거나 사용자 판정 실패 · RID 501·503 도 사용자 판정 실패) — 이 단언은 재지 못했다` |
| V5 | V1 + V2 (CI 모양 무력화) | 🔴 Failed | `C-12 ⓔ 개별 사용자(RID 501) 권한 줄이 붙은 기본 폴더를 거부했다: …` |
| 되돌림 | `git checkout` 두 파일 → status 0 → 재빌드 0/0 → **스위치 환경변수를 켠 채** 전체 게이트 | 🟢 8/8 · `[SKIP]` 0 | 스위치가 코드에서 사라졌음도 함께 증명 · stderr `RID 500` |
| DLL 함정 | 매 빌드 뒤 `HitPan.Tests.dll` > 게이트 소스 · `HitPan.Application.dll` > `BackupService.cs` | 🟢 True × 3 | 빌드 3회 모두 warnings 0 · errors 0 |

### 10-4. 게이트 전체

| 곳 | 결과 |
|---|---|
| 검증자 격리 인스턴스 | `mariadbd` 11.4.10 일반 프로세스 · `--no-defaults` · 127.0.0.1:**33983** · 새 datadir · 출하 DDL(origin/main) **152표** · `HITPAN_REQUIRE_DB=true` → **8/8 PASS × 2회**(착수 · 되돌림 뒤) · `[SKIP]` **0** · 로그 속 격리 비번 문자열 0회 |
| CI Build & Test run 34604084368 (`1e048090` · pull_request) | ✅ DB 게이트 잡: `BackupCredentialGate` **8 Passed · `[SKIP]` 0** (잡 전체 145) · ✅ **Build(Windows) 잡 success** — 원문 `[BackupCredentialGate] G-BK4 ⓔ 대조 계정 RID 501` · G-BK4·G-BK5 실측 PASS · G-BK1·2·3×2·6·7 = 설계대로 `[SKIP]` 6 (DB 없는 잡 · 전체 1367) · ✅ DDL Smoke · 마이그 번호 · 권한 메뉴 · → **C-14 CI 원문의 G-BK4 FAIL 해소** |

### 10-5. [3-V] 판정 — 제품이 Guest(501)·DefaultAccount(503) 권한 줄을 받아들이는 것

| 항목 | 내용 |
|---|---|
| 사실 | `BackupService.cs:857-864` — 허용 셋 밖 Allow 줄은 `LookupAccountSid` = SidTypeUser 면 통과 → Guest·DefaultAccount(·WDAGUtilityAccount 504 도 같은 판정) 통과 (V2·V3 에서 제품이 받아들임 실측) · 그룹 `Guests` 는 거부 |
| 위협 모델 | 보호 ACL 폴더에 줄을 붙이려면 권한 변경 권한 = SYSTEM · Administrators · 실행 계정(소유자) 뿐 · Guest·DefaultAccount 는 기본 비활성이고 켜는 것도 관리자 → 이 줄이 생기는 경로 = 이미 백업 파일 전부를 읽을 수 있는 손 → **권한 상승 경로 아님** |
| 남는 위험 | 관리자가 Guest 를 켜 둔 PC(암호 없는 로그온·공유) + Guest 줄 = 업무 데이터 덤프 전체를 비인가자가 읽는다 · 「Guests 그룹 거부 · Guest 계정 허용」 은 방침 문장(「개별 사용자」)과 결과가 어긋나 보인다 · 1.3.40 이전엔 폴더 권한 검사 자체가 없었다 → 후퇴 아님 |
| 등급 | 🟡 **P3** — §9-3 **P3-a** 와 같은 건 · 새 병렬이슈 번호 안 땀 |
| **1.3.40 게시 차단** | **아니오** — 이 줄을 붙일 수 있는 사람은 이미 데이터를 가진 관리자뿐이라 새로 여는 문이 없고, 핫픽스 목적(비번 대기 · C-8 · C-12)의 방어는 그대로 선다 |
| 방침 변경 후보 (제안만 · 결재 전) | 비활성·기본 계정 RID(501·503·504) 줄 거부 — **채택하면 CI 러너(RID 500)에서 ⓔ 대조 계정이 사라져 G-BK4 가 V4 모양 빨간불** → 대조 계정 전략(예: 시험이 만든 임시 로컬 사용자)을 같이 설계해야 한다 |

### 10-6. 판정 · 금지 준수 · 정리

- 판정: 🔴 **TruffleHog — pull_request 스캔 여전히 실패** (코드 반려 아님 · 게시·머지 조건 미충족 · 원인 `f747cda3` 역사) · 🟢 C-17 · 🟡 P3 · 게시 차단 **아니오** · 🟢 게이트(격리 8/8 · CI DB 잡 8 · CI Windows `RID 501` PASS)
- 금지 준수: 구현자 worktree 읽기만 · 제품 브랜치 쓰기 0 · 커밋·푸시 0 · 3306·5257·5234 접속 0 · 운영 DB 무접촉 · PID 21540·36284 목록 조회만(무접촉) · 비밀번호는 파일로만 전달 · 출력 0
- 정리: `mariadb-admin shutdown` → 33983 LISTEN 0 · `mariadbd` 프로세스 0 · 검증자 worktree status 0 확인 뒤 `git worktree remove` · 격리 폴더(datadir · 임시 비번 파일 · 빌드·게이트 로그) 삭제 · 게이트 임시 폴더: 되돌림 뒤 실행이 남긴 **빈** `hitpan_bkgate_*` 1개 삭제 → 0 — ⚠️ 관찰: 착수 실행은 0개, 되돌림 뒤 실행은 빈 폴더 1개(원인 ⬜ · 정리 경로 한 곳이 폴더 삭제를 놓침 · 차단 아님)
