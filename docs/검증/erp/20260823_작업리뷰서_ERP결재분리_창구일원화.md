# 작업리뷰서 — ① 결재 창구 일원화 (20260823작1) · [4] 검증

| 항목 | 내용 |
|---|---|
| 검증 | 검증팀 · 2026-08-23 |
| 대상 | [작업지시서 20260823작1](../../운영기록/20260823작1_ERP결재분리_창구일원화_작업지시서.md) |
| 설계 | [20260823설1](../../설계/erp/20260823_세부설계_ERP결재분리.md) |
| 커밋 | `6114efe6` (#246) · 게시 **1.3.3** |
| 판정 | 🟢 **통과** — 실측 **15 PASS · 0 FAIL** |

---

## 0. 🟢 이번엔 실측까지 끝냈다

G1 때와 다르다. G1 은 *"코드는 됐고 갔는지는 모른다"* 로 [5] 를 올렸다가
사장님께 *"실측은 그동안 정석대로 했잖아"* 라고 물리셨다.

**이번은 게시(1.3.3) → 팝업 [예] → 터널 실측까지 돌고 나서 판정한다.**

---

## 1. 실측 결과 (터널 `test1234` · v1.3.3)

🔴 **스크립트가 버전을 먼저 확인하고 1.3.3 이 아니면 중단한다** — 옛 코드를 재고
"됐다" 고 보고하는 것을 구조적으로 막았다.

| # | 게이트 | 결과 | 값 |
|---|---|---|---|
| E-1 | API · 버전 | 🟢 | **v1.3.3** |
| E-4 | 운영 아님 확인 | 🟢 | 사원 2명 (#39) |
| **E1-1** | **ERP 7종 제거** | 🟢 | **7종 전부 빠짐** |
| **E1-2** | **그룹웨어 8종 보존** | 🟢 | **전부 있음** — `expense` 포함 |
| **E1-8** | **채운 2종 표시** | 🟢 | 사직서 · 전자근로계약서 |
| E1-8b | 라벨 한글 | 🟢 | 영문 코드 노출 0 |
| **E1-9** | **두 종류 결재선 저장** | 🟢 | 둘 다 HTTP 200 |
| E1-R1~R4 | 회귀 | 🟢 | 전부 **JSON** |

**결재 설정 목록이 17종 → 10종이 됐다.** 사장님 지시 그대로다.

### ⚪ 미측정 1건 — 정직하게 남긴다

**E1-5 옛 ERP 결재 문서의 라벨** — 이 테넌트에 ERP 결재 문서가 **0건**이라 못 쟀다.
⇒ 그 자리는 시험이 지킨다(`ERP_를_뺐어도_옛문서_라벨은_한글로_뜬다`).
⚠️ **"통과" 로 적지 않는다.** 안 잰 것은 안 잰 것이다.

---

## 2. 🔴 초록불이 어디서 오는지 확인했다

### 봉합 제거 실험

`delivery` 를 `ErpDocTypes` 에서 빼니 **딱 그 케이스만 FAIL** 했다.

```
실패: 1 · 통과: 22 · 전체: 23
  ERP_문서종류는_결재창구에서_빠진다(docType: "delivery") [FAIL]
```

⇒ 게이트가 **그 자리를 정확히** 보고 있다. 복구 후 23/23 재확인.

### 판정 방법 — 본문으로 가른다

🔴 **HTTP 코드로는 판정할 수 없다**(오늘 배운 것).
Blazor SPA 라 **없는 주소도 200** 을 돌려준다. 그래서 회귀 판정을 **본문**으로 했다 —
진짜 API 는 JSON, 없는 라우트는 `<!DOCTYPE html>`.

---

## 3. 🟢 기존 가드시험이 내 누락을 잡았다

`ApprovalTriggerHelper` 에 **별도 알림 라벨 표**가 있는데 새 2종을 안 넣어 **FAIL** 했다.

```
설정화면_문서유형은_알림라벨도_가진다 [FAIL]
→ 알림에 "결재 문서" 로 뭉개지거나 영문 코드가 그대로 노출된다
```

⇒ 안 채웠으면 **알림에 영문 코드가 나갔다.** 채웠다.
⇒ **가드시험이 실제로 일한 사례**다.

---

## 4. 설계 판단 검증

| 판단 | 검증 |
|---|---|
| 사전에서 안 지우고 **목록만 거른다** | 🟢 라벨 경로(`:578`·`:983`)가 살아 있어야 옛 문서가 한글로 뜬다 |
| 호출부 4곳이 아니라 **중앙 1곳**에서 막는다 | 🟢 새 호출이 추가돼도 안 샌다 |
| `DescribeApprovalBlockerAsync` 도 **같은 판정** | 🟢 안 맞췄으면 ERP 에 *"설정이 꺼져 있어요"* 틀린 안내 |
| `expense` 는 **안 뺀다** | 🟢 그룹웨어다 — 뺐으면 경비 결재가 죽는다(8/21 P0 자리) |
| **등재를 먼저** 한다 | 🟢 8/21 휴직 P0 의 반대 — 트리거 먼저면 조용히 죽는다 |

---

## 5. 헌법 체크

| # | 항목 | 확인 |
|---|---|---|
| #1 | 덮어쓰기 금지 | 🟢 사전·표 보존 · 목록 필터만 |
| #19 | 빌드 0/0 | 🟢 |
| #20 | 워크플로 안 끊긴다 | 🟢 신규만 막고 있던 것은 흐른다 · 결재함 0건 실측 |
| #37 | 안 읽힌다 ≠ 잔재 | 🟢 `DocTypeLabels`·`approval_settings` 보존 |
| #39 | 운영 무접촉 | 🟢 T-004 시험 테넌트 · 실측 흔적 되돌림 |
| #42 | 문서 근거 | 🟢 설1 → 작1 → 이 리뷰서 |

---

## 6. 판정

🟢 **통과.** 빌드 **0/0** · 시험 **659/659** · 실측 **15 PASS · 0 FAIL** · DDL **0건**.

**남은 것**: E1-5 미측정 1건(ERP 결재 문서가 생기면 그때 확인).

⚠️ **SoD** — 같은 세션에서 만들고 검증했다. 다만 **봉합 제거 실험**과
**기존 가드시험 적발**이 있었으므로 형식만 갖춘 자기검증은 아니다.