# 작업리뷰서 2026-09-11 — 토큰 절약 물리장치 · 개발환경 셋팅

| 항목 | 내용 |
|---|---|
| 작업지시서(PRD) | `docs/운영기록/20260911작1_토큰절약_물리장치_개발환경셋팅_작업지시서.md` |
| 개발명세서 | `docs/개발/erp/20260911_개발명세서_토큰절약_개발환경셋팅.md` |
| 구현자 | PM 브라운킴 (직접 구현) |
| **검증자** | 검증팀장 데이비드 박 [4] + 보안상무 안철수 [3-V] (구현자와 분리 — SoD) |
| **판정** | ⚠️ **조건부 통과** — P2 훅 우회 1건(P1) 봉합 또는 수용 결정 필요, 그 외 전부 실측 PASS |
| **통과조건 이행** | ✅ (a) 봉합 · 실세션 재확인 — §8 (구현자 PM 기재 · 판정 칸은 검증팀 원문 그대로 · [5] 결재자 확인 대상) |

## 1. PRD 대비 검증 (지시대로 만들었나)

| PRD 요구 (§2) | 개발명세서 주장 | 실제 확인 | 판정 |
|---|---|---|---|
| P2 큰 문서 통째 읽기 차단 | ✅ (실세션 INDEX 거부 확인) | 정상 경로는 실제로 DENY. 단 `offset` 만 주고 `limit` 을 안 주면 **통째로 통과** — 아래 §3 | ⚠️ |
| P3 쉰 에이전트 재개 차단 | ✅ 모의 21/21, 실세션 미확인 | node 로 9개 시나리오(최근/오래쉼/재개승인/1h캐시 20·60분/이름조회/미상대상/빈값) 직접 호출 — **9/9 의도대로 동작** | ✅ |
| P4 표준 에이전트 4종 | ✅ | `.claude/agents/hp-{designer,implementer,verifier,judge}.md` frontmatter 정상(name·description·model·effort) · `.gitignore` 로 이 4개만 staging 대상(§5) | ✅ |
| P6 토큰 일일 리포트 | ✅ | `token-report.js` 로직 검토 — 캐시쓰기/읽기/출력 가중치·모델계수 계산식 확인. 실행 재현은 예산상 생략(대신 P7 build-brief 실측으로 대체 확인) | 🟡 |
| P7 빌드 · 테스트 요약 | ✅ build, ⚠️ test 미실행 | `build-brief.ps1` 을 **실제 API csproj** 로 직접 실행 → `BUILD exit=0 errors=0 warnings=0`(1줄). `test-brief.ps1` 도 필터로 직접 실행 → 정상 동작·`HITPAN_REQUIRE_DB` 안내문 출력 | ✅ |
| P11 CLAUDE.md 무손실 | 헌법행 49=49 | 헌법#1~43+6단계 표 **49행 diff 0**(history 파일 대비 byte 동일) | ✅ |
| P11 TEAM_DEFINITION 무손실 | — | 팀 절(PM~웹퍼블리셔) **227행 diff 0**(history 파일 대비 byte 동일) | ✅ |
| P11 운영기록 INDEX 무손실 | 첫 칸 링크 131=131 | 첫 칸(행 링크) 재검증 — **구 135 · 신 136(오늘 작1 행 +1) · 누락 0** | ✅ |
| P11 MEMORY.md | 100줄 14,034B | **88/88 링크 실재 확인** · 14,034B(<25KB) · 100줄(<200) | ✅ |
| .gitignore `.claude/*` + `!agents/` | — | `git add --dry-run .claude/` → **agents 4개만** 스테이징 대상, worktrees·settings.local.json 등은 무시 확인 | ✅ |
| settings.json 유효성·보안 | — | `JSON.parse` 성공 · `context-counter` 잔재 0 · hooks·scripts·agents 파일에 비밀값 출력/저장 코드 0건 | ✅ |

> ⚠️ **요약된 절(3개시스템헌법·업무 루틴)에서 빠진 원문**(옛 "업무 루틴" 루틴A/B·문서번호 포맷, "SaaS 3분할 방향성") — history 파일에만 존재. 개발명세서 §4 도 이를 **"이력에만 남은 줄"로 스스로 명시**했고, ①"문서번호" 포맷은 `docs/CATEGORIES.md`(파일명 표)에 별도로 살아있고 ②"SaaS 3분할"의 유일한 실질 규칙("결제·과금 로직→랜딩 분리")은 CLAUDE.md 에 남은 "3개 시스템 개발 헌법 요약"의 "랜딩=가입·결제·다운로드만" 과 동일 효력 ⇒ **결함 아님, 정보 소실 없음**으로 판정.

## 2. 3관문

| 관문 | 결과 | 근거 (실제 출력) |
|---|---|---|
| 빌드 errors 0 + warnings 0 (헌법 #19) | ✅ | `powershell scripts/dev/build-brief.ps1 -Project src/HitPan.API/HitPan.API.csproj` → `BUILD exit=0 errors=0 warnings=0` |
| ddl-smoke (DDL 변경 시) | — | 해당 없음(코드·스키마 무접촉, ERP 소스 0줄 변경) |
| 독립 반증 | ⚠️ | §3 — 3건 이상, 1건 실제 결함 적발 |

## 3. 독립 반증 — 깨뜨리려는 시도

| 시도한 공격 | 결과 | 판정 |
|---|---|---|
| guard-big-md.js: 20KB 초과 파일을 `offset` 만 주고 `limit` 없이 Read | **ALLOW(통과)** — Read 기본 2000줄 동작과 결합해 217KB·172줄급 문서가 통째로 나갈 수 있다 | ❌ **결함** — 병렬이슈21 |
| guard-big-md.js: 대문자 `.MD`·워크트리 경로(`.claude/worktrees/x/docs/`)·`/` 구분자·INDEX 10KB 문턱 | 전부 정상 DENY | ✅ |
| guard-stale-resume.js: `#재개승인` 없이 10분 쉰 일반 에이전트 재개 / 60분 쉰 hp-verifier(1h캐시) 재개 | 둘 다 정상 DENY, 20분 쉰 hp-verifier·2분 쉰 일반 에이전트·`#재개승인` 포함·미상 대상·빈 `to`는 정상 ALLOW | ✅ |
| `.gitignore`: `.claude/` 통째 add 시도 | agents 4개만 스테이징, worktrees·settings.local.json 등 제외 확인(`git add --dry-run`) | ✅ |
| INDEX.md 압축이 첫 칸(행) 문서 링크를 덜어냈는지 | 1차 스캔(전체 텍스트 링크, 설명 인용 포함)은 10건 "누락"으로 잘못 나왔으나 **행-첫칸만** 다시 추출하니 **누락 0**(1차 스캔은 설명문 안의 타 문서 인용을 섞어 센 방법론 오류였음 — 자체 정정) | ✅ (자체 반증 오류 정정 기록) |

## 4. 코드리뷰 발견

| # | 등급 | 위치 | 내용 | 재현 | 봉합 |
|---|---|---|---|---|---|
| 1 | P1 | `scripts/dev/claude-hooks/guard-big-md.js:22` | `offset != null \|\| limit != null` 이면 통과 — `offset` 단독으로도 통과되어, Read 기본값(limit 미지정 시 최대 2000줄)과 겹치면 줄 수가 적은 큰 문서가 **통째로** 빠져나간다 | `docs/검증/병렬이슈/20260911_병렬이슈21_offset만_있으면_큰문서_거부가_통째로_뚫린다.md` §2 (node 직접호출, 4-분기 대조) | ⬜ 미봉합(SoD — 검증팀은 코드 수정 안 함) → ✅ 구현자 봉합 · §8 |

**P0 = 워크플로우 차단·데이터 손상·보안 → 즉시 반려**. 위 1건은 P1(장치 목적 무력화이나 워크플로우 차단·데이터 손상·보안침해는 아님) — **반려 사유는 아니나 게시 전 정정 권고**.

## 5. 거짓봉합 점검

- [x] 봉합 대상 없음(신규 산출물 검증) — 해당 없음
- [x] 이번 산출물이 다른 곳을 깨뜨리지 않았나 — ERP 소스·appsettings·운영 DB 무접촉 확인(git status에 src/ 변경 없음)
- [x] 문서에만 쓰고 코드는 안 고친 것 아닌가 — 7개 신규 스크립트·4개 에이전트 정의 파일 실물 확인, 3개는 직접 실행까지 재현
- [x] 개발명세서 §5(안 한 것)가 비어 있지 않다 — test-brief 미실행·P3 실세션 미확인 등 스스로 기재 → 검증팀이 이번에 test-brief 는 실행 보완, P3 실세션 발동은 여전히 미확인(다음 세션 과제)

## 6. [3-V] 병렬검증 이슈 확인

| 이슈 문서 | 봉합 확인 |
|---|---|
| `docs/검증/병렬이슈/20260911_병렬이슈21_offset만_있으면_큰문서_거부가_통째로_뚫린다.md` | ❌미봉합 — [5] 결재 시 수용 여부 판단 필요 → ✅ 구현자 봉합 · 실세션 재확인 (§8) |

## 7. 판정 근거

⚠️ **조건부 통과**:
- 3관문 중 빌드 0/0 ✅, 독립반증 완료(P1 1건 적발) — ddl-smoke 해당없음
- P0 0건 — 워크플로우·데이터·보안 침해 없음, 개발환경 도구 자체의 설계 목적 일부 무력화(P1)
- P11 무손실 기준(헌법행·TEAM_DEFINITION·INDEX 첫칸 링크) **전부 PASS** — 최초 INDEX 스캔의 "10건 누락"은 검증팀 자체 방법론 오류였음을 자체 정정(§3)
- **통과 조건**: (a) `guard-big-md.js` 의 `offset`-단독 우회를 막거나(예: `limit == null` 이면 무제한 취급), 또는 (b) 사장님·PM 이 "이 우회는 감수한다"로 명시 결재 — 둘 중 하나 후 [5] CTO 결재로 올림
- P3 실세션 발동 미확인은 개발명세서가 이미 자인한 항목(§5) — 다음 세션 확인 과제로 승계, 이번 통과 판정의 발목은 아님

## 8. 통과조건 이행 기록 (구현자 PM 기재 — 판정 변경 아님)

| 조건 | 이행 | 근거 |
|---|---|---|
| (a) `guard-big-md.js` offset 단독 우회 봉합 | ✅ `if (toolInput.limit != null) process.exit(0);` | 병렬이슈21 §5 |
| 재확인 ① 훅 시험 | ✅ 25/25 (offset 단독 거부 3 · 명시 탈출구 허용 1 추가 · 대조군 포함) | 개발명세서 §4 |
| 재확인 ② 재검증 에이전트 (haiku) | ⛔ **판정 무효** — 대조군 [1] (둘 다 없음, 봉합 전에도 DENY) 이 ALLOW ⇒ 한글 경로 파일 접근 실패 · 에이전트 스스로 "환경 제약으로 미확인" | 병렬이슈21 §5 |
| 재확인 ③ **실세션** (실제 훅 발동) | ✅ `claude -p --model haiku` — `{"offset":1}` → `DENIED: [토큰규칙 P2] … 통째 읽기 차단` · `{"offset":1,"limit":5}` → `OK lines=5` | 병렬이슈21 §5 |
