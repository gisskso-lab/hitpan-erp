# 작업리뷰서 2026-09-14 — 메인PC 표식 2줄 봉합 (2차 + G-M4 교체)

| 항목 | 내용 |
|---|---|
| 작업지시서(PRD) | [`docs/운영기록/20260913작2_메인PC표식_2줄_봉합_작업지시서.md`](../../운영기록/20260913작2_메인PC표식_2줄_봉합_작업지시서.md) §3 · §6-2 · §9-1 C-1~C-5 · §9-2 D-1~D-4 · §9-3 |
| CTO 결재 | [`docs/운영기록/20260913결1_메인PC표식_V4_마이그정지_CTO결재.md`](../../운영기록/20260913결1_메인PC표식_V4_마이그정지_CTO결재.md) §1 · §6 |
| 개발명세서 | `docs/개발/erp/20260913_개발명세서_메인PC표식_2줄_봉합.md` (대상 브랜치 안 · §7-2·§7-3·§8) |
| 대상 | 브랜치 `fix/20260913-mainpc-mark-r2` HEAD `1aadfaac` · merge-base `223d6140` 대비 7파일 (원격 없음) |
| 구현자 | 구현 세션 · PM 브라운킴 |
| **검증자** | hp-verifier — [3-V] 보안상무 안철수 관점(C-1) + [4] 검증팀장 데이비드 박 관점 (구현·PM 문맥 아님) |
| 환경 | 스크래치패드 worktree(`1aadfaac`) · 격리 MariaDB 11.4.10 `127.0.0.1:33307` (서버 카운터 `Com_create_db=14` 로 격리 접속 확인) · 운영 3306/5257/5234 접속 0 |
| **판정 (게시 전 최종 `7273edc6` · 2026-09-14)** | 🟢 **통과 — 게시 차단 없음** — 병렬이슈35 A·B 봉합 확인: 표식 없는 서버줄을 `approved` 로 쓰는 입구 전수 남은 문 0 · 가드 ① 확대 회귀 0 · 기기 계열 8종 131/131 건너뜀 0 · sln 경고 0 오류 0 · 무력화 C·D·E 모두 해당 게이트만 FAIL(G-M8i×2 / G-M8i×2 / G-M8k×2). 새 적발 없음. P3 비차단: 알림 생략 컨트롤러 시험 0 · 지문 네임스페이스(범위 밖) — §9 |
| 판정 (최종 재확인 `306f5c71` · 2026-09-14) | 🟡 **조건부 통과** — 병렬이슈34 ②③ 봉합은 **과하지 않다**(기기 계열 게이트 129/129 · Skipped 0 · 빌드 0/0 · 무력화 빨간불 재현). 그러나 **맞지만 덜 닫혔다**: ③ 승인 거부가 [인증키 재발급]→verify-key 로 우회(승인 PC 1→2) · ② 대기(pending) 옛 서버줄은 대표 알림 계속 — [병렬이슈35](../병렬이슈/20260914_병렬이슈35_옛서버줄_승인거부를_재발급경유로_우회_대기서버줄은_알림계속.md) P2 · **게시 전 PM 결재(이번 봉합 / 기록 후 별건)** — §8 |
| 판정 (재검증 `e72f0c10` · 2026-09-14) | 🟡 **조건부 통과** — 병렬이슈33 네 경우 봉합·무력화 빨간불 재현 · 회귀 게이트 100/100 · 빌드 0/0. **조건**: [병렬이슈34](../병렬이슈/20260914_병렬이슈34_거부된_합류화면_문구미도달_반려갈래_게이트공백_표식없는서버줄_승인시_슬롯증가.md) ① 반려(rejected) 갈래 게이트 추가(게시 전 필수) · ②③ PM 결재(이번 범위/별건) — §7 |
| 판정 (1차 `1aadfaac`) | ❌ **반려 (게시 보류)** — 조건 C-1 이 실측으로 **깨졌다** ([병렬이슈33](../병렬이슈/20260914_병렬이슈33_표식내린_폐기서버줄을_합류한_화면이_다시_세우고_사장님줄을_폐기한다.md) P1). DB-120 자체(①③④단·G-M4 교체)는 결재대로 동작한다 |

## 1. PRD 대비 검증

| 요구 | 명세서 주장 | 실제 확인 | 판정 |
|---|---|---|---|
| 축1 되올림 조건 (A) `approved` ∧ (B) 표식 0줄 | 완료 | `MainPcRegistrationService.cs` diff — UPDATE 에 `d.status='approved' AND t.mark_rows=0` · 파생표 두 겹(1093 회피). G-M1·G-M1a·G-M1b·G-M2 초록 | ✅ |
| ①단 정리 (K3 · C-2) | 완료 | V1·V2(폐기 줄이 더 최신)·V3 초록 · 3테넌트 실측에서 승인줄 있는 회사만 정리 | ✅ |
| D-1 ③단 조건부 · **DB 전체 판정** | 완료 | `DB-120:168-178` `@dup_tenants` 에 tenant 절 없음 · 3테넌트 실측(§3-4) UNIQUE 0 | ✅ |
| D-2 `main_pc_key` 무조건 유지 | 완료 | V4 ④ · G-M4 곁단언 | ✅ |
| D-3 감사 1줄 · 멱등 | 완료 | V4 ⑤⑥ · 3테넌트 2회 실행 뒤 더러운 회사에만 1줄 | ✅ |
| D-4 V4 단언 무수정 | 완료 | V4 ①② 원문 유지 · 조이는 단언 ③~⑥만 추가 (코드 대조) | ✅ |
| §9-3 G-M4 ⓐⓑⓒ **한 시험에 전부** | 완료 | `MainPcRestartMarkGateTests.cs:627-646` ⓐ예외 null · ⓑidx 0 · ⓒaudit 1 — 셋 다 한 메서드 · 곁단언(표식 2·revoked·사유원문·컬럼) 유지 | ✅ |
| G-M4 ↔ V4 겹치지 않음 | 주장 | 입력이 다르다: G-M4 = 승인1+폐기1 · ①단 절단 / V4 = 폐기+대기 · 전체 파일. ①단이 **고칠 수 있는** 중복을 ③단이 스스로 거르는지는 G-M4 만 잰다 | ✅ |
| G-M4 교체가 **무르기**인가 | 아니라 주장 | 옛 G-M4 가 지키던 「①이 ③보다 먼저」는 **G-M3 `IndexExists==1`**(`:591`)이 여전히 잡는다(순서가 뒤집히면 중복이 남아 ③ 건너뜀 → 0) ⇒ 보장 손실 없음 | ✅ |
| **C-1** 폐기 서버줄이 로그인 경로를 안 탄다 | [3-V] 실측 의무 | 🔴 **탄다** — §3-1 | ❌ |
| C-3 CI 등록 | 완료 | `build-and-test.yml` db-gate 잡 `GATES` 에 `MainPcRestartMarkGate` — 격리 실행만 확인 · **CI 실행 미실측**(원격 없음) | ⚠️ |
| C-5 순서0 선캡처 | — | 게시 전 절차 · 이 리뷰 범위 밖 · **미실측** | ⬜ |

## 2. 3관문

| 관문 | 결과 | 근거 |
|---|---|---|
| 빌드 errors 0 + warnings 0 | ✅ | `build-brief.ps1 -Project src/HitPan.sln` → `BUILD exit=0 errors=0 warnings=0` (새 worktree · 전체 빌드) |
| 게이트 (격리 DB · `HITPAN_REQUIRE_DB=1`) | ✅ | `MainPcRestartMarkGate` **Passed 14 · Failed 0 · Skipped 0** |
| ddl-smoke / 신규설치 (G-M6) | ✅ | G-M6 초록 — 출하 DDL 만으로 컬럼·UNIQUE·`DB-120` 시드 |
| 독립 반증 | ❌ | §3 — C-1 적발 |

## 3. 독립 반증

### 3-1. C-1 — 표식 내린 폐기 서버줄이 로그인 경로를 타는가 → 🔴 탄다

- 조회 1순위는 `device_id` (`TenantDeviceService.cs:212-223`) — 지문·`user_id` 를 안 본다.
- 브라우저가 서버줄 번호를 신분으로 갖는 정식 경로: `DeviceAuthGate.razor:595` `JoinServerRowAsync` (합류 2-1).
- 탐침: 실제 `TenantDeviceService` + 실제 `DB-120` 파일 · 대조군(적용 전) 포함 4경우 — 결과표는 병렬이슈33 §2.
  - 적용 전: 원격·로컬 모두 거부 · 표 **무변화**
  - 적용 후 원격: `revoked→pending` · **`revoked_reason` 소거**
  - 적용 후 **로컬콘솔: 허용 · 서버줄 부활(approved·표식1) · 사장님 줄 revoked · 다음 로그인에 되뒤집힘(핑퐁)**
- 탐침 파일은 스크래치 worktree 에만 두고 worktree 와 함께 제거(대상 브랜치 변경 0).

### 3-2. 무력화 재현 (검증자 손 · node 치환 · 매번 1곳만 · 원복 sha256 확인)

> ⚠️ 첫 시도는 `python` 이 Windows 스토어 스텁이라 **치환 0건인데 14/14 초록**이 나왔다 — 무효 처리하고 node 로 다시 쟀다(치환 횟수 1 단언 · 치환 후 sha 가 원본과 다름 확인).

| # | 무력화 | PM 주장 FAIL | 검증자 실측 FAIL | 판정 | 원복 sha256 |
|---|---|---|---|---|---|
| M1 | ③단 `AND @dup_tenants = 0` 제거 | G-M4ⓐ · V4 | G-M4 · V4 | ✅ 일치 | 685220860c49… |
| M2 | ④단 INSERT 제거 | G-M4ⓒ · V4 | G-M4 · V4 | ✅ 일치 | 685220860c49… |
| M3 | ①단 UPDATE 제거 | G-M3 · V1 · V2 · V3 | G-M3 · V1 · V2 · V3 | ✅ 일치 (10/14 · 검증자 확인) | 685220860c49… (검증자 원복 확인) |
| M4 | ③단 ALTER → `SELECT 1` | G-M5 · G-M3 | ⬜ **미실측** — 실행 중 격리 DB 가 외부 `shutdown`(20:12:54 root@127.0.0.1)으로 내려가 접속 실패 11건 · 결과 무효 | — | 685220860c49… |
| M5 | (검증자 추가) ④단 `HAVING >= 2` → `>= 1` | — | ⬜ **미실측** | — | — |

### 3-3. 안 되는 경우 찾기

| 시도 | 결과 | 판정 |
|---|---|---|
| 3테넌트 한 DB (A 폐기+대기 · B 승인+폐기 · C 정상) · DB-120 **2회** | A 무처리 · B 정리 · C 무변화 · **UNIQUE 0 (B·C 도 방어선 없음)** · 감사 A 에만 1줄(2회 뒤도 1) | ✅ D-1 설계대로 · 관찰 O-1 |
| ①단 뒤 중복 판정 범위 | `@dup_tenants` tenant 절 없음 = DB 전체 | ✅ |
| 감사기록 멱등 | V4 ⑥ · 3테넌트 2회 | ✅ |
| 출하 DDL 신규설치 | G-M6 | ✅ |
| 요금 계수 무이동 (DB-120 만) | G-M7 초록 · 3테넌트에서 status 변화 0 | ✅ — 단 **C-1 경로에서 로그인 뒤 요금·상태가 움직인다**(병렬이슈33) |
| 되올림 스킵 로그 문구 | `MainPcRegistrationService.cs:237` 「옛 기기를 폐기하면 다음 기동에 새로 잡힌다」 — 자기 줄이 폐기면 `existingId` 로 `return` 해 **새로 안 잡힌다** | ⚠️ O-2 (로그만 · 고객 노출 아님) |

## 4. 발견

| # | 등급 | 위치 | 내용 | 봉합 |
|---|---|---|---|---|
| 1 | **P1** | `DB-120` ①단 × `TenantDeviceService.cs:269`·`:366` × `DeviceAuthGate.razor:595` | C-1 전제 반증 — 합류 화면이 표식 내린 폐기 서버줄로 로그인하면 **부활(결재 5 위반)·사장님 줄 폐기·핑퐁**, 원격이면 사유 원문 소거. 대조군(적용 전)은 무변화 → **게시가 여는 입구** · [병렬이슈33](../병렬이슈/20260914_병렬이슈33_표식내린_폐기서버줄을_합류한_화면이_다시_세우고_사장님줄을_폐기한다.md) | 🔴 결재 대기 |
| 2 | P2 | `DB-120:199` → `LogPage.razor:57` | ④단 `reason` 이 고객 [로그] 화면 「메모」칸에 그대로 뜬다 — `uq_tenant_main_pc` · 「사장님 결재 (가)」 · `DB-122` 등 개발·내부 용어 고객 노출 | ⬜ |
| 3 | P2 (미실측) | 게이트 | M5 미실측 — ④단이 **정상 회사에 거짓 기록**을 남기는 무력화(M5)를 잡는 대조군 | ⬜ |
| O-1 | 관찰 | `DB-120:194-211` | 한 회사가 더러우면 **깨끗한 회사도** UNIQUE 없이 남는데 감사 줄은 더러운 회사에만 — 재정합(DB-122+)은 테넌트가 아니라 **DB 단위**로 대상을 찾아야 한다 | 설계 입력 |
| O-2 | 관찰 | `MainPcRegistrationService.cs:237` | 스킵 로그의 안내 문구가 폐기 자기줄 경우와 안 맞음 | ⬜ |

## 5. 미실측 (통과로 세지 않는다)

- 무력화 M4(③단 ALTER 제거)·M5(④단 거짓 기록) — 격리 DB 외부 종료로 무효 · 재실측 필요
  - ⚠️ **PM 기록**: 그 `shutdown` 은 **PM 세션이 낸 것**이다. 검증자가 턴 한도로 멈춘 뒤 M3 실행이 끝나자 정리한다고 33307 을 내렸는데, 검증자가 재개돼 M4 를 돌리던 중이었다. 도는 에이전트의 자원을 메인이 건드리지 않는다(토큰 매뉴얼 §4-5 동류).
  - 검증자 추가 소견(추정 · 미실측): V5·G-M3 에 「감사기록 0줄」 단언이 없어 M5 를 못 잡을 가능성이 크다
- CI 원격 실행(C-3) — 브랜치가 원격에 없다
- C-5 순서0 선캡처 · 실물 `hitpan_erp_t004` 의 `MAINPC-9c1163c` 줄 `auth_key_hash` 유무(#39 — 검증자 운영 접속 금지 · PM 읽기 전용 확인 요청)
- 마이그 러너(`MigrationRunner`) 경유 실행 — 게이트·검증자 모두 `ExecuteAsync` 직접 호출로 대체
- 되올림 UPDATE 동시 기동 경합 · Korean `reason` 의 러너 연결 문자셋

## 6. 결론

DB-120 ①③④단과 G-M4 교체는 결재(§9-2 D-1~D-4 · §9-3)를 지켰고 무력화도 게이트가 잡는다.
그러나 **C-1 은 「빠지면 반려」 조건**이고 실측이 전제를 뒤집었다 — 표식을 0으로 내리는 순간 8/18작4 표식 이전 갈래와 8/20작2 합류가 만나는 **잠복 입구가 다시 열린다.** 병렬이슈33 결재·봉합·게이트(대조군 무변화 + 핑퐁 0) 전 게시 금지.

---

## 7. 재검증 (e72f0c10)

| 항목 | 내용 |
|---|---|
| 대상 | `fix/20260913-mainpc-mark-r2` HEAD `e72f0c10` · `1aadfaac` 대비 5파일 (가드 `TenantDeviceService.cs` +45 · G-M8a~d · DB-120 ④단 문구 · CI 주석 · 명세서 §7-4) |
| 근거 | 작업지시서 §9-4 (사장님 결재 (a)) |
| 검증자 | hp-verifier — [4] 데이비드 박 + [3-V] 안철수 관점 (구현·PM 문맥 아님) |
| 환경 | 스크래치패드 worktree `e72f0c10` (종료 후 remove) · 격리 MariaDB 11.4.10 `127.0.0.1:33307` (새 datadir · 종료 후 shutdown) · 운영 3306/5257/5234 접속 0 · 치환 node 1회 단언 · 매 회 `git checkout` 뒤 `git status` 0줄 |

### 7-1. 빌드·게이트

| 항목 | 결과 |
|---|---|
| `dotnet build src/HitPan.sln -c Release` | ✅ 경고 0 · 오류 0 |
| `MainPcRestartMarkGate` (HITPAN_REQUIRE_DB=1) | ✅ 20/20 · `[SKIP]` 0 |
| 회귀 — `DeviceAndKeyGate`·`DeviceApprovalGate`·`DeviceApprovalNotifyGate`·`DeviceSlotGuard`·`DeviceSlotSeedGate`·`DeviceTypeQrGate`·`MainPcLocalConsoleGate` | ✅ 100/100 · `[SKIP]` 0 |

### 7-2. 무력화 (검증자 손 · 1곳씩 · 원복 뒤 diff 0)

| # | 무력화 | 명세 주장 | 검증자 실측 FAIL | 판정 |
|---|---|---|---|---|
| M1 | 가드 ① 무력화 (조건 앞 항상 거짓) | G-M8b | G-M8b(local·원격) 2건 | ✅ |
| M2 | 가드 ② 제거 (`&& !isServerRowWithoutMark`) | G-M8c | G-M8c 1건 | ✅ |
| M3 | 둘 다 제거 (= 봉합 전) | G-M8b | G-M8b 2 · G-M8c 1 | ✅ |
| **M6** | **가드 ① 에서 `\|\| status == "rejected"` 만 제거** (검증자 추가) | — | **0건 · 20/20 초록** | 🔴 게이트 공백 → 병렬이슈34 ① |
| M4 | ③단 `ALTER … ADD UNIQUE` → `SELECT 1` | G-M5·G-M3 | G-M5 · G-M3 | ✅ |
| M5 | ④단 `HAVING COUNT(*) >= 1` | V5·G-M3 | V5 · G-M3 (감사 0줄 단언이 문다) | ✅ |

M6 뒤 검증자 탐침(스크래치 한정): 반려·표식 든 서버줄 + 사장님 줄 → DB-120 → 합류 로그인 ⇒ **봉합 코드**: local·원격 모두 표 무변화 · 가드 문구 · 장비넘버 반환 ✅ / **M6 코드**: local·원격 모두 `rejected → pending`(재신청) — 게이트는 이 변화를 못 본다. (가드 ② 가 남아 있어 부활까지는 안 간다.)

### 7-3. 반증 항목별

| # | 시도 | 결과 | 판정 |
|---|---|---|---|
| 1 | 병렬이슈33 네 경우(DB-120 전/후 × local/원격) | G-M8a 대조군 무변화 · G-M8b 거부·표 무변화·핑퐁 0 · 가드 제거 시 빨간불 재현 | ✅ (반려 상태는 게이트 밖 — M6) |
| 2 | 과한가 — 8/11 폐기 메인PC 구제 · DP-2 자가회복 | 가드는 `!isMainPc` 에서만 계산 · G-M8d 대조군 초록 · 표식 든 줄 갈래 무수정 | ✅ |
| 2 | 8/18작4 브라우저 줄 표식 이전 · 일반 기기 폐기→대기 | 브라우저 줄은 지문 `HFPv2-` ⇒ 가드 거짓 · MainPcLocalConsole·DeviceApproval 계열 100/100 | ✅ |
| 2 | 가드 ② 범위 | 작지 §9-4 는 「승인 ∧ localhost」, 코드는 **상태 무관**(대기 서버줄도 이전 건너뜀) — 원칙 방향과 같고 해는 못 찾음 · 기록 | 🟡 기록 |
| 3 | 거부된 합류 화면이 갇히나 | 반환 장비넘버 → `AuthController:162` 승인대기 갈래(401 아님) → 관문 → 대표면 [회사서버 컴퓨터] → `JoinServerRowAsync` 가 `IsMainPc ∧ approved` 줄로 옮김 ✅ · **그러나 가드 문구는 화면에 안 닿는다**(`DeviceNotice` 는 `Login.razor:316` 에서 신규등록일 때만 표시) — 관문은 「기기 등록을 요청했습니다 · 대표님이 허락하면」 · 대표 아닌 사람은 버튼이 없고 대표가 승인해도 「폐기된 기기는 승인할 수 없습니다. 그 기기에서 다시 접속해 주세요」 → 다시 접속하면 또 거부(순환) · 로그인마다 대표에게 `device_pending`「새 기기에서 접속했습니다」 | 🔴 병렬이슈34 ② |
| 4 | 브라우저가 `MAINPC-` 판정을 피하거나 흉내 | 판정은 요청이 아니라 **DB 줄 지문**으로 한다 ⇒ 요청으로 못 피한다. 브라우저가 자기 지문을 `MAINPC-…` 로 보내 새 줄을 만들면 그 줄이 폐기 뒤 영구 거부될 뿐(자해) — 권한 상승 없음 | ✅ (신규 등록이 접두어를 막는지 미실측) |
| 4 | 반려·대기 서버줄 승인 경로 | **표식 없는 대기 서버줄** 실측: 합류 로그인 → 대기(표 무변화) → `ApproveAsync` 성공 → `approved ∧ 표식0` · **승인 PC 1→2**(요금 이동) · 반려 서버줄도 `ApproveAsync` 는 `revoked` 만 막으므로 같은 길(코드 근거) | 🔴 병렬이슈34 ③ |
| 5 | DB-120 ④단 새 문구 | 「회사 서버로 표시된 기기가 여러 대라 자동으로 하나를 고르지 않았습니다. 등록 기기 관리에서 확인이 필요합니다.」 — 개발·내부 용어 0 · 금지어(#23) 0 · 메뉴명 실재 | ✅ |
| 6 | 빌드 0/0 · SKIP 0 | §7-1 | ✅ |

### 7-4. 미실측 (통과로 세지 않는다)

- 반려 서버줄 `ApproveAsync` 실제 실행(코드 근거만) · 관문 자기승인 버튼(`_selfApproving`)이 표식 없는 서버줄 장비넘버로 승인하는지
- 브라우저 화면 실측(관문 문구·순환) — 코드 추적만 · `gate-status` 의 `Reapplied` 값
- 신규 기기 등록이 클라이언트 지문 `MAINPC-` 접두어를 거부하는지
- 실물 고객 DB 에 「표식 없는 대기/반려 서버줄」이 몇 건인지(8/18작4 폐기 줄이 1.3.40 이전 원격 합류로 대기 전환됐을 수 있음 · #39 운영 읽기만)

### 7-5. 결론

병렬이슈33 의 결재 범위(가드 ①②)는 코드·게이트·무력화로 섰다 — **폐기 서버줄 부활·사장님 줄 폐기·핑퐁 0.** 회귀 없음.
다만 ① 가드가 막는 두 상태 중 **반려는 게이트가 재지 않는다**(M6 초록) — 게시 전 G-M8b 에 `rejected` 경우 추가 + M6 재실측 필수. ② 거부는 **표에서는 막혔지만 화면에는 엉뚱한 말**이 가고 대표 알림이 매번 샌다 — 「막는 것 ≠ 알려주는 것」. ③ 표식 없는 서버줄의 대기·반려 상태는 대표 승인으로 슬롯을 먹는다 — §9-4 가드 밖이라 이번 반려 사유는 아니나 작지 불변 「요금 계수 무이동」과 맞닿으므로 PM 결재.

## 8. 최종 재확인 (306f5c71)

> 대상 `git diff e72f0c10 306f5c71` 8파일(G-M8e 커밋 `e76510ef` 포함) · 작지 §9-5 · 검증자 격리 worktree(스크래치 · 끝나고 제거) · 격리 MariaDB 11.4 `127.0.0.1:33307`(끝나고 shutdown) · 운영 3306·5257·5234 무접촉 · 대상 코드 무수정.

### 8-1. 빌드·게이트

| 항목 | 결과 |
|---|---|
| `src/HitPan.sln` 빌드 | ✅ errors 0 · warnings 0 |
| 기기 계열 게이트 (`~Device`·`~MainPc`·`~Slot`·`~Revoke`·`~Mobile` · `HITPAN_REQUIRE_DB=1`) | ✅ **129/129 · Skipped 0** |
| #14 `.razor` raw string | ✅ diff 안 `"""` 0건 |
| #15 빈 catch | ✅ 새 catch 0 · 기존 알림 catch 는 `LogWarning` 유지 |
| 고객 노출 문구 | ✅ 「기기 정보」「회사 서버의 옛 기록」「회사서버(자료 보관) 컴퓨터」 — 개발용어·금지어 0 |

### 8-2. 무력화 (검증자 손 · node 치환 1곳 · 일치 1건 단언 · 원복 `git checkout`)

| # | 무력화 | 작지 주장 | 검증자 실측 FAIL | 판정 |
|---|---|---|---|---|
| R4-1 | `ApproveAsync` 가드 조건 거짓화 | G-M8f | **G-M8f pending·rejected 2건만** (G-M8 12건 중) | ✅ |
| R4-2 | `IsServerRowWithoutMarkAsync` 항상 0건(SQL `AND 1=0`) | G-M8h·G-M8b | **G-M8b×2 · G-M8c · G-M8e×2 · G-M8f×2 · G-M8h = 8건** | ✅ (주장보다 넓게 잡힘) |

### 8-3. 반증 항목별

| # | 시도 | 결과 | 판정 |
|---|---|---|---|
| 1 | ③ 거부가 과한가 — 표식 든 서버줄·일반 기기·모바일 QR·8/11·DP-2·메인PC 등록 서비스 | 판정 SQL 이 `COALESCE(is_main_pc,0)=0` 을 요구 ⇒ 표식 든 줄 제외(G-M8h 대조) · 브라우저 줄 제외 · 일반 대기 승인 성공(G-M8g) · 기기 계열 129 초록 · `MainPcRegistrationService` 는 `ApproveAsync` 를 안 부른다(grep) | ✅ 회귀 0 |
| 1-b | 로그인 가드 SQL 교체(표식 조건 추가) | 호출부가 `if (!isMainPc)` 안이라 종전과 동치 | ✅ |
| 2 | `approved` 로 바꾸는 다른 입구 | `:328` localhost 표식 이전(가드 ②로 서버줄 제외) · `:451` DP-2(표식 든 줄만) · **`VerifyAuthKeyAsync:1082` — 해시만 맞으면 승인** · `ReissueAuthKeyAsync` 는 `revoked` 만 막음 ⇒ 검증자 탐침: 대기·반려 옛 서버줄 **재발급→대조 = approved · 승인 PC 1→2** | 🔴 **우회로** → 병렬이슈35 A |
| 2-b | 관문 [회사서버] 합류(`JoinServerRowAsync:643`) | 재발급 대상은 `is-main-pc` 가 준 표식 든 줄 — 옛 줄 아님 | ✅ |
| 3 | ② 알림 생략이 다른 대기 기기 알림을 끄나 | 조건이 사유 글자 동일 비교 — 그 사유는 가드 ①(폐기·반려 옛 서버줄)만 반환 ⇒ 일반 대기 기기 알림 유지 | ✅ |
| 3-b | `if (...) try {} catch {}` 구문 | C# 에서 `if` 본문이 try-catch 문 전체 — 의도대로. 중괄호 없는 모양은 뒤에 문장이 끼면 조용히 빠진다(P3 모양) | ✅ (P3 기록) |
| 3-c | 대기(pending) 옛 서버줄 알림 | 검증자 탐침 local·원격: `reason=기기 승인 대기 중입니다.` ⇒ **알림 생략 안 됨** — 병렬이슈34 ② 의 원 실측 경우가 대기였다 | 🔴 → 병렬이슈35 B |
| 3-d | `staleServerRow` 일 때 관문 합류 버튼 | `_isOwner` 블록 안 · `_ownerKey` 비었을 때 `@if (!_staleServerRow)` 는 [이 컴퓨터 승인]만 감싼다 · [이 컴퓨터는 회사서버…] 버튼은 그 밖 ⇒ **대표에게 보인다** · 대표 아닌 계정은 버튼 0(안내 문구가 「대표님이시라면」으로 맞다) | ✅ (코드 추적) |
| 3-e | `DeviceAuthGate.razor:380` 주석 | 새 주석 뒤에 옛 주석이 한 줄에 붙음 — 동작 무관 | P3 |

### 8-4. 미실측 (통과로 세지 않는다)

- 관문 화면 실측(문구·버튼 숨김) · `AuthController` 알림 미발송 종단 — 컨트롤러·브라우저 시험 0 (작지 §9-5 도 미실측 명시)
- `staleServerRow` 관문에서 **키 입력칸**이 보이는지(병렬이슈35 A 의 화면 도달성)
- 실물 고객 DB 의 대기·반려 옛 서버줄 건수(#39 운영 읽기만 · 미조회)

### 8-5. 결론

②③ 봉합은 **과하지 않다** — 회귀 0 · 무력화 두 개 모두 빨간불. 그러나 **닫은 문 옆에 문이 하나씩 남았다**: ③ 은 승인 API 만 막아 재발급→대조 입구로 같은 슬롯 +1 이 난다(요금 계수 무이동 불변 우회) · ② 는 폐기·반려만 알림을 끄고 병렬이슈34 가 실제로 잰 **대기** 경우는 그대로다. 둘 다 같은 모집단(대기 옛 서버줄)이다. 🟡 **조건부 통과** — [병렬이슈35](../병렬이슈/20260914_병렬이슈35_옛서버줄_승인거부를_재발급경유로_우회_대기서버줄은_알림계속.md) 를 **게시 전 봉합할지 / 기록 후 별건으로 넘길지 PM 결재**. 「②③ 봉합 완료」로 보고하지 않는다.

## 9. 게시 전 최종 확인 (7273edc6)

> 대상 `git diff 306f5c71 7273edc6` — 코드 4파일(`TenantDeviceService.cs` +11/-1 · `AuthController.cs` 중괄호 2줄 · `DeviceAuthGate.razor` 주석 분리 · `MainPcRestartMarkGateTests.cs` G-M8i·k·l +73) + 명세서 §7-4-3. 작성자 PM 세션 · 검증자 hp-verifier(작성자 아님). 검증자 격리 worktree `scratchpad/verify-r5` · 격리 MariaDB 11.4 `127.0.0.1:33307`(`SELECT @@port`=33307 단언 · datadir 스크래치) · 운영 3306·5257·5234 무접촉 · 대상 코드 무수정(무력화는 worktree 사본에서 node 치환 1건 단언 후 원본 복구).

### 9-1. 빌드·게이트

| 항목 | 결과 |
|---|---|
| `src/HitPan.sln` 빌드 | ✅ 경고 0 · 오류 0 |
| 기기 계열 8종(`~MainPcRestartMark`·`~MainPcLocalConsole`·`~DeviceAndKey`·`~DeviceApproval`(Notify 포함)·`~DeviceSlotGuard`·`~DeviceSlotSeed`·`~DeviceTypeQr` · `HITPAN_REQUIRE_DB=1`) | ✅ **131/131 · 건너뜀 0** (7분 31초) — §8 의 129 는 필터가 달라(`~Device`·`~Slot`·`~Revoke`·`~Mobile`) 직접 비교 불가 · 새 5건(G-M8i×2·k×2·l) `--list-tests` 로 존재 확인 |

### 9-2. 반증 1 — 표식 없는 서버줄(`MAINPC-` ∧ `is_main_pc=0`)을 `approved` 로 만드는 입구 전수

grep: `src` 중 `tenant_devices` 참조 파일 15개(Backoffice 는 별 DB · Tests 제외) → `status` 를 쓰는 SQL 전부.

| 입구 | 서버줄 도달 | 판정 |
|---|---|---|
| `TenantDeviceService:328` localhost 표식 이전 | 가드 ② `!isServerRowWithoutMark` · 가드 ①(`status != approved`)이 먼저 반환 | ✅ 가드 |
| `:451` DP-2 반려 자가회복 | `isMainPc`(표식 든 줄)만 | ✅ 도달 불가 |
| `:753` 새 기기 INSERT | 기존 줄을 못 찾았을 때 **새 줄**만 만든다(옛 서버줄 승인 아님). ⚠️ 지문은 클라이언트 값이라 `MAINPC-` 로 시작하는 새 줄은 만들 수 있으나 상태는 일반 기기와 같은 규칙(localhost·승인끔만 approved) — 승인 우회 아님 | ✅ (P3 관찰 · 9-6) |
| `:954` `ApproveAsync` | `IsServerRowWithoutMarkAsync` 거부(§8) | ✅ 가드 |
| `:1086` `VerifyAuthKeyAsync` | **새 가드 `:1063`** — 해시 대조 전에 null | ✅ 가드 |
| `ReissueAuthKeyAsync:1166` | **새 가드** — 키 자체를 안 준다 | ✅ 가드 |
| `:1573` 모바일 QR INSERT | `qrStatus="pending"` 새 줄 | ✅ 도달 불가 |
| `MainPcRegistrationService:191` 되올림 | `is_main_pc` 만 1 로 올림(상태 무변경 · `status='approved'` 줄만) ⇒ 표식이 생기므로 대상 모집단에서 빠진다 | ✅ 도달 불가 |
| `MainPcRegistrationService:270` INSERT | `is_main_pc=1` 로 넣음 | ✅ 도달 불가 |
| `DB-89` 트리거 2개 + UPDATE | 모두 `is_main_pc = 1` 조건 | ✅ 도달 불가 |
| `DB-120` | 표식 정리(`SUM(status='approved')` 는 판정용 SELECT) | ✅ |
| EF `DbSet<TenantDevice>` | 0건 | ✅ |

⇒ **남은 입구 0** — 탐침 불요. 가드·UPDATE 가 별 문장(TOCTOU)이나 표식은 기동 시에만 바뀌어 실사용 경합 가능성 낮음(P3 · 미실측).

### 9-3. 반증 2 — 가드 ① `status != "approved"` 확대가 과한가

| 대상 | 근거 | 판정 |
|---|---|---|
| 표식 든 서버줄(8/11 폐기 구제 · DP-2) | 판정 전 `if (!isMainPc)` — 표식 든 줄은 `isServerRowWithoutMark=false` 고정 | ✅ 회귀 0 |
| 브라우저 줄(`HFPv2-`) · 모바일 QR | 판정 SQL `fingerprint LIKE 'MAINPC-%'` 불일치 · DeviceTypeQr·DeviceApproval 초록 | ✅ |
| `MainPcRegistrationService` 되올림 | 로그인 경로(`CheckOrRegister`)를 안 탄다 — 직접 SQL · `status='approved'` 조건이라 대기 줄은 종전에도 안 올림 | ✅ 무관 |
| 대기 옛 서버줄이 종전 일반 갈래에서 하던 쓰기(`last_seen_at` 등) | 이제 표 무변경 반환 — G-M8k 가 무변화를 요구 · 관문은 `!approved` 전부에 `staleServerRow` 를 실어 안내는 같다 | ✅ (의도) |
| 키 대조 가드가 **승인된** 옛 서버줄을 막나 | 승인된 줄은 키 대조를 부를 일이 없다(관문이 이미 통과). 대표가 그 줄에 [재발급]을 누르면 이제 400 「회사 서버의 옛 기록…」 — 그 줄로 새 브라우저를 묶는 길은 닫힘 | ✅ (의도 · P3 문구 관찰) |
| 대표 `device_pending` 알림 발송처 | `AuthController:214` **한 곳뿐**(grep) — 중괄호로 try-catch 전체가 `if` 안 · 사유 `StaleServerRow` 는 가드 ① 만 반환 ⇒ 일반 대기 기기 알림 유지 | ✅ (§8 3-b P3 모양 해소) |

### 9-4. 반증 3 — 무력화 (검증자 손)

worktree 사본 `TenantDeviceService.cs` 에 node 치환(일치 1건 단언 · 상수 `false` 대신 `"x".Length == 0` 로 도달불가 경고 회피) → 시험 프로젝트 빌드 → `~MainPcRestartMark` 31건 → 원본 복구(`git diff` 0줄 · 복구 빌드 경고 0 오류 0).

| # | 무력화 | 검증자 실측 FAIL (31건 중) | 판정 |
|---|---|---|---|
| C | `VerifyAuthKeyAsync:1063` 키 대조 가드 거짓화 | **G-M8i pending·rejected 2건** (시험이 해시를 직접 심어 재발급 가드와 독립으로 잡는다) | ✅ |
| D | `ReissueAuthKeyAsync:1166` 재발급 가드 거짓화 | **G-M8i pending·rejected 2건** | ✅ |
| E | 가드 ① 을 종전 `revoked‖rejected` 로 되돌림(대기 제외) | **G-M8k local·원격 2건** | ✅ |

- 대조군 G-M8l(표식 든 서버줄 재발급·대조 성공)은 세 무력화 모두 초록 — 가드가 합류 경로를 막지 않는다는 쪽도 살아 있다.
- ⚠️ 공백: `AuthController:200` 의 알림 생략 `if` 를 지워도 잡는 시험 0 — G-M8k 는 서비스 사유만 본다(P3 · 9-5).

### 9-5. 미실측 (통과로 세지 않는다)

- `AuthController` 알림 미발송 **종단**(컨트롤러 시험 0 — G-M8k 는 서비스 사유만 본다) · 관문 화면 실측 · 등록기기관리 [인증키 재발급] 400 문구 표시
- 실물 고객 DB 의 대기·반려 옛 서버줄 건수(#39 운영 읽기만 · 미조회)
- 가드·UPDATE 사이 경합

### 9-6. 범위 밖 관찰 (게시 비차단 · 이번 diff 가 만든 것 아님)

- P3: 로그인 지문 2순위 조회(`:226`)는 클라이언트가 보낸 지문을 그대로 쓴다 — `MAINPC-` 네임스페이스를 클라이언트에 막는 검사 0건(grep). 서버 지문은 `SHA256(컴퓨터이름|TENANT_CODE|SLOT_INDEX)` 앞 16자라 추측 난도는 있으나 비밀값은 아니다. 표식 든 줄을 지문으로 집어 원격에서 메인PC 로 대접받는지는 **미실측** — 별건 [3-V] 후보로만 기록.

### 9-7. 결론

병렬이슈35 A·B 는 **닫혔다** — 표식 없는 서버줄을 `approved` 로 쓰는 입구를 전수로 세어 남은 문 0(9-2) · 확대된 가드는 표식 든 줄·브라우저·모바일·되올림에 닿지 않는다(9-3 · 기기 계열 131/131 건너뜀 0) · 무력화 셋 모두 해당 게이트만 빨간불(9-4). 새 적발 없음(병렬이슈36 미발행). 🟢 **통과 — 게시 차단 사유 없음.** 남는 것은 P3 뿐: 알림 생략의 컨트롤러 시험 0 · 가드/쓰기 경합 미실측 · 범위 밖 지문 네임스페이스 관찰(9-6) — 모두 비차단 기록.
