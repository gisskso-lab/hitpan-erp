# 🔍 영역 4 — 매핑 손실 분석 (초안)

> **책임**: 백엔드 매니저 + ERP 매니저
> **PM 1차 분석**: 2026-05-18 17:20
> **대상**: partnerMap / itemMap / employeeMap / accountMap

---

## 📊 9차 마이그 로그 추출 — 매핑 손실 매트릭스

### 진범 #30 (NEW) — DOCF7 차/대변 불균형 23,166건
```
DOCF7 그룹화 — 엔트리=23170 라인=27661 미사용 KCODE=78
| 차/대변 불균형=23166 (헌법 §회계 — 마이그 예외 허용)
```
- **23,170 엔트리 중 23,166이 차/대변 불균형** = 99.98%
- 사장님 5/14 결재 *"마이그 예외: 거래명세서·세금계산서 정합성 무시"* + **회계는 마이그 예외 적용 중**
- 그러나 운영에서 정합성 절대 보장 = AutoJournalHelper의 마이그 분개 중복 가드 정상 작동 확인 필요

### 진범 #31 (NEW) — fallback partner 170건 사용
```
LEGACY_UNKNOWN_PARTNER fallback 거래처 생성
판매 0건, 매입 0건 이관 완료 (fallback partner 사용=170건)
```
- 170건이 partner 매핑 실패 → fallback partner로 INSERT
- 어느 메서드의 170건인지 정확히 박제 필요 (transactions 잡 안일 수 있음)

### 진범 #32 (NEW) — 매출주문 DOCFO 품목 158/222 skip = 71%
```
매출주문(DOCFO→sales_orders) MDB행수=222 헤더그룹=89 INSERT=89
| skip 파트너매핑실패=0 skip 품목매핑실패=158
```
- DOCFO 222행 → 헤더 그룹 89 → INSERT 89 (100%)
- 그러나 **품목 매핑 실패 158건** = DOCFO 한 헤더가 평균 2.5개 라인 → 라인 단위로 71% 품목 skip
- 결과: 수주 헤더는 정상이지만 라인 정보 71% 손실

### 진범 #33 (NEW) — 매입발주 DOCFA 품목 906/1033 skip = 88%
```
매입발주(DOCFA→purchase_orders) MDB행수=1033 헤더그룹=321 INSERT=321
| skip 품목매핑실패=906
```
- 동일 패턴 — 헤더 100% / 라인 88% 손실

### 진범 #34 (NEW) — DOCFB 품목 매핑 실패 92,646건 = 78%
```
DOCFB 그룹화 — 매출헤더=1962 매출라인=5049 매입헤더=115147 매입라인=118451
| skip 빈DT=1 skip 파트너=565 skip 품목=92646
| item샘플=거래명세서|도트용,거래명세서|잉크젯(A형)
```
- DOCFB 매출+매입 라인 합계 = 5,049 + 118,451 = **123,500행**
- 품목 매핑 실패 = **92,646건 (75%)**
- 샘플: `거래명세서|도트용` `거래명세서|잉크젯(A형)`

---

## 🎯 진범 #21 진짜 진짜 본질 — itemMap 키 불일치

### itemKey 패턴 (코드 분석)
```csharp
var itemKey = $"{pum}|{ku}";  // line 3988
```
- pum = `IJ_PUM` (예: "거래명세서")
- ku = `IJ_KU` (예: "도트용")

### items 마스터 키 패턴 (PYOJUN.DOCFS)
```csharp
var dt = ReadMdbTable(oleConn, "SELECT * FROM DOCFS ORDER BY S_PUM, S_KU");
// itemMap key = $"{S_PUM}|{S_KU}"
```
- DOCFS의 (S_PUM, S_KU) 조합 309개만 등록

### 손실 원인 가설
- DOCFB 라인의 (IJ_PUM, IJ_KU) 조합이 **DOCFS 309개에 없음** = itemMap 미발견 → skip
- 92,646건 = DOCFB 라인 중 75%가 마스터에 등록 안 된 품목
- = 레거시 30년 동안 등록·삭제된 품목이 거래 이력에는 남고 마스터에서는 사라짐

### 봉합 방향 (CTO 결재 대상)

| 옵션 | 변경 | 효과 |
|---|---|---|
| **A** | itemMap 미발견 시 → 'LEGACY_UNKNOWN_ITEM' fallback item 사용 (헌법 #20 워크플로우 끊김 0) | 라인 데이터 보존 + 재고원장 무결성 ↑ |
| **B** | 동적 itemMap 보강 — DOCFB 등장 (pum, ku) 조합을 items에 자동 INSERT | items 마스터 폭발 (수천 개 신규) |
| **C** | 라인 skip 유지 + legacy_pum/legacy_ku 컬럼만 보존 (현재 코드 = 부분 C안) | 라인 75% 손실 영구화 |
| **D** | items 마스터를 DOCFS + DOCFB 등장 조합 합집합으로 사전 빌드 | 마이그 시간 ↑ 정합성 ↑ |

**PM 1차 추천: A** — fallback item으로 라인 보존, items 마스터는 그대로 유지, 헌법 #20 정합

---

## 📋 영역 4 5/19 풀 분석 계획

| 시간 | 작업 | 담당 |
|---|---|---|
| 5/19 09:30 | ERP매니저 자문 — 마스터 외 품목 처리 정책 | ERP + 사장님 |
| 5/19 12:00 ~ 18:00 | partner 565건 skip 패턴 분석 (DOCFB) | 백엔드 매니저 |
| 5/19 18:00 ~ 22:00 | DOCFO/DOCFA 라인 품목 skip 분석 | 백엔드 + ERP |
| 5/19 22:00 ~ 5/20 02:00 | 봉합 옵션 A/B/C/D CTO 결재 안건 | PM + AI수석 |

---

**작성**: PM 브라운킴 2026-05-18 17:20
**상태**: 1차 초안. 5/19 09:30 ERP 자문 + 백엔드 풀가동.
