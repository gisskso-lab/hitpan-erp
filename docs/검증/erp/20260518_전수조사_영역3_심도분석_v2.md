# 🔍 영역 3 — MDB 원본 심도 분석 v2 (PM 자율가동)

> **작성**: 2026-05-18 22:30 PM 브라운킴
> **대상**: 진범 #24 (DOCF2/DOCF1 0건 + fallback 170건 진실)

---

## 🎯 진범 #24 진짜 본질 박제

### 9차 로그 박제
```
판매 0건, 매입 0건 이관 완료 (fallback partner 사용=170건)
```

### 코드 분석 (MdbMigrationService.cs line 1444~)
```csharp
foreach (DataRow header in headerDt.Rows)  // DOCF2 헤더 순회
{
    var gubun = GetStr(header, "K2_GUBUN");  // S=판매, B=매입 (예상)
    ...
    fallbackUsed++;  // partner 매핑 실패 시
    ...
    if (gubun.Equals("S", ...)) { salesCount++; ... }
    else if (gubun.Equals("B", ...)) { purchaseCount++; ... }
    // else는? skip!
}
```

### 추론 결과
- **DOCF2 헤더 = 170행 존재** (fallbackUsed=170이 카운트)
- **K2_GUBUN 값이 'S' 또는 'B' 아닌 경우 100%** → if/else if 모두 통과, INSERT 0건
- **가능 K2_GUBUN 값**: '1', '2', '0', 빈값, 'X', 한글값 등

### 진범 #24 봉합 옵션

| 옵션 | 변경 | 효과 |
|---|---|---|
| **A** | K2_GUBUN 값 분포 로그 추가 (분석용) | 진단만, 봉합은 ERP 자문 후 |
| **B** | K2_GUBUN 매핑 확장 — '1'→'S', '2'→'B' 등 ERP 매니저 매핑표 적용 | 본구현 |
| **C** | DOCF2 자체 deprecated 판정 후 잡 제거 | 카드 16개 (현재 17개) |

### PM 1차 추천: **옵션 A 즉시 (진단) + 옵션 B 5/20 본구현**

---

## 🎯 진범 #25 POTHER 3건 본질 박제

### 메서드 진단 (line 4923 / 4991 / 5058)
| 메서드 | MDB 테이블 | DB 테이블 | 9차 결과 |
|---|---|---|---|
| MigrateBusinessCardsAsync | DOCNM | partner_contacts | 0 |
| MigrateServiceTicketsAsync | DOCAS | service_tickets | 0 |
| MigrateDeliveryTrackingAsync | DELIVERY | delivery_tracking | 0 |
| MigrateEventsAsync | CALENDAR | events | **9,870 ✅** |

### 가설
- CALENDAR(9,870)만 성공 → POTHER.mdb 파일은 정상 연결
- DOCNM/DOCAS/DELIVERY 3개 테이블이 모두 0인 원인:
  - 가설 1 (가장 유력): **테이블 자체가 빈 데이터** (옛 사용자가 미사용)
  - 가설 2: **테이블명 다름** (DOC_NM·DOC_AS·DEL_INFO 등)
  - 가설 3: **메서드 그룹화/매핑 실패**

### 진범 #25 봉합 옵션

| 옵션 | 변경 | 효과 |
|---|---|---|
| **A** | 메서드 진입 시 ReadMdbTable 행수 로그 추가 | 가설 1·2 즉시 확인 |
| **B** | GetSchema("Tables")로 POTHER 모든 테이블 목록 로그 | 가설 2 확인 |
| **C** | 잡 자체 제거 + 카드 14개 | 데이터 없으면 정합 |

### PM 1차 추천: **옵션 A + B 즉시 (진단) → 결과 보고 후 C 결재**

---

## 🎯 진범 #32·#33 DOCFO/DOCFA 본질 박제

### 9차 로그 박제
```
매출주문(DOCFO→sales_orders) MDB행수=222 헤더그룹=89 INSERT=89 | skip 품목매핑실패=158
매입발주(DOCFA→purchase_orders) MDB행수=1033 헤더그룹=321 INSERT=321 | skip 품목매핑실패=906
```

### 분석
- DOCFO 222행 → 그룹화 89 → INSERT 89 (헤더 100%)
- 그러나 라인 단위 품목 skip = 158건 (71%)
- DOCFA 1033행 → 그룹화 321 → INSERT 321 (헤더 100%)
- 라인 단위 품목 skip = 906건 (88%)

### 봉합 옵션 — 진범 #34와 동일

옵션 A (LEGACY_UNKNOWN_ITEM fallback) 적용 시 라인 100% 보존 가능.

---

## 📋 영역 3 5/19 풀가동 추가 안건

1. 사장님 PC에서 PANDATA.mdb Access 직접 열기 → K2_GUBUN 분포 캡처
2. POTHER.mdb DOCNM/DOCAS/DELIVERY 행수 캡처
3. ERP 매니저 — DOCFE 컬럼 매핑표 작성

---

**작성**: PM 브라운킴 2026-05-18 22:30
**상태**: 1차 심도 분석. 5/19 09:00 본부장 회의에서 진범 #24·#25 봉합 옵션 발표.
