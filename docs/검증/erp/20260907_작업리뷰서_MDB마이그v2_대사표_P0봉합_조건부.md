# 작업리뷰서 2026-09-07 — MDB 마이그 v2 · W1 대사표 + W2 P0 봉합 (20260904작21)

| 항목 | 내용 |
|---|---|
| 작업지시서(PRD) | `docs/운영기록/20260904작21_MDB마이그v2_대사표_P0봉합_작업지시서.md` · 전결 `docs/운영기록/20260904전결1_MDB마이그v2_Q1-Q12_PM전결서.md` |
| 개발명세서 | `docs/개발/erp/20260907_개발명세서_MDB마이그v2_대사표_P0봉합.md` |
| 실측 짝 | `docs/검증/erp/20260907_검증_MDB마이그v2_베이스라인_봉합전_실측.md`(검1) · `…봉합후_실측_대사표.md`(검2) |
| 구현자 | 갈래 A·B·C 서브에이전트 + PM 브라운킴([3-V] 봉합 5건) |
| **검증자** | 검증팀장 데이비드 박 — 구현·[3-V] 무관 (SoD) |
| **판정** | ⚠️ **조건부 통과** — P0 0건 · **P1 1건**(5월 코드 잠복 · 작21 이 대사표로 드러냄 · 원인 문구 정정 조건) |
| 환경 | `hitpan_e2e`(헌법 #39) · tenant `e2e-mig-baseline-tenant` · 2026-09-07 16:06~16:17 · 도구 `tools/MdbMigrationBaselineRun`(기존 빌드 dll 실행만) · MDB 3개 **읽기 전용**(`Mode=Read` / OLEDB 직독) · `src`·`installer`·`tools` 무수정 · `dotnet build` 0회 |
| 원본 출력 | scratchpad `verify_gate.log` · `verify_full.log` · `verify_0.md`(대조군) · `verify_1.md`((a) 삭제 후) · `verify_2.md`((b) 재이관 후) · `verify_a_deleted_row.txt` · `verify_b_before_counts.txt` / `verify_b_after_counts.txt` · `chain2.log` · `mu_test_{A,B,C}.log` |

## 0. 한 줄

작지서 A0~A10·B1~B2·C1~C4 는 코드 줄로 전부 확인됐고 3관문(빌드 0/0 · 게이트 66/66 + 무력화 · 전체 1,217/1,217 · DB-118 멱등)을 지났다. 독립 반증 10건 중 **대사표는 진짜 잰다**(1행 지우니 OK→DIFF −1) · **재이관 멱등**(지운 행 복귀 · 21표 행수 전부 동일)이 섰다. 그러나 재이관 반증이 **`tax_invoices` 만 값이 바뀌는 자리**(Σamount_total −49,000 · 방향 분포 S/B 가 실행마다 다름)를 드러냈고, MDB 직독으로 원인을 잡았다 — **계산서 21건 누락은 "TX_NO 빈값·불량일자"(명세서 §5·검2 §3-④)가 아니라 같은 `(TX_NO, SEQ, PDT, TX_REM)` 를 가진 매입·매출 21쌍이 `invoice_no` UNIQUE 하나로 합쳐지는 것**이며, 승자는 스테이징 GUID 순서라 실행마다 바뀐다(P1 · 5월 #71 코드). 작21 범위 밖의 잠복 결함이므로 반려가 아니라 **원인 문구를 고쳐 [5] 에 올리고 작22 1번 항목으로 넘기는 조건**을 단다.

## 1. PRD 대비 검증 (지시대로 만들었나)

| PRD 요구 | 명세서 주장 | 실제 확인 (코드 줄을 직접 열었다) | 판정 |
|---|---|---|---|
| A0 순수함수 7개 · 시그니처 고정 | ✅ | `src/HitPan.Application/Services/LegacyMdbMapping.cs:37` `DeliveryKind(int)` · `:53` `LedgerMove(string, decimal)` → `(string, decimal, decimal)` · `:74` `LedgerSourceId(string,string,int,int,int)` · `:91` `TaxDirection(string,string)` · `:112` `JournalSides(decimal,decimal)` → `(decimal,decimal)` · `:129` `PartnerLedgerKind(string)` → `(string,string)` · `:153` `CashbookDirection(string)` — **작지서 A0 자구와 7개 전부 일치**. 규칙도 일치: 음수 반대 칸 절대값(`:57-63`) · 36자 초과 throw(`:77`) · 빈값→TX_GU→"S"(`:92`) · 양쪽 0→(0,0)(`:112-118`) · 수단표 1 cash·2/3 bank_transfer·4 check·5 card·B cash·C bank_transfer·D check·E card·F cash(`:134-143`) · 0 skip/1·3 income/2·4 expense(`:156-162`). W10 이 리플렉션으로 시그니처를 잰다(`MdbMigrationV2MappingGateTests.cs:529-543`) | ✅ |
| A1 P0-A 방향 | ✅ | `MdbMigrationService.cs:4586-4591` `if (io != 1 && io != 2) { skipUnknownIo++; continue; } var isSales = LegacyMdbMapping.DeliveryKind(io) == "sales";` · `:3478` `TaxDirection(io, GetStr(r,"TX_GU"))` → `Direction = direction`. DB: `direction` = `B 749 / S 65,861`(1회차) — 원값 1/2 없음 | ✅ |
| A2 P0-B 수량 | ✅ | `:1960` `"1"/"2"` 외 continue+카운트 · `:1967` `LedgerMove(io, qty)` · `:1997` `UnitCost = absQty != 0 ? Math.Abs(amt)/absQty : 0m`. DB: `Σqty_in 128,926,315.050 · Σqty_out 100,542,678.000` · `qty_in>0 AND qty_out>0` 0행 · 음수 0행 | ✅ |
| A3 P0-C 3갈래 | ✅ | `:1973` `LedgerSourceId(dtStr, io, IJ_SEQ, buyCode, IJ_SUN)` · `:1860` `RegisterUnlistedDocfbItemsAsync` 를 `:268` 1단계 `pyojun_master` 안에서 호출(`Task.WhenAll` `:430` 보다 앞 — W8 이 순서를 잰다) · `:4600` 거래처 미등록 `fallbackPartnerIdLazy ??= EnsureLegacyFallbackPartnerAsync` (종전 `continue` 제거). ⚠️ **작지서 자구 차이 1**: 원장 `:1810 continue 제거` 라 했는데 코드는 `:1949-1955` **카운트+continue 유지**(방어). 실측 로그(`baseline_after2.log:873`) `DISTINCT 1072키 중 신규 855건 · 빈 품명 키 2건` · 원장 제외 경고 줄 **0건**(= 미매핑 0) → 결과 차이 없음 | ✅ (자구 차이 기록) |
| A4 P0-D 회계 | ✅ | `:5318-5329` 양쪽 0 → `zeroBothCount++; continue` · `JournalSides(rawCr, rawDr)` · 음수 카운트. DB: `journal_lines` 27,661 · CHECK 위반 모양(`NOT((d>0 AND c=0) OR (d=0 AND c>0))`) **0행** | ✅ |
| A5 P0-E 수금/지급 | ✅ (`'purchase'` 로 정정) | `:2303` `PartnerLedgerKind(gu)` 3분기 · `S_BAL` 대체 줄 부재(diff `-` 확인) · `:2538` `MigratePaymentsAsync` 신설 — `:2613` `'purchase'` · `memo` `[레거시 S_GU={gu}] {S_REM}` · `source_id` `mig-{buy}-{ymd}-{sun:D5}-{gu}` · 1,000행 multi-row `INSERT IGNORE` · `:318` 별도 잡. DB: `payments` 2,227 · `payment_type` 전부 `purchase` · `memo LIKE '[레거시 S_GU=%'` **2,227** · `collections` 109,818 · 0원 수금 **0행** · MDB 직독 `S_GU IN (1..5) AND S_SUK=0` **0행** | ✅ |
| A5-DDL DB-118 | ✅ | `src/HitPan.API/Migrations/SQL/DB-118_payments_source.sql` 97줄 `@col_exists`/PREPARE 관용구 4블록 · `installer/hitpan_db_clean.sql:2841-2845` 컬럼 3+UNIQUE · `:3498` `('DB-118','clean-ddl',1)` 시드 | ✅ |
| A6 Q9 창고 | ✅ | `:230` `defaultWarehouseId = string.Empty` · `:250` 반환값 대입 · `:878` `EnsureMigrationWarehouseAsync` → `WarehouseLookup.ResolveTenantDefaultWarehouseAsync`(정의 `WarehouseLookup.cs:75`) → 없을 때만 `INSERT IGNORE … 'MAIN','기본창고'`. `"wh-migration"`·`'WH-MIG'` 파일 내 0건(W7). DB: `warehouses` = `MAIN`(is_active 1) 1행 · 원장 `COUNT(DISTINCT warehouse_id)` = **1** | ✅ |
| A7 Q3 현금출납 | ✅ | `:2689` `CashbookDirection(acJen)` · skip 분기 · `Category` `'경비'/'입금'` · `Description = AC_JEK` · `Memo = GetStr(row,"AC_cheri")`(`:2714`). DB: `expense/경비 16,109 · income/입금 3,755` = 19,864 (월계 311 제외) | ✅ |
| A8 Q12 셋트·BOM 원가 | ✅ | `:1207` `ItemMemoWithSet(S_SET, S_DESC)` · `:1329` `BomItemMemo(RT_GU, RT_SON, RT_KUM)`. DB: `items.memo LIKE '[셋트:%'` 16행 · `bom_items.memo` `원가 30000 금액 30000` 등 실재 | ✅ |
| A9 결과 클래스 | ✅ | `:6370` `Payments` · `:6436` `Total` 합산 · `MigrationJobStore.cs:138` · `MigrationController.cs:326` `Payments = result.Payments` | ✅ |
| A10 게이트 | ✅ 66 | `MdbMigrationV2MappingGateTests.cs` G1~G8 51케이스 **함수 실호출**(값 assert) + W1~W12 배선 15 = 66. 무력화 주석 각 테스트에 있음 | ✅ |
| B1 대사표 서비스 + DTO | ✅ | `MdbReconciliationService.cs:71` `BuildAsync(folderPath, mdbPassword, tenantId, ct)` · DTO `:956/:969/:982` 필드명 작지서 그대로 · `Mode=Read`(`:36-39`) · 경로 해석 자체 구현(`:849-868` 절대경로·`..`·대소문자 무시) · 항목 ①~⑩ 26개(`:88-107`) · ⑧⑨ 식이 `FinanceService.cs` `receivable`/`payable` kpiSql 과 **글자 그대로**(`:612-625` ↔ FinanceService 831-842 대조) · 거래처·품목 상위 20(`:42`) | ✅ |
| B2 API + DI | ✅ | `MigrationController.cs:125` `[HttpGet("legacy-mdb/reconcile")]` · `:132` `HttpContext.Items["TenantId"]` · 클래스 `[Authorize(Policy="TenantAdminOnly")]`(`:21`) · `Program.cs:296` `AddScoped<MdbReconciliationService>()` — `#pragma warning disable CA1416` 블록 안(`:293~:301 restore`) | ✅ |
| C1 이관 완료 → 대사표 | ✅ | `MdbMigration.razor:741-743` `_migrationResult` 세팅 뒤 `LoadReconAsync(afterMigration:true)` · `:244` 「레거시와 숫자 맞춰보기」 · 항목/레거시/히트판/차이/판정(✅❌—) · `OkCount/총` · 「차이 N건 — 아래 목록을 확인하세요」 · 거래처·품목 접이식(`MudExpansionPanels`) | ✅ |
| C2 미리보기 → 레거시 열만 | ✅ | `:532-535` 미리보기 성공 직후 `LoadReconAsync(false)` · `:368-` 「가져올 자료 요약」 히트판 열 "이관 전" | ✅ |
| C3 지급 카드 | ✅ | `:435` `_totalTables = 17` · `:453` `new("payments","지급")` | ✅ |
| C4 개발용어 0 | ✅ | 신설 문구에 "대사·마이그레이션" 없음(`:244·:371·:769`) · 기존 "마이그레이션 진행 중"→"이관 진행 중" | ✅ |

## 2. 3관문

| 관문 | 결과 | 근거 (실제 출력) |
|---|---|---|
| 빌드 errors 0 + warnings 0 (#19) | ✅ | `chain2.log` [a] `15:43:21 === [a] solution build === 경고 0개 오류 0개` · [e] `15:46:22 === [e] 원복 재빌드 + 게이트 === 경고 0개 오류 0개 통과! 66/66`. (지시대로 직접 빌드하지 않음) |
| 게이트 66/66 (직접 실행) | ✅ | `dotnet test src/HitPan.Tests/HitPan.Tests.csproj -c Release --no-build --nologo --filter "FullyQualifiedName~MdbMigrationV2MappingGate"` → `통과! - 실패: 0, 통과: 66, 건너뜀: 0, 전체: 66, 기간: 534 ms` (`verify_gate.log`) |
| 전체 테스트 (직접 실행) | ✅ | `dotnet test … --no-build --nologo` → `통과! - 실패: 0, 통과: 1217, 건너뜀: 0, 전체: 1217, 기간: 21 s` (`verify_full.log`). 실패 0 이라 회귀 갈림 불필요 |
| 게이트 무력화 (로그 판독) | ✅ 4자리 / ⚪ 1자리 로그 없음 | `mu_test_A.log`: `LedgerMove` 방향 스왑 → `G2_LedgerMove_방향과_음수_절대값` **5 FAIL**(`실패: 5, 통과: 59, 전체: 64`) · `mu_test_B.log`: `'purchase'`→`'payment'` → `W5_…` **1 FAIL**(`Not found: "'purchase'"`) · `chain2.log` [c] `TotalAmount = supplyTotal + vatTotal` 되돌림 → `W11_명세서_total_amount는_공급가` FAIL(`실패: 1, 통과: 65`) · [d] 발급을 `_migrationFactory.CreateOpenAsync` 직접 → `W12_…` FAIL. 각각 `restored` 뒤 [e] 66/66. `mu_test_C.log` 는 `64/64 통과` — 무력화 없는 실행(원복 확인)으로 읽힘. ⚪ **G1 `DeliveryKind` 스왑은 로그가 없다**(명세서 §4 "worktree 에서 2 FAIL" 주장만) — G1 은 값 assert 라 가짜일 여지가 작지만 **미확인으로 남긴다** |
| ddl-smoke (#36) | ✅ | `mysql hitpan_e2e < DB-118_payments_source.sql` 재적용 → `DB-118: payments.source_type already exists` · `…source_id already exists` · `…migrated_source_hash already exists` · `uq_payments_source already exists` (**skipped 4**). `SHOW CREATE TABLE payments` → `UNIQUE KEY uq_payments_source (tenant_id,source_type,source_id)` · 컬럼 `source_type varchar(30)`·`source_id varchar(80)`·`migrated_source_hash char(64)` |
| 독립 반증 | ⚠️ | §3 — 10건 중 P1 1건 |

## 3. 독립 반증 — 깨뜨리려는 시도

| # | 시도 | 결과 | 판정 |
|---|---|---|---|
| (a) **대사표가 진짜 재는가** | 대조군 먼저: `--skip-migrate --reconcile` → `verify_0.md` `OK 10 / DIFF 16` · `재고원장 행수 124,152 / 124,152 / 0 / OK`. 그다음 `SELECT ledger_id,… ORDER BY ledger_id LIMIT 1` → `131074 · mb-00000000-1-0--58093496-2` 를 골라 `DELETE … ledger_id=131074` (`Query OK, 1 row affected` · `after_cnt 124151`) → 같은 명령 → `verify_1.md` **`OK 9 / DIFF 17`** · `재고원장 행수 124,152 / 124,151 / -1.00 / DIFF` · 순수량·공급가는 OK 유지(지운 행이 qty 0·amt 0) | **초록불이 DB 에서 온다** — 1행에 반응 | ✅ |
| (b) **병합 재이관 멱등** (이관 1회) | (a) 직후 `--reconcile`(이관+대사) `START 16:11:49 … EXIT=0 END 16:16:46` · `verify_2.md` `표별 소요 (총 255.8s)`(2회차 · 기존 데이터 위 · 1회차 389.4s) · 지운 행 **복귀**: `SELECT … WHERE source_id='mb-00000000-1-0--58093496-2'` → `ledger_id 262144`(새 PK · 같은 키) · `stock_ledger` `124152 / Σqi 128926315.050 / Σqo 100542678.000 / Σamt 12724663567.20` · `diff verify_b_before_counts.txt verify_b_after_counts.txt` → **21개 표 행수·합계 전부 동일, 단 1줄 차이** `tax_invoices 66610 8197617041.00 → 66610 8197568041.00`. 중복 검사: 원장 `(source_id,item_id,move_type)` 중복 0 · `payments` `source_id` 중복 0 · 로그 `payments … INSERT 0행 (중복 IGNORE=2227)` · `cashbook INSERT 0행 (IGNORE=19864)` | 행수 멱등 ✅ · **값 비멱등 1표** → §4 #1 | ⚠️ |
| (b-2) **tax_invoices 값이 왜 바뀌나** | 대사표 ③: 1회차 `매출 −15 / 매입 −6`(`verify_0.md`) → 재이관 후 `매출 −8 / 매입 −13 · 매입 공급가 −364,000 · 부가세 −36,400`(`verify_2.md`) — 합은 늘 **21 누락**. DB: `SELECT direction, 2번째 토큰(source_id) …` → `B/1 734 · B/2 8 · S/1 6 · S/2 65,862` = **14행이 source_id 의 방향 토큰과 direction 이 다르다**(예 `mig-1-20070000-0-00000000-04D2EFE9 · S`). MDB 직독(읽기 전용): `TX_NO IS NULL OR ''` → **0행** · `(TX_NO, TX_PDT) GROUP BY HAVING COUNT>1` → **21그룹 / 42행** · TX_NO 중복 652그룹 **전부 매입/매출 교차**. 코드: `invoice_no = {txNo}-{seq}-{pdt}-{TX_REM hash}`(`:3502`) 에 **방향이 없고** 21쌍은 seq 0·같은 PDT·빈 TX_REM(해시 `04D2EFE9` 상수) 이라 같은 `invoice_no` → `uk_tax_invoices_invoice_no (tenant_id, invoice_no)` 에서 한 행으로 합쳐짐. `INSERT … SELECT FROM {stage} ON DUPLICATE KEY UPDATE direction=…, amount_total=…`(`:3817-3833`) 는 **ORDER BY 없이** 스테이징(`LIKE tax_invoices` · PK GUID)을 읽어 **승자가 실행마다 무작위** · `source_id` 는 UPDATE 목록에 없어 첫 행 것이 남는다 | 원인 확정 — 명세서 §5·검2 §3-④ 의 "TX_NO 빈값·TX_PDT 불량일자" 는 **틀림**(빈값 0행) | 🔴 P1 |
| (c) `PartnerLedgerKind` 수단 `note` 기대 자리 | `grep -rn '"note"\|'note''` → `ApprovalService.cs:169 ["note"]="어음"`(라벨 사전) · `CollectionPage.razor:229,275` · `PaymentPage.razor:218,264` `MudSelectItem Value="note"` (입력 선택지) — **`note` 를 조건으로 거는 필터·SQL 0건**. 이관이 내는 값 `cash/bank_transfer/check/card` 는 두 화면 선택지·라벨 사전에 전부 있음. DB: `collections` `bank_transfer 104,540 · card 2,513 · cash 2,735 · check 30` · `payments` `bank_transfer 1,108 · cash 1,093 · check 18 · card 8` | 깨지는 자리 없음 | ✅ (P1 아님) |
| (d-1) `LEGACY_UNKNOWN_ITEM` 폴백 원장 | `… JOIN items … WHERE item_code='LEGACY_UNKNOWN_ITEM'` → **379행 · qty_in 138 · qty_out 1,395 · amt 0.00**. 품목 분포 `MIG-AUTO 858 · other 309 · UNKNOWN 1` | 재고현황에 "레거시 미식별" 한 줄 −1,257 · 금액 0 — 소량 | ✅ 기록 |
| (d-2) 사원 10→11 의 정체 | `SELECT emp_no, emp_name …` → `LEGACY_FALLBACK 레거시이관` + `MIG-0001~0010`. 생성 코드 `:3172 EnsureLegacyFallbackEmployeeAsync`(2026-05-14) · 호출 `:2937 MigrateExpensesAsync`(DOCF7 expenses 의 employee_id NOT NULL 자리표시) | **작21 회귀 아님** — 5월부터 있던 자리표시 사원. 대사표 ⑩이 이것을 세어 영구 +1 | ✅ 규명 · P2 |
| (d-3) 판매 공급가 +33,059.20 · +353건 의 출처 | 폴백 거래처 `LEGACY_UNKNOWN_PTNR` 몫: `sales_deliveries` **356건 · 1,351,241.20**(≠353/33,059.20 — 명세서 §3 "폴백 353건 몫 추정" 은 **틀림**). MDB 직독: `DOCFE IO='2'` `115,150 / ΣAMT1 11,074,294,207` · `DOCFB IO='2'` `118,868행 / ΣIJ_AMT 11,074,327,266.2` · DOCFB `(DT,SEQ,BUY)` DISTINCT **115,503** · **DOCFB 헤더그룹 중 DOCFE 에 없는 것 353그룹 / Σ33,059.2** · DOCFE 중 DOCFB 에 없는 것 0. ERP `sales_deliveries` 115,503 / 11,074,327,266.20 = **DOCFB 와 0 차** | **레거시 내부(헤더표 DOCFE ↔ 라인표 DOCFB) 불일치**. 히트판은 라인표를 100% 옮겼다 | ✅ 규명 |
| (e) `RunTableStepAsync` 구조 변경의 의미 | `:549-637` 추적: 발급이 `try` 안(`:569`) → 발급 실패도 `catch (Exception)`(`:604`) → `failed` 콜백 + `TryInsertMigrationErrorAsync`(jobId 없으면 skip · `Db => _jobConnection.Value ?? _db` 라 conn null 안전) → **`if (!continueOnFail) throw;`(`:618`) 유지** = 마스터 단계(`continueOnFail:false`)는 여전히 전체 중단. 취소: `OpenJobConnectionWithRetryAsync:644` 필터 `when (attempt < 3 && !ct.IsCancellationRequested)` + `Task.Delay(…, ct)` → OCE 가 `catch (OperationCanceledException)`(`:590`) 으로 가 `tx?.Rollback()`(tx null) → `"취소됨"` 콜백 → `throw` — 종전과 동일. `finally`(`:620-636`) `tx?.Dispose` · AsyncLocal 2개 null · `jobConn` null 이면 Dispose skip | 의미 불변 · 취소 존중 · 정리 누수 없음 | ✅ |
| (f) 대사 SQL ↔ DESCRIBE | `DESCRIBE stock_ledger·journal_lines·journal_entries·purchase_receipts·sales_deliveries·payments·collections·tax_invoices·item_stock·employees·items·partners·purchase_returns·purchase_return_items·sales_delivery_items·purchase_receipt_items·bom_headers·bom_items`(18표). **`journal_lines` 컬럼 = line_id, entry_id, tenant_id, account_code, debit_amount, credit_amount, partner_id, memo, created_at, source_id — `source_type` 없음 확인**(서비스 `:484-494` `journal_entries` JOIN 이 맞다) · `purchase_receipts` 에 `is_deleted` 없음 확인(`:181`) · 26항목 SQL 이 참조하는 컬럼 전부 실재 | 추측 컬럼 0 | ✅ |
| (g) #2 · #5 · #15 | `grep -n -i tenant MigrationController.cs` → reconcile 은 `[FromQuery] folderPath, mdbPassword` 뿐 · `:132 HttpContext.Items["TenantId"]` · 클래스 `[Authorize(Policy="TenantAdminOnly")]`(`:21`). PII: `MdbReconciliationService.cs:581` `SELECT buy_code, buy_name FROM DOCF8` 만 · 주민·계좌·비번 컬럼 grep 0. 빈 catch: `:806` OCE 재던짐 · `:810` LogWarning+NA · `:900` `catch { Dispose; throw; }` 재던짐 — 빈 catch 0 | | ✅ |
| (h) #1 삭제 −108줄 전수 | `git diff` `-` 줄 전부 읽음: `"wh-migration"` 상수 · `WH-MIG` INSERT · `io=="I"/"O"` · 2키 `mig-{dt}-{seq}` · `S_BAL` 대체 · `gu switch(note/card)` · `isExpense=true` · `AC_JEN` 접두 description · `Direction = io` · 빈값 폴백 `gu=="2"?"B":"S"` · `if (io==1)` ×2 · `supplyTotal + vatTotal` ×2 · `var dr = rawCr; var cr = rawDr;` · 발급 블록 try 밖 · 주석/로그 서식 | **전부 결재된 판정식의 교체** — 기존 기능·엔드포인트·표 삭제 0 | ✅ |
| (i) 추가 — `JournalSides` 양쪽 비영 행 | `chk_jl_debit_or_credit` = `(d>0 AND c=0) OR (d=0 AND c>0)` **배타**. `JournalSides(5,3)` 은 `(5,3)` 을 돌려주고 `INSERT IGNORE journal_lines`(`:5699`) 가 CHECK 위반을 **조용히 IGNORE** 한다(봉합 전 3,667행이 죽은 것과 같은 통로). MDB 직독 `SC_CR<>0 AND SC_DR<>0` → **0행**(이 MDB) · `양쪽 0` 3행 · 총 27,664 | 이 MDB 는 무해 · 다른 고객 MDB 대비 카운트 없음 — 대사표 ⑥이 잡아 준다 | ⚪ P2 |
| (j) 추가 — 재이관 보고 행수 | 2회차 진행 콜백 `sales_deliveries rows=233,177`(실제 115,503 · 로그 `INSERT IGNORE 결과 231006행 / staging 115503행 (손실 -115503행)`) · `item_stock_rebuild rows=2,142`(실제 1,071) — `ON DUPLICATE KEY UPDATE` 갱신 행의 affected-rows 2 를 그대로 보고 | 화면·로그가 재이관 때 2배·"손실 −115,503" 로 읽힌다 | ⚪ P2 |

## 4. 코드리뷰 발견

| # | 등급 | 위치 | 내용 | 재현 | 봉합 |
|---|---|---|---|---|---|
| 1 | 🔴 **P1** | `MdbMigrationService.cs:3493-3502`(invoice_no 조립) · `:3817-3833`(stage→UPSERT · ORDER BY 없음) · DDL `uk_tax_invoices_invoice_no (tenant_id, invoice_no)` | **계산서 21쌍 병합 + 재이관마다 승자 무작위.** 같은 `(TX_NO, SEQ, PDT, TX_REM)` 를 가진 매입·매출 21쌍이 방향 없는 `invoice_no` 하나로 합쳐져 21건이 사라지고, 어느 쪽이 남는지(direction·금액)가 실행마다 바뀐다. 5월 #71 옵션 F(TX_REM 해시)는 TX_REM 이 빈 쌍을 못 가른다. 명세서 §5·검2 §3-④·작지서 "하지 않는 것" 의 원인 문구("TX_NO 빈값·TX_PDT 불량일자")는 **빈값 0행**으로 반증됨 — 그 위에 세운 작22 대책("NULL 일자로") 도 방향이 틀린다 | §3 (b)·(b-2): 같은 MDB 2회 이관 → `tax_invoices Σamount_total 8,197,617,041 → 8,197,568,041` · 대사 ③ `S −15/B −6 → S −8/B −13` · `direction ≠ source_id 토큰` 14행 | ⬜ **작22 1번 항목** — `invoice_no` 에 방향 토큰 포함(또는 UNIQUE 를 `(tenant_id, direction, invoice_no)` 로) + stage SELECT 에 결정적 ORDER BY. **[5] 전 문서 3곳 원인 문구 정정(PM)** |
| 2 | P2 | `MdbReconciliationService.cs:761` | ⑩ 사원 ERP 건수에 `LEGACY_FALLBACK` 자리표시 사원 포함 → 영구 +1 DIFF | §3 (d-2) | ⬜ 작22 — `emp_no <> 'LEGACY_FALLBACK'` 제외 또는 Detail 에 "자리표시 1 포함" |
| 3 | P2 | `MdbReconciliationService.cs:403-428` ↔ `MdbMigrationService.cs:1829`(`item_name = legacyKey`) | ⑤ 품목 차이 키 불일치 — MIG-AUTO 품목(규격 있음)은 `item_name="품명\|규격"`·`spec NULL` 이라 ERP 키 `품명\|규격\|` ≠ 레거시 `품명\|규격` → 상위 20 목록에 같은 품목이 두 줄 | DB 샘플 `MIG-AUTO-000BFD2BBF7F · 가산세\|<나나나1> · spec NULL` | ⬜ 작22 ⑤ 재정의와 함께 |
| 4 | P2 | `MdbMigrationService.cs` 진행 콜백(`RunTableStepAsync` rows) · `:4862` 부근 로그 | 재이관 시 보고 행수 2배·"손실 −115,503" 문구 (affected-rows 2) | §3 (j) | ⬜ 작22 병합 모드에서 "신규/갱신" 분리 보고 |
| 5 | P2 | `MdbMigrationService.cs:5329` + `:5699` | `JournalSides` 양쪽 비영 → CHECK 배타 위반 → INSERT IGNORE 조용히 탈락(카운트 없음) | §3 (i) · 이 MDB 0행 | ⬜ 작22 — 양쪽 비영 행 카운트+경고(또는 두 라인 분리) |
| 6 | P2 | `MdbMigrationService.cs:895-905` | 활성 창고 0 + 비활성 `MAIN` 잔존이면 비활성 창고에 원장 전량 적재(경고 로그만) | 코드 경로 · 실측 미발생 | ⬜ 관찰 |
| 7 | P2 | `MigrationController.cs:125-129` · `MdbMigration.razor:754` | 비번 쿼리스트링 (병렬이슈 11 · 기존 preview 와 동형) | — | ⬛ 보류 확인 (작22) |
| 8 | 기록 | `MdbMigrationService.cs:1949-1955` | 작지서 A3 "원장 continue 제거" ↔ 코드 "카운트+continue 유지" — 실측 미매핑 0 | §1 A3 | 자구 차이만 · 무해 |
| 9 | 기록 | 명세서 §3 ②·검2 §3 | "+353/+33,059.20 = 폴백 353건 몫 추정" → 실측 폴백은 356건/1,351,241.20 · 진짜 출처는 DOCFE↔DOCFB 레거시 내부 불일치 | §3 (d-3) | 문서 정정(PM) |

**P0 = 0건.** #1 은 데이터 손실·비결정이지만 **5월 코드의 잠복 결함**이고(검1 봉합 전에도 66,610), 작21 의 대사표가 처음으로 숫자로 드러냈다 — 작21 회귀가 아니므로 반려 사유로 쓰지 않는다.

## 5. 거짓봉합 점검

- [x] 봉합 전 증상을 재현해봤나 → 검1(봉합 전 실측)이 P0-A·B·C·D·E 를 숫자로 재현했고, 봉합 후 DB 에서 **직접** 재확인: `Σqty_in/out` 0 아님 · `sales 115,503 / purchase 2,171`(방향) · 원장 124,152 = DOCFB · `journal_lines` 27,661 · `collections` 109,818 / `payments` 2,227 · `WH-MIG` 없음
- [x] 봉합이 다른 곳을 깨뜨리지 않았나 → 전체 1,217/1,217 · (c) 화면 어휘 대조 · (e) 취소·마스터 단계 의미 불변 · (h) 삭제 줄 전수
- [x] 문서에만 고쳤다 쓰고 코드는 안 고친 것 아닌가 → §1 전 항목 코드 줄 인용 · §6 병렬이슈 봉합 자리 코드+DB 로 확인
- [x] 개발명세서 §5(안 한 것)가 비어 있지 않은가 → 12항목 있음. 단 **그중 "계산서 21건 누락" 의 원인 문구가 틀렸다**(§4 #1) · "사원 +1 미확인" 은 본 리뷰가 규명(§3 d-2)

## 6. [3-V] 병렬검증 이슈 확인

| 이슈 문서 | 봉합 확인 (코드 + DB) |
|---|---|
| `docs/검증/병렬이슈/20260907_병렬이슈9_지급어휘_payment가_아니라_purchase_봉합완료.md` | ✅ `MdbMigrationService.cs:2613` `'purchase'` · 게이트 W5 `Assert.Contains("'purchase'")` · `mu_test_B.log` 되돌리면 FAIL · DB `payment_type` 전부 `purchase`(2,227) |
| `…/20260907_병렬이슈10_이관수금_ref_doc_type_비어_미수_영구과대_봉합완료.md` | ✅ `:2359`·`:2436-2444`·`:2488-2503` 3경로+DataTable `'sales_delivery'` · DB `collections` `ref_doc_type='sales_delivery'` **109,818 = 100%** |
| `…/20260907_병렬이슈11_대사API_비번이_쿼리스트링_보류.md` | ⬛ 보류 확인 — 코드 그대로 GET 쿼리(`MigrationController.cs:125-129`) · 기존 preview 와 동형 · 작22 항목으로 명시돼 있음. 반려 사유 아님(신규 노출면 아님·범위 밖 명문화) |
| `…/20260907_병렬이슈12_잡conn발급_타임아웃1건이_이관전체를_죽임_봉합완료.md` | ✅ `:565-574` 발급 try 안 · `:644` 재시도 2·4·8초 · `MigrationDbConnectionFactory.cs:59` `ConnectionTimeout=60` · W12 + `chain2.log` [d] 되돌리면 FAIL · 2회차 이관 15잡 전부 completed |
| `…/20260907_병렬이슈13_명세서_total_amount에_부가세_이중계상_봉합완료.md` | ✅ `:4643`·`:4705` `TotalAmount = supplyTotal` · W11 + `chain2.log` [c] · DB `sales_deliveries Σtotal_amount 11,074,327,266.20 = sales_delivery_items Σsupply_amount 11,074,327,266.20`(부가세 미포함 증명) · `purchase_receipts` 1,650,336,301 = 라인 Σsupply |

## 7. 판정 근거

- 3관문 전부 통과 · P0 0건 · 병렬이슈 봉합분 4건 코드+DB 확인 · 보류 1건은 범위 밖으로 명문화됨.
- ⚠️ **조건부** 인 이유 — §4 #1: 작21 이 만든 대사표가 **레거시 계산서 21쌍 병합 + 재이관 비결정**을 드러냈는데, 명세서·검2·작지서가 적은 원인이 실측(빈값 0행)과 다르다. 그대로 [5] 에 올라가면 사장님이 **틀린 원인 위의 작22 대책("NULL 일자로")** 을 결재하시게 된다.
- **통과 조건 (PM · 코드 무접촉 · 문서만)**:
  1. 명세서 §5 "계산서 21건 누락" 행 · 검2 §3-④ · 작지서 "하지 않는 것" 의 원인을 본 리뷰 §3 (b-2)/§4 #1 로 정정하고, 작22 1번 항목으로 "`invoice_no` 방향 토큰 + stage ORDER BY" 를 적는다.
  2. 명세서 §3 ② "+353/+33,059.20 = 폴백 몫 추정" → "DOCFB 헤더그룹 중 DOCFE 에 없는 353그룹(레거시 내부 불일치)" 으로 정정(§3 d-3).
  3. 명세서 §5 "사원 10→11 원인 미확인" → "`LEGACY_FALLBACK` 자리표시 사원(5/14 · `MigrateExpensesAsync`) — 대사표 ⑩ 제외는 작22" 로 정정(§3 d-2).
- 위 3건이 반영되면 ✅ 통과로 보고 [5] CTO → 사장님 결재로 올린다. 코드 변경이 없으므로 재검증 불필요(문서 대조만).

## 8. 남기는 것 (작22 착수 전 읽을 것)

- 이관 실측 소요: 1회차(빈 DB) 389.4s → 2회차(병합·기존 데이터 위) **255.8s**. 대사표 단독 약 31s.
- 재이관은 행수 기준 멱등(21표 동일 · 중복 0)이나 **`tax_invoices` 만 값이 흔들린다**(§4 #1). 병합 모드(작22)는 이 자리를 먼저 닫아야 "두 번 돌려도 같은 값" 이 선다.
- `LEGACY_UNKNOWN_ITEM` 원장 379행(수량 −1,257 · 금액 0) · 폴백 거래처 명세서 565건(판매 356 · 매입 209) — 사장님 정리 대상(D1 결재 그대로).
- MDB 직독 명령(PowerShell OLEDB `Mode=Read`)은 비번을 코드 주석에서 변수로만 읽었다 — 어떤 파일에도 값이 남지 않았다.

---
**작성**: 검증팀장 데이비드 박 · 실측 2026-09-07 16:06~16:17 · 작성 2026-09-08 / **다음**: PM 문서 정정 3건 → [5] CTO 래리 → 사장님
