# 24. 핵심 16 테이블 컬럼 348개 정공 정독

**작성:** DB 매니저 + 백엔드 매니저
**실측:** `_db_columns_dump.txt` 348행

---

## 1. bom_headers (10 컬럼)
| 컬럼 | 타입 | NULL | KEY | DEFAULT |
|---|---|---|---|---|
| bom_id | varchar(36) | NO | **PRI** | - |
| tenant_id | varchar(36) | NO | MUL | - |
| product_item_id | varchar(36) | NO | - | - |
| bom_name | varchar(100) | NO | - | - |
| bom_version | int(11) | NO | - | 1 |
| is_default | tinyint(1) | NO | - | 1 |
| is_active | tinyint(1) | NO | - | 1 |
| memo | text | YES | - | - |
| created_at/updated_at | datetime(6) | NO | - | current_timestamp(6) |

## 2. bom_items (9 컬럼)
- bom_item_id PRI / bom_id MUL / tenant_id MUL
- seq_no(int 기본 1), material_item_id, qty(decimal 10,2 기본 1.00), unit('EA'), loss_rate(5,2 기본 0)

## 3. collections (14 컬럼) — **614,212행 (마이그 본)**
| 컬럼 | 타입 | 코멘트 |
|---|---|---|
| collection_id | varchar(36) PRI | 수금 PK |
| tenant_id | MUL | 테넌트 ID |
| partner_id | varchar(36) | 거래처 ID |
| collection_date | date | 수금일 |
| amount | **decimal(15,2)** | 수금액 (헌법 #4) |
| collection_method | varchar(20) 기본 'cash' | cash/bank_transfer/check/card/note |
| ref_doc_type | varchar(30) NULL | delivery / tax_invoice |
| ref_doc_id | varchar(36) NULL | 관련 문서 ID |
| memo | varchar(500) NULL | |
| is_active | tinyint(1) 기본 1 | soft delete |
| created_at/by, updated_at/by | | |

## 4. employees (54 컬럼) ★ AES 3컬럼 + 형사영역
**기본:**
- employee_id PRI / tenant_id MUL / user_id / emp_no / emp_name / dept_id / position / job_title
- emp_type **longtext** (enum 인코딩) / join_date / resign_date

**민감정보:**
- birth_date varchar(200) — **AES 적용 (Value Converter)**
- id_no_hash varchar(256) — SHA256 (주민번호 NOT)
- bank_account varchar(200) — **AES**
- base_salary varchar(200) — **AES**

**🔴 AES VARBINARY 형사영역 4컬럼 (헌법 #5):**
- resident_no_encrypted **varbinary(255)** — 주민번호 (소득세법 §127·§164, 4대보험)
- salary_encrypted **varbinary(255)** — 급여 (근로기준법 §48, 개보법 §29)
- salary_extra_encrypted **varbinary(500)** — 급여 기타
- (salary_type tinyint, salary_category tinyint, salary_country tinyint — 평문 코드만)

**legacy_bal1~10 varchar(150):** 레거시 잔액 10개 (마이그 호환)

**연차 관리:**
- annual_leave_total **decimal(5,1)** — 부여 일수
- annual_leave_used **decimal(5,1)** — 사용 일수

**기타:** address, zip_code, birth_calendar (1=양력 2=음력), home_phone, emergency_contact, memo, department, marriage_status, business_type, is_resigned, resign_reason, nationality

## 5. items (40 컬럼)
- item_id PRI / tenant_id MUL / item_code / item_name / item_type **longtext**
- 단가 7종: std_price, price_a~e, cost_price, **purchase_price (decimal 15,2 기본 0)**, **sale_price**, **standard_price**
- tax_type 기본 'taxable' / safe_stock decimal(15,3) / safety_stock decimal(10,2) 중복
- is_active / is_deleted (soft delete) / row_version (낙관적 잠금)
- **auto_order_enabled / auto_order_partner_id / auto_order_qty / auto_receive_on_order** — 안전재고 자동발주 (헌법 #20)
- spec, spec_detail, unit, unit_secondary, reorder_point, supplier_default_id
- item_group, barcode

⚠️ **중복 컬럼: purchase_price ↔ cost_price / sale_price ↔ std_price ↔ standard_price / safe_stock ↔ safety_stock** — 정공법: 마이그 시 데이터 일관성 검증 필수

## 6. journal_lines (9 컬럼) — INSERT ONLY (헌법 #3)
- line_id **bigint(20) PRI 자동증분** (UUID 아님)
- entry_id MUL / tenant_id MUL
- account_code varchar(10) — '40100' 매출, '25500' 부가세예수금, '10800' 외상매출금 등
- **debit_amount / credit_amount decimal(15,2) 기본 0** — CHECK 제약: 둘 다 0 금지
- partner_id NULL, memo varchar(200) NULL
- created_at current_timestamp(6) — **UPDATE/DELETE 금지**

## 7. migration_checkpoints (18 컬럼) — 옵션 B 인프라
| 컬럼 | 타입 | 의미 |
|---|---|---|
| checkpoint_id | char(36) PRI | UUID |
| job_id | char(36) MUL | migration_jobs FK |
| tenant_id | char(36) MUL | |
| mdb_file | varchar(50) | PYOJUN.MDB / PANDATA.mdb |
| table_name | varchar(50) | DOCF8, DOCFB, etc |
| table_order | smallint unsigned | 처리 순서 |
| status | **enum('pending','running','done','failed','skipped')** 기본 pending | |
| total_rows | int unsigned 기본 0 | |
| processed_count | int unsigned 기본 0 | 진행 |
| **last_pk_value** | **longtext utf8mb4_bin** | JSON 체크포인트 |
| chunk_size | smallint unsigned 기본 **1000** | AIMD 동적 |
| started_at / completed_at | datetime | |
| avg_commit_ms | int unsigned 기본 0 | AIMD 입력 |
| last_error | text | |
| retry_count | tinyint unsigned 기본 0 | 3회 초과 시 사장님 알람 |
| created_at / updated_at | datetime current_timestamp() | |

**현재 행수: 0** — 옵션 B에서 활용 예정

## 8. migration_errors (20 컬럼) — 🔴 raw_data AES 미구현
| 컬럼 | 타입 |
|---|---|
| error_id char(36) PRI | |
| job_id MUL / tenant_id MUL / checkpoint_id NULL | |
| mdb_file / table_name / row_pk_value longtext | |
| row_offset | int unsigned NULL |
| **error_type** | **enum('encoding','fk_missing','duplicate','schema','constraint','timeout','other')** |
| **error_severity** | **enum('warning','error','critical')** 기본 error |
| error_code varchar(20) | E001~E007 |
| error_message text NO | |
| error_detail text NULL | |
| **raw_data longtext utf8mb4_bin** | **🔴 AES 미구현 (헌법 #5 P0)** |
| is_resolved tinyint unsigned 기본 0 / resolved_at / resolved_by / resolution_note | |
| occurred_at / created_at datetime | |

## 9. migration_jobs (21 컬럼) — **현재 9행 (5/14 새벽 9회 시도)**
| 컬럼 | 의미 |
|---|---|
| job_id char(36) PRI | |
| tenant_id MUL / initiated_by | |
| source_folder varchar(500) | C:\HITWIN |
| **status** | **enum('pending','preview','running','paused','completed','failed','canceled')** 기본 pending |
| total_tables / completed_tables smallint | 16개 추적 |
| total_rows / processed_rows / skipped_rows / error_rows | int unsigned |
| preview_at / started_at / paused_at / completed_at | |
| error_summary text | |
| **checkpoint_data longtext utf8mb4_bin** | JSON |
| client_ip varchar(45), user_agent varchar(255) | |
| created_at / updated_at | |

## 10. partners (49 컬럼) ★ AES 1컬럼 (대표 주민번호)
**기본:**
- partner_id PRI / tenant_id MUL / partner_code / partner_name
- partner_type **longtext** / biz_no / ceo_name / biz_type / biz_item
- tel, fax, email, address, address_detail
- credit_limit decimal(15,2) / payment_terms tinyint
- bank_name, bank_account, account_holder
- is_active / is_deleted / row_version
- zip_code / manager_name / manager_tel / **price_grade char(1) 기본 'A'**
- tax_type 기본 'taxable'

**🔴 AES (헌법 #5):**
- **ceo_resident_no_encrypted varbinary(255)** — 대표 주민번호 (buy_topjumin, 부가세법 §32)

**레거시 컬럼 (마이그 호환):**
- card_commission_rate (buy_cardyul)
- classification_code (buy_ccode), manager_department (buy_damdangbu)
- **price_grade_code (buy_DOSCODE)** — 5/12 옵션 H 결정 근거
- legacy_extra (buy_fil), discount_rate (buy_halyul)
- keyman_birth/name/phone (buy_keybirth/keyname/keytel)
- margin_rate (buy_mayul), sales_employee (buy_sawon)
- trade_start_date (buy_startdt), business_registration_date (buy_taxdt)
- tel_secondary (buy_tel1), tax_classification (buy_taxgubun)

## 11. purchase_orders (18 컬럼)
- po_id PRI / tenant_id MUL / po_no
- partner_id MUL / employee_id NULL / po_date / expected_date
- **status enum('draft','ordered','partial','received','cancelled') 기본 'draft'** MUL
- total_amount / vat_amount decimal(15,2)
- memo / is_auto tinyint 기본 0 (자동발주 표시)
- created_at/by, updated_at/by, is_deleted, deleted_at

## 12. sales_deliveries (19 컬럼) — **66,692행 마이그**
- delivery_id PRI / tenant_id MUL / delivery_no
- order_id NULL (수주 FK) / partner_id MUL / employee_id NULL
- delivery_date / source_type varchar(20) / status **longtext**
- total_amount / vat_amount decimal(15,2)
- memo / created/updated/deleted
- **tax_invoice_id MUL** — 세금계산서 역참조 (미발행 시 NULL, 취소 시 NULL 환원)

## 13. sales_orders (17 컬럼)
- order_id PRI / tenant_id MUL / order_no
- partner_id MUL / employee_id NULL / order_date / delivery_date
- **status enum('draft','quotation','order','confirmed','partial','closed','invoiced','cancelled')** 기본 draft, MUL
- total_amount / vat_amount
- is_auto (자동 사슬 표시)

## 14. stock_ledger (17 컬럼) — **116,420행, 헌법 #3 INSERT ONLY** ★
| 컬럼 | 타입 | 정공 |
|---|---|---|
| **ledger_id bigint(20) PRI** | **자동증분** | UUID 아님 |
| tenant_id varchar(36) MUL | | |
| item_id varchar(36) | | |
| warehouse_id varchar(36) | | |
| partner_id NULL | | |
| employee_id NULL | | |
| ledger_date date | | |
| ym varchar(7) | YYYY-MM 집계용 |
| **move_type longtext** | in/out/adjust |
| source_type varchar(30) | purchase_receipt/sales_delivery/transfer/adjustment |
| source_id varchar(36) | 출처 ID |
| doc_no varchar(20) NULL | |
| **qty_in decimal(15,3)** | 입고량 |
| **qty_out decimal(15,3)** | 출고량 |
| unit_cost decimal(15,4) NULL | 단가 (4자리) |
| supply_amount decimal(15,2) NULL | 공급가 |
| memo varchar(200) NULL | |

**UPDATE/DELETE 트리거 없음 ✅ 헌법 #3 준수**

## 15. tax_invoices (21 컬럼) — **66,603행**
- invoice_id PRI / tenant_id MUL
- **delivery_id UNI** — sales_deliveries FK (1:1 강제, 헌법 #20)
- invoice_no varchar(32) — 테넌트별 고유
- issued_at datetime(6) / issued_by user_id
- amount_total / vat_total decimal(15,2)
- **status varchar(16) 기본 'issued'** MUL — issued | canceled
- **etax_status varchar(16) 기본 'pending'** — pending | issued | failed
- etax_issued_at / etax_scheduled_at / etax_confirmed_at datetime(6) NULL
- idempotency_key varchar(64) NULL
- **etax_mode varchar(20) 기본 'scheduled'** — scheduled/immediate
- hometax_approval_no / hometax_response text — 홈택스 응답
- **amendment_of MUL** — 수정계산서 원본 PK
- amendment_reason varchar(200)

## 16. warehouses (9 컬럼)
- warehouse_id PRI / tenant_id MUL
- wh_code / wh_name / wh_type **longtext** (enum 인코딩)
- location / is_active / created_at / updated_at

---

## 종합 발견

### 1) 헌법 #5 AES VARBINARY 5컬럼
- partners.ceo_resident_no_encrypted (대표 주민)
- employees.resident_no_encrypted / salary_encrypted / salary_extra_encrypted
- (etax_send_history.raw_response_encrypted — 별도 테이블)
- 🔴 **migration_errors.raw_data — AES 미구현 (longtext 평문 가능, P0 봉합 필수)**

### 2) 헌법 #4 decimal 검증
- 모든 금액 컬럼 decimal(15,2) ✅
- 재고: decimal(15,3) / 단가: decimal(15,4) — 정밀도 확보

### 3) 헌법 #3 INSERT ONLY 검증
- stock_ledger: UPDATE/DELETE 트리거 0건 ✅
- journal_lines: 동일 ✅
- 일반 마스터: soft delete (is_deleted=1) 패턴

### 4) longtext로 인코딩된 enum
- emp_type, item_type, partner_type, wh_type, move_type, status (sales_deliveries) — EF Core 매핑 시 string 변환
- 명시 enum: purchase_orders.status, sales_orders.status, migration_jobs.status, migration_checkpoints.status, migration_errors.error_type

### 5) 중복 컬럼 (마이그 호환 흔적)
- items: purchase_price/cost_price, sale_price/std_price/standard_price, safe_stock/safety_stock
- employees: legacy_bal1~10

### 6) 마이그 인프라 4종 (W3 옵션 B 준비 완료)
- migration_jobs (9행), migration_checkpoints (0), migration_errors (0), etax_send_history (0)
- **모든 컬럼 DDL 준비됨 — 옵션 B 즉시 적용 가능 (헌법 #9 DB 미리 다)**
