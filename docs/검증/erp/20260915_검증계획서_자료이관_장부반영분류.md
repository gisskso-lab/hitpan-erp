# 검증계획서 — 20260915작1 자료이관 머리 없는 줄 · 장부 반영 분류 봉합

| 항목 | 내용 |
|---|---|
| 루틴 | [4] 검증팀장 데이비드 박 + [3-V] 보안상무 안철수 — [3] 과 동시 착수 (2026-09-15) |
| 대상 | 브랜치 `feat/20260915-migration-posting` · 갈래 F·A·G 구현 중(worktree) → 2차 B·D·E → 3차 H·C |
| 근거 | 작업지시서 [`20260915작1`](../../운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md) §11·§13 · 설계 [`20260915_설계`](../../설계/erp/20260915_설계_자료이관_머리없는줄_분류봉합.md) §14~§18 |
| 원칙 | 대사표(C)가 스스로 맞다고 말하는 값은 증거 아님 → **MDB 사본 쪽 독립 계산 ↔ 테스트 DB 쪽 독립 SQL** 두 줄기를 서로 다른 도구로 대조 · 운영 DB(`hitpan_erp_t004` 등) 무접촉(#39) · 코드 수정 0 |
| 상태 | 🟡 계획 확정 · 사전 실측 1회 완료(§2) · 구현 도착 시 §3~§5 실행 |

## 1. 두 줄기 도구

- **L (레거시)**: `scratchpad\mdbq.ps1`(Access SQL, 읽기 전용) + `f3check.ps1`(DOCF5 전량 메모리 계산) + `r62check.ps1`(DOCFC 품목 끝전). 이관 코드(`LegacyMdbMapping`)를 **부르지 않는다** — 같은 함수로 기대값을 만들면 대조가 아니다.
- **H (히트판)**: 격리 테스트 DB(ddl-smoke 로 출하 DDL 적용한 새 DB) 에 **덮어쓰기 재이관**(`/start` mode=overwrite → `DataResetService.ResetAllAsync` → 이관) 후 MySQL 클라이언트로 직접 SQL. 대사표 API 응답은 마지막에 L·H 와 3자 대조만.
- 대조군: 봉합 전 코드(origin/main `564ec5d7`) 로 같은 MDB 를 **병합 모드** 이관한 두 번째 테스트 DB (excluded_leftover 562 확인용).

## 2. 사전 실측 (L 줄기 · 2026-09-15 · 설계 수치 재현 여부)

| 항목 | 설계 | 독립 실측 | 판정 |
|---|---|---|---|
| 판정 매트릭스 (묶음 = DISTINCT IJ_DT·IJ_IO·IJ_SEQ·IJ_BUY · 머리 = DOCFE 4키 · 연결 = DOCF5 이월 아닌 줄 (S_YMD,S_SSUN) + S_GU 0/A) | 매입 1,962 / 0 / 0 / 210 · 판매 115,148 / 2 / 0 / 353 | 같음 (매입 순번0 128 + 순번>0 82 = 210 · 판매 253 + 100 = 353) | ✅ |
| F3 미수 / 미지급 | 142,062,113 (1,059) / −51,577,479 (530) | 같음 (거래처 11,813 · 이월행 전부 S_GU 0) | ✅ |
| 재고 품목 / 수량 / 금액 (품목·규격 Trim+소문자, 창고별 마지막 DOCFC 행 합) | 1,192 / 3,147,291 / 54,753,974.4 | 같음 · 여러 창고 품목 10 | ✅ |
| avg_cost 2자리 끝전 | −0.11 | −0.11 | ✅ |
| avg_cost 6자리 | 차 0 | 품목별 ROUND(q×avg6,2) 합 = 차 0 · **반올림 없는 SUM(q×avg6) = 54,753,974.399974** | ⚠️ 비교식 주의 → 병렬이슈37 |

- 새로 드러난 사실 → 병렬이슈 36~40 (§6).

## 3. §11-4 기대값 — 항목별 독립 실측법

| # | 항목 · 기대 | L (MDB 사본) | H (테스트 DB · 재이관 후) | 화면 |
|---|---|---|---|---|
| E1 | 판매 명세서 115,150 / 11,074,294,207 / 1,029,201,157 | 반영 묶음(§2 매트릭스 머리O∨연결O) DOCFB SUM(IJ_AMT)·SUM(IJ_VAT) + **DOCFE 쪽 SUM(IJA_AMT*)** 따로 → 둘 차 0 | `SELECT COUNT(*),SUM(total_amount),SUM(vat_amount) FROM sales_deliveries WHERE tenant_id=? AND source_type='migration' AND is_deleted=0` + 줄 합 `sales_delivery_items` 따로 (머리합 = 줄합) | 매출현황 기간 전체 합계 |
| E2 | 매입 1,962 / 1,650,336,301 / 156,703,141 | 같은 식 IO 1 | `purchase_receipts`·`purchase_receipt_items` 같은 식 | 매입현황 |
| E3 | 보관 표 판매 353건 414줄 33,059.20 / −5 · 매입 210건 235줄 0 / +1 | 머리X∧연결X 묶음 DOCFB 줄 수·합 | `legacy_unposted_documents` io_type별 COUNT·SUM · `_lines` COUNT (doc_id 조인 **tenant_id 포함**) · 헤더합 = 줄합 | H 화면 합계 = SQL |
| E4 | 124,152 = 123,503 + 649 | `SELECT COUNT(*) FROM DOCFB` | 명세서 줄 두 표 + 보관 줄 COUNT · **교집합 0**: `source_id` 가 `sales_deliveries` 와 `legacy_unposted_documents` 양쪽에 있는 행 0 | — |
| E5 | 날짜 없는 3묶음 doc_date 2026-02-28 | `IJ_DT IN ('00000000','00000001')` 묶음 | `WHERE legacy_dt IN ('00000000','00000001')` → doc_date 전부 2026-02-28 · stock_ledger 해당 줄 ledger_date 도 2026-02-28 (`now` 아님) | — |
| E6 | 재고원장 124,152 · 입 128,926,315.05 · 출 100,542,678 | DOCFB IO별 SUM(IJ_QTY) | `stock_ledger WHERE source_type='migration'` 에서 **맞춤 줄 제외** COUNT·SUM(qty_in)·SUM(qty_out) | 재고원장 화면 1품목 표본 |
| E7 | 맞춤 줄 Σ −25,236,346.05 · 행수 ⚠️ | 품목별 (DOCFC 최종 − DOCFB 순수량) ≠0 품목 수 = 행수 기대 | 맞춤 줄 SUM(qty_in−qty_out) · COUNT | — |
| E8 | 재고 3,147,291 · 품목 불일치 0 (1,192) | `r62check.ps1` 품목별 표 CSV | `item_stock` 품목(tenant,item) 합 CSV → **품목별 전행 조인 diff 0** (합계만 비교 금지) | 재고현황 합계 |
| E9 | 재고 금액 54,753,974.4 | 품목별 ROUND(CAMT,2) 합 | `SUM(ROUND(current_qty*avg_cost,2))` **와** `StockService.cs:486` 식 그대로 `SUM(current_qty*avg_cost)` 둘 다 기록 | 재고현황 금액 표시 자리수 |
| E10 | 미수 142,062,113 (1,059) / 미지급 51,577,479 (530) | `f3check.ps1` 거래처별 CSV | KPI 식(`FinanceService.cs:833·843`) SQL 복사 + `partner_legacy_balances` 합산 → 거래처(legacy_buy_code) 별 diff 0 · **폴백 병합 곳**은 L 쪽 코드 합산 후 비교 | 대시보드 미수 · 거래처원장 1곳 |
| E11 | 계산서 발행 가능 이관 명세서 0 | — | API `POST 계산서 발행` 이관 명세서 3표본 → 거부 문구 · 사람 명세서 1 → 성공 | 거래명세서 화면 발행 버튼 문구 |
| E12 | excluded_leftover 명세서 562 / 새 DB 0 | 머리X∧연결X 묶음 중 봉합 전 코드가 명세서로 만든 수(판매 353 · 매입 209 — 00000000 1건 skip 차) | 대조군 DB: `sales_deliveries`·`purchase_receipts` 중 source_id 가 보관 표에도 있는 행 | 대사표 표시 |
| E13 | 상품 수 ⚠️ | DOCFA 품목 + DOCFC 전용 324 (Trim+소문자 키) | `items WHERE source_type='migration'` COUNT | — |

- 모든 H 수치는 **덮어쓰기 재이관 2회** 연속 실행 뒤 한 번 더 찍는다(멱등: 명세서·보관 표·맞춤 줄·partner_legacy_balances 행수 증가 0).

## 4. 게이트 G1~G7 — 「봉합 빼고 FAIL」 확인법

공통: 각 게이트를 ① 봉합 코드 PASS ② 봉합 한 줄 되돌린 사본(스크래치 브랜치) FAIL ③ DB 없는 로컬에서 SKIP 이면 FAIL 처리되는지(`HITPAN_REQUIRE_DB=1`) ④ `.github/workflows/build-and-test.yml` GATES 에 이름 등록 — 넷을 PR check-run 에서 확인. 대조 두 쪽을 같은 파서로 읽지 않는다.

| 게이트 | 되돌릴 봉합(무력화) → 기대 FAIL | 대조군 (봉합 있어도 PASS 여야) | 추가 요구 (§6 반영) |
|---|---|---|---|
| G1 Classify | (a) 종류 조건 제거 → 수금 줄 키 함정 FAIL (b) 키에 거래처 추가 → 20251111 SEQ5·2015-05-12 FAIL (c) `hasHeader ||` 제거 → 머리O·연결X FAIL (d) R5 분기 제거 → DOCFE 없음 FAIL (e) F3 를 「마지막 달 이월만」·F1 로 → FAIL (f) 기준일 `now` → FAIL | 머리O·연결O 일반 묶음 반영 | **순번 0 묶음 + 같은 날 매출세액 메모행(S_SSUN 0) → 미반영** (병렬이슈36) |
| G2 AllPaths | 보관 경로 호출 제거 → 줄 합 FAIL · 원장 판정 끼우기 → 원장 줄 FAIL | 반영 2 묶음 명세서 그대로 | 보관·명세서 source_id 교집합 0 단언 |
| G3 FinalStock | 대소문자 구분 키 → FAIL · 사람 입력 포함 → FAIL · 재실행 행 증가 | 사람 입력 원장 줄 보존 | **`item_stock_rebuild`(:571) 뒤 avg_cost 가 레거시 값 유지** · 비교는 ROUND 2 (병렬이슈37) |
| G4 Balance | 맞춤 제거 → KPI≠F3 FAIL · 재실행 2배 → FAIL · SKIP → FAIL | 사람 입력 수금 거래처 KPI 불변 · F1 과 다른 5곳 | **판매·매입 양쪽 거래처** 대조군 · 폴백으로 partner_id 바뀐 재이관 (병렬이슈39·40) |
| G5 MigratedLock | 발행 가드 제거 → 이관 발행 성공 FAIL · 취소 가드 제거 → FAIL | 사람 명세서 발행 성공 · 반품→마이너스 계산서(`IssueCreditNoteAsync`) 성공 · 수금 등록 성공 | **R-B2: `UpdateDeliveryAsync`·`CancelConfirmedDeliveryAsync`·`DeleteDeliveryAsync`(confirmed→취소 위임) 거부** (병렬이슈39) · 월합계 미발행분 결재 결과 (병렬이슈38) |
| G6 ReconPosting | 보관 줄 열 제거 → FAIL · F3 열을 KPI 로 → FAIL · SKIP → FAIL | 명세서 대사 기존 열 불변 | 레거시 열이 `LegacyMdbMapping` 아닌 독립 식과도 일치하는지는 [4] L 줄기로 |
| G7 DDL | ENGINE/collation 바꾼 DDL → FAIL · DB-121 칼럼 하나 빼기 → FAIL · PreservedTables 에 넣기 → 초기화 뒤 행 남음 FAIL | 기존 표 ddl-smoke PASS | **`partner_legacy_balances` 표 · `item_stock.avg_cost` decimal(19,6) 도 출하 DDL = 마이그** (병렬이슈39) |

## 5. [3-V] 코드 도착 시 확인 목록

- #2 tenant: H 컨트롤러·서비스에 tenant_id 파라미터 수신 0 (JWT 클레임만) · 두 표 조회 모두 `WHERE tenant_id=@T` · 줄 조인 `ON l.doc_id=d.doc_id AND l.tenant_id=d.tenant_id` · 거래처 이름 조인도 tenant 포함 · 다른 테넌트 doc_id 로 줄 펼침 요청 → 0행(격리 DB 두 테넌트 실측).
- #4: avg_cost·금액 경로 `double`/`float`/`Convert.ToDouble` 0 (사전 grep 0건 — 신규 파일 재grep) · razor 표시 포맷.
- #5: 새 표에 암호화 대상 칼럼 없음 확인(거래처명·메모 평문 저장 범위가 기존 명세서 표와 같은지).
- #18·#22: 보관 표·잔액 표가 백오피스 동기화 대상(Pull 목록)에 안 들어감.
- #23: 화면·문구에 금지어 0 · 개발용어(「보관 표」「F3」) 고객 노출 0.
- #3: stock_ledger 맞춤 줄 INSERT ONLY · 재실행 시 UPDATE/DELETE 0.
- #36: DB-121 = 출하 DDL (칼럼·키·collation) · 마이그 번호 origin/main 재확인.

## 6. 병렬이슈 (이 계획서 작성 중 적발)

| # | 등급 | 갈래 | 한 줄 |
|---|---|---|---|
| [36](../병렬이슈/20260915_병렬이슈36_연결키가_순번0_매출세액메모행에_붙어_미반영묶음을_반영으로_오판한다.md) | P1 | A (G1) | 연결 키가 순번 0 을 안 거른다 — DOCFB 순번0 381묶음 · DOCF5 판매 순번0 36,725줄(매출세액 메모) · 이 MDB 는 날짜 겹침 0 이라 실현 0 |
| [37](../병렬이슈/20260915_병렬이슈37_이관끝_재고리빌드가_avg_cost를_입고평균으로_덮어_끝전기대값이_깨진다.md) | P1 | B·D (G3) | `item_stock_rebuild`(:571-585) 가 avg_cost 를 입고 가중평균으로 덮는다 · 6자리 SUM 은 .399974 |
| [38](../병렬이슈/20260915_병렬이슈38_계산서잠금이_기준일직후_미발행_이관명세서의_월합계발행을_끊는다.md) | P2 · 재결재 | G (G5) | 레거시 미발행(`IJ_TAXNO` 00000000) 명세서도 발행 거부 — 월 ~145건 · 2026-02 1건 140,000 |
| [39](../병렬이슈/20260915_병렬이슈39_13절확정이_게이트와_DDL명세에_안들어갔다_잔액표키_RB2_매입경로.md) | P1 | F·E·G (G4·G5·G7) | §13 확정(R-A2·R-B2·R6-2)이 §11-3 게이트·§15 DDL 에 없음 · 잔액 표 UNIQUE 키 미정 · 매입 확정취소 경로 코드에 0 |
| [40](../병렬이슈/20260915_병렬이슈40_F3는_거래처순잔액인데_미수미지급을_부호로_갈라_양쪽거래처_배정규칙이_없다.md) | P2 | E·C (G4·G6) | 이월행은 S_GU 0 한 칸 순잔액 · 마지막 이월 뒤 판매·매입 양쪽 거래처 284곳 |

## 7. ⚠️ 미확인 (턴 한도 · 코드 미도착 — 구현 도착 시 §3~§5 로)

- ④ 새 두 표 · `partner_legacy_balances` 에 tenant_id 가 실제로 있고 DataReset 대상인지 · 병합 모드 재이관 누적 (F·E 코드 미도착 → 병렬이슈39 요구로만).
- ⑤ avg_cost scale 6 이 화면 반올림(재고현황 금액·단가 표시 포맷)에 주는 영향 — 사전 grep 으로 double/float 경로 0 만 확인. razor 포맷 미확인.
- ⑥ 보관 표 조회 화면(H) tenant 격리 — 갈래 H 미착수. §5 첫 줄 목록으로 실측 예정.
- ③ R-B2 이관 명세서 status('confirmed' 여부) 와 `UpdateDeliveryAsync` draft 가드의 실효 · 이관 계산서(`tax_invoices.source_type`) 존재 여부.
- 폴백 거래처 병합 뒤 F3 곳 수 · 맞춤 줄 행수 · 상품 수 · `item_stock.current_qty decimal(10,2)` 한도.
