# 14. Application Services 전수 학습서

**작성:** 백엔드 매니저
**범위:** `src/HitPan.Application/Services/` 44개 + `Interfaces/` 45개 + `Common/` 2개

---

## 0. Services 파일 44개

PartnerService, SalesService, PurchaseService, CollectionService, FinanceService, ApprovalService, ItemService, TenantService, ExcelImportService, CompanyService, PdfExportService, HrService, AuditService, MonthlyClosingService, FileSecurityHelper, PermissionService, SettingsService, MonthlySummaryGuard (멱등 가드), ApprovalTriggerHelper, DocumentNumberHelper (채번), EmployeeService, LeaveRequestService, PositionService, ApprovalLineService, ManualBillingProvider, BillingService, ReportService, BackupService, BillsCardsBankService, EmailService, PdfRenderService, QuotationService, UserService, TaxInvoiceService, ExcelExportService, ResellerService, BackofficeAuthService, BomService, AutoJournalHelper (자동 기표), StockService, AuthService, MigrationJobStore, MdbMigrationService.

## 1. 핵심 서비스 요약

### PartnerService (692줄)
- DI: ICurrentTenant, IPartnerBalanceRepository, IDbConnection
- 메서드: GetBalance, SearchPartners, GetListPaged(신규), Create/Update/Delete, SpecialPrice CRUD
- 헌법 #25 페이지네이션: GetPartnerListPagedAsync(req.PageSize 1~500 clamp)

### SalesService (1993줄)
- DI: IUnitOfWork, IDbConnection, IAuditService, IEventPublisher?
- ConfirmDeliveryAsync 8단계 트랜잭션:
  1) 상태 검증 + 월마감 체크
  2) EF+Dapper 공유 tx 시작
  3) stock_ledger OUT 기록
  4) item_stock UPSERT 감소
  5) MonthlySummaryGuard.TryApplyAsync (멱등)
  6) sales_order_items 배송수량 동기화
  7) AutoJournalHelper.RecordSalesConfirmAsync (자동 기표)
  8) partner_balance UPSERT 매출 가산 → Commit
- 이벤트 발행: SalesConfirmedEvent (안전재고 알림)

### PurchaseService (1193줄)
- ConfirmReceiptAsync — SalesService와 대칭, 7단계
- stock_alerts 종료 (자동사슬 반복 방지)
- ConfirmPurchaseReturnAsync 역분개

### CollectionService (483줄)
- GetReceivablesAsync — AR Aging Bucket (0-30/31-60/61-90/90+)
- partner_balance.total_receipt/total_payment 가산·차감

### FinanceService (627줄)
- GetCashbookAsync — Window Function 누적 잔액
- GetProfitAsync — 12개월 UNION + LEFT JOIN
- CheckIntegrityAsync — 8개 정합성 검증
- GetDashboardAsync — ConcurrentDictionary 30초 캐시

### ApprovalService (501줄)
- DocTypeLabels 9종 (quotation/sales_order/delivery/purchase_order/receipt/return/expense/leave/overtime)
- 결재 흐름: CreateApproval → Pending → Process(approved/rejected)
- 자동승인 (기준금액 미만)
- 위임 결재 (delegate_id + 기간)

### MonthlySummaryGuard (멱등 가드)
- monthly_summary_sources INSERT IGNORE (UNIQUE: tenant_id+ym+source_type+source_id+field_name)
- inserted=0 → 이미 처리 → 스킵
- 호출: SalesService.ConfirmDeliveryAsync (TotalSales), PurchaseService.ConfirmReceiptAsync (TotalPurchase)

### AutoJournalHelper (자동 기표)
- 계정 코드: 40100(매출) / 25500(부가세예수금) / 10800(외상매출금) / 50100(매입) / 17600(부가세대급금) / 23200(외상매입금) / 14600(원재료) / 16900(재공품)
- RecordSalesConfirmAsync, RecordPurchaseConfirmAsync, RecordBomProductionAsync, Cancel/Return 역분개

### DocumentNumberHelper (채번)
- DB MAX + 1 패턴 (2026-04-28 자동사슬 71건 누락 사고 후 도입)
- 예: `발-yyyyMMdd-NNN`, `매-yyyyMMdd-NNN`, `출-yyyyMMdd-NNN` (헌법 #14 한글 prefix)

### ApprovalTriggerHelper
- EnsureNotClosedAsync — 월마감 체크 (status='closed' 시 InvalidOperationException)
- TryCreateApprovalAsync — 자동 결재 문서 생성 (기준금액 + 결재라인 존재)

### MdbMigrationService (1541줄) — 별도 `22_MdbMigrationService_전수정독.md`

## 2. 인터페이스 45개

IPartnerService, ISalesService, IPurchaseService, ICollectionService, IFinanceService, IApprovalService, IItemService, ITenantService, ICompanyService, IPermissionService, ISettingsService, IReportService, IMonthlyClosingService, IHrService, IStockService, IAuditService, IUserService, ITenantCertificateService, ITenantDeviceService, IESignatureService, IChatbotService, IReportQueryBuilder, IUnitOfWork, ITaxInvoiceService, IBomService, IEmployeeService, ILeaveRequestService, IPositionService, IApprovalLineService, IBillingProvider, IBillingService, IBackupService, IBillsCardsBankService, IEmailService, IPasswordEncryptor, IAuthService, IBackofficeAuthService, IResellerService, IBinaryCryptoService, IRepository, IAuthUserLookup, IEventPublisher, ICurrentTenant.

## 3. Common 모델 2개

### PagedResult<T>
```csharp
public sealed class PagedResult<T>
{
    public int Page { get; init; } = 1;
    public int PageSize { get; init; } = 50;
    public int TotalCount { get; init; }
    public int TotalPages => PageSize <= 0 ? 0 : (int)Math.Ceiling((double)TotalCount/PageSize);
    public IReadOnlyList<T> Items { get; init; } = Array.Empty<T>();
}

public sealed class PagedRequest
{
    public int Page { get; init; } = 1;
    public int PageSize { get; init; } = 50;
    public string? Search { get; init; }
    public string? SortBy { get; init; }
    public bool SortDesc { get; init; }
    public int Skip => Math.Max(0, (Page-1) * Math.Clamp(PageSize, 1, 500));
    public int Take => Math.Clamp(PageSize, 1, 500);
}
```

### SensitiveFieldMasking
- 헬퍼: 민감 필드 마스킹 (전화·이메일·주민번호 등 로그 출력 시)

## 4. 트랜잭션 패턴

### EF + Dapper 공유 tx (Confirm·Cancel 등)
```csharp
using var tx = await _unitOfWork.BeginTransactionAsync(ct);
try
{
    await _unitOfWork.SaveChangesAsync(ct);
    var conn = _unitOfWork.GetDbConnection();
    var dbTx = tx.DbTransaction;
    await conn.ExecuteAsync(new CommandDefinition(sql, p, transaction: dbTx, cancellationToken: ct));
    await tx.CommitAsync(ct);
}
catch
{
    try { await tx.RollbackAsync(ct); } catch { }
    throw;
}
```

### Dapper 단독 tx
```csharp
using var tx = _db.BeginTransaction();
try { ... ; tx.Commit(); }
catch { try { tx.Rollback(); } catch { } throw; }
```

## 5. 헌법 #3 INSERT ONLY 적용

- stock_ledger: INSERT만, soft delete (is_deleted=1)
- journal_entries/lines: INSERT만
- approval_history: INSERT만
- monthly_summary_sources: INSERT만

## 6. 이벤트 발행 위치

| 이벤트 | 발행 |
|---|---|
| SalesConfirmedEvent | SalesService.ConfirmDeliveryAsync (안전재고 알림) |
| PurchaseConfirmedEvent | PurchaseService.ConfirmReceiptAsync |

## 7. 5/14 새벽 사고 본 서비스 영향

- MdbMigrationService.MigrateCoreAsync (line 164-202) — 거대 단일 tx (헌법 #20 오독)
- 다른 ConfirmXxx 메서드들과는 다름: 일반 ERP는 1개 문서당 1 tx로 정상
- 마이그는 16 테이블을 1 tx로 묶음 → 진앙
