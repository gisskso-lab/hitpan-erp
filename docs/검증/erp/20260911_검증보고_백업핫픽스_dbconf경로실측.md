# 검증보고서 2026-09-11 — 백업 핫픽스 C-13 설치 PC 같은 경로(db.conf) 실측 (20260911작5 [4])

| 항목 | 내용 |
|---|---|
| 검증 대상 | 작업지시서 `docs/운영기록/20260911작5_백업비번결함_핫픽스_작업지시서.md` §11-3 **C-13** / 커밋 `ccd9b4ec` (fix/20260911-backup-credential) |
| 구현자 | [3] 개발 갈래 (worktree `agent-a56c5bc94e5b218b7`) |
| 검증자 | [4] 검증팀장 데이비드 박 (분리 worktree `C:\HitPanBuild\wt-bk-c13` · 구현자 worktree 무접촉) |
| 판정 | ✅ **C-13 통과** · 범위 밖 적발 P1 1건(기존 코드 · 이번 커밋이 만든 것 아님) → PM 판단 |

격리 절차 기준: `docs/검증/선행/20260911_선행검증서_백업비번결함_격리재현.md` §1 (같은 방식 · 포트만 다름)

---

## 0. 격리 환경

| 항목 | 값 |
|---|---|
| 서버 | `MariaDB 11.4\bin\mariadbd.exe` 일반 프로세스(PID 30344) · `11.4.10-MariaDB` · `lower_case_table_names=1` · 서비스 등록 0 |
| datadir · 포트 | `C:\HitPanBuild\iso_c13\data` (`mariadb-install-db` 새로 만듦) · **33947** · bind 127.0.0.1 |
| 스키마 | `ccd9b4ec` 의 `installer/hitpan_db_clean.sql` → `hitpan_c13` 152개(BASE TABLE 143 · VIEW 9) |
| 계정 | **설치 프로그램과 같은 모양** — `hitpan_c13@localhost` 하나 · `GRANT ALL ON hitpan_c13.*` (DB 한정, `HitPan-Universal.iss:2155-2157`) · 임시 비번 22자, 설치 프로그램 허용 문자 중 파서를 흔들 만한 `! # = $ ( ^ *  ?` 포함 · 값은 파일로만, 출력 0 |
| 폴더 모양 | `C:\HitPanBuild\iso_c13\root\api\BkC13Harness.exe` + `C:\HitPanBuild\iso_c13\root\db.conf` (= 설치 `HitPan\api\HitPan.API.exe` + `HitPan\db.conf`) |
| db.conf | `HitPan-Universal.iss:2354-2360` 과 같은 키·순서 — `DB_HOST=localhost` · `DB_PORT=33947` · `DB_NAME` · `DB_USER` · `DB_PASSWORD` · `JWT_SECRET`·`ERP_ENCRYPTION_KEY`(더미) · ASCII |
| 하네스 | net8.0 콘솔 · `ccd9b4ec` 의 `HitPan.Application` + `HitPan.Infrastructure` 참조 · 빌드 **경고 0 · 오류 0** · 작업 폴더 `C:\Windows\System32` 로 기동(서비스 작업 폴더 모사) |
| 연결 조립 | **infra** = `services.AddInfrastructure()` → DI `IDbConnection`(닫힌 채 주입, 운영과 같음) · **sep**(대조 전용) = 연결만 비밀 파일로 따로 조립, 덤프 자격증명은 오직 db.conf |
| 시작 단언 | 프로세스 환경에 `DB_HOST·DB_PORT·DB_NAME·DB_USER·DB_PASSWORD·HITPAN_DB_CONF·MYSQL_PWD` **0개** (있으면 exit 2) · db.conf `DB_PORT` ≠ 33947 이면 어떤 접속도 없이 exit 3 |
| 설정 파일 후보 실측 | `root\api\db.conf` 없음 · **`root\db.conf` 있음** · `iso_c13\db.conf` 없음 — `TenantConfigReader.cs:47-49` 의 두 번째 후보로 읽음 |
| 덤프 프로세스 감지 | 10ms 간격으로 `mariadb-dump`·`mysqldump` 새 PID 수집(기존 PID 21540·36284 는 기준선으로만, 무접촉) — **R1 에서 1개 감지로 감지기 자체를 검증** |
| 클라이언트 옵션 파일 | `C:\Windows\my.ini/cnf` · `C:\my.ini/cnf` · `MariaDB 11.4\my.ini/cnf` · `.mylogin.cnf` · `~\.my.cnf` **0개** · `MYSQL_HOME` 없음 |

## 1. 3관문

| 관문 | 결과 | 근거 |
|---|---|---|
| 빌드 errors 0 + warnings 0 (#19) | ✅ | 하네스 + `ccd9b4ec` Application·Infrastructure Release 빌드 `경고 0개 · 오류 0개` |
| ddl-smoke (#36) | 해당 없음 | 이번 커밋 DDL 변경 0 · 출하 DDL 적재 152개 정상 |
| 독립 반증 | ✅ | §2 |

## 2. 측정 결과 (C-13 항목)

| # | 조건 | 걸린 시간 | 결과 | 덤프 프로세스 (감지/남음) | 백업 파일 | `backup_history` · `backup_settings` |
|---|---|---|---|---|---|---|
| **R1 실험군** | infra · db.conf 완전 | 7,412ms | **Success=True** · 실행파일 `MariaDB 11.4\bin\mariadb-dump.exe` | 1 / **0** | **265,720B** · `CREATE TABLE` **143**(= BASE TABLE 143) · `-- Dump completed` 있음 · 비번 문자열 포함 **없음** | `success` · 265,720 · finished_at 채움 / `last_status=success` |
| R1 전제 | 같은 실행 | — | 열린 연결 `ConnectionString` 에 `Password` 키 **없음** (드라이버가 뺌 = 옛 코드가 깨지던 조건 그대로) | — | — | — |
| **C1 대조** | sep · db.conf 에서 `DB_PASSWORD` 줄 삭제 · 새 프로세스 | **417ms** (설정 조회·이력 INSERT 포함) | **Success=False** · 이유 `DB 비밀번호 설정을 읽지 못했습니다. 백업을 시작하지 않았습니다.` · 실행파일 로그 **없음**(프로세스 전 차단) | **0 / 0** | 새 파일 0 (1→1) | `failed` · 같은 이유 · finished_at 채움 / `last_status=failed` |
| C1b 참고 | infra · 같은 db.conf | — | `AddInfrastructure` 가 먼저 예외 `DB_PASSWORD 영역 0건 — …` → **설치 모양에서는 API 가 기동조차 안 한다**. C1 은 연결을 따로 조립해야만 닿는 경로 | — | — | — |
| **C2 대조(추가)** | sep · 연결은 올바른 비번 · db.conf 비번만 마지막 1자 틀림 | 2,608ms | **Success=False** · `mariadb-dump.exe 실패 (exit=2): … 1045: Access denied … (using password: YES)` | 1 / **0** | **0B 파일 남음** | `failed` · 같은 이유 |
| **ⓑ** | infra · `DB_NAME=HITPAN_C13` (대소문자만 다름) | 6,579ms | **Success=True** · `conn.Database=HITPAN_C13` · DB 이름 대조(`OrdinalIgnoreCase`) 통과 | 1 / 0 | 266,625B · `CREATE TABLE` 143 | `success` |

읽는 법:
- **R1** = C-13 합격 조건(성공 · 0바이트 아님 · 이력 success) 충족. 환경변수 0 · 설치 폴더 모양 · 설치 계정 권한 모양(DB 한정 GRANT 로 `--routines --triggers` 도 통과)에서.
- **C2** 가 R1 의 성공이 연결 문자열이 아니라 **db.conf 의 비번**에서 왔음을 가른다(연결은 맞고 db.conf 만 틀리면 덤프가 1045). 비번이 틀려도 **입력 대기 없이 2.6초에 반환**, 프로세스 남음 0.
- **C1** = 합격 조건(즉시 실패 · 이유 문구 · 프로세스 0 · 대기 없음) 충족.
- **ⓑ** = Windows(`lower_case_table_names=1`)에서는 대소문자만 다른 DB 이름이 성공한다. 리눅스(`=0`)는 미측정 ⬜.
- stderr 출력은 모든 실행에서 0바이트.

## 3. 적발 사항

| # | 등급 | 내용 | 재현 방법 | 봉합 여부 |
|---|---|---|---|---|
| 1 | **P1** (C-13 범위 밖 · 기존 코드) | **실패한 덤프가 남긴 0B 파일이 보관 개수를 차지해 진짜 백업을 지운다.** `ApplyRetention`(`BackupService.cs:554-567`)은 `hitpan_backup_*.sql` 을 만든 시각 순으로 N개만 남기고 크기·성공 여부를 보지 않는다. 실패 경로는 0B 파일을 치우지 않는다(C2 · 선행검증 §4 X5). 실측: `retention_count=2` · 실패 2회(0B 2개) → 성공 1회 → 폴더에 **0B 1개 + 성공 1개**만 남고 이전 진짜 백업 265,720B·266,625B **삭제**. 기본값 30 이면 실패 29회 뒤 성공 1회 = 진짜 백업 1개만 남음 | 격리 DB · C2 조건 2회 → `retention_count=2` → infra 성공 1회 → 폴더 목록 | ⬜ — `ccd9b4ec` 는 호출 위치만 옮김(diff 68·70 → 83·85), 함수·실패 파일 처리 무변경 → **이번 커밋이 만든 결함 아님**. 병렬이슈 등록 여부 PM 판단 |
| 2 | P2 (정보) | C-13 대조 조건(`DB_PASSWORD` 줄 없음)은 설치 모양에서 백업까지 닿지 않는다 — API 기동 단계(`InfrastructureExtensions.cs:24`)에서 멈춘다. 설치 PC 에서 실제로 닿는 실패는 **비번 불일치(C2)** 쪽이며 그것도 즉시·프로세스 0 으로 확인 | C1b | 해당 없음 |
| 3 | P2 (기존) | `ApplyRetention` 의 `catch (IOException ex) { _ = ex; }` 는 기록이 없는 사실상 빈 catch (#15) | 코드 `:565` | ⬜ 기존 · 이번 범위 밖 |

## 4. 거짓봉합 점검

- [x] 봉합 전 증상 — 선행검증 §4 실험군(연결 문자열에서 비번이 빠져 입력 대기 20초)과 같은 전제(열린 연결에 `Password` 키 없음)를 R1 에서 확인 → 이제 7.4초 성공
- [x] 성공이 우연(환경변수·옵션 파일·연결 문자열)에서 오지 않았나 — 환경변수 0 단언 · 옵션 파일 0 · C2 로 db.conf 비번을 실제 쓰는지 가름
- [x] 감지기가 실제로 잡나 — R1·C2·ⓑ 에서 새 덤프 프로세스 1개 감지, C1 에서 0
- [ ] 회귀 — 복원 경로·가져넣기는 이번 측정 범위 밖(C-11 로 막힘)

## 5. 판정 근거

- ✅ **C-13 통과**: 설치 PC 와 같은 경로(EXE 상위 폴더 db.conf · 환경변수 0 · 설치 계정 권한 모양 · 운영과 같은 DI 연결)에서 `RunBackupAsync` 성공 · 265,720B · `CREATE TABLE` 143 · 이력 success / 대조 `DB_PASSWORD` 없음 → 417ms 실패 · 이유 문구 · 프로세스 0 / 추가 대조 비번 불일치 → 2.6초 실패 · 프로세스 0.
- 적발 #1(P1)은 이번 커밋이 만든 것이 아니고 C-13 조건 밖이라 C-13 판정을 막지 않는다. 다만 **백업 사본이 사라지는 데이터 안전 문제**라 1.3.40 게시 전 포함 여부 또는 병렬이슈 등록은 PM 결재 사항.

## 6. 한계

- 하네스는 이 PC **사용자 계정 콘솔**이다. 실물 `HitPan.API.exe`·서비스 계정(SYSTEM 등)·요청 파이프라인은 아니다(#29 범위).
- `TenantConfigReader` 는 Application·Infrastructure 두 사본을 같은 파일에서 각각 읽었다(실물과 같음). 캐시가 정적이라 조건마다 새 프로세스로 돌렸다.
- 기본 폴더(`%ProgramData%\HitPan\Backup`) 권한 생성(C-8)은 이 PC 실물 폴더를 건드리지 않으려고 `primary_path` 를 격리 폴더로 미리 넣었다 → 미측정.
- ⓒ(종료 직후 취소)는 작업지시서대로 코드 판정 갈음 — 이 보고서에서 실측 안 함.

## 7. 금지 준수 · 정리 기록

- **접속**: 3306 · `hitpan_erp_t004` · 127.0.0.1:5257 · localhost:5234 **0**. 모든 접속은 127.0.0.1/localhost:**33947** 격리 인스턴스만 (하네스 포트 가드).
- **실물 설정**: `C:\Program Files\HitPan\db.conf` 읽기 0. db.conf 모양은 레포 `installer/HitPan-Universal.iss` 로만 확인.
- **서비스·기존 프로세스**: 조작 0. 운영 덤프 PID **21540 · 36284** 는 존재 확인만(정리 뒤 둘 다 살아 있음).
- **종료한 프로세스 = 자기가 띄운 것만**: 격리 `mariadbd` PID 30344 (`mariadb-admin shutdown` 정상 종료 · 포트 33947 해제 확인). 하네스는 매 실행 스스로 종료(잔존 0).
- **출력**: 비밀번호 · 명령줄 출력 0 (하네스는 불린만 · 모든 문자열 비번 치환 · 계정 생성 SQL 은 표준입력으로만).
- **레포**: 코드·커밋·푸시 0 · appsettings 무접촉 · 작업리뷰서·폴더 INDEX 무수정 · 구현자 worktree 무접촉(정리 뒤에도 `ccd9b4ec` 그대로).
- **정리**: worktree `C:\HitPanBuild\wt-bk-c13` 제거(`git worktree list` 0건) · 격리 폴더 `C:\HitPanBuild\iso_c13` 제거(datadir · 임시 비번 파일 · db.conf · 덤프 결과 포함, `Test-Path` False) · 하네스 소스·빌드 산출물은 세션 scratchpad `harness_c13` 에만 남음(비번 없음).
