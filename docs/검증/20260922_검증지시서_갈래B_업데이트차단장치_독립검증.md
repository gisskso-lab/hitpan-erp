# [4] 독립검증 지시서 — **갈래 B 업데이트 부분 적용 차단 장치** (2026-09-22)

> [1] 사장 오더 2026-09-22 *"순서대로, 루틴대로 해"* → 작지 `20260922작1` **절 B**(인계1 §4-4)
> 🔴 **작성자 ≠ 검증자.** 아래 대상은 **[3] 개발 갈래가 만든 것**이고, **PM 은 [5] 상신 범위에서 이것을 빠뜨렸다.**
> 검증 관점 = **검증팀장 데이비드 박** — *"되는 것을 확인하지 말고 **안 되는 경우를 찾는다.**"*

---

## §0. 🔴 먼저 — **규명 결과: 미검증이 사실이다**

작지 절 B-2 가 시킨 규명을 먼저 했다. **판정: 🔴 미검증.**

| 조사 | 결과 |
|---|---|
| `docs/검증/` 전수에서 `갈래 B`·`T3-6`·`UpdateOrchestrator`·`VerifyNewVersionAsync` 검색 | 9/21~22 문서 **2건뿐** — 둘 다 **갈래 A** 전용 |
| 9/22 [4] **검증지시서** §1 대상 커밋 6개 | `ae3697f2`·`00d65972`·`cc61c84a`·`c03c2178`·`a93e901d`·직렬화 — **전부 갈래 A** |
| 9/22 [4] **작업리뷰서**에서 `갈래 B`·`T3-6`·`UpdateOrchestrator` 등장 횟수 | **0건** |
| 갈래 B 근거 문서 | `docs/개발/erp/20260921작2_갈래B_업데이트차단장치_개발명세서.md` — 🔴 **[3] 자기 명세서다. [4] 가 아니다** |
| 유일한 인접 문서 | `docs/검증/선행/20260921_선행검증서_부분적용구멍_업데이트차단지점.md` — 🔴 **[1-V]**(만들기 **전**, 「어디를 막을까」). **구현 검증이 아니다** |

⇒ **CTO 의 「내가 안 봤다」는 옳았다.** 갈래 B 는 **[3] 이 만들고 [3] 이 초록 준 상태**로 남아 있다.
⇒ 🔴 **게시 순서 P-22 상 이것이 고객 PC 에 먼저 닿는다.** 갈래 A 보다 **앞선다.**

---

## §1. 검증 대상 (커밋 고정)

브랜치 `feat/20260920-rc-safe-path` · 흡수 머지 `a5a93c58` (워크트리 `7cfa3575`)

| 커밋 | 무엇 |
|---|---|
| `f7945e24` | **S-1** 재결재 조건 **분류축 정정** (옛 주석 보존) |
| `0e96ef51` | **S-3** `information_schema.statistics` 읽기 메서드 |
| `bb92f035` | **S-2·S-3'** `VerifyNewVersionAsync` **3번째 조건** |
| `88cd0fba` | 게이트 **G-RC18 b·c·d · N-7 · V-2** |
| `85a07829` | **CI** `@verify-index:` 선언 누락 검출 |
| `a9c489ec` | stdin 차단 + 15초 상한 (**없던 실패 금지**) |
| `7cfa3575` | 개발명세서 |

**제품 표면**: 938 insertions / **1 deletion**(`return true;`)
**파일**: `UpdateOrchestrator.cs` · `WatchdogStatusWriter.cs` · `UpdateIndexVerifyGateTests.cs` · `scripts/check-verify-index-declaration.sh`

---

## §2. 🔴 반드시 반증해야 하는 주장 ([3] 이 한 말 그대로)

| # | [3] 의 주장 | 🔴 **어떻게 깨 볼 것인가** |
|---|---|---|
| **B-1** | 「조용한 부분 적용을 닫았다」 | 🔴 **장치가 실제로 막는 경우를 하나라도 만들어 보라.** 게이트는 **판정 함수**를 쟀다. **`VerifyNewVersionAsync` 종단에서 롤백까지 가는 것**은 아무도 안 봤다 |
| **B-2** 🔴 | 「판정 불가는 통과다(fail-open) — 드물다」 | 🔴 **「드물다」가 측정된 적이 없다.** **판정 불가를 쉽게 만들 수 있나?** DB 접속 지연·권한 없음·15초 상한 초과·비번 빈 값 — **평범한 고객 PC 에서 Undetermined 가 상시가 되면 장치는 이름만 남는다** |
| **B-3** 🔴 | 「V-2(fail-open)가 설계 §16-2 S-2·S-3(fail-closed)보다 뒤라 이긴다」 | 🔴 **[3] 이 문서 충돌을 스스로 판정했다.** 이건 **[3] 의 권한이 아니다.** 충돌이 실재하는지 확인하고 **[5] 안건으로 올려라**(명세서 §4-2 가 「PM·설계팀장 몫」이라고 적어 놓고 **그대로 남아 있다**) |
| **B-4** | 「①(스왑 전)은 null=차단, 3번째 조건(스왑 후)은 null=통과 — 의도적으로 반대」 | 두 경로가 **같은 릴리스에서 동시에 발화**하는 조합이 있나? 있으면 **어느 쪽이 이기나?** |
| **B-5** | 「선언은 마이그 머리말 `@verify-index:` 에서 읽는다 — 하드코딩 금지」 | 🔴 **선언을 적고 본문이 다른 인덱스를 만드는 경우 CI 가 안 본다**(명세서 §6-4 자백). **실제로 그 사례를 만들어 통과하는지 확인**하라 |
| **B-6** | 「stdin 차단 + 15초 상한으로 없던 실패를 막았다」 | 🔴 **상한 초과 = 통과**다. **느린 고객 PC 에서 15초가 상시 초과되면** 장치는 꺼진 것과 같다. **15초의 근거가 있나?** (함정 §6-2 — 「여유 n배」 척도 오독 재발 자리) |
| **B-7** 🔴 | 「#43 좁은 뜻 무접촉」 | 🔴 **Major 팝업 Y/N 흐름을 한 글자도 안 바꿨는지 `git diff` 로 직접 확인.** 롤백이 늘면 **고객이 본 팝업의 결과가 달라진다** — 넓은 뜻 저촉 여부를 판정하라 |
| **B-8** | 「워치독 회귀 123/123 · 건너뜀 0 · 빌드 0/0」 | **직접 돌려라.** [3] 의 숫자를 옮겨 적지 마라 |

---

## §3. 🔴 [3] 이 스스로 적은 한계 — **여기서 더 찾아라**

> 아래는 [3] 이 **이미 안다고 적은 것**이다. **이것만 보라는 뜻이 아니다.**

| # | [3] 이 적은 한계 | 출처 |
|---|---|---|
| 1 | **이미 나간 PC 는 못 구한다** | 명세서 §6-1 |
| 2 🔴 | **P-22 — 자기 릴리스를 못 지킨다.** DB-125 를 적용하는 워치독은 **구버전**이라 장치 밖 | §6-2 |
| 3 | **이름은 맞고 컬럼 구성이 다른 인덱스**는 못 잡는다(P-23 잠정 수용) | §6-3 |
| 4 | 판정은 **로컬 DB 하나**(`DATABASE()`) 기준 | §6-5 |
| 5 🔴 | **`VerifyDeclaredIndexesAsync` 폴링 껍질(30초·통지)은 코드 리뷰로만 봤다** — [3] 이 **「[4] 가 봐 달라」고 명시** | §7-5 |
| 6 🔴 | **실제 업데이트 1회전 실측 안 함** | §7-6 |

---

## §4. 판정 형식

- 🔴 **회차를 숫자로 병기**한다(n회 중 m회). **1회 관측 일반화 금지**(함정 §6-5).
- 🔴 **`EXIT=0` 은 통과가 아니다.** 결과 줄을 읽어라(함정 §6-8).
- 🔴 **`[SKIP]` 을 통과로 세지 마라.** DB 없는 로컬 PASS = SKIP.
- 🔴 **못 잰 축은 ⛔ 로 명시**한다. 안 쟀는데 조용하면 그게 이번에 봉합한 사고다.
- 판정은 🟢통과 / 🟡조건부 / 🔴반려 셋 중 하나. **조건부면 조건을 번호로.**

## §5. 건드리면 안 되는 것

- 🔴 **이 PC 서비스 · 운영 DB `hitpan_erp_t004` · `appsettings`**(#21) — 무접촉
- 🔴 **Major 팝업 Y/N**(#43) · 워치독 안전 원칙 6개 · `.old` 삭제 시점
- 🔴 **게이트를 고쳐 초록 만들기**(#32)

## §6. 산출물

`docs/검증/20260922_작업리뷰서_갈래B_업데이트차단장치_독립검증.md`

---
**발주**: PM 브라운킴 · 2026-09-22 · 근거 작지 `20260922작1` §2
