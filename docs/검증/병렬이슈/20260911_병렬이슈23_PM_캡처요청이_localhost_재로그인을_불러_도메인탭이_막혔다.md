# 병렬이슈 23 — PM 캡처 요청이 localhost 재로그인을 불러 **도메인 탭이 막혔다** (실물 기기표 상태 변경)

| 항목 | 내용 |
|---|---|
| 발견 | 2026-09-11 · 20260911작4 [1-V] 진행 중 · 사장님 실측 보고 (캡처 첨부) |
| 대상 | 사장님 PC 실물 `tenant_devices` (운영 DB `hitpan_erp_t004`) · 1.3.39 · 도메인 `test1234.hitpan.kr` · `localhost:5234` |
| 심각도 | **P1** — 사장님 평소 경로(도메인) 업무 통행 차단 · 원인 판정 증거(표식 줄 모양) 변경 |
| 상태 | 🔴 **미봉합** — 기기표 상태는 조회 후 설계 보완(20260911작4)에서 판정 · 임시 조작 0 |
| 표기 | 🟢 실측(코드 `파일:줄` · 사장님 화면) · 🟡 추정 · ⬜ 미측정 |

---

## 1. 무엇이 났나

| # | 사실 | 근거 |
|---|---|---|
| 1 | PM 이 사장님께 「막힌 화면 두 개(도메인, **localhost**) 캡처」를 요청하면서 **localhost 재로그인 금지 주의를 빼먹었다** | 이 세션 PM 보고 · 회의록 §7-5 사장님 3 「localhost 재로그인·임시 우회는 하지 마십시오」 · 작업지시서 §6 결재 6 운영 |
| 2 | 🟢 사장님: 「로컬호스트 원래 등록PC 아니라며, 안들어갔었는데, 지금은 잘 들어가지네」 | 사장님 보고 2026-09-11 |
| 3 | 🟢 사장님: 도메인은 브라우저 닫고 다시 여니 「인증된 기기가 아닙니다 · 인증 번호」 (`/settings/mdb-migration`) | 사장님 캡처 |
| 4 | 🟢 localhost 로그인이 기존 줄을 찾고 `IsLocalConsole` 이면 **옛 표식 줄을 `revoked` 로 내리고 지금 줄에 표식**을 붙인다 | `src/HitPan.Infrastructure/Services/TenantDeviceService.cs:269-296` (사유 `…(20260818작4)` `:280`) |
| 5 | 🟢 번호 통행(축②)은 `approved ∧ is_main_pc=1` 줄만 인정 → 옛 줄 번호를 쓰던 도메인 탭 403 `인증된 기기가 아닙니다` | `src/HitPan.API/Middleware/DeviceAuthMiddleware.cs:186-194·241-248` |
| 6 | 🟢 이 결과는 하브루타가 **미리 반려한 경로** 그대로다 — 「(나) localhost:5234 재로그인은 서버줄 ① revoked → 도메인 합류 탭까지 막히고 재표식 충돌 ⇒ 반려」 | 회의록 §4-3 Q-B (`:301-304`) |

🟡 추정 사슬: 첫 localhost 로그인(9/10) = 새 줄 approved·표식 0 → 403 · 오늘 두 번째 로그인 = 그 줄을 찾음 → #4 발동 → localhost 통과 · 도메인 탭 번호 줄 revoked → #5. ⬜ 실물 줄 모양은 사장님 조회로만 확정.

## 2. 무엇이 달라졌나

- 원인 판정 증거: 선행검증서 A §4-1 판정표에서 **J0/J0′(표식 0줄) → J2(표식이 브라우저 줄)** 로 옮겨졌을 가능성. 옛 줄의 `revoked_at`(오늘 시각)·`revoked_reason` 이 남아 **이동 전 표식 줄이 있었는지는 여전히 가를 수 있다** (감사 0행이라 `audit_trail` 은 근거 아님 · 병렬이슈20).
- 재기동 위험: API 재기동 시 서버 줄 재표식(`MainPcRegistrationService.cs:156-164`) → DB-89 없으면 표식 2줄(J3) · 있으면 1062 로그.
- 업무 데이터 무접촉 — 바뀐 것은 `tenant_devices` 뿐 (🟡 코드상).

## 3. 조치 (코드 0 · 실물 조작 0)

1. 사장님께 즉시 원인 설명 + 금지 안내: 도메인 탭 인증번호 입력 · 기기관리 승인/해제 · 재부팅·업데이트 · **백업 복원**(기기표 통째 교체 `BackupService.cs:302`) — 조회 전까지.
2. 사장님 결정(2026-09-11): *"일단 로컬호스트 주소로 마이그레이션 테스트 해볼게"* — 사장님 판단 존중. 숫자 대조는 「모두 지우고 새로 가져오기」 경로에서만 의미(결재 6).
3. 조회 결과로 J 판정 → 20260911작4 설계 보완에 「표식이 이미 옮겨진 DB」 처리(검증계획서 §2-1 3행 PM 판정 (i)/(ii))를 반드시 포함.

## 4. 재발 방지 (PM)

- **사장님께 실물 화면 확인을 부탁할 때는 「하지 말 것」을 같은 문장에 넣는다** — 특히 로그인·재로그인·승인·복원·재기동. 캡처 요청도 실물 상태를 바꾸는 행동을 부른다.
- 요청 전에 회의록 「오늘 할 일 · 금지」 절(§7-5)을 다시 대조한다.

---
**기록**: PM 브라운킴 · 2026-09-11 / **상태**: 🔴 미봉합 · 조회 대기
