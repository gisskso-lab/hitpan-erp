# [3-V] 6회차 — **본안 I 반려** · 🔴 **검증팀 자기 권고 철회** · 🟢 질문이 좁혀졌다

| 항목 | 내용 |
|---|---|
| 검증 | **보안상무 안철수** · 2026-08-19 (6회차) |
| 대상 | 본안 **I** — 🔴 **5회차에서 검증팀 자신이 권고한 안** |
| 판정 | 🔴 **반려.** 🔴 **5회차 권고가 틀렸음을 검증팀이 스스로 철회** |
| PM 교차확인 | ✅ 치명1·2·3·4 **전건 코드로 재확인** |

> **한 줄**
> 🔴 **문을 여는 것은 ``status='approved'`` 가 아니다. ``auth_key_hash`` 이거나 ``is_main_pc=1`` 이다.**
> **I-1 은 둘 중 어느 것도 만들지 않는다.**

---

## 🔴 0. **검증팀이 자기 권고를 철회한다**

> 5회차에서 I 를 권고한 것은 **틀렸다.** ``status='approved'`` 가 문을 연다고 **추측**했고
> **미들웨어를 끝까지 따라가지 않았다.**
> ⇒ 🔴 **I-1 을 넣으면 게이트는 초록불이 되고 사장님은 여전히 못 쓰신다** — **가짜게이트 10번째가 될 자리였다.**

⚠️ **PM 도 같은 자리에서 같은 추측을 했다**(설계서 I-0 자체반증).
🔴 **PM 과 검증팀이 **동시에** 같은 착각을 했다** — 이것이 이번 세션의 가장 위험한 순간이었다.

---

## 🔴 치명 1 — ``approved`` 만으로는 업무 API 가 **안 열린다**

| 축 | 근거 | 대표 터널 첫 로그인 |
|---|---|---|
| ① 인증키 | ``DeviceAuthMiddleware.cs:129-137`` | ❌ **키가 없다** |
| ② 장비넘버 | ``:178-186`` — ``AND is_main_pc = 1`` | ❌ **``is_main_pc=0``** |
| ③ 대표 예외 | ``:225-231`` + ``:260-264`` — ``/api/devices``·``/api/auth`` | ❌ **업무 API 아님** |

🔴 **결정적 근거 — ``auth_key_hash`` 를 채우는 자리는 전 코드에 두 곳뿐**(PM 전수 grep ✅):
``TenantDeviceService.cs:859``(``ApproveAsync``) · ``:1020``(재발급).

⇒ I-1 은 **INSERT 시 ``status`` 문자열만** 바꾼다. ``ApproveAsync`` 를 **안 거치므로 ``auth_key_hash`` 는 영원히 NULL.**
⇒ ``is_main_pc`` 도 안 붙인다(``:651-654`` 주석 — 일부러 안 붙인다).

### 🔴 **코드가 이미 이 결론을 적어 뒀다**

``TenantDeviceService.cs:1517-1531``:
> *"이 메서드는 **문을 열지 않는다.** 화면에게 상태를 알려줄 뿐이다.
> 업무 API 를 통과시키는 판정은 여전히 **인증키(미들웨어)와 IsDeviceAllowedAsync**(=메인PC만) 가 한다."*

⇒ 🔴 **증상이 *""승인 대기에 갇힘""* 에서 *""화면은 열렸는데 아무것도 안 됨""* 으로 자리만 옮겨간다.**
**「고쳤다 ≠ 갔다」 계통이다. I-1 단독은 거짓봉합이다.**

---

## 🔴 치명 2 — ``is_parent`` 축은 **원천적으로 불가**

| 실측 | 무엇 |
|---|---|
| ``AuthService.cs:414-426`` | 🔴 클레임은 ``tenant_id``·``user_id``·``name``·``account_type``·``employee_id``·``role`` 뿐 — **``is_parent`` 가 없다** |
| 출하 DDL ``idx_users_tenant_parent`` | 🔴 **비유니크 KEY.** UNIQUE 제약 **0건** ⇒ *"tenant당 1명"* 은 **주석일 뿐** |
| ``TenantDeviceService.cs:1191-1194`` | ``ORDER BY is_parent DESC … LIMIT 1`` — **다중행을 전제한 코드** |

«PM 확인» ✅ 클레임 목록 직접 확인.

⇒ 🔴 **§7-2 ⑨(축 선택) 결재 자체가 무의미했다.** 남는 축은 ``account_type='tenant_admin'`` **하나**이고
그것은 **테넌트당 N행**이다 ⇒ **관리자 계정 수만큼 무승인 통과가 늘어난다.**

---

## 🔴 치명 3 — I-1 은 **G-43 을 정면으로 깨뜨린다. 그리고 게이트가 옳다**

``MainPcLocalConsoleGateTests.cs:434`` — ``GC43_바깥에서는_메인PC가_못된다`` (주석: *"🔴 보안 본체"*)
- 시험 계정이 ``:176-177`` 에서 **``account_type='tenant_admin'``** 으로 INSERT 된다
- ``BrowserLogin(local: false)`` = **터널**
- 기대: ``status == "pending"``

«PM 확인» ✅

⇒ 🔴 **I-1 을 넣으면 이 시험이 정확히 그 이유로 빨간불이 된다.**
> **그리고 G-43 의 판정이 옳다** — *"봉합의 값어치는 열린 쪽이 아니라 **닫힌 쪽**이 증명한다."*
> 🔴 **I 는 닫힌 쪽을 여는 안이다.**

---

## 🔴 치명 4 — I-2 는 **안전한 삽입 지점이 없다**

``RegisterOrRefreshAsync`` 갱신 경로 실행 순서(실측):

| 줄 | 무엇 |
|---|---|
| ``:343`` | ``revoked && !isMainPc`` → **즉시 return false** |
| ``:384`` | ``rejected`` → pending 으로 되돌리고 **즉시 return** |
| ``:508`` | UPDATE (``status`` 를 **안 건드림**) |

- I-2 를 ``:508`` 에 얹으면 🔴 **``:384`` 에서 이미 return 된 ``rejected`` 대표는 못 지나간다**
- ``:343`` 앞으로 올리면 🔴 **대표 로그인만으로 폐기(``revoked``) 기기가 되살아난다** ⇒ ``RevokeAsync`` 의 의미 소멸

---

## 🟡 중 5·6·7

| # | 무엇 |
|---|---|
| 중5 | ``ReadJwtToken`` 서명 미검증은 **이 자리에선 문제 아님**(방금 서버가 서명한 값, 왕복 없음). 🔴 **단 *""``IsLocalConsole`` 옆""* 이라는 설계 문구가 부정확** — 그 자리는 소켓·헤더를 보는 자리이고 ``IsOwnerAccount`` 는 **응답 토큰을 읽는** 다른 성격이다 |
| 중6 | 🟢 **QR 경로는 영향 없음** — ``[AllowAnonymous]`` + ``qrStatus="pending"`` 하드코딩. **결함이 아니라 옳은 설계** |
| 중7 | 🔴 **요금이 샌다** — 대표가 집·카페·폰으로 접속할 때마다 **문지기 없이 슬롯이 확정 소모**된다. 사장님 8/11 설계(*"돈이 걸린 자원이라 문지기가 필요하다"* ``:636``)와 **정면 충돌** |

⚠️ **PM 정정**: 나는 §I 위험표에 *"요금은 오히려 더 엄격"* 이라 적었다. **한도 안에서는** 맞지만,
🔴 **문지기 없이 소모된다는 점을 안 봤다.** 사장님 설계와 충돌하는 쪽이 더 중요하다.

---

## 🔴 판정 — 반려. **그러나 질문이 좁혀졌다**

### 🟢 이번 차수가 **확정한 사실**

> 🔴 **``status`` 문자열만 바꾸는 안은 전부 무효다.**
> 문을 여는 것은 **``auth_key_hash``** 이거나 **``is_main_pc=1``** — 둘뿐이다.

### 🔴 다음 차수가 답할 질문 — **둘 중 하나**

| # | 무엇 | 성격 |
|---|---|---|
| **①** | 대표 터널 기기에 **``auth_key_hash`` 를 실제로 발급**한다(서버가 ``ApproveAsync`` 자동 호출) | 열쇠를 **준다** |
| **②** | **``OwnerEscapePrefixes`` 를 넓힌다** | 문을 **더 연다** |

### 🔴 사장님 결재가 **먼저**여야 하는 것

| # | 무엇 |
|---|---|
| ㉠ | 축은 **``account_type='tenant_admin'`` 하나뿐**이고 **N행**이다. 그 위에서 **요금·보안을 어떻게 지킬지** |
| ㉡ | 🔴 **어느 안이든 G-43 을 깨뜨린다.** *"바깥에서 온 대표 기기가 **승인 없이** 열려도 되는가"* — **검증팀이 정할 수 없는 값이다** |

### ⚠️ 미실측 (정직하게)

실제 DB 의 ``tenant_admin`` 행 수 · G-43 의 실제 FAIL 은 **DB 연결 환경에서만** 확인 가능(``ServerAvailable()`` 가드).
**위 판정은 전부 코드·DDL·테스트 소스 실측이며 런타임 실행 실측은 아니다.**
