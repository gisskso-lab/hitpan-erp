# 병렬이슈42 — 이월잔액 매칭 검사는 C# 에서 글자 그대로(Ordinal) 가르는데 합계 SQL 은 대소문자·끝 공백을 무시해, 검사 없이 M 에 들어간다

| 항목 | 내용 |
|---|---|
| 적발 | [3-V] 보안상무 안철수 관점 · hp-verifier (구현자·PM 아님) · 2026-09-17 |
| 근거 문서 | 작업지시서 [`20260915작1`](../../운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md) §15-2 · 설계 §22 · 개발명세서 `docs/개발/erp/20260917작1_갈래R1_공용식_수금지급서버_개발명세서.md` |
| 대상 브랜치 | `feat/20260915-migration-posting` `bc1c1092` (코드 수정 0) |
| 등급 | **P1** — 남은 이월잔액 R 을 임의로 부풀리거나 0 아래로 내린다(음수 금액 · 기준일 이전 · R 초과 전부 통과) |
| 상태 | 🔴 미결 |

## 원인 (파일:줄)
- `CollectionService.cs` `EnsureCollectionTargetAllowedAsync` — `string.Equals(request.RefDocType, "legacy_balance", StringComparison.Ordinal)` 이 아니면 `"sales_delivery"` 와도 Ordinal 비교 → 둘 다 아니면 **검사 없이 return**. 지급 `EnsurePaymentTargetAllowedAsync` 도 같다(`PaymentType`).
- `LegacyBalanceMatching.cs:68` `lc.ref_doc_type = 'legacy_balance' AND lc.ref_doc_id = lc.partner_id` · `:96` `lp.payment_type = ...` — 칼럼 콜레이션 `utf8mb4_unicode_ci`(대소문자 무시 · PAD SPACE 끝 공백 무시). 출하 DDL `installer/hitpan_db_clean.sql` collections·payments.
- 같은 뿌리: `"Sales_Delivery"` 로 보내면 P2(명세서 남은 금액 초과)·P3(이관 명세서 거절) 검사를 건너뛰고, `CollectionDocSideWhere`·`SalesService` 명세서 취소 연쇄(`ref_doc_type='sales_delivery'`)에는 잡힌다.

## 재현 (격리 33306 · 전용 DB `hitpan_v3_sec` = `hitpan_e2e_gi` 표 LIKE 복제 · 끝나고 DROP)
1. `partner_legacy_balances` P1 이월 100,000 · 기준일 2026-02-28.
2. 서버가 검사 없이 넣을 행 3개를 INSERT(서버 경로는 코드 읽음 — API 직접 호출은 ⚠️미실측):
   `'Legacy_Balance'` −500,000 (2025-01-01 · 기준일 이전) · `'legacy_balance '`(끝 공백) 70,000 · `'LEGACY_BALANCE'` + ref_doc_id `'p1'`(소문자) 1원.
3. `ReceivableRemainingSql` 그대로 실행.

## 실측
| | legacy | matched(M) | remaining(R) |
|---|---|---|---|
| 넣기 전 | 100,000 | 0 | 100,000 |
| 넣은 뒤 | 100,000 | **−429,999** | **529,999** |
세 행 모두 `ref_doc_type='legacy_balance'` = 1 · `ref_doc_id=partner_id` = 1.
→ 이월잔액보다 큰 R 이 미수 현황·대사표·(R2 소비자 11곳)에 그대로 뜬다. 음수 수금은 `partner_balance.total_receipt` 와 수금 분개도 음수로 기록.

## 요구 (코드는 [3] 몫)
- 서버 입구에서 `RefDocType`·`PaymentType` 을 **허용 목록 정확 일치**로 거르거나(모르는 값 400), 정규화(Trim·소문자) 뒤 같은 값으로 저장·검사.
- 게이트 G8 에 표기 변형 3종(대소문자 · 끝 공백 · ref_doc_id 대소문자) 대조군 추가 — 봉합 빼고 FAIL 확인.
