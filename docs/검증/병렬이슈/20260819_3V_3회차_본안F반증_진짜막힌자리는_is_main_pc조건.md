# [3-V] 3회차 — 본안 F 반증 · 🔴 **결재 불가 (3차 연속)** · 🟢 **진짜 자리를 찾았다**

| 항목 | 내용 |
|---|---|
| 검증 | **보안상무 안철수** · 2026-08-19 (3회차) |
| 대상 | 설계서 4차 §3-4 **본안 F** |
| 판정 | 🔴 **결재 불가** — 그러나 🟢 **이번엔 막힌 자리가 특정됐다** |
| PM 교차확인 | ✅ 치명1·2·3 **전건 코드로 재확인** |

> **한 줄**
> 🔴 **진짜 막힌 자리는 `DeviceAuthMiddleware.cs:183` 의 `AND is_main_pc = 1` 이다.**
> 네 번의 설계가 **전부 이 자리를 안 건드리고 브라우저 쪽에 우회로만 쌓았다.**

---

## 🟢 먼저 — F 에서 **확인된 것** (반증 실패 = 통과)

| # | 확인 | 근거 |
|---|---|---|
| 🟢 | **헤더 이름이 일치한다** — 하이픈 사고 재발 아님 | `HitPanApiAuthHandler.cs:18` ↔ `DeviceAuthMiddleware.cs:30` |
| 🟢 | **키를 요청에 붙이는 코드가 있다** | `HitPanApiAuthHandler.cs:138-141` |
| 🟢 | **F-2(`ConfirmCode` 대조)는 성립한다** — 두 화면이 **같은 계산 한 곳**을 쓴다 | `DeviceConfirmCode.cs:63` · `DeviceManagePage.razor:67-71` · `DeviceAuthGate.razor:237` |
| ⚠️ | 4자리 충돌 1/10000 — **동시 pending 여러 대일 때만** | 소규모 사업장 실질 위험 낮음 |
| 🟢 | reload 후에도 **localStorage 키는 살아남고 읽힌다** | `HitPanApiAuthHandler.cs:138` |

---

## 🔴🔴 치명 1 — **진짜 막힌 자리는 `is_main_pc = 1` 조건이다**

| 근거 (실측) | 무엇 |
|---|---|
| `DeviceAuthMiddleware.cs:180-186` | 장비넘버 축이 **`AND is_main_pc = 1`** 로 좁혀져 있다 |
| `TenantDeviceService.cs:245` | 🔴 **대표 브라우저 줄은 `is_main_pc = 0`** (서버 줄에만 1 이 붙는다) |
| `TenantDeviceService.cs:268-271` | 그 표식을 옮기는 유일한 경로가 **`IsLocalConsole`** ⇒ **터널이면 절대 거짓** |

«PM 확인» ✅ 세 자리 모두 직접 확인.

**무엇이 깨지나**: 터널로 접속한 대표는 승인으로 `status='approved'` 가 되어도
🔴 **장비넘버 축이 `is_main_pc=1` 을 요구하므로 업무 API 가 계속 403** 이다.

> 🔴 **이것이 8/18 부터 지금까지 실제로 막고 있던 자리다.**
> 내 설계 A·B·D·E·F 는 **전부 이 자리를 안 건드렸다.**
> ⇒ **브라우저 쪽에 우회로를 쌓는 것으로는 이 조건을 못 넘는다.**

---

## 🔴 치명 2 — F-1 이 **대표 브라우저에 남의 열쇠를 심는다** (PM 선제 적발과 동일 자리, 더 나쁨)

| 근거 (실측) | 무엇 |
|---|---|
| `TenantDeviceService.cs:825` | 키는 **승인당한 기기 줄**에 저장된다 |
| `DeviceAuthMiddleware.cs:131-137` | 🔴 대조는 `auth_key_hash` **하나뿐** — **`device_id` 를 안 본다** |
| `device-fingerprint.js:254-262` | 열쇠 자리는 **한 칸뿐** |

«PM 확인» ✅ 대조문에 `device_id` 가 **없음**을 확인.

**무엇이 깨지나**: 대표가 직원 기기를 승인하면 그 키가 대표 자리를 덮어쓴다.
⚠️ **그런데 403 이 나지는 않는다** — 대조가 키만 보므로 **남의 키로도 통과**한다.

> 🔴 **더 나쁘다.** 조용히 통과하므로 **아무도 이상을 못 느낀다.**
> 그리고 이것은 8/18 (1-2) 가 *"통과 범위를 메인PC 한 줄로 좁혔다"* 고 적은 것을
> **인증키 축으로 되돌린다** — 봉합을 거꾸로 감는다.

---

## 🔴 치명 3 — **F-3 은 `reload` 를 넘지 못한다** (범위 과장)

| 근거 (실측) | 무엇 |
|---|---|
| `DeviceAuthGate.razor:286` | 승인되면 `location.reload` 가 돈다 |
| `DeviceAuthState.cs:17,49` | 상태는 **static 필드** ⇒ reload 시 **전부 초기화** |
| `DeviceAuthGate.razor:180` | 재시작 후 `initialProbe` 로 **서버에 다시 묻는다** |

**무엇이 깨지나**: F-3 의 "제3 상태"는 **reload 이전 구간에서만** 산다.
관문 유지 여부는 결국 **서버 `gate-status` 답**이 다시 정한다.

> 🔴 설계서가 이것을 *"관문 해제 순서 보장"* 이라 적은 것은 **범위 과장**이다.
> 8/18 에 겪은 *"forceLoad 로 정적 메모리가 증발"* 사고와 **같은 계통**이다.

---

## ⚠️ 중 1 — **F-4 가 거꾸로 적혀 있다**

`DeviceAuthGate.razor:224-226` 의 `_checking` 락을 **분리하라**고 F-4 에 적었는데,
🔴 **분리하면 오히려** 승인 처리 중 폴링이 먼저 승인을 감지해
`MarkAllowed()`(`:278`) + `location.reload`(`:286`)를 **키 보관보다 먼저** 실행할 수 있다.

⇒ **F-3 의 순서가 폴링에 깨진다.** 🔴 **승인 처리 중에는 폴링을 멈춰야(StopPolling) 한다.**
(실측: 코드 / 추측: 3초 창 안 왕복 성립 여부는 미재현)

---

## 🟢 경 2 — 대표 첫 로그인은 갇히지 않는다 (통과)

`TenantDeviceService.cs:~298` — 메인PC 콘솔이면 신규 줄이 곧바로 `approved`.
⚠️ **터널 첫 로그인이면 `pending`** 이고, 그때 **치명1 이 그대로 발동**한다.

---

## 🔴 판정 — 결재 불가. **그러나 방향이 정해졌다**

| F 항목 | 판정 |
|---|---|
| F-1 (열쇠 즉시 보관) | 🔴 **폐기** — 대표에겐 불필요하고, 직원 승인 시 **남의 키를 심는다** |
| **F-2** (`ConfirmCode` 대조) | 🟢 **유효 — 살린다** |
| F-3 (제3 상태) | ⚠️ **범위 축소** — reload 를 못 넘는다 |
| F-4 (락 분리) | 🔴 **방향 반대** — 분리가 아니라 **승인 중 폴링 정지** |
| F-5 (PM 선제) | ⚠️ 치명2 에 흡수 — 근본 해결이 아님 |

### 🔴 다음 설계가 겨눌 자리

> **`DeviceAuthMiddleware.cs:183` 의 `AND is_main_pc = 1`**
> 이 조건이 **터널 접속 대표를 막는 진짜 문**이다.
>
> ⚠️ 그런데 그 조건은 **8/18 (1-2) 가 일부러 좁힌 것**이다 —
> *"``device_id`` 는 비밀이 아니다. 승인된 아무 기기 번호나 넣으면 문이 열렸다"*
> ⇒ 🔴 **그냥 풀면 그 사고가 되살아난다. 좁힘을 유지하면서 대표를 통과시킬 축이 필요하다.**

### PM 소견 — **3회 연속, 전부 코드 착수 전**

1회차는 **고칠 자리**(서버→화면)를 틀렸다.
2회차는 **고친 뒤에 일어날 일**(키가 안 간다)을 안 봤다.
3회차는 **그 키가 무슨 문을 여는지**(키 축이 아니라 `is_main_pc` 축)를 안 봤다.

> 🔴 **세 번 다 "내가 만든 것" 만 보고 "그것이 닿는 자리" 를 안 봤다.**
> 이번에야 **막고 있던 조건 한 줄**이 특정됐다. 다음 설계는 **거기서 시작한다.**
