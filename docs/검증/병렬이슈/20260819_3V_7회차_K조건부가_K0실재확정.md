# [3-V] 병렬검증 7회차 — 본안 K 조건부 가(可) · K-0 잠재 P0 실재 확정

> **문서번호**: 20260819검7 · **단계**: [3-V] 동시 병렬검증 (보안상무 안철수)
> **대상**: 11차 본안 K (`docs/설계/erp/20260819_설계서_메인PC_판정축_재설계.md` §3-10)
> **판정**: 🟢 **조건부 가 — 조건 2개를 지시서에 반영하고 코딩 착수 가능**
> **PM 재검증**: 2026-08-20 코드 원문 대조 완료 (아래 §2)

---

## §0. 사장님 정정 (2026-08-20 오더)

> **"데모계정이 왜나와. TEST1 계정!!. .88 업데이트 파일을 해야지"**

- 7회차 검증이 이 PC의 **demo DB를 읽었다**(SELECT만 — 헌법 #39 '운영=읽기만' 준수, 변경 0건).
  그러나 사장님 지시로 **확인 실측의 축은 TEST1 테넌트**다. 이후 모든 실측·검증 지시는 **TEST1 계정** 기준으로 적는다. demo 는 실측 축으로 쓰지 않는다.
- 봉합의 전달 경로 = **1.2.88 자동업데이트 게시** ("고쳤다" ≠ "갔다" — 게시 없이는 TEST1 에 아무것도 안 간다).

## §1. K-0 (잠재 P0) — **실재 확정** (소스 + DB 실측 / HTTP 종단만 미실측)

**"인증키를 넣고 통과한 직원 기기는 그 순간부터 업무 API 통행로가 없다."** 반증 시도 전부 실패(= 실재):

| 통행 축 | 왜 못 지나나 | 근거 |
|---|---|---|
| ① 인증키 해시 | verify 성공 순간 `auth_key_hash = NULL` 소거. 화면은 **죽은 사람 키**를 보관·전송 → `COUNT=0` | `TenantDeviceService.cs` UPDATE · `DeviceAuthGate.razor:343` · `DeviceAuthMiddleware.cs:133-137` |
| ② 장비넘버 | `is_main_pc=1` 로 좁혀짐 — 직원 기기는 0 | `DeviceAuthMiddleware.cs:178-186` |
| ③ 대표 탈출로 | `tenant_admin` + `/api/devices`·`/api/auth` 한정 — 직원 해당 없음 | `:260-264` |
| Bypass | 업무 컨트롤러(`api/employees`·`api/bom`·`api/finance` 등 전수 대조) **0개 포함** | `:38-66` |

- **두 봉합은 같은 커밋** — `git log -S "auth_key_hash = NULL"` → **c25a6dce (8/18)**. 소거와 축② 좁힘이 한 커밋에 함께 실렸고, 서로를 몰랐다.
- **증상 사슬**: 403 → `MarkBlocked()` → 키 입력창 → 죽은 키 거절 → 재발급 키도 verify가 또 소거 → **반복 갇힘** = 사장님 실측 6회 갇힘과 같은 모양.
- **게이트 사각 10번째**: `dotnet test` 38/38 초록인데 덫은 실재 — *"verify 통과한 직원 기기가 업무 API를 통과한다"* 를 값으로 세운 **종단 게이트가 0개**였다.

## §2. 본안 K 반증 결과 (PM 코드 원문 재검증 완료)

| # | 급 | 지적 | PM 대조 | 처분 |
|---|---|---|---|---|
| 1 | 치명 | **K-4 재시도 덫**: 자동 투입이 실패한 뒤 [이 컴퓨터 승인]을 다시 누르면 `ApproveAsync`가 **null 반환**(이미 approved — 멱등) → verify 할 키가 없다 → 대표 기기가 K-0과 같은 덫 | ✅ `TenantDeviceService.cs` "이미 승인됨 — 두 번 눌러도 안전(멱등)" 원문 확인 | 🔴 **조건 1**: 실패·재시도 갈래 = `reissue-key/{자기 device_id}` 경유 + 키 **화면 표시 fallback**(수동 입력) 지시서 명시 |
| 2 | 필수 | **종단 게이트 부재**(§1 사각 10번째) | ✅ 기존 게이트 전수에 "verify 후 직원 통행" 값 게이트 없음 확인 | 🔴 **조건 2**: G-V(직원 종단)·G-W(교차쌍 차단) 신설 — 코딩과 한 몸 |
| 3 | 중 | **K-3 결합의 영향군**: c25a6dce 이전 verify 기기 중 저장 deviceId ≠ 해시 줄인 것만 관문 회귀 → 재발급으로 복구 | ✅ demo DB 실측 해시 생존 0줄 — 영향군 극소 | 배포 문서에 "[인증키 재발급] 한 줄" 안내 |
| 4 | 경 | verify 전 경합(사내 다른 로그인 사용자가 본 키 선점) — **현행에도 있는** 경합, 피해자 즉시 드러남 + 재발급 복구 + 감사기록 | ✅ verify-key 는 로그인 필수(TenantOnly) 확인 | 수용 (현행 동일) |
| 5 | — | QR 모바일: 폰도 같은 관문·같은 verify — **지금 K-0에 똑같이 걸려 있고 K가 똑같이 고친다** | ✅ `MobileDeviceRegisterPage.razor:302` deviceId 보관 확인 | K-3 대조 성립 |
| 6 | — | 워치독·업데이트: Bypass 로 판정 전 통과 — K-3 영향 0 | ✅ | — |
| 부기 | — | K는 축②(메인PC 장비넘버 = 비밀 아닌 값, G-32-d 한계)를 고치지 않는다 | ✅ | 범위 밖 명시 유지 |

## §3. 처분

1. 조건 1·2를 작업지시서 §2·게이트 표에 반영 → **PM 결재 → [3] 착수**.
2. 실측 축 = **TEST1 계정 · `https://test1.hitpan.kr`** (사장님 §0 오더). demo 금지.
3. 전달 경로 = **1.2.88 게시** (`deploy-update.yml` dispatch · Major 채널 고정 · 헌법 #43).
