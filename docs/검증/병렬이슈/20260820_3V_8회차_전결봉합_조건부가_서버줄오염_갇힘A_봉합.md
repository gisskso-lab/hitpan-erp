# [3-V] 병렬검증 8회차 — 전결봉합(20260820작2) 조건부 가 · 지적 3건 같은 커밋 봉합

> **문서번호**: 20260820검8 · **단계**: [3-V] 동시 병렬검증 (보안상무 안철수) — 구현과 **동시에** 돌았다
> **대상**: 20260820작2 (메인PC 2슬롯 · 모바일 다중등록 · PM·CTO 전결)
> **판정**: 🟢 조건부 가 → **조건·실재 지적 전건을 같은 커밋에서 봉합/반영 완료**

## §1. 판정 요약 (PM 코드 재검증 포함)

| # | 지적 | PM 재검증 | 처분 |
|---|---|---|---|
| 1 | 합류 통행(재발급→verify→기계비밀) 성립 | ✅ | G-39 값 게이트 |
| 2 | 🔴 **갇힘 A**: 신분 유지 가드가 **한 브라우저 다중 사업자**(사장님 결재)에서 남의 회사 신분을 영구 고집 → 직원 탈출로 0 | ✅ 실재 — 종전엔 로그인 덮어쓰기가 고쳐주던 것을 가드가 막음(가드가 만든 회귀) | 🟢 **봉합** — 회사 표식(`hitpan_auth_tenant`) 짝: 같은 회사일 때만 신분 유지. verify·합류 성공 시 표식 저장 |
| 3 | verify-key 헤더 우선 + 핸들러 매 요청 localStorage 읽기 | ✅ | 신분 선전환 방식 성립 |
| 4 | 직원 악용 | 기각 — reissue 대표 전용 + 직원은 서버 줄 번호를 API 로 못 얻음(목록 필터) | — |
| 5 | 로컬 콘솔 파손 | 기각 — 축② 는 해시 무관 | — |
| 6-① | 🔴 지시서 *"무료 탑승 동시 1개 자기 제한"* 은 **과장** — 축② 는 번호만으로 통과(G-32-d) | ✅ 실재 | 🟢 지시서 **정정** — 축① 한정 명시, 축② 폐쇄는 기존대로 다음 차수 |
| 6-② | 🔴 **서버 줄 오염**: 합류 브라우저의 로그인 갱신(장비넘버 1순위 대조 — PM 이 `RegisterOrRefreshAsync` 1순위 SELECT 로 확정)이 서버 줄 이름·UA·IP 를 덮음 → CS 본체 식별 훼손 | ✅ **실재** | 🟢 **봉합** — is_main_pc=1 줄은 last_seen 만 갱신, 정체 보존. **G-41** (봉합 제거 실측 FAIL 확인) |
| 6-③ | 🔴 대표 **폰** 합류 시 COALESCE 가 서버 줄 종류를 mobile 로 변경 → **요금 칸 이동**(pc -1) | ✅ 실재 (pc→mobile 은 무검사 방향) | 🟢 6-② 와 같은 봉합이 닫음 — G-41 이 `type=='pc'` 를 값으로 고정 |
| 7 | ⚠️ 실기기 실측: 어젯밤 승인된 터널 브라우저 줄 때문에 관문이 안 떠 버튼 도달 불가 | ✅ | 🟢 실측 절차에 **[폐기] 선행 단계** 명시 |

## §2. 실행 확인

- G-41 봉합 제거 실측: `preserveMainPcIdentity = false` → **FAIL** → 복원 → GREEN (초록불 출처 = 내 봉합)
- 기기·키 계열 65/65 · 전체 **587/587** · 빌드 -warnaserror **0/0**

## §3. 남긴 것 (정직하게)

- 축②(서버 줄 번호만으로 통과) — 기존 한계(G-32-d), 이번에 넓히지도 막지도 않음. 다음 차수.
- 서버 아닌 기계에서의 [회사서버] 오클릭 — 피해는 요금 1대 이내 + 대표 본인 행위 한정. 오클릭 경고 다이얼로그(`is-main-pc` 조회 활용)는 아침 후속.
- 모바일 근본 병합(한 폰 = 한 줄) — 요금 축이라 전결 범위 제외, 아침 설계.
- 신분 유지 가드·관문 UI 는 브라우저 동작 — xUnit 불가, **실4′(재로그인 유지)·실4″(서버 줄 이름 보존) 실측이 판정**.
