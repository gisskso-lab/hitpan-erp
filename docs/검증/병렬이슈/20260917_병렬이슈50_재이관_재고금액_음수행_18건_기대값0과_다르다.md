# 병렬이슈50 — 재이관 뒤 재고금액 음수 행 18건(−139,877,164.60) · 검증계획서 §8-5 기대 「0」과 다르다

| 항목 | 내용 |
|---|---|
| 적발 | [4] 2단계A 검증팀장 데이비드 박 관점 · hp-verifier · 2026-09-17 |
| 근거 문서 | 검증계획서 `erp/20260915_검증계획서_자료이관_장부반영분류.md` §8-5 · 작업리뷰서 §9 |
| 대상 | `feat/20260915-migration-posting` `e498d4b8` (코드 수정 0) · 격리 33306 `hitpan_e2e`(=`hitpan_v4_a`) · `hitpan_trgtest`(=`hitpan_v4_b`) |
| 등급 | ⚠️미판정 — 기대값 오기인지 결함인지 미확인 |
| 상태 | ⬛ 종결 — 기대값 정정(아래 PM 판정) |

## 실측
- `item_stock` `current_qty*avg_cost<0` : **A 18행 −139,877,164.60 · B 18행 −139,878,154.60** (최소 수량 −887) · 18행 전부 수량 음수 × 단가 양수 · `avg_cost<0` 0행 · 수량 음수 행 122.
- 합계는 대사표와 일치(A 54,753,974.40 = 레거시 · B 54,632,984.40 = 이어 계산) — 음수 행이 합에 이미 들어 있다.
- A(이어 계산 안 탐)에도 같은 18행 → 이어 계산(R3)이 만든 것이 아니다.

## 모르는 것
- 레거시 DOCFC 마지막 달에 음수 재고 행이 원래 있는지(⚠️미조회) · 계획서 §8-5 「재고금액 음수 행 0」이 무엇을 셌는지(단가 음수? 이어 계산으로 새로 생긴 음수?).

## 요구
- 기대값의 정의를 먼저 확정(맞추지 않는다). 레거시에도 음수면 기대값 정정 · 아니면 재고 맞춤(①맞춤→②리빌드) 경로 조사.

## PM 판정 (브라운킴 · 2026-09-17)
- **결함 아님 · 기대값 정정.** 레거시 MDB 사본(`C:/HitPanBuild/mdb-0917/PANDATA.mdb` · `scripts/readonly/20260917_mdb_select.ps1` SELECT) DOCFC 키별 마지막 달 실측: 기말금액 음수 **19행 −140,229,114.6** · 최소 수량 **−887** · 수량 음수 **125행**. 레거시 최종에 음수 재고가 원래 있다 → 원칙 「첫 화면 = 레거시 최종」 그대로.
- 히트판 18행 −139,877,164.60 · 수량 음수 122 와 레거시 19행 · 125 의 행 수 차는 키 병합(창고 합산·품명 규격 정규화) 추정 — ⚠️미실측 · 합계는 대사표 OK(A 54,753,974.40 = 레거시)라 금액 영향 0. 행 단위 대조는 백로그 P2.
- 검증계획서 §8-5 「재고금액 음수 행 0」 → **「이어 계산이 새로 만든 음수 0 (A·B 음수 행 동일)」** 로 정정. B−A 음수 합 차 −990 = HTP21C/SQL-SERVER 이어 계산 차(선행검증 B-1)와 일치.
- 상태: ⬛ 종결(기대값 정정).
