# 병렬이슈52 — 이월잔액 수금·지급 1건 뒤 대사표 미수금·미지급금 합계가 「차이」로 뜬다(거래처별은 차이 없음)

| 항목 | 내용 |
|---|---|
| 적발 | [4] 3판 2단계B 검증팀장 데이비드 박 관점 · hp-verifier · 2026-09-17 |
| 근거 문서 | 설계 `erp/20260915_설계_자료이관_머리없는줄_분류봉합.md` §20 #9·#10 · §24 · 작업리뷰서 §11 |
| 대상 | `feat/20260915-migration-posting` `cd50acd2`(코드 수정 0) · 격리 33306 `hitpan_v4_a2` · API 15257 |
| 등급 | 🟡 ⚠️미판정 — 설계 의도(#10 은 레거시와 비교라 차이가 맞다)인지 결함인지 결재 필요 |
| 상태 | ⬛ 봉합 — PM 판정 (가) · 49b970b3 |

## 실측
- P `(주)신화씨앤에스` 이월 수금 10,000 · Q `09시스템` 이월 지급 10,000 뒤 `POST api/migration/legacy-mdb/reconcile`(mdbA):
  - `receivable` legacy 142,062,113 · erp 142,052,113 · **status DIFF** · `payable` 51,577,479 / 51,567,479 **DIFF** · `diffCount` 0 → **2**
  - 같은 응답 `partnerDiffs` 에 P·Q 행 **없음**(#9 = 식 값 + M(P) 로 매칭만으로 차이가 안 생김 · 설계 §20-9)
  - 정보 행 `receivable_matched` 10,000 · `payable_matched` 10,000(INFO) — 값은 맞다
- 두 건 삭제 뒤 DIFF → OK 복귀(`diffCount` 0).

## 왜 문제일 수 있나
- 같은 대사표 안에서 거래처별(#9)은 매칭을 더해 「차이 없음」인데 합계(#10)는 매칭을 안 더해 「차이」 — 두 줄이 서로 다른 말을 한다.
- 이관 뒤 고객이 이월 수금을 한 건만 해도 대사표 첫 줄에 미수금·미지급금 차이가 뜬다 → 「이관이 틀렸나」 문의(쉽게·정확하게 #25).

## 모르는 것
- 설계 §20-10「#1·#2 와 같은 상수」가 레거시 비교 칸에 매칭을 더하지 않는 것을 의도했는지(⚠️) · 화면에서 DIFF 가 어떻게 보이는지(대사표 화면은 이관 직후에만 열려 ⚠️미실측).

## 요구
- 설계팀장 판단: (가) `receivable`/`payable` 비교 erp 값 = 남은 금액 + 그 뒤 매칭(= #9 와 같은 규칙) (나) 지금대로 두고 detail 에 「이관 뒤 수금·지급 n원 때문」 설명 · 맞추지 않고 결재 후 수정.

## PM 판정 · 봉합 (브라운킴 · 2026-09-17 · 사장님 전결 §15-15)
- **(가) 채택** — ⑥ `receivable`/`payable` 비교 칸 = 히트판 잔액 + 이관 뒤 이월 매칭(M). 거래처별 #9(설계 §20-9 「매칭만으로 차이가 안 생기게」)와 같은 규칙 · 같은 표 안에서 말이 엇갈리지 않게. M 은 「그 뒤 수금·지급」 참고 행에 따로 보인다. 이관 뒤 **새 거래**(매출·매입)는 종전대로 차이로 드러난다.
- 봉합 `49b970b3` (`MdbReconPosting.AddBalanceItems` · 설명 줄에 기준 문구) · 🆕 G9-52(순수 · 매칭만 → OK) · G9-a 기대값 200,000(160,000 + M 40,000 · 여전히 DIFF).
- 빌드 Tests 0/0 · `LegacyBalanceFormulaGate|MdbReconPostingGate|MdbMigrationV2Recon|MdbLegacyBalanceGate|LegacyBalanceLabelParityGate` 56/56.
- 봉합 빼고 FAIL(커밋 뒤 · 치환 1): M 가산 제거 → G9-52·G9-a FAIL(2/6) → 원복 · src diff 0.
- ⚠️ API 실경로 재실측은 안 함(순수 식 한 곳 · 입력 `ReadBalanceErpAsync` 는 2단계B 에서 값 확인됨) — [5] CTO 판단 대상으로 명시.
- 상태: ⬛ 봉합.
