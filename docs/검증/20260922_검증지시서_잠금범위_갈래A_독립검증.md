# [4] 독립검증 지시서 — 잠금 범위 갈래 A (2026-09-22)

> 🟢 **사장님 오더 (2026-09-22)**: *"4-3 · 4-4 봉합해."*
> 🔴 **작성자 ≠ 검증자.** 아래 대상은 **PM 브라운킴이 만든 것**이다. **PM 이 스스로 검증하면 [4] 가 아니다.**
> 검증 관점 = **검증팀장 데이비드 박** — *"되는 것을 확인하지 말고 **안 되는 경우를 찾는다.**"*

## §1 검증 대상 (커밋 고정)

브랜치 `feat/20260920-rc-safe-path`

| 커밋 | 무엇 |
|---|---|
| `ae3697f2` | **A-1 봉합** — G-RC17 측정이 `innodb_trx` 캐시 지연에 걸리던 것 |
| `00d65972` | **G-RC16 신설** — W-3(등록 경로 침해 없음) 증명 |
| `cc61c84a` | **A-4** — 기존 회귀 전수 실행 결과 기록 |
| `c03c2178` | **`.gitignore`** — DB 덤프 백업 폴더 누출 봉합 |
| `a93e901d` | **별건 3건 분리 + 게시 조건 P-25~P-27** |
| (직렬화 커밋) | **A-4 봉합** — 잠금 비율 게이트를 `DisableParallelization` 컬렉션으로 |

**근거 문서**: 개발명세서 `docs/개발/erp/20260921작2_갈래A_잠금봉합_개발명세서.md` **§9 · §10 · §11** · 작지 **§18** · 별건 등록부 `docs/운영기록/20260922별건1_…`

## §2 🔴 반드시 반증해야 하는 주장 (PM 이 한 말 그대로)

| # | PM 의 주장 | 🔴 **어떻게 깨 볼 것인가** |
|---|---|---|
| **V-1** | 「A-1 원인은 `innodb_trx` 캐시 지연이다」 | **재시도를 1회로 줄이면** 다시 `-1` 이 나오나? **다른 설명**(권한·풀링·read-only 최적화)으로도 같은 현상이 나오나? |
| **V-2** | 「끝내 못 잡으면 `-1` 을 그대로 둔다 — 거짓 초록을 안 만든다」 | **재시도 상한을 0 으로** 만들거나 **잠금을 안 쥐게** 해서 **A-1′ 가 실제로 우는지** 확인 |
| **V-3** | 「G-RC16 이 W-3 을 증명한다」 | 🔴 **프로브가 보유자와 같은 자리를 짚나?** 대조군이 통과한다고 프로브가 **의미 있는 자리**를 잡은 건 아니다. **보유자 없이도 막히는 사례를 만들어 보라** |
| **V-4** | 「봉합 후 새로 막힌 등록 0건」 | **4%·5%·10% 칸**에서도 0건인가? **PM 은 0%·2% 만 쟀다** |
| **V-5** | 「이번 변경이 만든 실패 0건」 | **대조에 쓴 커밋이 옳은 기준선인가?** `d457d6f3` 말고 **봉합 직전 커밋**에서도 같은가? |
| **V-6** | 「병렬 간섭이 원인이었다」 | 직렬화 **뒤에도** TaxInvoice 가 죽는 조합이 있나? **순서를 바꿔** 돌려 보라 |
| **V-7** | 「제품 코드 무변경」 | `git diff` 로 **직접** 확인. 테스트만 바뀌었나? |
| **V-8** | 「`.gitignore` 가 소스를 안 지운다」 | **추적 파일 수 3,169 불변**을 직접 재라. 🔴 **다른 이름의 덤프**(`backup_2026.sql` 등)는 여전히 새 나가는가? |

## §3 🔴 PM 이 스스로 적은 한계 — **여기서 더 찾아라**

> 아래는 **PM 이 이미 안다고 적은 것**이다. **이것만 보라는 뜻이 아니다.** PM 이 **못 본 것**을 찾는 게 [4] 다.

| # | PM 이 적은 한계 | 출처 |
|---|---|---|
| 1 | **이웃 프로브의 보유자는 수금 축(L2) 하나** — 지급 축을 쥔 채는 안 쟀다 | 명세서 §10-4-1 |
| 2 | **⑤ 는 서비스 계층이 아닌 SQL 근사** — 응용 계층 검증·분기를 안 탄다 | §10-4-2 |
| 3 | `purchase_receipts` 가 **빈 표**라 ⑤ 의 L4 읽기는 0행을 센다 | §10-4-3 |
| 4 | 판정 구간 **0%·2% 두 칸** | §10-4-4 |
| 5 | **`HLL=0` 을 못 맞춘다** — 기록만 하고 판정에 안 쓴다 | §8-5 · §9-6-3 |
| 6 | **W-1 8문장 전수 값 동등은 아직** | §18-5 |
| 7 | 보고표 **한글이 콘솔 인코딩에서 깨진다** | §10-4-5 |

## §4 🔴 무력화 재현 (전부 직접 돌려라 — 「적혀 있다」로 통과시키지 마라)

| # | 무엇을 빼면 | 어느 게이트가 울어야 하나 |
|---|---|---|
| N-1 | **C4 인덱스를 뺀다** | G-RC17 의 2%·4% |
| N-2 | **㉪ 를 뺀다** | G-RC17 의 0% · **A-3 살아있음** |
| N-3 | **C2 인덱스를 뺀다** | G-RC17 의 2~4% |
| **N-8** 🆕 | **봉합 쪽을 나쁘게 만든다**(봉합 사본 보유자를 ㉪ 없는 SQL 로) | **G-RC16 이 W-3 위반으로** — PM 이 한 번 확인했다. **다시 하라** |

🔴 **하나라도 「울어야 하는데 안 울면」 그 게이트는 아무것도 안 재고 있다.**

## §5 실행 환경

```
33306 (안전)  : mariadbd --defaults-file=C:/HitPanBuild/mariadb-iso-0917/my.ini
33406 (STATEMENT): mariadbd --defaults-file=C:/HitPanBuild/mariadb-stmt-0920/my.ini
⚠️ 세션 시작 시 둘 다 꺼져 있을 수 있다 — 반드시 확인하고 띄워라.

HITPAN_REQUIRE_DB=true   ← 🔴 SKIP 을 통과로 세지 않는다
HITPAN_DB_HOST=127.0.0.1  HITPAN_DB_PORT=33306  HITPAN_DB_USER=root  HITPAN_DB_PASS=
HITPAN_TEST_DB=hitpan_e2e
HITPAN_MYSQL="C:/Program Files/MariaDB 11.4/bin/mysql.exe"
HITPAN_DB_STMT_PORT=33406  HITPAN_DB_LOCK_PORT=33306
```

🔴 **`EXIT` 코드를 통과로 읽지 마라** — PM 이 그 함정에 걸릴 뻔했다. **결과 줄**(`실패: N · 통과: N · 건너뜀: N`)을 읽어라.
🔴 **운영 DB `hitpan_erp_t004`(3306)·이 PC 서비스·`appsettings` 무접촉**(#21 · #39).
🔴 **측정 DB `dp_full` 은 오염됐다** — 쓰지 마라. 어느 DB 에서 쟀는지 **매번 적어라**.

## §6 산출물

`docs/검증/20260922_작업리뷰서_잠금범위_갈래A_독립검증.md`

**반드시 담을 것**:
1. **V-1~V-8 각각에 대한 판정** — 🟢확인 / 🔴반증 / ⚠️못 쟀다. **「못 쟀다」를 숨기지 마라**
2. **무력화 N-1·N-2·N-3·N-8 을 직접 돌린 결과**(울었나)
3. **PM 이 못 본 것** — 새로 찾은 결함
4. **어느 DB · 어느 커밋 · 몇 회** 쟀는지
5. 🔴 **판정**: [5] 로 올릴 수 있나, 아니면 반려인가

## §7 🔴 진행 규칙 (이 트랙에서 서브에이전트가 여러 번 턴 한도로 멈췄다)

1. **문서 뼈대를 먼저 만들고 커밋**한 뒤 검증을 시작한다
2. **10턴마다** 문서를 갱신하고 커밋한다
3. **멈추기 전에 남은 것을 「안 한 것」으로 적고 커밋**한다 — 빈손으로 끝내지 마라
4. **코드를 고치지 마라.** [4] 는 **판정**이다. 결함을 찾으면 **문서에 적는다**

---

**지시**: PM 브라운킴 · 2026-09-22 · **사장님 오더 「4-3·4-4 봉합해」** · **다음: [4] 착수 → [5]**
