# INDEX — docs/검증

> 작업리뷰서·전수조사·병렬이슈 — **루틴 [4]·[3-V]** 산출물이 쌓이는 곳
> 문서 181개 (하위 2폴더) | 최신갱신 2026-08-28 | 헌법 #41·#42
> **담당**: 검증팀장 데이비드 박 · 보안상무 안철수 · 법무팀장 김앤장  ([폴더담당 표](../POLICY_폴더담당.md))

## 이 폴더는 루틴 어디인가

```
[3] 개발명세서(→ 개발/)          ⟷  [3-V] 동시 병렬검증 — 보안상무 안철수
      ↓                                    📄 병렬이슈 = 여기 병렬이슈/
[4] 개발명세서 + 코드리뷰 → 📄 작업리뷰서 = 여기 {영역}/
      ↓
[5] 작업리뷰서 + 병렬이슈 참조 → CTO 래리 → 사장님 결재 (→ 운영기록/)
      ↓
[6] 🔴 결재 + 검증 통과 = 무조건 커밋   ← 1파츠 완성
```

**앞 문서**: [`개발/`](../개발/INDEX.md) 개발명세서 · **다음**: [5] CTO·사장님 결재 → [`운영기록/`](../운영기록/INDEX.md)
**틀**: [`_틀/작업리뷰서_틀.md`](../_틀/작업리뷰서_틀.md) · [`_틀/검증보고서_틀.md`](../_틀/검증보고서_틀.md)

> ## 🔴 SoD — 만든 사람 ≠ 검증하는 사람
> **구현한 사람은 여기 못 들어온다.** 자기검증은 거짓봉합을 통과시킨다.
> 검증은 "됐다"를 확인하는 게 아니라 **"안 되는 경우를 찾는 것"**이다.

---

## 하위 폴더

| 폴더 | 문서 | 무엇 |
|---|---|---|
| [erp](erp/INDEX.md) | 77 | 전수조사·검증팀 판정·감사 결과 |
| [병렬이슈](병렬이슈/INDEX.md) | — | ⭐ **[3-V] 동시 병렬검증 적발 건** (1건 = 1파일) |

> **병렬이슈가 왜 따로인가**: 최종 작업리뷰서에만 적으면 **묻힌다.**
> 언제 무엇이 적발·봉합됐는지 추적하려고 별도 관리한다. 상세 → [병렬이슈/INDEX.md](병렬이슈/INDEX.md)

## 루트 작업리뷰서

| 문서 | 판정 | 최종수정 |
|---|---|---|
| [20260828_5축전수조사_상품BOM매입매출계산서_실물실측.md](20260828_5축전수조사_상품BOM매입매출계산서_실물실측.md) 🆕🟢📌 | 🟢 **[1-V] 선행검증 — 5축 전수조사 (실물 환경 실측)**. 사장님 오더 *"전체 흐름을 보자"* · 대상 = **설치 실물**(`test1234.hitpan.kr` · `hitpan_erp_t004` 3307 · 1.3.30), 시험용 `hitpan_e2e` 아님. 🔴**매입 정상·매출 반쪽** — 무결성 **매입 9 / 매출 3**(정합성 9/6 · 속도 10/9). **P0 4건**: ①**명세서 중복생성 — 방어선 3겹이 전부 뚫린다**(1차 가드는 **화면이 쓰는 경로에 없고** · 2차는 `OrderItemId` 있는 줄만 봐서 빼면 우회 · 3차는 `delivered_qty` 가 안 올라 OK 통과) ⇒ 수주1→명세서3장·**재고 실제 출고** ②**반품 수량상한 없음**(납품1개에 99개 통과 · 재고 2→101 · 유령매출 9,900 · 🟢**취소는 정상 복구**⇒되돌리는 장치는 멀쩡, **못 넘게 하는 장치가 없다**) ③🆕**명세서 취소가 과거 반품을 증발**(`:1181` 이 `total_sales` 를 **명세서만 보고 통째로 재계산**, `sales_returns` 를 안 뺀다 ⇒ 무관한 오타 명세서 1장 취소에 4월 반품 200만이 사라지고 미수 부활 · 🔴**조용하다** · **매입엔 이 코드가 없다**) ④🆕**업체별 원장이 「돈 원장」이 아니다**(상품축과 **같은 표를 GROUP BY만 달리** 읽어 잔량이 **금액 아닌 개수** · 🟢**계산은 맞다** — `partner_balance.balance` 정확, **화면이 어느 표를 보느냐**의 문제 · 영업: **데모에서 수량 나오면 제품 카테고리를 의심받는다**). 🔴🔴**가장 무거운 발견 = CI 에 DB 가 없다** — `build` 잡(시험 1100개 전부) **DB 없음** / `ddl-smoke` 잡(스모크 1개) **DB 있음** ⇒ **DB 띄우는 잡은 시험을 안 돌리고, 시험 돌리는 잡은 DB 가 없다.** DB 게이트 **101곳이 `return`(≠`Assert`)** = xUnit 에겐 **PASS** ⇒ **진짜 게이트 3종도 한 번도 안 돌았다**, *"1100 통과"* 는 **글자검사만의 통과**였고 **그게 P0 4건이 뚫린 이유**. 무력화시험 **진짜 3/가짜 8**(P0 완전복원·사슬표기 NULL·국세청 이중전송 차단 제거·분개 불균형 — **전부 통과**) ⇒ 가짜 누적 22번 중 최대 규모. 🟢**설비는 있다**(`ddl-smoke` MariaDB 복제 · `TrialBalanceGateTests` 가 실서비스를 실DB에 물려 부름) ⇒ **새로 만들 게 아니라 연결만**. 🟢**사장님 판정식이 그대로 게이트**(상품축 총합 == 업체축 총합 — 깨지면 재고 2배·회계 1배라 **반드시 FAIL**, 글자로 흉내 불가). P1 8건(**부가세 신고 매출측 `sales_returns` UNION 없음**·**매입매출장에 매출반품 행 자체가 없음** ⇒ 🔴**현장 기준 P0 1순위, 세무사에게 나간다** · draft 를 매출로 집계 · 반품 미상계 · 안전재고 `<=` **8/25 건과 다른 건**(증식 없음·대조실험 2건) · `/api/stock/ledger` 7종 전부 500 인데 **고객 도달 경로 0**(라우트명이 화면과 같아 살아있는 것처럼 보이는 함정)). 🟢**오탐 7건 확정**(철자갈림·취소분 비대칭·계산서 `tenant_id`(**이미 봉합, 메모리가 낡음**)·반제품 사슬오염(**봉합됨**)·매입리포트 반품(**회귀 없음**)·stock vs ledger·**`stock_ledger` UNIQUE 는 의도였다**). 🔴**§7 = 이 조사가 뒤집은 PM 보고 8건** — [1-V] 가 왜 설계보다 먼저인지의 증거 | 2026-08-28 |
| [20260827_마이그_정합_전수조사_보류트랙.md](20260827_마이그_정합_전수조사_보류트랙.md) 🆕🟡📌 | 🟡 **조사 완료 · 착수 보류** (사장님: *"마이그 영역은 나중에 따로 작업할거야"*). **레거시 MDB 직접 실측**(`공영정보DB\PANDATA.mdb` · 읽기전용). 🔴🔴**P0 미수금 341억 과소계상** — `MdbMigrationService.cs:2153` 의 `if (amount == 0) amount = GetDec(row,"S_BAL")` 한 줄. `DOCF5` 는 `S_GU` 로 **두 계열**(`S_SUK` 수금 / `S_BAL` 발생)인데 전량 `collections`(수금)로 들어간다. 실측: 마이그가 넣는 480.7억 중 **진짜 수금 139억 · 잘못 섞인 발생분 341.7억**. `S_GU='0'` **50만 건**은 적요가 *"매출세액"·"HTP21C 표준형-S"* = **채권 발생**이지 수금이 아니다. 🔴**P0 미지급 과대** — `INSERT INTO payments` **0건**(수금 61만 건은 이관하는데 지급만 없다). 🟡**S_GU 코드 의미가 코드·문서에 정의 0** ⇒ **사장님 확인이 P0 둘의 전제**. 🔴**대사 수단 자체가 없다** — `MdbMigrationResult` 는 **행 수만** 보고, 금액·잔액 대사 0건 ⇒ *"레거시와 정합하나"* 를 **잴 자가 없다**(착수 시 1순위 = 대사표). 🟢**정합 확인**: 원장·재고(`item_stock` 리빌드 있음)·회계(`source_type='migration'` 격리). 🔴**마이그가 확정동작을 안 거치고 결과를 직접 넣는 건 옳은 설계** — 동작을 태우면 **이중 반영**된다(나중에 이걸 바꾸지 마라). 🔴**수주·발주 소급 생성 금지 확정** — 레거시 실물이 명세서 236,139 vs 수주 **180**·발주 **682**(0.1% 미만, 안 쓰던 기능) · DOCFB 에 수주번호 필드 없음 · 만들면 수주현황 리포트가 `is_auto` 미필터라 **180→수만 건 왜곡**되는데 목록은 `is_auto=0` 라 **안 보임**(비대칭) · **회계·재고 기여 0**. 🟡**명세서↔계산서 링크 SQL 은 작성돼 있으나 실행 배선 0건**(파일명이 `DB-NN_*` 아님) | 2026-08-27 |
| [erp/20260821_검증리뷰서_업체마스터3건_자동발주멱등_기본창고.md](erp/20260821_검증리뷰서_업체마스터3건_자동발주멱등_기본창고.md) 🆕🟢 | 🟡 **조건부 통과** (20260821작1 · 1.2.96) — 봉합 2건은 **반증으로 확인**. ⭐**W4 자동발주 멱등 대조실험**: 필터 빼면 6건 → 넣으면 4건, **결과가 실제로 달라짐**(가짜 게이트 아님). `partial`(부분입고) 제외가 핵심 — DESCRIBE 실측이 설계 초안의 누락을 잡았다(빠뜨렸으면 부분입고에서 중복 재발). 🔴**음성 대조**: `tenant_id` 조건을 일부러 빼니 **크로스테넌트 누수 재현** ⇒ 조건이 장식이 아님 입증(§#2). ⭐**W6 기본창고 유령창고 회귀검사(10차 P0-4 재발 여부)**: 없는 창고·비활성·타테넌트 **전부 차단**, `is_active` 토글로 인과 확인 ⇒ **재발 없음**. 사장님 지적 원인은 예상과 달랐다 — `ItemDetail.razor:121` *"참조용 — DB에 저장하지 않음"*, **저장 버그가 아니라 저장할 자리가 없었다**(items 40컬럼에 창고 컬럼 부재). ddl-smoke 백지→150테이블·마이그 멱등·신규=업그레이드 일치(§#36)·빌드 0/0(§#19). 🔴**미검증 3건은 "됐다"가 아니다**: ①좌표 — **카카오 REST 키 미발급**으로 한 번도 실행 안 됨(x=경도/y=위도 매핑 미확인) ②이메일 실수신 — SMTP 미설정 ③팩스 실송출 — **벤더 미결이라 경로 자체가 없음**(Mock 이 `Success=false` 로 정직하게 차단 — *미완이 아니라 정직한 미완*). 🔴**사장님 판단 필요**: 멱등 필터가 `is_auto=1` 만 제외 ⇒ **수동발주 직후 자동발주가 또 나갈 수 있다**(오더 범위 밖이라 미조치) | 2026-08-21 |
| [20260807_검증리뷰서_작2_N-10_발견주기_4안혼합.md](20260807_검증리뷰서_작2_N-10_발견주기_4안혼합.md) 🆕🟢 | 🟡 **조건부 통과** (작2 · 미커밋) — 채점표 9개 중 **통과 5 / 미시험 4 / 실패 0**, 코드 결함 **P0·P1 0건**. 빌드 0/0 검증팀 직접 실측. ⭐**반증 9종 전부 수행**: 경계값 `<`(무한대기 없음)·미래시각 방어(`Stamp:93-98` — 없었으면 시계사고 1건에 업데이트 영구정지)·쓰기실패 fail-open(**P0 조건 미해당** — 디스크 가득차도 업데이트 진행)·clamp 우회 **호출부 전수 확인**(게이트 진입점은 `Worker.cs:419` 단 하나, 1440→60/0·음수→5)·야간창 **위상 60종 시뮬레이션 0건 누락**(실현주기 정확히 3600초). 🟡**신규 P2 2건**: ①야간창 보장이 `상한60분==야간창60분` 우연에 의존(역방향 경고 없음) ②예외경로(`:531`)가 결정론적 예외 시 **60초마다 재조회=의도의 60배** — 둘 다 **종전부터 있던 성질**(종전 1,440배)이라 반려 아님. ⭐**자체신고 4건 전부 정직 확인**: payload 건은 `.iss:1163-1206` 직접 열어 **통째 덮어쓰기 사실 확인** → 고객 PC 에 키는 없으나 `Bind` 가 기본값 60 보존 ⇒ **동작영향 0·별건 인정**. 종전지연 "최대24h"는 **부정확 — 실제 1분~24시간**(자정 해제라 마지막 확인시각에 좌우) ⇒ **기존 문서 정정 필요**. 🔴**§7-3(재시작 없이 발견)=이번 작업의 존재 이유인데 미시험** — 코드상 성립하나 **60분 실측 1회 필요**, 통과로 쓰지 않음(개발PC 정상작동은 검증 아님). 커밋 권고=**(나) 지금 커밋 후 실측**(실측하려면 이 코드로 빌드해야 하므로 커밋이 실측의 전제) | 2026-08-07 |
| [20260807_프로젝트_시험성적서_배포종단_설치_워치독_업데이트.md](20260807_프로젝트_시험성적서_배포종단_설치_워치독_업데이트.md) 🆕🟢 | 🟡 **조건부 합격 (제2판 · 오후 개정)** (대상 **1.2.55** · 종단 7단계 **45항목**) — ⭐**C-1 채증 반영**: PASS **15**(+6) / 조건부 12 / **미시험 9**(−2) / 문서주장only·FAIL **9**(+3) = 증거 없이 통과로 읽힐 항목 **12건(27%)**, 오전 45%→27%. 🟢**G6(워치독 자기교체) PASS** — `1.2.52→1.2.55` 고객 조작 0회 실측(채증기록). 🔴**신규 적발 N-10: 워치독이 새 버전을 스스로 발견 못 함** — 사람이 `sc start` 를 쳐야 갱신됨(헌법 #30 미성립, **원인 미규명·P0 후보**). ⬜**C-3(실패 주입) 사장님 A안 결재로 이월** — 미채증은 미채증으로 유지. 🔴**작7 커밋 `ab566a4` 는 `origin/main` 미포함 = 1.2.55 고객에겐 안내화면 없음**(커밋≠게시). **양보불가 조건이 C-1·C-3 → C-3·C-8 로 교체.** **누리집 = 장별 판정**(워치독 버전확인·통신판별 장 🔴→🟢 전환 / "밤에 저절로 업데이트" 안내는 N-10 이 반증 ⇒ CS 매뉴얼 §4-4 정정 권고) | 2026-08-07 |
| [20260807_채증기록_C-1_워치독자기교체_실동작_원본.md](20260807_채증기록_C-1_워치독자기교체_실동작_원본.md) 🆕🟢 | 🟢 **진실원 (1차 증거 · 가공 없음)** — 사장님 **백지환경(Windows Sandbox · TEST1) 직접 채증**. ⭐**C-1 PASS**: 자기교체 능력이 **없던** 1.2.52 워치독을 새 API 핸드오프(W4-3-H)가 `1.2.55.0` 으로 교체 — **두 달 무한루프의 탈출구가 실제로 작동**. 잔재(`.new`/`.old`/`.handoff`) 정리 확인 · `--health` OverallStatus healthy · 테넌트 식별자는 **해시만** 출력(#18·#22 정합). 🔴**새로 드러난 3건**: ①워치독이 새 버전 자력 발견 실패(사람 재시작 필요) ②`Get-ScheduledTask` CIM 거부(CS 진단 절차 미반영) ③디스크 여유공간 제품 검사 0건(설계팀 P0-2 동일). ⬜**C-3 미채증 — 교체가 이미 끝나 재료가 없어 시험 불성립, 강행하면 거짓봉합**이라 다음 차수 이월(사장님 A안) | 2026-08-07 |
| [20260731_검증리뷰서_6-1_taskkill봉합_시크릿진탐.md](20260731_검증리뷰서_6-1_taskkill봉합_시크릿진탐.md) 🆕🔴 | 🟡 **조건부 통과** (PR #31) — ⭐**SoD 2연속 실증**: v1 이 "봉합한 척"으로 반려됐다(`= 1060` 은 종료코드가 아니라 **메시지 텍스트**, 실측 `sc stop` 부재=**36**). PM 이 자기 설계서 §2 경고를 스스로 어긴 사례. v2 는 `sc query <> 0` 실측 판정으로 재작성. ⚠️**진범 재현 미완** — Sandbox hang 을 못 만들었다(조건부인 이유). 시크릿 1건=`.iss:1545` **주석**·6주전 `eaa8f15`·무해→A안 머지 결재. **실측 로그 2줄(`6-1-svcprobe`/`6-1-taskkill 건너뜀`) 없으면 재반려** | 2026-07-31 |
| [20260731_판정기준서_작10_RV3-RV7_백지실측.md](20260731_판정기준서_작10_RV3-RV7_백지실측.md) 🆕 | 🟢 **실측 착수 전 확정** — RV-3~RV-7 통과/실패를 값으로 못박음. ⚠️`.iss:1933` 주석이 RV-6(토큰 에코)을 미리 단정 → 근거 삼지 말 것. §3 결과 3분기(성공/원인확보/여전히침묵=작10 실패) | 2026-07-31 |
| [20260730_작10_P0-5_설치6단계관측성_검증리뷰서.md](20260730_작10_P0-5_설치6단계관측성_검증리뷰서.md) | 🔴 **반려 P0 3건** → 봉합 후 재검증 대기. ⭐**SoD 실증 사례** — PM 자기검증 0건 vs 독립검증 P0 3건. PM 봉합코드가 침묵을 '거짓 성공'으로 바꾼 것을 실측으로 적발 | 2026-07-30 |
| [20260730_네트워크진단서_터널1033_원인확정_실측완료.md](20260730_네트워크진단서_터널1033_원인확정_실측완료.md) | 🟢 진실원 — 1033 원인 실측 확정 | 2026-07-30 |
| [20260730_NCP침해지표_채증결과.md](20260730_NCP침해지표_채증결과.md) | 🟢 SSH 무차별대입 채증 | 2026-07-30 |

---

## 3관문 판정 (검증팀장 기준)

| 관문 | 통과 조건 |
|---|---|
| 빌드 | errors 0 + **warnings 0** (헌법 #19) |
| ddl-smoke | `scripts/ddl-smoke-test.sh` 통과 |
| 독립반증 | 구현자가 아닌 사람이 "안 되는 경우"를 찾아봤나 |

> ⚠️ **개발PC 정상은 검증이 아니다** — 백지 환경(Windows Sandbox) 실측만 인정.
> **전수조사 정의(정본)**: 빠짐없이 + 실동작 + 독립검증. 셋 다 아니면 전수조사가 아니다.

## 새 작업리뷰서를 쓸 때

```
① 근거 작업지시서·개발명세서 주소 확보  → 운영기록/INDEX.md · 개발/INDEX.md
② 검증자가 구현자와 다른지 확인          ← SoD
③ [3-V] 병렬이슈 봉합 여부 확인          ← 미봉합 있으면 반려 사유
④ 저장 후 해당 하위 INDEX 한 줄 갱신     ← 빠뜨리면 [5] 결재가 근거를 못 찾는다
```

> 전체 목록은 각 하위 폴더 INDEX / `INDEX_ALL.md` 에 있다. 이 문서는 **길잡이**다.
