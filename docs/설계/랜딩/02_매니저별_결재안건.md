# 어벤져스 9명 매니저별 결재 안건 (2026-06-01 점심 후)

> **PM**: 브라운킴
> **목적**: 사장님 1차 결재 → 작업지시서 발행
> **헌법**: #11 (사장님 의견조차 검증) + #32 (받아쓰기 금지) 정합

---

## ⚠ PM 헌법 #11 첫 응답 3종 (자가 적용)

이 문서는 매니저들이 자가 검증한 안건만 박제. PM이 받아쓰기한 내용 0건.

---

## 1. AI수석 (구조·프롬프트·검증)

### 안건
- **신규 코드 5중 검증 강화** (헌법 #23)
- 작업지시서 보안 요구사항 명시 의무
- 매니저 리뷰 → SAST → DAST → 데이터 최소주의 → 백신 호환

### 함정 (자가 검증)
- 베타 7/22까지 EVF·DAST 미실측은 출시 게이트 위반
- 받아쓰기 헌법 #32 — 매니저 결재안 무비판 수용 금지

### 결재 요청
- ✅ AI 협업 5중 검증 게이트 즉시 적용
- ✅ 작업지시서 보안 명세 미박제 시 반려

---

## 2. 백엔드 매니저

### 안건
**A. Pull 동기화 API 신규 (P0)**
- `GET /api/sync/employees` — 이름·이메일·직급·재직여부만 (헌법 #18 정합)
- `GET /api/sync/devices` — 기기ID·기기명·등록일만
- 인증: Sync 전용 토큰 (일반 JWT 분리, 읽기 전용)

**B. 백오피스 API 확장**
- `/api/backoffice/tenants/{id}/employees` (Pull 복사본 조회)
- `/api/backoffice/tenants/{id}/devices`
- `/api/backoffice/provisioning` (현황·재시도)
- `/api/backoffice/subscriptions/{id}/refund` (환불)

**C. 결제 Webhook**
- `/api/billing/webhook/toss` — 토스페이먼츠 콜백
- 멱등성 (Idempotency-Key)

### 함정
- Sync 토큰 탈취 시 영구 권한 → 24시간 만료 + 회전 필수
- Webhook 서명 검증 누락 시 위변조 가능

### 결재 요청
- ✅ 1차 Pull API 2종 신규 (작업지시 가도)
- ✅ Sync 토큰 정책 (24h + 회전) 사장님 결재
- ✅ Webhook 서명 검증 표준 박제

---

## 3. DB 매니저

### 안건
**A. 신규 테이블 2건**
- `tenant_employees_snapshot` — Pull 복사본 (5분 주기 UPSERT)
- `tenant_devices_snapshot` — Pull 복사본

**B. 기존 테이블 확장**
- `licenses` — 프로비저닝 상태 컬럼 (`provisioning_status` ENUM)
- `tenants` — `subdomain` 컬럼 (hitpan-{name}.kr)

**C. 본사·고객 데이터 경계 (헌법 #18·#22)**
- snapshot 테이블에 업무 데이터 컬럼 절대 금지
- 5중 검증 ⑤ (데이터 최소주의)에서 grep 차단

### 함정
- snapshot 테이블에 무심코 컬럼 추가하면 헌법 #18 위반
- Pull 실패 시 stale 데이터 보존 정책 (마지막 성공 시각 표시)

### 결재 요청
- ✅ DB-77 / DB-78 신규 테이블 DDL (sandbox 결재 후 적용)
- ✅ 본사 데이터 그렙 차단 룰 박제

---

## 4. 보안 매니저 1 (헌법·앱·인증)

### 안건
**A. Sync 토큰 정책**
- 별도 발급 endpoint (`POST /api/auth/sync-token`)
- 24시간 만료 + 자동 회전
- 읽기 전용 권한 (Authorization Policy)
- 본사 IP 화이트리스트 권장

**B. 백오피스 인증 (BackofficeAuthController 정합)**
- platform_admin / reseller_admin 분리
- 본사 IP 화이트리스트
- 2FA (향후)

**C. 약관·법령 정합**
- 랜딩 가입 시 약관 동의 필수 4
- 헌법 #18 v3 (본사 데이터 경계 법령 9개)

### 함정
- 베타 출시 전 OWASP ZAP DAST 1회+ 의무 (헌법 #23)
- 본사 IP 화이트리스트 누락 시 reseller_admin 외부 노출

### 결재 요청
- ✅ Sync 토큰 보안 정책 박제
- ✅ 백오피스 IP 화이트리스트 정책 결재
- ✅ DAST 일정 7/15 박제

---

## 5. 보안 매니저 2 (인프라·OS)

### 안건
**A. 프로비저닝 Cloudflare 자동화** (헌법 #29 사전 승인)
- Cloudflare API 토큰 본사 서버에만
- DNS 생성 + 터널 발급 자동화
- 인스톨러 EXE에 토큰 절대 포함 금지

**B. 워치독 17개 시나리오**
- AdminWatchdogMonitor 화면 + watchdog_pings 테이블 정합
- 5분 자동 복구 정책

### 함정
- Cloudflare API 토큰 노출 시 전체 인프라 장악
- 헌법 #29 위반 시 PM 사임/해고

### 결재 요청
- ✅ Cloudflare API 토큰 보관 정책 (본사 KMS or 환경변수)
- ✅ 인프라 작업 사전 결재 절차 명시

---

## 6. 프론트 매니저

### 안건
**A. 랜딩 Razor 변환**
- `landing-web.html` (897 라인) → Blazor 페이지 + 라우트 분리
- `/` `/pricing` `/signup` `/signup/verify` `/payment` `/welcome` `/download`
- WASM 첫 fetch 패턴 (진범 #100 정합 — OnAfterRenderAsync)

**B. 백오피스 UI 확장**
- 고객사 상세 화면 (직원·기기 Pull 복사본 표시)
- 프로비저닝 현황 화면 (상태·재시도 버튼)
- 모바일 반응형 (랜딩 우선)

### 함정
- `landing-web.html` 통째로 Razor 전환은 위험 → 라우트별 분리
- WASM 부팅 fetch 진범 #100 재발 가능

### 결재 요청
- ✅ 랜딩 라우트 7개 신규 Razor 박제
- ✅ 백오피스 신규 화면 3개 박제
- ✅ WASM 첫 fetch 표준 5중 검증 ②번에 포함

---

## 7. 수석 웹디자이너

### 안건
**A. 랜딩 UI 일관성**
- `landing-web.html` 그린·오렌지 톤 유지
- 더존·이카운트 차별화 ("3분 안에 쓸 수 있는 ERP")
- 토스 스타일 결제 흐름 (`preview-design-toss.html` 참조)

**B. 백오피스 UI 가이드**
- 본사 운영자 효율 우선 (정보 밀도)
- 한 화면 완결 원칙 정합

### 함정
- 랜딩이 너무 화려하면 "신뢰 0" — ERP 정공법 차분한 톤
- 백오피스가 화려하면 "장난감" — 운영 도구 정공법

### 결재 요청
- ✅ 랜딩 톤 유지 (현재 톤 GO)
- ✅ 백오피스 정보 밀도 가이드 박제

---

## 8. ERP 매니저 (30년 SI)

### 안건
**A. 마이그 흐름 정합**
- 신규 가입 → 빈 DB 또는 MDB 마이그 선택
- MDB 마이그는 ERP 내부 `/settings/mdb-migration` 기존 자산

**B. 백오피스 ↔ ERP 정합**
- 직원·기기는 ERP 원본 (헌법 #18·#22)
- 백오피스는 복사본만 (조회 전용)
- ERP에서 직원·기기 수정 → 5분 내 백오피스 반영

### 함정
- 백오피스 화면에서 직원 수정 시도 → 무조건 차단
- Pull 실패 시 백오피스가 "직원 0명" 표시 → 운영자 혼란

### 결재 요청
- ✅ 백오피스 직원·기기 화면 "수정 불가" UI 명시 (편집 버튼 0건)
- ✅ Pull 마지막 성공 시각 + 실패 시 경고 배너 박제

---

## 9. 기술영업팀장

### 안건
**A. 랜딩 셀링 포인트 5가지** (이미 박제)
1. 로컬 DB + 터널 (데이터 주권)
2. 원클릭 설치 10분
3. 3분 안에 쓰는 UI
4. 모바일 동등 시민
5. AI CS 챗봇 24/7

**B. 대리점 가입 흐름** (랜딩)
- 별도 페이지 `/reseller/signup`
- 백오피스 reseller_accounts 박제

### 함정
- 셀링 포인트 너무 많으면 "뭔지 모름" → 5개 고정
- 대리점 신청이 일반 가입과 섞이면 혼란

### 결재 요청
- ✅ 랜딩 5대 셀링 포인트 박제 GO
- ✅ 대리점 신청 별도 페이지 박제

---

## 10. 마케팅팀장

### 안건
**A. 랜딩 카피 30초 룰** (헌법 #25 "쉽게")
- 첫 화면 5초 안에 "히트판이 뭐냐"
- 30초 안에 "왜 좋냐"
- 모든 버튼명 8자 이내

**B. SEO·SNS** (랜딩-sns.html 활용)
- 가입 키워드 → "쉬운 ERP", "설치 5분"
- SNS 카드 (페이스북·카카오톡)

### 함정
- 정통 ERP 포지셔닝 + "AI/바이브코딩" 단어 금지 (헌법 #23)
- 셀링 카피가 헌법 #25 "쉽게"보다 어려우면 모순

### 결재 요청
- ✅ 랜딩 카피 30초 검증 의무
- ✅ SEO 키워드 5개 박제

---

## 11. PM 최종 통합 결재 요청

### 11.1 P0 (즉시 작업지시)
1. **DB-77/78 snapshot 테이블 DDL** — DB 매니저
2. **Pull API 2종 + Sync 토큰** — 백엔드 + 보안 1
3. **랜딩 라우트 7개 Razor 변환** — 프론트 + 디자이너
4. **프로비저닝 Cloudflare API 정책** — 보안 2 (헌법 #29 사장님 사전 결재)
5. **토스페이먼츠 Webhook 표준** — 백엔드

### 11.2 P1 (6/8까지)
6. 백오피스 고객사 상세·직원·기기 화면
7. 결제 환불 처리
8. 대리점 신청 별도 페이지

### 11.3 P2 (6/15까지 1차 마감)
9. OWASP ZAP DAST 1차
10. 백신 5종 호환 테스트
11. 통합 E2E

### 11.4 마일스톤
| 일자 | 목표 |
|---|---|
| 06-01~06-08 | P0 5건 봉합 |
| 06-08~06-15 | P1 3건 봉합 + 1차 통합 |
| 06-15 | 1차 마감 검증 |
| 06-15~06-30 | 3시스템 통합 |
| 07-01~07-15 | EVF 6대 |
| 07-15~07-22 | 백신 5종 |
| 07-22~08-15 | 베타 체험단 4주 |
| **08-24** | **정식 출시 1차 (서비스업 8곳)** |

---

## 12. 사장님 결재 받을 사항 (별도 박제)

| # | 안건 | 헌법 |
|---|---|---|
| 1 | Cloudflare API 토큰 보관 정책 | #29 |
| 2 | Sync 토큰 24h + 회전 정책 | #5·#23 |
| 3 | 본사 IP 화이트리스트 정책 | #5 |
| 4 | DAST 일정 7/15 | #23 |
| 5 | 1차 마감 6/15 | 마스터플랜 v3 |
| 6 | DB-77/78 신규 테이블 적용 | 일반 작업 |
| 7 | 랜딩 라우트 7개 Razor 박제 | 일반 작업 |

**받아쓰기 0건, 매니저 9명 자가 검증 후 통합 박제.**
**PM 브라운킴 / 2026-06-01 점심 후**
