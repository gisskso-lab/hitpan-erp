# [2] 설계 — 자료이관 「머리 없는 명세서 줄」 분류 봉합

## §0 [카드]

| 항목 | 내용 |
|---|---|
| 상태 | 🟡 **개정 2판**(9/15 사장님 (라)·R6·R7·R8·R-A~R-D 전결 반영 · §14~§18 이 정본) · ⬜PM 브라운킴 결재 · 새 안건 R6-2·R-A2·R-B2·R5-2·화면위치(§18) · 1판 §1 포함표·§5 11건 명세서·§8-A·§13 안건은 **2판으로 대체(참고 보존)** |
| 작성 | 설계팀장 존 · 2026-09-15 (작성자 ≠ 결재자) |
| 근거 | 선행검증 `docs/검증/선행/20260915_선행검증서_자료이관_대사차이_머리없는명세서줄_실측.md` §2-2·§3·§5 (Q1~Q4) · 작업지시서 §8-1·8-2·8-3 (사장님 결정 원문) |
| 작업지시서 | `docs/운영기록/20260915작1_자료이관_머리없는줄_분류봉합_작업지시서.md` |
| 규모 | **L** (2판: 갈래 8 · 새 파일 5 · DDL 1(DB-121 + 출하 DDL) · 게이트 7) — §18 · 1판: 파일 5 · DDL 0 |
| 실측 | MDB 사본 읽기 전용(`scratchpad/mdb/PANDATA.mdb`·`PYOJUN.mdb`) · 운영 DB 무접촉 · 코드는 Grep+부분 Read |

**🟢 2판 결론 (30초 · 작업지시서 §10-1~10-6 반영 · 존 MDB 실측 9/15)**
- 판정 = **머리표(DOCFE) 있음 또는 거래처원장(DOCF5) 연결 있음** → 장부 반영 명세서. 연결 키 = (IJ_DT, IJ_SEQ)↔(S_YMD, S_SSUN) + **종류 맞춤**(판매↔S_GU 0 · 매입↔S_GU A). 종류를 안 가리면 머리 없는 232묶음이 오판(§14).
- 반영 판매 115,150 · 매입 1,962 / 🆕 보관 표(장부 미반영) 판매 353 · 매입 210 · 649줄 · 공급가 33,059.20 — **뺀 줄 0** · 재고원장 124,152줄 전부 · 날짜 없는 3묶음 = 2026-02-28(§14·§15).
- 재고 3,147,291 / 54,753,974.4 · 키 = 품명·규격 Trim+대소문자 무시(구분하면 55,253,974.4 로 틀림) · 🔴 끝전은 avg_cost 소수 2자리로 **수학적으로 불가**(3품목 · 차 0.11) → R6-2(§16).
- 미수·미지급 F3 = 거래처별 **마지막 이월행 + 그 뒤 줄** → 142,062,113(1,059) / 51,577,479(530). PM 140,865,113 은 2월 이월 뒤 440줄 누락(§17).
- 🔴 히트판 미수는 기초잔액 칸 없는 분리형 — 수금·지급 표에 맞춤 줄을 넣으면 음수 수금 1,225줄(−1.36억) 오염 → 위치 R-A2(§17·§18).
- 계산서 잠금 = `TaxInvoiceService.cs:76` 뒤 이관 명세서 거부 + `CancelAsync:421` 이관 계산서(§17).

**개정 결론 (1판 · 참고)**
- 🔴 최상위 원칙: **이관 직후 첫 화면 = 레거시 이관 직전 최종 · 누락 없이 모두 정확하게 · 뺀 행은 대사표에 사유·건수·금액.**
- 재고 기준 = **DOCFC 품목별 마지막 달 기말**(1,204줄 = 품명·규격 1,192종 · 수량 3,147,291 · 금액 54,753,974.4). DOCFB 이력은 원장에 그대로 두고 **품목별 「맞춤 줄」 1행**으로 첫 화면을 DOCFC 에 맞춘다(§10).
- 뺀다: 단가행④ 4행 · 금액 있는 시험③ **8행**(7묶음 · 공급가 33,059.20) · 값 전부 0 인 빈 품명 **13행**. 그 밖은 전부 이관.
- 남긴다: 조립·해체① 전량(재고만) · 금액 0 시험③ **12행·11묶음**(명세서+재고 · R2) · 빈 품명 값 있는 **366행**(「미상」 · 재고만, 머리 없음).
- 🔴 **PM 사실 정정**: R2 「금액 0 출고 10행」 → 실측 **12행**(도트용 6 · 공CD 4 · HTP21C 2) · 금액 있는 행은 8. §5.

**초판 결론 (참고 — 아래 3·5 는 개정으로 대체)**
1. **판정 = DOCFE 머리 조인.** 규칙(BUY/SEQ/날짜)과 교차 실측 → 어긋남 **0건**(아래 §1). 규칙은 **사유 이름표**로만 쓴다 — 거래처코드 규칙은 다른 고객 MDB에서 깨진다(실제 거래처 코드 2,147,442,590 · DOCF8 에 2,147,483,647 「불량,폐기처분」 실재).
2. **명세서 경로와 재고원장 경로가 같은 판정 함수를 부른다** — 한쪽만 고치는 사고 차단(오더 4).
3. **조립·해체① = 재고원장에만.** `move_type` in/out · `source_type='migration'` 그대로 → **DDL 0**. 레거시 월별재고(DOCFC)가 조립을 **특수입출고 칸(IM_ISQTY/OSQTY)** 으로 따로 셌다 = 「레거시가 장부로 보던 것」 근거.
4. 2001~2002 수천만 수량은 **품목 안에서 서로 상쇄**(예 잉크젯(A형) 입 49,930,600 / 출 49,930,201 → 순 +399). 조립 전체 순영향 −21,688 · 품목 60 → R1 재결재.
5. 🔴 **새로 드러남**: ③「시험 입력」 20행 중 **금액 0 수량 출고 10행(거래명세서 도트용 10,233 등)** 은 시험이 아니라 기타출고로 보인다 → R2 재결재.
6. 이미 이관된 고객 DB: **병합은 잘못 들어간 행을 못 지운다**(#3 원장 INSERT ONLY · 명세서 UPSERT). 치료 경로 = **덮어쓰기**(백업→초기화→재이관, 메인PC 전용) 하나 → R4.

## §1 [판정] 분류 판정 규칙 — 교차 실측

| IO | 머리 있음·규칙 해당 | 머리 있음·규칙 아님 | 머리 없음·규칙 해당 | 머리 없음·규칙 아님 |
|---|---|---|---|---|
| 1 매입 | **0** | 1,962 | 210 | **0** |
| 2 판매 | **0** | 115,150 | 353 | **0** |

- 규칙 = `BUY≥2147480000 OR (BUY=0 AND SEQ=0) OR DT∈{00000000,00000001}` · 조인 키 (DT,IO,SEQ,BUY) · DOCFE 117,112행 = DISTINCT 키 117,112(중복 0).
- **반증(5만 고객사)**:
  - DOCFE 에 **실제 머리가 달린 거래처 코드 2,147,438,071~2,147,442,590** 가 8,409종 · DOCF8 에 +2,147,483,647 「<불량,폐기처분>」 · 음수 코드 −2,147,483,648 등 2,507건이 실제 거래처명 → **코드 크기로 가르면 다른 고객 MDB에서 진짜 명세서를 버린다.** 규칙 판정 기각.
  - 단가행④ 은 BUY −58,093,496 / 2,147,448,444 로 코드 규칙에 안 걸리고 **날짜로만** 걸린다 → 규칙이 이미 한 고객에서도 누더기.
  - **DOCFE 가 없는/비어 있는 옛 MDB** → ⚠️미확인(사본 1개뿐). 설계: `FindMdbTable` 로 DOCFE 부재·0행이면 **분류하지 않고 종전대로 전부 이관** + 경고 로그 + 대사표 「머리표 없음 — 분류 못 함」 표시(버리지 않는다 · #20). → R5 재결재.
- 판정 함수(순수 static · `LegacyMdbMapping`): `ClassifyDocfbGroup(hasHeader, dt, buy, seq) → Reason`
  - `Header`(진짜 명세서) · `Assembly`(BUY=2147483500) · `Danga`(DT 00000000/00000001) · `NoName`(BUY=0 AND SEQ=0) · `Reserved31xxx`(SEQ 31001~31004 계열, 기존 ③) · `OtherHeadless`(위 어디에도 안 맞는 머리 없음 — 이 MDB 0건).
  - 이름표 우선순위: Danga → Assembly → NoName → Reserved31xxx → OtherHeadless (실측 분포 §2 와 합 일치).
  - 🟢 개정: `TestZeroAmount`(③ 중 묶음 금액·부가세 0 — §5) 추가 · 행 단위 `IsBlankLine`(§4) 추가 · DOCFE 없음 = `Unclassified`(R5).
  - **개정 포함표** (A3 한 곳에만 둔다):

| 이름표 | 명세서 | 재고원장 | 품목 등록 |
|---|---|---|---|
| Header | O | O | O |
| TestZeroAmount | O (머리 없이) | O | O |
| Assembly | X | O | O |
| NoName (값 있는 줄) | X | O (「미상」) | 폴백 1개 |
| Reserved31xxx(금액 있음) · Danga | X | X | 제외 행만의 품목은 X (단 DOCFC 최종 ≠0 이면 §12 로 등록) |
| OtherHeadless | X | O (버리지 않음 #20 · 이 MDB 0건) | O |
| Unclassified (DOCFE 없음) | O | O | O |
| 모든 이름표 + `IsBlankLine` | 줄 X | X | X |

## §2 [분포] 머리 없는 줄 — 사유별 실측 (행 기준)

| 사유 | IO | 묶음 | 행 | Σ수량 | 음수행(Σ) | 날짜 |
|---|---|---|---|---|---|---|
| 머리 있음 | 1 | 1,962 | 5,049 | 28,542,725.05 | 16 (−16) | 2000~2026 |
| 머리 있음 | 2 | 115,150 | 118,454 | 125,897 | 373 (−379) | 2000~2026 |
| ① 조립·해체 | 1 / 2 | 82 / 82 | 106 / 141 | 100,383,022 / 100,404,710 | 0 | 2001~2024 |
| ② 품명 없음 | 1 / 2 | 126 / 252 | 126 / 252 | 138 / 1,395 | 0 | 2004~2006 |
| ③ 31xxx | 2 | 18 | 20 | 10,246 | 4 (−35) | 2001~2025 |
| ④ 단가행 | 1 / 2 | 2 / 1 | 3 / 1 | 0 | 0 | 00000000/01 |

검산: IO=1 Σ 128,925,885.05 · IO=2 Σ 100,542,248 = 대사표 레거시 식(`MdbReconciliationService.cs:415` 주석)과 일치.

## §3 [조립] 조립·해체① 재고 반영

- DDL 확인(#13·#36): `installer/hitpan_db_clean.sql:3620` stock_ledger — `move_type varchar(10)`(CHECK 없음) · `source_type varchar(30)` 주석에 `bom_production/bom_disassemble/adjust/migration` · UNIQUE `(tenant,source_type,source_id,item_id,move_type,warehouse_id)`. 코드 전역 move_type 값 = `in`/`out` 뿐(`BomService.cs:710·725·882·955`, `StockService.cs:305`).
- **설계: 새 값 없음 · DDL 0.** ①은 지금처럼 `source_type='migration'` · `LedgerMove` 로 in/out. `bom_production` 등으로 바꾸지 않는다 — ⓐ 멱등키(UNIQUE 의 source_type)가 바뀌어 병합 재이관 때 **이중 적재** ⓑ `MigrationController.cs:606-627` 덮어쓰기 영향 계산이 `source_type<>'migration'` 을 「사람이 입력한 거래」로 셈 ⓒ `AutoJournalHelper` 가 bom_* 을 분개 대상으로 봄.
- 명세서 쪽: ①은 `sales_deliveries`/`purchase_receipts` 에 **안 만든다**.
- **레거시 독립 근거 (DOCFC 월별재고, 레거시 프로그램 자체 집계)**: 품명 있는 행 Σ IM_ISQTY 100,383,182 ↔ ① 입고 100,383,022 (+160) · Σ IM_OSQTY 100,414,957 ↔ ① 출고+③ 출고 100,414,956 (+1). 연도별 2001 ISQTY 500,173 = ① 2001 입고 **정확 일치** · 2002 99,861,128 = **정확 일치**. 2023년 3월 제품BOX 싱글: 기초 −20,054 + ISQTY 20,054 = 기말 0. → 레거시는 2001~2002 대량분까지 **재고에 넣어 보고 있었다.**
- **2001~2002 대량분 품목별 순영향 (R1 안건 자료)**: ① 전체 품목 60 · 순영향≠0 55 · Σ순 −21,688. 상위: 제품CD|싱글용 −20,054 · 메뉴얼 −20,054 · 제품BOX|싱글 +20,054(2023 실조립) · 거래명세서|도트용 +1,005 · 거래명세서|잉크젯(A형) +399 · 공CD|BLUE<K> +364 · 기|테스트 −1,120 · 가산세 계열 ±1,000(2018, 시험성 이름). 수천만 수량은 **같은 품목 입·출이 서로 상쇄**되어 기말 재고를 수천만 왜곡하지 않는다.

## §4 [품명없음] ② 품명 없는 줄 378행

- 실측: 378묶음=378행 · BUY 0 · SEQ 0 · **SUN 0**(정상 행 SUN=0 은 **0건** — 레거시가 줄번호도 안 매긴 찌꺼기) · 금액 0 · 순수량 138−1,395 = **−1,257** → 현재 `LEGACY_UNKNOWN_ITEM`(`MdbMigrationService.cs:2110`) 재고로 들어감.
- 레거시 독립 근거: DOCFC 빈 품명 행 2004~2006 IM_IQTY·OQTY·CQTY **전부 0** → **레거시 월별재고는 이 줄을 재고로 안 셌다.** (2014~ 빈 품명 IM_ISQTY 38억은 DOCFB 에 대응 행 없는 레거시 쪽 쓰레기 — 대사 판정칸에 쓰면 안 됨)
- 빈 품명 키는 **머리 있는 진짜 명세서에도 있다** → 폴백 품목 자체는 없어지지 않는다.
- 선택지 (R3): ~~A 이관 제외~~ · B 「품목 미상」 재고에만 −1,257 유지(현행) · C 명세서만 빼고 원장 유지 = 현행과 재고 같음.
- 🟢 **R3 결정(8-1) 반영 설계** — 품명·규격 둘 다 빈 DOCFB **379행** 실측(9/15 존 재실측):
  - 수량·금액·부가세 **전부 0 = 13행** = 머리 없음 12(2005-11~2006-09) + **머리 있음 1**(20080118·IO2·SEQ3·BUY 2147438373·SUN1) → **생략**. 머리 있는 1행은 명세서 **줄만** 생략하고 명세서 머리(DOCFE)는 그대로 만든다(건수 불변). 그 묶음에 다른 줄이 없으면 줄 0 개 명세서가 된다 ⚠️[4] 확인.
  - 값 있는 **366행 = 전부 머리 없음**(순 −1,257 · 금액 0) → 머리 판정 규칙대로 **명세서 안 만듦** · 재고원장에만 **「미상」 품목**으로 이관. 첫 화면 재고는 §10 맞춤 줄이 DOCFC(0)로 맞춘다 → 8-1 의 충돌 해소.
  - 판정은 행 단위: `IsBlankLine(pum, ku, qty, amt, vat)` = 이름 둘 다 빈칸 AND 세 값 0 → 모든 경로에서 제외.
  - 「미상」 품목 = 기존 `LEGACY_UNKNOWN_ITEM`(`MdbMigrationService.cs:2113`) 을 그대로 쓰고 **item_name 을 「미상」** 으로 한다. 현재 item_name 값 ⚠️미확인(§13) — 다르면 신규 등록 때만 「미상」(기존 행 UPDATE 는 사람이 고친 이름을 덮으므로 안 한다 #1).

## §5 [제외] 시험③·단가행④ — 원장에서도 빠지는가

- 현행: 명세서 경로는 DT `00000000` 만 skip(`:4948`), 재고원장은 **아무것도 안 뺀다**(`:2292-2351`) · 날짜 파싱 실패 시 `ledger_date = now`(`:2306`) → 단가행 4행이 **오늘 날짜 수량 0 원장**으로 들어가 있다.
- 설계: 두 경로 모두 `ClassifyDocfbGroup` 결과가 `Danga`·`Reserved31xxx`(R2 결과 반영)면 **둘 다** 제외 · `RegisterUnlistedDocfbItemsAsync`(`:2219`) 도 제외 행만의 품목은 등록 안 함(가산세|112233 · 가나다라…|123456789 · 거래명세서|잉크젯B-TYPE레이져용 = 3종).
- 🔴 R2: ③ 20행 = 금액 있는 시험 10행(가나다라… · 가산세1 · 거래명세서|1 등 Σ공급가 33,059.20) + **금액 0 수량만 10행**(거래명세서|도트용 10,233 · 공CD 2 · HTP21C 2 · 기타 2). 후자는 DOCFC OSQTY 에 들어가 있다 = 레거시가 재고 출고로 봤다. 선행검증 「시험 입력」 이름표가 절반만 맞다.
  - ~~추천: 명세서에선 ③ 전부 제외~~ → 🔴 **R2 결정(8-3): 금액 0 시험 행은 명세서·재고 둘 다 남긴다.**
- 🟢 **R2 반영 설계 — ③ 20행 존 재실측(9/15)**: PM 사실 「10행」은 **12행**이 맞다(위 줄의 「기타 2」도 틀림 — 기타|ss 는 금액 200).

| 구분 | 묶음(DT·SEQ·BUY) | 행 | 순수량(출고+) | 공급가 | 처리 |
|---|---|---|---|---|---|
| 금액 0 | 20010820 31001·31002·31003 · 20011031 · 20011105 · 20020318 (도트용) · 20021211 31001~31004 (공CD) · 20071016 31001 (HTP21C 2줄) | **12** / **11묶음** | 10,237 (출 10,260 · 입 23) | 0 | 판매 명세서 **11건**(머리 없이 만든다) + 재고 |
| 금액 있음 | 20051215 ×2 · 20110601 · 20120425 · 20130214 · 20140617(2줄) · 20250425 | **8** / **7묶음** | 출 21 · 입 12 | **33,059.20** · 부가세 0 | 전 장부 제외 · 대사표 사유 |

  - 한 묶음 안에 금액 0 줄과 금액 있는 줄이 섞인 경우 **0건** → 판정은 **묶음 단위**: 묶음 Σ|IJ_AMT|=0 AND Σ|IJ_VAT|=0 이면 `TestZeroAmount`(남김), 아니면 `Reserved31xxx`(제외). 다른 고객 MDB 에서 섞이면 **줄 단위로 가르지 않고 묶음 전체 제외 + 대사표에 「섞인 시험 묶음」 건수** (명세서 반쪽 생성 방지).
  - 11건 명세서: 머리 정보가 없으므로 날짜 = IJ_DT · 거래처 = 기존 IJ_BUY→거래처 폴백 경로 그대로 · 문서번호·멱등키 `mig-docfb-{dt}-{io}-{seq}-{buy}` 기존 규칙 그대로 · status 기존 이관과 동일(#6 변경 없음).

## §6 [기존DB] 이미 이관된 고객 DB

| 경로 | 현행 코드 | 봉합 후 결과 |
|---|---|---|
| 병합(없는 것만 보태기) | 명세서 `ON DUPLICATE KEY UPDATE`(`:5215·5347`) · 원장 `INSERT IGNORE`(`:2382·2510`) | 새로 제외된 묶음은 **안 만들 뿐 기존 행은 그대로 남는다** — 원장 DELETE 는 #3 위반, 명세서만 지우면 한쪽만 고친 것 |
| 덮어쓰기 | `MigrationController.cs:675` 메인PC·확인문구·비번 → `DataResetService.ResetAllAsync`(백업 후 원장 포함 포맷, 사장님 결재 예외 `DataResetService.cs:18`) → 재시드 → 재이관 | **깨끗이 교정** |

- 설계: 코드는 **지우지 않는다.** 대사표가 「이전 가져오기에서 들어간 제외 대상」을 센다 — 레거시 쪽 판정으로 만든 제외 source_id 집합(명세서 `mig-docfb-{dt}-{io}-{seq}-{buy}` · 원장 `mb-{dt}-{io}-{seq}-{buy}-{sun}`)과 ERP 행을 대조 → N>0 이면 화면에 「덮어쓰기로 다시 가져오기가 필요합니다」 1줄.
- 운영(demo·고객 PC) 직접 수술 **금지**(#39). 공영정보 운영 DB(`hitpan_erp_t004`) 교정 시점·방법 = R4.

## §7 [대사] 대사표 Q4

| 항목 | 레거시 열 (새) | 히트판 열 | 따로 보이는 줄 |
|---|---|---|---|
| 판매·매입 건수·공급가·부가세 | DOCFE (현행 유지 `:179`) | 이관 명세서 | Detail 을 「DOCFB 헤더그룹」→ **「뺀 묶음 사유별: 조립·해체 82 · 품명없음 252 · 시험 18 · 단가행 1」** 로 교체 + 사유별 공급가·부가세 |
| 제외 대상 잔존 | 0 | 제외 source_id 와 겹치는 ERP 행 수 | 신규 항목 `excluded_leftover` (0 이면 일치) |
| 재고원장 행수 | DOCFB 중 **이관 대상**(머리 있음+①[+R 결과]) | stock_ledger migration | Detail 사유별 제외 행수 |
| 재고원장 입고·출고 | **LedgerMove 와 같은 규칙**(음수는 반대 칸 절대값)으로 레거시를 계산 | qty_in/qty_out | 입·출 칸이 따로 일치해야 통과 (현행 순수량만 비교 `:374`) |
| 재고 기말 | ~~이관 대상 행으로 Σ입−Σ출~~ → 🟢 **DOCFC 최종 기말**(8-2) | item_stock | Detail: DOCFB 이력 순수량 · 맞춤 줄 Σ · 창고 00 외 합침(§11) |

**🟢 개정 대사표 (8-2·8-3 반영 — 위 표 중 바뀐 줄만)**

| 항목 | 레거시 열 | 히트판 열 | 판정 |
|---|---|---|---|
| 판매 건수·공급가·부가세 | DOCFE IO2 115,150 **+ 남긴 머리 없는 묶음 11**(R2) | 이관 판매 명세서 | 0 |
| 🆕 제외 사유별 (한 줄씩) | 사유 · 행수 · 묶음수 · 수량 · 공급가 · 부가세 | — | 표시만 · 합계가 DOCFB 전체 − 이관 대상과 일치해야 통과 |
| 재고원장 행수·입·출 | 이관 대상 DOCFB 행(LedgerMove 규칙) | migration 원장 중 **맞춤 줄 제외** | 0 |
| 🆕 맞춤 줄 | (DOCFC 최종 − 이관 이력 순수량) 품목 수 · Σ입 · Σ출 | 맞춤 줄 행수·입·출 | 0 |
| 재고 수량 기말 | **DOCFC 최종 Σ IM_CQTY** 3,147,291 | Σ item_stock.current_qty | 0 |
| 🆕 재고 금액 기말 | **DOCFC 최종 Σ IM_CAMT** 54,753,974.4 | Σ current_qty × avg_cost (`StockService.cs:486` 과 같은 식) | 차 ≤ 0.11 (R6 · §10) |
| 🆕 재고 품목별 불일치 수 | DOCFC 최종 품목 | item_stock | **0 품목**(수량) — 합만 맞고 품목이 틀리는 것 차단 |

- 「자기 자신과 비교」 제거: 재고 판정칸이 DOCFB(이관 원천)가 아니라 **DOCFC(레거시 프로그램이 따로 집계한 표)** 가 되어 독립 대조가 된다. 아래 초판 한계 문단은 판정칸이 아닌 **이력 행수·입출 칸**에만 남는다.

- 「자기 자신과 비교」 한계(선행검증 P2): 재고는 레거시 쪽 독립 집계가 DOCFC 뿐인데 DOCFC 가 +109/+145/+160/+1 어긋나고 빈 품명 쓰레기 38억이 있어 **판정칸으로 못 쓴다.** 그래서 ⓐ 판정칸 = DOCFB(분류 적용) ⓑ 독립 확인 = DOCFC 품명 있는 행 특수입출 ↔ ① 연도별 대조를 Detail 에 표시 ⓒ 분류 자체의 독립성은 **DOCFE 조인**(명세서 머리는 DOCFB 와 다른 표)이 준다. 완전 독립이 아님을 화면 Detail 에 적는다(숨기지 않는다).

## §8 [기대값] 봉합 후 대사표 예측 (공영정보 MDB · 덮어쓰기 재이관 기준)

### §8-A 🟢 개정 기대값 (R1~R5·8-2 반영 · 이 표가 정본 · 아래 초판 표는 참고)

| 항목 | 레거시 열 | 히트판 열 | 차 |
|---|---|---|---|
| 판매 건수 | 115,150 + 11 = **115,161** | 115,161 | 0 |
| 판매 공급가 / 부가세 | DOCFE 합(현행 식) | 같음 (남긴 11건 금액 0) | 0 / 0 |
| 매입 건수 / 부가세 | 1,962 / DOCFE 합 | 같음 | 0 / 0 |
| 미수금 · 미지급금 · 거래처 차이표 합 | | | 0 · 0 · 0 |
| 업체 | | | **+1 유지** (폴백 거래처 · R2 11건 BUY 2147483631~34 도 폴백 여부 ⚠️[4]) |
| 상품 | | | ⚠️[4] 실측 — 초판 +856 에서 §12 DOCFC 전용 등록 **+16**, 금액 있는 시험 전용 3종 미등록 유지 |
| 재고원장 이력 행수 (맞춤 줄 제외) | 124,152 − 단가 4 − 시험(금액) 8 − 빈 줄 13 = **124,127** | 124,127 | 0 |
| 이력 입고 칸 / 출고 칸 | **128,926,303.05 / 100,542,657** | 같음 | 0 / 0 |
| 이력 순수량 | 28,383,646.05 | 같음 | 0 |
| 맞춤 줄 행수 | 존 추정 **약 657**(수량 다름 633 + DOCFC 전용 ≠0 16 + DOCFB 전용 ≠0 8) ⚠️ 판정 반영 ±6 · 이름 정규화 차이 [4] 확정 | 행수 | 0 |
| 맞춤 줄 Σ순수량 | 3,147,291 − 28,383,646.05 = **−25,236,355.05** | Σ(qty_in − qty_out) | 0 |
| 재고 수량 기말 Σ | **3,147,291** | Σ item_stock | 0 |
| 재고 금액 기말 Σ | **54,753,974.4** | Σ qty × avg_cost | **≤ 0.11** (avg_cost 소수 2자리 반올림 · 3품목 · R6) |
| 재고 품목별 수량 불일치 | 0 | | 0 |
| excluded_leftover (새 DB) | 0 | 봉합 전 DB 병합 시 명세서 **551**(562 − R2 11) · 원장 **25**(4+8+13) | 표시만 |

계산 근거(존 실측 9/15 · MDB 사본): DOCFC 최종 품명·규격 1,192종 중 DOCFB 순수량과 **같음 235**(Σ22,593) · **다름 633**(DOCFC 3,114,642 vs DOCFB 28,363,288.05) · **DOCFC 에만 324**(수량≠0 **16** · Σ10,056 · 금액 10,408,229.4) · DOCFB 에만 204(순≠0 **8** · Σ−2,244). DOCFC 최종 수량 0 인데 금액 ≠0 = **0품목**. 수량 음수 125품목(Σ−2,195 · 금액 −140,229,114.6).

| 항목 | 현재 차이 | 봉합 후 (Q1 전부 제외 · R2·R3 미결 시) | R3=A(②제외) 추가 시 |
|---|---|---|---|
| 판매 건수 / 공급가 / 부가세 | +353 / +33,059.20 / −5 | **0 / 0 / 0** | 0 |
| 매입 건수 / 부가세 | +209 / +1 | **0 / 0** | 0 |
| 미수금 | +33,054.20 | **0** | 0 |
| 미지급금 | +1 | **0** | 0 |
| 거래처별 차이표 합 | +33,053.20 | **0** (U5 1원 규명: 표는 미수−미지급 = 33,054.20−1) | 0 |
| 업체 | +1 | **+1 유지** — 폴백 거래처는 머리 있는 진짜 명세서 4건(1,450,000원, DOCF8 에 없는 코드 2147438765·2147442588~90)도 받는다. 선행검증 §3 「거래처 없는 줄 받이」 추정은 절반만 맞음 | +1 |
| 상품 | +859 | **+856** (제외 행만의 품목 3종 미등록) ⚠️ DB 미조회 · 내 계산 자동후보 856 vs 화면 858 차 2 는 [4] 실측 | +856 (빈 품명 폴백은 머리 있는 행이 계속 씀) |
| 재고원장 행수 (히트판) | 124,152 | **124,128** (−24) | 123,750 |
| 원장 입고 칸 (히트판) | 128,926,315.05 | **128,926,280.05** (③ 음수 −35 가 입고 칸이었음) | 128,926,142.05 |
| 원장 출고 칸 (히트판) | 100,542,678 | **100,532,397** (③ 양수 10,281 제거) | 100,531,002 |
| 재고 기말 Σ | 28,383,637.05 | **28,393,883.05** | **28,395,140.05** |
| 입출 칸 레거시↔히트판 | 각 +430 | **0** (레거시도 절대값 규칙) | 0 |

⚠️ 원장 칸 값은 MDB 실측 합에서 계산한 **예측**이다. [4] 가 테스트 DB 재이관으로 실측해 기대값을 확정한다. 계산식: 입고 칸 = IO1 양수 + IO2 음수 절대값 · 출고 칸 = IO2 양수 + IO1 음수 절대값. R2 추천안(③ 금액 0 행은 원장에 남김)이면 ③ 중 그 행만큼 다시 더해 [4] 가 재계산.

## §9 [게이트] 게이트 · [4] 실측

- G1 `MdbHeadlessClassifyGate`(순수): 사유 6종 × 경계값 — BUY 2147442590 **머리 있음 → Header**(코드 규칙이면 FAIL) · BUY −2147483648 머리 있음 → Header · DT 00000001 머리 없음 → Danga · BUY 0 SEQ 0 → NoName. 봉합(판정함수) 빼고 → 컴파일 실패가 아니라 **Assert FAIL** 확인 기록.
- G2 `MdbHeadlessBothPathsGate`: 명세서·원장·품목등록 세 경로가 **같은 함수**를 부르는지 — 글자검사 아님: 합성 DataTable(머리 있음 2 · ① 2 · ④ 1 · ② 1)을 세 변환부에 넣어 산출 행수 비교. 변환부를 순수 메서드로 떼어야 가능 → 작업지시서 §4 A2.
- G3 `MdbReconHeadlessGate`: 대사표 사유별 Detail · 입출 절대값 규칙 · `excluded_leftover` — `HITPAN_REQUIRE_DB` 에서 SKIP 이면 FAIL.
- 🆕 G4 `MdbLegacyFinalStockGate`(순수 + DB): ⓐ 합성 DOCFC(같은 품목 3달 · 창고 00·01 · 음수 기말 1) + 합성 이력 → 맞춤 줄 계산이 **마지막 달·창고 합산**만 쓰는지(첫 달이나 Σ를 쓰면 FAIL) ⓑ 두 번 돌려 맞춤 줄 **추가 0행**(멱등) ⓒ 이력에 사람이 넣은 `sales_delivery` 원장이 있어도 맞춤 계산에서 빠지는지(대조군) ⓓ `HITPAN_REQUIRE_DB` 에서 item_stock 품목별 = DOCFC 최종 · SKIP 이면 FAIL. 봉합(맞춤 단계 호출) 빼고 FAIL 기록.
- G1 추가 경계: ③ 금액 0 묶음 → TestZeroAmount · 금액 섞인 묶음 → Reserved31xxx · 빈 이름+값 0 → 전 경로 제외 · 빈 이름+수량 → NoName 원장만.
- CI: `.github/workflows/build-and-test.yml:171~235` GATES 에 **4개** 등록.
- [4] 실측 추가: ⑥ §8-A 표 전 항목 ⑦ 이관 직후 **재고현황 화면**(사람 눈) 상위 5품목(유지보수 관리비 2,453,867 · 전자결제용 충전 660,524 등) 수량이 DOCFC 최종과 같은지 캡처.
- [4] 실측: ① 빌드 0/0 ② 테스트 DB(운영 아님)에 사본 MDB **덮어쓰기** 이관 → §8 표 전 항목 ③ 같은 DB에 **병합** 재이관 → 건수 불변·`excluded_leftover=0` ④ 봉합 전 빌드로 이관한 DB에 봉합 후 병합 → `excluded_leftover` = 명세서 562 · 원장 24 표시(지우지 않음 확인) ⑤ DOCFE 테이블 이름을 바꾼 MDB 사본 → 분류 안 함·경고·전부 이관.

## §10 [맞춤] 「레거시 최종재고 맞춤」 조정 줄 (8-2 · 설계 질문 a)

- **원천 식**: DOCFC 품목별 마지막 달 — `(IM_PUM, IM_KU, IM_CHANG)` 별 `MAX(IM_YM)` 행의 IM_CQTY·IM_CAMT → 히트판 품목(품명·규격 → item_id, 기존 `RegisterUnlistedDocfbItemsAsync` 와 **같은 이름 정규화 함수**) 단위로 창고 합산(§11). PM 1,204 = 이 식의 줄 수와 일치(존 재실측).
- **넣을 값**: `diff = DOCFC 최종 수량 − Σ(qty_in − qty_out) [stock_ledger · tenant · item · source_type='migration' · 맞춤 줄 포함]`. diff=0 이면 안 넣음. DOCFB 에만 있고 DOCFC 에 없는 품목은 목표 0.
  - `source_type='migration'` 만 합한다 → 병합 재이관에서 이관 뒤 **사람이 입력한 거래는 건드리지 않는다** · 봉합 전 잘못 들어간 잔존 행(§6)도 재고 수치로는 상쇄된다(행은 안 지움 #3).
- **move_type**: 새 값 없음 — diff>0 `in`(qty_in=diff) · diff<0 `out`(qty_out=|diff|). 근거: `hitpan_db_clean.sql:3629` varchar(10) · 코드 전역 in/out 뿐(§3). **DDL 0 · 마이그 0.**
- **source_type = `migration`**(`adjust` 아님): `adjust` 는 실사조정(StockService) 사람 입력으로 읽혀 `MigrationController.cs:606-627` 덮어쓰기 영향 계산에 「사람이 입력한 거래」로 잡힌다 · 회계 자동분개 대상 판단 회피(§3 ⓒ와 같은 이유).
- **source_id** = `mb-adj-{YYYYMM}` (DOCFC 전체 MAX(IM_YM) · 이 MDB `mb-adj-202602`, 36자 이내) · UNIQUE `(tenant,source_type,source_id,item_id,move_type,warehouse_id)` 가 품목별 1행을 보장 · `migrated_source_hash` = SHA256(`adj|{YYYYMM}|{item_id}|{move_type}`). INSERT IGNORE(기존 원장 경로와 동일).
  - 멱등: 같은 MDB 로 재실행하면 diff=0 → 0행. 한 품목에 in·out 두 맞춤 줄이 생기는 경우(나중 MDB 로 병합) = 다른 YYYYMM 이면 별 행 · 같은 YYYYMM 인데 부호가 뒤집히면 UNIQUE 로 안 막히므로 **diff 를 맞춤 줄 포함 합에서 다시 계산**하는 식이 정답을 보장한다.
- **날짜** = DOCFC 전체 MAX(IM_YM) 의 **말일**(이 MDB 2026-02-28) · `ym` 은 기존 이관 원장과 같은 형식. 이유: DOCFB 마지막 줄 20260220 보다 뒤 → 재고원장 화면 누계가 모든 이력 **다음에** 맞춰진다. 이관 실행일로 두면 백업일~이관일 사이에 레거시 밖 기간이 생긴다. 품목별 마지막 달 말일로 두면 그 뒤 DOCFB 이력(용역 품목 등)이 다시 틀어 놓는다 → 기각.
- **단가·금액**: 맞춤 줄 `unit_cost` NULL · `supply_amount` NULL · `partner_id` NULL · `memo` = 「이전 프로그램 최종재고 맞춤」(고객 노출 문구 · 개발용어 금지 · 문구 확인 필요 R8). 금액은 원장이 아니라 **item_stock.avg_cost** 에서 맞춘다.
- **item_stock 경로**: 맞춤 줄 INSERT 는 `item_stock_rebuild`(`MdbMigrationService.cs:571`) **바로 앞** 새 단계 `legacy_final_stock` 에서 → 기존 리빌드가 수량을 그대로 합산(코드 변경 없음) → 리빌드 **뒤** 새 단계 `legacy_final_cost`: DOCFC 최종 수량≠0 품목만 `UPDATE item_stock SET avg_cost = ROUND(IM_CAMT / IM_CQTY, 2)`(원장 아님 → #3 무관). 수량 0 품목은 금액도 0(실측 0건 예외).
  - 금액 정확도: avg_cost `decimal(15,2)`(`hitpan_db_clean.sql:1796`) → Σ 오차 **0.11**(295품목 중 3품목 > 0.005). 소수 4자리면 0.0016. → R6.
  - 음수 기말 125품목: avg_cost = 음수 금액 ÷ 음수 수량 = 양수 → 수량×avg_cost 가 음수 금액으로 돌아온다(식 성립).
- **DOCFE·DOCFC 없는 MDB**: DOCFC 없음/0행 → 맞춤 단계 skip + 경고 + 대사표 「레거시 최종재고 표 없음 — 이력 누계로 이관」(버리지 않음 #20). R5 와 같은 모양.

## §11 [창고] IM_CHANG ↔ 히트판 창고 (설계 질문 b)

- 실측: DOCFB `IJ_CHANG` 124,152행 **전부 '00'** · DOCFC 전체 52,664행 중 '00' 52,652 · 01 5 · 02 2 · 03 1 · 04 3 · 06 1. 최종 기말 기준 00 1,192줄(3,147,282 · 54,404,924.4) · 00 외 **12줄**(수량 +9 · 금액 349,050 · 10품목이 창고 둘 이상).
- 현행: 이관 원장은 전부 테넌트 `MAIN` 창고(작21 A6 `:281`) · `stock_ledger.warehouse_id NOT NULL`(`:3624`) → 원장에 「미지정 NULL」 은 스키마상 불가.
- **설계: 창고를 새로 만들지 않고 `MAIN` 에 품목별 합산.** 근거 — 창고 규칙(자동배분 금지 · 창고는 다수가 안 쓰는 기능) · 이력(DOCFB)이 창고 00 뿐이라 01~06 은 이력 없는 월별표 값 · 레거시 창고 이름표(마스터) 위치 ⚠️미확인. 합산 내역은 대사표 Detail 「이전 프로그램 창고 00 외 12줄 수량 9 · 금액 349,050 을 기본창고에 합침」.
- 품목별 수량·금액 합은 그대로 맞으므로 「첫 화면 재고」 품목 합계는 같다 · 창고별 화면만 다르다 → R7.

## §12 [DOCFC전용] DOCFC 에만 있는 품목 (설계 질문 c)

- 실측: DOCFC 최종 1,192종 중 DOCFB 에 품명·규격이 없는 **324종** — 수량≠0 **16종**(Σ10,056 · 금액 10,408,229.4) · 수량 0·금액 0 **308종**. PYOJUN DOCFS(상품마스터)와의 교차 ⚠️미실측(파일이 달라 조인 못 함 · [3] 이 품목 이관 결과로 확인).
- 설계: 맞춤 단계가 품목을 못 찾으면 **등록 후 맞춤** — 기존 `RegisterUnlistedDocfbItemsAsync` 의 INSERT IGNORE·item_code 규칙을 그대로 부르는 공용 메서드(새 등록 경로를 따로 만들지 않음 #1). 대상 = DOCFC 최종 수량≠0 **또는** 금액≠0(=16종). 이미 DOCFS 로 들어온 품목이면 찾아서 등록 안 함.
- 수량·금액 0 인 308종 = **등록 안 함**(첫 화면에 보일 재고가 없음) + 대사표 Detail 「기말 0 · 거래 없음 품목 N — 등록 안 함」. 「누락 없이」 원칙과 부딪히는지 → R8 에 함께.

## §13 [규모] 갈래 분할 · 새 안건 · 미확인

- 규모 **L**: 파일 5(`LegacyMdbMapping.cs` · `MdbMigrationService.cs` · 🆕`MdbLegacyFinalStock.cs` · `MdbReconciliationService.cs` · `MdbMigration.razor`) + 게이트 4 · DDL 0 · 원장에 새 종류 줄 → [5] CTO 1차.
- 갈래: **A** 판정(§1 포함표) → **B** 이관 3경로 + R2 머리 없는 명세서 11건 + 빈 줄 13 → **D** 🆕 맞춤 단계(새 파일 · B 가 `:571` 앞뒤에 호출 2줄만 추가 — 파일 겹침은 호출 2줄뿐, B 머지 뒤 착수) → **C** 대사표(§7 개정표). A 선행 · B·C 병렬 · D 는 B 뒤.
- 🔴 새 재결재 안건: **R6** 재고 금액 반올림 0.11원 허용(DDL 0 · 추천) vs avg_cost 소수 4자리 DDL(#36 출하 DDL + 마이그 · 오차 0.0016) · **R7** 창고 00 외 12줄 기본창고 합산(추천) vs 레거시 창고별 창고 생성 · **R8** 맞춤 줄 문구 「이전 프로그램 최종재고 맞춤」 + 기말 0·거래 없는 DOCFC 전용 308종 미등록(추천) · 조정 줄 방식 자체(8-3 「명시 답 없음」) 재확인.
- ⚠️미확인([3]·[4] 로 넘김): ① `LEGACY_UNKNOWN_ITEM` 현재 item_name ② 머리 있는 빈 줄 1행(20080118) 묶음에 다른 줄 유무 ③ R2 11건 거래처(BUY 2147483631~34) DOCF8 존재 여부 ④ 맞춤 줄 행수(이름 정규화·Trim 차이) ⑤ DOCFC 전용 16종이 DOCFS 로 이미 들어오는지 ⑥ DOCFC 빈 품명 행이 최종 기말식에서 빠지는지(IM_PUM NULL 조인) ⑦ 재고현황 화면이 금액을 `current_qty × avg_cost` 로 보이는지(`StockService.cs:486` 외 화면 경로) ⑧ 레거시 창고 마스터 표 위치.

## §14 [2판판정] (라-2) 판정 · 3경로 — 1판 §1 포함표·§5 11건 명세서 대체

- 판정(순수 `LegacyMdbMapping.IsPostedToBooks(headerTableOk, hasHeader, hasLedgerLink)`) = `!headerTableOk` → 반영(R5) · 아니면 `hasHeader || hasLedgerLink`. 이름표(조립·품명없음·시험·단가행)는 **보관 표 사유 칸 표시에만** 쓴다(모양 규칙 판정 금지).
- 연결 키 = DOCFB (IJ_DT, IJ_SEQ) ↔ DOCF5 이월 아닌 줄 (S_YMD, S_SSUN) · 종류 맞춤: 판매(IO 2)↔S_GU `0` · 매입(IO 1)↔S_GU `A` · 거래처 코드는 키에 넣지 않는다(선행 ⑥). 「계산」 메모행 포함/제외 차 0묶음.
- 존 실측 (MDB 사본 · 묶음 수):

| IO | 머리 O·연결 O | 머리 O·연결 X | 머리 X·연결 O | 머리 X·연결 X | 🔴 종류 안 가린 키로 연결 O 가 되는 머리 X |
|---|---|---|---|---|---|
| 1 매입 | 1,962 | 0 | **0** | 210 | 109 |
| 2 판매 | 115,148 | **2** (20250310 SEQ27 0원 · 20251111 SEQ5 BUY 2147444473 10,000+1,000) | **0** | 353 | 123 |

- 선행 ⑥ 2015-05-12 330,000 은 (날짜·순번) 키로 **연결 O** — 거래처 키였으면 오판. 대신 20251111 건이 새 대조군.
- 1판 뺀 행(단가 4 · 금액 시험 8 · 빈 줄 13)은 **전부 등록**(10-1·10-2) → `IsBlankLine`·`TestZeroAmount`·`Reserved31xxx` 제외 규칙 폐기. R2 금액 0 시험 11묶음 = 머리 X·연결 X → 보관 표(명세서로 남음) + 재고 ✅.

| 경로 | 반영 117,112묶음 · 123,503줄 | 미반영 563묶음 · 649줄 | 코드 위치 |
|---|---|---|---|
| 명세서 `sales_deliveries`·`purchase_receipts` | O | X | `MdbMigrationService.cs:4890` 루프(`:4941`)에 판정 1줄 · 1단계 `:318` 부근에서 DOCFE 키 집합 + DOCF5 (날짜·순번) 판매·매입 집합 1회 읽기(#16) |
| 🆕 보관 표 | X | O 전 줄 | 🆕 `MdbLegacyUnpostedArchive.cs` — 같은 DOCFB 표·같은 판정 함수 · 거래처·품목 폴백은 명세서 경로와 같은 메서드 |
| 재고원장 | O | O (124,152 전 줄) | `:2257` 판정 없음 · `:2306` `?? now` → 기준일 |
| 품목 등록 | O | O | `:2208` 현행 유지 |

- 🆕 기준일 `LegacyBaseDate` = DOCFC MAX(IM_YM) 말일 → 없으면 DOCF5 MAX 이월 달 말일 → 없으면 DOCFB 최대 유효 IJ_DT. 이 MDB **2026-02-28**. 날짜 파싱 안 되는 3묶음 4줄(00000000 매입 1 · 00000001 매입 1·판매 1) = 기준일 + 보관 표(R-C) · 원본 IJ_DT 는 `legacy_dt` 에 그대로. `:4948` 00000000 skip 은 명세서 경로에만 남는다(반영 묶음엔 해당 0).
- R5: DOCFE 없음/0행 → 전 묶음 반영 + 대사표 「머리표 없음 — 분류 못 함」(DOCF5 유무 무관) · DOCFE 있음·DOCF5 없음/0행 → 머리표로만 판정 + 「거래처원장 표 없음」 표시 → R5-2 확인.

## §15 [DDL] 보관 표 — DB-121 + 출하 DDL (#36)

- 번호: origin/main 최신 `DB-120_main_pc_single_row_guard.sql` → **`DB-121_legacy_unposted_documents.sql`** · [3] 착수 때 origin/main 재확인(겹치면 뒤 파일 skip).
- `legacy_unposted_documents`: doc_id varchar(36) PK · tenant_id varchar(36) NOT NULL · io_type varchar(10) (`sales`/`purchase`) · doc_date date NOT NULL · legacy_dt varchar(8) · legacy_seq int · legacy_buy_code bigint · partner_id varchar(36) NULL(FK 없음) · reason varchar(30) · supply_amount·vat_amount decimal(15,2) · line_count int · memo varchar(500) · source_type varchar(20) · source_id varchar(80) (`mig-docfb-{dt}-{io}-{seq}-{buy}`) · migrated_source_hash char(64) · created_at datetime(6) · UNIQUE(tenant_id, source_id) · KEY(tenant_id, doc_date) · KEY(tenant_id, partner_id).
- `legacy_unposted_document_lines`: line_id varchar(36) PK · tenant_id · doc_id · line_no int · item_id varchar(36) NULL · item_name·spec varchar(200) · qty·unit_price·supply_amount·vat_amount decimal · memo varchar(500) · stock_source_id varchar(80)(재고원장 `mb-…` 연결) · migrated_source_hash char(64) · created_at · UNIQUE(tenant_id, migrated_source_hash) · KEY(tenant_id, doc_id). qty 자리수 = DOCFB MAX(|IJ_QTY|) [3] DESCRIBE·실측 후 확정(2001~2002 수천만).
- 공통: `ENGINE=InnoDB`(#17) · `utf8mb4_unicode_ci`(`sales_deliveries` `hitpan_db_clean.sql:3192` 와 같음) · 금액 decimal(#4) · 이관만 INSERT · 화면·API 는 읽기만.
- 덮어쓰기: `DataResetService.cs:111-119` 가 information_schema 에서 tenant_id 있는 표를 자동 삭제 → **코드 수정 0** · `PreservedTables`(`:47`) 에 넣지 않는다(G7).
- 조회 화면(갈래 H · 읽기 전용): 기간·거래처·사유 필터 + 줄 펼침 · 머리글 「이전 프로그램 장부 미반영 명세서」 · 매출·미수·계산서 합계에 안 들어간다는 안내 1줄.

## §16 [끝전] 재고 기준 · R6 — DDL 0 으로는 불가

- 키 = 품명·규격·창고 **Trim + 대소문자 무시**(Access·`unicode_ci` 와 같음) → 1,192품목 · 3,147,291 · 54,753,974.4 (PM 일치). 🔴 구분 키 → 1,199 · 3,147,281 · 55,253,974.4 → G3 함정.
- 맞춤 줄 Σ = 3,147,291 − 이력 전 줄 순수량 28,383,637.05 = **−25,236,346.05** (1판 −25,236,355.05 대체).
- 화면 금액 = current_qty × avg_cost(`StockService.cs:486`) · avg_cost decimal(15,2)(`hitpan_db_clean.sql:1796`). 안 떨어지는 3품목: HTP21C|표준형-L (−51 · −15,599,500 → −15,599,500.05) · 마우스외 4종|EA (6 · 18,500 → 18,499.98) · 팩시밀리용지 (12 · 26,182 → 26,181.96) · 합 −0.11.
  - 세 품목 올림·내림 8조합 최소 차 0.01 — 세 수량 최대공약수 3 이 11전을 못 나눈다. −15,599,500 ÷ 51 은 무한소수 → 2자리로는 품목도 합계도 정확 불가.
  - 소수 6자리면 곱의 2자리 반올림이 레거시와 같다(−51 × 305,872.549020 = −15,599,500.00002). → **R6-2**.
- R7 기본창고 합산 · R8 DOCFC 전용 324종 전부 등록(재고 0 포함) — §11·§12 그대로 · 「등록 안 함」 폐기.

## §17 [미수] R-A · R-B

- **F3 확정**(존 실측): 거래처별 **마지막 이월행 S_BAL(없으면 0) + 그 뒤 이월 아닌 줄 Σ(S_BAL−S_SUK)** → **+142,062,113 (1,059곳) / −51,577,479 (530곳)** = 선행 F3.
  - PM 140,865,113(1,054)/50,622,479(529) = `20260200` 이월행**만** — 그 뒤 2월 줄 440행(149곳 · 순 +242,000 · 이월 없는 2147438512 +1,540,000 등) 누락.
  - 「2026-02 이월 + 그 뒤」 = 「거래처별 마지막 이월 + 그 뒤」 합계 같음 · 마지막 이월이 202602 아닌 1,296곳 합 +11,000 → 다른 MDB 에도 서는 **마지막 이월** 식 채택. 이월행 S_SUK≠0 0행 · 같은 날 이월 중복 0. F1 과 다른 5곳 = G4 대조군.
- **히트판 미수 경로(실측)** — 기초잔액 칸 없음 · 분리형: KPI `FinanceService.cs:833`(명세서−수금) · `:843`(매입−반품−지급) · 뷰 `v_partner_balance_calc`·`v_partner_balance`(`hitpan_db_clean.sql:4496·4478` → `PartnerBalanceRepository.cs:31`·`SalesService.cs:833`) · `partner_balance` 표(`PartnerService.cs:240·275·371` · 이관이 안 채움 `MdbMigrationService.cs:2891`) · 명세서별 `CollectionService.cs:372·433` · `v_partner_aging_buckets`(`PartnerController.cs:202`) · 대사 `MdbReconciliationService.cs:736-775`. 거래처원장 화면 경로 ⚠️[3] 확인.
- 맞춤 크기(거래처 코드 기준 · 폴백 병합 전 추정): 이관 직후 명세서−수금 5,565,664 · 매입−지급 −82,606,946 → F3 로 가려면 수금 쪽 **1,238곳 Σ −136,496,449 (음수 1,225)** · 지급 쪽 **1,190곳 Σ −134,184,425 (음수 1,178)**. 수금·지급 표 맞춤 줄이면 2026-02 수금·지급 현황이 억 단위 음수로 오염 → **R-A2**. 명세서별 미수 화면(`CollectionService.cs:372`)은 레거시에 없던 모양이라 어느 안이든 「이전 프로그램 이월」 줄 1개로 보인다.
- 대사표 미수·미지급: 레거시 열 = F3 · 히트판 열 = KPI 식(맞춤 포함) · F3 계산은 `LegacyMdbMapping` 순수 함수 1개를 이관·대사가 같이 부른다. DOCF5 없음 → 맞춤 skip + 「거래처원장 표 없음」.
- **R-B**: `TaxInvoiceService.cs:54-66` SELECT 에 source_type 추가 → `:76` 뒤 `migration` 이면 거부(「이전 프로그램에서 가져온 거래명세서는 계산서를 새로 발행할 수 없습니다」) · `CancelAsync:421` 이관 계산서(`tax_invoices.source_type`) 취소 거부 · 묶음 발행 `DeliveryBatchService.cs:169` 가 IssueAsync 를 안 거치면 같은 조건. 보관 표는 계산서 경로에 없다.

## §18 [2판] 기대값 · 게이트 · 갈래 · 새 안건

| 항목 (공영정보 MDB · 테스트 DB 덮어쓰기 재이관) | 기대 |
|---|---|
| 판매 명세서 건수 / 공급가 / 부가세 | **115,150** / 11,074,294,207 / 1,029,201,157 (반영 묶음 DOCFB 줄 합 · DOCFE 합과 차 0 [4]) |
| 매입 명세서 | **1,962** / 1,650,336,301 / 156,703,141 |
| 보관 표 판매 | **353건 · 414줄** · 공급가 33,059.20 · 부가세 −5 · 출고 100,416,386 · 입고 35 |
| 보관 표 매입 | **210건 · 235줄** · 공급가 0 · 부가세 +1 · 입고 100,383,160 |
| DOCFB 줄 = 명세서 줄 + 보관 줄 | 124,152 = 123,503 + 649 · 날짜 없는 3건 doc_date 2026-02-28 |
| 재고원장 이력 행 · 입 · 출 | **124,152** · 128,926,315.05 · 100,542,678 |
| 맞춤 줄 Σ / 행수 | −25,236,346.05 / ⚠️[4] |
| 재고 수량 / 품목별 불일치 | **3,147,291** / 0 (1,192품목) |
| 재고 금액 | **54,753,974.4** · R6-2 (가) 차 0 · (나) −0.11 |
| 미수 / 미지급 | **142,062,113 (1,059) / 51,577,479 (530)** · 폴백 거래처 병합 곳 ⚠️[4] |
| 계산서 발행 가능 이관 명세서 | **0** (현재 115,150) |
| excluded_leftover (봉합 전 DB 병합) | 명세서 562 (판매 353 · 매입 209) · 새 DB 0 |
| 상품 | ⚠️[4] (DOCFC 전용 324 포함) |

- 게이트 (각 개발명세서에 **봉합 빼고 FAIL** 기록 · `.github/workflows/build-and-test.yml` GATES 7개):
  - G1 `MdbPostingClassifyGate`(A 순수): 위 매트릭스 · ⓐ 머리 X + 같은 날짜·순번 **수금** 줄만 → 미반영(종류 안 가리면 FAIL) ⓑ 머리 X + 같은 날짜·순번 S_GU 0 **다른 거래처** → 반영(거래처 키면 FAIL) ⓒ 머리 O·연결 X → 반영 ⓓ DOCFE 없음 → 전부 반영 ⓔ F3: 2019 마지막 이월+뒤 줄 · 이월 없음 · 202602 이월+뒤 줄(「마지막 달 이월만」·F1 이면 FAIL) ⓕ 00000000 → 기준일.
  - G2 `MdbPostingAllPathsGate`(B): 합성 DOCFB(반영 2 · 미반영 3, 00000000 1) → 명세서 줄 + 보관 줄 = 입력 줄 · 원장 = 입력 전 줄.
  - G3 `MdbLegacyFinalStockGate`(D): 1판 G4 + 대소문자·뒤 공백만 다른 같은 품목 → 한 품목(구분 키면 FAIL) · 끝전(R6-2 결과).
  - G4 `MdbLegacyBalanceGate`(E · `HITPAN_REQUIRE_DB`): 맞춤 뒤 KPI 미수·미지급 = F3 · 사람 입력 수금 거래처 대조군 · 재실행 추가 0 · SKIP=FAIL.
  - G5 `TaxInvoiceMigratedLockGate`(G · DB): 이관 명세서 발행 거부 · 사람 명세서 발행 성공(대조군) · 이관 계산서 취소 거부.
  - G6 `MdbReconPostingGate`(C · DB): 보관 표 줄 · 미수 F3 열 · SKIP=FAIL.
  - G7 `LegacyUnpostedDdlGate`(F · DB): ddl-smoke 뒤 information_schema 로 두 표 ENGINE·collation·tenant_id · DB-121 칼럼 = 출하 DDL 칼럼 · 초기화 뒤 0행.
- 갈래 (파일 겹침 0 · 규모 L · [5] CTO):

| 갈래 | 파일 | 선행 |
|---|---|---|
| F DDL | 🆕 `src/HitPan.API/Migrations/SQL/DB-121_legacy_unposted_documents.sql` · `installer/hitpan_db_clean.sql` | — (R6-2·R-A2 (나)면 여기 함께) |
| A 판정·F3·기준일·품목 키 | `src/HitPan.Application/Services/LegacyMdbMapping.cs` | — |
| B 이관 명세서·원장·보관 | `MdbMigrationService.cs` · 🆕 `MdbLegacyUnpostedArchive.cs` (D·E 단계 호출 줄은 B 소유) | A · F |
| D 재고 맞춤 | 🆕 `MdbLegacyFinalStock.cs` | A |
| E 잔액 맞춤 | 🆕 `MdbLegacyPartnerBalance.cs` (+ (나)면 `FinanceService.cs`·`CollectionService.cs`) | A · F · R-A2 결재 |
| G 계산서 잠금 | `TaxInvoiceService.cs` (+ `DeliveryBatchService.cs` 확인) | — |
| H 보관 조회 화면 | 🆕 API 컨트롤러 · 서비스 · razor 각 1 | F |
| C 대사표 | `MdbReconciliationService.cs` · `MdbMigration.razor`(H 링크 1줄) | A |

- 🔴 새 재결재 안건:

| # | 안건 | 근거 | 추천 |
|---|---|---|---|
| R6-2 | 재고 끝전 | §16 — 2자리로 3품목 불가 | **(가) avg_cost decimal(19,6)** DB-121 에 함께(어차피 DDL 있음 · 품목·합계 원 단위 같음 · avg_cost 쓰는 `PurchaseService.cs:335`·`BomService.cs:688` 등 [3-V] 확인) / (나) 0.11 대사표 표시 / (다) 다른 품목에 넣어 합계만 — 비추천 |
| R-A2 | 잔액 맞춤 위치 | §17 | **(나) 🆕 `partner_legacy_balances`(DB-121) + 미수 식 8곳 합산 + partner_balance 재집계** / (가) DDL 0 · 수금·지급 맞춤 줄 → 음수 수금 1,225줄 · 현황 오염 |
| R-B2 | 이관 거래명세서 자체 수정·확정취소(`SalesService.cs:913·1144`) 잠금 | 결정 문구 = 계산서 | 확인 필요 — 이번엔 계산서만 |
| R5-2 | DOCFE 없음 · DOCF5 있음 | R5 문구 | 전부 반영 |
| H 위치 | 보관 표 조회 화면 입구 | Sidebar 수정 여부 | 설정 > 자료 가져오기 화면 버튼(Sidebar 무수정) |

- ⚠️[3]·[4] 로 넘김: 맞춤 줄 행수 · 폴백 거래처 병합 뒤 F3 곳 수 · 거래처원장 화면 경로 · 보관 표 qty 자리수 · DOCFE 합 = 반영 묶음 줄 합 · 상품 수. 운영 DB 무접촉(#39) · 공영정보 교정 = R4.
