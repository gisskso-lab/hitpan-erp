# [2] 설계 — 수금·지급 등록 READ COMMITTED 안전 경로

> 설계팀장 존 · 2026-09-20 · 코드 무수정 · 커밋 없음 (결재는 PM 브라운킴)
> 근거: [1-V] `docs/검증/선행/20260920_선행검증서_수금등록_READCOMMITTED_서버설정의존_실측.md` · 작지 `docs/운영기록/20260920작1_수금등록_RC_안전경로_작업지시서.md`
> 기준 `origin/main` `43e1fe4d` · 브랜치 `feat/20260920-rc-safe-path`

---

## 📇 카드 (≤40줄)

### 결정: **서버 설정을 보고 두 경로로 가른다. 둘 다 같은 정확성 계약을 지킨다.**

| 축 | 내용 |
|---|---|
| 판정식 | `SELECT @@log_bin,@@binlog_format` → **위험 = log_bin=ON AND STATEMENT**, 그 밖은 안전 |
| 안전 경로 (RC) | 지금 3판 그대로 — RC + 거래처 행 `FOR UPDATE` + 일반 읽기로 R 재계산 (교착 0 실측 · G8-w) |
| 위험 경로 (RR) | RR + 거래처 행 잠금 + **R 재계산·전표 남은금액을 잠금 읽기(`LOCK IN SHARE MODE`)** + 교착(1213/1205) **트랜잭션 통째 짧은 재시도** |
| 기본값(조회 실패) | 🔴 **RR 경로** — RR 경로는 두 서버 **모두에서 돈다**. RC 는 안전 서버에서만 돈다 → 모르면 도는 쪽 (#20) |
| 캐시 | 프로세스 1개 값 + TTL 5분 · 1665 를 실제로 만나면 즉시 무효화 · 재기동 시 초기화 |
| 가드 | `MatchIsolation` 단독 비교 폐기 → **`LegacyMatchMode` 를 호출자가 선언** + (모드 ↔ 실제 격리수준) 쌍 검사. 모드가 SQL 의 잠금절도 고른다 → 둘이 따로 놀 수 없다 |
| 등록 차단 | **없다.** 재시도 소진 시에만 「잠시 후 다시 저장」 문구(고객어) — 마지막 수단 |
| 갈래 | S1 판정·경로(파일 4) · S2 게이트·CI(파일 3) — 2갈래 |
| 게이트 | 신설 `RcSafePathGate` — **STATEMENT 인스턴스**(CI: `docker run … --log-bin --binlog-format=STATEMENT`, 포트 `HITPAN_DB_STMT_PORT`)에서 9사례 · 기존 G8 24사례는 안전 서버에서 회귀(1줄만 기대값 변경) |
| 설계 추가 발견 | **D1 — 명세서·매입전표 남은금액 SUM 도 RR 에서 스냅숏 오염.** 선행검증·오더에 없던 구멍. RR 경로에서 같이 잠금 읽기로 (게이트 G-RC6) |

### 기대값 (봉합 빼면 빨간불)

| 게이트 | 잰다 | 무력화 |
|---|---|---|
| G-RC1·2 | 위험 서버에서 수금·지급 등록 3경로 전부 성공 | 경로 분기 제거 → 1665 |
| G-RC3 | G8-u 동등(앞선 일반 읽기 뒤에도 최신 R · 최종 R ≥ 0) | R 재계산 잠금절 제거 → 음수 R |
| G-RC4 | G8-x 동등(같은 거래처 동시 합 초과 거절) | 같은 무력화 → 거절 안 됨 |
| G-RC5 | G8-w 동등(다른 거래처 동시) 둘 다 성공 | 재시도 래퍼 제거 → 1213 노출 |
| G-RC6 | **D1** 같은 명세서 동시 수금 합 초과 거절(매입 대칭) | 전표 SUM 잠금절 제거 → 초과 통과 |
| G-RC7 | 판정표 4조합 + 조회 실패 → RR | 기본값을 RC 로 → 빨간불 |
| G-RC8 | 안전 서버는 RC 로 간다(대조군) | 항상 RR 이면 빨간불 |
| G-RC9 | 캐시 — 2번째 등록은 조회 왕복 0 · TTL 뒤 1회 | 매번 조회면 빨간불 |

### 새 결재 안건 — §9 표 (사장님 3건 · PM 2건). **BINLOG ADMIN 권한 확대·설치본의 남의 my.ini 수술은 기본안에서 뺐다**(#29).
### ⚠️ 미실측 — §10. 특히 **RR+STATEMENT 에서 `FOR UPDATE` 가 허용되는지**는 안 쟀다. 구현 0단계에서 raw 로 먼저 확인한다(틀리면 §5-3 분기).
### 🔴 **2판(§11)** — §4-2 의 「파생표 꼬리 잠금절」은 실측에서 안 섰다(S1 §3-2). R 재계산만 **(나) RR 전용 직접읽기 식**으로 갈아탄다 · 공용 식·RC 경로·나머지 절 전부 무변경.

---

## 본문

## §1 [문제] 무엇이 왜 막히나

선행검증 확정 사실만 쓴다(재측정 금지).

- `log_bin=ON` **AND** `binlog_format=STATEMENT` → RC 트랜잭션의 `FOR UPDATE`·INSERT 가 **ERROR 1665**. `BEGIN`·일반 `SELECT`·`LOCK IN SHARE MODE` 는 통과.
- RC 를 쓰는 곳은 `CollectionService.cs:132`(수금)·`:301`(지급) 2곳뿐. 매출·매입·재고·BOM·회계는 무영향.
- 그 서버에서는 **수금·지급 등록 3경로(이월잔액 매칭 · 명세서/전표 지정 · ref 없는 순수 등록)가 전부** 막힌다 = #20 흐름 끊김 P0.
- `SET SESSION binlog_format='ROW'` 는 BINLOG ADMIN 권한 필요 → 응용 계정 불가.
- **RR 단순 폴백은 대안이 아니다**: 1665 는 사라지지만 G8-u 최종 R −20,000 · G8-x 초과 매칭 미거절 = 조용한 잔액 오류.

### §1-1 RR 이 왜 틀렸나 — 원인을 한 줄로

RR 은 트랜잭션 첫 읽기에서 스냅숏이 고정된다. 거래처 행을 잠근 뒤에도 **R 재계산이 「일반 SELECT」라서 옛 스냅숏**을 본다.
InnoDB 에서 **잠금 읽기(`FOR UPDATE`·`LOCK IN SHARE MODE`)는 RR 에서도 최신 커밋을 읽는다.**
→ RR 이 틀린 것이 아니라 **RR 에서 일반 읽기로 판정한 것**이 틀렸다. 고칠 자리는 격리수준이 아니라 **R 재계산 문장**이다.

## §2 [축] 경로를 가르는 기준

| 판정 | log_bin | binlog_format | 경로 | 근거 |
|---|---|---|---|---|
| 안전 | OFF | 무엇이든 | **RC** | 1665 조건은 AND (선행 §3) |
| 안전 | ON | MIXED · ROW | **RC** | 대조군 통과 (선행 §3) |
| 위험 | ON | STATEMENT | **RR** | 1665 재현 (선행 §1) |
| 알 수 없음 | 조회 실패·권한거절·타임아웃 | — | **RR** | RR 경로는 두 서버 모두에서 돈다 |

🔴 **왜 RC 를 기본으로 남기나**(반증 답): RC 경로는 3판에서 교착 0 으로 실측되고 이미 main 에 있다. RR 경로는 파생표 공유잠금이 넓어 교착을 재시도로 삼킨다 — 지연과 드문 재시도 소진을 안고 간다.
검증된 경로를 버릴 이유가 없으므로 **안 되는 서버에서만** RR 로 간다. 「코드 하나로 통일(항상 RR)」은 더 단순하지만 전 고객에게 재시도 경로를 강제한다 → **결재 안건 A-3 으로 올린다**(설계 기본안 아님).

## §3 [판정] 시점 · 캐시 · 실패

- **시점**: 등록 트랜잭션을 **열기 직전**. 캐시 히트면 DB 왕복 0.
- **범위**: 프로세스 단위 값 1개(ERP 는 고객 PC 1 프로세스). 연결마다 재조회하지 않는다.
- **수명**: TTL **5분**. 근거 — `SET GLOBAL binlog_format` 은 재기동 없이도 바뀌고, 세션값은 연결이 새로 맺힐 때 따라간다. 5분이면 「고객이 설정을 고친 뒤 다시 저장」이 사람 시간 안에 반영된다. (상수 한 곳 · 값은 PM 재량)
- **즉시 무효화**: ① 프로세스 재기동 ② **실행 중 1665 를 만나면** 캐시를 버리고 다시 판정 → 그 요청은 RR 로 재시도(재시도 조건에 1665 포함 · §4-3).
- **조회 실패**: 예외를 삼키지 않고 `LogWarning` 한 줄(#15) + **RR 경로**. 실패가 등록을 막지 않는다.
- 판정 문장은 시스템 변수 조회 — 테이블·잠금 접근 0 (선행 F8).

## §4 [RR 경로] 정확성을 어떻게 지키나

### §4-1 거래처 직렬화 — G8-x

`SELECT balance_id FROM partner_legacy_balances … FOR UPDATE` **그대로 둔다**. 1665 는 RC/RU 한정이므로 RR 에서는 허용된다(⚠️ §10-1 미실측 · 0단계 확인).
뒤 연결은 앞 연결 커밋까지 이 잠금에서 기다린다 → G8-x 의 「기다렸다 거절」이 성립.

### §4-2 R 재계산 — G8-u · G8-x

`GetForUpdateAsync` 의 두 번째 문장(파생표 R 조회)을 **모드에 따라** 바꾼다.

```
RC 모드 : SELECT … FROM (공용식) x WHERE x.partner_id = @PartnerId
RR 모드 : SELECT … FROM (공용식) x WHERE x.partner_id = @PartnerId LOCK IN SHARE MODE
```

- 공용 식(`ReceivableRemainingSql`·`PayableRemainingSql`) **본문은 한 글자도 안 바꾼다** — 11곳 소비자와 같은 식이어야 한다(3판 계약).
- 잠금절은 **꼬리 한 줄**만 모드로 갈린다. 붙이는 자리는 `GetForUpdateAsync` 한 곳.
- 근거: 3판 1차안(R1b)이 이 방식으로 G8-u·G8-x 를 통과했다(현행 `MatchIsolation` 주석에 폐기 사유가 **교착**이라고만 적혀 있다 = 정확성은 통과했다는 기록). ⚠️ STATEMENT 인스턴스에서는 미실측 → **G-RC3·G-RC4 가 실측한다**(가정으로 넘기지 않는다).

### §4-3 교착 — G8-w · 재시도

1차안이 폐기된 이유가 여기다. 파생표 공유잠금이 회사 범위를 훑어 **다른 거래처 동시 매칭에서도 교착**(G8-w).
RR 경로에서는 교착을 없애는 대신 **삼킨다**.

- **재시도 범위 = 트랜잭션 통째.** 부분 재시도 금지 — INSERT·`partner_balance`·자동기표가 한 트랜잭션이라 중간 재개는 이중 반영이 된다.
- 구현 자리: `CreateCollectionAsync`/`CreatePaymentAsync` 의 **본문을 내부 메서드로 빼고 얇은 재시도 껍질**을 씌운다. 커밋 **뒤**의 감사로그는 껍질 밖(재시도에 안 섞인다).
- **재시도 조건(화이트리스트)**: `MySqlException` 번호 **1213**(교착) · **1205**(잠금 대기 초과) · **1665**(캐시가 낡아 RC 로 갔을 때 · §3).
  `InvalidOperationException`(고객 문구 거절)·그 밖의 예외는 **절대 재시도 안 한다** — 거절을 재시도로 뒤집으면 잔액이 틀어진다.
- **상한·간격**: 총 3회 시도(원 1 + 재시도 2) · 50ms → 150ms + 지터 0~50ms. 사용자 체감 상한 약 0.5초. 값은 상수 한 곳 · 근거는 체감 기준이지 실측 아님(⚠️ §10-3) → G-RC5 가 실제 시도 횟수를 잰다.
- **소진 시 문구**(고객어 · 개발용어 금지 #23):
  > 지금 다른 사용자가 같은 거래처의 수금을 처리하고 있습니다. 잠시 후 다시 저장해 주세요.
  (지급은 "수금"→"지급") — 문구 상수는 `CollectionService` 문구 모음 한 곳.

### §4-4 D1 — 설계가 새로 찾은 구멍

`EnsureCollectionTargetAllowedAsync`(`:782`)·`EnsurePaymentTargetAllowedAsync`(`:820`) 의 **남은금액 SUM 은 일반 읽기**다.
RC 에서는 문장마다 최신이라 맞지만, **RR 에서는 옛 스냅숏** → 같은 명세서에 동시 수금이 들어오면 **합이 전표금액을 넘어도 통과**한다.
선행검증(이월잔액 축)·PM 추천안에 없던 항목이다. RR 경로에서 같은 처방을 쓴다.

```
RR 모드 : SELECT COALESCE(SUM(amount),0) FROM collections … LOCK IN SHARE MODE
          (지급 = payments + purchase_returns 합산 문장 · 같은 모양)
```

전표 행 `FOR UPDATE` 는 이미 있으므로 같은 전표 동시 등록은 직렬화된다. 잠금 읽기는 **그 뒤에 최신 합을 읽게** 한다. 게이트 G-RC6.

## §5 [가드] `LegacyBalanceMatching.cs:185` 를 어떻게 바꾸나

지금: `tx.IsolationLevel != MatchIsolation → NotSupportedException`. RR 을 허용하면 이 방어선이 무너진다. **격리수준을 느슨하게 푸는 게 아니라, 모드를 선언받는다.**

### §5-1 계약

```csharp
public enum LegacyMatchMode { ReadCommittedFresh, RepeatableReadLocking }
public static IsolationLevel IsolationFor(LegacyMatchMode mode);   // RC↔ReadCommitted · RR↔RepeatableRead
```

호출자는 트랜잭션과 **모드를 같이** 넘긴다. 가드는 두 가지를 본다.

1. 모드가 **선언됐나**(기본값 없음 — 인자 필수).
2. `tx.IsolationLevel == IsolationFor(mode)` 인가.

둘 중 하나라도 어긋나면 지금과 같은 `NotSupportedException`. `Serializable`·`ReadUncommitted`·`Snapshot`·`Unspecified` 는 **어느 모드로도 통과 못 한다** = 「아무 격리 수준이나」가 되지 않는다.

### §5-2 모드가 SQL 을 고른다

같은 `mode` 값이 §4-2·§4-4 의 잠금절을 고른다. → **격리수준과 잠금식이 한 값에서 갈린다.**
「RR 로 열고 일반 읽기로 판정」하는 조합은 코드로 표현할 수 없다(3판이 밟은 그 사고를 구조로 막는다).

### §5-3 상수

- `MatchIsolation` 은 **남긴다** — 뜻을 「RC 경로의 격리수준」으로 좁히고 주석 갱신. 외부 참조(게이트 `MatchIsolation`)가 깨지지 않는다.
- `MatchIsolationFallback = IsolationLevel.RepeatableRead` 신설.
- ⚠️ §10-1 이 틀리면(RR+STATEMENT 에서도 `FOR UPDATE` 가 1665) → RR 모드의 거래처 행 잠금도 `LOCK IN SHARE MODE` 로 내리고, 같은 거래처 동시는 **교착으로만 갈린다** = §4-3 재시도가 그 자리를 메운다. 이 분기는 작지 §8 에 미리 적어 둔다.

## §6 [게이트] STATEMENT 서버에서 도는 동작 게이트

### §6-1 인스턴스를 어떻게 얻나

| 안 | 판정 |
|---|---|
| GitHub Actions `services:` 두 개 | ❌ 서비스 컨테이너에 `--log-bin --binlog-format=STATEMENT` 같은 **기동 인자**를 못 준다(`options` 는 docker run 옵션, CMD 인자 아님) |
| 게이트(테스트 코드)가 인스턴스를 띄운다 | ❌ 로컬·CI 제각각 · 서비스 등록 위험(#29) |
| **CI 스텝에서 `docker run`** | ✅ 채택 — `docker run -d -p 3307:3306 -e MARIADB_ROOT_PASSWORD=… mariadb:11.4 --log-bin=stmt --binlog-format=STATEMENT --server-id=9920` |

- 게이트는 **`HITPAN_DB_STMT_PORT`** 로만 그 인스턴스를 찾는다(HOST·USER·PASS 는 기존 변수 재사용).
- **CI 에서 그 변수가 없거나 못 붙으면 FAIL**(`HITPAN_REQUIRE_DB` 와 같은 규칙 — SKIP 을 통과로 세지 않는다).
- 로컬은 선행검증이 쓴 방식(새 datadir · 포그라운드 `mariadbd` · **서비스 등록 금지** · 시험 후 SHUTDOWN)으로 띄운다. 절차는 개발명세서에 적는다.
- 운영 3306 · 격리본 33306 무접촉(#39).

### §6-2 사례 (신설 `RcSafePathGate`)

카드 기대값 표 9건이 정본. 요지만 다시 적는다.

- G-RC1·2 — **위험 서버에서 수금·지급 등록 3경로 전부 성공**(서비스 진입점 경유 · raw SQL 아님). 이게 P0 본안이다.
- G-RC3·4 — G8-u·G8-x 를 **위험 서버에서** 다시 잰다. 최종 R ≥ 0 · 초과 거절 문구 동일.
- G-RC5 — G8-w 동등. **서비스 진입점을 두 연결로 동시 호출** → 둘 다 성공 · 시도 횟수 기록. (기존 G8-w 는 `LegacyBalanceMatching` 을 직접 불러 재시도 껍질 밖이다 → RR 경로 판정에 쓸 수 없다. 그래서 **신설**하고 기존 G8-w 는 안전 서버 회귀로 남긴다.)
- G-RC6 — D1. 같은 명세서 동시 수금 합 초과 거절 + 매입전표 대칭.
- G-RC7 — 판정표 4조합 + 조회 실패 → RR (순수 · DB 불필요).
- G-RC8 — 안전 서버(3306)에서 모드 = RC **대조군**. 이게 없으면 「항상 RR」과 구별이 안 된다.
- G-RC9 — 캐시 왕복 0 / TTL 뒤 1회. 판정기를 인터페이스로 빼고 **세는 구현체**를 주입해 잰다.

### §6-3 기존 G8 24사례 회귀

- 24사례는 **안전 서버에서 그대로 초록**이어야 한다(경로 분기가 RC 를 안 건드린다는 증거).
- **바뀌는 한 줄**: `G8-u` 끝의 계약 가드 — 지금은 `RepeatableRead` 로 부르면 `NotSupportedException` 을 기대한다. RR 이 정식 경로가 되므로 그 줄을 **`Serializable`** 로 바꿔 「아무 격리나 통과 아님」을 계속 잰다. (사례 삭제 아님 · 수 24 유지)
- G9·G10·LabelParity 는 무변경.
- CI 등록: `GATES="$GATES|RcSafePathGate"` + docker 스텝 + **등록 근거는 무력화 시험 실측표**(관례).

## §7 [갈래] 분할과 계약

최대 2갈래 · 각 파일 5개 이하.

### 갈래 S1 — 판정·경로 (파일 4)

| 파일 | 하는 일 |
|---|---|
| `src/HitPan.Application/Services/BinlogSafetyProbe.cs` 🆕 | 판정식 · 프로세스 캐시 · TTL · 실패 시 RR · `IBinlogSafetyProbe` |
| `src/HitPan.Application/Services/LegacyBalanceMatching.cs` | `LegacyMatchMode` · 가드(§5) · R 재계산 잠금절(§4-2) |
| `src/HitPan.Application/Services/CollectionService.cs` | 트랜잭션 분기 · 재시도 껍질(§4-3) · 전표 SUM 잠금절(§4-4) |
| `src/HitPan.API/Program.cs` | `IBinlogSafetyProbe` 싱글턴 등록 (#21 대상 아님 — appsettings 무접촉) |

**계약(시그니처 · 갈래 간 고정)**

```csharp
public interface IBinlogSafetyProbe {
    Task<LegacyMatchMode> GetModeAsync(IDbConnection db, ILogger? logger, CancellationToken ct);
    void Invalidate();                       // 1665 를 만났을 때
}
public sealed class BinlogSafetyProbe : IBinlogSafetyProbe {
    public BinlogSafetyProbe(TimeSpan? ttl = null, TimeProvider? clock = null);
}

// LegacyBalanceMatching — mode 가 tenantId 앞에 온다(잊고 못 지나간다)
Task<Remaining?> GetForUpdateAsync(IDbConnection db, IDbTransaction tx, LegacyMatchMode mode,
    string tenantId, string partnerId, bool receivable, CancellationToken ct);
Task EnsureMatchAllowedAsync(IDbConnection db, IDbTransaction tx, LegacyMatchMode mode,
    string tenantId, string partnerId, string? refId, decimal amount, DateTime date,
    bool receivable, ILogger? logger, CancellationToken ct);

// CollectionService — 재시도 껍질(트랜잭션 통째)
private Task<T> RunMatchTxAsync<T>(LegacyMatchMode mode,
    Func<IDbTransaction, CancellationToken, Task<T>> body, string what, CancellationToken ct);
```

- `CollectionService` 생성자에 `IBinlogSafetyProbe? probe = null` **선택 인자**로 추가 → 기존 호출자·테스트 무변경(계약 안정).
- `ILogger? logger` 는 지금처럼 **null 허용**(서비스는 null 을 넘기고 있다) — 이번 작업에서 로거 배선을 늘리지 않는다(#33 범위 밖).

### 갈래 S2 — 게이트·CI (파일 3)

`src/HitPan.Tests/Integrity/RcSafePathGateTests.cs` 🆕 · `…/LegacyBalanceMatchGateTests.cs`(§6-3 한 줄) · `.github/workflows/build-and-test.yml`.

### 60턴 대비 — 상태 커밋 순서

S1: ① `BinlogSafetyProbe` + 순수 판정 사례 → **빌드 0/0 · 커밋** ② `LegacyMatchMode`·가드·잠금절 → 커밋 ③ 서비스 분기·재시도·D1 → 커밋.
S2 는 S1 ①이 커밋된 뒤 시작한다(계약 확정 후). **개발명세서는 코딩 전에 뼈대(§0 카드 · §1 계약 · §2 변경 파일 목록 · §4 무력화 표 빈칸)를 먼저 커밋**하고 채운다.

## §8 [리스크·롤백]

| 리스크 | 대비 |
|---|---|
| RR 경로가 현장에서 교착을 자주 낸다 | 재시도 상수 한 곳 · 로그 한 줄(`LogWarning`)로 횟수가 남는다 · 값 조정은 상수만 |
| 판정이 틀려 안전 서버가 RR 로 간다 | 정확성은 두 경로가 같다(잠금 읽기) → 틀려도 **돈이 안 틀어진다**. 대가는 지연뿐 |
| 파생표 잠금 읽기가 회사 범위를 훑는다 | 좁히려면 공용 식에 거래처 조건을 밀어 넣어야 하는데 **11곳 소비자와 식이 갈라진다** → 이번엔 안 한다(재시도로 흡수) |
| 롤백 | `BinlogSafetyProbe` 가 항상 RC 를 돌려주게 하는 **상수 1줄**로 3판 동작으로 되돌아온다. 게시 전 즉시 가능 |

## §9 [결재] 새 안건

| # | 안건 | 누구 | 설계 입장 |
|---|---|---|---|
| A-1 | 고객 DB 계정에 **BINLOG ADMIN** 부여 후 세션 `binlog_format='ROW'` | 사장님 | ❌ **기본안 아님** — 권한 확대 = 공격면 확대. 이 설계는 권한 없이 해결한다 |
| A-2 | 설치본이 **기존 MariaDB 의 my.ini/SET GLOBAL 을 고치는** 안 | 사장님 | ❌ **기본안 금지** — 남의 서버 설정 조작 = #29 사전 승인 대상 |
| A-3 | 2경로 유지 vs **항상 RR 한 경로**로 통일 | 사장님 | 기본안 = 2경로(§2). 한 경로가 더 단순하나 전 고객이 재시도를 안는다 |
| A-4 | **이미 STATEMENT 인 고객 PC 가 있다면 지금도 수금·지급이 막혀 있다** — 현장 확인 주체·방법 | 사장님 | 설계 밖. 본사가 설정을 긁어오는 방식은 #18·#22 와 걸린다 |
| A-5 | 재시도 상한 3회·50/150ms · 소진 문구 · TTL 5분 | PM | 상수 한 곳 · 게이트가 실측 |

## §10 [⚠️ 미실측] 추측을 사실로 쓰지 않는다

1. **RR + STATEMENT 에서 `FOR UPDATE` 가 허용되는가** — 선행검증은 RC 에서만 쟀다. 문서상 1665 는 RC/RU 한정이지만 **안 쟀다.** → 구현 **0단계에서 raw 로 확인**(§5-3 분기 준비).
2. **파생표 `LOCK IN SHARE MODE` 가 STATEMENT 서버에서 최신 커밋을 읽는가** — 3판 1차안 기록이 근거일 뿐, 이 조합은 미실측. G-RC3·G-RC4 가 판정한다.
3. **교착 빈도·재시도 성공률** — 미실측. 상한 3회는 체감 기준이지 실측 근거가 아니다.
4. **고객 PC·사장님 PC 의 `log_bin`/`binlog_format` 분포** — 안 봤다. 「모른다」가 사실이다. 빈도를 숫자로 말하면 받아쓰기(#32).
5. **D1(명세서·전표 SUM 스냅숏 오염)이 현장에서 실제로 났는지** — 모른다. RR 경로를 새로 여는 이번 작업이 **만들지 않게** 막는 것이지, 과거 사고 보고가 있는 게 아니다.

---
**작성**: 설계팀장 존 · 2026-09-20 / **상태**: ⬜ PM 결재 대기 (작성자 ≠ 결재자)

---

## §11 [2판] RR 최신값 처방 확정

> 설계팀장 존 · 2026-09-21 · **§1~§10 은 한 글자도 안 고친다**. 이 절은 **§4-2 한 점**만 갈아끼운다.
> 근거: 갈래 S1 개발명세서 §3-2 실측 — 파생표 바깥 꼬리절 = 옛값(R `90000` / 기대 `70000`) · 실테이블 직접읽기(D1) = 최신(`0.00`→`11000.00`).

### §11-1 후보와 위험

| 후보 | 무엇 | 공용 식 갈라짐 | 잠금 범위·교착 | 11곳 영향 | 판정 |
|---|---|---|---|---|---|
| (가) 잠금절을 파생표 **안쪽** 하위질의마다 | 공용 식 본문에 모드별 잠금절 삽입 | 🔴 본문이 모드별로 갈린다 = 식이 둘 | 🔴 `lm`/`em` 은 **회사 전체** GROUP BY → 3판 1차안이 폐기된 그 교착(§4-3) 재진입 | 🔴 11곳이 같이 바뀐다 | ❌ |
| (나) **RR 전용 재계산 SQL** (실테이블 직접 · D1 과 같은 모양) | RR 모드에서만 파생표 대신 직접읽기 2~3문장 | 🟡 식이 둘 → **§11-3 동등성 게이트로 묶는다** | 🟢 **거래처 1건으로 좁혀진다**(파생표보다 좁다) | 🟢 0 — 공용 식 무접촉 | ✅ **채택** |
| (다) 「트랜잭션 첫 문장 = 거래처 행 `FOR UPDATE`」 계약 + 일반 읽기 | 읽기 뷰가 잠금 읽기 뒤에 열리는 성질에 기댄다 | 🟢 없음 | 🟢 현행과 같음 | 🟢 0 | ❌ **§11-2** |
| (라) `SERIALIZABLE` 로 전부 암묵 공유잠금 | 격리수준으로 해결 | 🟢 없음 | 🔴 모든 읽기가 잠금 · 교착 폭증 | 🟢 0 | ❌ 가드가 금지(§5-1) · 파생표 안쪽까지 닿는지도 **미실측** |
| (라-2) 재계산 **직전에** 기본 테이블 행을 잠그고 파생표는 일반 읽기 | 잠금으로 뷰를 새로 연다는 기대 | — | — | — | ❌ **실측이 반증**: 이미 열린 읽기 뷰는 잠금으로 안 바뀐다. 잠금 읽기 **그 문장만** 최신이다(S1 §3-2) |

### §11-2 (다)를 기각하는 이유 — 코드로 못 막는다

(다)는 「이 트랜잭션에 **앞선 일반 읽기가 하나도 없었다**」가 전제다. 이 전제를 코드가 강제하려면 그 연결에서 지금까지 나간 문장을 전부 알아야 한다. 우리 코드의 읽기는 **호출자마다 Dapper 를 직접** 부른다 — 관문이 없다. 결국 「먼저 잠그고 나서 읽어라」는 **사람이 지키는 호출 순서 규칙**으로만 남고, 이건 3판 병렬이슈44 가 밟은 바로 그 함정이다. 게다가 어긋나도 예외가 아니라 **조용히 옛 금액**이다(돈이 틀린다).
→ 성립 조건이 맞아떨어지는 경우가 있더라도(⚠️ §11-6) **처방으로 삼지 않는다.**

### §11-3 (나)를 고르면 — 두 식이 갈라지지 않음을 무엇이 보장하나

**장치 3개를 같이 건다. 하나라도 빠지면 (나)는 못 쓴다.**

1. **G-RC10 동등성 게이트(값)** — 같은 DB 상태에서 `공용 식(RC 읽기)` 과 `RR 재계산` 의 L0·M·R 이 **세 값 모두 같아야** 한다. 사례(기대값을 같이 적는다 · 대조군):

| 사례 | 상태 | 기대 |
|---|---|---|
| E1 부분매칭 | L0 100,000 · 이월수금 30,000 | 양쪽 R = 70,000 |
| E2 취소분 | 위 + `is_active=0` 수금 20,000 | 양쪽 M 에서 제외 (R 70,000 유지) |
| E3 이관분 | `source_type='migration'` 수금 50,000 + 이관 명세서(`sd.source_type='migration'`)에 붙은 정상 수금 10,000 | 앞은 제외·뒤는 **포함**(em) → 양쪽 M = 40,000 |
| E4 음수 이월 | `balance_amount = -50,000` (미수 축) | 양쪽 L0 = 0 · R = -M (음수 그대로 · 둘이 같다) |
| E5 행 없음 | `partner_legacy_balances` 행 없음 | 공용 식 0행 · RR 경로 `null` — 양쪽 「없음」 |
| E6 매입 대칭 | 지급(lm·em) + **확정** 반품(rm) + 미확정 반품 | 미확정 제외 · 양쪽 M 동일 |
| E7 초과 | M > L0 | 양쪽 R 음수 동일값(자르지 않는다) |

2. **G-RC11 앵커 게이트(텍스트)** — `ReceivableRemainingSql`·`PayableRemainingSql` 의 **SHA-256 을 게이트에 기록**해 둔다. 공용 식을 고치면 이 게이트가 **먼저 빨간불** → RR 식을 같이 고치고 앵커를 갱신할 수밖에 없다. (낱말 세기 아님 · 문자열 전체 동일성)
3. **기존 `LegacyBalanceFormulaGate` 무변경 초록** — 11곳 소비자 쪽은 애초에 안 건드렸다는 증거.

> 🔴 공용 식을 조각 상수로 쪼개 RR 식과 **텍스트를 공유**하는 안은 더 낫지만 11곳이 쓰는 문자열을 손대야 한다 → **기본안에서 뺀다**(범위 최소 · #33). §11-7 결재 안건.

### §11-4 RR 재계산의 모양 (실측된 모양만 쓴다)

- L0·base_date 는 **이미 있는 첫 문장**(`… FROM partner_legacy_balances … FOR UPDATE`)에서 같이 받는다 — 잠금 읽기라 최신이고 **잠금이 늘지 않는다**.
- M 은 **실테이블 직접 · 거래처 1건 필터 · 꼬리 `LOCK IN SHARE MODE`** 문장으로 나눠 읽는다(미수 2문장 · 미지급 3문장). 공용 식의 각 `LEFT JOIN` 하위질의 술어를 **그대로** 옮긴다(별칭 `lc·ec·lp·ep·rt/rti/pr` 유지).
- ❌ **금지**: `SELECT SUM(..) FROM (… UNION ALL …) t` · `SELECT (SELECT …), (SELECT …)` — 파생표·스칼라 하위질의는 이번에 **안 서는 것으로 실측된 모양**이다. 같은 이유로 #16(`UNION ALL` 권장)은 여기서는 **적용 예외**다(한 연결·순차 · `Task.WhenAll` 아님).
- R = `GREATEST(L0,0) - ΣM` 을 **C# decimal** 로(#4) — SQL 의 산술과 같은 식, 같은 자릿수.
- 🔴 **§4-4(D1) 지급 쪽 재점검**: 현행 지급 합산은 **스칼라 하위질의 2개** 안에 잠금절이 있다(`CollectionService.cs:851·855`). S1 이 실측한 것은 **수금 쪽 단일 직접 문장**뿐이다 → 같은 함정일 수 있다. **직접 문장 2개로 분리**하고 G-RC6 에 매입 축을 실측으로 넣는다.

### §11-5 안전 경로(RC) 는 무변경

RC 모드는 지금 코드 그대로 — 파생표 + 빈 꼬리절. 새 RR 문장은 `mode == RepeatableReadLocking` 에서만 만들어진다. 근거: 문장마다 최신을 보는 RC 에서는 애초에 문제가 없고, 이미 안전 서버 회귀 **40/40 · Skipped 0**(S1 §5) 이 무변경을 실측했다. **G-RC8 대조군**이 「안전 서버는 RC 로 간다」를 계속 잰다.

### §11-6 ⚠️ 2판이 안 잰 것

1. **(다)의 갈림** — 「앞선 일반 읽기가 없으면 일반 읽기도 최신인가」는 **안 쟀다.** 기각 사유는 실측이 아니라 **강제 불가**다. (나)를 채택하므로 재지 않는다.
2. **새 RR 재계산 문장이 STATEMENT 서버에서 최신을 돌려주는가** — S1 이 잰 것은 D1 의 `collections` 단일 문장이다. 모양이 같지만 **조인 있는 em/rm 문장은 안 쟀다.** → S1b **0단계에서 raw 두 연결로 먼저 잰다**(이번 2판이 태어난 이유가 「안 재고 처방했다」이다).
3. **좁아진 잠금의 교착 빈도** — 여전히 미실측. §4-3 재시도가 그대로 받는다.

### §11-7 새 결재 안건 (2판)

| # | 안건 | 누구 | 설계 입장 |
|---|---|---|---|
| B-1 | 공용 식을 **조각 상수로 분해**해 RR 식과 텍스트 공유(§11-3 각주) | PM | 기본안 제외 — 11곳 텍스트를 건드린다. 앵커 게이트로 대체. 3판 이후 백로그 |
| B-2 | RR 재계산이 문장 2~3개로 나뉜다(왕복 증가) | PM | 승인 요청 — 한 트랜잭션·순차 · 잠금 범위는 오히려 좁아진다 |
| B-3 | 지급 D1 문장 모양 변경(스칼라 하위질의 → 직접 문장 2개) | PM | 승인 요청 — S1 이 실측한 모양으로 맞춘다(§11-4) |
