# 설계서 — 메인PC 판정축 재설계 (2026-08-19) · **3차 · [3-V] 반증 반영 · 결재 대기**

| 항목 | 내용 |
|---|---|
| 작성 | 설계팀장 존 · 2026-08-19 |
| 오더 | 사장님 — *"설계부터 해"* (8/19) |
| 🔴 **1차 폐기** | 사장님 답변 *"테스트1으로 하고 있어"*(터널만 사용) 로 **1차 설계안이 무너졌다** (§3-3) |
| 근거 | 인수인계서 §4-1 (P0) — `docs/운영기록/20260818_인수인계서_기기슬롯4·5차_1.2.87게시_실측6회갇힘_참조식.md` |
| 상태 | 🔴 **결재 대기** — 이 문서 결재 전 코드 금지 (헌법 #33·#42) |
| 🔴 결재 성격 | **자동 판정을 포기하는 결정** — PM 이 혼자 못 정한다 (§7-1) |
| 🔴🔴 **[3-V] 판정** | 2차 **결재 불가** → **3차에서 화면 축으로 재작성 완료** → `docs/검증/병렬이슈/20260819_3V_D1설계반증_서버는이미열려있다_진범은화면.md` |
| 🟢 3차 본안 | **E — 관문이 자기 안에서 승인을 끝낸다** (§3-4). 라우팅·`@Body` 예외·주소대조 **전부 안 씀** |
| 짝 문서 | 작업지시서 — **결재 후 발행** |

> **참조식**: 헌법은 조항 번호로, 코드는 `파일:줄` 로만 가리킨다. 원문을 옮기지 않는다.

---

## 🔴🔴 [3-V] 반증 결과 — **2차가 반증됐고, 3차로 재작성했다** (2026-08-19)

> 사장님 지시 *"루틴대로 설계-개발-검증 =실시간검증"* 에 따라 **코드 착수 전** 병렬검증을 돌렸고,
> **본안 D-1 이 반증됐다.** 전문 → `docs/검증/병렬이슈/20260819_3V_D1설계반증_서버는이미열려있다_진범은화면.md`

| # | 무엇 | 아래 어느 절이 틀렸나 |
|---|---|---|
| 🔴 치명1 | **서버 관문에는 대표 예외가 이미 있다** (`DeviceAuthMiddleware.cs:225-231` · `:38-41` Bypass 에 `/api/devices`) ⇒ 대표는 승인 API 를 **지금도 200 으로 부른다** | **§3-4 의 전제**가 무너짐 |
| 🔴 치명2 | **진범은 화면이다** — `MainLayout.razor:67` 이 관문 켜지면 `@Body` 를 통째로 안 그리고, `DeviceAuthGate.razor:147` 은 **계정 종류를 안 본다** ⇒ 주소를 직접 쳐도 승인 화면이 안 나온다 | **§3-4 가 고칠 자리를 잘못 짚음** |
| 🔴 치명3 | `_gateActive` 는 **bool 하나**라 *"한 페이지만 예외"* 방법이 없다. 대표 예외로 짜면 **모든 업무 화면**이 열리고(§G-C 실패), 주소 문자열로 짜면 **하이픈 사고 계통 재발** | **§3-4 에 구현 방법이 없음** |
| ⚠️ 중5 | `tenant_admin` **0줄 가능** (출하 DDL 시드 0건 · 옛 프로비저닝 사고 이력) ⇒ 그 회사는 **아무도 못 구한다** | **갈래 누락** |
| ⚠️ 중6 | §4-1 *"그대로 둔다"* 가 모순 — 누군가 `localhost` 로 한 번 열면 옛 봉합이 **메인PC 표식을 빼앗아 간다** | **§4-1 이 반대 방향을 안 봄** |

✅ **3차에서 반영 완료** — §3-4 를 **본안 E**(관문 안에서 승인)로 재작성 · §4-1 에 중6 · §4-2 에 중5 · §5-2-1 에 게이트 축 교정.
🟢 **살아남은 것**: §1(실측 진단) · §2 · §3-3(1차안 폐기 근거) · §5-1-1(로컬 게이트 함정) · §6.

> 🔴 **이 반증이 무엇을 막았나**: 코딩 후였으면 **미들웨어에 이미 있는 줄을 한 번 더 쓰고**
> *"고쳤다"* 고 보고했을 것이다. G-E 는 옛 예외 덕에 **초록불**이 떴을 것이다(**가짜게이트 10번째**).
> ⇒ *"고쳤다 ≠ 갔다"* 6차 재발을 **착수 전에** 끊었다.

---

## 0. 무엇이 났나 — 한 줄

**메인PC 를 알아보는 축이 세 곳에 있는데, 셋 다 사장님이 실제로 쓰시는 길에서 거짓이 된다.**

8/18 에 짠 봉합(`TenantDeviceService.cs:269`)은 빌드 0/0 · 게이트 5/5 · 반증 3회를 다 통과했다.
그런데 사장님은 수동으로 탈출하셨다. **자동은 한 번도 돌지 않았다.**

---

## 1. 🔴 실측 — 축이 왜 거짓이 되는가

> 헌법 #32 에 따라 어제 결론을 받아쓰지 않고 코드로 다시 확인했다.

### 1-1. 판정식은 **세 곳에 같은 모양**으로 있다

| # | 자리 | 무엇을 막나 |
|---|---|---|
| ① | `src/HitPan.API/Controllers/AuthController.cs:136-140` | 메인PC 자동 승인 (`IsLocalConsole`) |
| ② | `src/HitPan.API/Controllers/DeviceController.cs:106-113` | 자료관리 화면 (`is-main-pc`) |
| ③ | `src/HitPan.API/Controllers/AuthController.cs:509-512` | 로컬 업데이트 상태 |

셋 다 **같은 두 조건**이다 — 터널 헤더가 없을 것 + 소켓이 loopback 일 것.

### 1-2. 🔴 그 두 조건은 **사장님 경로에서 반드시 거짓**이다

사장님과 고객은 `https://test1.hitpan.kr` 로 여신다. **메인PC 앞에 앉아서도 그렇다.**

그 요청은 브라우저 → 인터넷 → Cloudflare → cloudflared → API 로 온다.
⇒ `CF-Connecting-IP` 가 **반드시 붙는다.** ⇒ ①②③ 전부 거짓.

🔴 **결론: 봉합이 틀린 게 아니라, 봉합이 깔린 길로 아무도 안 다닌다.**
`localhost:5257` 로 여는 사람은 **개발 PC 의 우리뿐**이다.

### 1-3. 🔴 그런데 그 조건을 뺄 수도 없다 — 8/10 에 두 번 무너진 자리

터널은 **고객 PC 안에서 돌며 자기 PC 의 localhost 를 다시 부른다**
(`MainPcRegistrationService.cs` 클래스 주석 · `HitPan-Universal.iss:1212` ingress origin).
API 는 loopback 에만 귀를 연다.

⇒ 헤더 배제절을 빼면 **도달한 요청이 100% loopback** 이라 판정이 **항상 참**이 된다.
그러면 **바깥 휴대폰까지 메인PC 를 자칭**한다. 그것이 8/10 에 걷어낸 사고다.

### 🔴 1-4. 이것이 이 문제의 정체

> **접속 경로로는 이 문제를 풀 수 없다.**
> 헤더를 보면 사장님이 막히고, 안 보면 아무나 통과한다. **양쪽 다 사고다.**
> ⇒ 판정축을 **접속 경로가 아닌 다른 축**으로 옮겨야 한다.

이것은 이미 우리가 아는 결론이다 — `MainPcRegistrationService` 클래스 주석이
*"접속 경로를 아예 보지 않는다. **내가 어디서 도나** 를 본다"* 고 **8/10 에 적어 뒀다.**
🔴 **8/18 봉합은 그 결론을 잊고 접속 경로로 되돌아간 것**이다. (받아쓰기 계통 · 헌법 #33)

---

## 2. 🔴 축을 다시 세운다 — 무엇이 "그 컴퓨터"인가

### 2-1. 서버는 **이미 답을 알고 있다**

`MainPcRegistrationService` 가 기동 때마다 `MAINPC-{해시}` 지문 줄을 심는다
(`MainPcRegistrationService.cs` — `BuildServerFingerprint()`, `MachineName|TENANT_CODE|SLOT_INDEX`).

**그 프로세스가 도는 컴퓨터 = 메인PC.** 이 축은 터널과 무관하다 — 흔들린 적이 없다.

### 2-2. 🔴 그런데 브라우저 줄과 **이어지지 않는다**

| | 서버 줄 | 화면 줄 |
|---|---|---|
| 지문 | `MAINPC-…` (컴퓨터 이름) | `HFPv2-…` (화면·브라우저 특성) |
| `is_main_pc` | 1 | 0 |
| 상태 | approved | **pending** ← 여기서 갇힌다 |

두 지문은 **서로 만들 수 없다.** 일부러 그렇게 갈라 놨다(네임스페이스 충돌 방지).
브라우저는 **컴퓨터 이름을 알 수 없다** — OS 정보 접근이 막혀 있다
(`device-fingerprint.js` — `getDeviceName` 주석).

🔴 **진짜 문제는 이 한 줄이다: 서버는 자기가 메인PC 인 걸 알지만, 그 앞에 앉은 브라우저에게 그것을 증명해 줄 길이 없다.**

---

## 3. 🔴 설계안 — **판정을 포기하고 순환을 끊는다**
### 3-1. 뼈대 (헌법 #34 — 뼈대 먼저)

> ⚠️ **아래 1차 뼈대는 §3-3 에서 폐기됐다.** 사장님이 터널만 쓰신다는 답변으로 무너졌다.
> **판단 과정을 지우지 않고 남긴다**(헌법 #1) — 다음 사람이 같은 길을 다시 파지 않게.

<details>
<summary>🔴 폐기된 1차 뼈대 — 「서버가 자기 디스크에 표식을 적는다」</summary>

```
[서버 기동] MainPcRegistrationService → 지문을 자기 PC 파일에 적는다
[화면 열림] 브라우저가 API 에 "이 컴퓨터 표식 좀 줘" → 파일을 읽어 돌려준다
[로그인]   브라우저가 그 값을 같이 보낸다 → 서버가 자기 파일 값과 대조
```

**폐기 사유**: 표식을 **건네주는 경로**가 터널로 열려 있다. 그 경로를 loopback 으로 잠그면
사장님은 표식을 **한 번도 못 받으신다**(터널만 쓰시므로) ⇒ 어제와 똑같은 *"아무도 안 가는 길"*.

</details>

#### 🟢 본안 뼈대 — **메인PC 를 알아보려는 시도를 그만둔다**

```
[문제] 대표가 갇혔다 → 자기를 승인해야 풀린다 → 승인 화면이 관문 뒤에 있다 → 못 들어간다
                                                                    ↑ 순환
[본안] 관문이 대표계정에게는 **승인 화면 하나만** 열어 준다
       └ 업무 화면은 그대로 막힌다 (관문의 뜻은 유지)
       └ 메인PC 인지 **묻지 않는다** — 물을 방법이 없으므로(§3-4)
```

🔴 **"그 컴퓨터인가" 를 푸는 대신, 그것을 몰라도 되게 만든다.**

### 3-2. 🔴 왜 이것이 안전한가 — 자칭을 막는 자리

| 반증 질문 | 답 |
|---|---|
| 바깥 휴대폰이 승인 화면에 들어가나? | ⚠️ **대표 비밀번호를 알면 들어간다** — 아래 |
| 그것이 위험한가? | ❌ **대표 계정을 뺏긴 상황은 이미 전면 장악**이다. 기기 관문이 막을 급이 아니다 |
| 자칭할 수 있나? | ❌ 브라우저가 보내는 값이 아니라 **로그인 통과 사실**로 판정한다 |
| 슬롯이 새나? | ❌ 화면 하나를 여는 것이지 **기기가 승인되는 것이 아니다.** 줄을 만들지 않는다 |
| 8/10 처럼 항상 참이 되나? | ❌ 접속 경로를 **아예 안 본다** |

🔴 **판정축이 "어느 컴퓨터인가"(알 수 없다) 에서 "누가 로그인했나"(서버가 이미 안다) 로 바뀐다.**
### 3-3. 🔴 급소 — **표식을 어떻게 건네주는가**

값을 돌려주는 경로가 **터널로도 열려 있으면** 바깥에서 그 주소를 불러 표식을 받아 간다.
⇒ **그때부터 아무나 메인PC 다.** 🔴 **이 자리가 이 설계의 전부다.**

#### 🔴 1차 설계안은 폐기한다 — 사장님 답변으로 무너졌다

1차는 *"값을 주는 경로만 loopback 으로 잠근다"* 였다. 근거는 **한 번은 로컬로 여실 것**이라는
**PM 의 추측**이었다.

> **사장님 답변 (8/19)**: *"테스트1으로 하고 있어."* — **터널만 쓰신다.**

⇒ 그 발급 경로는 **한 번도 실행되지 않는다.** 🔴 **어제 봉합과 똑같이 "아무도 안 가는 길"이다.**
같은 계통을 **하루 만에 두 번** 낼 뻔했다 — 추측을 근거로 삼았기 때문이다.

> ⚠️ **API 는 `127.0.0.1` 에만 귀를 연다**(`HitPan-Universal.iss:2643`). `localhost:5257` 로 여는 것이
> 기술적으로 **가능은 하다.** 그러나 **사장님이 안 그러신다.** 되는 것과 하시는 것은 다르다.

#### 🔴 본안 — 브라우저를 거치지 않는다

표식을 **화면에 건네주지 않는다.** 건네주는 순간 그 경로를 지켜야 하고, 그 경로는 터널로 열려 있다.

> **대신 서버가 자기 파일을 자기가 읽고, 자기가 판정한다.**
> 브라우저는 표식을 **모르고, 보관하지 않고, 보내지 않는다.**

그러면 **무엇으로 그 브라우저가 그 컴퓨터에 있다고 아는가** — 이것이 남는 유일한 질문이다.

**답: 서버가 이미 알고 있는 것과 대조한다.**

`MainPcRegistrationService` 는 `MAINPC-` 줄을 **자기 손으로 심는다.** 그 줄이 있다는 것은
**이 프로세스가 이 컴퓨터에서 돈다**는 뜻이다. 필요한 것은 그 줄과 지금 로그인한 화면 줄을
**잇는 근거** 하나뿐이다.

| 안 | 잇는 근거 | 판정 |
|---|---|---|
| A | 접속 경로(loopback·헤더) | 🔴 **폐기** — §1-4. 사장님 경로에서 항상 거짓 |
| B | 브라우저가 보관한 표식 | 🔴 **폐기** — 건네주는 경로가 터널에 열린다(위) |
| C | **서버가 발급한 값을 화면이 되돌려 보냄** | ⚠️ B 와 같은 문제 — 발급 경로가 그대로 남는다 |
| D | 🟢 **표식을 아예 안 쓴다 — 사람이 한 번 지정한다** | **본안 후보** (§3-4) |

### 3-4. 🟢 본안 **E** — 관문이 **자기 안에서** 승인을 끝낸다 (3차 · [3-V] 반영)

> ⚠️ **D-1(2차안)은 [3-V] 에서 반증됐다.** 폐기 이유는 §3-5 에 남긴다.

#### 🔴 [3-V] 가 뒤집은 것 — **고칠 자리가 서버가 아니었다**

| 내가 믿었던 것 | 실제 (실측) |
|---|---|
| 대표가 승인 API 에 못 간다 | 🔴 **간다.** `DeviceAuthMiddleware.cs:225-231` 대표 예외 + `:38-41` `/api/devices` **Bypass** |
| 서버 관문을 고쳐야 한다 | 🔴 **서버는 이미 열려 있다.** 고치면 **있는 줄을 한 번 더 쓰는 것** |
| 화면은 라우팅으로 열면 된다 | 🔴 **못 연다.** `MainLayout.razor:67` 이 `@Body` 를 **통째로** 안 그린다 |

> 🔴 **API 는 열려 있는데 그 API 를 부를 화면이 없다.** 이것이 8/18 의 진짜 자리다.

#### 🟢 본안 E — **화면을 열지 말고, 관문 안에 승인 UI 를 둔다**

D-1 이 막힌 이유는 *"관문 뒤의 페이지를 어떻게 보여줄까"* 를 풀려 했기 때문이다.
🔴 **그 문제를 풀지 않는다. 관문 밖으로 나갈 필요 자체를 없앤다.**

```
[지금]  대표가 갇힘 → /settings/devices 로 가야 함 → MainLayout 이 Body 를 안 그림 → 막힘
[본안]  대표가 갇힘 → 관문 화면 안에 **[기기 승인] 구역**이 뜬다 → 거기서 바로 승인 → 관문이 스스로 사라짐
```

#### 🔴 왜 이것이 치명3 을 피하는가

[3-V] 치명3 은 *"`_gateActive` 가 bool 하나라 한 페이지만 예외를 못 만든다"* 였다.

> 🟢 **본안 E 는 `_gateActive` 를 건드리지 않는다.** 관문은 켜진 채로 있고,
> **관문 컴포넌트 자신이** 승인 구역을 하나 더 그린다.
> ⇒ 라우팅 없음 · `@Body` 예외 없음 · **주소 문자열 대조 없음**(하이픈 사고 계통 회피).

#### 🟢 왜 구현이 작은가 — 필요한 것이 **이미 다 있다** (실측)

| 필요한 것 | 이미 있나 | 근거 |
|---|---|---|
| 승인 대기 목록 | 🟢 `GET api/devices` 후 `status=="pending"` 추리기 | `DeviceManagePage.razor:418-420` |
| 승인 실행 | 🟢 `POST api/devices/approve/{id}` | `DeviceManagePage.razor:470` |
| 그 경로가 관문을 통과하나 | 🟢 **Bypass 목록에 있다** | `DeviceAuthMiddleware.cs:41` |
| 대표만 부를 수 있나 | 🟢 **서버가 막는다** | `DeviceController.cs:196` · `Program.cs:355` |
| 관문에 `HttpClient` 가 있나 | 🟢 이미 주입돼 있다 | `DeviceAuthGate.razor` `@inject HttpClient Http` |
| 인증키 발급·표시 | 🟢 승인 응답에 실려 온다 | `DeviceManagePage.razor:482-486` |

🔴 **새로 만드는 API 0건 · DDL 0건 · 라우팅 변경 0건.**
관문 컴포넌트 안에 **대표일 때만 그리는 구역**을 하나 더하는 것이 전부다.

#### 🔴 대표 여부는 **서버가 정한다** (치명 회피 · 헌법 #2)

화면이 *"나 대표야"* 로 정하면 [3-V] 경8 이 지적한 대로 **화면이 판단하게 된다.**

⇒ `gate-status`(`DeviceController.cs:129`) 응답에 **서버가 판정한 값**을 하나 더 실어 내린다.
- 🔴 판정 근거는 **JWT `account_type` 클레임**(헌법 #2) — 파라미터로 받지 않는다
- ⚠️ 화면은 그 값을 **표시 용도로만** 쓴다. **실제 통제는 서버가 이미 한다**(`approve` 는 대표 전용)
  ⇒ 화면 값이 조작돼도 **승인은 서버에서 거부된다.** 백지 구역이 보일 뿐 실피해 없음

#### ⚠️ [3-V] 중4 대응 — **두 축을 같이 본다**

`DeviceManagePage.razor:5` 는 **Role** 축, D-1 판정은 **`account_type`** 축이었다(`AccountTypeHelper.cs:31-35` 가 두 축 존재의 증거).
🟢 **본안 E 는 그 페이지로 안 가므로 Role 축을 안 지난다.** 서버 `approve` 의 `account_type` 한 축만 지난다.
⇒ **두 축이 갈린 계정에서도 막히지 않는다.** 중4 가 저절로 해소된다.

### 🔴 3-5. 폐기한 안 — 지우지 않고 남긴다 (헌법 #1)

| 안 | 무엇 | 폐기 사유 |
|---|---|---|
| A | 접속 경로(loopback·헤더) | §1-4 — 사장님 경로에서 항상 거짓 / 빼면 항상 참 |
| B·C | 서버가 디스크 표식 발급 → 화면이 보관·회신 | §3-3 — **발급 경로를 아무도 안 지난다**(터널만 쓰심) |
| **D-1** | **서버 관문에 대표 예외 추가** | 🔴 **[3-V] 치명1 — 이미 있다.** 짜면 동작 0 변화 + **가짜게이트 10번째** |
| **D-1′** | `MainLayout` 에서 대표만 `@Body` 렌더 | 🔴 **[3-V] 치명3 — 모든 업무 화면이 열린다**(G-C 실패) |
| **D-1″** | `NavigationManager.Uri` 대조로 한 페이지만 | 🔴 **[3-V] 치명3 — `app-version` 하이픈 사고 계통** |

> 🔴 **다섯 번 갈아엎었다. 그중 세 번은 코드 한 줄 짜기 전에 문서에서 죽었다.**
> 그것이 이 절차가 하는 일이다.
## 4. 🔴 이 축이 고치는 자리 — **그리고 못 고치는 자리**

| 자리 | 지금 | 본안 E 적용 후 |
|---|---|---|
| **대표가 자기 기기 승인** (`DeviceAuthGate.razor`) | 🔴 **갇힘 — P0** | ✅ **닫힌다** — 관문 안에서 끝 |
| 메인PC 자동 승인 (`AuthController.cs:136`) | 🔴 터널이면 안 돎 | ⚠️ **그대로 둔다** (§4-1) |
| 자료관리 화면 (`DeviceController.cs:102`) | 🔴 터널로는 백업·복구 못 하심 | ⚠️ **D-2 별건 결재** (§7-2 ③) |
| 로컬 업데이트 상태 (`AuthController.cs:509`) | ⚠️ 같은 축 | ⚠️ 판정 보류 (§7-2 ②) |

### 🔴 4-1. `IsLocalConsole` 을 **왜 안 걷어내나** — 그리고 [3-V] 중6

`IsLocalConsole` 축은 **틀린 게 아니라 사장님 경로에서 안 돌 뿐**이다.
`localhost` 로 여는 경우엔 정상 동작한다 ⇒ **걷어내면 그 경우가 죽는다.**

⇒ **그대로 두고, 그 위에 본안 E 를 얹는다.** 🔴 **고장난 것을 고치는 대신, 고장나도 갇히지 않게 만든다.**

#### 🔴 그런데 [3-V] 중6 이 반대 방향을 짚었다 — **그 축이 참이 될 때**

`TenantDeviceService.cs:270-315` 는 `IsLocalConsole && !isMainPc` 이면
① **옛 메인PC 줄을 `revoked` 로 내리고** ② 지금 줄을 `is_main_pc=1, approved` 로 세운다.

**시나리오**: 사장님이 본안 E 로 터널에서 정리한 뒤, 누군가 그 PC 에서 **`localhost:5257` 로 한 번만**
열면(개발자·CS 원격지원이 하는 일) ①이 돌아 **방금 세운 줄을 내리고 표식을 가져간다.**

⚠️ **이번 작업에서 이것을 고치지 않는다.** 이유:

| 왜 | |
|---|---|
| **본안 E 의 P0 는 안 깨진다** | E 는 `is_main_pc` 를 **안 쓴다.** 대표 여부(`account_type`)만 본다 ⇒ 표식이 옮겨가도 **대표는 계속 승인할 수 있다** |
| 영향 범위가 **자료관리(D-2)** 다 | `is_main_pc` 를 읽는 곳은 자료관리와 `DeviceAuthMiddleware.cs:180-186` ⇒ **D-2 결재와 같이 봐야 한다** |
| 🔴 **여기서 손대면 슬롯·과금 줄을 건드린다** | ①은 `status` 를 바꾼다. 돈이 걸린 자리를 P0 봉합에 끼워 넣지 않는다 |

🔴 **그러나 "없는 문제" 로 적지 않는다.** `docs/검증/병렬이슈/…_진범은화면.md` 중6 에 남아 있고,
**D-2 작업지시서의 선행 조건**으로 이월한다(§7-2 ③).

### 🔴 4-2. [3-V] 중5 — **대표계정이 0줄이면 본안 E 도 아무도 못 구한다**

| 근거 (실측) | |
|---|---|
| `installer/hitpan_db_clean.sql:4045` | `account_type … DEFAULT 'tenant_user'` |
| 같은 파일 | `tenant_admin` **시드 INSERT 0건** |
| `TenantsController.cs:21` 주석 | 🔴 **사고 이력** — *"account_type 미설정 → 기본값 `tenant_user`(자식)로 만들어…"* |

⇒ 프로비저닝이 실패했거나 옛 사고를 겪은 회사는 **`tenant_admin` 이 0줄**이고,
그러면 **관문 안 승인 구역이 아무에게도 안 뜬다.**

#### 🔴 이번 작업에서 **탐지만** 한다 — 고치지 않는다

| 하는 것 | 안 하는 것 |
|---|---|
| 🟢 `tenant_admin` 이 0줄이면 **로그에 경고 한 줄** | 🚫 계정을 **자동 승격**시키기 |
| 🟢 관문 화면에 **"강제 경로" 안내** 노출 | 🚫 관문을 **자동으로 열어주기** |

🔴 **왜 자동 승격을 안 하나**: 누구를 대표로 올릴지 **시스템이 정하면 안 된다**
(헌법 #11 — 권한은 어드민이 직접 · *"100% 자동화는 없다"*).
🔴 **왜 관문을 자동으로 안 여나**: 그것은 **기기 통제를 통째로 끄는 것**이다.

⇒ 그 회사의 탈출구는 **강제 경로**(DB 한 줄 + `Restart-Service HitPanWatchdog`)다.
그것은 **이미 있고 8/18 에 실측으로 확인됐다**(1.2.87 `force-update.ps1`).
🔴 **관문 화면이 그 안내를 보여준다** — 갇힌 사람이 **다음에 뭘 할지 알게** 하는 것이 이번 몫이다.
## 5. 🔴 검증 설계 — **"고쳤나" 가 아니라 "갔나"**

> `feedback_gate_must_test_behavior` (가짜게이트 누적 9번) · `project_fixed_vs_delivered_gap`

### 5-1. 🚫 이번에 **쓰지 않을** 게이트

| 금지 | 왜 |
|---|---|
| 문자열이 소스에 있나 검사 | 8번째 가짜게이트가 정확히 이것이었다 (`/api/appversion`) |
| 하드코딩 주소를 하드코딩 주소와 대조 | **둘 다 틀려도 통과**한다 |
| 함수 반환값으로 판정 | 그 함수가 판정 주체다 — 자기가 자기를 시험한다 |

### 🔴 5-1-1. **로컬(loopback)로 시험하는 것** — 이번의 가장 큰 함정

> **사장님 (8/19)**: *"로컬로 두고 테스트를 하면 당연히 안되지."*

🔴 **이것은 "틀린 답이 나온다" 가 아니다. 그보다 나쁘다.**

로컬에서는 `IsLocalConsole` 이 **참**이다 ⇒ **8/18 봉합(`TenantDeviceService.cs:269`)이 이미 돈다.**
⇒ 대표는 로컬에서 **원래부터 안 갇힌다.**

| 무엇 | 로컬에서 | 터널에서 |
|---|---|---|
| D-1 **있을 때** | ✅ 통과 | ✅ 통과 |
| D-1 **없을 때** | ✅ **통과** ← 🔴 여기 | ❌ 갇힘 |

🔴 **로컬 게이트는 D-1 이 있으나 없으나 초록불이다.**
**시험 대상을 시험하지 않는다** — 옆에 있는 옛 봉합이 초록불을 만들어 준다.

> 🔴 **그리고 그 초록불은 "통과했다" 고 보고된다.**
> 어제 게이트 5/5·반증 3회가 정확히 이 모양이었다. **초록불의 출처를 안 물었다.**

⇒ **모든 게이트에 터널 헤더를 붙인다.** 로컬 게이트는 **한 건도 만들지 않는다.**
붙이지 않은 게이트는 통과해도 **판정 근거로 쓰지 않는다.**

### 5-2. ✅ 이번에 쓸 게이트 — **대조실험** (3차 · [3-V] 반영)

> 🔴 **전제 ①**: 전부 `CF-Connecting-IP` 를 붙여 돌린다 (= 사장님 경로).
> 🔴 **전제 ②**: **게이트를 화면 축에 건다.** 서버 축에 걸면 아래 §5-2-1 대로 **또 가짜가 된다.**

#### 🔴 5-2-1. [3-V] 가 잡은 것 — **2차 게이트는 가짜가 될 뻔했다**

2차 설계의 `G-E`(D-1 빼면 FAIL)를 **서버에 돌리면**, D-1 을 빼도
`DeviceAuthMiddleware.cs:225-231` 옛 대표예외와 `:41` Bypass 가 남아 **여전히 PASS** 한다.

> 🔴 **§5-1-1 이 경고한 "옆의 옛 봉합이 만든 초록불" 을 게이트 스스로 다시 만든다.**
> 로컬 함정을 막아 놓고 **같은 함정을 다른 자리에** 놓을 뻔했다 — **가짜게이트 10번째.**

⇒ 🔴 **게이트가 겨눌 자리는 `DeviceAuthGate` 가 승인 구역을 그리는가**이지,
**서버가 200 을 주는가가 아니다.** 서버는 봉합 전에도 200 을 준다.

| G | 무엇 | 통과 조건 |
|---|---|---|
| G-A | 터널 + **대표** + `pending` | ✅ **관문 안에 승인 구역이 뜬다** ← P0 의 답 |
| G-B | 터널 + **직원** + `pending` | ❌ 승인 구역 **안 뜬다** (관문 안내만) |
| G-C | 터널 + 대표 + `pending` | ❌ 🔴 **`_gateActive` 는 여전히 참** = 업무 화면 안 그려짐 |
| G-D | 터널 + 대표 + 기기 `approved` | ❌ 관문 자체가 안 뜬다(종전 동작 무변경) |
| G-E | 🔴 **봉합 빼고 G-A** | 🔴 **FAIL 이 나야 한다 — 가장 먼저 확인** |
| G-F | 🔴 로컬(헤더 없음) + 봉합 뺀 상태 | ⚠️ **통과할 것이고 그것이 정상** — 초록불 출처가 접속경로임을 증명 |
| **G-G** | 🔴 **`account_type` 을 화면에서 위조** + 승인 시도 | 🔴 **서버가 거부해야 한다** (화면 값은 표시용) |
| **G-H** | 🔴 **`tenant_admin` 0줄** 상태 | ⚠️ **승인 구역 안 뜨고 강제경로 안내가 뜬다** (§4-2) |

🔴 **G-C 가 이번 설계의 수문장이다.** 본안 E 는 `_gateActive` 를 **안 건드리는 것**이 핵심인데,
G-C 가 없으면 *"관문을 통째로 열어버린 것"* 과 구별이 안 된다. **G-A 와 한 묶음으로 돌린다.**

🔴 **G-G 는 [3-V] 경8 대응**이다. 화면이 판단하게 되는 순간을 시험한다.
🔴 **G-H 는 [3-V] 중5 대응**이다. 대표가 0줄인 회사가 **조용히 갇히지 않는지** 본다.

### 5-3. 🔴 실측 — 게이트로는 끝나지 않는다

> *"개발PC 정상은 검증이 아니다"* · *"빌드 0/0 · 게이트 5/5 였는데 실사용을 못 덮었다"*

| # | 확인 | 어디서 |
|---|---|---|
| 실1 | `https://test1.hitpan.kr` 에서 **대표로 로그인 → 관문 안에 승인 구역이 뜬다** | 🔴 사장님 실측 |
| 실2 | **거기서 바로 자기 기기를 승인 → 관문이 스스로 사라지고 업무 화면** | 🔴 사장님 실측 |
| 실3 | **휴대폰**에서 직원계정 로그인 → **여전히 승인 대기** | 🔴 사장님 실측 |

⚠️ **실1·3 은 한 묶음이다.** 실1만 보면 **관문을 통째로 열어버린 것**을 못 잡는다.

#### 🔴 실측에도 §5-1-1 이 그대로 적용된다

**실측 주소는 반드시 `https://test1.hitpan.kr` 다.**

🚫 **`localhost:5257` 로 여시면 안 된다** — 거기서는 옛 봉합이 이미 돌아서
**봉합이 없어도 통과한다.** 통과해도 **아무것도 확인한 것이 아니다.**

> 🔴 **PM 이 실측을 요청할 때 주소를 반드시 함께 적는다.**
> 어제 *"관문 켜서 실측하십시오"* 라고만 하고 **어디서 여실지를 안 적었다.**

### 5-4. ⚠️ 실측 전 안전장치 — **또 갇히시면 안 된다**

어제 사장님을 6회 갇히게 했다. 그중 1회는 **봉합 없는 버전에서 관문을 켜시게** 한 것이다.

🔴 **실측 순서를 고정한다:**
1. 봉합이 **실린 버전이 게시된 것**을 PM 이 먼저 확인 (버전 번호로)
2. 그 버전이 TEST1 에 **올라간 것**을 확인
3. **그 다음에** 사장님께 실측 요청
4. 🔴 **탈출구를 먼저 확인** — `{app}\scripts\force-update.ps1` 이 있는지
5. 🚫 **고객 PC 파일을 고치라는 명령을 만들지 않는다** — DB 한 줄 + 서비스 재시작으로 끝낸다

---

## 6. 🔴 이 설계가 건드리지 않는 것

> 사장님 지시 — *"기존의 실측 성공한 건 단 하나라도 건들면 안 돼"*

| 무변경 | 근거 |
|---|---|
| 설치마법사 `.iss` | 8/10 부터 지켜 온 선 |
| 워치독 · 업데이트 경로 | 어제 실측으로 겨우 살린 자리 (1.2.87) |
| 슬롯 계수 · 요금 판정 | 돈이 걸린 자리 — **줄을 만들지 않는다** |
| `appsettings.json` | 헌법 #21 |
| 브라우저 지문 산식 | 바꾸면 **기존 등록 기기가 전부 재인식**된다 |
| 🔴 `IsLocalConsole` 세 자리 | **그대로 둔다**(§4-1) — 걷어내면 `localhost` 경우가 죽는다 |
| 🔴 `DeviceAuthMiddleware` | **무변경** — 대표 예외가 **이미 있다**([3-V] 치명1) |
| 🔴 `MainLayout.razor` `@Body` 조건 | **무변경** — 건드리면 업무 화면이 샌다([3-V] 치명3) |
| 🔴 `MainPcRegistrationService` | 무변경 — 서버 줄은 지금대로 심는다 |

🔴 **줄(row)을 새로 만들지 않는다.** 관문 화면 하나의 조건만 바뀐다.

---
## 7. 🔴 결재 요청

### 7-1. 🔴 설계 방향 — 이것부터

> **"메인PC 를 알아보는 것" 을 포기하고, "대표가 안 갇히게" 로 목표를 바꾼다.**

터널만 쓰시는 환경에서 서버가 스스로 그 컴퓨터를 알아낼 방법이 **없다**(§3-4).
없는 것을 있는 척 짜면 어제와 같은 일이 반복된다.

⇒ 🔴 **사장님 결재 필요** — 자동 판정을 **포기**하는 결정이다.

### 🟢 7-1-1. 3차에서 달라진 것 — **더 작아졌다**

[3-V] 반증 뒤 본안이 **E** 로 바뀌면서 손대는 자리가 줄었다.

| | 2차 (D-1) | 🟢 3차 (E) |
|---|---|---|
| 서버 미들웨어 | 대표 예외 **추가** | 🟢 **무변경** (이미 있다) |
| `MainLayout.razor` | `@Body` 렌더 조건 **변경** | 🟢 **무변경** |
| 라우팅 · 주소 대조 | 필요 | 🟢 **없음** |
| 새 API · DDL | 없음 | 🟢 **없음** |
| 실제 변경 | — | **관문 컴포넌트에 구역 하나 + `gate-status` 응답에 값 하나** |

🔴 **관문(`_gateActive`)을 안 건드리는 것이 본안 E 의 핵심이다.** 그래서 G-C 가 수문장이다.

### 7-2. 나머지

| # | 무엇 | 누가 |
|---|---|---|
| ① | **본안 E** — 관문 안에서 승인을 끝내는 것이 **관문의 뜻을 해치지 않는가** | 🔴 **보안 상무 안철수** (2차 반증자) |
| ② | §4 넷째 줄(업데이트 상태 축)을 **이번에 같이 볼 것인가** | CTO 래리 |
| ③ | **D-2 = 자료관리** — *"부모계정 + 메인PC 환경에서만"* 지시의 변경. 🔴 **[3-V] 중6(표식 탈취)이 이 건의 선행 조건으로 이월됨**(§4-1) | 🔴 **사장님** |

### ✅ 해소된 미확인 (8/19 사장님 답변)

- ~~메인PC 를 터널로만 쓰시는지~~ → **"테스트1으로 하고 있어"** = **터널만.**
  ⇒ 이 답변이 **1차 설계안을 폐기**시켰다(§3-3). 🔴 **묻지 않고 짰으면 또 헛다리였다.**

### ⚠️ 남은 미확인 — 추측하지 않고 적어 둔다

| # | 무엇 | 왜 지금 못 정하나 |
|---|---|---|
| ㉮ | TEST1 의 대표계정이 `account_type='tenant_admin'` **한 줄로 정상**인가 | 🔴 **운영 DB 를 안 봤다**(헌법 #39 — 운영은 읽기만). §4-2 G-H 가 이것을 전제하지 않게 짰다 |
| ㉯ | 옛 사고를 겪은 회사가 **실제로 있는지** | 사고 이력은 코드 주석(`TenantsController.cs:21`)에 있으나 **몇 곳인지는 백오피스 조회가 필요** |

🔴 **㉮㉯ 를 모른 채로도 본안 E 는 짤 수 있다** — 0줄이면 **강제경로 안내가 뜨게**(§4-2) 해 뒀다.
⚠️ 다만 **"대표계정은 항상 있다" 고 가정한 코드를 짜지 않는다.**

---

## 8. 다음 단계

```
§7-1 설계 방향 결재  ← 지금 여기 (자동 판정 포기 결정 · 본안 E)
   ↓
작업지시서 20260819작1 **개정** (반려 상태 → 본안 E 로 재작성)
   ↓
PM 결재 → 코딩 (§5-2 · 🔴 G-E 먼저 · 🔴 화면 축에 건다)
   ↓
[3-V] 2회차 병렬검증 (본안 E 반증) → 검증팀장 → CTO → 사장님
   ↓
게시 → 🔴 사장님 실측 (§5-3 · 순서는 §5-4)
```

🔴 **1차는 추측으로 무너졌고, 2차는 반증으로 무너졌다. 둘 다 코드 짜기 전이었다.**
