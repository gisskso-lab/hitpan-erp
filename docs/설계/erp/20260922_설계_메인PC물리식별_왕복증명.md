# 설계 — 메인PC 물리식별 「왕복 증명」

> **오더**: 사장님 2026-09-22
> *"메인PC식별은 A안으로, 물리적인 방법으로 메인을 잡도록 하고"*
> *"설치파일 안에 메인PC인증키가 있다면?? 은행인증서 처럼"*
> *"백엔드에 인증키가 들어가 있으니, 실제 사용하는 고객은 인증키가 도는지 안도는지 모르게"*
> *"**기기가 직접 인증키를 인식하고 메인PC로 알아보는게 정확함**"*
>
> **근거**: [1-V] [선행검증서](../../검증/선행/20260922_선행검증서_메인PC물리식별_기기슬롯재설계.md) — 전제 2건 반증 · 결함 1건 · A안 성립 실측
> **작성**: 설계팀장 존 · **결재**: PM → CTO → 사장님
> **범위**: 🔴 **메인PC 물리식별만.** 계정 과금·계정관리는 이 문서에 없다(§11)

---

## 1. 이 설계가 푸는 것

| # | 증상 | 이 설계가 닫는가 |
|---|---|---|
| ② | 메인PC 인식 불안정 (= 도메인으로 열면 항상 실패) | 🟢 닫는다 |
| ③ | 자료관리 메뉴가 안 됨 | 🟢 ②의 결과 — 같이 닫힌다 |
| 결함 | 외부 PC 가 메인PC 를 자칭하면 통과 | 🟢 닫는다 |
| ① | 기기가 2~3줄로 늘어남 | ⬜ **이 설계 밖** — 슬롯 과금 폐기로 증상이 아니게 된다 |

---

## 2. 왜 지금 방식이 안 되는가 (요약 · 상세는 [1-V])

```csharp
// MainPcOnlyAttribute.cs:38 — 현행
return !viaTunnel && isLoopback;        // 터널 헤더가 있으면 무조건 false
```

고객은 **도메인으로 접속한다.** 도메인 접속은 Cloudflare 가 `CF-Connecting-IP` 를 반드시 붙인다.
⇒ **첫 조건에서 항상 탈락한다.**

그리고 IP 로는 영영 못 가린다 — cloudflared 가 **그 PC 안에서** 히트판을 다시 부르므로
서버가 보는 소켓 주소는 **외부 접속이든 메인PC든 항상 `127.0.0.1`** 이다.
같은 사무실이면 공인 IP 도 같다(실측 `124.194.59.154`).

> **그러므로 판정의 축을 「어디서 왔나(IP)」에서 「무엇을 증명할 수 있나」로 옮긴다.**

---

## 3. 설계 — 「왕복 증명」

### 3-1. 한 줄 요약

> **브라우저가 자기 PC 안의 히트판을 두드려 증표를 받아 온다.**
> 받아 오면 메인PC, 못 받아 오면 클라이언트. **고객은 아무것도 하지 않는다.**

### 3-2. 무엇이 증거인가

메인PC 와 클라이언트 PC 의 차이는 **터널을 지나와도 사라지지 않는다**:

| | 메인PC | 클라이언트 PC |
|---|---|---|
| 그 PC 안에 히트판 본체 | 🟢 있다 | 🔴 없다 |
| 그 PC 안에 `hitpan-keys.conf` | 🟢 있다 | 🔴 없다 |
| `http://127.0.0.1:5257` 를 두드리면 | 🟢 **자기 히트판이 답한다** | 🔴 **아무도 없다** |

⚠️ API 는 **루프백 전용 바인딩**이다(실측: `LocalAddress = 127.0.0.1`).
즉 클라이언트 PC 가 이 주소를 두드리면 **자기 PC 를 두드리는 것**이고, 거기엔 히트판이 없다.
**남의 메인PC 를 두드릴 방법이 없다.** 이것이 이 설계의 물리적 근거다.

### 3-3. 흐름 (3왕복)

```
 브라우저(도메인)                 서버 = 히트판 본체 (같은 프로세스)
      |
 (1)  |-- POST /api/devices/mainpc-challenge --------->  1회용 표 N 발급
      |   (터널 경유)                                     · 60초 유효 · 1회 사용
      |<-------------- { challenge: N } ---------------|  · 세션(JWT jti)에 묶는다
      |
 (2)  |-- POST http://127.0.0.1:5257/api/devices/mainpc-proof  { N }
      |   🔴 터널을 안 지난다 — 브라우저가 자기 PC 로 직접 보낸다
      |   ⚠️ 서버는 이 요청을 「CF헤더 없음 + 루프백」으로 받는다 = 진짜 로컬
      |<------ { proof: HMAC(설치키, N + tenantId + 만료) } ------|
      |
 (3)  |-- POST /api/devices/mainpc-verify { N, proof } ->  자기 키로 재계산해 대조
      |   (터널 경유)                                     · N 미사용·미만료·세션 일치
      |<------------ { isMainPc: true } ---------------|  · 통과 → 세션에 메인PC 표식
```

**클라이언트 PC 는 (2)에서 끝난다.** 두드릴 히트판이 없어 연결 자체가 실패한다.
⇒ 증표를 못 얻고, (3)을 못 한다. **거짓말할 여지가 없다.**

### 3-4. 왜 (2)가 「물리적」인가

(2)는 **네트워크를 타고 나가지 않는다.** 브라우저가 자기 컴퓨터 안에서 자기 컴퓨터에게 묻는다.
이 요청은 Cloudflare 를 거치지 않으므로 서버가 **CF헤더 없음 + 루프백**으로 받는다 —
현행 판정이 유일하게 믿을 수 있다고 본 바로 그 조건이다.

즉 **현행 판정을 버리는 게 아니라, 그 판정이 성립하는 통로를 따로 열어 주는 것**이다.

---

## 4. 왜 이 형태인가 — 기각한 안들

| 안 | 기각 사유 |
|---|---|
| **IP·헤더로 판정** (현행) | 🔴 터널이 소켓 주소를 지운다. 같은 사무실이면 공인 IP 도 같다 → **원리적으로 불가** |
| **장치 이름으로 판정** | 🔴 브라우저가 장치 이름을 서버에 못 보낸다(보안상 차단). 그리고 *"이 PC의 이름 바꾸기"* 로 바뀐다 |
| **브라우저에 증표를 저장** | 🟡 되지만 **브라우저를 바꾸면 깨진다.** 사장님 요구(*"어떤 브라우져로 접속을 해도"*)를 못 지킨다 |
| **클라이언트 PC 에 프로그램 설치** (A안 2번) | 🔴 *"온전한 웹 SaaS 로 보이게"* 와 충돌. ActiveX 는 실행할 브라우저가 이미 없다 |
| **사람이 [이 컴퓨터가 메인] 선택** (현행 우회로) | 🔴 **누구나 자칭 가능.** 사장님 판정: 반자동 원칙의 오용 |
| **🟢 왕복 증명** (본 설계) | 브라우저 무관 · 설치 0 · 고객 조작 0 · 자칭 불가 |

---

## 5. 보안 — 공격 시나리오와 방어

| # | 공격 | 방어 |
|---|---|---|
| A-1 | 외부 PC 가 `isMainPc=true` 를 본문에 실어 보냄 | 🟢 서버가 **본문을 안 읽는다.** 증표만 본다 |
| A-2 | 메인PC 에서 받은 증표를 복사해 외부에서 씀 | 🟢 표 N 은 **1회용 · 60초 · 그 세션(jti)에만** 유효 |
| A-3 | 표 N 을 미리 잔뜩 받아 둠 | 🟢 증표 만료가 N 발급 시각에 묶인다. 세션이 바뀌면 무효 |
| A-4 | 같은 PC 에 가짜 서버를 5257 로 띄워 증표를 위조 | 🟢 **HMAC 을 위조하려면 `hitpan-keys.conf` 의 설치키가 있어야 한다.** 그 파일은 메인PC 에만 있다 |
| A-5 | 다른 회사 히트판이 그 PC 에 깔려 있음 | 🟢 증표에 `tenantId` 를 섞는다. 남의 회사 키로는 우리 서버 검증을 못 통과 |
| A-6 | 대표 계정 비번 탈취 후 외부 접속 | 🟢 **자료관리가 안 열린다** — (2)를 못 하므로 메인PC 가 못 된다 (**현행은 열린다**) |
| A-7 | 브라우저 확장·악성 스크립트가 로컬 통로를 두드림 | 🟡 CSP 를 **그 주소 하나로만** 연다(§6-2). 그 PC 에 이미 악성코드가 있으면 이 설계 밖의 문제 |

### ⚠️ 이 설계가 막지 못하는 것 (정직하게 적는다)

- **메인PC 그 컴퓨터 앞에 앉은 사람**은 당연히 메인PC 로 인정된다. 그건 설계 의도다
- 메인PC 에 원격데스크톱으로 들어온 사람도 메인PC 로 보인다 — **원격 접속 통제는 이 설계 밖**
- `hitpan-keys.conf` 를 통째로 훔쳐 다른 PC 에 심으면 그 PC 가 메인PC 가 된다
  ⇒ 🔴 **이 파일의 접근 권한은 별도 확인이 필요하다** (§10 D-3)

---

## 6. 바꾸는 자리

### 6-1. 새로 만드는 것

| 파일 | 무엇 |
|---|---|
| `src/HitPan.API/Controllers/DeviceController.cs` | `mainpc-challenge` · `mainpc-proof` · `mainpc-verify` 3개 추가 |
| `src/HitPan.Infrastructure/Services/MainPcProofService.cs` (신규) | 표 발급·보관(메모리)·증표 생성·검증 |
| `src/HitPan.Web/wwwroot/js/hitpan-mainpc-proof.js` (신규) | 브라우저가 (2)를 두드리는 한 줄 |
| `installer/` 설치 스크립트 | `hitpan-keys.conf` 에 `MAINPC_PROOF_KEY` 한 줄 추가 |

⚠️ 표는 **메모리에 둔다.** 서버와 로컬이 같은 프로세스이므로 DB 가 필요 없다.
API 가 재시작되면 표가 사라지지만 **다시 왕복하면 그만**이다(고객은 모른다).

### 6-2. 고치는 것

| 파일 | 무엇 | 어떻게 |
|---|---|---|
| `MainPcOnlyAttribute.cs:38` | 판정에 **한 갈래 추가** | `기존 판정 OR 왕복증명 통과` — 🔴 기존 줄은 **지우지 않는다**(#1) |
| `DeviceController.IsMainPc:101` | 같은 규칙 | ⚠️ 두 곳이 **같은 규칙을 봐야 한다.** 한쪽만 고치면 *"화면은 열리는데 저장이 안 된다"* |
| `Program.cs:548` CSP | `connect-src` 에 로컬 주소 한 개 추가 | 🔴 **`http://127.0.0.1:5257` 만.** `http:` 전체를 열지 않는다 |
| `DeviceAuthGate.razor:618` | 사람이 메인PC 를 자칭하는 경로 | 🔴 **끈다. 지우지 않는다**(#1·#37) — 버튼·호출을 막고 코드는 남긴다 |

### 6-3. 🚫 손대지 않는 것

- **워치독·업데이트 흐름** — Major 팝업 Y/N 불변 (#43)
- `fingerprint` 컬럼·`uq_tenant_fp` — 옛 기기 전부가 이 값으로만 찾힌다 (#37)
- `hardware_id` 컬럼 — 이 설계는 이 칸을 **쓰지 않는다**. 기기 식별(증상 ①)은 별건
- 슬롯 계수 SQL — 과금 폐기는 별도 작업지시서
- `appsettings.json` · `appsettings.Development.json` (#21)

---

## 7. 고객이 보는 것

**아무것도 안 보인다.** 로그인하면 화면 뒤에서 (1)(2)(3)이 돌고 끝난다.

| 상황 | 고객 경험 |
|---|---|
| 메인PC 에서 도메인 접속 | 그냥 된다. 자료관리 메뉴가 열려 있다 |
| 클라이언트 PC 에서 접속 | 그냥 된다. 자료관리 메뉴만 안 보인다 |
| 브라우저를 바꿈 | **아무 일도 안 일어난다.** 매번 새로 증명하므로 저장된 게 없다 |
| 히트판을 껐다 켬 | 아무 일도 안 일어난다 |

🔴 *"로컬호스트로 접속하시면 메인 인식됩니다"* 라는 안내는 **사라진다.**

---

## 8. 게이트 (검증 방법 — [4] 가 쓴다)

| # | 무엇을 재나 | 통과 조건 |
|---|---|---|
| G-1 | 메인PC + 도메인 접속 | `isMainPc = true` · 자료관리 **열린다** |
| G-2 | 클라이언트 PC + 도메인 접속 | `isMainPc = false` · 자료관리 **안 열린다** |
| G-3 | **봉합을 빼면 FAIL 하는가** (대조군) | 왕복증명 코드를 끄면 G-1 이 **실패해야** 한다 |
| G-4 | 증표 재사용 | 같은 표를 두 번 쓰면 **거부** |
| G-5 | 증표 만료 | 61초 뒤 제출하면 **거부** |
| G-6 | 다른 세션의 증표 | 다른 계정 세션에서 제출하면 **거부** |
| G-7 | 자칭 차단 | 본문에 `isMainPc=true` 를 실어 보내면 **무시** |
| G-8 | 브라우저 교체 | Edge→Chrome 바꿔도 G-1 이 **그대로 통과** |
| G-9 | 기존 경로 무회귀 | `localhost` 직접 접속도 **여전히 통과** |
| G-10 | CSP 게이트 | `CspFrameSrcGateTests` 가 **깨지지 않는다** |

🚨 **G-3 이 없으면 게이트가 아니다.** 봉합을 빼고 실제로 FAIL 하는지 확인한다(누적 25회 지적).
🚨 **실측은 반드시 실제 브라우저 창으로.** 헤드리스는 결과가 정반대로 나온다([1-V] §4-3).

---

## 9. 순서 (P 단계)

```
P1  MainPcProofService + API 3개          ← 서버만. 화면 무변경. 여기까지는 아무 영향 없다
P2  CSP 한 줄 + 브라우저 JS               ← 왕복이 실제로 도는지 실측 (G-1·G-2)
P3  MainPcOnly 두 곳에 갈래 추가          ← 자료관리가 열린다
P4  DeviceAuthGate 자칭 경로 차단         ← 결함 봉합 (G-7)
P5  설치본에 MAINPC_PROOF_KEY 한 줄       ← 신규 설치 · 기존 고객 처리
```

⚠️ **P5 의 기존 고객 처리가 쟁점이다** — 이미 깔린 고객은 `MAINPC_PROOF_KEY` 가 없다.
없으면 **서버가 기동 시 만들어 파일에 보탠다**(없을 때만 · 있으면 손대지 않는다).

---

## 10. 🔴 결재가 필요한 것

| # | 쟁점 | 왜 PM 이 못 정하나 |
|---|---|---|
| D-1 | 왕복 증명을 **언제** 도나 — 로그인 직후 1회 / 자료관리 열 때마다 | 매번이면 확실하고 1회면 가볍다. **고객 체감과 보안의 맞바꿈** |
| D-2 | 증표 유효시간 60초가 적당한가 | 짧으면 느린 PC 에서 실패, 길면 창이 넓어진다 |
| D-3 | `hitpan-keys.conf` 접근 권한을 이번에 볼 것인가 | 이 파일을 훔치면 메인PC 가 복제된다(§5). **보안 상무 영역 · 범위 확장이라 먼저 여쭌다** |
| D-4 | 기존 고객 키 생성 시점 — 서버 기동 시 자동 / 업데이트 때 | #43 업데이트 흐름에 닿는다. **워치독 로직은 불변이어야 한다** |
| D-5 | 왕복이 실패했을 때 — 조용히 클라이언트 취급 / 한 번 재시도 | 네트워크 순간 장애로 메인PC 가 클라이언트가 되면 자료관리가 닫힌다 |

---

## 11. 이 설계의 범위 밖 (별도 작업지시서)

- **계정 과금 전환** — 5계정 3만 / 10계정 5만 / 추가 1만, 등록 계정 수 기준
- **계정관리 기능 추가** — 사장님 오더. 직원 ≠ 계정
- **기기 등록 절차 제거 · 조회만 남기기** — 사장님 오더
- **백오피스 자식계정 카운트** — 개수만 올린다(#18·#22)
- **기기 2~3줄(증상 ①)** — 슬롯 과금이 없어지면 아프지 않다. 정리는 별건
- **`hardware_id` 배선** — 위와 같은 이유로 급하지 않다

🔴 사장님 결재: **범위 (ㄱ) ERP 먼저 · 그 안에서 메인PC A안이 선행.**
