# INDEX — docs/설계/erp

> ERP 설계·PRD·ERD·아키텍처
> 문서 41개 | 자동생성 2026-07-28 | 헌법 #41
> **담당**: 설계팀장 존(=풀스택 매니저) · PM · CTO 래리  ([폴더담당 표](../../POLICY_폴더담당.md))

> 최근 12건만 표시. 전체 41건 = [INDEX_ALL.md](INDEX_ALL.md)

| 문서 | 제목 | 최종수정 |
|---|---|---|
| **[20260822_세부설계_G1_결재선2중화_배선.md](20260822_세부설계_G1_결재선2중화_배선.md)** 🆕🟢🔧 | 🔧 **파이프라인 [3] 세부설계 — G1(토대) · v2**. 🔴🔴**사장님이 PM 안을 두 번 물리셨고 둘 다 사장님이 맞다**: ①Q5 *"권한설정 한 자"*(PM 의 *"직급 높은 순→입사일 순"* 은 결국 **시스템이 사람을 고르는 것**이라 #11 위반 소지 · 부장 2명이면 회사마다 답이 다르다) ②**축 정정** — *"결재라인 pk를 직책이 아니라 **사원이름이나 계정으로**"* ⇒ **최종 확정 = *"대표이사 마지막 결재 외는 권한가진자가 하는걸로"***. 🟢**이 한 줄로 PM v1 이 통째로 폐기**: position_id DDL 추가 · 이름→ID 마이그 · 직책→사람 펼치는 로직 · 권한자 0명/여럿 분기 **전부 불필요**(PM 이 놓친 것 = pproval_doc_lines 가 **이미 pprover_id(사원)로 돌고 있었다** · 사장님 원칙 ②(있는 걸 쓴다) 위반 · *"직책이면 사람 바뀌어도 안 죽는다"* 는 곧 **자동 승계**라 **반자동 원칙 위반**). 상무 지지: *"히트판 고객사는 **발령 시스템이 없다**. 직책으로 걸어도 결국 손으로 넣어야 하니 **한 번 더 돌아가는 것뿐**"*. 🟢🟢**직책관리 메뉴 불필요 확정**(사장님) — 필요했던 유일한 이유가 결재선이었고, 직책 등재는 **EnsurePositionExistsAsync(:275)가 이미 자동**(쓰레기 값도 거른다 · 주석 실측 *"12명 중 8명 직급없음·그중 '0' 1건"*) ⇒ **메뉴만 뺀다, 표는 살린다**(CompanyBootstrapProvisioner 시드 등 3곳이 쓴다 · #37). 🟢**실측**: APPROVAL 권한 **이미 있고 EnforcedMenus 등록됨**(신규 0 · 1.2.74 위험 없음) · 대표는 **is_parent=1** 로 가려짐(T-004 실측 — **position 이 
ull 이어도 대표**라 position 판정 금지). 🔴**G1 내용이 배선에서 가드로 바뀜**: ①최종=대표 보장 ②**권한 없는 사람을 결재자로 못 넣게**(넣으면 그 사람은 결재함 진입 자체가 막혀 **문서가 영영 안 간다**) ③권한이 나중에 꺼지면 **오늘 확정한 공석 알림과 같은 틀**로 알림. 🟢**DDL 0 · 표신설 0 · 엔진 무변경**. 게이트 6종(🔴G1-5 = position 으로 대표 판정하면 FAIL) | 2026-08-22 |
| **[20260822_설계_그룹웨어_정본_메뉴통합_결재조회일원화.md](20260822_설계_그룹웨어_정본_메뉴통합_결재조회일원화.md)** 🆕🟢📌 | 🟢📌 **그룹웨어 정본 — 이 문서 하나를 본다**(설1~설4 흡수). 🔴🔴**축 = 메뉴 통합 17→10 + 결재조회 일원화**. **PM 오판 정정 기록**: PM 이 *"가장 중요한 축은 결재선 2중화"* 라 보고했으나 사장님이 *"메뉴구성이자너 · 메뉴통합"* 으로 물리심 ⇒ 결재선은 **고객이 모르는 내부 배선(수단)** 이고 메뉴는 **고객이 보는 것(목적)**. 오늘 논의 전부가 *"메뉴를 합치자"* 에서 **파생된 질문**이었다(상무님은 이미 *"나는 순서를 얘기했는데 사장님은 축을 바꾸셨다 — 급이 다른 얘기"* 로 알아들었는데 PM 이 못 알아들음). 🔴**사장님 3원칙이 잣대**(①라인 하나·필터로 결재·창구만 구분·메뉴최소 ②**컬럼·기능 안 없어짐, 메뉴에 녹아들어감** ③**새 화면 금지, 이미 있는 화면으로 링크**) ⇒ **빠지는 화면 0개**. 🔴**실측이 집계를 두 번 정정 — 현재 17개**(*"15"*·*"16"* 둘 다 틀림: 경비·연차부여·전자근로계약서 누락). 🔴**1번 결재조회 = 축의 절반**: API 필터 **파라미터 0개**(`:106`·`:119`·`:132`)라 화면·API 양쪽 신규 · **[반려] 필터 필요**(실측 — `GetCompleted` 가 `approval_history` 매칭이라 **반려가 완료에 섞인다**) · ⚠️`DocTypeLabels` 에서 뽑지 마라(작9 전 ERP 7종 생존). 🔴🔴**상태반영이 유일한 치명상** — *"목록조회→문서생성→결재→**목록에 상태반영**"* 의 마지막 화살표가 **B1 경비 P0 그 자체**(이미 출하됐던 것) ⇒ 🟢현재 8종 전부 원본반영 있음 · **새로 넣는 2종도 반드시**. 🔧**수단(목적 아님)**: **결재선 2중화**(엔진이 `approval_line_steps` **참조 0건** ⇒ 직책으로 짜도 사람 고정 1행으로 돎 · 배선은 **엔진무변경·표신설 0**) · **직권**(반자동·공석3사유 휴가/퇴사/휴직·`is_delegated` 재사용으로 **DDL 0건**·`APPROVAL_OVERRIDE` 기본OFF·**[Y]=`/settings/approval` 링크이동**(그 자리 결재 아님 — 한 건만 넘기면 다음도 막힘)·**[N]=상태유지**·알림은 **기존 자리**(`Dashboard.razor:216`·`ChatPage.razor:166`)) · **전결**(`approval_final_rules` · **금액·문서종류 다 고객사 설정** · 🟢`approval_documents.amount` 이미 있어 **표 신설 1개뿐**) · **대결 🟢이미 완비 — 손대지 마라** · **퇴직서**(🟢퇴사 *처리* 는 멀쩡 — PM 의 *"아무도 안 채운다"* 는 **옛 주석 오독** · 없는 건 **직원이 올리는 문서** · 🔴**대표 사직서 차단** #38·#40) · **계약서 결재연결**(화면·설계 이미 있고 **전용 서비스만 없음** — 읽는 곳이 `ChatService` 뿐). 🟡**회계 이월 4건**(급여명세서·경비·초과근무수당·연봉계약서) — **축만**. 🟢**구멍 전수조사: 표 20개·결재8종·트리거·알림·연차 전부 훑어 논의한 3건 외 새 구멍 없음**(⚠️메신저 미조사 · 코드훑기지 실측 아님). **경계**: 근로계약서=그룹웨어 / 거래처계약서=**ERP** / 연봉계약서=**회계** | 2026-08-22 |
| [20260824_세부설계_결재관리_필터일원화.md](20260824_세부설계_결재관리_필터일원화.md) 🆕🟢 | **② 결재관리 필터 세부설계** — 사장님: *"결재관리 메뉴에 **필터링1(결재대기, 결재완료, 반려), 필터링2(각 결재들)** 만들어"*. 🔴**착수 전 6건 결재** — 필터1 **5개**(대기·완료·**반려**·내가보낸·모두) · 필터2 **11줄**(보고서 4종 **펼침**) · 사이드바 3줄 → **「결재관리」 1줄** · 초과근무 *"없는건 만들어야지"*. 🔴🔴**PM 권고가 두 번 물러났다** — ①보고서 *"묶기"* 권고 → 사장님 **펼치기**(묶으면 일일/주간 구분이 사라져 필터의 목적이 없어진다) ②「모든결재」 PM 이 낸 **선택지 둘 다 아님** → 사장님 ***"보낸이 · 결재자 · 대표이사"*** 세 자리. 🟢**상무 8/22 지적 2건이 여기서 풀린다** — *"반려함이 비어있다"*(`GetCompleted` 가 `ad.status` 를 안 봐 **반려가 완료에 섞임** — 실측 확인) · *"내린 주소는 리다이렉트로 살려라"*(즐겨찾기 링크). 🔴**대표 판정 = `users.is_parent`**(G1 이 쓴 그 축) — `account_type` 은 **로그인 축**이라 쓰면 안 된다(6/21 A-P0-1 자리) · **갈래를 C# 에서 눈에 보이게 가른다**(SQL 에 섞으면 조건 하나 빠뜨렸을 때 전 직원에게 회사 전체가 샌다). 🔴**DDL 변경 0** — `idx_status` 이미 있음 · 기존 API 3개·라벨 사전 **무접촉**(#1) | 2026-08-24 |
| [20260822_설계_결재_직권_전결_결재선배선.md](20260822_설계_결재_직권_전결_결재선배선.md) ⬛ | 🔑 **결재 예외 3종 확정 — 사장님 지시 4차 문답으로 전건 확정**. 🔴🔴**실측이 "3건 신규" 를 뒤집었다**: **상신 결재라인**은 `approval_lines`+`approval_line_steps`(단계·직책·사원) **이미 있고**(엔진이 안 읽을 뿐 = 8/12 흐름설계서의 *"표는 있는데 연결 안 됨"*), **대결도 이미 완비**(`delegate_id`·기간·`is_delegated`·`original_approver_id`) ⇒ **실제 신규는 직권·전결 2건 + 배선 1건**. *"3건 신규"* 로 잡으면 자동근퇴 때와 같은 **없는 일 만들기**. 🟢🟢**사장 직권 = 사장님이 상무님보다 나은 답을 내신 자리**(상무 인정: *"내가 최우선이라 해놓고 답을 못 냈던 자리 · 교과서만 읊었지 한국 중소기업이 실제로 뭘 하는지는 못 말했다"*). **반자동 확정** — *"사장이 아무때나 누를수있게"* ⇒ 🔴**자동 판정 금지**(조건으로 막으면 반드시 빈 칸이 생긴다) · 시스템은 **묻기만** 한다. 공석 3사유 **휴가·퇴사·휴직**(상무는 퇴사만 봤는데 사장님이 휴가를 넣으심 — *"퇴사는 어쩌다 한 번인데 부장 휴가는 매달 있다"*). 🟢**Q1 사장님이 PM 권고를 물리심** — PM *"새 칸 신설"* → 사장님 *"델리게이트로"* ⇒ **DDL 변경 0건**(대결·직권 성격 같고 `original_approver_id`+`comment` 로 갈린다). 🟢**Q3 도 PM 안보다 나음** — PM은 *"팝업에서 바로 직권 결재"* 였으나 사장님은 **[Y]=결재설정 화면으로 링크이동**(`/settings/approval` — 결재자·위임결재자 칸 **이미 있음** `:99`·`:101`·`:109`) ⇒ 한 건만 넘기는 게 아니라 **그 자리를 정리**해 뒤에 밀린 것까지 풀린다 · **[N]=상태유지**(다음 로그인 재질의, #43 정신). 🔴**새 화면 0개**(사장님 *"불필요하게 화면을 더 만들지 말고"*) · 알림도 **기존 자리에 얹음**(`Dashboard.razor:216` 5/26 브리핑팝업 · `ChatPage.razor:166` 결재봇 딱지). 🟢**전결 = 고객사가 금액·문서종류 다 설정**(#11 · 기본값 코드에 박기 금지) · 표 **`approval_final_rules`**(사장님 명명 — `delegation` 은 대결과 헷갈려 폐기) · 🟢**금액칸 이미 있음**(`approval_documents.amount decimal(15,2)` DESCRIBE 완료 #13·#4 정합, 상신시점 금액이라 #6 정합) ⇒ **표 신설 1개뿐**. 🔴권한 `APPROVAL_OVERRIDE` **기본 OFF·`EnforcedMenus` 등록 필수**(1.2.74 P0 재발 방지) · 직권도 **`ProcessAsync` 원본반영 경로**(B1 동형 방지) · `approval_history` **INSERT ONLY**(#3) | 2026-08-22 |
| [20260822_김삼성상무_판정_메뉴10개_결재조회축.md](20260822_김삼성상무_판정_메뉴10개_결재조회축.md) ⬛ | ⚖️ **상무 판정 — *"축은 옳다. 8/21 내 지적 절반이 이 안에서 저절로 풀렸다"***. 🟢**핵심 인정**: *"나는 메뉴 **순서**를 얘기했는데 사장님은 **축**을 바꾸셨다 — 급이 다른 얘기"* · *"삼성도 **결재함은 하나**였다. 휴가는 휴가메뉴, 경비는 경비메뉴에서 결재하게 하면 **직원이 결재를 놓친다**. 하루 열 군데 돌게 하면 아무도 안 돈다"*. 🟢**8/21 지적 3건 자동해소**(직원 것이 4번째부터 → 1·4·5·6·7 앞배치 / 휴가 3형제 이름 → 6번 통합 / *"입사퇴사에 뭘 담나"* → **10번이 그 자리**). 🔴**비어있는 3자리**: ①**결재자 퇴사 마비 — 모으면 더 아파진다**(*"흩어져 있으면 하나 막혀도 나머지는 도는데 **모아놓으면 한 사람 퇴사에 결재함 전체가 선다**"*) ②**반려함**(🔴PM 실측으로 사실 확인 — `GetCompleted` 가 `approval_history` 매칭이라 **반려가 완료에 섞인다**) ③**대결·전결 자리 비워두기**. 🟡**급여 = 금액 노출 주의**(*"부장이 부원 휴가를 결재하는 건 당연한데 **부원 연봉을 결재함에서 보는 건 사고**"*) — 🔴**상무가 자기 안을 철회**: 처음 *"급여 결재선 분리"* 를 떠올렸다가 사장님 *"라인은 하나"* 를 듣고 **폐기**(*"라인을 쪼개면 통합이 아니다"*) ⇒ **A안=목록엔 제목만·금액은 8번에서**. 🔴**가장 강조**: 3단계의 마지막 칸(*"→목록에서 상태반영"*)이 **이 설계의 전부**이고 **히트판은 바로 거기서 이미 터졌다**(B1 경비 P0 — 이미 출하) ⇒ **4·6·7·9·10 문서종류마다 `ProcessAsync` 확인**. 🟢**"녹아들어간다" 판정**: *"**빠지는 화면이 0개**다. 8/13에 근태달력을 기간버튼으로, 휴가현황·달력을 탭으로 흡수하며 **기능은 그대로 뒀던** 그 방식"* — ⚠️단 **내린 주소는 리다이렉트로 살려라**(*"즐겨찾기한 직원이 있다. 링크가 죽으면 '기능이 없어졌다'고 느낀다"*). 마지막 말: *"**1번에 모으는 만큼 1번이 막히면 회사가 선다** — 고속도로 만들고 갓길 안 만드는 격"* | 2026-08-22 |
| [20260822_설계_그룹웨어_메뉴10개_확정_결재조회축.md](20260822_설계_그룹웨어_메뉴10개_확정_결재조회축.md) ⬛ | 📐 **그룹웨어 메뉴 17→10 · 결재조회 축**. 사장님 안: 1결재조회 2결재설정 3사원관리 4보고서 5근태 6연차 7휴직 8급여 9경비 10전자근로계약서/사직서. 🔴**축 = 만드는 곳과 결재하는 곳을 나눈다** — *"4,6,7,9,10에서 그리드 목록조회·문서생성 → **결재조회에서 결재** → 목록에서 상태반영"* ⇒ 각 화면은 **결재 버튼을 안 만든다**. 🔴**사장님 3원칙이 잣대**(①라인 하나·창구만 구분·메뉴 최소 ②**컬럼·기능 안 없어짐, 메뉴에 녹아들어감** ③**새 화면 만들지 말고 이미 있는 화면으로 링크**) ⇒ **빠지는 화면 0개**. 🔴🔴**실측이 집계를 두 번 정정** — 현재 **17개**(종전 *"15개"*·*"16개"* 둘 다 틀림: **경비**·**연차부여**·**전자근로계약서** 누락). 🟢**전자근로계약서는 화면 3개가 이미 있다**(`/hr/labor-contracts` 목록·작성·서명) — **메뉴에만 없다** ⇒ 10번은 신규가 아니라 **메뉴에 올리는 것**(8/12 *"계약서는 화면이 없는 게 아니라 안 이어진 것"* 그 자리). 🔴**정말 없는 건 사직서 하나**(화면·문서유형 0 · 지금 퇴사는 사원관리 상태변경뿐). 🔴**경비는 두 군데 있다** — 그룹웨어 `/hr/expense-request`(`hr_expense_requests`, 직원) + 회계 `/accounting/expenses`(`expenses`, 경리) ⇒ **화면만 합치고 표 2개는 그대로**(정본 고르면 한쪽 회사유형이 죽는다 · 사장님 *"케바케"*). 🔴**1번 통합은 메뉴만 합치는 게 아니다** — `ApprovalController.cs:106`·`:119`·`:132` **필터 파라미터 0개** ⇒ 화면·API 양쪽 신규. ⚠️**문서종류 필터를 `DocTypeLabels` 에서 뽑지 마라**(작9 전엔 ERP 7종이 살아 있어 견적·발주가 섞인다) ⇒ **작11→작9→④** 순서. 🟢**단 3·5·6·10 메뉴 흡수는 작9 와 무관 — 지금 가능**. 🚨`GroupwareStage0GuardTests` 가 **사이드바 주소를 단언**하므로 함께 갱신 필수 · 내린 주소는 **리다이렉트**(404 금지) | 2026-08-22 |
| **[20260821_설계_확정권한_전결판정_작11_선후교체.md](20260821_설계_확정권한_전결판정_작11_선후교체.md)** 🆕🟢⚖️ | ⚖️ **PM·CTO 전결 3건 판정** — 사장님 *"전결 루틴대로 진행해"*. 🔴**전결 범위부터 그었다** — 선례 20260820검8 이 한계를 남겼다(*"모바일 근본 병합은 **요금 축이라 전결 범위 제외**"*) ⇒ **요금·가격에 닿으면 전결 아니다**. 이 잣대로 ①선후교체 🟢전결 ②U2 기본값 🟢전결 ③**U1 경비문구 🔴전결 안 함**(사장님 **말**을 PM 이 지어내면 받아쓰기의 반대편)으로 갈랐다. 🟢**전결1 = 작11 신설 · 작9 보다 선행** — 근거는 사장님 지시 자체다(지시 7 *"확정은 암묵적인 결재기능"* ⇒ 확정이 결재를 **대신하려면 결재만큼 통제**돼야 성립). ⇒ 작9 를 미루는 게 아니라 **작9 를 성립시키러 가는 것**. 🔴🔴**전결2 = 내 질문이 반쯤 틀렸다** — 설7 에서 *"기본 OFF 면 회귀"* 라 적고 물으려 했으나 실측(`PermissionService.cs:236-266`·`PermissionFilter.cs`)하니 **이 시스템은 원래 전부 기본 OFF**(행 없으면 `COALESCE(col,0)`→`ForbidResult`) ⇒ *"기본값 OFF 로 할까요"* 는 **이미 정해진 것을 다시 묻는 질문**이었고 사장님 예외처리 축(*"기본 OFF, 고객사가 켬"*)과 **이미 일치**. 🟢**회귀도 갈라야 한다**: 대표(`tenant_admin`)는 **Layer 0 무조건 바이패스라 절대 안 막힘**(1인·소규모 영향 0) · 회귀는 **직원 계정 쓰는 고객사만**. ⇒ 판정 = **기본값을 새로 안 정한다**(기존 규칙 유지) + **마이그로 기존 `sales_manager`·`purchase_manager` 행을 1 로 넣어 회귀 흡수**(*지금 할 수 있던 사람은 계속*). 🔴**단 `sales_user` 는 안 옮긴다** — 그게 확정할 수 있는 것 **자체가 이번에 찾은 결함**이고, 옮기면 *"고쳤는데 아무것도 안 바뀌는"* 봉합이 된다(필요하면 대표가 화면에서 켠다 #11). ⇒ U2 는 **사전 결재가 아니라 사후 보고**(일부 고객사 *"어제 되던 게 안 된다"* 발생 가능). ⚠️**작11 은 권한부여+`EnforcedMenus` 등록+마이그가 한 커밋** — `SALES`·`PURCHASE` 가 현재 등록 9개에 **없어서** 빠뜨리면 **체크박스가 안 뜨는데 서버는 막는 상태**(1.2.74 P0 재발 = 고객이 켤 방법 없음). 게이트 6종 · 🔴**G-1 은 `if(false)` 로 초록불 출처 확인**(이번 세션 2회 뚫린 자리). 코드 **0건** | 2026-08-21 |
| **[20260821_설계_확정API_권한통제_실측_작9착수조건.md](20260821_설계_확정API_권한통제_실측_작9착수조건.md)** 🆕🟢🚨 | 🚨 **작9 착수조건 판정 = 지금 착수하면 안 된다**. 인수인계서 §4-4 가 건 조건(*"확정 API 권한 통제 실측이 먼저"*)을 실측함. 🔴**확정 엔드포인트 5개 중 4개에 개별 권한 검사가 없다** — `sales/deliveries/{id}/confirm` · `sales/deliveries/bulk-confirm` · `purchase/returns/{id}/confirm` · `purchase/receipts/{id}/confirm` **전부 없음**, `sales/returns/{id}/confirm` 만 `Authorize(SalesManager)` 보유. 🔴**클래스 정책(`SalesOnly`·`PurchaseOnly`)은 권한이 아니라 부서 칸막이** — *모듈에 들어올 수 있나*를 가를 뿐 *확정할 수 있나*를 안 가른다 ⇒ 판매 모듈 접근권만 있으면 신입도 거래명세서를 확정, 그 시점에 **원장이 움직인다**(#6). 🔴**5개 중 1개만 다르다는 것이 방증** — 같은 성격 행위에 통제가 제각각이면 **그 1개가 옳고 4개가 빠진 것**. ⇒ **결재가 지금 뒷문을 막고 있다**: 작9 로 7종을 빼면 *확정(통제 약함) + 결재(통제 있음)* 가 *확정(통제 약함)* 만 남아 **막는 게 없어진다**(특히 bulk 는 한 번에 다량). 이건 작9 의 부작용이 아니라 **작9 의 전제가 아직 성립하지 않은 것**. 🟢**건의 = 선후 교체** — 확정 4곳 권한 부여(**작11** 신설 제안) → 그 다음 작9. 🔴**권한명·기본값은 PM 이 안 정한다**(사장님 결재) — 다만 예외처리 축(*"기본 OFF"*)을 그대로 적용하면 **기존 고객이 확정을 못 하는 회귀** ⇒ **U2 질의**. 코드 수정 **0건**(#29·#33·#42). ⚠️화면 실측 미포함 — T-004 테스트 테넌트 **계정 미상** | 2026-08-21 |
| **[20260821_설계_근태축_경비통합_옵션모듈화.md](20260821_설계_근태축_경비통합_옵션모듈화.md)** 🆕🟢 | 🟢 **S5 근태축 + 경비 통합 — 전결 완료**. 🔴🔴**실측이 지시의 절반을 뒤집었다: 자동 근퇴는 이미 있다**(`AuthController.cs:236-258` 로그인 CheckIn · `:313` 로그아웃 CheckOut · 6/22 봉합이 `employee_id` 키까지 통일) ⇒ 사장님 *"그룹웨어 처음 붙일때 얘기했던 내용"* 이 **그때 반영됐다**. 실측 안 했으면 **없는 일을 "만들었다"고 적는 개발명세서**가 나올 뻔했다. 🔴**진짜 구멍은 다른 자리** — `HrController.cs:44-53` 이 `HttpContext.Items["EmployeeId"]` 고정이라 **남을 지목하는 경로 0건** ⇒ 계정 없는 직원은 근태를 남길 방법이 **아예 없다**(그룹웨어 흐름설계서 실측: **사원 12명 중 계정 1명**). 🟢`attendance` 는 `employee_id` 키(계정 아님)라 **토대는 맞고 막는 건 API 하나뿐**. 🔴**예외처리 축 확정(사장님)**: *"권한이 예외. 기본 OFF, 고객사가 켬"* — **경로는 정상 2개**(자동 근퇴 / 대리 입력)로 같은 수준으로 만든다. PM 이 처음 *"예외 경로"* 로 이해한 것을 실측이 반증 — **대리입력이 예외면 92%가 예외**이고, 예외 경로는 덜 다듬어지는데 **제일 많이 쓰는 길이 제일 허술해진다**. 🔴**PM 질문 자체가 틀렸던 자리**: *"계정 없는 직원이 앞으로 계정을 받나"* → 사장님 *"고객사 마음이지 / 우리가 정할게 아님"* ⇒ 우리가 못 정하는 것을 **코드가 이미 전제**(자기 것만 입력)한 것이 이번 결함의 원인. 🔴**권한 칸도 예외** — 5축(view/create/update/delete/export)은 *"내 데이터"* 축, 대리입력은 *"남의 데이터"* 축이라 `update` 에 얹으면 **자기 근태 권한이 남의 근태까지 연다**. ⚠️`PermissionPage.razor.cs:63-85` — 20개 중 서버 강제 8개뿐(1.2.74 P0) ⇒ **`EnforcedMenus` 누락 시 안 먹는 체크박스 재발**. 🔴**경비도 예외 컬럼** — `expenses` 에 `payment_method` **이미 있음**(법인카드·현금·개인카드 `ExpensesPage.razor:38-41`)이고 *"지출결의"* 는 성격이 달라(결제수단 vs 문서절차) 섞으면 *"법인카드로 낸 지출결의"* 표현 불가. **표는 안 합침**(사장님 *"화면만 합치고"*) · 옵션값은 `labor_policy_settings` 방식으로 **설정표에서 읽어 모듈화**(코드 `switch` 금지). ⚠️**U1 옵션 문구만 미확정** | 2026-08-21 || **[20260821_설계_그룹웨어결재_범위확정_ERP분리_회계기표.md](20260821_설계_그룹웨어결재_범위확정_ERP분리_회계기표.md)** 🆕🟢 | 🟢 **결재 범위 확정 — 사장님 오더 12항**. 🔴**축 2개로 갈림: ERP=확정이 곧 승인(권한 통제) / 그룹웨어=결재함(결재선 통제). 예외 0개**. 🔴🔴**지시 7 이 설계 근거를 바꿨다** — *"판매확정·구매확정은 암묵적인 결재기능"* ⇒ 트리거 4곳이 **전부 확정 시점**(`SalesService.cs:588`·`:2128`·`PurchaseService.cs:484`·`:1160`)이라 **기능 제거가 아니라 중복 승인 제거**다(PM 이 처음 *"결재를 뺀다"* 로 브리핑한 것이 오독 — 승인은 확정 메뉴에 그대로 남는다). ⇒ 결재함 = **그룹웨어 8종**(경비·휴가·휴직·초과근무·보고서4종) · **2단 필터 불요**(상무님 안은 15종 전제라 자연 폐기) ⇒ **S4 해소**. 🔴**PM 자기반증 기록**: *"발주 결재 원하는 고객"* 리스크를 남기려 한 것이 **SI 사고**(지시 6)이자 **잣대 불일치**(상무님의 *"월 300건이면 일괄승인"* 판정을 건수 더 많은 영업·구매엔 미적용) ⇒ 항목 삭제. 🔴**지시 11 이 회계를 교정**: *"사람이 확정 후 기표"* 를 PM 이 금액까지 수동으로 브리핑 ⇒ 실제는 **시점만 사람 · 금액·계정·거래처는 자동**(*"안그러면 데이터 틀어지니"*) = 반자동 원칙의 정확한 모습. 모듈화 = 승인이 **회계를 직접 호출 안 함**(기표 대기만) ⇒ 나중 자동화 때 **바뀌는 건 확정 주체 하나뿐**. ⚠️**실측 전수**: `ProcessAsync` 원본 반영이 `leave`·`absence`·`overtime`·`report_*` 는 있고 🔴**`expense` 만 없다 = 이미 출하**(B1 이 회계 연결 **선행**) · 필터 파라미터 **0개**(`ApprovalController.cs:106`·`:119`·`:132`) · 문서유형 **15종** · 세금계산서는 **원래 목록에 없다**. 🔴**지시 9 로 ERP 제거는 작9 로 분리**(그룹웨어 지시서에 넣은 것이 월권). ⚠️**④ 필터를 `DocTypeLabels` 에서 뽑지 마라** — 작9 이전엔 ERP 7종이 살아 있다. 🔴**S5 미결**: 근퇴 = 출퇴근 기록(결재 아님) vs 연장·조퇴·외출 신청(결재 맞음) | 2026-08-21 |

| **[20260820_설계1_우편번호API_CSP_frame차단_설계.md](20260820_설계1_우편번호API_CSP_frame차단_설계.md)** 🆕🟢 | 🟢 **소·원인확정**. 사장님 *"우편번호API 연결 끊어짐"*. **코드는 멀쩡**(스크립트·interop·버튼·콜백 4층 전부 정상) — 🔴**진범 = CSP 에 `frame-src` 누락**(`Program.cs:502`). 6/17 *"CSP 3종 정합"* 봉합이 **script-src 만 넣고 frame-src 를 빠뜨림** ⇒ 카카오 우편번호는 **iframe** 이라 `default-src 'self'` 상속으로 차단. 스크립트는 받아지고 버튼도 눌리는데 **틀만 안 뜬다**. 🔴🔴**CSP 가 `if (!isDevelopment)` 안에만 있어 개발PC 에선 구조적으로 항상 통과** — *"개발PC에서 됩니다"* 무의미(`feedback_dev_pc_proves_nothing`). ⚠️영향 = 업체등록 **+ 사업장정보**(`UserInfoPage.razor:167`) **둘 다 죽어 있음**. 게이트 6 — G-1 봉합제거 FAIL · G-2 **Production 실측만 인정** · G-5 열리는 것 ≠ 값이 오는 것. 보안매니저 결재(헤더 완화) | 2026-08-20 |
| **[20260820_설계2_업체특별단가_판매미연결_설계.md](20260820_설계2_업체특별단가_판매미연결_설계.md)** 🆕🟢 | 🟢 **설계 확정 · 결재 대기 0 · 착수 가능**(사장님 3차 확정). 조사: 화면·API·DB 는 정상인데 **`partner_special_prices` 를 읽는 곳이 자기 화면뿐** — 견적·수주·명세서 어디도 안 부름(**"고쳤다≠갔다"** 계통). 🔴🔴**설계가 두 번 폐기되고 단순해졌다**: ①PM 초안 *"업체→상품→기본 고정 우선순위 자동적용"* ❌ ②*"두 축 독립 + 필터 선택"* ❌ ⇒ 🟢**본안 = 자동 적용을 안 한다. 단가는 사람이 직접 치고, 커서 올리면 말풍선으로 근거만 보여준다**(업체특별단가·최종단가·표준단가·상품특별단가 4값, 고르면 들어감). 🟢**사장님이 문제 자체를 없애셨다** — 정하지 않으니 우선순위도·필터도·*"곱하나 더하나"*(돈 갈리던 질문)도 **전부 소멸**. ⚠️*"업체 특별단가 우선"* = **자동 적용 아니라 말풍선 표시 순서**(자동 적용하면 사장님 3) 위반). ⚠️상품특별단가는 *"존재 자체가 큰 의미 없다"*(사장님) — **지우진 않되 위에 새 기능 얹지 마라**(#1·#37). 범위 **6화면 전부**(발주·매입·반품·견적·수주·판매). 🔴**G-4 가 수문장 = 화면 열었을 때 단가칸이 저 혼자 채워지면 FAIL** · G-8 값 없을 때 **0원 표시 금지**. ⚠️최종단가 정의만 조사 | 2026-08-20 |
| **[20260820_설계3_업체별원장_회계경비_미연결_설계.md](20260820_설계3_업체별원장_회계경비_미연결_설계.md)** 🆕🔴🔴 | 🔴 **대 · 결재2+조사6 대기**. 사장님 *"수불부는 **회계, 재고 연결 축 제대로 점검**할 것"* ⇒ **DB 실측**(SELECT 만·#39). 🔴🔴**결론: 재고와 회계가 한 건도 안 이어진다 — JOIN 결과 0건(양방향 2회 확인)**. 실측: `stock_ledger` **115,460행**(partner_id **115,391 🟢**) vs `journal_lines` **23,994행**(partner_id **0 🔴**) · 문서수 113,306 vs 5,832. 🔴**진범 = `source_id` 형식이 서로 다르다** — 재고 `mig-00000000-0` / 회계 `mig-docf7-20000401-0000` ⇒ **애초에 만나도록 안 만들어졌다. 화면·쿼리로는 절대 못 푼다**. 마이그 두 자리(`MdbMigrationService.cs:4999` **`PartnerId=null` 하드코딩** + source_id 규칙 불일치). 🟢**실거래 경로는 같은 문서ID 를 양쪽에 쓴다**(`SalesService.cs:508`·`:514`) ⇒ 앞으로 것은 이어질 것 — ⚠️**단 demo 가 전부 migration 이라 이 경로 미실행, 코드로만 확인**. ⚠️추가발견: `stock_ledger` INSERT **10곳 중 3곳이 partner_id 미기입** · 🔴**source_id 인덱스 없어 JOIN 5분+**(화면이 안 뜬다·선행). 🔴**실거래 1건 검증이 먼저** — 그 전엔 범위 확정 불가. 선행 = 경비→회계 | 2026-08-20 |
| **[20260820_모바일트랙_반응형UI_메신저_기기병합_범위서.md](20260820_모바일트랙_반응형UI_메신저_기기병합_범위서.md)** 🆕🟡 | 🟡 **범위만 잡음 — 착수 미정**. 🟢**성격(사장님 정의)**: *"기기인증 수정하며 **모바일 인증 시스템 축을 세운건 기본적 설계구조를 짰던 것**이고, 그 축에 **출발점 바꾸고 수정·보완**"* ⇒ **백지 설계 아니다.** 8월에 세운 축(**입구=QR 하나** · **QR도 pending 승인**(8/16 전결로 뒤집힌 결정) · **4자리 눈대조**(`DeviceConfirmCode` — 비밀번호 아님) · **저장은 세밀 과금은 단순**(tablet 저장·요금칸은 mobile 과 한 칸) · **PC가 QR문으로 못 들어옴** · **폐기=대기 회귀**)를 **걷어내지 마라 — 대부분 한 번 뒤집혔다 되돌아온 자리다**. 🔴**순서 = *"PC버젼부터. 모바일은 나중에 몰아서!!!"*** (⚠️초판이 *"반응형 UI부터 착수"* 로 **왜곡 — 정정함**). 담는 것 4(**순서 아님**): ①반응형 UI 전반 ②**히트판 메신저**(범위 미확정 — 구술 필요 · 그룹웨어 13화면 겹침 여부 김삼성 상무) ③**모바일 근본 병합**(8/20 실측 ❌ **슬롯 2개** — 열쇠 `device_id`·`fingerprint` **둘 다 브라우저 저장소 안**이라 사파리·크롬·홈아이콘이 갈린다. PC는 `IsLocalConsole` 이라는 **브라우저 바깥 사실**로 풀렸으나 폰엔 그 자리가 **없다** ⇒ **새 축 필요 · 요금축이라 결재 전 금지**) ④등록화면 ⚠️경고문 제거(③과 한 몸). **선행조건 0번 = PC버전 완료**. ⚠️[기기관문 설계](../보안/20260820_기기관문_로그인앞_이동_설계.md)는 **이 트랙을 안 기다린다**(PC·공용) — 슬롯 2개를 **부수적으로 누르나 근본 아님**. *"관문 했으니 모바일 됐다"* 로 읽으면 ③이 묻힌다 | 2026-08-20 |
| **[20260819_설계서_메인PC_판정축_재설계.md](20260819_설계서_메인PC_판정축_재설계.md)** 🆕🔴🚨 | 🔴 **3차 · 결재 대기 — 코드 금지**. P0(8/18 인수인계서 §4-1). 🔴**실측**: 판정식 **3곳**(`AuthController.cs:136`·`DeviceController.cs:106`·`:509`)이 *터널헤더 없음+loopback* 을 보는데 사장님은 `test1.hitpan.kr` 로 여시므로 **전부 거짓**. 조건을 빼면 8/10 사고(터널이 안에서 localhost 재호출 ⇒ **항상 참**) 재발 ⇒ **접속 경로로는 못 푼다**. 🔴🔴**설계가 세 번 갈아엎혔고 셋 다 코드 짜기 전 문서에서 죽었다**: ①**1차(디스크 표식) 폐기** — 사장님 *"테스트1으로 하고 있어"*(터널만) ⇒ 발급 경로를 **아무도 안 지남**. ②**2차 D-1(서버 관문에 대표 예외) 폐기** — 🔴**[3-V] 치명1: 이미 있다**(`DeviceAuthMiddleware.cs:225-231` + `:38-41` Bypass 에 `/api/devices`) ⇒ 짜면 **동작 0 변화**이고 G-E 는 옛 예외로 **PASS = 가짜게이트 10번째**. ③**치명2: 진범은 화면** — `MainLayout.razor:67` 이 `@Body` 를 통째로 안 그리고 `DeviceAuthGate.razor:147` 은 **계정 종류를 안 본다** ⇒ 주소 직접 쳐도 승인 화면이 안 나온다(**API 는 열려 있는데 부를 화면이 없다**). ⇒ 🟢**본안 = 관문이 자기 안에서 승인을 끝낸다** — 관문 밖으로 **나갈 필요 자체를 없앤다**. `_gateActive` **무변경**(G-C 가 수문장) · `MainLayout`·미들웨어·라우팅·주소대조 **전부 무변경** · 새 API·DDL **0건** · 기존 3콜 재사용(`DeviceManagePage.razor:418-420`·`:470`). 대표 판정은 **서버가 JWT 클레임으로**(#2), 화면 값은 표시용(**G-G** 가 위조 시험). ⚠️**[3-V] 중5: `tenant_admin` 0줄 가능**(출하 DDL 시드 0건 + `TenantsController.cs:21` 사고 이력) ⇒ **자동 승격 안 함**(#11), 0줄이면 **강제경로 안내**(G-H). ⚠️**중6: 표식 탈취** — 누가 `localhost` 로 한 번 열면 옛 봉합이 `is_main_pc` 를 가져감 ⇒ E 는 그 축을 **안 쓰므로 P0 무관**, **D-2 선행조건 이월**. 🔴🔴**[3-V] 2회차가 3차안(E)도 반증** — **승인은 되는데 열쇠가 주인에게 안 간다**: `setAuthKey` 가 **수동입력 칸에만** 있고(`DeviceAuthGate.razor:343`) 승인 경로엔 **0건**, 업무 API 는 `auth_key_hash` 대조(`DeviceAuthMiddleware.cs:128-138`) ⇒ **관문 풀려도 전 화면 403**. 🔴그 403 은 **버그가 아니라 8/18 봉합의 의도**(대표 예외를 `/api/devices`·`/api/auth` 로 일부러 좁힘 `:207-224`) ⇒ **우회로 없음**. 또 `_gateActive` 는 `_awaitingApproval` 의 **파생값**이라 *관문 유지*와 *관문 해제*가 **양립 불가**. ⇒ 🟢**4차 본안 F** — **열쇠를 사람 눈 경유 없이 화면이 즉시 보관**(F-1) · `ConfirmCode` 로 자기 줄 특정(F-2 · 메인PC는 줄이 둘) · **제3 상태로 *승인→보관→해제* 순서**(F-3) · 재클릭 잠금(F-4 · 멱등이라 두 번째는 키가 `null`). §5 게이트 **12종** — 🔴**G-J(승인 직후 업무 API 200)가 진짜 수문장** · 실측에 **실2′(실제로 조회·저장 되나)** 추가. 🔴🔴**[3-V] 3회차가 F 도 반증 — 그러나 진짜 자리를 찾았다**: 막고 있던 것은 열쇠가 아니라 `DeviceAuthMiddleware.cs:183` 의 **`AND is_main_pc = 1`**. 대표 브라우저 줄은 `is_main_pc=0`(표식은 서버 줄에만 · `TenantDeviceService.cs:245`)이고 그 표식을 옮기는 유일한 길이 `IsLocalConsole` 이라 **터널이면 절대 거짓** ⇒ **승인돼도 업무 API 403**. 🔴**다섯 안(A·B·D·E·F)이 전부 이 조건을 안 건드리고 브라우저 쪽 우회로만 쌓았다.** ⇒ 🟢**5차 본안 G** — 판정문을 **`is_main_pc=1` OR `user_id=지금 로그인한 사람`** 으로 넓힌다(`UserId` 는 `TenantMiddleware.cs:70` 이 이미 채움, 기기줄도 `user_id` 보유). `device_id` 가 **비밀이 아닌 것**이 8/18 사고의 원인이었으므로 **비밀 아닌 값 하나로 판정하지 않는다** — 번호를 주워도 **그 줄의 주인(JWT)** 이어야 통과 ⇒ **좁힘이 오히려 더 강해진다**. ⇒ F-1·F-3·F-5 **폐기**(불필요), **F-2 만 생존**(3회차 통과). 🔴🔴**[3-V] 4회차가 G 도 반려**: `user_id` 는 **소유 축이 아니다** — `:674` INSERT 때만 채우고 **갱신(`:508-524`)은 안 건드려 "처음 만든 사람"에 고정**(출하 DDL 주석 `'최근 로그인 사용자'` 가 **코드와 어긋나 PM 을 속였다** — 별건 정정 필요). ⇒ **직원이 그 브라우저를 먼저 열었으면 대표가 그대로 갇힌다**(P0 미해결) + **공유계정 하나로 미승인 기기 무제한 통과**(과금 붕괴) ⇒ 🔴**설계서의 *"좁힘이 더 강해진다"* 는 거짓 — 집합이 순증**(정정 완료). ⇒ 🟢**6차 본안 H = 열쇠 축 복귀** — F 의 문제의식은 옳았고 **구현 세 곳이 틀렸다**: ①**자기 줄(`ConfirmCode`)일 때만** 열쇠 보관(아니면 대표 열쇠를 덮어씀) ②**제3 상태 안 만듦**(`reload` 넘는 건 `localStorage` 뿐) ③**락 분리가 아니라 승인 중 `StopPolling`**. 🟢**미들웨어 무변경** — 열쇠 축(`:128-138`)은 이미 `is_main_pc` 를 안 본다. ⚠️**H 는 열쇠 도용을 안 막는다**(키 대조에 `device_id` 없음) — *"고쳤다"* 로 적지 않고 **보안상무 판정 요청**. 🟢🟢**[3-V] 5회차가 H 를 반려하고 I 를 권고**(PM 원점 재검토안과 일치). H 반려 사유: `ConfirmCode` 는 `device_id` 를 **4자리로 압축**한 값이라 20대면 충돌 **2%**·100대면 **39%**(더 단순한 `device_id` 를 두고 정확도를 버림) + 🔴**관문 안에서 직원 폰을 승인하면 키 표시 UI 가 관문 뒤라 원문이 소멸** ⇒ 그 직원 **영구 인증 불가 = 새 P0**. ⇒ 🟢**7차 본안 I = 원인을 다룬다**: 8/18 주석(`:643-649`)이 이미 *"승인해 줄 사람이 자기인데 그 화면에 못 들어간다"* 로 **원칙을 세워 뒀고, 틀린 것은 시험 방법(`IsLocalConsole`)뿐**이었다. **I-1** `:657` 판정에 `!IsOwnerAccount` 추가(서버가 JWT 로 채움) + **I-2** 갱신 경로에도 같은 판정(**없으면 이미 갇힌 대표가 재로그인해도 영원히 pending** — `:508-524` 에 `status` 없음). 🟢**요금은 오히려 더 엄격**(한도검사가 앞 · approved 는 즉시 계수). 🟢**관문·미들웨어·화면 전부 무변경** ⇒ H 의 위험 5건이 **발생 자체에서 사라진다**. ⚠️**유일한 실질 위험 = 대표가 여럿일 수 있다**(`UserService.cs:113` · 우리 코드 `:1125` 가 이미 *"대표가 여럿일 수 있고"* 라 적어 뒀다) ⇒ 축을 `is_parent=1`(#40)로 좁힐지 **사장님 결재**. 🔴🔴**[3-V] 6회차가 I 도 반려**(검증팀 자기 권고 철회) — **`status='approved'` 는 문을 열지 않는다**(축은 `auth_key_hash`·`is_main_pc=1` 둘뿐 · 코드 주석 `:1517-1531` 이 이미 적어 뒀는데 PM·검증팀이 동시에 안 읽음). ⇒ 🟢🟢**11차 본안 K(§3-10) = 소거를 교체로**: 🔴**새 발견 K-0 — 8/18 두 봉합(1회용 소거 + 축② `is_main_pc=1` 좁힘)이 합쳐져 **인증 완료한 직원 기기도 매 요청 통행로가 없다**(잠재 P0 · 스위치 `Enabled:true` 켜짐 · 소스 확정/런타임 미확정). **K-1** verify 성공 시 `auth_key_hash` 를 소거 대신 **기계비밀로 교체**(사람이 본 키는 지금처럼 죽는다 = 결재 4 의도 유지·자구 수정 결재 ⑪) · **K-2** 화면이 기계비밀 보관 · **K-3** 축①에 `AND device_id` 결합(8/18 *"다음 차수 몫"* 이행) · **K-4** 관문 안 대표 자기승인(자기 줄 = `device_id` **직접** 대조 · verify-key **자동 투입** = 직원과 같은 문 · readback 후 reload). 🟢**G-43 무접촉**(등록은 여전히 pending · 대표의 명시적 승인 행위 존재) · 🟢**요금 문지기 유지**(승인 버튼 = 문지기) ⇒ §3-9 의 *"승인 없이 열 것인가"* 질문이 **해소** — *승인 없이 열지 않는다. 승인을 그 자리에서 하게 한다*. [3-V] 7회차(K 반증 + K-0 런타임 확인) 진행 중. §5-1-1 **로컬 게이트 함정**(사장님 *"로컬로 두고 테스트를 하면 당연히 안되지"*) | 2026-08-19 |
| **[고용형태별_노무판정_법령조사_20260812.md](고용형태별_노무판정_법령조사_20260812.md)** 🆕🔴⚖️ | ⚖️ **고용형태별 연차·퇴직금·4대보험 법령 명세** — 사장님 지시 *"정직원이냐, 알바냐, 계약직이냐, 무기계약직이냐에 따라서도 달라짐"*. 🔴**대전제: 4개 다 법률 용어가 아니다** — 법은 ①계약기간 ②주당 소정근로시간 두 축으로 판정하며 **주 15시간**이 연차·주휴·퇴직금을 가르는 최대 분기점(근기법 §18③ = §55·§60 **조 단위 배제**). 🔴**그 숫자를 담을 컬럼이 우리 DB에 없다** ⇒ `emp_type` 외 **최소 5필드 추가 필요**(주소정시간·통상근로자시간·상시근로자수·요일별시간·시간단위연차). 🔴**비례연차는 "일"이 아니라 "시간" 단위**(시행령 별표2 제4호나목, 1시간 미만 절상)인데 현재 `annual_leave_total decimal(5,1)` 은 일 단위라 **담을 자리가 없다**. 🔴**1년 계약직 연차=11일**(대법 2021다227100, 15일은 366일째 존속 시 발생) · **일용직도 계속근로면 연차·퇴직금 발생**(대법 93다26168, 근기법에 일용직 정의 자체가 없음) · **5인 미만은 고용형태 무관 연차·가산수당 0**(§11) · **단시간 초과분은 40h 미만이어도 50% 가산**(기간제법 §6③). 🔴**완전 자동판정 불가** — 계속근로 단절은 "상당한 이유" 규범평가(대법 2009다35040) ⇒ **반자동(제안→사람확정)**, 판정 불가 시 **주지도 빼지도 말고 멈춘다**(과소=체불 형사처벌·과다=근로조건 고착). 🔴**실측**: `contract_type` 4값(`regular/contract/part_time/daily`)이 **법과 어긋난다** — `part_time` 이 단시간/초단시간(연차 유/무)을 못 가르고, `contract`↔`part_time` 이 배타적으로 놓였는데 **실제로는 겹치며**, `daily` 에 배제를 연결하면 위법 ⇒ **판정 근거로 쓰면 안 됨**(표시용 존치). `EmployeeType` enum 은 **파싱 실패 시 Regular 폴백**(과거 P0 봉합)이라 새 값 추가 시 조용히 흡수됨. §Part4 차별금지(기간제법 §8, 제척기간 6개월) + **⚠️확인필요 10건** | 2026-08-12 |
| **[20260812_그룹웨어_워크플로우_흐름설계서_전수조사기반.md](20260812_그룹웨어_워크플로우_흐름설계서_전수조사기반.md)** 🆕🟢🔴 | 🟢 **그룹웨어 설계 5종의 기준문서 — 이것부터 읽어라**. 사장님 오더 *"워크플로우 흐름 잡고 설계수준의 조립과정, ERP 부록수준이 아닌 탄탄한 그룹웨어 (코드금지)"*. 전수조사 3건 실측 기반. 🔴**축 확정 = 사원(`employees`)**(§3-5) — 그룹웨어 8표 중 7표가 이미 사원을 가리키고, 실측 사원 12명 중 계정 보유 1명이라 계정을 축으로 잡으면 11명이 사라진다. **단 메신저·알림은 계정 기준**(사장님 결재 *"물리적으로 그럴 수밖에"*). 🔴**8/11 문서 전제 3건 정정**(§2): 직급 결재선 테이블이 **엔진에 연결 안 됨**(용도분리 아니라 연결 문제) · 계약서는 화면이 없는 게 아니라 **안 이어진 것** · **연차는 이미 정합(6/23 봉합) 손대지 말 것**. 🔴**P0 3건**: 결재선 2중화 · 경비 막다른길 · 계약서 단절. **숨은 7화면**(초과근무·부서관리·캘린더2·현황2·직급) — 그룹웨어는 13화면이 아니라 **20화면**. §5 조립순서 9단계 | 2026-08-12 |
| **[20260812_PRD_그룹웨어.md](20260812_PRD_그룹웨어.md)** 🆕🔴 | 🔴 **제품 정의** — *"부록 수준 vs 탄탄한 제품"* 을 **끊긴 자리의 수**로 판정. 사용자 4역(직원·팀장·인사담당자·대표) + **계정 없는 사원 11명도 사용자**. 범위: A보이게·B잇기·C짓기·D고치기. **하지 않는 것 명시**(OCR·급여계산·자연어AI·결재엔진 재작성). 핵심결정 5: 축=사원 / 입구는 여럿 흐름은 하나 / 결재선 직급 / 돈은 승인시점에만 / 🔴**AI챗봇과 메신저 절대 분리**(사내대화 외부유출 경로). 성공기준=흐름·정직·실측 | 2026-08-12 |
| **[20260812_요구사항정의서_그룹웨어.md](20260812_요구사항정의서_그룹웨어.md)** 🆕🔴 | 🔴 **개발팀이 작업지시서를 받는 번호 체계** `GW-NNN`. P0 4건(결재선정본·경비→회계·계약서→사원·**연차 법정기준**) / 보이게 3 / 잇기 4 / 짓기 4(보고서·영수증·급여·메신저) / **고치기 8**(🔴급여 0원·무효서명 "유효"·**무효화 버튼이 아무것도 안 함**) / 토대 3(부서축·양방향·서명축). §10 **미결 6건이 무엇을 막는지** 표 | 2026-08-12 |
| **[20260812_기능정의서_그룹웨어.md](20260812_기능정의서_그룹웨어.md)** 🆕🔴 | 🔴 **화면 27개 명세**(기존20+신규7). 보고서4종 공통뼈대·급여명세서(관리자발급/직원열람)·**메신저 4단계**(결재알림→1:1전체→부서방→**채팅창 양식작성→결재상신**). 팝업 별도창 배치도. **"되는 척" 3건 최우선**. 공통 정직규칙(조회실패≠0건·안되면 버튼 제거). §5 화면전이도(물흐르듯) · §6 착수순서 9단계 | 2026-08-12 |
| **[20260812_ERD_그룹웨어.md](20260812_ERD_그룹웨어.md)** 🆕🔴 | 🔴 **데이터 설계**. 전체 관계도 + 기존 14표 실측. 구조적 결함 3: 결재선 2중화 · 부서축 2갈래(마스터 **0행**인데 자유문자 10건) · 🆕**직급도 같은 문제 — `employees.position_id` 가 없다**(직급 결재선의 숨은 선행조건). 신규 8표(reports·payslips2·영수증·notifications·메신저4) · ALTER 6건(**추가만**, #1·#37) · **마이그 DB-92~97 계획**(멱등+시드등록+올바른폴더). FK 정책 결재 필요 | 2026-08-12 |
| **[20260810_PRD_사업자정보_수집보관_체인.md](20260810_PRD_사업자정보_수집보관_체인.md)** 🆕🔴 | 🟢 **사업자정보 수집·보관·반영 PRD** — 등록증 1장으로 기본사업장정보가 **자동으로 써지게** 한다(사장님 의도). 무단사용 차단 **2중 관문**(①가입 국세청API ②설치후 OCR잠금). ⚠️**신규 설계 아님** — 랜딩 설계에 `is_match`·약관 4건이 이미 있는데 코드가 미이행. 🔴검증팀 반증 5건 반영(§6-5) | 2026-08-10 |
| **[20260811_그룹웨어_HR_워크플로우_흐름재설계_김삼성상무_조언.md](20260811_그룹웨어_HR_워크플로우_흐름재설계_김삼성상무_조언.md)** 🆕🔴👤 | 🔴 **그룹웨어·HR 흐름 재설계 — 착수 전 필독**. ⭐사장님이 그은 선: *"**기능은 우리가 이미 구현했어.** 다만 **이 구현의 흐름을 어떻게 잡고 갈지**가 중요"* + *"**HR·인사·그룹웨어는 직원계정으로부터 모든 연결이 돼!!!!**"* ⇒ 갈아엎지 않고 **13화면을 뿌리 기준으로 재점검**(실측: 그룹웨어 13화면·결재테이블 7개 이미 가동, **사내 메신저만 0건=유일한 신규**). 👤**김삼성 상무(삼성인재개발원장) 추대** — 부품은 다 있고 **조립 순서를 잡는 분**. 🔴**구조 결함**: `employees.user_id` 가 **NULL 허용**이라 화살표가 거꾸로다(계정이 뿌리가 아니라 사원에 딸린 선택항목) ⇒ **계정 없는 사원이 결재선에 들어가 로그인 수단이 없어 결재가 멈춘다**(헌법 #20 정면). ⚠️**어제 챕터3 은 화면 이동만 연결·데이터 축은 안 건드렸다**. 🔴🔴**사장님이 짚은 결정적 지점 — MDB 마이그레이션 직원**: 레거시에서 온 직원은 계정이 없다 ⇒ *"계정 없는 사원 금지"* 로 막으면 **50명이 아예 안 들어오고**, 합격선(*"레거시와 숫자가 맞는다"*)이 깨진다 ⇒ **막지 않는다로 확정**. ✅**양방향 확정**: *"계정추가→직원등록 도 되야 하지만 **역으로 직원등록→계정등록도 되야 정합**"* (한 방향만 허용하면 마이그가 막다른 길). ⇒ 핵심전환=**"허용하느냐"가 아니라 "미완성 상태를 시스템이 아느냐"** (지금 `user_id=NULL` 은 조용하다) ⇒ 양쪽 연결버튼 + **"계정 미발급 12명"** 표시. ✅**계정 발급 수동 확정**: *"**수동!!!** 그 계정을 사용하고자 하는 직원이 결정해야지"* — 자동이었으면 따라왔을 **초기비번 전달경로 신설·안 쓰는 계정 방치·약관 동의 주체 부재**(첫로그인 필수동의인데 본인이 동의한 적 없는 계정) 3건이 전부 사라진다. 김삼성 조언 4건: **인사 6단계**(계정→신분→**계약**→일상→신청→결재) · 🔴**③계약이 빠져 ②→④ 로 건너뜀 = 입사일 없이 연차 계산**(법정 1년미만 월1개/이상 15개 ⇒ **노동법 문제**, 법무팀장 교차확인) · 🔴**결재선은 직급으로**(사람으로 짜면 *"퇴사하는 날 회사 모든 결재가 멈춘다"*, 컬럼은 #1·#37 로 둘 다 유지하고 **용도를 나눔**) · **메신저는 결재알림부터**(채팅 먼저면 잡담방). ⬜결재 남은 것: 결재선 직급전환 · 입사일→연차 자동계산 · 메신저 순서 · 체크박스 일괄발급 UI. 🔴**기존 결재 데이터 보존(#1)** 마이그 설계 동반 필요 | 2026-08-11 |
| **[20260810_디자인명세서_기본사업장정보_화면.md](20260810_디자인명세서_기본사업장정보_화면.md)** 🆕✅📐 | ✅ **안 A 확정본 — 구현자가 이것만 보고 만든다**. 색상은 기존 `hitpan.css:755` 토큰 재사용(신규 정의 금지) · 6행 유지 · 폭 합계 12 · **`ReadOnly` 확정**(`Disabled` 는 글자가 흐려지고 복사 불가) · 버튼/머리글/CSS 사양 · 🔴문구 정정 2건 · 완료판정 8항 | 2026-08-10 |
| **[20260810_디자인안_기본사업장정보_화면.md](20260810_디자인안_기본사업장정보_화면.md)** 🆕✅🎨 | ✅ **사장님 결재 = 안 A(구분선 분리형)**. 🔒자동(위)/⬜입력(아래) 분리·헬퍼텍스트 제거·`ReadOnly` 확정. 🔴문구 2건 정정(`:26` 3개 명시가 거짓 됨 · `"랜딩"`은 내부 이름) | 2026-08-10 |
| **[20260810_법무검토_사업자정보_현행법_경쟁사비교.md](20260810_법무검토_사업자정보_현행법_경쟁사비교.md)** 🆕⚖️ | **현행법 + 경쟁사 4곳 비교**. 🟢사장님 안이 업계보다 타이트(등록증 보관 명시 0건). 🔴**보유기간 5년 강제**(국세기본법 제85조의3·부가세법 제71조) — 현행 약관 "탈퇴 즉시 파기"와 충돌. ⭐#18·#22 법적 근거 확보. ⚠️위하고·SERP 미확인 | 2026-08-10 |
| [WATCHDOG_WS28_SKELETON.md](WATCHDOG_WS28_SKELETON.md) | 워치독 WS-28-A~I 9단계 — C# Windows Service 의사코드 골격 | 2026-07-28 |
| [VELOPACK_PHASE2_INTEGRATION_PLAN.md](VELOPACK_PHASE2_INTEGRATION_PLAN.md) | Velopack 자동 업데이트 Phase 2 통합 계획 | 2026-07-23 |
| [THREE_SYSTEM_ARCHITECTURE.md](THREE_SYSTEM_ARCHITECTURE.md) | 히트판 3개 시스템 아키텍처 설계 | 2026-07-23 |
| [TENANT_ID_NULL_POLICY.md](TENANT_ID_NULL_POLICY.md) | tenant_id NULL 허용 정책 | 2026-07-23 |
| [TABLE_SPEC.md](TABLE_SPEC.md) | 히트판 백오피스 — 테이블명세서 | 2026-07-23 |
| [STATE_MACHINE_SUBSCRIPTION.md](STATE_MACHINE_SUBSCRIPTION.md) | 상태 머신 — Tenant / Subscription / Payment | 2026-07-23 |
| [SEQUENCE_3SYSTEMS.md](SEQUENCE_3SYSTEMS.md) | 3시스템 시퀀스 다이어그램 (랜딩 · 백오피스 · ERP) | 2026-07-23 |
| [SCREEN_SPEC.md](SCREEN_SPEC.md) | 히트판 백오피스 — 화면정의서 | 2026-07-23 |
| [SCENARIO_20_VERIFY_SKELETON.md](SCENARIO_20_VERIFY_SKELETON.md) | 20개 강제 시나리오 검증 스크립트 골격 | 2026-07-23 |
| [PRD_THREE_SYSTEMS.md](PRD_THREE_SYSTEMS.md) | 히트판 PRD — 3개 시스템 제품 요구사항 정의서 | 2026-07-23 |
| [PRD_ERP.md](PRD_ERP.md) | 히트판 ERP 상세 PRD | 2026-07-23 |
| [PAYMENT_INTERFACE.md](PAYMENT_INTERFACE.md) | 결제 어댑터 인터페이스 명세 | 2026-07-23 |
